#FeMe Roadmap : Finishing the Design, and Growing End - to - End Coverage

## What this document is

[Design.md](Design.md), [FeMeCPUDesign.md](FeMeCPUDesign.md),
[FeMeGraphicsDesign.md](FeMeGraphicsDesign.md),
[FeMeVulkanDesign.md](FeMeVulkanDesign.md) and
[FeMeWARPDesign.md](FeMeWARPDesign.md) each carry their own "Roadmap /
Milestones" section, and each records, inline, which of its milestones are
implemented and how each implementation narrowed the design (the
"Status"/"Deviation" notes). Read together they describe *what FeMe is*, but
none answers the two questions that matter for planning the next stretch of
work:

1. What is left, across both documents, and in what order should it be done?
2. FeMe now executes shaders (`feme-run`, the CPU target). What should it be
   executing that it isn't, so that the next feature to land is caught by a
   test rather than by a user?

This document answers those two questions. It does not restate any design;
every item below cites the section of the design document that owns the
decision, and those documents remain authoritative for *how* a thing should
work. This one is only about *what is missing* and *what order*.

Priorities are relative, not scheduled:

- **P0** — blocks a claim the design already makes, or blocks a milestone
  that is otherwise "done".
- **P1** — needed for the design's stated v1 scope, but nothing currently
  landed depends on it.
- **P2** — genuinely later; listed so it isn't rediscovered as a surprise.

### The two tracks

Everything through §1.7 and R1–R15 is the *retargeting and compute
execution* track: FeMe as a library that imports DXIL/SPIR-V/DXBC, retargets
it, and executes compute shaders on the CPU. That track is the one every
landed milestone belongs to.

§1.8–§1.10 and R16 onward are the *graphics and API runtime* track added by
FeMeGraphicsDesign.md, FeMeVulkanDesign.md and FeMeWARPDesign.md. Nothing in
it exists in tree today — there is no `feme::ShaderStage`, no signature
reflection, no `feme.stage.*`/`feme.image.*` operation, no `CompiledStage`,
no image or sampler descriptor, and no `lib/Graphics`, `lib/RayTracing`,
`lib/Vulkan` or `lib/Direct3D` directory of any kind. Its priorities are
relative *within that track*: a P0 there does not outrank a P1 in §1.1–§1.7,
it means "the graphics/runtime work stalls until this lands".

Since §1.9.2, both tracks answer to a third thing that is neither a
subsystem nor a milestone: the **conformance target**. Per
[FeMeVulkanDesign.md](FeMeVulkanDesign.md)'s "Conformance Target", the
Vulkan ICD now aims at full Vulkan 1.4 conformance covering compute,
graphics *and* ray tracing (and, eventually, the Direct3D equivalent —
§1.10.1). That changes what "done" means for the graphics/runtime track: a
CTS group reporting zero failures because nothing behind it is advertised
used to be a correct end state, and is now a measured gap (§1.9.7, §1.9.8).
It does not reorder the tracks — the graphics core still has to exist
before it can be conformant — but every P-priority below is now read
against §1.9.9's definition of done rather than against "the design says
so".

The two tracks are not independent. Four compute-track narrowings recorded in
§1.6 are load-bearing prerequisites for graphics milestones
(FeMeGraphicsDesign.md, "Prerequisites from the compute CPU target"), and
§1.6's "Dispatch is sequential, not thread-pooled" row is the *first* thing
both API runtimes need. §1.8's table records which is which.

## Part 1: Gap inventory

### 1.1 Core library plumbing

The library-shape parts of Design.md are the least finished part of FeMe,
because every milestone so far has been able to route around them.

| Gap | Owner section | Priority |
|---|---|---|
| `Context` registers no MLIR dialects (`lib/Core/Context.cpp` still carries the `TODO`; every tool registers its own dialects instead) | "`feme::Context`" | P1 |
| ~~No `FormatRegistry`;
`Driver` hard -
        codes `detectFormat`~~(
            done by R11
            : `feme::FormatRegistry` maps format names
                      to `Importer`/`Exporter` instances,
              populated by `Driver`'s constructor rather than `Context`' s
                  own-- see the Deviation note this adds to
              "`feme::Context`" --and `detectFormat` now looks each Importer up
                  in it) |
    "Status: `feme::Driver`" | P1 | |
    ~~No `Exporter` interface exists at all — the symmetric half
     of `Importer` was never written;
DXIL / SPIR - V "export" is spelled as a `Backend` today ~~(
                  done by R11
                  : `feme::Exporter`/`ExportOptions` plus `feme::
                            DXILExporter`/`feme::SPIRVExporter`,
                    each resolving the same target
                        triple `Driver::resolveTargetTriple` already computes
                            and delegating to `TargetMachineBackend`) |
    "`Exporter`" | P1 | |
    ~~`Context` has no `setDiagnosticHandler`/`diagnose` at
        all(`include / feme / Core /
            Context.h` exposes only the two context accessors); fallible library code returns `llvm::Error` and each tool prints it itself~~ (done by R11: `feme::Diagnostic`/`Context::setDiagnosticHandler`/`diagnose` now carry `Driver::run`'s `--wave-size`-ignored warning, the one library-code `errs()` write that existed, with `feme`/`feme-run` each installing their own stderr handler) | "Diagnostics and Error Handling" | P1 |
| ~~Thread-safety (one `Context` per thread, stateless components) is a stated invariant with no test asserting it~~ (done by R11: `unittests/Driver/ThreadSafetyTest.cpp`) | "Core Architectural Principle" | P0 |
| C API | Design.md milestone 10 | P2 |

The thread-safety item was P0 because it is a *claim in the design*, made
about a library whose primary use case is a multi-threaded driver, that
nothing verified. `ThreadSafetyTest.cpp` drives one shared, stateless
`DXILImporter` plus the DXIL raising passes from N threads, each with its
own `Context`, and checks every `Context`'s underlying
`LLVMContext`/`MLIRContext` stays distinct while all are simultaneously
alive (run it under TSan for the stronger form of this check).

### 1.2 SPIR-V

The SPIR-V *input* half is the narrowest edge of the translation matrix.

- **P0 — `spirv` → `llvm` dialect conversion breadth (done by R9).** Per
  "Known gap: `spirv` dialect -> `llvm` dialect conversion coverage":
  `StorageBuffer` blocks (`RWStructuredBuffer<T>`/`StructuredBuffer<T>`,
  converting to `target("spirv.VulkanBuffer", ...)`), push constants, basic
  (unmodified) `spirv.ImageSampleImplicitLod` sampling and `OpImageFetch`
  (which reuses `spirv.ImageRead`'s exact lowering -- LLVM's SPIRV backend
  itself picks the opcode from the handle's image type) all now convert.
  Still missing, and narrower than originally scoped here: the sampling
  bias/gradient/comparison/gather variants (each needs its own pattern
  supplying additional operands; explicit-LOD sampling with a lone `Lod`
  operand is covered by R30's `ImageSampleExplicitLodPattern`),
  `Uniform`-storage-class buffer
  blocks (`cbuffer`/`ConstantBuffer<T>`, which real `clang`-compiled access
  spells entirely differently -- per-member globals in a separate address
  space tied together by `!hlsl.cbs` metadata -- and so needs its own design
  decision rather than reusing the storage buffer access chain pattern), and
  graphics stage inputs/outputs. "SPIR-V input" now covers a compute shader
  binding, reading and writing storage buffers and sampling a texture, which
  is enough to run more (but not yet all) HLSL through both front ends (see
  §2.3).
- **P0 — SPIR-V shaders cannot execute (done by R10).** `feme-run` now
  links `FeMeImportSPIRV`/`FeMeTranslateSPIRV` too (see §2.4.2), so a
  SPIR-V binary module is imported and translated to LLVM IR the same way
  a DXIL bitcode file/DXContainer is; `feme::cpu::SPIRVResourceLoweringPass`
  and `feme::cpu::SPIRVBuiltinFoldingPass` (new CPU-pipeline passes) let a
  storage-buffer compute shader using thread/group ID builtins execute,
  closing this asymmetry for that shape of shader (still not every
  resource kind §1.2's conversion-breadth note above lists as open --
  image/sampler resources and per-field structured-buffer access remain
  future work on the CPU-execution side specifically). See §2.4.2 and the
  Deviation note roadmap step R10 adds to feme/docs/FeMeCPUDesign.md's
  Status section.
- **P1 — SPIR-V → DXIL direction (done by R13).** Design.md milestone 6's
  remaining half: `feme::dxil::SPIRVRaisingPass` raises the `llvm.spv.*`/
  `target("spirv.")` conventions back to `llvm.dx.*`/`target("dx.")`,
  covering the thread/group index queries and a `StorageBuffer` resource
  accessed through a flat element access. A typed-buffer image resource
  remains unraised, since it is still blocked on the conversion breadth
  above (no SPIR-V shader reading/writing one reaches LLVM IR at all yet).
- **P1 — SPIR-V bound resources (done by R26).** `SPV_EXT_descriptor_heap`
  remains unraised, so `feme::cpu::BoundResourceNormalizationPass` still
  handles DXIL's `handlefrombinding` only, and `handlefromimplicitbinding`
  still has no raiser to produce it -- but SPIR-V's own bound-resource
  form no longer needs that raiser to normalize an *arrayed* binding: see
  R26 below and §1.9's SPIR-V binding-to-heap normalization row.

### 1.3 DXIL

DXIL import is the most complete path, and its gaps are enumerable.

- **P0 — aggregate-returning ops (done by R3).** `IMul`/`UMul`/`UAddc`/
  `SplitDouble`/`WaveActiveBallot` all needed the same general
  multi-return-value `extractvalue`-reconstruction mechanism, which is why
  they were deferred together (Design.md milestone 4 status; FeMeCPUDesign.md
  milestone 1 deviation). `feme::dxil::OpRaisingPass::raiseAggregateCall` is
  that one mechanism, unblocking all five; `WaveActiveBallot` -- the only
  wave intrinsic HLSL programs reach for that FeMe could not lower -- is now
  lowered on the CPU target too (`feme::cpu::WaveCallKind::Ballot` in
  WaveLowering.cpp), exercised end to end by `ballot.hlsl`.
- **P0 — flag-selected opcode families (done by R4).** `WaveActiveOp`/
  `WaveActiveBit`/`WavePrefixOp`/`QuadOp` select their operation from a flag
  operand rather than mapping 1:1 onto an intrinsic; `Barrier` (done
  earlier) likewise. `feme::dxil::OpRaisingPass::raiseReduceOpCall`/
  `raiseWaveActiveBitCall`/`raiseQuadOpCall` raise all four; the CPU target
  lowers every one of them except `QuadOp` (`feme::cpu::WaveLoweringPass::
  lowerActiveReduce`/`lowerPrefixReduce`) -- quad ops need a fixed
  lane-to-quad mapping that remains an explicit v1 non-goal (see
  FeMeCPUDesign.md's "Non-Goals"), so raising `QuadOp` closes the "hard
  pipeline error downstream" risk without yet making it executable.
- **P1 — texture/sampler handle kinds (legacy path done by this roadmap
  step).** The blocking decision is now recorded in Design.md's DXIL section
  ("Decision: texture and sampler handle kinds"): the dimension/multi-sample/
  feedback bits this entry said were missing are in fact carried by
  `ResourceProperties` (Word0's `ResourceKind` byte *is* the dimension; Word1
  carries component type/count, sample count and feedback kind), so the
  raised handle types are LLVM's own
  `dx.Texture`/`dx.MSTexture`/`dx.FeedbackTexture`/`dx.Sampler`. What is
  actually left is implementation, plus two narrower gaps the decision names:
  the legacy `!dx.resources` path has no component count and must recover the
  texel width from access sites the way typed buffers already do, and
  UNORM/SNORM/packed element kinds stay unraised until G2's format table
  exists. Implemented by R30 for the bindless `handlefromheap`/
  `handlefrombinding` path (see Design.md's status note); the legacy
  `!dx.resources`-based path's texture kinds (`buildHandleType` generalized
  the same way `TypedBuffer` already worked, `inferTypedBufferWidth`
  generalized to read `dx.op.textureLoad`/`textureStore` too) are now raised
  as well -- `dxil-raise-legacy-resources.ll`'s `load_texture2d` covers this,
  closing the gap a real `dxc -T cs_6_2` (below SM6.6's dynamic-resource
  threshold, so it takes this legacy path) compute shader hit. `Sampler`
  (no access op raises to consume it yet either way) and UNORM/SNORM/packed
  formats remain future work.
- **P1 — the remaining resource access ops (done for texture load/store and
  the 16-/64-bit `CBufferLoadLegacy` row overloads by this roadmap step).**
  `raiseTextureStore` (DXIL opcode 67) is now symmetric with the existing
  `raiseTextureLoad`, using a new canonical `llvm.dx.resource.store.texture`
  intrinsic added upstream (DXIL's `TextureStore` op had a `DXILOpClass` but
  no numbered `DXILOp<67, ...>` definition, canonical intrinsic, or
  `DXILOpLowering` support at all, unlike `TextureLoad`/op 66); generalizing
  `raiseCBufferLoadLegacy` to `getCBufferRowIntrinsic`'s 2-/8-field 64-/16-bit
  row shapes (alongside the existing 4-field 32-bit one) covers a `cbuffer`
  of `double`/`half`/16-bit-int members, which `-enable-16bit-types`
  produces. Both were needed by the same real shader
  (`feme-dxil-to-amdgpu-texture.ll`): raw/structured buffer load/store and
  samplers remain the rest of this entry's scope. `GetDimensions`' `.xy`
  (width+height) field pair now raises too (`raiseGetDimensions`, plus a new
  `DXILOpLowering::lowerGetDimensionsXY` forward lowering added upstream the
  same way `TextureStore`'s was, and `feme::amdgpu::ResourceLoweringPass`'s
  new `Binding::NumDimensionArgs` kernel arguments to consume it when
  retargeting to `amdgcn-*` -- see `feme-dxil-to-amdgpu-texture-
  getdimensions.ll`); a mip-count out-parameter (`.z`/`.w`) is still left
  unraised, since there is no `levels_xy`-shaped forward lowering to verify
  against yet.
- **P1 — shader model 6.9's unified `FDot` op (done by this roadmap step).**
  A real `dxc -T cs_6_9` shader calling HLSL's `dot()` on a 16-bit vector
  (e.g. `dot(half2, half2)`) emits DXIL opcode 311 (`dx.op.dot.*`), not the
  older arity-specific `Dot2`/`Dot3`/`Dot4` (opcodes 54-56) this pass already
  covered -- `feme::dxil::OpRaisingPass` had no row for it at all, so
  retargeting such a shader to `amdgcn-*` failed with "'dx.op.dot.v2f32' is
  not supported when targeting 'amdgcn-amd-amdhsa'". `FDot` still fits
  the same table-driven `raiseCall` path as `Dot2`..`Dot4` (its overload key
  is still the first operand's type), it just takes its two operand
  *vectors* directly rather than 2*N separate scalars, matching
  `int_dx_fdot`'s existing signature; `feme::dxil::IntrinsicExpansionPass`
  gained a matching `expandFDot` (per-lane `extractelement` +
  `llvm.fmuladd` chain, mirroring `expandDot`'s scalar-operand version) so
  every non-DXIL target still sees plain LLVM IR. LLVM's own DirectX backend
  never emits opcode 311 itself (`DXILIntrinsicExpansion` scalarizes
  `llvm.dx.fdot` before `DXILOpLowering` runs), so this has no
  `-dxil-op-lower` round-trip test -- `feme-dxil-to-amdgpu-dot.ll` instead
  feeds a hand-written `dx.op.dot` call through the raw-bitcode import path
  (`llvm-as`, no `llc`) straight into the full `feme` CLI targeting
  `amdgcn-amd-amdhsa`.

### 1.4 DXBC / `dxsa`

- **P0 — DXBC is not reachable from `feme` or `Driver` at all (done by
  R7).** `detectFormat` now distinguishes a legacy DXBC `DXContainer` (an
  `SHEX`/`SHDR` part) from a DXIL one (a `DXIL`/`ILDB` part) sharing the
  same "DXBC" magic; `feme::DXBCImporter` (`feme/lib/Import/DXBC`) parses
  the former into the `dxsa` dialect via the existing `BinaryParser`, and
  `feme::dxsa::DXSAToLLVMIRTranslator` wraps the existing
  `translateToLLVMIR` behind the `Translator` interface so `feme::Driver`
  dispatches to it exactly like `SPIRVToLLVMTranslator`. Both are
  registered with `feme-translate` (`--import-dxbc`), closing Design.md
  milestone 8 ("DXBC → DXIL end to end") -- see
  `test/Tools/feme/feme-dxbc-to-dxil.dxasm` for the resulting end-to-end
  DXBC-in, DXIL-`DXContainer`-out round trip through the full `feme` CLI.
  This was indeed mostly wiring, but exposed one real bug in
  `translateToLLVMIR` itself along the way: a UAV's `!dx.resources` entry
  was missing the three `i1` flags (globally-coherent/has-counter/
  rasterizer-ordered) a UAV entry needs beyond an SRV's, which silently
  made every DXBC-derived UAV unraisable (`feme::dxil::OpRaisingPass`
  could never look its binding up) -- invisible until an actual retargeting
  path existed to notice, since no existing test `FileCheck`ed that far
  into the metadata. Retargeting a DXBC-derived module inherits every
  limitation retargeting a DXIL one already has (see §1.3), plus
  `translateToLLVMIR`'s own remaining gaps below -- notably, no DXBC
  graphics-stage shader is retargetable yet at all (`loadInput`/
  `storeOutput` are not raised, a gap shared with real DXIL input, see
  `feme/docs/CommandGuide/feme.md`), and a DXBC compute shader's declared
  `NumThreads` does not yet reach DXIL's metadata.
- **P0 — DXBC importer fuzzer (done by R6).** Design.md's Testing Strategy
  makes "a fuzzing harness lands alongside each importer" a v1 requirement,
  and DXBC was the one importer without one (`dxbc-as-fuzzer` fuzzes the
  assembler's text parser, not `BinaryParser`'s binary one).
  `feme-dxbc-import-fuzzer` (`feme/tools/feme-dxbc-import-fuzzer`) now
  fuzzes `feme::dxsa::deserialize` directly, and immediately found a real
  crash: a malformed `l`/`d` immediate source operand whose decoded
  component count didn't match its payload hit an unchecked
  `SrcOperandAttr::get` builder call and asserted instead of surfacing a
  diagnostic; `BinaryParser.cpp` now builds it with `getChecked`, falling
  back to the existing `dxsa.unknown` diagnostic path like every other
  malformed-operand case (see
  `test/Target/DXSA/src_operand_immediate_zero_components_invalid.dxasm`).
- **P1 — `BinaryWriter` (`feme::dxsa::serialize`, done by R13)** implements
  the DXBC export path's hard prerequisite: it reuses `dxbc-as`'s own
  mnemonic-to-opcode table and encoder (`lookupOpcode`/`getOpcodeInfo`/
  `encodeProgram`) and covers every operation built from DXSAOpBase.td's
  five generic shapes (no-operand/unary/binary/ternary/multiply-add) plus
  `DXSA_MovConditionalOp`'s `movc`/`dmovc` family -- the ISA's arithmetic/
  logic/comparison/conversion core. Declarations, control flow, and
  resource/texture ops still diagnose rather than serialize, so a real,
  complete shader cannot round-trip through it end to end yet.
- **P1 — `translateToLLVMIR` coverage**, in the dependency order its own
  status note gives: resource queries (`bufinfo`/`resinfo`/`sampleinfo`/
  `samplepos`), atomics and UAV counters, groupshared memory, doubles,
  subroutines (`label`/`call`), non-pixel-shader stage declarations (which
  includes the `NumThreads` gap R7 found, above).
- **P2 — SM5 opcode coverage** in the dialect/parser itself, incrementally.

### 1.5 Retargeting

- **P1 — NVPTX and AArch64 (done by R13)** (Design.md milestone 9's
  remainder). `feme::nvptx::RaisedLoweringPass`/`ResourceLoweringPass` are
  the NVPTX counterparts to AMDGPU's own pair, mapping the same raised
  conventions onto NVVM/PTX-kernel primitives; unlike AMDGPU, NVPTX has no
  native object-file codegen (only PTX assembly text) and
  `feme::Backend`'s `BackendOptions::FileType` has no knob yet to request
  that, so there is no end-to-end `feme --target=nvptx*` object-file test,
  only the two passes' own `feme-opt` coverage. `Driver`'s triple
  resolution turned out to already be generic enough for AArch64 with no
  code changes at all -- `test/Tools/feme/feme-dxil-to-aarch64.ll` is a new
  end-to-end test proving that against a genuine non-host CPU ISA, not a
  new capability.
- **P1 — `feme::amdgpu::RaisedLoweringPass`/`ResourceLoweringPass` breadth**
  tracks §1.2/§1.3: every newly-raised intrinsic needs an AMDGPU lowering or
  it becomes a new end-to-end failure on a path that used to work.
  `ResourceLoweringPass` now also models `dx.Texture` (a flat data pointer
  plus one trailing per-extra-dimension row/slice-pitch stride argument,
  `Binding::NumAuxArgs` -- DXIL carries no such stride itself, unlike a
  typed buffer's flat element index) and `dx.CBuffer` (a 16-byte-strided
  flat load, needing no extra argument) alongside its existing
  `dx.TypedBuffer`/`spirv.Image` support, closing the two-part gap §1.3's
  own entry above added raising for; doing so surfaced (and fixed) two
  `collectBindings` bugs only reachable once more than one DX resource
  family was modeled at once (mismatching a handle's family by intrinsic ID
  alone, and keying a binding by (space, register) alone despite HLSL's
  `t`/`u` registers being independent namespaces) -- see Design.md's status
  note on this section for both. `feme-dxil-to-amdgpu-texture.ll` is a new
  end-to-end test proving a real `Texture2D`/`RWTexture2D`/`cbuffer<half>`
  shader now retargets to AMDGPU; raw/structured buffers and samplers
  remain unmodeled.
- **P2 — MLIR `gpu`-dialect retargeting**, deferred by Design.md's Non-Goals
  until a client needs it. No action.

### 1.6 CPU target

FeMeCPUDesign.md milestones 1–11 are done; 12 (resource performance) and 13
(general performance) are explicitly correctness-first-then-measure and stay
P2. What is P0/P1 is the set of things earlier milestones *narrowed*, each of
which is a diagnostic today and a wrong answer tomorrow if forgotten:

| Narrowing | Milestone | Priority |
|---|---|---|
| ~~Barrier inside a surviving *branch* (not a loop; R5 closed the loop case) is diagnosed, not split~~ (closed by R24: a uniform two-way branch with no merge-block phi is now split, not diagnosed; a merge-block phi, or a value live across a barrier within one arm, remain diagnosed) | 9 | P1 |
| ~~Divergent groupshared access is diagnosed~~ (closed by R23: a divergent index, an access through a `getelementptr`, and a masked store at a uniform address are all now canonicalized; only a *nested* `getelementptr` -- a groupshared array of arrays/structs -- remains diagnosed) | 9 | P1 |
| Vector/aggregate leaf decomposition narrower than the design (narrowed further by C3: `phi`, scalar-condition `select`, `shufflevector`, and non-constant-index `extractelement` producers/consumers are now supported alongside R12's resource-load producer and constant-index `extractelement` consumer; only a per-lane `<N x i1>`-condition `select` and every divergent aggregate remain diagnosed -- see FeMeCPUDesign.md's deviation note) | 7 | P2 |
| Masked memory always lowers to `gather`/`scatter` (no uniform/contiguous cases) | 7 | P2 (perf) |
| `Device` vs `All` memory scope not distinguished | 9 | P2 |
| ~~Dispatch is sequential, not thread-pooled~~ (closed by R21) | 4 | P1 |
| `feme-run`'s heap YAML has no `class`/`kind`/`stride`/`format` | 11 | P0 (see §2.4) |

R12 closed this table's "Root constants unimplemented" row (`feme::cpu::RootConstantLoweringPass`, see its own roadmap entry) and "`WaveReadLaneAt`'s lane operand assumed uniform" row (`feme::cpu::WaveLowering.cpp`'s `lowerReadLane` now builds a genuine per-lane gather; SPIR-V's shuffle-style read, the only form with no HLSL uniform-index guarantee, is the one that can actually be divergent).

R5 closed this table's "no SSA value may be live across a group-sync
barrier" row outright (`feme::cpu::spillValuesLiveAcrossBarriers`) and
narrowed the "barrier inside a surviving branch or loop" row to just
branches: `feme::cpu::matchLoopShape`/`buildWrapperForLoop` split a barrier
inside a uniform, header-tested loop (the stride-halving reduction shape
`reduction.hlsl` compiles to) by cloning the loop's header/latch directly
into the wrapper as an ordinary scalar loop and running the barrier-split
body regions through the usual per-wave wave loop once per iteration. It
also found (while writing `reduction.hlsl`) that the groupshared-access
narrowing was one case broader than recorded: not just a divergent
*index*, but also a *masked* (conditionally-executed) store at a uniform
address, since `feme::cpu::LinearizePass` lowers that into a call
`feme::cpu::rewriteGroupSharedGlobals` does not recognize -- both closed
by R23.

R2 closed both of this table's former P0 rows: the scalarization fallback
now masks a divergent `atomicrmw`'s per-lane execution (an unmasked lane in
a scalarized atomic was not a crash, it was a silently wrong answer, which
is why it was P0 -- see `feme::cpu::FunctionWidener::widenMaskedAtomicRMW`,
`feme/test/Tools/feme-run/HLSL/histogram.hlsl`), and `feme::cpu::runPipeline`
now fails outright, with the underlying diagnostic surfaced, the moment any
CPU-pipeline pass (`feme::cpu::LinearizePass`/`SIMDizePass` in particular)
reports one instead of silently continuing to a later pass or the JIT (see
`feme::cpu::ErrorDiagnosticGuard` in Pipeline.cpp) -- investigating the
"`feme-cpu-simdize` divergent-branch check does not catch every such case"
symptom found that check itself (`FunctionWidener::checkSupportedControlFlow`)
already correctly flags every shape `feme-cfg-gen --unstructured` produces
that `feme::cpu::LinearizePass` leaves untouched (verified directly via
`feme-opt -passes=feme-cpu-linearize,feme-cpu-simdize`); the actual gap was
that neither pass's diagnostic ever stopped `runPipeline` from linking and
JIT-dispatching the untouched, still-divergent function anyway.

### 1.7 Robustness

- **P0 — the fuzzers do not run anywhere (done by R6).** Five libFuzzer
  targets now exist (`feme-dxil-import-fuzzer`, `feme-dxbc-import-fuzzer`,
  `feme-spirv-import-fuzzer`, `dxbc-as-fuzzer`,
  `feme-cpu-restructure-fuzzer`); all five are in `FEME_TEST_DEPENDS`
  (`test/CMakeLists.txt`), so `ninja check-feme` builds every one, and a
  new `check-feme-fuzz` CMake target (driven by
  `feme/utils/check-feme-fuzz.py`) runs each fuzzer for a bounded number of
  iterations (`-runs=N`/`-max_total_time=N`) over its own checked-in seed
  corpus. Wiring the build half up alone was enough to catch a real,
  already-landed regression: `dxbc-as-fuzzer.cpp` had bit-rotted against a
  `wrapInContainer` signature change (a `Signatures` parameter added by a
  later commit) and no longer compiled, unnoticed because nothing built it
  — exactly the failure mode this item warned about.
- **P1 — sanitizer coverage.** The one previously-recorded crash class in
  this tree (see `agent_thoughts.md`) was a UB/null-deref bug found by hand;
  an ASan/UBSan `check-feme` configuration is the systematic version. It is
  also a stated requirement of all three new designs, each of which adds
  attacker-controlled parsers (SPIR-V from applications, descriptor updates,
  command streams, pipeline-cache blobs, acceleration-structure builds), so
  it stops being a nice-to-have the moment §1.9/§1.10 start.

### 1.8 Graphics core and the CPU stage ABI

Owned by [FeMeGraphicsDesign.md](FeMeGraphicsDesign.md). None of it exists.
This section is the shared half of §1.9 and §1.10: every row here blocks both
API runtimes, which is why it is scheduled ahead of either.

#### 1.8.1 Compute-track prerequisites the graphics design depends on

These are already in §1.1–§1.7 as compute gaps. They are repeated here only
because the graphics design's "Prerequisites from the compute CPU target"
table makes them blocking, which changes their priority: each is P1 in §1.6
for compute and P0 for the track below.

| §1.6 narrowing | Blocks | Priority here |
|---|---|---|
| ~~Dispatch is sequential, not thread-pooled (`JITEngine` has no unit of work smaller than a whole dispatch; `JITOptions::NumThreads` is accepted and ignored)~~ (closed by R21: `feme::cpu::CompiledStage::invokeGroup` plus a real `JITOptions::NumThreads`) | Every runtime milestone — Vulkan V1, Direct3D W1, and graphics G1 all need `CompiledStage::invokeGroup` | P0 |
| ~~`ArtifactInfo`'s `WaveSize`/`GroupSize`/`GroupSharedSize`/`GroupSharedAlign` are in the version-2 layout but always written as 0 (see ResourceInfo.h's own note)~~ (closed by R22's first half: `feme::cpu::CompiledStage::getArtifactInfo` (JIT) and `feme::Driver`'s CPU retargeting path (AOT, via `emitArtifactGlobal`) both now populate every field from the same resolved wave size/thread-group size and the new `feme::cpu::getGroupSharedRequirements` (feme/include/feme/Transforms/CPU/GroupSharedInfo.h)) | V1, W1 — a runtime cannot size a workgroup or its groupshared block from reflection | P0 |
| ~~Divergent groupshared access is diagnosed~~ (closed by R23) | V2, W2 (an `SV_GroupIndex`-indexed `groupshared` array is what ordinary shaders write), G5, G6 | P0 |
| ~~A barrier inside a surviving *branch* is diagnosed, and a `phi` live across a group-sync barrier cannot be spilled~~ (closed by R24) | G5, G6 — a tessellation-control stage that cannot synchronize inside control flow cannot express its source model | P1 |
| ~~Root constants cover only the default `(b0, space0)`, non-array `dx.CBuffer`, constant-row-index shape R12 landed~~ (closed by R25: any single register-bound binding, array bindings, and a dynamic row/array index are now accepted, and the reported size is the binding's full advertised span) | V3's full advertised `maxPushConstantsSize`, W2's CBVs, G1's `FemeShaderResources::RootConstants` | P1 |

The last row was a *correction* to all three new documents: FeMeWARPDesign.md's
status section says "Root-constant lowering does not exist,
`ResourceInfo::RootConstantSize` is always zero", and
FeMeGraphicsDesign.md's prerequisite table lists root constants as an
unsupported resource kind. Both predate R12, which landed
`feme::cpu::RootConstantLoweringPass` and made `ResourceInfo::RootConstantSize`
real (`lib/Target/CPU/ResourceInfo.cpp`); R25 then closed the remaining
breadth gap the correction called out.

#### 1.8.2 Core reflection and canonical graphics IR (G0)

| Gap | Owner section | Priority |
|---|---|---|
| ~~No `feme::ShaderStage` enumeration and no `feme.shader.stage` entry-point attribute; CPU stage selection is `feme::cpu::PreparePass`'s `isComputeEntryPoint` string comparison against `"compute"`~~ (closed by R16) | "Stage identity" | P0 |
| No signature reflection of any kind: no element ID, direction, location, semantic, system value, component type, shape, interpolation, frequency, or stream | "Signature reflection" | P0 |
| ~~`feme::dxil::MetadataRaisingPass` erases `!dx.entryPoints` — including the input, output, patch-constant and root-signature rows — after recovering only `hlsl.shader`/`hlsl.numthreads`/`hlsl.wavesize`~~ (closed by R18) | "Signature reflection" | P0 |
| SPIR-V conversion deliberately fails to legalize non-builtin `Input`/`Output` variables, and converts no `Location`/`Component`/`Index`/interpolation/per-primitive/per-patch decoration | "Signature reflection"; §1.2's own "graphics stage inputs/outputs" gap | P0 |
| ~~No canonical stage operations (`feme.stage.input.load`/`output.store`/`discard`/`demote`/`is_helper`/derivative/quad/interpolate/emit/cut/mesh/ray families) and no `lib/Transforms/Graphics` canonicalization or validation pass~~ (closed for vertex/fragment by R20; the stream-emission/mesh-output/ray families remain open for their own milestones) | "Canonical stage operations" | P0 |
| No `StageInterfaceMap` or cross-stage linkage validation | "Signature reflection" | P1 |

DXIL's `loadInput`/`storeOutput` being unraised is *already* recorded as a
compute-track gap (§1.4's R7 entry notes no DXBC graphics-stage shader is
retargetable because of it, and that real DXIL input shares the gap). G0 is
where it is finally owned by a design rather than noted as a limitation.

#### 1.8.3 CPU stage compilation (G1)

| Gap | Owner section | Priority |
|---|---|---|
| ~~`runPipeline(llvm::Module &, llvm::StringRef, unsigned)` has no stage parameter and no `StageCompileOptions`~~ (closed by R27: a new `runPipeline(llvm::Module &, const feme::cpu::StageCompileOptions &)` overload selects by `feme::ShaderStage`, tagging its result with the compiled stage; the original signature is kept as the compute-only compatibility overload the design asks for) | "CPU Lowering Pipeline" | P0 |
| ~~No `CompiledStage`/`PreparedDispatch`/`invokeGroup` — the type FeMeVulkanDesign.md calls `CompiledKernel` and FeMeWARPDesign.md asks to share; the graphics design's answer is that there is exactly one type, so V1/W1 should build against the final name~~ (closed by R21, landed under the `CompiledStage` name; `create` still takes the existing compute-only `JITOptions` rather than `StageCompileOptions`, and the stage-specific `invokeVertices`/etc. methods do not exist yet — R27 lands `StageCompileOptions` itself and the stage-aware `runPipeline` it threads through, but `CompiledStage::create` is deliberately left on `JITOptions` until R28 builds the vertex/fragment wrappers that would give a non-compute `CompiledStage` something to dispatch) | "Compiled stage API" | P0 |
| `feme::cpu::EntryWrapperPass` emits only `feme_cpu_entry_<name>(const FemeDispatchArgs *)`; there is no vertex, fragment, patch, mesh or ray continuation wrapper | "Vertex wrapper" … "Ray continuation wrappers" | P0 |
| ~~One implicit active mask controls both execution and stores; fragment execution needs a separate live mask and side-effect mask, with `discard` clearing both and `demote` clearing only the second~~ (closed by R27: `feme::cpu::LinearizePass`'s `DiamondFlattener`/`LoopLinearizer` thread a `MaskPair{Live, SideEffect}` instead of a single scalar mask -- `feme.stage.discard` narrows both, `feme.stage.demote` narrows only `SideEffect`, `feme.stage.is_helper` reads `Live && !SideEffect` -- and `--reference` mode gets its own counterpart (a real conditional early return for `discard`, a per-invocation `helper` flag for `demote`/`is_helper`). Scoped, like the rest of milestone 6/7's masking, to the divergent-diamond/divergent-loop-exit shapes already supported; a stage-mask call inside an otherwise-uniform loop is diagnosed rather than mis-widened, and `--reference` does not yet suppress a `demote`d invocation's later side effects) | "Shared middle-end phases" | P0 |
| No derivative or quad lowering at all (`QuadOp` is raised but not lowered — §1.3's R4 entry — and FeMeCPUDesign.md's Non-Goals still defer the lane-to-quad mapping a fragment stage requires at wave sizes 4 and 8) | "Derivatives and quad operations" | P0 |
| No `FemeStageLayout`, `FemeVertexArgs`, `FemeFragmentArgs` or any stage argument block | "Graphics Runtime ABI" | P0 |
| ~~`ArtifactInfo` is compute-shaped; there is no stage-tagged `StageArtifactInfo` carrying signatures, side-effect summaries, tessellation/mesh/ray layouts~~ (closed by R22 for the shared, stage-independent core: `feme::cpu::StageArtifactInfo` (renamed from `ArtifactInfo`) now carries a `feme::ShaderStage` tag, a serialized `feme::EntrySignature` (`Signature`, empty until R28 makes `CompiledStage` itself stage-aware), and side-effect-summary `Flags` bits (`FEME_CPU_ARTIFACT_USES_DISCARD`/`_DEMOTE`/`_HELPER`, computed by `feme::cpu::computeSideEffectFlags`); `ArtifactAbiVersion` is bumped to 3, and both `feme::cpu::CompiledStage::getArtifactInfo` (JIT) and `feme::Driver`'s CPU retargeting path (AOT) build the same structure. Tessellation/mesh/ray layouts remain for R34/R35/R37, once those stages exist to describe) | "Artifact reflection" | P1 |

G1 is the design's own discriminating milestone: if a vertex or fragment
shader cannot pass through the existing uniformity, linearization, SIMDization
and wave-lowering phases with localized extensions, the shared middle-end
boundary is wrong and must be revised before any fixed function is built. It
should therefore be treated as a decision point, not just another step.

#### 1.8.4 Images, samplers, and formats (G2)

| Gap | Owner section | Priority |
|---|---|---|
| ~~`FemeDescriptor` cannot express dimensionality, mip/array ranges, sample or plane layout; `ResourceKind` is `{
  None, Typed, Structured, Raw, CBuffer}` with no image kind~~ (closed by R29: `FemeImageDescriptor` is its own descriptor type rather than a `ResourceKind` case, so `FemeDescriptor`/`ResourceKind` are unchanged and images never share their shape) | "Separate descriptor kinds" | P0 |
| ~~`FemeDispatchArgs::SamplerHeap` is typed `const FemeDescriptor *` — reserved before sampling had any representation — and must become `const FemeSamplerDescriptor *`~~ (closed by R29) | "Relationship to the compute ABI" | P0 |
| ~~No `FemeShaderResources` block shared by compute and graphics; the resource fields are inlined into `FemeDispatchArgs`~~ (closed by R29: `FemeDispatchArgs` now embeds a `FemeShaderResources Resources` member instead of declaring its own resource fields) | "Relationship to the compute ABI" | P0 |
| ~~No `feme.image.*`/`feme.sampler.*` operations and no sampling, filtering, mip-selection, addressing-mode, sRGB or format-conversion helpers in `runtime/CPU`~~ (closed by R30 for 2D images: DXIL's `dx.op.sample`/`sampleLevel`/`textureLoad` raise to LLVM's own `llvm.dx.resource.sample`/`samplelevel`/`load.level` -- the canonical, already target-generic-in-spelling intrinsics, not a bespoke `feme.image.*` dialect -- and `feme::cpu::ResourceLoweringPass` lowers a 2D `dx.Texture`/`dx.Sampler` pair's access into new `feme.cpu.image.sample.2d.v4f32`/`samplecmp.2d.f32`/`load.2d.v4f32` calls (`feme::cpu::ImageCalls`), implemented by `runtime/CPU/FeMeRuntimeCPU.c`'s point/bilinear filtering, all five addressing modes, explicit-LOD mip selection, PCF-style comparison sampling and an initial `R32G32B32A32_FLOAT`/`R8G8B8A8_UNORM(_SRGB)` format table with sRGB decode. A follow-up pass added the SPIR-V half (`feme::cpu::SPIRVResourceLoweringPass` lowering a bound 2D `spirv.Image`/`spirv.Sampler` pair, plus the image/sampler heap classes and materialization a host needs to feed it) and active-lane SIMD widening for a *divergent* sample; 1D/3D/cube sampling, arrayed/multisampled/storage images, bias/gradient sampling, gather and nonzero texel offsets remain -- see "Canonical image operations"/"Texture layout and formats" in FeMeGraphicsDesign.md for the itemized list and why each is deferred) | "Canonical image operations", "Texture layout and formats" | P0 |
| ~~DXIL texture/sampler handle kinds are unraised (§1.3's own P1 row; the encoding is now decided in Design.md's "Decision: texture and sampler handle kinds", so only the implementation is left) and SPIR-V sampling beyond basic `ImageSampleImplicitLod`/`OpImageFetch` is unconverted (§1.2's R9 entry)~~ (closed by R30 for the bindless heap path and the DXIL ops LLVM's own backend already lowers a canonical intrinsic to (`Sample`/`SampleLevel`/`TextureLoad`/`GetDimensions.x`/`.xy`), plus SPIR-V's `ImageSampleExplicitLod`; comparison sampling/gather have no numbered DXIL wire opcode in this LLVM tree to raise from at all, an upstream gap, not FeMe's -- see Design.md's "Decision: texture and sampler handle kinds" status note) | §1.2, §1.3 | P0 |

G2 is scheduled before any raster stage on purpose: compute shaders that
sample images are required by Vulkan V5 and Direct3D W3, both of which precede
graphics in their own documents. It is also the ABI break — compiled artifacts
produced before it stop loading, which the design accepts explicitly now and
would not later.

#### 1.8.5 Software graphics and ray executors (G3–G8)

Everything here is greenfield: `FeMeGraphics`, `FeMeRayTracing`, the
normalized pipeline and prepared-draw descriptions, vertex/index fetch,
clipping, viewport transform, culling, tile binning, coverage, interpolation,
depth/stencil, blending, MSAA, tessellation, meshlets, acceleration-structure
builds and traversal, and the ray continuation transform. Priorities inside
this group are the milestone order itself (G3 → G8); nothing else depends on
them.

Two constraints from the design are worth restating because they are easy to
lose in scheduling:

- Neither runtime may advertise a graphics-capable queue,
  `VK_QUEUE_GRAPHICS_BIT`, or a raster-implying Direct3D feature level until
  the corresponding G milestone's completion test passes for every format and
  state combination it reports. Partial graphics support is worse than none.
- Wavefront packetization of ray continuations (G8) is only allowed *after*
  scalar continuation execution exists as the differential reference.

Work graphs are explicitly a later, separate design, not an extension of
amplification fanout or the ray continuation queues.

### 1.9 Vulkan runtime

Owned by [FeMeVulkanDesign.md](FeMeVulkanDesign.md). V0 (see §3.3) landed
`lib/Vulkan`, the development manifest, and the `vk.xml` generator; the rows
below are what remains for V0.5 onward.

| Gap | Owner section | Priority |
|---|---|---|
| ~~The Vulkan SDK would be FeMe's **first external dependency**. FeMe is built in-tree only, with no optional external package pattern to copy: the configuration surface, the disabled-path CI coverage, and the `vk.xml` version floor are new project-wide obligations~~ (closed by V0: `feme/CMakeLists.txt`'s `FEME_ENABLE_VULKAN` option `find_package(Vulkan 1.3)`s using CMake's own FindVulkan module, leaving every other feme component buildable when it isn't found) | "Project and Library Boundaries" | P0 |
| ~~No generated entrypoint table; hand-maintaining command names, aliases, core-version promotions and extension guards is the failure mode the design rejects~~ (closed by V0: `feme/utils/vk_gen_entrypoints.py` reads vk.xml's core `VK_VERSION_1_0`/`VK_VERSION_1_1` `<feature>` blocks directly, resolving aliases and classifying each command's loader dispatch level; extension guards remain future work once an extension is implemented) | "Loader Integration" | P0 |
| Symbol visibility and LLVM coexistence: the loader loads *every* ICD into the process, so `libfeme_vulkan.so` shares an address space with Mesa drivers linking their own LLVM. Static LLVM/MLIR, `-fvisibility=hidden`, an exports version script, no `llvm::cl` registration on any reachable path, and one-shot target initialization under `std::once_flag` are hard requirements, verified by a two-ICD test and an exported-symbol-set link check (V0 closed everything but the last clause: `feme_vulkan` links LLVM/MLIR statically with `-fvisibility=hidden` and `libfeme_vulkan.map`, verified by `test/Vulkan/two-icd-coexistence.test` against Mesa lavapipe and `nm -D`'s exact four-symbol check; one-shot target initialization has nothing to guard yet, since V0 does no shader compilation -- see FeMeVulkanDesign.md's V0 Status note -- and is deferred to whichever milestone first JIT-compiles a pipeline) | "Process Coexistence and Symbol Visibility" | P0 |
| ~~**`feme::SPIRVImporter` cannot ingest realistic Vulkan SPIR-V at all.** It wraps `mlir::spirv::deserialize`, whose structurized reconstruction rejects an `OpPhi` in a loop merge block — which any loop carrying a value-producing `break` emits — and has been observed to fail on `OpCopyObject`. Only trivial control flow imports today~~ (closed by V0.5: `ImportOptions::SPIRVEnableControlFlowStructurization` now defaults to `false`, so `SPIRVImporter` deserializes straight to unstructured block arguments/branches unconditionally rather than attempting -- and only falling back away from -- structured reconstruction; the prototype found a *second* reason this had to be the default rather than a failure-triggered fallback, not only the documented `OpPhi` rejection: MLIR's own `spirv.mlir.loop` -> `llvm` dialect conversion pattern crashes on a loop-carried value even when structurization itself succeeds. Validated against a real `dxc -spirv` corpus (`feme/test/Tools/feme-run/SPIRV`), not only hand-written fixtures, and `feme-spirv-import-fuzzer`'s seed corpus gained a matching unstructured, multi-block seed. See FeMeVulkanDesign.md's "SPIR-V import prerequisites" Status note for the full writeup, including the still-open `OpCopyObject`/glslang-corpus gaps) | "SPIR-V import prerequisites" | P0 |
| ~~No SPIR-V binding-to-heap normalization: `feme::cpu::BoundResourceNormalizationPass` rewrites DXIL's `handlefrombinding` only, and R10's `SPIRVResourceLoweringPass` normalizes a *single* bound storage buffer directly, with no descriptor-set, arrayed-binding or dynamic-offset model~~ (closed by R26: `feme::cpu::SPIRVResourceLoweringPass` now reads `llvm.spv.resource.handlefrombinding`'s own range-size and array-index operands rather than assuming an implicit range size of 1, assigning each (descriptor set, binding) identity a contiguous run of heap slots and range-checking a (possibly dynamic) array index into it exactly as `BoundResourceNormalizationPass` does for a DXIL array binding -- see that pass's updated header comment and feme/docs/FeMeCPUDesign.md's "Bound-resource normalization". A Vulkan *dynamic* storage/uniform buffer offset needs no shader-side model at all: per "Memory and Buffers" in feme/docs/FeMeVulkanDesign.md, it is folded into `FemeDescriptor::Data` when a host materializes a dispatch's heap, the same way every other buffer's binding offset is, so the existing `BoundResourceRange`/`materializeResourceHeap` model already carries it without a Vulkan-specific reflection record -- answering FeMeVulkanDesign.md's open questions 3 and 7) | "Required SPIR-V resource work"; §1.2 | P0 |
| ~~Everything else in the object model — instance/device/queue,~~ memory, buffers, ~~command pools and buffers, submission, fences~~ (closed by V1's `lib/Vulkan/{Memory,Buffer,CommandBuffer,Sync}.{h,cpp}`), ~~descriptor pools/sets/updates~~ (closed by V2's `lib/Vulkan/Descriptor.{
  h, cpp}`), ~~binary and timeline semaphores, events, query pools~~ (closed by V3's `lib/Vulkan/{Sync,QueryPool}.{h,cpp}`), ~~pipeline cache~~ (closed by V4's `lib/Vulkan/PipelineCache.{
  h, cpp}`, with the "no relocatable object code yet" deviation FeMeVulkanDesign.md's "Pipeline Cache" section's own Status note records) | V0–V4 | P1 |
| ~~Images, image views, layout tracking, copies, storage/sampled images and samplers~~ (closed by V5's `lib/Vulkan/Image.{h,cpp}` plus the `Descriptor.{h,cpp}`/`CommandBuffer.{h,cpp}` extensions it needed, and shader-side consumption closed by R30's follow-up -- a bound sampled image and sampler now reach a real dispatch through the image/sampler heaps; see FeMeVulkanDesign.md's "V5" Status note) | V5 | P1 |
| ~~Graphics, WSI and presentation: FeMeVulkanDesign.md's V6–V8~~ (done: its "Graphics, Presentation, and Window-System Integration" section now specifies the graphics queue family, `VkRenderPass`/dynamic rendering normalized into one render-target binding, graphics pipeline state translation, draw commands, the headless-first WSI decision, and mesh/ray exposure, and V6–V8 are written against G3–G8) | "Graphics, Presentation, and Window-System Integration" (Vulkan) | P1 |
| ~~A real Vulkan-CTS run had never actually happened (every milestone through V6 recorded only the *infrastructure* to run one)~~ (closed, once a VK-GL-CTS checkout became available: see feme/docs/VulkanCTSReport.md. Found and fixed four core commands this ICD had never implemented at all -- each crashed the loader's device dispatch table rather than merely rejecting -- `vkTrimCommandPool`, `vkCreateRenderPass2`'s command family, `vkCreateDescriptorUpdateTemplate`'s command family, and `vkCmdSetLineWidth`/`DepthBias`/`DepthBounds`/`DeviceMask`) | "V4"/"V6" Status notes; VulkanCTSReport.md | P0 |
| ~~An upstream MLIR SPIR-V deserializer bug (`processSpecConstantComposite`, `mlir/lib/Target/SPIRV/Deserialization/Deserializer.cpp`) crashes on any spec-constant composite whose constituents are not themselves spec constants~~ (closed: `spirv.SpecConstantComposite`'s constituents now accept either a spec-constant symbol reference or an inline typed-attribute constant, and the six groups it crashed all run to completion) | VulkanCTSReport.md | P1 |
| ~~An upstream MLIR `spirv`→`llvm` bug: `IComparePattern`/`FComparePattern` built their `llvm.icmp`/`llvm.fcmp` from the *unconverted* operands, so any comparison over a deserialized `si32` produced ill-typed IR~~ (closed; it was 31% of all CTS failures by diagnostic count, though closing it moved rather than removed those failures -- see VulkanCTSReport.md, "Shader compilation") | VulkanCTSReport.md | P1 |
| **27,094 `dEQP-VK` cases fail, in 26 of 54 groups.** No case produces a wrong answer -- every failure is a clean rejection -- but the failure count is the gating number for any conformance claim, and it decomposes into four buckets with very different owners (78.3% shader compilation, 12.4% pipeline state, 7.2% format table, 2.1% API object model) | §1.9.1; VulkanCTSReport.md | P0 |

The SPIR-V import row is the largest single unknown in the Vulkan design, and
it is scheduled as its own milestone (V0.5) *before* V1 precisely because its
outcome — fixing MLIR's deserializer upstream versus translating the SPIR-V
CFG directly to unstructured LLVM IR and leaning on `feme::cpu::PreparePass`'s
existing structurizer — may change V1's design. It is also the one row here
that is a *FeMe* gap rather than a runtime gap, so it stays owned by this
roadmap even though the Vulkan document schedules it.

V0.5 settled that decision in favor of the unstructured path: see
FeMeVulkanDesign.md's "SPIR-V import prerequisites" Status note for the full
writeup, including a second, independent reason (a downstream MLIR
conversion crash on any loop-carried value, not only the originally
documented `OpPhi`-in-loop-merge-block deserialization rejection) that ruled
out "fix the upstream structurizer" as sufficient on its own even before
weighing cost.

#### 1.9.1 The road to Vulkan conformance

[VulkanCTSReport.md](VulkanCTSReport.md) measures where this ICD stands;
this section is the plan that measurement implies. Conformance is not the
same as "no failures": a Vulkan 1.2 conformance submission requires that
the mandatory CTS list run with **zero** `Fail`, *and* that the device
expose every mandatory feature, limit, format and queue capability, *and*
that a stock (unpatched) `deqp-vk` be used. The current run satisfies none
of the three, and the three are not independent -- most mandatory-capability
gaps are themselves the reason a test fails rather than reports
`NotSupported`.

Two framing decisions come first, because they change what the rest of this
list costs:

- **Conformance target version.** This ICD advertises apiVersion 1.2, which
  drags in every extension promoted through 1.2 as mandatory
  (`imagelessFramebuffer`, `uniformBufferStandardLayout`,
  `separateDepthStencilLayouts`, `hostQueryReset`,
  `shaderSubgroupExtendedTypes`, `subgroupBroadcastDynamicId`, `multiview`,
  timeline semaphores at their required limits -- all of which
  `dEQP-VK.info.device_mandatory_features` currently reports missing).
  Dropping the advertised version to 1.0 or 1.1 removes most of that set
  from the critical path at the cost of the claim. **This decision should
  be made before C1 below**, since it determines whether C6 exists at all.
- **Roadmap deviation to record.** FeMeVulkanDesign.md's V4 and V6 both
  frame the CTS as validation of an *intentionally narrow* advertised
  surface. Pursuing conformance inverts that: the surface must grow to the
  mandatory floor, and "reject cleanly" stops being a sufficient answer
  for anything the spec calls mandatory. That inversion is recorded here
  rather than silently applied; FeMeVulkanDesign.md's own scope sections
  stay authoritative for everything *above* the mandatory floor.

The ordered plan. "Cases" is the number of currently-failing `dEQP-VK`
cases the step is the *first* blocker for; because these blockers stack on
the same shaders (see VulkanCTSReport.md's note on the comparison-pattern
fix moving rather than removing 8,369 failures), the column is an upper
bound on each step's standalone value and a lower bound on its value once
its successors land. They do not sum to 27,094.

| # | Step | Cases | Owner | Priority |
|---|---|---:|---|---|
| C1 | ~~**Mandatory formats.** Add `B8G8R8A8_UNORM` (and the rest of the Vulkan mandatory color-attachment/blend table) to `isSupportedColorAttachmentFormat`, and at least one combined depth+stencil format (`D24_UNORM_S8_UINT` or `D32_SFLOAT_S8_UINT`) to `isSupportedDepthAttachmentFormat`/`isSupportedStencilAttachmentFormat`, backing each with a real pack/unpack path in `feme::graphics`. This is the cheapest step by far and unblocks every Amber-based CTS test, which is most of the CTS's own end-to-end coverage~~ (done: `isSupportedColorAttachmentFormat` now accepts `B8G8R8A8_UNORM` and `R10G10B10A2_UNORM` alongside the pre-existing `R8G8B8A8_UNORM`/`R16G16B16A16_SFLOAT`, completing the Vulkan 1.2 mandatory `COLOR_ATTACHMENT_BIT`/`_BLEND_BIT` set; `D24_UNORM_S8_UINT` is now a real combined depth+stencil format via `feme::graphics::packDepthClear`/`packStencilClear` and their `unpack*` inverses, each an independent read-modify-write of its own half of the shared 4-byte word so testing/writing one never corrupts the other; `RenderPass.cpp`'s `buildRenderTargetBinding`, `CommandBuffer.cpp`'s clears, `ImageOps.cpp`'s `vkCmdClearDepthStencilImage`/`vkCmdClearAttachments`, and `Executor.cpp`'s `readDepth`/`writeDepth`/`readStencil`/`writeStencil` all updated to match; see `FeMeVulkanDesign.md`'s "Deviations" note and `FeMeGraphicsDesign.md`'s R33 status note for the updated scope. Whether this actually moves CTS's 1,938-case count is unmeasured until C10 lands continuous measurement -- **measured, and it does not yet**: see VulkanCTSReport.md's "Roadmap C1: measured impact", which traces the remaining blocker to `vkGetPhysicalDeviceFormatProperties` unconditionally reporting zero support for every format (a separate, pre-existing stub CTS's own capability probes consult ahead of `vkCreateRenderPass`/`vkCreateGraphicsPipelines`) and records three newly-uncovered crashes a prototype fix for that stub surfaced, none in the format tables themselves) | 1,938 | `feme/lib/Vulkan/{RenderPass,Format}.cpp`, `feme::graphics` | P0 |
| C2 | ~~**`Uniform`-storage-class blocks.** Teach `feme::spirv::getBufferBlockElementArray`/`getUniformBlockElementStruct` the four shapes glslang actually emits: a `BufferBlock`-decorated struct in `Uniform` (the pre-SPIR-V-1.3 SSBO), a `Block` struct with more than one member, a sized-array member, and a matrix member with `RowMajor`/`ColMajor`/`MatrixStride`. Then arrayed bindings (an array-of-blocks pointer)~~ (done: `feme::spirv::getBufferBlockElement`/`getUniformBlockElement` -- the generalized `getBufferBlockElementArray`/`getUniformBlockElementStruct` -- now recognize glslang's own direct (unwrapped) block shape alongside FeMe's narrower upstream HLSL one: a `Uniform`-class pointer to a `BufferBlock`-decorated struct (the pre-1.3 SSBO spelling); a `Block`/`BufferBlock` struct with more than one member, with no separate wrapper to strip; and a sized-array member. `StorageBufferAccessChainPattern`/`UniformBufferAccessChainPattern` merged into one `BlockAccessChainPattern`, since both shapes reduce to the same "selector, then an ordinary GEP for anything past it" access once the selector's own index position is known. A `spirv.MatrixType` -> `llvm` dialect conversion (MLIR upstream has none at all) covers matrix members, converting to the natural column-major array-of-vectors representation LLVM's SPIRV backend itself expects; a `RowMajor` member (whose physical layout that representation cannot reproduce by reinterpreting the same bytes) or a `MatrixStride` that does not match the natural per-column stride declines the whole struct's conversion rather than miscompiling it. An array-of-blocks binding (`T blocks[N]` in GLSL) is handled by a new `ArrayedBlockAccessChainPattern`, which builds the handle itself once its own access chain's leading (array) index is available -- unlike a non-arrayed block, an arrayed one's handle needs to know *which* descriptor to bind, which the type converter alone cannot supply. See `Design.md`'s "Known gap: `spirv` dialect -> `llvm` dialect conversion coverage" for the updated scope. Whether this actually moves the 10,121-case count is unmeasured until C10 lands continuous measurement, and -- per this row's own note about C1's stacked-blocker discovery -- is only a lower bound on this step's own value in any case) | 10,121 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P0 |
| C3 | ~~**Divergent-vector decomposition in `feme-cpu-simdize`.** The single largest *FeMe-owned* compute-track gap, already tracked as "roadmap milestone 7 deviation" by the pass's own diagnostic (§1.6). Conformance makes it P0 rather than P1: it is the first blocker for a third of all failures once C2 lands~~ (done: `feme::cpu::SIMDizePass` now decomposes a divergent `phi` of vector type (the shape a uniform diamond's merge block gives a value reconciled across two divergent arms -- the common case a `LinearizePass`-flattened conditional produces) into one per-component `phi` (`FunctionWidener::createWidenedVectorPHIStub`/`fillWidenedVectorPHIIncoming`); a `select` of vector type with a scalar `i1` condition into one per-component `select` sharing that condition (`widenVectorSelect`); a `shufflevector` at compile time into a selection among its two operands' own components, since its mask is always a compile-time constant in LLVM IR, with no runtime cost at all (`widenShuffleVector`); and a non-constant-index `extractelement` into a `select` chain over the widened index against each component's compile-time position (`widenExtractElement`), closing the "a shuffle or a dynamic index becomes selects across the components" gap FeMeCPUDesign.md's "Vectors become components, not nested vectors" always described. A `getVectorComponents` helper, shared by every one of these and by the pre-existing `insertelement`/resource-load producers, builds a uniform vector's components on demand exactly like `getWidened` does for a scalar. A `select` with a per-lane `<N x i1>` condition remains diagnosed (no shape that reaches this pass produces one, and decomposing it would need a per-component condition, not just a per-component value), and every divergent aggregate of any kind is unaffected -- see FeMeCPUDesign.md's deviation note and Roadmap.md's §1.6 table for the updated scope. `test/Transforms/CPU/simdize-vector-{phi,select,shufflevector,dynamic-extractelement}.ll` and `SIMDizeTest.{
  DecomposesVectorPHIAcrossUniformDiamond,
      DecomposesScalarConditionVectorSelect,
      DecomposesShuffleVectorAtCompileTime,
      WidensNonConstantIndexExtractElementIntoSelectChain}` cover the four newly-supported shapes; `simdize-vector-unsupported.ll` now covers the one shape (a vector-condition `select`) that remains diagnosed. Whether this actually moves the 9,067-case count is measured in VulkanCTSReport.md's "Roadmap C3: measured impact") | 9,067 | `feme/lib/Target/CPU` | P0 |
| C4 | **Graphics pipeline state breadth.** `mapTopology` beyond triangles (point, line, line-strip, fan), `mapDynamicState` beyond its six states, `FRONT_AND_BACK` culling, dual-source blend factors, and the sample counts `isSupportedAttachmentSampleCount` declines. Every one of these is a rasterizer/executor feature, not a translation gap, so this is really G-track work surfaced by the Vulkan track. ~~**Sub-step C4a, do first and separately: make every silent rejection diagnose itself.** A state-side rejection currently emits nothing at all, so triaging this bucket means reading `GraphicsPipeline.cpp` instead of the ICD's output~~ (C4a done: `feme::vulkan::logCreationFailure` (`feme/lib/Vulkan/Diagnostics.h`/`.cpp`) is the opt-in log callback FeMeVulkanDesign.md's "Error Handling and Security" section already named ahead of it existing; `vkCreateGraphicsPipelines` calls it instead of a bare `consumeError`, printing the discarded `llvm::Error`'s message when `FEME_VULKAN_LOG_CREATION_ERRORS` is set in the host environment and staying silent otherwise, so triaging a state-side rejection no longer requires reading this file's source -- see FeMeVulkanDesign.md's updated bullet for the full deviation note, including why an environment variable rather than `VK_EXT_debug_utils` itself. C4b done in part: `CullMode::FrontAndBack` (culls every primitive, matching `VK_CULL_MODE_FRONT_AND_BACK`'s "no primitive of the pipeline's type is rasterized" semantics) and 8x multisampling (`samplePositions`' documented "mechanical, on-demand addition of another row" in `Executor.cpp`, `isSupportedAttachmentSampleCount` in `RenderPass.cpp`, and the matching `VK_SAMPLE_COUNT_8_BIT` advertisement in `PhysicalDeviceInfo.cpp`) are both implemented and tested. ~~`mapDynamicState` beyond its six states... each needs new rasterizer primitives... that are a materially larger, separate unit of G-track work rather than a mechanical table addition~~ (C4c done, and this framing turned out to be wrong for dynamic state specifically: every one of `VK_EXT_extended_dynamic_state`'s 12 dynamic states -- cull mode, front face, depth test/write/compare-op, depth-bounds-test-enable, stencil test-enable/op, viewport/scissor "with count", primitive topology restricted to the triangle class already implemented, and vertex-input-binding-stride via `vkCmdBindVertexBuffers2EXT` -- already had a complete *static* path or a bounded, mechanical extension of one before this milestone, so all 12 are now implemented and the extension is advertised (`feme/lib/Vulkan/{GraphicsPipeline,CommandBuffer,PhysicalDeviceInfo,EntryPoints}.{
  h, cpp}`, `feme/utils/vk_gen_entrypoints.py`) -- see FeMeGraphicsDesign.md's updated status note for the full per-state breakdown. ~~`mapTopology` beyond `TriangleList`/`TriangleStrip`... each needs new rasterizer primitives (point/line assembly and width, a second fragment-stage color output) that are a materially larger, separate unit of G-track work rather than a mechanical table addition~~ (C4d done, and this framing held for only one of the four remaining topologies: `TriangleFan` needed no new rasterizer primitive at all, just a different per-primitive vertex-index assembly sharing the existing triangle clip/rasterize path; point and line topologies do need a new primitive shape, but `executeDraws` gets one by expanding each into a two-triangle screen-space quad and feeding it through that same existing path, fixed at a 1-pixel point size/line width since `largePoints`/`wideLines` are not advertised device features -- see FeMeGraphicsDesign.md's updated status note for the full breakdown, including the one accepted deviation (no side-plane clip for points/lines, only a whole-primitive near-plane reject). ~~Only the dual-source blend factors remain open: they need a second fragment-stage color output that is a materially larger, separate unit of G-track work rather than a mechanical table addition~~ (C4e done: the missing piece was narrower than that framing suggested -- the `Index` SPIR-V decoration already survived `spirv` -> `llvm` conversion unmodified, so the only real gap was `SignatureElement` gaining an `Index` field and `CanonicalizeStage.cpp` reflecting the decoration into it; `executeDraws` now reads a fragment stage's `Index=1` output at `Location=0` for `VK_BLEND_FACTOR_SRC1_*`, and `dualSrcBlend` is an advertised device feature -- see FeMeGraphicsDesign.md's updated status note for the full breakdown). Roadmap C4 is now closed in full. | 3,354 | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme::graphics` | P0 |
| C5 | **Mandatory API object model.** ~~Occlusion queries in `vkCreateQueryPool` (mandatory in 1.0)~~ (done: `feme::vulkan::QueryPool` now accepts `VK_QUERY_TYPE_OCCLUSION` and accumulates the exact per-sample depth/stencil-test-pass count across draws recorded between `vkCmdBeginQuery`/`vkCmdEndQuery`, reusing V6's real rasterizer coverage and test results; timestamp queries remain truthful zero-valued results, and `VK_QUERY_TYPE_PIPELINE_STATISTICS` still declines because this ICD has no truthful counter for it); ~~the descriptor types `isSupportedDescriptorType` declines (input attachment, dynamic uniform/storage buffer)~~ (done: the dynamic uniform/storage buffer types were already present; the only remaining gap, `VK_DESCRIPTOR_TYPE_INPUT_ATTACHMENT`, is now accepted and materialized as the same read-only image-view+layout record as `SAMPLED_IMAGE`); ~~the `VkRenderPass` shapes `feme::vulkan::RenderPass` declines (resolve and input attachments, multi-subpass dependencies)~~ (done: resolve attachments and multi-subpass execution were already in place; `vkCreateRenderPass`/`vkCreateRenderPass2` now also accept and retain input-attachment references, and validate subpass dependencies -- including out-of-range `srcSubpass`/`dstSubpass` and `VkSubpassDependency2::viewOffset`/`VK_DEPENDENCY_VIEW_LOCAL_BIT` -- before collapsing them to this ICD's existing full-join semantics); ~~the `VkSubgroupFeatureFlags` contradiction (`BASIC_BIT` must be set whenever a graphics or compute queue exists)~~ (done: both `VkPhysicalDeviceSubgroupProperties` and the promoted `VkPhysicalDeviceVulkan11Properties` chain now report the same compute-stage `VK_SUBGROUP_FEATURE_BASIC_BIT` baseline); `VkPhysicalDeviceDriverProperties` remains only partially closed (done: the struct and the promoted `VkPhysicalDeviceVulkan12Properties` chain are now queryable, with non-empty null-terminated `driverName`/`driverInfo` strings and a truthful zero `VkConformanceVersion`; remaining deviation: FeMe still has no Khronos-assigned `VkDriverId`, so `driverID` stays `VK_DRIVER_ID_MAX_ENUM` rather than impersonating another driver, and the CTS `api.driver_properties.conformance_version` case still fails on the deliberate zero) | 558 | `feme/lib/Vulkan/{QueryPool,Descriptor,RenderPass,PhysicalDeviceInfo}.cpp` | P0 |
| C6 | ~~**Mandatory 1.2 features and limits.** Only if the version decision above keeps 1.2: `multiview`, `imagelessFramebuffer`, `uniformBufferStandardLayout`, `separateDepthStencilLayouts`, `hostQueryReset`, `shaderSubgroupExtendedTypes`, `subgroupBroadcastDynamicId`, plus raising `maxTimelineSemaphoreValueDifference` and `maxMemoryAllocationSize` to their required minimums and fixing the `vkGetPhysicalDeviceFeatures2`-versus-promoted-struct disagreements~~ (done, with one deliberate exception: `hostQueryReset` (`vkResetQueryPool` already existed), `uniformBufferStandardLayout`/`separateDepthStencilLayouts` (neither restriction they relax was ever enforced), `shaderSubgroupExtendedTypes`/`subgroupBroadcastDynamicId` (vacuously true -- no `OpGroupNonUniform*` operation is converted at all yet), and `imagelessFramebuffer` (`vkCreateFramebuffer`/`vkCmdBeginRenderPass` now defer a framebuffer's attachment views to `VkRenderPassAttachmentBeginInfo`, needing no layered-rendering support since it stays single-layer) are all now advertised and implemented; `maxMemoryAllocationSize`, `maxPerSetDescriptors`, `maxMultiviewViewCount`/`maxMultiviewInstanceIndex`, and `maxTimelineSemaphoreValueDifference` (`UINT64_MAX`) are raised to or above their required minimums in both their dedicated structs and the promoted `VkPhysicalDeviceVulkan11Properties`/`VkPhysicalDeviceVulkan12Properties` chains, closing the disagreement between them. **`multiview` stays unadvertised**: it needs layered rendering (roadmap V7, not yet implemented -- `vkCreateFramebuffer`'s `layers != 1` and `vkCreateRenderPass2`'s `viewMask != 0` rejections are both untouched), so the feature bit alone would be untruthful even though `maxMultiviewViewCount`/`maxMultiviewInstanceIndex` must still report real minimums once the advertised API version is >= 1.2, regardless of `multiview` itself -- see FeMeVulkanDesign.md's updated "Limits and features" and "Render passes and dynamic rendering" status notes, and VulkanCTSReport.md's "Roadmap C6: measured impact" for the CTS effect) | 28 | `feme/lib/Vulkan/PhysicalDeviceInfo.cpp`, V7 | P1 |
| C7 | ~~**Queue family capability combinations.** 99,324 cases report `NotSupported` because no queue family matches a required capability set. A family advertising `GRAPHICS` must also advertise `TRANSFER`; the mandatory combinations must all be coverable. These are `NotSupported`, not `Fail`, today, so they cost nothing in the failure count -- but a conformance run that declines a hundred thousand mandatory cases is not a conformance run~~ (done: the universal family already advertised `GRAPHICS`-implies-`TRANSFER`; the actual gap was that no family *excluded* graphics/compute at all, which several mandatory `dEQP-VK` cases require (e.g. `dEQP-VK.pipeline.*.timestamp.transfer_tests.*_transfer_queue`, `dEQP-VK.api.buffer_marker.compute.*`). `feme::vulkan::PhysicalDeviceInfo` now reports two more, narrower families alongside the universal one -- a `TRANSFER`-only family and a `COMPUTE | TRANSFER`-only family, both excluding `GRAPHICS` -- neither of which claims an independent execution engine the single worker pool does not have; each only restricts what one logical submission queue promises to accept. `vkCreateDevice`/`vkCreateCommandPool` now validate against `PhysicalDeviceInfo::NumQueueFamilies` instead of hardcoding "only family 0 exists". Measured impact: every `findQueueFamilyIndexWithCaps`-shaped `NotSupported` this pass could reach (`dEQP-VK.pipeline.monolithic.timestamp`, `api`, `synchronization`, `synchronization2`, `renderpasses`, `sparse_resources`, `fragment_shading_rate`) dropped to zero, save two cases needing a dedicated video-decode queue -- correctly `NotSupported`, since video extensions are not advertised at all; see VulkanCTSReport.md's "Roadmap C7: measured impact". Reaching further also surfaced and closed one CTS-side null-function-pointer crash (`dEQP-VK.api.copy_and_blit.copy_commands2.image_to_image_transfer_queue.misc.ms_then_ss`, a real, previously-unreachable local CTS gap -- see "Deviations from a stock CTS") | 0 (99,324 `NotSupported`) | `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P1 |
| C8 | **Shader long tail.** Descriptor arrays of combined image samplers (816), matrix/aggregate stage IO (309), the SPIR-V importer's `unhandled opcode` set (171), `Workgroup` arrays-of-arrays (151), the 277 individually-unlegalized ops (`spirv.SpecConstant`, `spirv.VectorExtractDynamic`, `spirv.CompositeConstruct`, the `spirv.Atomic*` family, ...), and the 242-case diagnostic tail. Best attacked *after* C2/C3, since the true size of this bucket is unknown until the stacked blockers ahead of it are gone. ~~**C3's own measurement found a new member of this bucket, larger than any row already in it**: a plain, non-atomic `load`/`store` on a raw SPIR-V `Input`/`Output`-storage-class global (address space 7/8, see `getStageIOAddressSpace` in SPIRVToLLVMPatterns.cpp) is never canonicalized into the `feme.stage.*` calls `feme::cpu::LinearizePass`/`SIMDizePass` already know how to widen the way a DXIL/HLSL-imported shader's stage IO always is (`feme::dxil::OpRaisingPass`) -- so a SPIR-V-imported fragment/vertex shader's own divergent output store hits `feme-cpu-simdize`'s vector-decomposition diagnostic (or, for a scalar output, presumably a similar unmasked-side-effect gap) not because vector decomposition itself is incomplete, but because the value it is being asked to decompose was never routed into the mechanism that already knows what to do with it. This is a *different* root cause from C3's own scope (which is genuinely closed -- see FeMeCPUDesign.md's deviation note) and from this row's existing matrix/aggregate-stage-IO entry (a `spirv`-\>`llvm` *conversion* gap, not a CPU-target *raising* gap); it is the largest single reason C3's own headline barely moved despite closing every producer/consumer shape "Vectors become components" describes~~ (done: the actual root cause was narrower, and more general, than this finding's own framing -- it was never specific to SPIR-V. `feme::graphics::CanonicalizeStagePass` (the pass that rewrites both a DXIL `loadInput`/`storeOutput` call *and* a raw SPIR-V `Input`/`Output`-storage-class global load/store into `feme.stage.*`) was never run by `feme::cpu::runPipeline` *at all* -- only by the separate Vulkan graphics pipeline (`GraphicsPipeline.cpp`), which every real CTS draw call goes through, but which the CPU-target pipeline `feme-run`/`feme-cpu-simdize` measure against does not. A DXIL-imported fragment/vertex shader reaching this pipeline directly hit the exact same gap SPIR-V's did (`checkSupportedRaisedOps` diagnosing the still-raw `dx.op.loadInput`/`storeOutput` call outright, rather than `feme-cpu-simdize`'s vector-decomposition diagnostic reaching a raw store the way SPIR-V's did) -- the roadmap text's framing of DXIL's stage IO as "always" canonicalized this way did not hold once traced through this pipeline specifically, only through the Vulkan one. `runPipeline` now runs `CanonicalizeStagePass` immediately before `ValidateStagePass`, matching the ordering "CPU Lowering Pipeline"'s own diagram in FeMeGraphicsDesign.md already drew, closing this gap for both import paths with one change (`feme/lib/Target/CPU/Pipeline.cpp`); see that document's updated status note for the full deviation record and `unittests/Target/CPU/PipelineTest.cpp`'s `CanonicalizesRaw{SPIRV,DXIL}StageIOBeforeWidening` for the two regression tests, each first confirmed failing (in its own distinct way) against the pre-fix pipeline. Measured against a real `deqp-vk` run, this fix moves **nothing**: `feme::vulkan::compileGraphicsStage` (`GraphicsPipeline.cpp`) already calls `CanonicalizeStagePass` directly, since roadmap V6, before every real `vkCreateGraphicsPipelines` call reaches `runPipeline` at all, so no `dEQP-VK` case was ever routed through the gap this fix closes -- it only mattered for `feme::cpu::JITEngine`/`feme-run`'s direct entry points. The C3 section's own attribution of its stalled headline to this finding does not hold once measured, and the rest of C8's bucket -- descriptor arrays of combined image samplers, matrix/aggregate stage IO, the remaining `spirv`-\>`llvm` conversion gaps, and the broader unlegalized-op/diagnostic tail -- remains open, exactly as large as before; see VulkanCTSReport.md's "Roadmap C8: measured impact" for the full before/after comparison) | 1,966 | mixed, mostly `feme/lib/Conversion/SPIRVToLLVM` and `mlir/lib/Target/SPIRV` | P1 |
| C8a | ~~**This milestone's own named "matrix/aggregate stage IO (309)" row.** `feme::graphics::CanonicalizeStagePass`'s SPIR-V-side rewrite (`CanonicalizeStage.cpp`) only ever recognized a scalar or `FixedVectorType` `Input`/`Output` global; a matrix (`ArrayType` of column vectors, the shape SPIRVToLLVM's `spirv.MatrixType` conversion produces) or a single-member-struct-wrapped variant (glslang's own shape for a `varying`-block *member*, confirmed against a real `dEQP-VK.glsl.linkage.varying.struct.mat4x2` run: the LLVM type is `{
  [4 x<2 x float>] }`) fell through to a wrong default (`ComponentCount`/`RowCount` both 1, the whole aggregate treated as one opaque "scalar" store) -- the exact "aggregate type" `feme-cpu-simdize` diagnostic and out-of-range `feme-graphics-validate-stage` diagnostics this row's own bucket already named~~ (done: `getStageIORowShape`/`peelSingleMemberStruct` recognize both shapes and set the signature element's `RowCount` to the matrix's column count; `loadStageIOValue`/`storeStageIOValue` recursively decompose the aggregate into one `feme.stage.input.load`/`output.store` per (row, component), the same shape a plain vector already used, one level further out. `feme::graphics::Executor.cpp`'s CPU-target raster executor -- which had explicitly and unconditionally rejected any `RowCount != 1` element outright (`buildStageStorage`) -- now supports it end to end: `StageStorage::readRaw`/`writeRaw` take a `Row` parameter, `LinkedVarying` carries `RowCount`, and the vertex-output -> fragment-input link/interpolate/clip path (`vertexAt`, the quad interpolation loop, and -- a bug caught only by a real triangle-draw regression test, not the unit-level signature tests -- `lerpVertex`'s own clip-time interpolation, which does not receive `LinkedVarying` and so needed its own explicit row loop) is now `Row`-aware throughout. A vertex *attribute* matrix (bound from a vertex buffer, needing one `VkVertexInputAttributeDescription` per column at consecutive locations) is a distinct, still-unimplemented shape and is still rejected explicitly, not silently mishandled. New regression tests: `CanonicalizeStageTest.RewritesSPIRVMatrix{Output,Input}{Store,Load}OneRowAtATime`, `CanonicalizeStageTest.RewritesSPIRVSingleMemberStructWrappedMatrixOutputStore`, `ExecutorTest.InterpolatesConstantColorPackedInAMatrixVarying`. Measured against a real `deqp-vk.glsl` run (26,808 cases): every occurrence of the "aggregate type"/"out of range for element" diagnostics this gap produced (63 total) is gone, with the group's `Passed`/`Failed`/`Not supported` totals byte-for-byte unchanged (13,921 `Failed` both before and after) -- every affected case now fails one step later, at `feme-cpu-simdize`'s own pre-existing divergent-vector-decomposition diagnostic (roadmap C3's scope, not fully closed for this pattern -- see the new C8b row below) instead of this row's aggregate-legalization gap. See VulkanCTSReport.md's "Roadmap C8a: measured impact" for the full before/after comparison) | 63 (of C8's 309) | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Graphics/Executor.cpp` | P1 |
| C8b | **The actual blocker C8a's own measurement exposed**: even once a matrix/aggregate stage-IO value is correctly legalized into `feme.stage.*` calls (C8a), `feme::cpu::SIMDizePass` still cannot widen it -- its divergent-vector decomposition (roadmap C3's own scope) recognizes four producer/consumer shapes (`insertelement`-chain, resource-store, `extractelement`/`select`/`shufflevector`/`phi`, and plain elementwise), but a matrix/aggregate's own `insertvalue`/`extractvalue` chain is a fifth shape none of the four cover, so every one of C8a's newly-legalized cases still fails, just later and with a different diagnostic. This looks like the same shape roadmap H2a already flagged for `dEQP-VK.multiview`'s fragment shaders (454 of 838 cases, also *not* one of C3's four closed patterns) -- triage the two together before assuming they are the same root cause | H2a, C3, C8a | `feme/lib/Transforms/CPU/SIMDize.cpp` | P1 |
| C9 | **Upstream the CTS fix.** `dEQP-VK.api.invariance.random` segfaults on any narrow-format ICD because CTS's own `ImageAllocator` indexes an empty format vector; the fix is applied locally to the VK-GL-CTS checkout today. A conformance submission must run a stock CTS, so this must land in VK-GL-CTS itself | 0 | VK-GL-CTS | P1 |
| C10 | **Continuous measurement.** Nothing in this tree runs the CTS automatically: `feme/test/Vulkan/cts-compute-subset.test` is gated on `REQUIRES: system-vulkan-cts` and covers a compute subset only. A conformance push needs the full 54-group run, its per-case root-cause attribution, and a checked-in expected-failure list, so that a regression is a test failure rather than a re-reading of this report | 0 | `feme/utils`, `feme/test/Vulkan` | P1 |

Sequencing: C1 first (cheapest, largest end-to-end unlock, and a hard
conformance requirement rather than a scope choice), then C4a (so the
remaining triage is readable), then C2 and C3 in parallel (different
subsystems, and jointly ~70% of the failure count), then C4, C5 and C7,
then C6 if the 1.2 claim survives, then C8 measured afresh, with C9 and C10
running alongside from the start. C10 is what keeps this report from
needing to be regenerated by hand again.

What is *not* on this list, deliberately: WSI/presentation, ray tracing,
mesh shading, tessellation, geometry, sparse resources, YCbCr,
transform feedback, `VK_EXT_shader_object` and cooperative matrix/vector.
Every one is optional for Vulkan 1.2 conformance, every one is correctly
reported `NotSupported` today, and each remains scheduled by its own V or G
milestone rather than by conformance.

**That paragraph no longer holds in full, and is kept as written because
the C-series was planned under it.** The declared scope is now full Vulkan
1.4 conformance including graphics and ray tracing
([FeMeVulkanDesign.md](FeMeVulkanDesign.md)'s "Conformance Target"), so
WSI, ray tracing, mesh shading, tessellation and geometry are tracked gaps
with assigned rows (§1.9.7's H-series, §1.9.8's J-series) rather than
milestone-scheduled options. Sparse resources, YCbCr, transform feedback,
`VK_EXT_shader_object` and cooperative matrix/vector are still out of
scope, and §1.9.7's H12 is the row that has to say so once, on the record,
instead of leaving it to be re-decided per CTS run.

#### 1.9.2 The road to Vulkan 1.4 conformance

§1.9.1's own framing decision -- "Conformance target version... This
decision should be made before C1" -- was answered by keeping 1.2
throughout C1-C8. Roadmap D0 revisits it: the advertised `apiVersion` is
now `VK_API_VERSION_1_4`, ahead of C1-C8's own discipline of only
advertising a version once its mandatory surface was actually
implemented (1.0 -> 1.1 -> 1.2 each happened that way; 1.2 -> 1.4 did
not). This section is the plan that inversion now needs, mirroring
§1.9.1's own shape but starting from an honest accounting of what a
version-ahead-of-implementation claim costs, measured rather than
assumed.

| # | Step | Owner | Priority |
|---|---|---|---|
| D0 | ~~**Advertise `VK_API_VERSION_1_4`, and fix whatever it immediately breaks.**~~ (done: `vkEnumerateInstanceVersion`/`VkPhysicalDeviceProperties::apiVersion` now report 1.4. This alone made `deqp-vk` exercise `VK_KHR_copy_commands2`'s six commands for the first time -- they have no feature-bit gate of their own, so a device claiming `apiVersion >= 1.3` is assumed to implement them unconditionally -- and neither the pre-promotion `KHR`-suffixed names nor the promoted core names existed in this ICD at all, producing a real segfault. Fixed as part of this same step: all six now delegate to their already-implemented, already-tested non-`2` counterparts. A second, distinct crash was found and left open by this step -- see D2) | `feme/lib/Vulkan/{EntryPoints,PhysicalDeviceInfo,CommandBuffer}.cpp`, `vk_gen_entrypoints.py`; VulkanCTSReport.md's "Roadmap D0: measured impact" | P0 |
| D1 | ~~**An accurate 1.3/1.4 mandatory-feature/limit/extension inventory.** `vk_gen_entrypoints.py`'s `CORE_FEATURES` now resolves through `VK_VERSION_1_3` (D0), but `VK_VERSION_1_4` is not yet included, and no promoted-1.3/1.4 feature struct (`VkPhysicalDeviceVulkan13Features`/`Vulkan14Features` and their per-extension originals -- `dynamicRendering` is already advertised via its pre-promotion `VK_KHR_dynamic_rendering` path, but `synchronization2`, `maintenance4`/`5`/`6`, `subgroupSizeControl`, `shaderIntegerDotProduct`, `pipelineCreationCacheControl`, `pushDescriptor`, and the rest are not) has been audited against what claiming 1.4 actually requires. This is D0's own "measure honestly" step turned into a checklist: enumerate the full set from `vk.xml` itself (the same way `vk_gen_entrypoints.py` already resolves `CORE_FEATURES` transitively), rather than re-deriving it by hand the way C6 did for 1.2's much shorter list~~ (done: `CORE_FEATURES` now includes `VK_VERSION_1_4` itself, closing the entrypoint-table coverage gap this row's own premise named -- purely a coverage fix, since an unlisted name and a listed-but-unimplemented one both already resolved to null via `ProcAddr.cpp`'s `findEntry`. The actual audit is a new tool, `feme/utils/vk_gen_feature_inventory.py`, enumerating every `VkPhysicalDeviceVulkan{13,14}Features`/`...Properties` struct member and every extension `vk.xml` records as `promotedto` that version, cross-checked against a checked-in `feme/lib/Vulkan/AdvertisedPromoted{Features,Extensions}.txt`; see `feme/docs/Vulkan14FeatureInventory.md` for the generated checklist and `FeMeVulkanDesign.md`'s updated "Limits and features" status note for the summary. Headline: of 1.3/1.4's 36 mandatory feature bits, only `dynamicRendering` is genuinely implemented (and only through its pre-promotion `VK_KHR_dynamic_rendering` struct, not yet the aggregate one); all 70 mandatory limit fields are unenumerated (neither promoted `...Properties` struct has a `vkGetPhysicalDeviceProperties2` case); of 39 promoted extensions, only the 2 already advertised are implemented. This milestone is the inventory itself, not closing what it found -- see the doc's own "Findings" for the full per-row breakdown that future roadmap rows will triage against) | `feme/utils/vk_gen_entrypoints.py`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P0 |
| D2 | ~~**The system Vulkan loader crash D0's second CTS pass found.** `dEQP-VK.api.object_management.multithreaded_per_thread_resources.*`, run as one sequence, segfaults inside Ubuntu's `libvulkan1` (`vkGetDeviceProcAddr`, called from concurrent `vkCreateDevice`s) -- not inside any FeMe code, and does not reproduce against the pre-D0 (apiVersion 1.2) build. Characterize further (does it reproduce with a *smaller* `apiVersion`-dependent entrypoint table than 1.4's full one? does a newer/older `libvulkan1` avoid it?) and, if confirmed as a loader bug rather than something this ICD's own dispatch-table generation can influence, file it upstream rather than attempt a local workaround in a component this project does not own~~ (done: confirmed as a race (3/5 repeated runs crash, not 5/5), with a `gdb` backtrace identical to D0's (`vkGetDeviceProcAddr` <- `vk::DeviceDriver::DeviceDriver` <- a `ThreadGroupThread` constructing its own `VkDevice`, no FeMe frame anywhere). Trimming `CORE_FEATURES` back to `VK_VERSION_1_3` (a smaller generated table) and rebuilding `feme_vulkan` did **not** reduce the crash rate against the same system loader (4/5 still crashed) -- ruling out table size as the deciding variable and superseding D0's "larger dispatch table" guess. Traced to an *already-tracked, already-fixed* upstream defect, KhronosGroup/Vulkan-Loader#1436/#1438 (a `-fno-strict-aliasing`-removal regression in the loader's own Release-build GPA code, fixed in loader tag `v1.3.277`): building loader `v1.3.280` from source cut the rate to 1/6, and building current upstream `main` (v1.4.360-era, with further mutex hardening since `v1.3.280`) eliminated it across 10/10 runs. Ubuntu 24.04's `libvulkan1` package is pinned to the pre-fix `1.3.275.0` with no newer candidate in `noble`/`noble-updates`/`noble-backports`. Not filed as a *new* upstream issue -- it would duplicate the already-closed #1436 -- since the actionable gap is Ubuntu's packaging lag, not anything left for Vulkan-Loader or this ICD to fix; see VulkanCTSReport.md's "Roadmap D2: measured impact" for the full loader-build comparison table and backtraces) | VulkanCTSReport.md's "Roadmap D2: measured impact" | P1 |
| D3 | ~~**Per-bucket attribution of D0's net +2,553 newly-failing cases**, at C1-C8's level of rigor. One bucket is already traced (`ubo.*.std430`'s 2,650 cases: a pre-existing, already-tracked `feme-cpu-simdize` divergent-vector-decomposition gap, C3's own "milestone 7 deviation", newly *reached* rather than newly created); `spirv_assembly.instruction.compute` (417), `synchronization.op.{multi,single}_queue` (277), and the remaining tail are not yet traced~~ (done, with a correction: re-running the *exact* pre-D0/post-D0 commits against the currently-checked-out CTS (which has since drifted one local commit past what D0's own report cites) does **not** reproduce a net +2,553 -- it reproduces a net **-417** (525 newly-`Fail`, 942 no-longer-`Fail`), with every group individually attributed. Two of D0's three named buckets do not survive re-verification at the same rigor D0 itself used for `ubo.*.std430`: `ubo.*.std430` itself is **not** apiVersion-gated at all -- `feme-cpu-simdize`'s divergent-vector diagnostic fires identically at 1.2 and 1.4 (a byte-identical `ubo` group log both ways), so its 2,650 cases were already failing pre-D0 and this bucket should never have been attributed to the version bump; `synchronization.op.{multi,single}_queue` likewise reproduces an *identical* 498-case failing set at both versions (confirmed deterministic across repeated isolated runs), so its 277-case attribution does not hold up either. `spirv_assembly.instruction.compute` **does** reproduce (422 cases, close to the original 417): rooted to `SPIRVToLLVMPatterns.cpp`'s `ImageFetchPattern`/`ImageFetchLodPattern` (and the analogous `ImageSampleExplicitLod` patterns) requiring an exact image-operands match and having no case at all for the SPIR-V 1.6 `Nontemporal` hint bit deqp-vk's SPIR-V-1.6-only (`apiVersion >= 1.3`) shaders now emit -- a cache hint with no correctness effect, currently unhandled anywhere in this ICD. Three further newly-failing buckets D0 did not name: `graphicsfuzz` (72, `VK_KHR_shader_terminate_invocation` assumed implemented once promoted-to-1.3, per D1's inventory it is not); `dEQP-VK.api.info.*`'s own `vulkan1p3`/`get_physical_device_properties2` consistency checks (18, exactly the "vulkan1p3_consistency" shape D0's first draft guessed and discarded -- it materializes in `api.info.*`, not the separate top-level `info` group D0 actually checked); `compute.pipeline.zero_initialize_workgroup_memory` (7, same "promoted extension assumed implemented" shape as `graphicsfuzz`, per D1's inventory); and `robustness.oob_access` (6, a texel-buffer-format/robustness mismatch). A large, undercounted no-longer-failing side (942 cases -- D0's report never measured a complete `api` group, so its 1,999 figure could not have included this) turned out to be a good-news case of the same D1-tracked gap: `draw`/`renderpasses`/`pipeline`'s `dynamic_rendering` cases (613 + 204 + 5) go from a crash-adjacent `VK_ERROR_INITIALIZATION_FAILED` Fail to a correctly-truthful `NotSupported`, because deqp-vk's own support check for `VK_KHR_dynamic_rendering` consults `VkPhysicalDeviceVulkan13Features.dynamicRendering` once `apiVersion >= 1.3`, and FeMe's `vkGetPhysicalDeviceFeatures2` has no case at all for that blob struct's type (D1's own finding), so it reads back `false` even though `dynamicRendering` is truthfully advertised through its pre-promotion struct. See VulkanCTSReport.md's "Roadmap D3: measured impact" for the full per-bucket table, root causes, and the reproducibility caveat) | VulkanCTSReport.md's "Roadmap D3: measured impact" | P1 |
| D4 | **Continuous, crash-tolerant measurement**, generalizing roadmap C10: this pass's own two full runs each needed a hand-rolled Python pass over raw `.log` files to diff two runs' actual per-case result sets against each other, since a single crashed group's totals silently corrupt naive count-only comparisons (see D0's own "diffing the two runs' actual per-case result sets, not just the aggregate counts" step) | `feme/utils`, `feme/test/Vulkan` | P1 |

Sequencing: D1 first (an inventory is a prerequisite for every subsequent
1.3/1.4 feature-implementation row this section will eventually need, none
of which are listed yet since D1 hasn't produced them), D2 and D3 alongside
it (independent of D1, and each already has a concrete lead), D4 whenever
time allows since it only pays off on the *next* full run. Every row this
section will need beyond D1's own inventory -- implementing
`synchronization2`, `maintenance4`/`5`/`6`, `dynamicRendering`'s core
(non-`KHR`) names, and the rest of §1.9.2's actual conformance-floor work
-- is deliberately not listed yet, the same way C1-C8's own rows were not
listed until §1.9.1 existed to schedule them against a similarly-measured
baseline.

D1, D2 and D3 are now done, and §1.9.2's own promise -- "every row this
section will need beyond D1's own inventory is deliberately not listed yet"
-- is what §1.9.3-§1.9.5 below deliver: the actual, distributable task
breakdown, derived directly from `Vulkan14FeatureInventory.md`'s findings
and D3's regression attribution, at a granularity intended for
parallel work by independent agents and humans rather than a single
sequential owner.

#### 1.9.3 Framing decision this breakdown does not get to skip

§1.9.1 flagged "conformance target version... should be made before C1" and
C1-C8 honored it: 1.0 -> 1.1 -> 1.2 each only advertised once its floor was
implemented. D0 broke that discipline (apiVersion jumped straight to 1.4
ahead of any 1.3/1.4 feature work), and D1's inventory is the honest
accounting of what that costs: **36 mandatory feature bits with 1
implemented, 70 mandatory limit fields with 0 wired, 39 promoted
extensions with 2 implemented.** D3 additionally found that the version
bump alone made three previously-passing buckets regress into `Fail`
(`spirv_assembly.instruction.compute`, `graphicsfuzz`,
`compute.pipeline.zero_initialize_workgroup_memory`, 501 cases total) by
making `deqp-vk` assume promoted-but-unimplemented capabilities are real.

This roadmap does **not** revert the version bump -- undoing D0 would
throw away D1-D3's own measurements and restart the same climb from a
different number -- but every row below is written so a distributor can
choose either of two strategies without redoing the inventory:

- **Strategy A (recommended): finish §1.9.4 (the full 1.3 floor) before
  starting any §1.9.5 (1.4-only) row**, actually reaching 1.3 conformance
  as a real, submittable milestone before continuing to 1.4, mirroring
  C1-C8's own "advertise what's implemented" discipline one level higher
  instead of abandoning it a second time.
- **Strategy B: run both tracks in parallel** if the distributor has
  enough independent agents/humans that §1.9.4 and §1.9.5 do not compete
  for the same reviewer bandwidth -- the two tracks touch almost entirely
  disjoint files (§1.9.5's rows are new `lib/Vulkan/*` cases and new
  `EntryPoints.cpp` command stubs; only E1/E2's struct plumbing is a
  shared dependency both tracks reuse, so it should land first regardless
  of which strategy is chosen).

Either way, **E1 and E2 below are the one true prerequisite**: they are
the aggregate feature/limit struct plumbing every other row in both
§1.9.4 and §1.9.5 registers itself into, the same "one small addition,
every subsequent row plugs into it" role R11's `Diagnostic`/`Exporter`
scaffolding played for §1.1-§1.5.

#### 1.9.4 Closing the Vulkan 1.3 mandatory floor (E-series)

Each row is independently assignable: one agent or human, one feature (or
a tightly-coupled pair sharing one code path), one commit series, one
lit/unittest addition, following `.instructions.md`'s "small, individually
testable" rule. "CTS" cites the specific regression D3 traced to that gap,
where one is known; most rows have no known regression today because the
capability is simply unadvertised (`NotSupported`, not `Fail`) rather than
lied about, so their CTS payoff is newly-*passing* mandatory cases, not
newly-fixed failures, and is unmeasured until each row's own CTS run.

| # | Task | Depends on | Files | Priority |
|---|---|---|---|---|
| E1 | ~~**Wire the aggregate `VkPhysicalDeviceVulkan13Features`/`Vulkan14Features` `vkGetPhysicalDeviceFeatures2` cases.** Today only the pre-promotion `VK_KHR_dynamic_rendering` struct case exists; add the two aggregate cases, populating every member from whatever per-extension state already exists (`dynamicRendering=VK_TRUE` today) and `VK_FALSE` for everything still unimplemented, exactly mirroring the existing `VULKAN_1_2_FEATURES` case's pattern. This is what `dEQP-VK.api.info.*`'s consistency checks and every `VK_KHR_dynamic_rendering`-support probe (`draw`/`renderpasses`/`pipeline`, 822 cases) actually read~~ (closed: both aggregate cases now exist in `EntryPoints.cpp`, mirroring `VULKAN_1_2_FEATURES`'s explicit-every-field pattern; `dynamicRendering=VK_TRUE`, every other 1.3/1.4 bit `VK_FALSE`) | none | `feme/lib/Vulkan/EntryPoints.cpp` | P0 |
| E2 | ~~**Wire the aggregate `VkPhysicalDeviceVulkan13Properties`/`Vulkan14Properties` `vkGetPhysicalDeviceProperties2` cases**, enumerating all 70 mandatory limit fields from `Vulkan14FeatureInventory.md`'s table. Most are either a real minimum this ICD can already compute (e.g. `maxBufferSize`, `storageTexelBufferOffsetAlignmentBytes`) or a truthful `VK_FALSE`/`0` for a capability not yet implemented (every `integerDotProduct*Accelerated` bit until E8 lands, `maxPushDescriptors` until F12 lands). Land the struct case with every field set to a conservative, honest value first, then let each later row (E8, F5, F11, F12, ...) raise its own subset once the feature behind it is real, instead of blocking this row on every other one~~ (closed, with a correction to this row's own premise: both aggregate cases now exist in `EntryPoints.cpp` -- following `VULKAN_1_2_PROPERTIES`'s own explicit-every-field pattern -- with every one of the 46 (1.3) + 25 (1.4) fields written, but *every* field is the conservative `0`/`VK_FALSE`/`nullptr`, not a mix of real minima and placeholders as this row originally proposed. A first draft did compute real values for a handful of fields (`maxBufferSize`, `storageTexelBufferOffsetAlignmentBytes`, `minSubgroupSize`, ...) from state this ICD already tracks; a targeted CTS run caught the reason that was wrong before landing it: `dEQP-VK.api.info.vulkan1p3/1p4.property_extensions_consistency` cross-checks *every* aggregate-struct field against its own pre-promotion, per-extension dedicated struct (`VkPhysicalDeviceSubgroupSizeControlProperties`, `VkPhysicalDeviceTexelBufferAlignmentProperties`, `VkPhysicalDeviceMaintenance4Properties`, `VkPhysicalDeviceLineRasterizationPropertiesKHR`, ...) once apiVersion >= 1.3/1.4, the same way `vulkan1p3.feature_extensions_consistency` cross-checked `dynamicRendering` before E1 -- and none of those dedicated structs has its own `Properties2` case yet, so they all still read back zero. A real, nonzero aggregate-struct value disagrees with that zero and regresses a currently-passing consistency case instead of closing one. See "Roadmap E2: measured impact" in VulkanCTSReport.md for the full before/after. Each field's own later row (E4, E6, E7, E8, E18, F5, F6, F8, F10, F11, F12) is therefore the one that gets to raise it, in lockstep with adding that row's own dedicated-struct case) | none | `feme/lib/Vulkan/EntryPoints.cpp` | P0 |
| E3 | ~~**`VK_KHR_synchronization2`/`synchronization2`.** `vkCmdPipelineBarrier2`/`vkCmdWriteTimestamp2`/`vkQueueSubmit2`/`vkCmdSetEvent2`/`vkCmdResetEvent2`/`vkCmdWaitEvents2` translate `VkDependencyInfo`'s per-resource `VkMemoryBarrier2`/`VkBufferMemoryBarrier2`/`VkImageMemoryBarrier2` (2-stage-mask, 2-access-mask shape) down to the existing 1-mask `Sync.{h,cpp}` model, the same "new entrypoint, old backing model" pattern C7 used for queue families~~ (done, with a correction to this row's own premise: all six commands were already core, non-`KHR`-suffixed `VK_VERSION_1_3` entries in `vk_gen_entrypoints.py`'s generated table -- like `VK_KHR_copy_commands2` before it (roadmap D0), this needed no `vk_gen_entrypoints.py`/`SUPPORTED_EXTENSIONS` change, only `ImplementedEntrypoints.txt` entries and real `CommandBuffer.cpp`/`Sync.cpp` bodies. Each image barrier's layout transition (`vkCmdPipelineBarrier2`) and each event/wait-events/write-timestamp command translates straight down to the identical payload its non-`2` counterpart already produces; `vkQueueSubmit2`'s per-element `VkSemaphoreSubmitInfo`/`VkCommandBufferSubmitInfo` structs translate down to the same `Fence`/`Semaphore`/`CommandBuffer` execution `vkQueueSubmit` itself now shares through two small `Sync.cpp`-local helpers. Unlike D0's precedent, though, `getSupportedDeviceExtensions` *does* need `VK_KHR_synchronization2` added -- `dEQP-VK.synchronization2`'s own multi-queue/custom-device cases enable it by name at `vkCreateDevice` regardless of the advertised `apiVersion`, discovered by a targeted CTS run before landing (see "Roadmap E3: measured impact" in VulkanCTSReport.md). `EntryPoints.cpp` also gained the dedicated `VkPhysicalDeviceSynchronization2Features` case, agreeing with `synchronization2 = VK_TRUE` in the aggregate `VkPhysicalDeviceVulkan13Features` struct E1 already wired, closing the last `api.info` consistency gap D1's inventory found for this bit)~~ | E2 (for the feature bit) | `feme/lib/Vulkan/Sync.{h,cpp}`, `CommandBuffer.cpp`, `EntryPoints.cpp`, `PhysicalDeviceInfo.cpp` | P0 |
| E4 | ~~**`VK_KHR_maintenance4`/`maintenance4`.** `vkGetDeviceBufferMemoryRequirements`/`vkGetDeviceImageMemoryRequirements`/`vkGetDeviceImageSparseMemoryRequirements` compute requirements from a `VkBufferCreateInfo`/`VkImageCreateInfo` directly (no live object needed) by factoring the existing `vkCreateBuffer`/`vkCreateImage` sizing logic into a shared helper both the live and the info-only entrypoint call; also relaxes `vkCreateShaderModule`'s local-size-id/local-size validation and `VK_KHR_maintenance4`'s zero-size-descriptor-array rule~~ (closed, with a correction to this row's own two "relaxes" clauses: the zero-size-descriptor-array rule needed no change at all -- `vkCreateDescriptorSetLayout`/`vkAllocateDescriptorSets` already accept `descriptorCount == 0`, closed by a regression test alone (`DescriptorTest.AcceptsZeroSizeReservedBinding`) -- and `GroupSize.cpp`'s own `LocalSizeId` resolution was *also* already correct and unit-tested (`GroupSizeTest.ResolvesFromLocalSizeIdDefaults`); what actually needed relaxing, found only once `PipelineTest.CompilesLocalSizeIdComputeShader` exercised the whole compile pipeline rather than `GroupSize.cpp`'s raw-word scan alone, was `SPIRVToLLVMPatterns.cpp`'s legalization: neither `spirv.SpecConstant` (`LocalSizeId`'s only way to spell its operands) nor `spirv.ExecutionModeId` has an upstream MLIR conversion pattern, so a new `ExecutionModeIdPattern`/`SpecConstantErasurePattern` pair erases both, mirroring the existing `ExecutionModePattern` precedent for plain `LocalSize`. The three new entrypoints themselves landed as designed, sharing `isValidBufferCreateInfo`/`computeBufferMemoryRequirements` (Buffer.cpp) and `isValidImageShape`/`computeImageCreateInfoSize`/`fillImageMemoryRequirements` (Image.cpp) with the existing live `vkCreate*`/`vkGet*MemoryRequirements(2)` bodies -- but a targeted CTS run (`dEQP-VK.api.invariance.memory_dedicated_requirements_matching`) found a real gap the row's own premise didn't anticipate: none of the four `vkGet*MemoryRequirements(2)`/`vkGetDevice*MemoryRequirements` entrypoints ever walked their `VkMemoryRequirements2` output's `pNext` chain, so a chained `VkMemoryDedicatedRequirements` disagreed between the live and info-only calls purely because neither touched it. `Memory.h`/`.cpp`'s new `fillMemoryRequirements2PNextChain`, shared by all four, closed it. `maintenance4` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and its own dedicated `VkPhysicalDeviceMaintenance4Features` struct, and `maxBufferSize` (both the aggregate `VkPhysicalDeviceVulkan13Properties` and dedicated `VkPhysicalDeviceMaintenance4Properties` cases) now reads the same real host-memory-size value `VkPhysicalDeviceMaintenance3Properties::maxMemoryAllocationSize` already did, rather than E2's placeholder `0`. See "Roadmap E4: measured impact" in VulkanCTSReport.md for the full before/after) | none | `feme/lib/Vulkan/{Buffer,Image,Memory,EntryPoints}.cpp`, `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P1 |
| E5 | ~~**`VK_KHR_maintenance5`/`maintenance5`.** Chiefly `VkRenderingAttachmentInfo::imageView == VK_NULL_HANDLE` (an attachment slot present but unused, needing `RenderPass.cpp`'s dynamic-rendering path to skip rather than reject a null view), `VK_FORMAT_A8_UNORM`/`A1B5G5R5_UNORM_PACK16`, and `vkCmdBindIndexBuffer2` (a `size`-bounded variant of the existing bind, sharing `CommandBuffer.cpp`'s existing validation minus the "whole buffer" assumption)~~ (closed, with a correction to this row's own file attribution: the null-view skip actually lives in `CommandBuffer.cpp`, not `RenderPass.cpp` -- `normalizeRenderingAttachment` already produced a `RenderTargetView` with a null `View` for a null-imageView slot, but every downstream consumer (the load-op clear, and each draw's per-attachment write and multisample resolve) unconditionally called `resolveAttachmentView` on it and failed; each now skips the slot instead, using the empty-`AttachmentView` "not bound" convention `DepthStencilAttachment` already established, so no live image is required for it. The two new formats slot into the existing `Format.cpp`/`ImageFixture.cpp`/`RenderPass.cpp` format-table machinery exactly like C1's own mandatory formats did, packed the same way `R10G10B10A2_UNORM` already is (one opaque word, special-cased ahead of the generic per-component loop). `vkCmdBindIndexBuffer2` shares `CommandBuffer::bindIndexBuffer`'s recording (`DstSize` reused for its `size`, the same way it already is for `FillBuffer`'s size and `DrawIndirect`'s stride) and `runDraw`/`validateDrawFetchBounds`'s bounds checking, narrowing the readable range to `[offset, offset + size)` instead of assuming "through the end of the buffer" when `size != VK_WHOLE_SIZE`. `maintenance5` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan14Features` struct and its own dedicated `VkPhysicalDeviceMaintenance5FeaturesKHR` struct; none of `VkPhysicalDeviceMaintenance5PropertiesKHR`'s fixed-function rasterizer guarantees are verified yet, so that struct (and the aggregate `VkPhysicalDeviceVulkan14Properties` case's matching fields) stay the conservative `VK_FALSE` E2 established) | E2 | `feme/lib/Vulkan/{RenderPass,CommandBuffer,Format}.cpp` | P1 |
| E6 | ~~**`VK_KHR_maintenance6`/`maintenance6`.** `vkCmdBindDescriptorSets2`/`vkCmdPushConstants2`/`vkCmdPushDescriptorSet2` are shape-compatible wrappers around the existing `Descriptor.cpp`/`CommandBuffer.cpp` entrypoints, taking a `pNext`-extensible info struct instead of a flat argument list; `maxCombinedImageSamplerDescriptorCount`'s reporting is the one new limit (E2 already reserves the field)~~ (closed, with a correction to this row's own scope: `vkCmdPushDescriptorSet2` is deliberately left unimplemented, per this row's own fallback clause -- F12's `pushDescriptor` groundwork has not landed, so `getSupportedDeviceExtensions` advertises `VK_KHR_maintenance6` without it, exactly the same "an extension's optional sub-mechanism can lag its own row" precedent C4c's own 12-of-12 completeness note does not set but F11/F12's own dependency notes anticipate. `vkCmdBindDescriptorSets2`/`vkCmdPushConstants2` share `CommandBuffer::bindDescriptorSets`/`pushConstants`'s existing recording unchanged: neither `VkBindDescriptorSetsInfo`'s `stageFlags` nor `VkPushConstantsInfo`'s `layout`/`stageFlags` needs translation, since this model already stores one shared set of bound descriptor sets/push-constant bytes across every pipeline bind point (see `vkCmdBindDescriptorSets`'s own comment). `maxCombinedImageSamplerDescriptorCount` is a real `1` (this ICD supports no multi-planar/YCbCr samplers, so a combined image sampler descriptor always consumes exactly one slot), in both the aggregate `VkPhysicalDeviceVulkan14Properties` case and a new dedicated `VkPhysicalDeviceMaintenance6Properties` case; `maintenance6` reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan14Features` struct and a new dedicated `VkPhysicalDeviceMaintenance6Features` struct. A targeted CTS run found one real, pre-existing gap this row's own scope does not cover: `dEQP-VK.api.command_buffers.secondary_push_constants_2` fails pipeline creation (`VK_ERROR_INITIALIZATION_FAILED`) on a non-array, single-`vec4`-field `std430` storage buffer block -- the same "resource handle the FeMe CPU target cannot normalize" class of failure that already accounts for 73 of this same CTS group's 77 failures (e.g. every `indirect_compute_dispatch_offsets_*` case), not a regression `vkCmdPushConstants2` itself introduces; see "Roadmap E6: measured impact" in VulkanCTSReport.md) | E2, E12 (push descriptor sets need F12's `pushDescriptor` groundwork first if implemented together, otherwise stub `PushDescriptorSet` count `0`) | `feme/lib/Vulkan/{Descriptor,CommandBuffer}.cpp` | P1 |
| E7 | ~~**`VK_EXT_subgroup_size_control`/`subgroupSizeControl` + `computeFullSubgroups`.** `VkPipelineShaderStageRequiredSubgroupSizeCreateInfo` lets a compute pipeline request an explicit subgroup size; `GroupSize.cpp` (already the home of subgroup-size computation, per its name) needs an override path, and `minSubgroupSize`/`maxSubgroupSize`/`maxComputeWorkgroupSubgroups`/`requiredSubgroupSizeStages` (E2's placeholders) become real once this lands~~ (closed, with a correction to this row's own file attribution: the override path actually lives in `Pipeline.cpp`, not `GroupSize.cpp` -- `GroupSize.cpp`'s `resolveComputeGroupSize` resolves a shader's *workgroup* size (`LocalSize`/`LocalSizeId`/`BuiltIn WorkgroupSize`), a different quantity from a dispatch's *subgroup* (wave) size, which this row's own name-pun premise ("`GroupSize.cpp` ... per its name") conflated; no change to `GroupSize.cpp` was needed. `Pipeline.cpp`'s `compileComputePipeline` now reads a chained `VkPipelineShaderStageRequiredSubgroupSizeCreateInfo` and forwards it to `feme::cpu::JITOptions::WaveSize` (already validated by `feme::cpu::resolveWaveSize`), and rejects a `VK_PIPELINE_SHADER_STAGE_CREATE_REQUIRE_FULL_SUBGROUPS_BIT` pipeline whose workgroup size in X is not a multiple of the resolved subgroup size; `PipelineCache.cpp`'s cache key now folds in both to avoid a stale hit. `minSubgroupSize`/`maxSubgroupSize`/`maxComputeWorkgroupSubgroups`/`requiredSubgroupSizeStages` are real (`4`/`128`/`32`/`VK_SHADER_STAGE_COMPUTE_BIT`) in both the aggregate `VkPhysicalDeviceVulkan13Properties` case and a new dedicated `VkPhysicalDeviceSubgroupSizeControlProperties` case; a targeted CTS run found 8 pre-existing, out-of-scope `Fail` cases newly reached (a `spirv.SpecConstantComposite` for a shader-body-referenced `BuiltIn WorkgroupSize`, which no conversion pattern lowers) -- see "Roadmap E7: measured impact" in VulkanCTSReport.md for the full before/after) | E2 | `feme/lib/Vulkan/Pipeline.cpp`, `PipelineCache.{h,cpp}`, `EntryPoints.cpp`, `PhysicalDeviceInfo.{h,cpp}` | P1 |
| E8 | ~~**`VK_KHR_shader_integer_dot_product`/`shaderIntegerDotProduct`.** The largest single limit cluster in E2's placeholder set (36 of the 70 fields are `integerDotProduct*Accelerated` bits): decide, measure, and report truthfully whether this CPU target's `OpSDot`/`OpUDot`/`OpSUDot`-family lowering (new `spirv`->`llvm` conversion patterns, since none exist today per `Vulkan14FeatureInventory.md`) is actually hardware-accelerated (likely `VK_FALSE` for all 36 on a CPU executor -- a truthful "supported but not accelerated" is a valid, conformant answer and cheaper than claiming acceleration this target cannot deliver)~~ (closed, exactly as this row's own premise anticipated: `SPIRVToLLVMPatterns.cpp` gained six new patterns -- `spirv.SDot`/`spirv.UDot`/`spirv.SUDot` and their `*AccSat` counterparts, none of which upstream MLIR converts at all, the same "MLIR has no pattern for this op" gap `DotConversionPattern` already closed for the unrelated float `spirv.Dot` -- each lowering to a per-lane sign/zero-extend, multiply, and add chain (a scalar 32-bit operand, legal only with the `PackedVectorFormat4x8Bit` format, is unpacked into its four constituent bytes first), with a final `llvm.intr.sadd.sat`/`uadd.sat` for the `*AccSat` variants. `shaderIntegerDotProduct` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDeviceShaderIntegerDotProductFeatures` struct; `getSupportedDeviceExtensions` gained `VK_KHR_shader_integer_dot_product` itself, needed for the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6 already established (`dEQP-VK.spirv_assembly.instruction.compute`'s own `vktSpvAsmIntegerDotProductTests.cpp` requests it explicitly). This row's own prediction held exactly: all 36 `integerDotProduct*Accelerated` bits stay a truthful `VK_FALSE`, in both the aggregate `VkPhysicalDeviceVulkan13Properties` case and a new dedicated `VkPhysicalDeviceShaderIntegerDotProductProperties` case -- this CPU target runs every one of the six new patterns as an ordinary scalar multiply-add sequence, not a hardware-accelerated one. A targeted CTS run confirms this is a fully conformant answer, not merely a cheaper one: `dEQP-VK.spirv_assembly.instruction.compute`'s six new dot-product groups (1,248 cases within this ICD's advertised operand-width scope) pass 80/0/1,168 (Pass/Fail/NotSupported), zero `Fail`, and `api.info.get_physical_device_properties2.features.shader_integer_dot_product_features` flips from `Fail` (D1/D3's own bucket) to `Pass` -- see "Roadmap E8: measured impact" in VulkanCTSReport.md for the full breakdown) | E2, §1.2 (new `spirv` dialect conversion patterns) | `feme/lib/Conversion/SPIRVToLLVM`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P1 |
| E9 | ~~**`VK_EXT_pipeline_creation_cache_control`/`pipelineCreationCacheControl`.** `VK_PIPELINE_CREATE_2_FAIL_ON_PIPELINE_COMPILE_REQUIRED_BIT`/`VK_PIPELINE_CACHE_CREATE_EXTERNALLY_SYNCHRONIZED_BIT` are flag-only additions to `GraphicsPipeline.cpp`/`Pipeline.cpp`'s existing creation path and `PipelineCache.{h,cpp}`'s existing cache object -- no new object model, purely accepting and honoring two new bits~~ (closed, exactly as this row's own premise anticipated: both bits are flag-only additions, with no new object model. `vkCreateComputePipelines`/`vkCreateGraphicsPipelines` (Pipeline.cpp/GraphicsPipeline.cpp) now check `VK_PIPELINE_CREATE_FAIL_ON_PIPELINE_COMPILE_REQUIRED_BIT` (the 32-bit `VkPipelineCreateFlags` spelling suffices; no chained `VkPipelineCreateFlags2CreateInfo` is needed for a bit already within the first 32) on a cache miss (or with no cache at all, which can never hit), reporting `VK_PIPELINE_COMPILE_REQUIRED` and leaving that pipeline null instead of compiling for real, without inserting anything into the cache; a more severe result elsewhere in the same batch (a real compile failure, or out-of-host-memory) still wins, per the extension's own spec -- `compileGraphicsPipeline`'s return type changed to `Expected<std::optional<GraphicsPipelineState>>` so a skipped-compile `std::nullopt` is distinguishable from a real `Error` at the call site. `PipelineCache` gained a mutex (and an `ExternallySynchronized` constructor parameter `vkCreatePipelineCache` now threads `VK_PIPELINE_CACHE_CREATE_EXTERNALLY_SYNCHRONIZED_BIT` into) guarding `lookup`/`insert`/`lookupGraphics`/`insertGraphics` -- the four accessors `vkCreateComputePipelines`/`vkCreateGraphicsPipelines` call -- taken unless that bit is set, since `pipelineCache` is not one of those two commands' own externally-synchronized parameters by default (unlike `vkMergePipelineCaches`'s `dstCache`/`pSrcCaches` and `vkGetPipelineCacheData`'s `pipelineCache`, which always are per the base spec regardless of this extension, so `merge`/`keys` needed no lock at all). `pipelineCreationCacheControl` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDevicePipelineCreationCacheControlFeatures` struct; `getSupportedDeviceExtensions` gained `VK_EXT_pipeline_creation_cache_control` itself, for the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6/E8 already established. See "Roadmap E9: measured impact" in VulkanCTSReport.md for the CTS run) | none | `feme/lib/Vulkan/{GraphicsPipeline,Pipeline,PipelineCache}.{h,cpp}`, `EntryPoints.cpp`, `PhysicalDeviceInfo.cpp` | P2 |
| E10 | ~~**`VK_EXT_private_data`/`privateData`.** `VkPrivateDataSlot` is a new, small object (an opaque per-(object-handle) `uint64_t` map) alongside the existing object model in `Objects.h`; `vkCreatePrivateDataSlot`/`vkSetPrivateData`/`vkGetPrivateData`/`vkDestroyPrivateDataSlot` are new, self-contained entrypoints with no dependency on any other row here~~ (closed, with one file-attribution correction to this row's own premise: `PrivateDataSlot` lives entirely in its own new `PrivateData.{h,cpp}`, not "alongside the existing object model in `Objects.h`" -- it needed no change to `Objects.h` at all, since it never dereferences the `(VkObjectType, uint64_t handle)` pair it is keyed on and so has no dependency on what `Objects.h`'s existing classes represent. All four entrypoints (already core, non-`EXT`-suffixed `VK_VERSION_1_3` names `vk_gen_entrypoints.py`'s `CORE_FEATURES` resolves) landed as designed. `privateData` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDevicePrivateDataFeatures` struct; `getSupportedDeviceExtensions` gained `VK_EXT_private_data` itself, the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6/E8/E9 already established. A targeted CTS run confirms this closes D3's own `api.info.*` bucket for this bit (`private_data_features` flips from `Fail` to `Pass`) and all five `vulkan1p3.*` consistency cases pass, agreeing with the aggregate struct rather than repeating E2's own first-draft regression; `dEQP-VK.api.object_management.private_data.*` passes 37/40, with the 3 non-passes (2 `Fail`, 1 `NotSupported`) all pre-existing, out-of-scope gaps unrelated to this row's own bits (E6/E9's own SIMD-lowering/GEP gap, C3/D3's own divergent-vector "milestone 7 deviation", and an unimplemented `imageCubeArray`, respectively). See "Roadmap E10: measured impact" in VulkanCTSReport.md for the full breakdown) | none | `feme/lib/Vulkan/PrivateData.{h,cpp}`, `EntryPoints.cpp`, `PhysicalDeviceInfo.cpp` | P2 |
| E11 | ~~**`VK_EXT_shader_demote_to_helper_invocation`/`shaderDemoteToHelperInvocation`.** SPIR-V's `OpDemoteToHelperInvocation` needs a new `spirv`->`llvm` conversion pattern (mark the invocation inactive for further side effects, matching HLSL `discard`'s existing non-terminating semantics rather than DXIL's `discard`'s current lowering, if one exists -- audit `SPIRVToLLVMPatterns.cpp` first)~~ (closed, with one premise correction: the audit found no existing `spirv`->`llvm` pattern for *either* `OpKill` or `OpDemoteToHelperInvocation` at all -- and MLIR's own upstream SPIR-V dialect had no op at all for `OpDemoteToHelperInvocation`, despite already having its `Capability`/`Extension` enum cases, so `mlir::spirv::deserialize` would have rejected any real module using it. This row therefore added `spirv.DemoteToHelperInvocation` to MLIR itself (a non-terminator, unlike the deprecated `spirv.Kill`) alongside a new `llvm.spv.demote.to.helper.invocation` LLVM intrinsic (mirroring `llvm.spv.discard`'s shape, but kept distinct since this ICD's own import direction needs to preserve the terminating-vs-non-terminating distinction the emit-direction backend collapses when selecting between `OpKill`/`OpDemoteToHelperInvocation`). `SPIRVToLLVMPatterns.cpp`'s new `DemoteToHelperInvocationConversionPattern` converts the op to that intrinsic; `CanonicalizeStage.cpp` raises it into `feme.stage.demote(true)` -- unconditional, matching `llvm.spv.discard`'s own existing raising -- whose reference/SIMD lowering (`StageOpKind::Demote`) already existed. `shaderDemoteToHelperInvocation` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDeviceShaderDemoteToHelperInvocationFeatures` struct; `getSupportedDeviceExtensions` gained `VK_EXT_shader_demote_to_helper_invocation` itself, the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6/E8/E9/E10 already established. `OpTerminateInvocation` (roadmap E12) remains a separate, unimplemented op. See "Roadmap E11: measured impact" in VulkanCTSReport.md for the CTS run) | §1.2 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P1 |
| E12 | ~~**`VK_KHR_shader_terminate_invocation`/`shaderTerminateInvocation`.** SPIR-V's `OpTerminateInvocation` (a true terminator, unlike `OpDemoteToHelperInvocation`) needs its own conversion pattern lowering to an unconditional discard-and-return. **Closes D3's `graphicsfuzz` 72-case regression** (CTS assumes this promoted-to-1.3 extension is real once `apiVersion >= 1.3`)~~ (closed, exactly as this row's own premise anticipated -- with one premise correction the audit found, the same shape E11's own audit found: no `spirv`->`llvm` conversion pattern existed for `OpTerminateInvocation` at all, and MLIR's own upstream SPIR-V dialect had no op for it either, so a new `spirv.TerminateInvocation` op (a true terminator, requiring no capability beyond the existing `Shader` one) had to be added to MLIR itself first. `SPIRVToLLVMPatterns.cpp`'s new `TerminateInvocationConversionPattern` then converts it into exactly the unconditional discard-and-return this row's own premise specified: a call to the same `llvm.spv.discard` intrinsic `OpKill` itself would use (already raised into `feme.stage.discard(true)` by the existing `CanonicalizeStagePass` renaming, unmodified by this milestone), followed by an `llvm.return`. `shaderTerminateInvocation` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDeviceShaderTerminateInvocationFeatures` struct; `getSupportedDeviceExtensions` gained `VK_KHR_shader_terminate_invocation` itself, the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6/E8/E9/E10/E11 already established. A targeted CTS run found a *second* premise correction, to the `graphicsfuzz` row's own D3-era characterization rather than this row's: all 72 `OpTerminateInvocation`-using `graphicsfuzz` cases fail identically at Amber's own pre-flight color-attachment-format check, before any pipeline is created or shader executed -- the same pre-existing, already-documented `vkGetPhysicalDeviceFormatProperties` stub gap that blocks the entire 757-case `graphicsfuzz` group uniformly (confirmed against a 20-case control sample unrelated to this extension), not a wrong-image mismatch, and out of this row's own scope to fix; see "Roadmap E12: measured impact" in VulkanCTSReport.md) | §1.2 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P0 |
| E13 | ~~**`VK_KHR_zero_initialize_workgroup_memory`/`shaderZeroInitializeWorkgroupMemory`.** `Workgroup`-storage-class SPIR-V globals need a zero-initializer emitted once per dispatch (likely in `feme::cpu::SPIRVResourceLoweringPass` or a small new pass run before it) rather than reading whatever the host's memory allocator happened to leave behind. **Closes D3's `compute.pipeline.zero_initialize_workgroup_memory` 7-case regression**~~ (closed, with a premise correction the audit found, a different shape than E11/E12's own missing-conversion-pattern one: `Workgroup`-storage-class `spirv.GlobalVariable`s had no conversion pattern at all -- MLIR's own upstream `GlobalVariablePattern` supports only `Input`/`Private`/`Output`/`StorageBuffer`/`UniformConstant` -- so `feme::spirv::WorkgroupGlobalVariablePattern` (SPIRVToLLVMPatterns.cpp) had to be added first, converting one to an ordinary `llvm.mlir.global` in address space 3 (the same convention Clang's own HLSL `groupshared` codegen already uses), alongside a new pointer-type conversion routing an access chain through the same address space. Importing the zero-initializer itself needed two further MLIR-level fixes: `spirv.GlobalVariable` gained `zero_initialized`, a new unit attribute representing the one further shape SPIR-V's `Initializer` operand permits (a plain `OpConstantNull`, which has no symbol of its own to reference, unlike the existing `initializer` symbol-reference case), and `processConstantNull` (the deserializer) was generalized to build a null value for a composite (`spirv.array`/`spirv.struct`) type too, not just a scalar/vector/tensor one -- both real, independent gaps, not specific to this row's own feature. `feme::cpu::GroupSharedLayout` gained `NeedsZeroInit` (GroupShared.h), read off the imported global's own `hasInitializer()`; `feme::cpu::EntryWrapperPass` `memset`s the whole flat groupshared buffer to zero, once per group, when it is set (EntryWrapper.cpp) -- deliberately over-broad (zeroing every groupshared global in the module, not just the flagged one) but still correct, since a groupshared global with no zero-initializer of its own was already free to read as anything. `shaderZeroInitializeWorkgroupMemory` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDeviceZeroInitializeWorkgroupMemoryFeatures` struct; `getSupportedDeviceExtensions` gained `VK_KHR_zero_initialize_workgroup_memory` itself, the same "CTS enables it by name regardless of `apiVersion`" reason E3/E5/E6/E8/E9/E10/E11/E12 already established. A targeted CTS run found this row's own "Closes ... 7-case regression" text undercounts both directions: the real `compute.pipeline.zero_initialize_workgroup_memory` group is 644 cases, of which 4 (`types.{bool,float32_t,int32_t,uint32_t}`, the exact scalar zero-init shape this row's own scope targets) now genuinely `Pass`, while the remainder stay `Fail`/`NotSupported` behind two further pre-existing, out-of-scope gaps this row's own audit found but does not fix: `OpTypeArray`'s Length operand accepting only a normal constant, not the specialization constant every non-scalar case in this CTS source sizes its array from; and a vector/matrix `shared` variable's own element access producing a vector (not scalar) GEP base LLVM's translation rejects. See "Roadmap E13: measured impact" in VulkanCTSReport.md for the full per-subgroup breakdown, including a third, genuine robustness gap (an `i1`-composite GEP crashing `mlir::translateModuleToLLVMIR` outright) found and isolated but left unfixed, same as the other two) | §1.2/§1.6 | `feme/lib/Target/CPU` | P0 |
| E14 | ~~**`VK_EXT_inline_uniform_block`/`inlineUniformBlock` + `descriptorBindingInlineUniformBlockUpdateAfterBind`.** A new `VkDescriptorType` whose "descriptor" is inline byte storage rather than a handle -- `Descriptor.{h,cpp}`'s existing per-binding storage needs a byte-blob variant alongside its current handle-array one, and `VkWriteDescriptorSetInlineUniformBlock` is a new `pNext` case in the existing `vkUpdateDescriptorSets` walk~~ (closed, exactly as this row's own premise anticipated -- with one further mechanical extension the premise did not spell out: `vkUpdateDescriptorSetWithTemplate` and `VkCopyDescriptorSet`'s own copy path (both in Descriptor.cpp, sharing the same object model) needed the identical byte-ranged special case as `vkUpdateDescriptorSets` itself, since all three read `dstArrayElement`/`srcArrayElement`/`descriptorCount` as byte offsets/counts rather than array indices/counts for this one descriptor type. `DescriptorSet` gained `InlineUniformBlockBindings` (a `std::map<uint32_t, std::vector<uint8_t>>`), sized from the layout binding's own `descriptorCount` reinterpreted as a byte size per spec. `inlineUniformBlock` now reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan13Features` struct and a new dedicated `VkPhysicalDeviceInlineUniformBlockFeatures` struct, alongside real (spec-minimum-floor) `VkPhysicalDeviceInlineUniformBlockProperties` limits agreeing with the aggregate `VkPhysicalDeviceVulkan13Properties` case; `descriptorBindingInlineUniformBlockUpdateAfterBind` stays `VK_FALSE` -- no update-after-bind/descriptor-indexing mechanism exists anywhere in this ICD yet, so there is no honest "yes" to give that bit independent of the rest of that extension family. `getSupportedDeviceExtensions` gained `VK_EXT_inline_uniform_block` itself. This milestone's own scope is the descriptor object model only, per its "closes: none": no `feme::cpu::SPIRVResourceLoweringPass` conversion consumes an inline uniform block from a real dispatch yet, the same "object model first, shader consumption later" shape V5's image/sampler descriptor types already established (`FeMeVulkanDesign.md`'s Descriptor Model table updated to match). A targeted CTS run found a genuine, in-scope limits bug this row's own premise did not anticipate: `dEQP-VK.api.info.vulkan1p2_limits_validation.ext_inline_uniform_block` requires `maxPerStageDescriptorUpdateAfterBindInlineUniformBlocks`/`maxDescriptorSetUpdateAfterBindInlineUniformBlocks` to meet the same `>= 4` floor as their non-`UpdateAfterBind` counterparts *unconditionally*, independent of `descriptorBindingInlineUniformBlockUpdateAfterBind`'s own value -- unlike Vulkan 1.2's own descriptor-indexing `UpdateAfterBind` limits, which stay `0` uncontested alongside a `VK_FALSE` `descriptorIndexing`; fixed by reporting both equal to their non-`UpdateAfterBind` counterparts. Of the 136 `*inline_uniform_block*`-named cases run, 3 genuinely `Pass` (the feature/property advertisement itself), 17 fail at graphics pipeline creation (this ICD is compute-only, unrelated to this row), and 8 (`descriptor_copy.compute.inline_uniform_block_*`) fail cleanly at compute pipeline creation -- a real compute shader consuming the binding, exactly the deferred-dispatch-consumption scope this row's own text states. See "Roadmap E14: measured impact" in VulkanCTSReport.md for the full breakdown, including a full `dEQP-VK.api.*`/`dEQP-VK.binding_model.*` regression check finding no crash or new failure from advertising this extension) | none | `feme/lib/Vulkan/Descriptor.{h,cpp}` | P1 |
| E15 | ~~**`VK_EXT_texture_compression_astc_hdr`/`textureCompressionASTC_HDR`.** A new decode path in `Format.{h,cpp}` for the 14 ASTC HDR block formats, reusing whatever LDR ASTC decode already exists (check `Format.cpp` first — if LDR ASTC is itself unimplemented, this row's scope grows to include it and should be split)~~ (superseded by a split, per this row's own verify-first gate and G4: `Format.cpp` has no ASTC decode of any kind, LDR or HDR — in fact no block-compressed format is recognized at all (no BC/ETC2 either), and `mapVkFormat`'s doc comment (Format.h) independently confirms this table exists to feed `ResourceFormat` in `feme/include/feme/Target/CPU/RuntimeABI.h`, whose enum likewise has zero compressed entries. The scope explosion is larger than this row's own premise anticipated: it is not only "LDR ASTC decode is a prerequisite", but that `Image.{h,cpp}`'s subresource layout (`FemeImageSubresourceLayout`, computed once at `vkCreateImage` time per the "V5" design note) is hard-coded to a per-texel, non-block-based stride (`Image.cpp`'s `formatElementSize(Format) * SampleCount`-shaped math) — so this row's own file scope (`Format.{h,cpp}` only) was too narrow even before the LDR-prerequisite finding, and any real ASTC support (LDR or HDR) needs `Image.{h,cpp}` reworked to a block-based layout first. Split into E20 (the LDR prerequisite plus the block-layout groundwork this finding adds) and E21 (this row's original HDR-specific scope, now depending on E20); see E20/E21 below and `agent_thoughts.md` for the full investigation) | none (verify LDR ASTC status first) | `feme/lib/Vulkan/Format.{h,cpp}` | P2 |
| E20 | ~~**Block-compressed image groundwork + ASTC LDR decode**, the prerequisite E15's own investigation found missing: (1) extend `feme::cpu::ResourceFormat`/`Image.{h,cpp}`'s subresource layout math from its current per-texel stride to a block-based one (block-dimension-aligned extent, bytes-per-block instead of `formatElementSize`, still computed once at `vkCreateImage` time per V5's existing "packed table" design); (2) a real ASTC bitstream decoder (integer sequence/trit-quint decoding, all weight grid and color-endpoint-mode combinations, 1-or-2 partitions, dual-plane, void-extent blocks) for the 14 LDR-only block footprints (`VK_FORMAT_ASTC_{4x4,5x4,5x5,6x5,6x6,8x5,8x6,8x8,10x5,10x6,10x8,10x10,12x10,12x12}_UNORM/SRGB_BLOCK`), gating the previously-untracked `VkPhysicalDeviceFeatures::textureCompressionASTC_LDR` bit (a Vulkan 1.0 core, not 1.3/1.4-promoted, feature — add it to `Vulkan14FeatureInventory.md`'s tracked set since this investigation is what surfaced that it had no row at all). This is the largest single new subsystem in this whole roadmap (comparable in scope to a from-spec software codec) and should be scheduled as such rather than folded into a "small extension" lane~~ (closed, with a scope narrowing this row's own text did not anticipate: `Format.h`/`Image.{h,cpp}`'s block-aware layout math (1) and a real, directly-unit-tested `feme::vulkan::decodeASTCBlock` (2, new `ASTCDecode.{h,cpp}`) both landed, covering every LDR color endpoint mode, dual-plane weights, void-extent blocks, and (exceeding this row's own "1-or-2 partitions" text at negligible extra cost, since the partition-selection hash and CEM-decode formulas are already parameterized by count either way) 1-4 partitions -- but `vkCreateImage` still rejects every ASTC `VkFormat` outright, the same result an unrecognized format already got, because `ImageOps.cpp`'s copy/blit/resolve paths and every shader-image-sampling path still address a bound `Image` per texel, not per block, and reworking those is genuinely separate work this row's own file scope (`{Format,Image}.{h,cpp}`, `ASTCDecode.{h,cpp}`) never listed. `textureCompressionASTC_LDR` is now tracked explicitly (`PhysicalDeviceInfo.cpp`) but stays honestly `VK_FALSE` for the same reason -- the same "an unadvertised feature is honest, a partially-wired one risks a CTS `Fail` instead of `NotSupported`" reasoning E15's own investigation already established (agent_thoughts.md). Split off as new row E22 (copy/sampling pipeline rework, flips the feature bit once it lands), analogous to how E15 itself split into E20/E21. A regression build/test run (`ninja check-feme`, RelWithDebInfo + assertions + ccache) caught a real bug during ASTCDecodeTest.cpp authoring, not just confirmed no regression: the initial ISE range-shape lookup only checked bit-only ranges up to 127, one short of the 8-bit (255) range color endpoints commonly use, which would have silently substituted the next-narrower range's values for any color endpoint quantized at 255 -- fixed and covered by a dedicated test case before this row's own code was considered done) | none | `feme/lib/Vulkan/{Format,Image}.{h,cpp}`, new `feme/lib/Vulkan/ASTCDecode.{h,cpp}` | P2 |
| E21 | ~~**`VK_EXT_texture_compression_astc_hdr`/`textureCompressionASTC_HDR`** (E15's original scope, unchanged): the 14 additional HDR block formats (`VK_FORMAT_ASTC_{4x4,...,12x12}_SFLOAT_BLOCK_EXT`), reusing E20's shared bit-unpacking/weight-grid decode and adding the HDR-only float color-endpoint decode path~~ (closed: `mapVkFormat` now recognizes all 14 `_SFLOAT_BLOCK_EXT` formats (`Format.cpp`, new `ResourceFormat::ASTC_*_SFLOAT` entries), and a new `feme::vulkan::decodeASTCBlockHDR` (`ASTCDecode.{h,cpp}`) adds the six HDR-only color endpoint modes -- 2, 3, 7, 11, 14, 15, specification "HDR Endpoint Decoding" -- plus the pseudo-logarithmic HDR weight-interpolation formula, reusing E20's existing bit-unpacking/block-mode/weight-grid machinery unchanged. Unlike `decodeASTCBlock` (UNORM8 output), this function returns full-precision floats and handles a block's endpoints being LDR, HDR, or a mix across partitions (CEM 14 pairs an HDR RGB with an LDR alpha) through one shared interpolation pipeline, per the specification's own text. `vkCreateImage` still rejects every ASTC `VkFormat` outright and `textureCompressionASTC_HDR` stays `VK_FALSE`, the same E20/E22 boundary this row inherited unchanged -- E22's copy/sampling pipeline rework covers both the LDR and HDR feature bits once it lands. A real bug a hand-verified unit test caught before this landed, not just confirmed absent: the HDR mode 11 "major component 3, specify directly" shortcut initially assigned the specification's low/high endpoints backwards relative to this decoder's own `Lo`/`Hi` convention (`agent_thoughts.md` has the full derivation, including resolving an apparent variable-naming typo in the published specification pseudocode for mode 11's own bit-shift amount)) | E20 | `feme/lib/Vulkan/{Format,ASTCDecode}.{h,cpp}` | P2 |
| E22 | ~~**ASTC LDR copy/sampling pipeline wiring**, the scope E20's own closing note split off: rework `ImageOps.cpp`'s copy/blit/resolve paths (and any shader-image-sampling path that reaches one) to address a block-compressed `Image` per block rather than per texel, wire `feme::vulkan::decodeASTCBlock` into whichever of those paths needs decoded texel data, remove `vkCreateImage`'s current outright rejection of an ASTC `VkFormat`, and only then flip `VkPhysicalDeviceFeatures::textureCompressionASTC_LDR` from `VK_FALSE` to `VK_TRUE`~~ (closed, with a scope narrowing this row's own text did not anticipate: `vkCreateImage` no longer rejects a block-compressed `VkFormat` (`Image.cpp`); `Image::blockPointer`, a block-grid-addressed analogue of `texelPointer`, lets `CommandBuffer.cpp`'s `vkCmdCopyImage`/`vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer` address one a whole block at a time -- `bytesPerBlock`/`blockWidth`/`blockHeight` (Format.h) replace `formatElementSize` throughout those paths, a no-op change for a non-block-compressed format since the two agree there, so one implementation now covers both families; `ImageOps.cpp`'s `runResolveImage` rejects a block-compressed format outright (never multisampled in real Vulkan, so meaningless input, not a case to silently mishandle); and `runBlitImage` rejects a block-compressed *destination* (no ASTC encoder exists -- this ICD's ASTC support is decode-only) and an HDR ASTC *source* (`decodeASTCBlock` is LDR-only; `decodeASTCBlockHDR`'s float-producing interface doesn't fit the UNORM8 unpack/pack path every other format here shares), but decodes an LDR ASTC source's fetched texels through `feme::vulkan::decodeASTCBlock` before resampling -- this row's only caller of that decoder, since a copy reinterprets whole blocks verbatim and never decodes. `textureCompressionASTC_LDR` now reads `VK_TRUE` (`PhysicalDeviceInfo.cpp`). The scope this row's own premise did not anticipate: a shader's own *sampling* of a block-compressed image is a separate path from `ImageOps.cpp`/`CommandBuffer.cpp`'s copy/blit/resolve operations entirely -- `CommandBuffer.cpp`'s `materializeImageDescriptor` hands the CPU runtime (`feme/runtime/CPU/FeMeRuntimeCPU.c`, a separate C module with no dependency on this C++ decoder) a raw `Image::data()` pointer for its own per-texel `femeRTFetchTexel2D` to read directly, and that runtime's `femeRTImageFormatElementSize` has no block-compressed case (returns 0, so a sample safely reads as all-zero rather than misinterpreting compressed bytes, but does not decode real ASTC data either). Porting a decoder into that runtime, or bridging it back into this one, is genuinely separate follow-up work outside this row's own file scope (`{Image,ImageOps}.{h,cpp}`) -- tracked as new row E23) | E20 | `feme/lib/Vulkan/{Image,ImageOps}.{h,cpp}` | P2 |
| E23 | ~~**ASTC LDR shader-sampling wiring**, the scope E22's own closing note split off: a shader's `OpImageSample`/`OpImageFetch` of a block-compressed image still reads all-zero (safe, but not real decoded data) because `CommandBuffer.cpp`'s `materializeImageDescriptor` hands the separate CPU runtime (`feme/runtime/CPU/FeMeRuntimeCPU.c`) a raw per-texel `Image::data()` pointer, and that runtime's own `femeRTImageFormatElementSize`/`femeRTFetchTexel2D` have no block-compressed case at all. Needs either a decoder ported into that C runtime module, or the image-descriptor-materialization path bridging back into this C++ one (`ASTCDecode.h`) before a compute shader binds one~~ (closed, choosing the second of this row's own two options: `materializeImageDescriptor` (`CommandBuffer.cpp`) now detects an ASTC LDR-format image (`feme::cpu::isASTCLdrFormat`) and, before handing anything to the CPU runtime, decodes every sampled mip level -- one `Image::blockPointer` plus `feme::vulkan::decodeASTCBlock` call per block, via a new file-local `decodeASTCImageForSampling` helper -- into a per-texel RGBA8 buffer owned by the dispatch's own `MaterializedBoundResources` (new `DecodedImageStorage`/`DecodedImageLayoutStorage` members, kept alive exactly as long as every other descriptor array already is). The resulting descriptor reports `R8G8B8A8_UNORM` (or `_UNORM_SRGB` for a `_SRGB` block format, so the runtime's existing sRGB-decode-at-sample-time path still applies) instead of an ASTC `ResourceFormat` -- so `feme/runtime/CPU/FeMeRuntimeCPU.c` needed **no changes at all**: its `R8G8B8A8_UNORM`/`_UNORM_SRGB` unpack case already reads exactly this shape of data, unmodified. This is a smaller footprint than this row's own file-scope guess (`CommandBuffer.cpp` only, not `FeMeRuntimeCPU.c`) anticipated, and avoids duplicating the decoder's ~600 lines of bitstream logic into a second, C-only implementation that would need its own independent test coverage to trust. An HDR ASTC format is unaffected -- still samples as all-zero, the same "safe but not decoded" result as before, since `decodeASTCBlockHDR`'s float-producing interface doesn't fit this UNORM8 bridge and stays outside this row's own LDR-only scope, mirroring `runBlitImage`'s identical HDR-source restriction (E22). A new `CommandBufferTest.cpp` fixture (`ASTCSampledImageDispatchTest`, subclassing `SampledImageDispatchTest` and overriding just `createImage`) samples a single-block `VK_FORMAT_ASTC_4x4_UNORM_BLOCK` image through a real dispatch end to end and checks the decoded RGBA result -- confirmed to actually catch the regression this row closes by temporarily reverting the `CommandBuffer.cpp` change and observing the new test (only) fail, reading back all-zero exactly as the pre-fix behavior predicted, before restoring the fix) | E22 | `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| E24 | ~~**`vkGetPhysicalDeviceImageFormatProperties`/`vkGetPhysicalDeviceFormatProperties` are still pre-V5 stubs**, a genuine, pre-existing gap roadmap E22's own CTS run found rather than introduced (`git blame` confirms both predate this row's own commits by 10+): `vkGetPhysicalDeviceFormatProperties` unconditionally reports an all-zero `VkFormatProperties` and `vkGetPhysicalDeviceImageFormatProperties` unconditionally returns `VK_ERROR_FORMAT_NOT_SUPPORTED`, for *every* `VkFormat`, not only a block-compressed one -- their own comments still say "no image is supported yet (images are out of scope before V5)", stale since V5 landed real image support. This is why E22's own CTS run measured zero headline movement despite `textureCompressionASTC_LDR` now reading `VK_TRUE`: `dEQP-VK.texture.*`'s own capability probe (`vktTextureTestUtil.cpp`) calls `vkGetPhysicalDeviceImageFormatProperties` before creating any image, of any format, and this stub fails it unconditionally -- so no texture-creation-shaped CTS case can pass regardless of which formats/features this ICD actually implements. A real implementation needs to report accurate `VkFormatFeatureFlags`/`VkImageFormatProperties` (max extent, mip levels, array layers, sample counts, `formatElementSize`/`bytesPerBlock`-derived resource size) for every format `mapVkFormat` recognizes, gated on `usage`/`tiling`/`flags` the same way `isValidImageShape` (Image.cpp) already gates `vkCreateImage` -- a substantial, separate subsystem in its own right, well beyond this row's own ASTC-specific scope~~ (closed: `feme::vulkan::formatFeatureFlags` (new, `Format.{h,cpp}`) computes a real `VkFormatFeatureFlags` per format from already-implemented, already-tested predicates -- `RenderPass.h`'s `isSupportedColorAttachmentFormat`/`isSupportedDepthAttachmentFormat`/`isSupportedStencilAttachmentFormat` for the two attachment bits, the CPU runtime's own three-format sampling table plus the ASTC LDR bridge `materializeImageDescriptor` builds on it (roadmap E23) for the two sampled-image bits, and `ImageOps.cpp`'s `runBlitImage` block-compressed-destination/HDR-ASTC-source rejections for the two blit bits -- and deliberately never sets `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` for any format, since no `feme.cpu.image.store.*` runtime helper exists yet (V5's own still-open "not yet writable" narrowing). `vkGetPhysicalDeviceFormatProperties`/`vkGetPhysicalDeviceFormatProperties2` now return this real value (`VK_IMAGE_TILING_LINEAR`/`_OPTIMAL` still not distinguished, per Image.h's file comment, so both tiling fields match) instead of an unconditional all-zero, and `vkGetPhysicalDeviceImageFormatProperties` rejects any `usage` bit `formatFeatureFlags` has no matching feature for, then reports real `maxExtent`/`maxMipLevels`/`maxArrayLayers`/`sampleCounts`/`maxResourceSize` by reusing `vkCreateImage`'s own shape validation (`isValidImageShape`/`supportedSampleCounts`, both exposed from `Image.cpp` for exactly this rather than duplicated) and subresource-size math (`computeImageCreateInfoSize`, likewise exposed) against the maximal shape this device's limits allow for the requested type/flags, instead of an unconditional `VK_ERROR_FORMAT_NOT_SUPPORTED`. Measuring this against a real `deqp-vk` run (see VulkanCTSReport.md's "Roadmap E24: measured impact") found, and this row's own fix closed, a genuine pre-existing crash this query's own honesty newly made reachable: `CommandBuffer.cpp`'s `runCopyImage` derived a single `Compressed` flag from the *source* image alone and applied it to both sides of a `vkCmdCopyImage` call, asserting inside `Dst->blockPointer` the instant a real CTS case copied a block-compressed source into a same-byte-size *uncompressed* destination (one ASTC block <-> one `R32G32B32A32_UINT` texel, both 16 bytes) -- a copy real Vulkan's own "compatible formats" rule already permits and this ICD already claimed to support. `runCopyImage` now tracks each side's compressed-ness (and block shape) independently. `dEQP-VK.*astc*` went from 873 passed/1 failed/98,053 not supported (E20-E23's unchanged baseline) to 8,237 passed/12,225 failed/78,465 not supported -- the real headline movement this whole ASTC sequence (E15/E20-E23) had been blocked on. The 12,225 new failures are a *different*, narrower gap than the crash above: mostly real, honest mandatory-format-support shortfalls this query's own accuracy now surfaces as `Fail` instead of hiding behind a stub (e.g. most Vulkan-mandatory sampled formats besides `R32G32B32A32_FLOAT`/`R8G8B8A8_UNORM`/`_SRGB`/ASTC LDR genuinely cannot be sampled by this ICD yet) -- tracked as new row E25 rather than fixed here, since closing it means substantially broadening the CPU runtime's/graphics pipeline's own format support, not another capability-query fix) | none | `feme/lib/Vulkan/EntryPoints.cpp` | P1 |
| E25 | ~~**Broaden real per-format feature support**, the gap roadmap E24's own `deqp-vk` run surfaced rather than introduced: `dEQP-VK.api.info.format_properties.*`/`image_format_properties.*`/`unsupported_image_usage.*` now fail ~500 cases (up from `format_properties`'s own smaller pre-existing baseline) purely because `formatFeatureFlags` (Format.h) honestly reports most Vulkan-mandatory sampled/attachment formats as unsupported -- this ICD's CPU runtime only actually samples `R32G32B32A32_FLOAT`/`R8G8B8A8_UNORM`/`_UNORM_SRGB` (plus ASTC LDR, bridged) and its render-target pack/unpack table (`RenderPass.cpp`'s `isSupportedColorAttachmentFormat`) covers a narrower set than the Vulkan 1.0 mandatory floor. Similarly, `dEQP-VK.*astc*`'s new 12,225 failures are mostly real texture-path gaps (e.g. `feme-cpu-simdize`'s divergent-vector limitation blocking `vktTextureTestUtil`'s fragment-shader cases), not query-accuracy ones. Closing this means extending the CPU runtime's typed sample table and `feme::graphics`'s pack/unpack table to the full mandatory format list -- a substantial, separate broadening of existing subsystems, not a single mechanical fix~~ (closed, with a scope narrower than this row's own "full mandatory format list" text: cross-checked against the Vulkan spec's own "Mandatory Format Support" tables (`formats.adoc`) rather than guessed, the CPU runtime's typed sample table (`femeRTImageFormatElementSize`/`femeRTUnpackImageTexel`, FeMeRuntimeCPU.c) now decodes every non-integer, non-block-compressed, non-depth/stencil format `feme::cpu::ResourceFormat` lists -- `R32_FLOAT`/`R32G32_FLOAT`/`R32G32B32_FLOAT` (padding a missing component the way `OpImageFetch` does), `R8G8B8A8_SNORM`, a hand-written binary16-to-`float` conversion for `R16G16B16A16_FLOAT` (no hardware half-float support can be assumed on this freestanding file's build), and three more packed-word formats (`R11G11B10_FLOAT`, `R10G10B10A2_UNORM`, `B8G8R8A8_UNORM`) plus the two `VK_KHR_maintenance5` formats already tracked (`A8_UNORM`, `A1B5G5R5_UNORM`) -- and `formatFeatureFlags` (Format.cpp) now advertises `SAMPLED_IMAGE_BIT`/`SAMPLED_IMAGE_FILTER_LINEAR_BIT` for exactly that broadened set. The mandatory-format tables also show this ICD's *existing* `isSupportedColorAttachmentFormat` (RenderPass.cpp) set already covers every format the spec actually requires `COLOR_ATTACHMENT_BIT`/`_BLEND_BIT` for that `ResourceFormat` can represent (`R32G32B32A32_FLOAT` group, `R8G8B8A8_UNORM`/`_SRGB`, `B8G8R8A8_UNORM`, `A2B10G10R10_UNORM`/`R10G10B10A2_UNORM`, `R16G16B16A16_SFLOAT`) -- `R11G11B10_FLOAT`/`R8G8B8A8_SNORM` are mandatory *sampled*, not mandatory *attachment*, formats, so `feme::graphics`'s pack/unpack table needed no new attachment case, narrower work than this row's own text assumed. What this row does **not** close: a `_UINT`/`_SINT` format (also mandatory-sampled per the spec, e.g. `R32G32B32A32_UINT`/`R16G16B16A16_UINT`) still cannot be sampled, because every `feme.cpu.image.*` entry point returns `<4 x float>` and no integer-returning counterpart exists yet to consume one -- decoding an integer format's bits here would have nothing correct to hand them to, so this is left as follow-up (new row E26) rather than papered over. The `dEQP-VK.*astc*` texture-path gap this row's own text also named (`feme-cpu-simdize`'s divergent-vector limitation) is unrelated to per-format feature support and is likewise left to its own follow-up rather than folded in here) | E24 | `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Vulkan/Format.cpp` | P1 |
| E26 | ~~**Integer-format image sampling/loading.** No `feme.cpu.image.*` entry point returns an integer vector (every sample/load call is `<4 x float>`), so a mandatory-sampled `_UINT`/`_SINT` format (`R32G32B32A32_UINT`/`_SINT`, `R16G16B16A16_UINT`/`_SINT`, `R8G8B8A8_UINT`/`_SINT`, `R10G10B10A2_UINT`, per the spec's own "Mandatory Format Support" tables, roadmap E25's own investigation) cannot be sampled or `OpImageFetch`-loaded at all today, even though `SPIRVResourceLowering.cpp` already raises an integer `OpImageFetch`/`OpImageRead` to a canonical call. Needs a `feme.cpu.image.load.2d.v4i32` (and, if a filtered integer sample is ever legal in SPIR-V, a decision on what "filtering" even means for one) entry point in `FeMeRuntimeCPU.c`, the matching `ImageCalls.{h,cpp}` builder, and an integer decode table (`femeRTUnpackImageTexelI32`-shaped) analogous to E25's own `femeRTUnpackImageTexel`, before `formatFeatureFlags` can honestly set `SAMPLED_IMAGE_BIT` for any integer format~~ (closed: `feme::cpu::ImageCalls` gained `ImageCallKind::Load2DI32`/`createLoad2DI32`, the canonical `feme.cpu.image.load.2d.v4i32` call -- same shape as the existing float `Load2D`, just `<4 x i32>`-returning. This row's own "already raises an integer `OpImageFetch` to a canonical call" premise held only for the texel-buffer path (`classifyTexelBufferHandle`); the 2D sampled-image path (`classifySampledImage2DHandle`) rejected every non-float channel type outright, so that gap needed closing too -- it now accepts an `i32`-channel handle alongside the existing float one, and `hasOnlySupportedImageUses` narrows an integer-channel handle's uses to `OpImageFetch` only, rejecting every sample intrinsic: SPIR-V never legalizes a filtered `OpImageSample*` against an integer-sampled image, so this row's own "what would filtering even mean" question has a definite answer (nothing -- no such op exists to lower), and no `SampleImage2DI32`-shaped `ImageCallKind` is added. `FeMeRuntimeCPU.c`'s `femeRTImageFormatElementSize` (shared with the float path -- a texel's byte size does not depend on which vector type reads it back) and a new `femeRTUnpackImageTexelI32` decode exactly the mandatory-sampled formats this row names; the non-mandatory partial-component integer formats (`R32_UINT`/`R32G32_UINT`/`R32G32B32_UINT` and their `_SINT` counterparts) are left undecoded, the same "mechanical, added on demand" scoping E25 itself used. `formatFeatureFlags` (Format.cpp) now advertises `SAMPLED_IMAGE_BIT` (never `_FILTER_LINEAR_BIT`, for the same no-filtered-integer-sample reason) for exactly this set. `SIMDize.cpp`'s `widenImageCall` needed no change: it was already generic over its scalar helper's result element type) | E25 | `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Transforms/CPU/{ImageCalls,SPIRVResourceLowering}.cpp`, `feme/lib/Vulkan/Format.cpp` | P2 |
| E16 | ~~**`VK_EXT_image_robustness`/`robustImageAccess`.** Out-of-bounds image reads/writes must return/discard rather than fault; audit `Image.cpp`'s/`ImageOps.cpp`'s existing bounds handling (Executor's texel read/write path) and add an explicit clamp-or-discard for any coordinate outside the image's declared extent that isn't already handled~~ (closed, for this row's own `Image.cpp`/`ImageOps.cpp` scope: auditing `FeMeRuntimeCPU.c`'s shader-visible texel path first (`femeRTFetchTexel2D`/`femeRTFetchTexel2DI32`) found it already returns all-zero for any out-of-range access -- the "Executor's texel read/write path" this row's own text named was already handled, and there is no shader-visible texel *write* path to audit at all yet, per `Image.h`'s own "not yet writable" note. The actual gap was host-side: `Image::texelPointer`/`Image::blockPointer` (Image.{h,cpp}) trusted every caller's coordinates unconditionally, so `ImageOps.cpp`'s `runBlitImage`/`runResolveImage` -- the only two operations here whose region comes from arbitrary, only mip-level-checked application input rather than from the image's own dimensions -- could compute a wild pointer for an out-of-bounds source/destination coordinate and fault. Both now return null instead, and `runBlitImage` clamps every source read into the mip's own declared extent (matching its existing clamp-to-requested-rectangle behavior) while discarding an out-of-bounds destination write; `runResolveImage` discards a whole texel (read and write) the same way. `runClearColorImage`/`runClearDepthStencilImage`/`clearAttachmentRects` needed no change: every coordinate they touch is already derived from the image's/attachment's own dimensions, never from unchecked application input. **Deliberately left open, as a narrower remaining scope than this row's own text:** `CommandBuffer.cpp`'s `vkCmdCopyImage`/`vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer` share the exact same unaudited-region shape (only a mip level is range-checked; offset/extent are not validated against either image's declared extent before addressing `texelPointer`/`blockPointer`) but that file is outside this row's own `{Image,ImageOps}.cpp` file scope -- tracked as follow-up work rather than folded in here. `VkPhysicalDeviceVulkan13Features::robustImageAccess` (EntryPoints.cpp) is therefore deliberately left `VK_FALSE`: flipping it would over-claim a device-wide guarantee this row's own narrower audit does not yet establish) | none | `feme/lib/Vulkan/{Image,ImageOps}.cpp` | P1 |
| E17 | ~~**SPIR-V 1.6 `Nontemporal` image-operand bit.** Not a feature-bit gap but a shader-compilation one: `ImageFetchPattern`/`ImageFetchLodPattern`/`ImageSampleExplicitLodPattern` (`SPIRVToLLVMPatterns.cpp`) reject any image operand mask they don't recognize exactly, and SPIR-V 1.6's cache hint bit has no case. Since it has no correctness effect, the fix is to accept and discard it rather than model caching. **Closes D3's `spirv_assembly.instruction.compute` 422-case regression**, the largest single item in this section~~ (closed, with a correction to this row's own headline claim: `hasImageOperands`/a new `hasExactImageOperands` helper now mask out the `Nontemporal` bit before checking for an actually-unsupported modifier, so all three named patterns accept and discard it exactly as this row's own text proposed (as does `ImageReadPattern`/`ImageWritePattern`, sharing `hasImageOperands` but not named by this row's own text) -- confirmed by a targeted `dEQP-VK.spirv_assembly.instruction.compute` run, both before and after, against the exact same CTS build. That run does **not** reproduce a 422-case regression or reproduce any newly-*passing* cases from this fix: this tree's current state (already past D0-E16) shows an identical 264 Passed/1387 Failed/18253 NotSupported split before and after, and a full per-case `Fail`-set diff between the two runs is empty. Tracing why: of the 1387 `Fail` cases, exactly 351 fail pre-fix with the `ImageOperands::Nontemporal`-mask legalization error this row's own text describes (183 `spirv.ImageSampleExplicitLod`, 126 `spirv.ImageFetch`, 42 `spirv.ImageRead`) -- but every one of those 351 still fails post-fix, now past legalization, on a different, pre-existing gap this fix newly unmasks rather than one it introduces: `'llvm.getelementptr' op operand #0 must be LLVM pointer type ... but got 'vector<3xi32>'` (a `combined_image_sampler_separate_descriptors` address-computation bug, reproducing identically on the *non*-`nontemporal`-suffixed sibling case in the same log, so it long predates this row). The remaining 3 of the 354 total cases this row's own text's "422" figure was meant to cover (`memory_access.{nontemporal,aligned_nontemporal,aligned_volatile}`) fail on a distinct, unrelated bit -- `spirv.CopyMemory`'s own `MemoryAccess::Nontemporal` (a different bit-enum than `ImageOperands::Nontemporal`, SPIRVBase.td's `SPIRV_MA_Nontemporal` vs `SPIRV_IO_Nontemporal`) -- outside this row's own `ImageFetchPattern`/`ImageFetchLodPattern`/`ImageSampleExplicitLodPattern` scope and untouched by this fix, so they were never expected to close here. The fix itself is real and correctly scoped -- the `ImageOperands::Nontemporal`-specific error is gone from every one of the 351 -- but its CTS payoff is nil in this tree's *current* state, entirely masked by the pre-existing address-computation bug above; see "Roadmap E17: measured impact" in VulkanCTSReport.md for the full breakdown. That bug is left open as follow-up work (a future roadmap row), being outside this row's own file scope) | none | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P0 |
| E18 | ~~**Texel-buffer-format/robustness inconsistency** (D3's `robustness.oob_access` 6-case regression, not yet root-caused past "format/robustness mismatch" -- this row starts with tracing it to a specific line, the way D3 did for its other five buckets, before fixing it) plus `VK_EXT_texel_buffer_alignment`'s two limit fields (`storageTexelBufferOffsetAlignmentBytes`/`SingleTexelAlignment`, `uniformTexelBufferOffsetAlignmentBytes`/`SingleTexelAlignment`, already reserved by E2)~~ (closed, with a correction to this row's own premise for its first half: tracing the `robustness.oob_access` regression to a specific line found there is no longer a line to fix. `vkCreateBufferView` (Buffer.cpp) and `vkGetPhysicalDeviceFormatProperties`'s `bufferFeatures` (EntryPoints.cpp) both already gate on the exact same `isTexelBufferFormatSupported` predicate (Format.cpp) -- roadmap E24/E25's `formatFeatureFlags` work, landed after D3's own pass, made the two paths share one predicate by construction, so they cannot disagree anymore. A targeted CTS run of the full `dEQP-VK.robustness.oob_access.*` group (121 cases, not just the 6 D3 named) confirms this directly: zero `Fail`, 120 `NotSupported` and 1 `Pass`, both before and after this row's own second half landed -- the same "traced, and does not survive re-verification" outcome D3 itself found for two of its own five other buckets (`ubo.*.std430`, `synchronization.op.*`). This row's second half closes for real: `storageTexelBufferOffsetAlignmentBytes`/`uniformTexelBufferOffsetAlignmentBytes` (EntryPoints.cpp) are now the real `minTexelBufferOffsetAlignment` (256) -- `vkCreateBufferView` never enforces anything stricter, and the CPU runtime's typed texel-buffer load/store helpers read and write through `__builtin_memcpy`, which has no alignment requirement of its own -- and both `SingleTexelAlignment` companions are `VK_TRUE` for the same reason. A new dedicated `VkPhysicalDeviceTexelBufferAlignmentProperties` case agrees with these four aggregate fields, exactly like `VkPhysicalDeviceSubgroupSizeControlProperties` (E7) already does, and `VK_EXT_texel_buffer_alignment` is now listed in `getSupportedDeviceExtensions`. A targeted CTS run after that first draft found one real gap it did not anticipate: this extension's own feature struct, `VkPhysicalDeviceTexelBufferAlignmentFeaturesEXT`, had no case in `vkGetPhysicalDeviceFeatures2` at all, so `dEQP-VK.api.info.get_physical_device_properties2.features.texel_buffer_alignment_features_ext` and `dEQP-VK.api.device_init.create_device_unsupported_features.texel_buffer_alignment_features_ext` both failed -- unlike every other extension this document tracks, `VK_EXT_texel_buffer_alignment` promoted only its *properties* struct to core 1.3, leaving its features struct with no aggregate `VkPhysicalDeviceVulkan13Features` field to fall back on. `texelBufferAlignment` is now unconditionally `VK_TRUE` (`vkCreateBufferView` needs no more than the dedicated properties struct above already promises), closing both cases. See "Roadmap E18: measured impact" in VulkanCTSReport.md for the full before/after) | E2 | `feme/lib/Vulkan/EntryPoints.cpp`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P0 |
| E19 | ~~**Remaining small 1.3 extensions with no feature bit of their own**, each a mechanical, independent addition: `VK_EXT_4444_formats` (2 new `Format.cpp` formats), `VK_EXT_ycbcr_2plane_444_formats` (1 new format, decline if YCbCr sampling itself is unimplemented — check first), `VK_EXT_pipeline_creation_feedback` (`VkPipelineCreationFeedback` timing struct, can honestly report zero/estimated timings),~~ ~~`VK_KHR_format_feature_flags2` (`VkFormatProperties3`, mirrors the existing `VkFormatProperties2` case with the 2 new bit ranges)~~ (roadmap E25 found and fixed `vkGetPhysicalDeviceFormatProperties2` silently dropping a chained `VkFormatProperties3` entirely -- it now fills `linearTilingFeatures`/`optimalTilingFeatures`/`bufferFeatures` from the same `formatFeatureFlags` result as `VkFormatProperties`, since every bit this ICD sets today has an identical numeric value in `VkFormatFeatureFlags`/`VkFormatFeatureFlags2`; the two 64-bit-only bit ranges `VkFormatFeatureFlags2` alone defines remain unset, since no code path here implements the capability either range names, and the extension name itself is still not in the advertised list -- narrower remaining scope than this row's own text, tracked here rather than a new row since nothing else changed), ~~`VK_KHR_shader_non_semantic_info` (SPIR-V debug-info-only opcodes the importer can already skip/ignore), `VK_EXT_tooling_info` (`vkGetPhysicalDeviceToolProperties` can truthfully report zero tools)~~ (closed, with a correction to this row's own "`VK_EXT_ycbcr_2plane_444_formats`" clause: declined outright, per this row's own "decline if YCbCr sampling itself is unimplemented -- check first" instruction -- `samplerYcbcrConversion` (EntryPoints.cpp) is unconditionally `VK_FALSE` and no multi-planar/YCbCr sampler support exists anywhere in this ICD, so the one new format this extension names would be unusable regardless of whether `Format.cpp` recognized it. The other four all land for real: `VK_FORMAT_A4R4G4B4_UNORM_PACK16`/`A4B4G4R4_UNORM_PACK16` are now recognized `VkFormat` values (`Format.cpp`'s `mapVkFormat`/`formatElementSize`, a new `VkPhysicalDevice4444FormatsFeaturesEXT` case in `EntryPoints.cpp`), correctly left unbacked by any sampled-image/color-attachment `VkFormatFeatureFlags` bit (no `feme::graphics::packClearColor`/`unpackColor` case exists for either yet, so only the transfer/blit bits every recognized format already gets apply); `VkPipelineCreationFeedbackCreateInfo`, chained onto `vkCreateGraphicsPipelines`/`vkCreateComputePipelines`, is filled by a new shared `fillPipelineCreationFeedback` (Pipeline.cpp) -- `VALID_BIT` always (a pipeline that reaches the call always compiled successfully), `APPLICATION_PIPELINE_CACHE_HIT_BIT` added on a real `VkPipelineCache` hit, `duration` honestly `0` (no per-stage compile-timing instrumentation exists to fabricate a number from) for both the one overall and each per-stage feedback slot; `VK_KHR_shader_non_semantic_info` needed a correction to this row's own "the importer can already skip/ignore" premise -- it could not, in fact: MLIR's own SPIR-V deserializer has no generic "unrecognized extended-instruction-set name" tolerance at all (`dispatchToExtensionSetAutogenDeserialization` fails outright for any set name outside its autogenerated dispatch table, `NonSemantic.*` included), so `SPIRVImporter.cpp` now strips the `NonSemantic.*` `OpExtInstImport` and every `OpExtInst` referencing it out of the raw word stream *before* MLIR ever sees it, honoring the SPIR-V specification's own "ignorable" contract for the whole family at the one layer that can, verified against a hand-assembled `OpExtInst` into a `NonSemantic.DebugPrintf` set that fails pre-fix and succeeds post-fix; `vkGetPhysicalDeviceToolProperties` is now implemented, truthfully reporting zero tools via the same `enumerate` helper every other `vkEnumerate*` command already shares) | none | `feme/lib/Vulkan/{Format,Pipeline,EntryPoints,PhysicalDeviceInfo}.cpp`, `feme/lib/Import/SPIRV/SPIRVImporter.cpp` | P2 |
| E27 | ~~**`VK_KHR_maintenance5`'s `VK_REMAINING_ARRAY_LAYERS` sentinel in a copy/blit/resolve region hangs rather than fails.** A full `deqp-vk` run (this row's own session) found `copyBufferImageRegion`/`runCopyImage`/`runBlitImage`/`runResolveImage` (CommandBuffer.cpp, ImageOps.cpp) each loop `Layer != Region.*Subresource.layerCount` directly; `maintenance5` (already advertised, roadmap E5) legalizes the sentinel's literal `0xFFFFFFFF` value in that field, turning every one of those loops into an effectively infinite one instead of a large but finite one, confirmed hanging `dEQP-VK.api.copy_and_blit.copy_commands2.{buffer_to_image,blit_image}.*.*remaining_layers*`~~ (closed: `Image::resolvedLayerCount`, a new small accessor, resolves the sentinel against the image's own array layer count the same way `Image::setLayout`'s existing `VkImageSubresourceRange` handling and `ImageOps.cpp`'s clear functions already do; all four call sites now go through it. Four new regression tests (`ImageTest.CopyBufferToImageWithRemainingArrayLayers`, `ImageTest.CopyImageWithRemainingArrayLayers`, `ImageOpsTest.BlitsWithRemainingArrayLayers`, `ImageOpsTest.ResolvesWithRemainingArrayLayers`) each copy/blit/resolve the last two of a three-layer image using the sentinel, none of which terminate without this fix. See "Roadmap E27/E28: measured impact" in VulkanCTSReport.md) | none | `feme/lib/Vulkan/{Image.h,CommandBuffer,ImageOps}.cpp` | P0 |
| E28 | ~~**`vkCmdCopyImage` between a 2D-array image and a 3D image `SIGSEGV`s.** The same full run that found E27 also found `runCopyImage`'s nested `Layer`/`Z` loop applies `Region.srcOffset.z`/`Region.dstOffset.z` to *both* sides of every copy, including a 2D (array) side's own out-of-range Z coordinate; real Vulkan's own "Image Copies" rule (`vkCmdCopyImage`'s spec text) is that a 3D image's `extent.depth` slices correspond to a paired 2D image's `layerCount` layers, not a `Z` offset on both. `Image::texelPointer`/`blockPointer` return null for the resulting out-of-bounds Z, and the following `memcpy` faulted -- reproduced under `gdb` on `dEQP-VK.api.copy_and_blit.copy_commands2.image_to_image.3d_images.2d_to_3d_whole` (and, by the same code path, every other case in the ~22,800-case `*_2d_to_3d_*`/`*_3d_to_2d_*` family)~~ (closed: the double loop is now a single loop over a `SliceCount` (`extent.depth` if either side is 3D, otherwise the layer count), stepping whichever side is 3D through its own `offset.z` and whichever side is not through its own `baseArrayLayer` -- never both, matching the spec rule exactly, and reducing to the previous behavior for a same-dimensionality (2D-to-2D or 3D-to-3D) copy. `ImageTest.CopyImage2DArrayToImage3D`, a new regression test, copies all three layers of a 2D-array image into a same-sized 3D image and byte-compares the result; it `SIGSEGV`s without this fix. See "Roadmap E27/E28: measured impact" in VulkanCTSReport.md) | none | `feme/lib/Vulkan/CommandBuffer.cpp` | P0 |
| E29a | ~~**`SIGSEGV` in `api.granularity.in_dynamic_render_pass.*`.** The same full run that found E27/E28 found a seventh, different `api`-group crash once those two were fixed: `VK_KHR_maintenance5` is advertised (roadmap E5) but `vkGetRenderingAreaGranularityKHR` was never added to the entrypoint generator's `SUPPORTED_EXTENSIONS`, so it resolved to a null function pointer through the loader (whose `icd.json` `api_version` of 1.1 refuses to route a direct, non-`KHR`-suffixed query for a command newer than that -- the same quirk `VK_KHR_dynamic_rendering`'s `vkCmdBeginRenderingKHR`/`vkCmdEndRenderingKHR` already work around) -- confirmed under `gdb`/`valgrind`, a null virtual-dispatch target reached from `GranularityInstance::iterate()`~~ (closed: `vkGetRenderingAreaGranularityKHR` (RenderPass.cpp) now implements the same constant 1x1 answer `vkGetRenderAreaGranularity` already gives, for the same reason -- a software rasterizer has no tile-alignment requirement. Two new unit tests (`RenderPassTest.GetRenderingAreaGranularityReportsNonZeroGranularity`, `RenderPassTest.DynamicRenderPassGranularitySequenceForPacked10BitFormat`) cover the entrypoint directly and the full image/view/barrier/dynamic-render-pass sequence the CTS crash exercised. The full `dEQP-VK.api.granularity.*` group (650 cases) now runs to completion) | none | `feme/lib/Vulkan/{EntryPoints.h,RenderPass.cpp,ImplementedEntrypoints.txt}`, `feme/utils/vk_gen_entrypoints.py` | P0 |
| E29b | ~~**`SIGSEGV` in `image.subresource_layout.*`.** `vkGetImageSubresourceLayout` was never implemented at all (a pure gap, unrelated to any loader quirk since it is core Vulkan 1.0), and `VK_KHR_maintenance5`'s own `vkGetImageSubresourceLayout2KHR`/`vkGetDeviceImageSubresourceLayoutKHR` were advertised but likewise unimplemented -- the same null-virtual-dispatch crash shape as E29a, reached from every case in this group instead~~ (closed: all three now implemented -- a live image's own byte layout (from `Image`'s existing per-mip-level layout table), its `pNext`-extensible wrapper, and an info-only variant computed from a `VkImageCreateInfo` alone (mirroring `vkGetDeviceImageMemoryRequirements`'s own relationship to `vkGetImageMemoryRequirements`). A new `Image::subresourceLayout`/`ImageSubresourceLayout` pair captures the one real subtlety: a 3D image's subresource size spans every depth slice at that mip level, while a 2D(-array) image's is exactly one layer, matching the Vulkan spec's own `VkSubresourceLayout` rule. Four new unit tests cover a multi-level 2D image, the `pNext`-extensible/info-only variants agreeing with a live query, and the 3D whole-depth-range case. The full `dEQP-VK.image.subresource_layout.*` group (3,537 cases) now runs to completion) | none | `feme/lib/Vulkan/{Image.h,Image.cpp,EntryPoints.h,ImplementedEntrypoints.txt}` | P0 |
| E29c | ~~**`SIGSEGV` in `glsl.texture_functions.query.texturesamples.*`.** `vkGetPhysicalDeviceImageFormatProperties`'s `sampleCounts` result was gated on `MaxProbe.mipLevels != 1` -- the *maximal, single-sample* image's own mip chain length for this type/usage, unrelated to whether *a* single-mip multisample image of this type can exist -- which collapsed `sampleCounts` to `VK_SAMPLE_COUNT_1_BIT` for essentially every sampled/storage 2D image. `dEQP-VK.glsl.texture_functions.query.texturesamples.*` assumes a 2D sampled image can report more than one sample count and, when none is offered for a non-integer format, hits an omitted `DE_ASSERT(false)` (compiled out in this build) before indexing an empty vector~~ (closed: the gate now checks only what real Vulkan actually restricts multisampling to -- `VUID-VkImageCreateInfo-samples-02257`, 2D only, any mip count -- matching `isValidImageShape`'s own invariant. Two new unit tests cover a 2D sampled image reporting `VK_SAMPLE_COUNT_2/4_BIT` and a 3D image staying single-sample. The full `dEQP-VK.glsl.texture_functions.query.texturesamples.*` group (24 cases) now runs to completion) | none | `feme/lib/Vulkan/EntryPoints.cpp` | P0 |
| E29d | ~~**`llvm_unreachable` in `ResourceCalls.cpp` from `spirv_assembly.instruction.spirv1p4.opselect.array_select`.** `hasOnlySupportedUses` (SPIRVResourceLowering.cpp) only validated a *texel*-buffer load/store's element type; a storage/uniform buffer load or store of any type -- including an aggregate like the array `OpSelect` produces choosing between two array-typed constants -- was accepted as normalizable, reaching `createRawLoad`/`createRawStore` and, transitively, `feme::cpu::mangleResourceCallName`'s `appendScalarMangling`, whose `llvm_unreachable` only handles a scalar or fixed vector of half/float/double/integer~~ (closed: a new `isSupportedRawElementType` (mirroring the texel-buffer one) is now checked for the non-texel path too, leaving an unsupported aggregate load/store unclassified -- a normal SPIR-V-to-LLVM legalization failure downstream instead of a crash. A new unit test covers a storage-buffer array store left un-normalized. The full `dEQP-VK.spirv_assembly.instruction.spirv1p4.opselect.*` group (12 cases) now runs to completion, with clean `Fail`/`NotSupported` results for the still-unimplemented aggregate shapes) | none | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P0 |
| E29e | ~~**`llvm::Value::setNameImpl` assertion from `renderpasses.dynamic_rendering...low_resolution_z.blend.color_masked_after_color_depth`.** `SIMDize.cpp`'s `widenScalarizedFallback` already computed `HasResult = !I.getType()->isVoidTy()` to decide whether to build a result vector, but unconditionally passed `I.getName() + ".lane"` to `Builder.Insert` even when `HasResult` was false -- a non-empty name on a void-typed clone (a divergent `store`, reached here because this fragment shader's exact blend/color-mask/depth-stencil combination lowers to a plain divergent store rather than the masked-output-store intrinsic) asserts~~ (closed: only a `HasResult` clone is named now, matching the guard the function already applies to building the result vector itself. A new unit test covers a divergent, non-masked-intrinsic, non-groupshared store reaching this fallback directly (confirmed crashing without the fix by reverting it and re-running). The full `dEQP-VK.renderpasses.dynamic_rendering.*` group (28,602 cases) now runs to completion) | none | `feme/lib/Transforms/CPU/SIMDize.cpp` | P0 |
| E29f | ~~**`GetElementPtrTypeIterator.h` assertion from `compute.pipeline.zero_initialize_workgroup_memory.composites.*`.** A `Workgroup`-storage `spirv.GlobalVariable` whose pointee is (or contains, through a struct/array) SPIR-V's `OpTypeBool` -- represented, like MLIR's own SPIR-V dialect, as a plain `i1` -- converted straight through to an addressable LLVM aggregate containing a raw `i1` member; once addressed by `getelementptr` (any `shared`/`groupshared` struct mixing a `bool`/`bvec*` field in with real scalars, this test group's own per-case shape), `i1`'s 1-bit size is not byte-addressable, asserting deep in LLVM IR translation~~ (closed: a new `containsAddressableBool` (SPIRVToLLVMPatterns.cpp), checked by `WorkgroupGlobalVariablePattern` before converting, now fails this shape to legalize cleanly instead -- MLIR upstream's own `GlobalVariablePattern` already rejects the `Workgroup` storage class outright, so no other pattern picks up the failed match. A new lit test (`spirv-to-llvm-workgroup-bool.mlir`) covers the rejection directly. The full `dEQP-VK.compute.pipeline.zero_initialize_workgroup_memory.*` group (646 cases) now runs to completion) | none | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P0 |
| E29g | ~~**`SIGSEGV`, no diagnostic, somewhere in `synchronization.*` (originally reported at `timeline_semaphore.device_host.write_copy_buffer_read_copy_buffer.buffer_262144`).** Unlike E29a-E29f, this one did not reproduce: the named case passes/fails cleanly in isolation, the full `dEQP-VK.synchronization.timeline_semaphore.*` subgroup (2,880 cases) and the full `dEQP-VK.synchronization.*` group (64,872 cases) both ran to completion cleanly twice in a row (once under `gdb`, once plain) with no crash anywhere -- the same "long single-process run, not reproducible standalone" shape "Roadmap C1: measured impact" (VulkanCTSReport.md) already documented for an unrelated `api`-group flake~~ (closed as unreproducible: two clean full runs of the entire `synchronization` group found no crash, so this is treated as a one-off, environmental flake -- most likely transient resource contention from concurrent CTS runs earlier in the same investigating session -- rather than a code defect to fix. If a future full run reproduces it again, re-open with a fresh crash-site log line) | none (no code change; not reproducible) | none | P2 |

Six of E29's seven crashes (E29a-E29f) are closed with real fixes and new
regression tests, each independently verified by running its own full
`dEQP-VK` group to completion afterward (previously crashing partway
through every one of them); the seventh (E29g) did not reproduce after
two full, clean re-runs of its entire group and is closed as an
unreproducible environmental flake instead. See "Roadmap E29: measured
impact" in VulkanCTSReport.md for the full per-group before/after.

Recommended independent lanes for parallel distribution (each lane's rows
share files/reviewers; lanes themselves are independent once E1/E2 land):
**Lane 1** (sync/dispatch): E3, E6. **Lane 2** (memory/image queries): E4,
E5, E16, E18. **Lane 3** (compute/shader): E7, E8, E11, E12, E13 (E12/E13
are P0 and should not wait for E7/E8/E11 in the same lane). **Lane 4**
(descriptors/misc small extensions): E9, E10, E14, E19. **Lane 5**
(shader compilation, no feature-bit dependency, start immediately): E17.
**Lane 6** (new subsystem, largest single item in this section, good
candidate for a dedicated owner rather than a quick task): E20 (closed),
then E22 (closed) and E21 (HDR formats, closed), both having depended on
E20 but independent of each other, then E23 (shader-sampling wiring,
closed, depended on E22), then E24 (closed, pre-existing capability-query
gap E22's own CTS run found, independent of E20-E23's own ASTC-specific
scope), then E25 (closed, broadened real per-format feature support, the
narrower gap E24's own CTS run found, depended on E24), then E26 (closed,
integer-format image sampling, the gap E25's own investigation found,
depended on E25). **Lane 7** (found by a full CTS run, independent of
every lane above): E27, E28 (both closed), then E29a-E29g (all closed,
seven crashes found by the same run -- six real fixes, one unreproducible
flake).

#### 1.9.5 Closing the Vulkan 1.4 mandatory floor (F-series)

Same granularity and testing discipline as §1.9.4. Every row here depends
on E1/E2's struct plumbing (the 1.4 aggregate cases, not just the 1.3
ones) but not on any specific E-row otherwise, so — per §1.9.3's Strategy
B — this whole section can start as soon as E1/E2 land if enough
independent capacity exists, even before §1.9.4 fully closes.

| # | Task | Depends on | Files | Priority |
|---|---|---|---|---|
| F1 | ~~**`VK_KHR_global_priority`/`globalPriorityQuery`.** `VkDeviceQueueGlobalPriorityCreateInfo` at `vkCreateDevice` and `vkGetPhysicalDeviceQueueFamilyProperties2`'s `VkQueueFamilyGlobalPriorityProperties` chain; since this ICD has one worker pool with no real OS-level scheduling priority, report the full mandatory priority list as supported and treat the create-time hint as a no-op (matching the "single logical queue, narrowed by capability flags only" precedent C7 set)~~ (done: `VK_KHR_global_priority` is now advertised (`getSupportedDeviceExtensions`), `globalPriorityQuery` reads `VK_TRUE` from both the aggregate `VkPhysicalDeviceVulkan14Features` struct and a new dedicated `VkPhysicalDeviceGlobalPriorityQueryFeatures` struct (`EntryPoints.cpp`), and `vkGetPhysicalDeviceQueueFamilyProperties2`'s `VkQueueFamilyGlobalPriorityProperties` chain reports all four mandatory priorities (`LOW`, `MEDIUM`, `HIGH`, `REALTIME`, ascending) for every queue family. `vkCreateDevice` already ignored every `pQueueCreateInfos[i].pNext` entry it did not recognize, so `VkDeviceQueueGlobalPriorityCreateInfo`'s `globalPriority` hint was already a no-op; this row only made that deliberate rather than incidental) | E1/E2 | `feme/lib/Vulkan/{PhysicalDeviceInfo,EntryPoints}.cpp` | P2 |
| F2 | ~~**`VK_KHR_shader_subgroup_rotate`/`shaderSubgroupRotate`(+`Clustered`).** New `spirv`->`llvm` conversion patterns for `OpGroupNonUniformRotateKHR`; note `Vulkan14FeatureInventory.md`'s existing finding that no `OpGroupNonUniform*` operation converts at all yet (`shaderSubgroupExtendedTypes` is vacuously true for the same reason) — this row should audit and likely close that whole family together rather than add one more vacuous bit~~ (done: `RotateConversionPattern` (`SPIRVToLLVMPatterns.cpp`) converts `spirv.GroupNonUniformRotateKHR` -- both the plain and `cluster_size` forms -- into the SPIR-V spec's own invocation-id arithmetic followed by an `llvm.spv.wave.readlane` shuffle, since LLVM's SPIR-V backend has no generic `llvm.spv.*` intrinsic for rotate itself (only an OpenCL-C-builtin-call path no other pattern here uses). `shaderSubgroupRotate`/`shaderSubgroupRotateClustered` and `VK_KHR_shader_subgroup_rotate` are now advertised (`EntryPoints.cpp`/`PhysicalDeviceInfo.cpp`). Per the audit this row asked for: closing the *whole* `GroupNonUniform*` family (~30 more ops: vote/ballot/broadcast/shuffle/arithmetic-reduce/quad) turned out to be a separate, comparably-sized effort of its own rather than a small addition alongside Rotate, so it is **not** done by this row -- `shaderSubgroupExtendedTypes` therefore remains just as vacuously `VK_TRUE` as `Vulkan14FeatureInventory.md` originally found, since Rotate's own operand types never exercise an 8/16-bit/bool group operation; see that file's own new finding, and `agent_thoughts.md`'s F2 entry, for the full audit and why the rest is left as future work) | §1.2 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P1 |
| F3 | ~~**`VK_KHR_shader_float_controls2`/`shaderFloatControls2`.** Per-instruction (rather than per-module) rounding-mode/denorm-preservation execution modes; audit whether `VK_KHR_shader_float_controls`'s per-module form is implemented at all first (`Vulkan14FeatureInventory.md` doesn't list it, so check `SPIRVToLLVMPatterns.cpp`/`ImportSPIRV` before scoping this row)~~ (audited, per-instruction scope split off as F15: `VK_KHR_shader_float_controls`'s per-module form was **not** implemented at all -- `ExecutionModePattern` (`SPIRVToLLVMPatterns.cpp`) unconditionally erased every `spirv.ExecutionMode`, including `DenormPreserve`/`DenormFlushToZero`/`SignedZeroInfNanPreserve`/`RoundingModeRTE`/`RoundingModeRTZ`, and `EntryPoints.cpp` correctly advertised every one of those properties/features as unsupported, but a shader that actually declared one of these modes silently got whatever behavior the default conversion happens to produce rather than a diagnostic. Closed the correctness half of that gap: no FP op conversion pattern ever sets fast-math flags or a non-default rounding mode, so every `spirv.FAdd`/`FMul`/etc. this conversion produces is already the strict, round-to-nearest-even, denormal-preserving code `DenormPreserve`/`RoundingModeRTE`/`SignedZeroInfNanPreserve` ask for at every bit width -- `collectEntryPoints` (`ConvertSPIRVToLLVMPass.cpp`) now reads and accepts those three (still ultimately erased, like every other execution mode, by `ExecutionModePattern`), but rejects `DenormFlushToZero`/`RoundingModeRTZ` with a hard diagnostic instead of silently miscompiling the entry point, since nothing downstream can actually flush denormals or truncate rather than round. The extension itself remains unadvertised (`EntryPoints.cpp`'s properties are all still the conservative "no independent per-width handling" value) since a conformant implementation must support whichever modes it advertises, and `DenormFlushToZero`/`RoundingModeRTZ` still are not -- actually producing flush-to-zero and truncating-rounding code (not just detecting and rejecting shaders that ask for it) is future work; see `agent_thoughts.md`'s F3 entry and F15 below) | §1.2 | `feme/lib/Conversion/SPIRVToLLVM` | P2 |
| F15 | ~~**`VK_KHR_shader_float_controls2`/`shaderFloatControls2`** (per-instruction rounding-mode/denorm-preservation, superseding F3's per-module `VK_KHR_shader_float_controls`) **, plus actually producing `DenormFlushToZero`/`RoundingModeRTZ` code for `VK_KHR_shader_float_controls` itself** (F3 only diagnoses a shader that declares either rather than producing them). Both need every arithmetic FP op conversion pattern (`FAdd`/`FSub`/`FMul`/`FDiv`/`FRem`/`FNegate`/converts) to route through LLVM's `llvm.experimental.constrained.*` intrinsics with an explicit rounding-mode/exception-behavior metadata operand and the enclosing function marked `strictfp`, rather than the plain unconstrained ops they emit today, plus a way to represent flush-to-zero (LLVM's `denormal-fp-math`/`denormal-fp-math-f32` function attributes cover `f32` only, not `f16`/`f64` independently, so `float_controls2`'s explicit `FPDenormMode`/`FPRoundingMode` *decorations* on individual instructions -- rather than only a whole-function execution mode -- may need a different lowering strategy entirely). The largest single row F3's own audit produced; consider splitting the constrained-intrinsics plumbing (a prerequisite both the flush-to-zero and rounding-mode-RTZ halves of `VK_KHR_shader_float_controls` share) from the newer `float_controls2` decorations themselves~~ (split as this row's own text suggested: the constrained-intrinsics-plumbing prerequisite closed as F15a -- `RoundingModeRTZ` now genuinely produces truncating-rounding-mode code, verified against LLVM's real, in-tree SPIRV backend, though still not advertised pending an unrelated `feme::cpu` resource-lowering gap a targeted CTS run found -- `DenormFlushToZero`'s own flush-to-zero lowering split off as F15b, and `VK_KHR_shader_float_controls2`'s newer per-instruction decorations split off as F15c; `spirv.FNegate` turned out not to need any of this at all, since negation is an exact sign-bit flip LLVM has no constrained form of; see `agent_thoughts.md`'s F15a entry and F15b/F15c below) | F3 | `feme/lib/Conversion/SPIRVToLLVM` | P2 |
| F15b | ~~**Actually produce `DenormFlushToZero` code for `VK_KHR_shader_float_controls`** (F15a's own remaining half; still only diagnosed, per F3). Unlike `RoundingModeRTZ`, LLVM has no constrained-intrinsics equivalent for flush-to-zero -- `denormal-fp-math`/`denormal-fp-math-f32` are whole-function attributes covering `f32` only, not `f16`/`f64` independently -- so honoring this execution mode's own per-width operand likely needs an explicit software flush (zero any subnormal operand/result of the declared width's arithmetic ops) rather than an intrinsic swap, a materially different lowering strategy than F15a's~~ (closed: unified `ConstrainedRoundTowardZeroPattern` and the new flush logic into one `FloatControlArithmeticPattern` (`SPIRVToLLVMPatterns.cpp`) so a width can honor `RoundingModeRTZ`, `DenormFlushToZero`, both, or neither, independently; the flush itself is exactly the software swap this row predicted -- `llvm.is.fpclass`'s subnormal bit-mask, `llvm.copysign` to preserve the flushed value's sign, and `llvm.select` -- applied to each arithmetic op's operands and its own result, needing no `strictfp` (unlike `RoundingModeRTZ`'s constrained intrinsics) since none of it is a constrained-FP operation. `collectEntryPoints`'s former `rejectUnhonoredFloatControls` diagnostic is gone entirely: every `VK_KHR_shader_float_controls` execution mode is now genuinely honored rather than merely diagnosed or accepted-and-dropped. `EntryPoints.cpp`'s advertised properties remain conservatively unset pending the same unrelated `feme::cpu` resource-lowering CTS gap F15a's row found; see `agent_thoughts.md`'s F15b entry) | F15a | `feme/lib/Conversion/SPIRVToLLVM` | P2 |
| F15c | ~~**`VK_KHR_shader_float_controls2`/`shaderFloatControls2`**: per-instruction (rather than only per-entry-point) `FPRoundingMode` decorations -- MLIR's `spirv` dialect already models the decoration (`SPIRV_FPRoundingModeAttr`), so this is "read a decoration on the individual `spirv.FAdd`/etc. op, not a whole-entry-point `spirv.ExecutionMode`, then reuse F15a's `ConstrainedRoundTowardZeroPattern`-shaped lowering" rather than a new lowering strategy -- plus the extension's own `FPFastMathMode`/`FPFastMathDefault` decorations (contraction/reassociation/etc. fast-math bits), which are a separate, additive mechanism (LLVM's ordinary fast-math flags) rather than another constrained-intrinsics consumer. `VK_KHR_shader_float_controls2` does **not** add a per-instruction denorm-mode decoration at all (confirmed against the SPIR-V spec and LLVM's own `SPIRVSymbolicOperands.td`, which has no `FPDenormMode` decoration whatsoever) -- F15's original text assumed one existed; F15b's whole-execution-mode `DenormFlushToZero` remains the only way to ask for flushed denormals~~ (`FPRoundingMode` closed: `FloatControlArithmeticPattern` (SPIRVToLLVMPatterns.cpp, F15a/F15b's own pattern) now also reads this per-instruction decoration directly off the `spirv.FAdd`/`FSub`/`FMul`/`FDiv`/`FRem` op it decorates -- no separate before-conversion collection pass needed, unlike `RoundingModeRTZ`/`DenormFlushToZero`, since MLIR's deserializer already attaches it there -- honoring all four `FPRoundingMode` directions (not just `RTZ`), including an explicit `RTE` overriding an entry point's own whole-module `RoundingModeRTZ` back to plain code for one instruction; verified against LLVM's real, in-tree SPIRV backend for `RTP` (`spirv-backend-per-instruction-rounding-mode.mlir`), the one direction neither `VK_KHR_shader_float_controls`'s execution mode nor F15a's own test could express. `FPFastMathMode` also closed: translated to LLVM's ordinary fast-math flags on the plain (non-constrained) op, confirmed at the real, translated LLVM IR level (`spirv-to-llvmir-per-instruction-fast-math.mlir`) -- dropped instead on an instruction that also needs a non-default rounding mode, since LLVM's constrained intrinsics carry no fast-math flags of their own. `FPFastMathDefault` (an execution mode, not a decoration, setting a per-floating-point-type default `FPFastMathMode` for a whole entry point) is **not** closed: this MLIR fork's SPIR-V dialect has no `FPFastMathDefault` execution-mode enumerant at all (confirmed against `SPIRVBase.td`'s full `SPIRV_EM_*` list), a dialect-level gap outside `feme/lib/Conversion/SPIRVToLLVM`'s own layering to fix; split off as F15d. See `agent_thoughts.md`'s F15c entry) | F15a | `feme/lib/Conversion/SPIRVToLLVM` | P2 |
| F15d | ~~**`VK_KHR_shader_float_controls2`'s `FPFastMathDefault` execution mode, and the missing `FloatControls2` capability** (F15c's own remaining half): a targeted CTS run (`dEQP-VK.spirv_assembly.instruction.compute.float_controls2.*`, see `VulkanCTSReport.md`) found a second, more fundamental gap alongside `FPFastMathDefault` itself: MLIR's `spirv` dialect has no `FloatControls2` capability enumerant at all (SPIR-V capability 6029; absent from `SPIRVBase.td`'s `SPIRV_C_*` list), so a real CTS-generated shader declaring `OpCapability FloatControls2` -- which every shader using this extension's own decorations does -- fails to deserialize at all ("unknown capability: 6029"), regardless of how correct F15c's own decoration handling is. This row needs, in order: (1) the `FloatControls2` capability enumerant itself, so real shaders using this extension can be imported in the first place; (2) the `FPFastMathDefault` execution-mode enumerant (also absent from `SPIRVBase.td`'s `SPIRV_EM_*` list), which sets a per-floating-point-type default `FPFastMathMode` for every otherwise-undecorated instruction in an entry point, via `OpExecutionModeId` naming a spec constant; (3) the newer, non-INTEL `AllowContract`/`AllowReassoc`/`AllowTransform` `FPFastMathMode` bits the extension itself adds (distinct from this dialect's existing `AllowContractFastINTEL`/`AllowReassocINTEL` vendor pair, which F15c's own `FPFastMathMode` decoration support already maps); (4) `FloatControlArithmeticPattern`/collectEntryPoints (SPIRVToLLVMPatterns.cpp/ConvertSPIRVToLLVMPass.cpp) to apply an entry point's declared default, per floating-point type, to every arithmetic op of that type lacking its own `FPFastMathMode` decoration -- the same "decoration overrides entry-point-wide default" precedence F15c's own `FPRoundingMode` override already established for `RoundingModeRTZ`. (1)-(3) are dialect-level gaps outside `feme/lib/Conversion/SPIRVToLLVM`'s own layering to fix~~ (all four closed: (1) `SPIRV_C_FloatControls2` (`SPIRVBase.td`) plus the `SPV_KHR_float_controls2` extension it needs (confirmed against the real SPIR-V registry name, which drops "shader\_" -- unlike the Vulkan-side `VK_KHR_shader_float_controls2` -- matching `SPV_KHR_float_controls` itself); (2) `SPIRV_EM_FPFastMathDefault`, whose two `OpExecutionModeId` operands (a Target Type <id> and a Fast-Math Mode <id> that must not be a specialization constant) needed `ExecutionModeIdOp` itself widened from `SymbolRefArrayAttr` to a plain `ArrayAttr`, since neither operand is a symbol reference the way `LocalSizeId`'s are -- verified through a full binary round trip via `mlir-translate`/`spirv-val`, not just textual `spirv` dialect parsing; (3) `AllowTransform`, genuinely new, as its own bit, and `AllowContract`/`AllowReassoc` by widening the existing `AllowContractFastINTEL`/`AllowReassocINTEL` enumerants' own capability list instead of adding same-valued siblings, which is a duplicate-`case` compile error in the generated availability switch even though the SPIR-V spec itself reuses the bit position; (4) `FastMathDefaultMap`, collected by `collectEntryPoints` the same way `RoundingModeRTZWidths` is, consumed by `FloatControlArithmeticPattern` through a new shared `translateFastMathMode` helper (factored out of F15c's own decoration-reading code) -- verified against the real translated LLVM IR, confirming both the entry-point default applying to an undecorated op and a per-instruction decoration still overriding it for one instruction. A targeted CTS re-run (`dEQP-VK.spirv_assembly.instruction.compute.float_controls2.fp32.*`, temporarily flipping the same four feature-advertisement gates F15c's own probe used) found the "unknown capability: 6029" failure completely gone, replaced by the already-known `feme::cpu` resource-lowering gap (F15a/F15b's own finding) for every case that got as far as `vkCreateComputePipelines` -- and one further, unrelated new gap: an assertion failure deserializing a `FrexpStruct`/`ModfStruct` result type, which does not involve float controls at all and is split off as its own row, F16, rather than folded into this one; see `agent_thoughts.md`'s F15d entry and F16 below) | F15c | `mlir/include/mlir/Dialect/SPIRV/IR` (dialect gaps), then `feme/lib/Conversion/SPIRVToLLVM` | P2 |
| F16 | ~~**MLIR's SPIR-V deserializer asserts on `FrexpStruct`/`ModfStruct` result types** (`Deserializer::processStructType`, `decoration.has_value()`), found incidentally by F15d's own targeted CTS re-run (`dEQP-VK.spirv_assembly.instruction.compute.float_controls2.fp32.input_args.frexp_st_*`) rather than anything float-controls-specific: these two GLSL.std.450 extended instructions return a two-member struct (fraction and exponent) whose members this dialect's deserializer apparently expects a member decoration on (likely `Offset`, the usual struct-layout one) that a `Function`-storage-class-only local struct like this one's result type has no reason to carry, crashing (`assert`, not a diagnosed error) rather than importing or cleanly rejecting the module. Needs, in order: (1) root-causing exactly which decoration `processStructType` unconditionally expects and why a plain (non-interface) struct type is exempt from needing it per the SPIR-V spec's own layout rules, then (2) a real `spirv.FrexpStruct`/`spirv.ModfStruct`-shaped op (`SPIRVGLSLOps.td` or the CL variant) and its `SPIRVToLLVM` conversion pattern, if this dialect does not already model either instruction at all -- unconfirmed as of this row~~ (root cause was neither guess: no decoration is *required* on `struct_fi`'s own members at all -- `OpMemberDecorate ... Offset` round-trips fine, since `Offset` is special-cased before ever reaching the crashing code. The real bug is CTS's `deco` variants decorating the *struct type itself* (not a member) with `FPFastMathMode`, which every one of these operations' own `IDsToDecorate` list includes (`struct_fi`/`struct_ff`) -- `processStructType`'s struct-level-decoration path guesses the original `spirv::Decoration` enum back from its mangled attribute name via `convertToCamelFromSnakeCase`, which is not reliably reversible: `FPFastMathMode` mangles to `fp_fast_math_mode` the same way the nonexistent `FpFastMathMode` would, so unmangling it back guesses wrong and `symbolizeDecoration` fails, hitting the assert on any decoration sharing this ambiguity (also `FPRoundingMode`), not something specific to a struct-layout decoration. Fixed by building an exact reverse lookup once (mangling every known `spirv::Decoration`'s own name forward, the same way `setFunctionArgAttrs` already resolves ambiguity for `Aliased`/`Restrict`/etc., rather than unmangling an arbitrary string) and replacing the `assert` with a diagnosed error for the (now unreachable, since the lookup is exhaustive) case it still doesn't recognize. (2): the dialect already modeled `spirv.GL.FrexpStruct` but not `spirv.GL.ModfStruct` at all (added, opcode 36, mirroring `FrexpStruct`'s shape and verifier); neither had a `SPIRVToLLVM` conversion pattern, added upstream (not feme-specific: `mlir::populateSPIRVToLLVMConversionPatterns`, which feme already consumes) -- `FrexpStruct` maps directly onto LLVM's own `llvm.intr.frexp` (`DirectConversionPattern`, since its result struct shape already matches exactly), while `ModfStruct` has no matching intrinsic and decomposes into a truncation (integer part) plus a subtraction (fractional part), matching GLSL.std.450's own "x = i + fraction" definition, the same "no single intrinsic, build the struct by hand" shape `ArithmeticWithOverflowPattern` already established for `IAddCarry`/`ISubBorrow`. Verified against a hand-built SPIR-V binary reproducing CTS's exact `struct_fi`/`FPFastMathMode` shape (`spirv-as`/`spirv-val`, then `mlir-translate --deserialize-spirv`, which crashed before the fix and imports -- decoration included -- after it; then a full binary round trip back through `--serialize-spirv` and `spirv-val` again), not just textual `spirv` dialect parsing. A targeted CTS re-run of the exact triggering case was attempted (temporarily advertising `shaderFloatControls2` plus its dedicated and 1.2-promoted FP32 property triples, mirroring F15c/F15d's own methodology) but still could not reach shader compilation: CTS separately checks the dedicated `VkPhysicalDeviceShaderFloatControls2FeaturesKHR` chained struct, which this ICD does not yet handle at all (a distinct, unadvertised-extension gap, not this row's own deserializer bug) -- reverted immediately, nothing committed, same as F15d's own unsuccessful-flip precedent. `check-feme`/`check-mlir-dialect-spirv`/`check-mlir-target-spirv`/`check-mlir-conversion` all pass with this row's new tests | none | `mlir/lib/Target/SPIRV/Deserialization`, `mlir/include/mlir/Dialect/SPIRV/IR`, `mlir/lib/Conversion/SPIRVToLLVM` (dialect gaps, all fixed upstream) | P2 |
| F4 | ~~**`VK_KHR_shader_expect_assume`/`shaderExpectAssume`.** `OpAssumeTrueKHR`/`OpExpectKHR` lower directly to `llvm.assume`/`llvm.expect` intrinsics — one of the smallest rows in this whole breakdown, good as a first task for a new contributor~~ (done: `spirv.KHR.AssumeTrue`/`spirv.KHR.Expect` -- both already modeled by MLIR's own `spirv` dialect, needing no dialect-level work unlike several other F-rows -- now convert directly (`SPIRVToLLVMPatterns.cpp`'s `AssumeTrueConversionPattern`/`ExpectConversionPattern`) into `llvm.assume`/`llvm.expect`; `spirv.KHR.Expect`'s operand may be a vector of integer/bool, unlike `spirv.KHR.AssumeTrue`'s scalar-only condition, but LLVM's `llvm.expect` intrinsic is scalar-only, so a vector operand is expanded into one `llvm.expect` call per lane, mirroring `DotConversionPattern`'s own per-lane vector expansion. Verified against a real, hand-assembled SPIR-V binary (`spirv-as`/`spirv-val`) deserialized through MLIR's own importer (not just textual `spirv` dialect parsing), and against LLVM's real, in-tree SPIRV backend, which already lowers both intrinsics back to `OpAssumeTrueKHR`/`OpExpectKHR` (`SPIRVPrepareFunctions.cpp`'s `lowerExpectAssume`) once its own `-spirv-ext` allow-list enables `SPV_KHR_expect_assume` (empty by default, unlike every other capability this backend exercises so far). `shaderExpectAssume`/`VK_KHR_shader_expect_assume` are now advertised, agreeing between the aggregate `VkPhysicalDeviceVulkan14Features` struct and the dedicated `VkPhysicalDeviceShaderExpectAssumeFeatures` struct, exactly like `shaderSubgroupRotate`'s own precedent) | §1.2 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| F5 | ~~**`VK_KHR_line_rasterization`** (`rectangularLines`/`bresenhamLines`/`smoothLines` + their `stippled*` variants) **+ its 2 limit fields.** C4d already built a line-topology rasterizer (1-pixel-wide quad expansion); this row generalizes it to variable width and the three line styles, then adds stippling as a per-fragment pattern test against `VkPipelineRasterizationLineStateCreateInfo`'s stipple factor/pattern. The largest single G-track item in this section — consider splitting rectangular/bresenham/smooth from the three `stippled*` variants into two separately assignable pieces~~ (done, landed as one row rather than split in two -- the split turned out not to matter once both pieces used the same C4d quad-expansion path: `feme::graphics::RasterState` gained `LineRasterizationMode`/`LineWidth`/`StippledLineEnable`/`StippleFactor`/`StipplePattern`, and `executeDraws`' line-topology expansion (`feme/lib/Graphics/Executor.cpp`) now builds a variable-width rectangle for `Rectangular`/`RectangularSmooth` (the latter feathered 1 pixel for antialiasing coverage, carried through as `ScreenTriangle::EdgeDistance` and multiplied into the written alpha) or walks the pixel grid directly with Bresenham's own algorithm for `Bresenham` (always 1 pixel wide, per spec), and every mode carries each corner's arc-length distance (`ScreenTriangle::ArcLength`, accumulating across a `LineStrip`'s connected segments) to reject a covered sample whose position falls in one of the stipple pattern's "off" bits. `GraphicsPipeline.cpp`'s `translateRasterState` reads `VkPipelineRasterizationLineStateCreateInfoKHR` from the rasterization state's `pNext` chain, and `VK_DYNAMIC_STATE_LINE_WIDTH` (core 1.0, previously a `vkCmdSetLineWidth` no-op stub)/`VK_DYNAMIC_STATE_LINE_STIPPLE_KHR` (new, via `vkCmdSetLineStippleKHR`) both flow through `DynamicGraphicsState` like every other dynamic state. `rectangularLines`/`bresenhamLines`/`smoothLines` and their three `stippled*` variants are now advertised `VK_TRUE` (aggregate and dedicated `VkPhysicalDeviceLineRasterizationFeaturesKHR` structs), and `lineSubPixelPrecisionBits` is a real `4` (both structs) -- the same conservative floor `subPixelPrecisionBits`/`subTexelPrecisionBits` already use, honest since this software rasterizer's line positions are full `float` coordinates with no separate fixed-point snapping grid of their own. One accepted, explicitly out-of-scope deviation: `wideLines`/`largePoints` stay `VK_FALSE` and `lineWidthRange` stays the degenerate `[1.0, 1.0]` -- that is roadmap H7's row, not this one's, even though the line rasterizer itself now genuinely honors whatever width it is given) | E2, §1.8 (rasterizer) | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme::graphics` executor | P1 |
| F6 | ~~**`VK_KHR_vertex_attribute_divisor`/`vertexAttributeInstanceRateDivisor`(+`Zero`) + `maxVertexAttribDivisor`.** `VkVertexInputBindingDivisorDescription` extends the existing per-binding instance-rate stepping already implied by instanced draws; a divisor of 0 (every instance reads vertex 0) is the one new case, not a new mechanism~~ (done: `VkPipelineVertexInputDivisorStateCreateInfo`, chained from `VkPipelineVertexInputStateCreateInfo::pNext`, is translated (`GraphicsPipeline.cpp`'s `translateVertexInput`) into a `Divisor` field on the pipeline's own `VertexInputBinding` -- rejected at creation, not silently ignored or clamped, when it names a binding the pipeline never declared, a `VK_VERTEX_INPUT_RATE_VERTEX` binding (the divisor only ever applies to a per-instance one), or a value exceeding `maxVertexAttribDivisor`. The executor's per-instance fetch (`Executor.cpp`) generalizes its existing "index by instance, not vertex" step to the spec's own formula, `firstInstance + (instanceIndex - firstInstance) / divisor` -- `divisor == 1` (the default) reduces to exactly the plain per-instance fetch this already did before this row, confirming the roadmap row's own premise that this is not a new mechanism, and `divisor == 0` is that same formula's one further, explicitly-handled case, "every instance reads `firstInstance`". `maxVertexAttribDivisor` is a real, non-placeholder `0xFFFFFFFF` (`GraphicsPipeline.h`'s `MaxVertexAttribDivisor`, shared between the validation and the advertised property so the two can never disagree): the fetch-index division it bounds is a plain 32-bit integer divide with no narrower limit of its own, and `supportsNonZeroFirstInstance` is genuinely `VK_TRUE` since the formula above is not the simpler `instanceIndex / divisor` a driver restricted to `firstInstance == 0` would need instead. `vertexAttributeInstanceRateDivisor`/`vertexAttributeInstanceRateZeroDivisor` and `VK_KHR_vertex_attribute_divisor` are now advertised, agreeing between the aggregate `VkPhysicalDeviceVulkan14Features`/`...Properties` structs and the dedicated `VkPhysicalDeviceVertexAttributeDivisorFeaturesKHR`/`PropertiesKHR` structs, exactly like `VK_KHR_line_rasterization`'s own precedent) | E2 | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P2 |
| F7 | ~~**`VK_KHR_index_type_uint8`/`indexTypeUint8`.** `vkCmdBindIndexBuffer`'s existing 16/32-bit index read in `CommandBuffer.cpp`/the executor gains an 8-bit case — mechanical, narrow, good second task after F4~~ (done: `feme::graphics::IndexType` gained a third `UInt8` enumerator alongside `UInt16`/`UInt32`; `CommandBuffer.cpp`'s draw-time validation (`validateGraphicsDraw`'s index-type check, its `IndexBinding.Type` translation, and `validateDrawFetchBounds`'s `IndexSize` bounds check) accepts `VK_INDEX_TYPE_UINT8` alongside its two pre-existing cases, and the executor's index fetch (`Executor.cpp`) reads a 1-byte-per-element index (`ElemSize == 1`) with the type's own all-1-bits restart marker (`0xFF`, not 16/32-bit's `0xFFFF`/`0xFFFFFFFF`) for `primitiveRestartEnable`, mirroring the pre-existing 16-/32-bit cases exactly rather than introducing a parallel code path. `feme-render`'s scene YAML gained a matching `format: uint8` index-buffer encoding for lit-test coverage. `indexTypeUint8`/`VK_KHR_index_type_uint8` are now advertised, agreeing between the aggregate `VkPhysicalDeviceVulkan14Features` struct and the dedicated `VkPhysicalDeviceIndexTypeUint8FeaturesKHR` struct, exactly like `shaderExpectAssume`'s own precedent) | none | `feme/lib/Vulkan/CommandBuffer.cpp`, `feme::graphics` executor | P2 |
| F8 | ~~**`VK_KHR_dynamic_rendering_local_read`/`dynamicRenderingLocalRead` + its 2 limit fields.** `vkCmdSetRenderingAttachmentLocations`/`vkCmdSetRenderingInputAttachmentIndices` let a fragment shader read the current attachment bindings as input attachments without a render-pass restart; builds on V6's dynamic-rendering render-target binding~~ (done, jointly with F8a: both commands are implemented and validated -- restricted to a `vkCmdBeginRendering` instance, `colorAttachmentCount` checked against the current rendering instance, out-of-range/duplicate attachment indices rejected -- and reset to their identity-mapping default on every `vkCmdBeginRendering`, per each command's own man page. `vkCmdSetRenderingAttachmentLocations`' remapping is genuinely honored: `feme::graphics::PreparedDraw::ColorAttachmentLocations` (`CommandBuffer.cpp`'s `runDraw`) carries the mapping into the executor, whose fragment-output-to-attachment linkage (`Executor.cpp`) now searches by each attachment's own mapped location rather than assuming location `i` always feeds attachment `i`, honoring `VK_ATTACHMENT_UNUSED`'s "this location writes nowhere" case too. `vkCmdSetRenderingInputAttachmentIndices`' mapping is now genuinely consulted too, once F8a gave shader-side `subpassInput` local reads somewhere to go: `dynamicRenderingLocalRead`/`VK_KHR_dynamic_rendering_local_read` are now advertised (agreeing between the aggregate `VkPhysicalDeviceVulkan14Features` struct and the dedicated `VkPhysicalDeviceDynamicRenderingLocalReadFeaturesKHR` struct), scoped to a single-sample color attachment -- see F8a's own status note for exactly what that scope excludes, split off as F8b) | E2, V6 (done) | `feme/lib/Vulkan/{RenderPass,GraphicsPipeline,CommandBuffer}.cpp`, `feme::graphics` executor | P1 |
| F8a | ~~**F8's own remaining half: shader-side `subpassInput` local-read consumption.** Needs, in order: (1) `OpTypeImage` with `Dim=SubpassData` and `OpImageRead`'s subpass-local form modeled by the `spirv` dialect if it is not already (unconfirmed as of F8 -- audit first); (2) a `SPIRVToLLVM` conversion pattern reading directly from the currently-bound dynamic-rendering color/depth/stencil attachment (`feme::vulkan::RenderTargetBinding`, not a descriptor-set image) named by the shader's own `InputAttachmentIndex` decoration, resolved through `vkCmdSetRenderingInputAttachmentIndices`' mapping (`GraphicsState::ColorAttachmentInputIndices`/`DepthInputAttachmentIndex`/`StencilInputAttachmentIndex`, F8's own row) or, when that command was never called, the identity default the same row's man-page citation describes; (3) once real local reads produce correct pixels for a CTS-shaped test, `dynamicRenderingLocalRead`/`VK_KHR_dynamic_rendering_local_read` can finally be advertised (`EntryPoints.cpp`/`PhysicalDeviceInfo.cpp`, mirroring every other F-row's dedicated-struct-agrees-with-aggregate precedent); (4) `dynamicRenderingLocalReadDepthStencilAttachments`/`dynamicRenderingLocalReadMultisampledAttachments` need their own depth/stencil- and multisample-attachment local-read cases demonstrated (not just the plain color case (1)-(3) cover) before either limit flips to `VK_TRUE`~~ (done, for (1)-(3): (1) `OpTypeImage(Dim=SubpassData)`/`OpImageRead`'s subpass-local form were already modeled by the `spirv` dialect, but the audit found the deserializer *and* serializer both had no case for the `InputAttachmentIndex` decoration itself (43) -- both now fold it into the existing single-integer-literal decoration group, alongside `DescriptorSet`/`Binding`/`Location`/..., attaching it as a plain `input_attachment_index` attribute exactly the way `component`/`index` already flow through. (2) `feme::spirv::SubpassLoadPattern` (`SPIRVToLLVMPatterns.cpp`) converts a `Dim::SubpassData` `spirv.ImageRead` directly into `feme.stage.subpass.load` calls -- a new `feme::StageOpKind` -- reading the shader's own `InputAttachmentIndex` attribute rather than the descriptor handle `Adaptor.getImage()` still produces (left dead; `checkSupportedRaisedOps` (`UnsupportedOps.cpp`) now tolerates a totally unused register-bound handle, since a subpass read's whole point is *not* reading through it). `feme::cpu::SPIRVSubpassLoweringPass` (new) gives a using function its own `subpass_input_heap`/`subpass_input_heap_count` ABI parameters (`FemeShaderResources`, RuntimeABI.h), and `feme::vulkan::runDraw`'s new `buildSubpassInputHeap` (`CommandBuffer.cpp`) fills one `FemeImageDescriptor` per logical input-attachment index straight from the currently-bound attachments (not `VkDescriptorSet` state) via `GraphicsState::ColorAttachmentInputIndices`/`DepthInputAttachmentIndex`/`StencilInputAttachmentIndex`, threaded through `PreparedDraw`/`Executor.cpp` into `FragmentWrapper.cpp`'s `lowerFragmentSubpassLoad`, which reads the invocation's own `FragCoord` and calls the existing `feme.cpu.image.load.2d.v4f32` runtime helper -- no new runtime C code needed. (3) `dynamicRenderingLocalRead`/`VK_KHR_dynamic_rendering_local_read` are now advertised, proven by `DrawTest.SubpassLoadReadsBackTheColorAttachmentItWrote`'s real read-after-write pixel round-trip. (4) is *not* done: `dynamicRenderingLocalReadDepthStencilAttachments`/`dynamicRenderingLocalReadMultisampledAttachments` stay `VK_FALSE` -- split off as F8b, which also covers a depth/stencil subpass input's own `Dim::SubpassData` format-decode gap `buildSubpassInputHeap` does not yet address) | F8 | `mlir/lib/Target/SPIRV/{Deserialization,Serialization}`, `feme/lib/Conversion/SPIRVToLLVM`, `feme/lib/Transforms/CPU/{SPIRVSubpassLowering,FragmentWrapper}.cpp`, `feme/lib/Vulkan/{CommandBuffer,EntryPoints}.cpp` | P1 |
| F8b | ~~**F8a's own remaining quarter: depth/stencil and multisample subpass-input local-read coverage.** `feme::vulkan::buildSubpassInputHeap` (`CommandBuffer.cpp`) already resolves `DepthInputAttachmentIndex`/`StencilInputAttachmentIndex` into heap slots, but (a) leaves a `SampleCount > 1` attachment's slot unpopulated rather than addressing its per-sample layout (`FemeImageDescriptor::SampleCount`/`MipLayouts::SampleStride` already model one, unused so far), and (b) has not been exercised against a real depth (`D16_UNORM`/`D32_FLOAT`) or stencil (`S8_UINT`) format at all -- a CTS-shaped test reading a depth/stencil input attachment back through `subpassLoad`/`OpTypeImage(Dim=SubpassData)`'s single-component form is needed to find whatever format-decode gap remains before either limit field can honestly flip to `VK_TRUE`~~ (done, for the depth/stencil half of (b): the format-decode gap was in the CPU runtime, not `buildSubpassInputHeap` -- neither `femeRTImageFormatElementSize` nor `femeRTUnpackImageTexel` (`FeMeRuntimeCPU.c`) had a case for `D16_UNORM`/`D32_FLOAT`/`S8_UINT`, so every such fetch's format-guarded bound check silently rejected it and read back zero. Both tables now decode all three (`D16_UNORM`/`S8_UINT` normalized to `[0.0, 1.0]` in component 0, matching `A8_UNORM`'s own convention; `D32_FLOAT` the identity case, like `R32_FLOAT`), proven end to end by `DrawTest.SubpassLoadReadsBackThe{Depth,Stencil}AttachmentItWrote`, so `dynamicRenderingLocalReadDepthStencilAttachments` now honestly advertises `VK_TRUE`. (a) is also done -- `buildSubpassInputHeap`'s `populate` lambda no longer bails out on `SampleCount > 1`; it now derives `SampleCount`/`SampleStride` for a multisample attachment's heap slot the same way `Image.cpp`'s `computeSubresourceLayouts` does (every sample of one texel stored contiguously), and `femeRTFetchTexel2D`/`femeRTFetchTexel2DI32` derive their per-texel stride from that field instead of assuming `SampleCount == 1` (a latent `SampleStride == SlicePitch` mistake in `decodeASTCImageForSampling`'s own per-mip layout, harmless while the field went unread, surfaced and was fixed by this same change). But no caller yet threads an explicit sample index through a subpass load at all -- `feme::StageOpKind::SubpassLoad` has no `Sample` operand, and `SubpassLoadPattern` (`SPIRVToLLVMPatterns.cpp`) rejects any `spirv.ImageRead` that carries one -- so `dynamicRenderingLocalReadMultisampledAttachments` cannot honestly flip yet even though the heap-population half of its own gap is closed; the remaining SPIR-V/shader-side plumbing is split off as F8c) | F8a | `feme/lib/Vulkan/CommandBuffer.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme::graphics` executor | P2 |
| F8c | ~~**F8b's own remaining piece: an explicit-sample subpass-input local read, for `dynamicRenderingLocalReadMultisampledAttachments`.** `buildSubpassInputHeap`'s heap slot for a multisampled attachment is now correctly laid out (F8b), but nothing can address a sample other than 0 of it yet. Needs, in order: (1) a `Sample` operand on `feme::StageOpKind::SubpassLoad` (`StageOps.h`/`StageOps.cpp`), defaulting to a constant `0` where absent so single-sample callers are unaffected; (2) `SubpassLoadPattern` (`SPIRVToLLVMPatterns.cpp`) reading a real `spirv.ImageRead` `Sample` image operand instead of unconditionally rejecting one (`hasImageOperands`); (3) `FragmentWrapper.cpp`'s `lowerFragmentSubpassLoad` and the CPU runtime's `femeRTFetchTexel2D`/`feme.cpu.image.load.2d.v4f32` threading that sample index into the texel address `SampleStride` already supports, instead of always reading sample 0; (4) a CTS-shaped test binding a multisample color (and/or depth/stencil) attachment and reading a specific sample back through `subpassLoad`'s explicit-sample form before `dynamicRenderingLocalReadMultisampledAttachments` can honestly flip to `VK_TRUE`~~ (done: (1) `feme::StageOpKind::SubpassLoad` (`StageOps.h`/`StageOps.cpp`) now carries a third `sample` operand, `createStageSubpassLoad` defaulting it to a constant `i32 0` when the caller passes none, so every existing (single-sample) caller/test is unaffected. (2) `SubpassLoadPattern` (`SPIRVToLLVMPatterns.cpp`) now accepts a lone `Sample` image operand (`hasExactImageOperands`), threading its operand argument through as the third `feme.stage.subpass.load` argument instead of rejecting it via `hasImageOperands`; a real `spirv.ImageRead`/`OpImageRead` with a `Sample` operand was previously *unrepresentable* at all, not just unhandled by this pattern -- MLIR's own generic `spirv.ImageRead`/`ImageFetch`/`ImageWrite`/`ImageDrefGather` operand verifier (`mlir/lib/Dialect/SPIRV/IR/ImageOps.cpp`) asserted `Sample` was an "unimplemented Image Operand" unconditionally, so a minimal, spec-shaped validation case for it (integer-scalar operand, only legal on a fetch/read/write op whose image type has `MS=1`) had to be added there first, mirroring the existing `Lod` case's own shape. (3) `lowerFragmentSubpassLoad` (`FragmentWrapper.cpp`) now extracts `feme.stage.subpass.load`'s third operand per-lane and threads it into `feme::cpu::createLoad2D`'s new `Sample` parameter (`ImageCalls.h`/`.cpp`); every other caller of `createLoad2D`/`femeRTFetchTexel2D` (point/bilinear sampling, `OpImageFetch`'s non-subpass path) passes a constant `0`, unaffected. `femeRTFetchTexel2D`/`feme.cpu.image.load.2d.v4f32` (`FeMeRuntimeCPU.c`) add `Sample * Layout->SampleStride` to the texel address the `SampleStride`-aware stride F8b already computed. (4) `DrawTest.SubpassLoadReadsBackAnExplicitSampleOfTheColorAttachmentItWrote` seeds a 4-sample color attachment's backing memory directly with 4 distinct per-sample values (a real draw cannot produce those: this rasterizer's multisample write always stores one uniformly-shaded value into every covered sample, per `ResolvesMultisampleColorDuringRenderPass`'s own note, so there is no other way to construct 4 *different* per-sample values to read back distinctly), then a draw's fragment shader reads sample 2 specifically back through `subpassLoad`'s explicit-sample form (a `Dim::SubpassData`, `MultiSampled` `spirv.ImageRead` with a `["Sample"]` image operand) and re-stores it, proving the whole path end to end. A pre-existing, unrelated gap surfaced while writing that test and was fixed too: a dynamic-rendering pipeline's creation-time sample-count check (`GraphicsPipeline.cpp`'s `getRenderTargets`) always compared against a hardcoded single-sample default, since (unlike a `VkRenderPass`'s `VkAttachmentDescription::samples`) `VkPipelineRenderingCreateInfo` carries no sample-count field of its own -- every genuinely multisampled dynamic-rendering pipeline was rejected at creation time before this fix, with no dynamic-rendering multisample pipeline test around to catch it until this row's own test needed exactly that combination; the real per-draw sample-count check (`CommandBuffer.cpp`, against the actually bound attachment) already existed and is unaffected. `dynamicRenderingLocalReadMultisampledAttachments`/`VK_KHR_dynamic_rendering_local_read`'s scope is now complete, agreeing between `VkPhysicalDeviceVulkan14Properties` and the dedicated feature struct, closing F8 in full) | F8b | `feme/include/feme/Core/StageOps.h`, `feme/lib/Core/StageOps.cpp`, `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/lib/Transforms/CPU/FragmentWrapper.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `mlir/lib/Dialect/SPIRV/IR/ImageOps.cpp`, `feme/lib/Vulkan/GraphicsPipeline.cpp` | P2 |
| F9 | ~~**`VK_EXT_pipeline_protected_access`/`pipelineProtectedAccess`.** This ICD has no protected-memory model at all (no protected queue/allocation exists); the honest, conformant answer is likely to accept the flag as a no-op restriction (reject creating a protected+unprotected-mixed pipeline, matching the spec's validation rules) without implementing real memory protection — confirm against the spec's exact conformance requirement before assuming this is sufficient~~ (done: confirming against the spec found the roadmap row's own initial guess ("reject creating a...mixed pipeline") was not quite the actual rule -- the spec places no restriction on a pipeline's *creation* at all (both `VK_PIPELINE_CREATE_NO_PROTECTED_ACCESS_BIT`/`VK_PIPELINE_CREATE_PROTECTED_ACCESS_ONLY_BIT` are legal on their own, and nothing in the spec forbids setting both), only on where it may later be *bound* (`VUID-vkCmdBindPipeline-pipelineProtectedAccess-07408`/`-07409`). `Pipeline::createFlags` (`Pipeline.h`) now records `VkPipelineCreateInfo::flags` verbatim on every `VkPipeline` (compute and graphics alike -- `Pipeline.cpp`'s `vkCreateComputePipelines`, `GraphicsPipeline.cpp`'s `vkCreateGraphicsPipelines`), and `vkCmdBindPipeline` (`CommandBuffer.cpp`) consults it: since this ICD's `protectedMemory` feature always reports `VK_FALSE` (no protected queue/command-buffer/allocation exists, see `EntryPoints.cpp`), no `VkCommandBuffer` it ever hands out is a protected one, so a pipeline created with `VK_PIPELINE_CREATE_PROTECTED_ACCESS_ONLY_BIT` can never be legally bound anywhere -- `vkCmdBindPipeline` silently skips recording such a bind (matching `-07409`), exactly like its pre-existing ray-tracing-bind-point rejection. `VK_PIPELINE_CREATE_NO_PROTECTED_ACCESS_BIT` trivially satisfies its own restriction (`-07408`) instead, since every command buffer is already unprotected, so it binds normally. `pipelineProtectedAccess`/`VK_EXT_pipeline_protected_access` are now advertised, agreeing between the aggregate `VkPhysicalDeviceVulkan14Features` struct and the dedicated `VkPhysicalDevicePipelineProtectedAccessFeatures` struct, exactly like every other F-row's own precedent) | none | `feme/lib/Vulkan/{GraphicsPipeline,Pipeline,CommandBuffer}.cpp` | P2 |
| F10 | ~~**`VK_EXT_pipeline_robustness`/`pipelineRobustness` + its 4 `defaultRobustness*` limit fields.** `VkPipelineRobustnessCreateInfo` lets a pipeline opt in/out of robust buffer/image access per-binding-class rather than only device-wide; depends on E16's image-robustness groundwork existing to have something to opt in/out of~~ (closed: `Pipeline.h`'s new `PipelineRobustness`/`resolvePipelineRobustness` find, validate, and (for a stage's own chain, taking precedence over the pipeline-level one per the extension's own spec text) resolve a `VkPipelineRobustnessCreateInfo` at both `vkCreateComputePipelines` (`Pipeline.cpp`) and `vkCreateGraphicsPipelines` (`GraphicsPipeline.cpp`'s `translateFixedFunctionState`, independently per vertex/fragment stage). Closing this row's own four `defaultRobustness*` properties first required fixing two real gaps this row's own audit found: `Executor.cpp`'s vertex-attribute fetch failed the whole draw on an out-of-bounds read instead of reading zero per component, and `CommandBuffer.cpp`'s `validateDrawFetchBounds` pre-rejected any such draw before the executor even ran -- both contradicted `robustBufferAccess` already being unconditionally on. With those fixed, `defaultRobustnessStorageBuffers`/`UniformBuffers`/`VertexInputs` are `..._ROBUST_BUFFER_ACCESS` (bounds checking is unconditionally on regardless of any feature, always reading zero) and `defaultRobustnessImages` is `..._ROBUST_IMAGE_ACCESS` (E16's shader-visible texel fetch already returns all-zero for any out-of-range access) -- the weaker, non-`..._2` value in both cases, since the `..._2` siblings additionally require a null descriptor to read as zero too (`VK_EXT_robustness2`, unimplemented and unverified). `pipelineRobustness`/`VK_EXT_pipeline_robustness` are now advertised, agreeing between the aggregate `VkPhysicalDeviceVulkan14Features`/`Properties` structs and the dedicated `VkPhysicalDevicePipelineRobustnessFeatures`/`...Properties` structs, exactly like every other F-row's own precedent) | E2, E16 | `feme/lib/Vulkan/{GraphicsPipeline,Pipeline}.cpp` | P1 |
| F11 | ~~**`VK_EXT_host_image_copy`/`hostImageCopy` + its 6 limit/list fields.** The largest single new mechanism in this section: `vkCopyMemoryToImage`/`vkCopyImageToMemory`/`vkCopyImageToImage`/`vkTransitionImageLayout` copy/transition without a command buffer at all, needing a host-side (not executor-queued) path into `Image.cpp`'s existing layout/format machinery; `pCopySrcLayouts`/`pCopyDstLayouts` (per D1's own finding, an enumerated list, not a scalar limit) is the supported-layout list this new path accepts~~ (closed: `HostImageCopy.{h,cpp}` implements all four commands, reusing `ImageOps.h`'s `copyBufferImageRegion`/`runCopyImage` -- both moved there from `CommandBuffer.cpp`'s own anonymous namespace first, in a small non-functional refactor, since `copyBufferImageRegion` already took a `(void*, size)` region rather than a `Buffer&` and needed no other change to serve a raw host pointer with no bound `VkBuffer` of its own. `getSupportedHostImageCopySrcLayouts`/`DstLayouts` (`HostImageCopy.h`) report the conservative, spec-minimum `{VK_IMAGE_LAYOUT_GENERAL, VK_IMAGE_LAYOUT_TRANSFER_{SRC,DST}_OPTIMAL}` pair for `pCopySrcLayouts`/`pCopyDstLayouts` respectively (this ICD's copy is genuinely layout-agnostic -- plain memcpy regardless of the declared layout, `VK_IMAGE_TILING_OPTIMAL`/`_LINEAR` already being identical, Image.h's own file comment -- so a larger list would still be honest, but the minimum pair keeps this row's own scope narrow and matches the extension's own only-mandatory member). `identicalMemoryTypeRequirements` is a real `VK_TRUE` (one memory type only); `optimalTilingLayoutUUID` derives from the same `fillUUID` helper `deviceUUID`/`pipelineCacheUUID` already use (`PhysicalDeviceInfo.cpp`). A targeted CTS run against `dEQP-VK.image.host_image_copy.*` found two real, pre-existing gaps neither this row's own scope nor `copyBufferImageRegion`'s original CommandBuffer.cpp incarnation had ever been exercised against before: (1) the system's installed Vulkan loader (1.3.275) does not route a purely-1.4-core (no pre-promotion alias) command like `vkTransitionImageLayout`/`vkCopyMemoryToImage` to this ICD at all -- `vkGetDeviceProcAddr` still returns a *non-null* pointer (a generic loader-owned trampoline into its own never-populated per-device dispatch slot for a name it does not recognize as core), so calling it crashes on a null internal call rather than failing to resolve; this is an environment/loader-version limitation, not an ICD bug, confirmed by building a current (1.4.328) Vulkan-Loader from source and re-running the identical case, which then passed. (2) `copyBufferImageRegion`'s buffer-side row sizing always used the *whole* image format's own combined `bytesPerBlock`, silently wrong for a combined depth/stencil format (`D24_UNORM_S8_UINT`/`D32_FLOAT_S8X24_UINT`), whose copy always names exactly one aspect with its own, smaller, buffer-side size (e.g. `D32_FLOAT_S8X24_UINT`'s depth aspect is 4 buffer bytes/texel, not the combined format's 8) -- previously latent because a bound `VkBuffer`'s own size usually caught the resulting oversized row as a clean rejection first, but `vkCopyMemoryToImage`'s raw host pointer has no such size to catch it against, turning the same latent mis-sizing into a real out-of-bounds host-memory read/write (a segfault or heap corruption depending on timing). Fixed by rejecting a single-aspect copy of a combined depth/stencil format cleanly in `copyBufferImageRegion` itself (benefiting `vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer` too, not just this row's own new commands) rather than implementing the aspect-specific interleaved packing/unpacking `packDepthClear`/`packStencilClear` (ImageFixture.cpp) would need to do it correctly -- itself only implemented for `D24_UNORM_S8_UINT` today, not `D32_FLOAT_S8X24_UINT`, so real support is a separate, larger follow-up. See "Roadmap F11: measured impact" in VulkanCTSReport.md for the full CTS numbers) | E2, Image.cpp | new `feme/lib/Vulkan/HostImageCopy.{h,cpp}` | P1 |
| F12 | ~~**`VK_KHR_push_descriptor`/`pushDescriptor` + `maxPushDescriptors`.** `vkCmdPushDescriptorSet` writes descriptors directly into a command buffer's recorded state without a `VkDescriptorSet` object at all — a new, lighter-weight descriptor path alongside `Descriptor.{h,cpp}`'s existing pool-backed one, sharing its binding-to-heap-slot translation~~ (closed, together with E6's own deferred `vkCmdPushDescriptorSet2`/`vkCmdPushDescriptorSetWithTemplate2` per this row's own dependency note: `vkCmdPushDescriptorSet`/`vkCmdPushDescriptorSetWithTemplate` (`CommandBuffer.{h,cpp}`) each build a `DescriptorSet` snapshot this command buffer itself owns -- not a pool -- against the target `VkPipelineLayout`'s own declared layout for that set number (there is no `VkDescriptorSetLayout` handle argument at all, unlike every other descriptor-set entry point), apply the write/template array to it via two functions promoted out of `Descriptor.cpp`'s own file-local helpers (`applyDescriptorWrites`/`applyDescriptorUpdateTemplate`, now shared with `vkUpdateDescriptorSets`/`vkUpdateDescriptorSetWithTemplate`'s own loops), then record an ordinary `BindDescriptorSets`-shaped bind of it -- reusing `Descriptor.{h,cpp}`'s existing binding-to-heap-slot translation exactly as this row's own text called for, with no new dispatch-preparation path of its own. Per the extension's own spec text ("can be updated incrementally" until "the set is disturbed"), a push must build on the same slot's own most-recent snapshot rather than starting from empty -- but since commands only take effect later, when `executeCommandBuffer` replays every recorded command in order, each push's own snapshot must be an independent *copy* (`CommandBuffer::getOrCreatePushDescriptorSet`), not the same object mutated in place, or an earlier `BindDescriptorSets` in program order would see a later push's own writes too once execution reaches it -- proven by `CommandBufferTest.cpp`'s `PushDescriptorSetDispatchTest.WritesToOneBindingAccumulateAcrossPushes`, modeled on VK-GL-CTS's own `PushDescriptorIncrementalUpdatesComputeTest`. `vkCreateDescriptorUpdateTemplate` (`Descriptor.cpp`) now also accepts `VK_DESCRIPTOR_UPDATE_TEMPLATE_TYPE_PUSH_DESCRIPTORS` alongside the plain `_DESCRIPTOR_SET` type it already did, since `DescriptorUpdateTemplate`'s own entry list needs no distinction between the two. `maxPushDescriptors` is a real `32` (both the aggregate `VkPhysicalDeviceVulkan14Properties` case and a new dedicated `VkPhysicalDevicePushDescriptorProperties` case): the spec-mandated minimum, and also the real value, since a push descriptor set is an ordinary `DescriptorSet` object with no smaller heap of its own to cap it further. `pushDescriptor` reads `VK_TRUE` from the aggregate `VkPhysicalDeviceVulkan14Features` struct; unlike every other F-row's own feature bit, there is no dedicated `VkPhysicalDevicePushDescriptor*Features` struct to agree with (`VK_KHR_push_descriptor`'s own core promotion added only the properties struct above, not a features one) -- `EntryPoints.cpp`'s own comment records why. `VK_KHR_maintenance6`'s `vkCmdPushDescriptorSet2`/`vkCmdPushDescriptorSetWithTemplate2` -- deferred by E6 pending exactly this groundwork -- are thin `pNext`-extensible wrappers around the same two `CommandBuffer` methods, closing that row's own leftover item too)  | E2, E6 (F12 and E6's `vkCmdPushDescriptorSet2` share the same underlying mechanism — implement together or explicitly sequence one before the other) | `feme/lib/Vulkan/{Descriptor,CommandBuffer}.cpp` | P1 |
| F11a | ~~**`copyBufferImageRegion`'s aspect-specific combined depth/stencil copy, split off F11's own measured-impact finding.** `vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer`/`vkCopyMemoryToImage`/`vkCopyImageToMemory` all reject (rather than mis-copy or crash) a `VkBufferImageCopy`/`VkMemoryToImageCopy`/`VkImageToMemoryCopy` naming a single `VK_IMAGE_ASPECT_DEPTH_BIT`/`STENCIL_BIT` aspect of a combined depth/stencil image (`D24_UNORM_S8_UINT`/`D32_FLOAT_S8X24_UINT`) today; real support needs a per-aspect interleaved read-modify-write into the shared texel (extending `feme::graphics::packDepthClear`/`packStencilClear`/`unpackDepth`/`unpackStencil`, ImageFixture.cpp, from a single repeated clear/reference value to an arbitrary per-texel buffer region, and adding `D32_FLOAT_S8X24_UINT`'s own case to all four, which none of them have yet even for the existing `vkCmdClearDepthStencilImage`)~~ (closed: `D32_FLOAT_S8X24_UINT`'s own case is now added to `packDepthClear`/`unpackDepth`/`packStencilClear`/`unpackStencil` (ImageFixture.cpp) alongside `D24_UNORM_S8_UINT`'s existing one -- a plain write/read of the first of its two standalone 4-byte words for depth, a read-modify-write of the second word's low byte for stencil, unlike `D24_UNORM_S8_UINT`'s single shared 32-bit word. The real per-region generalization F11's own finding called for is two new functions, `copyDepthAspectRegion`/`copyStencilAspectRegion` (ImageFixture.h), rather than a signature change to `packDepthClear`/`packStencilClear` themselves (which stay single-clear-value APIs for `vkCmdClearDepthStencilImage`'s own use) -- each a per-texel read-modify-write between a tightly packed, aspect-only buffer region and the image's own interleaved combined texels, direct bit manipulation rather than a lossy round trip through `packDepthClear`'s `[0, 1]`-double clear-value path. `copyBufferImageRegion` (ImageOps.cpp) now rejects only the genuinely ambiguous case -- an `aspectMask` naming neither or both of `DEPTH_BIT`/`STENCIL_BIT` for a combined format -- and otherwise sizes the buffer side by the named aspect's own smaller size (`getDepthAspectBufferSize`/`getStencilAspectBufferSize`) and copies through the new region functions instead of one contiguous row `memcpy`, benefiting `vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer`/`vkCopyMemoryToImage`/`vkCopyImageToMemory` alike, since all four share this one function) | F11, ImageFixture.cpp's existing `D24_UNORM_S8_UINT`-only pack/unpack | `feme/lib/Vulkan/ImageOps.cpp`, `feme/include/feme/Graphics/ImageFixture.h`, `feme/lib/Graphics/ImageFixture.cpp` | P2 |
| F12a | ~~**A `std140` uniform buffer array's `spirv.AccessChain` fails SPIR-V->LLVM legalization when dynamically indexed, split off F12's own measured-impact finding.** `dEQP-VK.pipeline.monolithic.push_descriptor.compute.incremental_updates*` (4 cases) all fail `vkCreateComputePipelines` with `failed to legalize operation 'spirv.AccessChain' that was explicitly marked illegal`, indexing a `layout(std140) uniform Input { uint data[16]; } ubo;` block by `gl_GlobalInvocationID.x` (`!spirv.ptr<!spirv.struct<(!spirv.array<16 x i32, stride=16> [0]), Block>, Uniform>`) -- unlike the equivalent `std430 buffer` (storage buffer) array, which every other passing shader in this report's own scope already indexes dynamically without issue. `std140`'s own 16-byte array stride for a scalar element (as opposed to `std430`'s 4-byte one) is the one shape difference a uniform-buffer array's own lowering must additionally handle that a storage-buffer array's does not; root-causing needs comparing the two paths in `feme::cpu::SPIRVResourceLoweringPass`/the SPIR-V->LLVM conversion patterns to find where the `std140` stride case is unhandled~~ (closed, with a scope narrower than the 4 CTS cases this row's own text names: `feme::spirv::getUniformBlockElement` (SPIRVToLLVMPatterns.cpp) now recognizes a uniform block whose sole member is a fixed-size array the same way a storage buffer's own sole runtime-array member already is (`BlockElement::HasWrapper`), so its dynamic index reaches `llvm.spv.resource.getpointer` directly instead of a `getelementptr` MLIR's own `spirv::ArrayType` conversion cannot always produce (it refuses to convert an array at all unless its stride equals its element's own natural size, true for `std430` but not `std140`); the real stride is instead carried as `spirv.VulkanBuffer`'s own new third integer parameter, read back by a new `HandleKind::UniformArray` case in `feme::cpu::SPIRVResourceLoweringPass::classifyVulkanBufferHandle`, which multiplies the dynamic index by it exactly like a storage buffer's own. This closes the `spirv.AccessChain` legalization failure this row's own text names exactly -- confirmed by re-running this row's own 4 CTS cases, whose diagnostic no longer mentions `spirv.AccessChain` at all -- but does not, on its own, turn any of those 4 cases green: doing so surfaced a second, unrelated pre-existing gap (this shader's own `gl_GlobalInvocationID.x`, an `OpAccessChain` selecting one lane of a builtin `Input` vector FeMe models as a value rather than memory, has no dedicated conversion pattern and falls through to MLIR's own default one, which treats that value as if it were a real pointer -- `'llvm.getelementptr' op operand #0 must be LLVM pointer type ... but got 'vector<3xi32>'`), split off as its own new row (F12b) rather than folded into this one, matching how this row itself was split off F12's own measured-impact finding) | none | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme::cpu::SPIRVResourceLoweringPass` | P2 |
| F12b | ~~**A builtin `Input` vector's `spirv.AccessChain` (selecting one lane, e.g. `gl_GlobalInvocationID.x`) fails SPIR-V->LLVM legalization, split off F12a's own measured-impact finding.** `dEQP-VK.pipeline.monolithic.push_descriptor.compute.incremental_updates*` (the same 4 cases F12a's own text names) now fail `vkCreateComputePipelines` with `'llvm.getelementptr' op operand #0 must be LLVM pointer type or LLVM dialect-compatible vector of LLVM pointer type, but got 'vector<3xi32>'` once F12a's own std140 fix is applied. `BuiltInAddressOfPattern`/`LoadValuePattern` (SPIRVToLLVMPatterns.cpp) already model a builtin `Input` variable like `gl_GlobalInvocationID` as a plain SSA value rather than memory, and handle a *direct* load of the whole variable correctly, but an `spirv.AccessChain` selecting a single lane first (`gl_GlobalInvocationID.x`'s own `OpAccessChain %ptr_uint_Input %gl_GlobalInvocationID %uint_0` -- the shape glslang emits when only one component is ever read, apparently distinct from the shape it emits when the whole vector or more than one lane is read, which is presumably why every other passing shader in this report's own scope indexes by `gl_GlobalInvocationID.x`/`.xy`/etc. without issue) has no dedicated pattern of its own and falls through to MLIR's own default `AccessChainPattern`, which assumes its base operand converted to a real `!llvm.ptr` the way every other (memory-backed) `spirv.AccessChain` base does, and builds a `getelementptr` treating the raw vector value as if it were one instead. Root-causing needs a new pattern recognizing an `spirv.AccessChain` whose base is one of these value-modeled builtin variables and rewriting it (plus the `spirv.Load` that always follows it) to an ordinary `llvm.extractelement`, mirroring how `MatrixCompositeExtractPattern`'s own lane-selecting logic already works for a value rather than memory~~ (closed, exactly matching this row's own scope: a new `BuiltInAccessChainPattern` (SPIRVToLLVMPatterns.cpp) recognizes an `spirv.AccessChain` whose base operand converted to a value-modeled builtin vector rather than a real `!llvm.ptr` -- registered at the same `FeMeBenefit` as `BuiltInAddressOfPattern` so it wins over MLIR's own default `AccessChainPattern` -- and rewrites a single-lane selection of it directly to `llvm.extractelement`, mirroring `MatrixCompositeExtractPattern`'s own lane-selecting logic exactly as this row's own text called for; the `spirv.Load` that always follows collapses to the identity via the pre-existing `LoadValuePattern`, unchanged. A targeted re-run of this row's own 4 CTS cases (reproduced directly through the standalone `feme` driver, since -- unlike `GraphicsPipeline.cpp`'s `logCreationFailure` hook -- `Pipeline.cpp`'s compute pipeline path has no equivalent way to surface the underlying diagnostic through `deqp-vk` itself) confirms the exact `getelementptr`/`vector<3xi32>` diagnostic this row's own text names is gone, without reopening F12a's own `spirv.AccessChain` legalization failure; the same reproduction now hits a different, pre-existing, already-tracked gap instead (the "resource handle the FeMe CPU target cannot normalize" class E6/F8's own reports already note, this time for a `std140` uniform block reached through a runtime `vkCmdPushDescriptorSet` write) -- not a new one to split off, and not this row's own scope to close. See "Roadmap F12b: measured impact" in VulkanCTSReport.md) | none | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| F13 | ~~**`VK_KHR_load_store_op_none` (no feature bit).** `VK_ATTACHMENT_LOAD_OP_NONE`/`VK_ATTACHMENT_STORE_OP_NONE` are two new enumerants `RenderPass.cpp`'s existing load/store-op switch already has a natural "do nothing" case for — one of the smallest rows in this whole breakdown~~ (closed: this row's own prediction held exactly -- `applyClear`'s existing `LoadOp != VK_ATTACHMENT_LOAD_OP_CLEAR` check (`CommandBuffer.cpp`, not `RenderPass.cpp`: the actual load-op switch moved there when render-pass and dynamic-rendering attachments were unified into one `RenderTargetView`) already treats `NONE` as "do nothing" like `DONT_CARE`/`LOAD`, and `StoreOp` is never read to act on at all in this real-memory-backed software renderer, so `STORE`/`DONT_CARE`/`NONE` are already indistinguishable outcomes. The only real work was declarative: advertising `VK_KHR_load_store_op_none` (`PhysicalDeviceInfo.cpp`), plus discovering and fixing a pre-existing bookkeeping gap this row's own doc update surfaced -- `AdvertisedExtensions.txt`/`AdvertisedPromotedExtensions.txt`/`AdvertisedPromotedFeatures.txt`/`PlannedExtensions.txt` had drifted out of sync with `PhysicalDeviceInfo.cpp`/`EntryPoints.cpp` across six already-closed rows (F2, F5, F7, F8, F9, F12), understating both inventories. See "Roadmap F13: measured impact" in VulkanCTSReport.md) | none | `feme/lib/Vulkan/{PhysicalDeviceInfo,CommandBuffer}.cpp` | P2 |
| F14 | ~~**`VK_KHR_map_memory2`.** `vkMapMemory2`/`vkUnmapMemory2` are `pNext`-extensible wrappers around the existing `Memory.cpp` map/unmap, adding `VkMemoryUnmapFlagsKHR`'s reserve-on-unmap bit~~ (done: `vkMapMemory2`/`vkUnmapMemory2` (Memory.cpp) unwrap `VkMemoryMapInfo`/`VkMemoryUnmapInfo` and call the existing `vkMapMemory`/`vkUnmapMemory`; `VK_MEMORY_UNMAP_RESERVE_BIT_EXT` is accepted as a validate-only no-op, since it only has real meaning together with `VK_EXT_map_memory_placed`'s reservation-based mapping, which this ICD does not implement. `VK_KHR_map_memory2` is now advertised (`getSupportedDeviceExtensions`). A real `dEQP-VK.memory.mapping.*` run first SIGSEGV'd through a null function pointer -- this CTS checkout only loads the core, non-`KHR`-suffixed names below its own negotiated `usedApiVersion >= 1.4`, and this driver's `icd.json` `api_version` (1.1) makes that core name otherwise unreachable through the real loader, exactly the `VK_KHR_maintenance5` granularity-function gap `vk_gen_entrypoints.py`'s `SUPPORTED_EXTENSIONS` comment already documents -- fixed by also implementing `vkMapMemory2KHR`/`vkUnmapMemory2KHR` and adding `VK_KHR_map_memory2` to that tuple; the full 4466-case group then passed 100%. See `agent_thoughts.md`'s F14 entry and `VulkanCTSReport.md`'s own "Roadmap F14" section) | none | `feme/lib/Vulkan/Memory.cpp` | P2 |

Recommended independent lanes: **Lane 1** (shader compilation, no
feature-bit dependency once E1/E2 land): F2, F3, F4, F16. **Lane 2**
(rasterizer/graphics pipeline): F5, F6, F13. **Lane 3** (command
buffer/index/memory): F7, F14. **Lane 4** (descriptors, sequence F12 and
E6 together): F12. **Lane 5** (robustness/protected access, sequence
after E16): F9, F10. **Lane 6** (new subsystems, largest single items,
good candidates for a dedicated owner rather than a quick task): F1,
F11.

#### 1.9.6 Cross-cutting tasks, not gated on any single E/F/H/J row

(This table's own rows are numbered G1-G5 for historical reasons and are
*not* the FeMeGraphicsDesign.md milestones G0-G8 that §1.8, §1.9.7 and
§1.9.8 cite. Where a row below says "G", read "this table"; everywhere
else in this document, "G0"-"G8" is a graphics milestone.)

| # | Task | Priority |
|---|---|---|
| G1 | **Promote D4 (continuous, crash-tolerant CTS measurement) from "whenever time allows" to a hard prerequisite for merging any E/F row past the first two or three.** With ~33 independent rows landing from potentially-independent agents, a hand-rolled per-run Python diff (D0-D3's own method) does not scale; land D4 early enough that each E/F row's own PR can cite an automated before/after rather than a manually-run one | P0 |
| G2 | **Finish roadmap C10** (a checked-in expected-failure list and full 54-group CI job), still open per §1.9.1's table. Without it, an E/F row's regression is discovered only by whoever happens to run the full suite next, the same class of gap D0's own second-pass loader crash and D3's misattributed buckets both trace back to | P0 |
| G3 | **A per-row CTS re-measurement discipline**, matching D1-D3's own precedent: every E/F row's PR should cite the specific `dEQP-VK` group(s) it targets and the actual before/after count for that group — not the whole 54-group suite unless the change plausibly touches more than one (the same "targeted, real, not simulated" scoping D1 used for its own no-op entrypoint-table change) | P0 |
| G4 | ~~**Re-triage `Vulkan14FeatureInventory.md`'s two verify-first rows** (E15's ASTC HDR depends on LDR ASTC's own status, F3's `shaderFloatControls2` depends on the unpromoted `shaderFloatControls`'s own status) before assigning them, since each may expand into its own separate row once checked~~ (E15 half done: confirmed no ASTC decode of any kind, LDR or HDR, and no block-compressed format support at all -- split into E20 (LDR prerequisite plus the block-layout groundwork the investigation also found missing in `Image.cpp`) and E21 (E15's original HDR-specific scope, now depending on E20); see E15/E20/E21 above and `agent_thoughts.md`. F3's `shaderFloatControls2` half is now also done: confirmed `VK_KHR_shader_float_controls`'s per-module execution modes were silently dropped by every conversion rather than honored or diagnosed; split into F3 itself (closed: reject the two modes -- `DenormFlushToZero`, `RoundingModeRTZ` -- this conversion cannot honor, accept the three it already honors by construction) and F15 (further split, once its own shared constrained-intrinsics prerequisite closed as F15a, into F15b's remaining `DenormFlushToZero` half and F15c's `float_controls2` per-instruction decorations); see F3/F15/F15a/F15b/F15c above and `agent_thoughts.md`) | P1 |
| G5 | **Once §1.9.4 (1.3) is fully closed under Strategy A, or continuously under Strategy B, re-run and update this section's own "not yet measured" rows** with real CTS deltas, the same way §1.9.1's C-rows each grew a "measured impact" parenthetical as they closed | P1 |

#### 1.9.7 Closing the graphics conformance surface (H-series)

§1.9.4 and §1.9.5 close the *core version* floor: the feature bits, limit
fields and promoted extensions a device claiming `VK_API_VERSION_1_4` must
report truthfully. They do not, on their own, make this a graphics device.
[FeMeVulkanDesign.md](FeMeVulkanDesign.md)'s "Conformance Target" now
declares graphics part of the conformance claim rather than a capability
that may or may not be added, so the gaps below stop being "correctly
`NotSupported`" and become tracked rows.

The evidence this section is derived from is
[VulkanCTSReport.md](VulkanCTSReport.md)'s own headline: of the 26 groups
with **zero** failures, `tessellation`, `geometry`, `multiview`,
`mesh_shader`, `wsi`, `transform_feedback`, `fragment_shading_rate`,
`fragment_shader_interlock`, `fragment_shading_barycentric`, `depth` and
`shader_object` are zero precisely because nothing behind them is
advertised. Under the old scope that was the correct end state; under this
one it is the measurement of how much graphics surface is missing.

Same granularity and testing discipline as §1.9.4: one row, one owner, one
commit series, one lit/unittest addition, and one targeted `deqp-vk`
before/after per §1.9.6's G3. "G5"/"G6"/"G7"/"G8" in the Depends-on column
are FeMeGraphicsDesign.md milestones (§1.8.5), not §1.9.6's own G-rows.

| # | Task | Depends on | Files | Priority |
|---|---|---|---|---|
| H1 | ~~**Triage the graphics half of E29's crash list first.** Two of the seven crashed groups in the current full run are graphics: `renderpasses.dynamic_rendering.*` (an `llvm::Value::setNameImpl` assertion, "Cannot assign a name to void values") and `glsl.texture_functions.query.texturesamples.*` (`SIGSEGV`). A crashed group's totals are unusable, so every H-row below is measured against a baseline these two corrupt; fixing them is what makes the rest of this section's numbers mean anything~~ (closed by roadmap E29e/E29c respectively -- see those rows for each fix's own detail. Both groups' totals are now real, usable baselines for every H-row below) | E29 | `feme/lib/Transforms/CPU/SIMDize.cpp`, `feme/lib/Vulkan/EntryPoints.cpp` | P0 |
| H2 | ~~**Layered rendering + `multiview`.** `vkCreateFramebuffer` rejects `layers != 1` (`RenderPass.cpp:383`) and `vkCreateRenderPass2` rejects any nonzero `viewMask` (`RenderPass.cpp:274`); the executor renders one layer. Add a layer dimension to the render-target binding and the raster loop, then `gl_Layer`/`ViewIndex` as stage system values, and only then advertise `multiview` — the one 1.2 feature roadmap C6 deliberately left unadvertised for exactly this reason. Closes C6's open exception and the whole `dEQP-VK.multiview` group~~ (done, with a narrower "whole group" outcome than this row's own text claimed: `vkCreateFramebuffer` accepts `layers > 1` (only `layers == 0` is still rejected, `isCompatibleAttachmentView`'s new `Layers` parameter checking each bound view actually covers that many); `vkCreateRenderPass`/`vkCreateRenderPass2` accept a nonzero `viewMask` (`VkRenderPassMultiviewCreateInfo`/`VkSubpassDescription2::viewMask`, both landing in a new `SubpassDescription::ViewMask`), and `VK_DEPENDENCY_VIEW_LOCAL_BIT`/a nonzero `viewOffset` are accepted rather than rejected, for the same "already satisfied by strictly sequential execution" reason every other subpass dependency field is. `RenderTargetBinding` grows `Layers`/`ViewMask`; `resolveAttachmentView` returns every requested array layer of a `VK_IMAGE_VIEW_TYPE_2D`/`_2D_ARRAY` view (addressed layer-major via roadmap R34's own, previously-unwired, `AttachmentView::ArrayLayers`/`getAttachmentLayerByteOffset`) instead of only layer 0. `CommandBuffer.cpp`'s `runDraw` runs one draw per set `ViewMask` bit (or once, at view 0, for the non-multiview common case), slicing every attachment down to that view's own layer (`sliceAttachmentLayer`) and setting a new `PreparedDraw::ViewIndex` field; per Vulkan's own multiview rule, a view with no explicit `gl_Layer` output writes into the array layer numbered the same as its view index, which needs no vertex/geometry stage cooperation to be correct -- the general per-primitive case (an explicit `RenderTargetArrayIndex`/`gl_Layer` write routing to an arbitrary layer) still has no producer (`shaderOutputLayer` is `VK_FALSE`, no geometry stage exists yet), so it is unaffected either way, left to H3/H5. Two system values land: `SignatureSystemValue::ViewIndex` (`gl_ViewIndex`, SPIR-V `BuiltIn` 4440), read from a new `FemeVertexInvocation`/`FemeFragmentInvocation::ViewIndex` ABI field by `VertexWrapper.cpp`/`FragmentWrapper.cpp`; `RenderTargetArrayIndex`/`gl_Layer`'s SPIR-V import mapping already existed (`BuiltIn` 9) but remains without a real producer for the same reason above. `multiview` is now advertised `VK_TRUE` (both the dedicated struct and the aggregate `VkPhysicalDeviceVulkan11Features`); `multiviewGeometryShader`/`multiviewTessellationShader` stay `VK_FALSE` (H4/H5); `VK_KHR_multiview` is added to `getSupportedDeviceExtensions` by name. `unittests/Vulkan/{RenderPass,DrawTest,PhysicalDeviceInfo}Test.cpp` gain layered-framebuffer-acceptance/rejection cases, a real end-to-end two-view/two-layer/two-`gl_ViewIndex`-color draw (`DrawTest.MultiviewRendersDifferentColorPerViewIntoItsOwnLayer`, confirmed correct per-layer output), and the flipped feature-bit expectation; `unittests/Transforms/Graphics/CanonicalizeStageTest.cpp` covers the new `BuiltIn ViewIndex` mapping; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1799/1800 (1 pre-existing, unrelated `Unsupported`), up from 1795/1796 before this row. **Deviation**: a real, targeted `dEQP-VK.multiview` run (838 cases) found the "whole group" claim did not hold -- zero cases pass outright. 454 hit a *different* shape of the pre-existing `feme-cpu-simdize` "divergent vector value ... component decomposition" diagnostic than the four roadmap C3 already closed (root cause unstarted -- see new row H2a); 42 hit `view_mask_iteration`'s `VK_FORMAT_R8G8B8A8_UINT` color attachment, an integer format `isSupportedColorAttachmentFormat` correctly declines, pre-existing and out of this row's own scope (roadmap H8); 3 hit a zero-color-attachment "depth without a fragment shader" pipeline shape this executor has never supported (see new row H2b); the remainder are legitimately `NotSupported` (`geometryShader`, `VK_EXT_depth_range_unrestricted`). See "Roadmap H2: measured impact" in VulkanCTSReport.md for the full breakdown, and FeMeVulkanDesign.md's updated "Render passes and dynamic rendering" status note for the implementation detail) | G-track layered rendering | `feme/lib/Vulkan/{RenderPass,CommandBuffer}.cpp`, `feme/lib/Graphics/Executor.cpp` | P0 |
| H2a | ~~**Root-cause the `feme-cpu-simdize` divergent-vector shape `dEQP-VK.multiview`'s own fragment shaders hit** (454 of 838 cases in H2's own measured run): a shape distinct from the four `phi`/`select`/`shufflevector`/`extractelement` patterns roadmap C3 already closed, since C3's own fix does not reach it. Likely a new member of C8's "shader long tail" bucket rather than a new root cause of its own — triage against C8 first~~ (done -- root-caused, not fixed: this row's own framing did not hold. Every one of the 454 cases is the *vertex* shader's `gl_Position` write via glslang's implicit `gl_PerVertex` interface block (`{Position, PointSize, ClipDistance[1], CullDistance[1]}`), not a fragment shader's output. SPIR-V decorates a `BuiltIn` interface block's members individually (`OpMemberDecorate`), not the block variable itself (`OpDecorate`) the way a standalone builtin variable is, but `SPIRVToLLVMPatterns.cpp`'s `buildStageIODecorationsAttr` only ever reads a whole-variable `built_in`/`location` attribute -- so the block's own `llvm.mlir.global` carries no `!spirv.Decorations` metadata at all, `feme::graphics::CanonicalizeStagePass`'s `isSPIRVStageIOGlobal` never recognizes it, and its `gl_Position` store is left exactly as raw as it started, reaching `feme::cpu::SIMDizePass` directly and correctly diagnosing the now-divergent vector value it was never supposed to see. Triage against C8 confirms it is a new member of C8's "shader long tail" bucket, as this row's own text predicted, and a different root cause from both C3 (SIMDize's own decomposition shapes, unaffected -- this gap never reaches SIMDize having been legalized at all) and C8b (a matrix/aggregate value SIMDize cannot widen *once legalized*; this one is never legalized in the first place). It is also distinct from C8's own original, since-closed finding: that one's root cause was "`CanonicalizeStagePass` is never invoked"; this one is invoked, but its own interface-block recognition has a scope gap neither roadmap R19 nor R20 covered (both scoped to "a non-builtin `Input`/`Output` variable" or "a builtin *variable*", never a builtin interface *block*). `CanonicalizeStageTest.DoesNotRecognizeMemberDecoratedInterfaceBlockAsStageIO` locks down the still-open gap at the unit level, using the real (undecorated, struct-typed) shape -- the *existing* `MapsSPIRVBuiltInsToSystemValues` test's own `@gl_Position` fixture turns out to have modeled it incorrectly all along (as a standalone, whole-variable-decorated global), which is exactly why this gap went unnoticed until a real `deqp-vk` run caught it. See "Roadmap H2a: measured impact" in VulkanCTSReport.md for the full reproduction. The fix itself -- extending both the SPIR-V import side and `CanonicalizeStage.cpp`'s signature-building/legalization to decompose a builtin interface block into one `SignatureElement` per member -- is left to new rows H2c/H2d) | C3, C8 | `feme/lib/Transforms/CPU/SIMDize.cpp` | P1 |
| H2c | ~~**Preserve a SPIR-V builtin interface block's (e.g. `gl_PerVertex`) per-member decorations.** `SPIRVToLLVMPatterns.cpp`'s `buildStageIODecorationsAttr` only reads a whole-variable `BuiltIn`/`Location`/`Component`/`Index` attribute (`Op.getBuiltIn()`), never a struct type's own per-member decorations (`mlir::spirv::StructType::getMemberDecorations`, already used by this same file's `isBufferBlockWritable` for a storage-buffer block's `NonWritable` member decoration) -- so a builtin interface block's own `llvm.mlir.global` carries no `!spirv.Decorations` metadata at all (roadmap H2a's own root cause). Needs a per-member decorations encoding (extending, not replacing, the existing whole-variable `!spirv.Decorations` shape `feme::spirv::attachStageIODecorations`/`parseSPIRVDecorations` already round-trip) attached to the block's global, covering `gl_PerVertex`'s four members at minimum~~ (done -- `StageIOGlobalVariablePattern` now also checks whether a stage-IO variable's pointee is a `mlir::spirv::StructType`, and if so builds a *second* `llvm.mlir.global` attribute, `feme.spirv.member.decorations` (`buildMemberDecorationsAttr`/`buildMemberDecorationTuple`, new in `SPIRVToLLVMPatterns.cpp`), from its members' own `OpMemberDecorate`d `BuiltIn`/`Location`/`Component`/`Index`/interpolation decorations, in an `(memberIndex, tuples)` shape whose `tuples` reuses the existing whole-variable `(i32 decoration, i32 arg...)` tuple encoding unchanged. `feme::spirv::collectStageIOMemberDecorations`/`attachStageIOMemberDecorations` (`StageIODecorations.cpp`, `SPIRVToLLVM.h`) mirror the existing whole-variable collect/attach pair, turning it into `feme.spirv.MemberDecorations` metadata on the real `llvm::GlobalVariable`, and `SPIRVToLLVMTranslator.cpp` wires both into the same collect-before/attach-after `translateModuleToLLVMIR` sequence the whole-variable channel already uses. **Deviation**: unlike `!spirv.Decorations`, this new metadata kind is *not* real SPIR-V backend metadata -- `OpMemberDecorate` decorates a struct *type*, not a global variable, so there is nothing for LLVM's SPIRV backend to attach a per-member decoration to at the global-variable granularity `!spirv.Decorations` uses; the channel is FeMe-internal only, existing purely for H2d's `CanonicalizeStagePass` to read. `SPIRVToLLVMTest.{BuiltinInterfaceBlockPreservesMemberDecorations,UnrecognizedMemberDecorationIsFilteredOut,CollectStageIOMemberDecorationsFindsDecoratedGlobals,AttachStageIOMemberDecorationsBuildsMetadata,AttachStageIOMemberDecorationsIgnoresMissingGlobals}` cover the new attribute/collect/attach path at the unit level; `spirv-to-llvm-stage-io.mlir` gains a `gl_PerVertex`-shaped case and a new `spirv-to-llvmir-stage-io-member-decorations.mlir` covers the full end-to-end `!feme.spirv.MemberDecorations` metadata shape. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1810/1811 (1 pre-existing, unrelated `Unsupported`), up from 1804/1805 before this row. A targeted `dEQP-VK.multiview` re-run (838 cases) confirms this row's own numbers are unchanged from H2's own baseline (0 pass / 499 fail / 339 not-supported) -- expected, since `CanonicalizeStagePass` does not read the new metadata until H2d lands; see "Roadmap H2c: measured impact" in VulkanCTSReport.md) | H2a | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/lib/Conversion/SPIRVToLLVM/StageIODecorations.cpp` | P1 |
| H2d | ~~**Decompose a builtin interface block into one `SignatureElement` per member during `CanonicalizeStagePass` legalization**, now that H2c makes each member's own decorations available: `isSPIRVStageIOGlobal`'s "one global, one signature element" assumption (`CanonicalizeStage.cpp`) needs to become "one global, N signature elements, one per struct member", each keeping its own `BuiltIn`/system-value identity (`gl_Position` -> `SignatureSystemValue::Position`, `gl_PointSize`/`gl_ClipDistance`/`gl_CullDistance` -> `None`, unmodeled system values, matching how an unrecognized DXIL semantic already converts), and `loadStageIOValue`/`storeStageIOValue`'s existing recursive per-(struct member, row, component) decomposition (built for C8a's matrix/aggregate case) needs to route each member through its own `ElementID` instead of the whole block's single one. `isSPIRVStageIOGlobal` also needs to recognize a global carrying only `feme.spirv.MemberDecorations` (no whole-variable `!spirv.Decorations`), the shape H2c's own builtin-interface-block global now produces. Closes the whole `dEQP-VK.multiview` group's own remaining largest blocker~~ (done, with the "closes the whole group" framing narrower than claimed, matching H2's own precedent: `isSPIRVStageIOGlobal` now recognizes a `feme.spirv.MemberDecorations`-only global, and `canonicalizeSPIRVStage` decomposes it into one `SignatureElement` per struct member (`gl_Position` -> `SignatureSystemValue::Position`; `gl_PointSize`/`gl_ClipDistance`/`gl_CullDistance` -> `None`, changed from this row's own dead, never-before-reachable `ClipDistance`/`CullDistance` mapping). **Deviation**: the first landing modeled a builtin interface block's own load/store as one whole aggregate value, a shape a real `deqp-vk` run found does not occur in practice -- real SPIR-V-derived IR addresses each member (even each individual component of `gl_Position`) with its own scalar load/store, either a bare global (SPIR-V's own offset-0 access, which LLVM's constant-`getelementptr` folding erases entirely) or a `getelementptr (i8, ptr @block, i64 ByteOffset)` `ConstantExpr` (LLVM's own canonical byte-offset form, not a struct-member-indexed `getelementptr`), crashing the first landing's `cast<StructType>` outright. A follow-up commit replaces the whole-struct-only resolution with `getStageIOBaseAndOffset`/`resolveStageIOAccess` (`Value::stripAndAccumulateConstantOffsets` plus the block's own `StructLayout`), fixing the crash. `CanonicalizeStageTest` gains `RecognizesMemberDecoratedInterfaceBlockAsStageIO` (the whole-aggregate shape) and `RecognizesInterfaceBlockPerMemberByteOffsetAccess` (the real shape); two new `spirv-canonicalize-stage-interface-block{,-byte-offset}.ll` lit tests cover both end-to-end. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1814/1815 (1 pre-existing, unrelated `Unsupported`), up from 1811/1812 before this row. A targeted `dEQP-VK.multiview` re-run (838 cases) confirms the crash this row's own root cause (H2a) diagnosed is gone: 78/838 now pass outright (up from 0/838), with the remaining 421 failures breaking down into three already-tracked buckets (H2b's 3, H8's 28+14) and three **new** ones spun off below (H2e, H2f, H2g) -- see "Roadmap H2d: measured impact" in VulkanCTSReport.md for the full breakdown) | H2a, H2c, C8a | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P1 |
| H2e | ~~**A SPIR-V `Output` storage-class variable read back after being written within the same invocation** (`dEQP-VK.multiview.input_instance`'s own shape, 24 of H2d's own measured 421 failures): unlike DXIL's `loadInput`/`storeOutput` split (where an output is genuinely write-only), SPIR-V's `Output` storage class permits reading back a value already written in the same invocation (e.g. a compound `gl_Position.x += 1.0`-shaped update); `feme.stage.input.load`/`.output.store`'s Input-vs-Output dichotomy (and `ValidateStagePass`'s enforcement of it, `'feme.stage.input.load' ... refers to element N with the wrong direction`) has no representation for this. Needs either a per-element local shadow value `canonicalizeSPIRVStage` can thread through (replacing the read-back load with the last stored value directly, since both sides are already known at legalization time) or a relaxation of `ValidateStagePass`'s direction check for this specific pattern~~ (done, via the first of the two options this row's own text offered: `canonicalizeSPIRVStage` routes every `Output`-direction leaf scalar (one per (`ElementID`, `Row`, `Component`), the granularity `loadStageIOValue`/`storeStageIOValue`'s existing recursion already decomposes every access to) through its own shadow `AllocaInst` (`ShadowValueMap`) instead of a wrong-direction `feme.stage.input.load`; every rewritten store also writes through to its own shadow alloca, and `PromoteMemToReg` (once every instruction has been rewritten) converts every shadow alloca to SSA form, resolving each read-back to the dominance-correct reaching store -- inserting a `phi` for a real control-flow join, exactly the SSA construction a compiler's own `mem2reg` pass performs for a local variable, which a linear "last stored value" scan (`ValidateStagePass`'s own relaxation option) could not do correctly in general. **Deviation**: fixing this row's own root cause does not turn `input_instance` green -- all 24 cases now build and run to completion (`grep -c "wrong direction"` against a full `dEQP-VK.multiview` run now returns 0, down from 24), but land in the same `Fail (Fail)` image-comparison bucket roadmap H2g already tracks (334 -> 358) rather than failing earlier at `vkCreateGraphicsPipelines`, so the group's own headline totals (78 pass / 421 fail / 339 not-supported) are unchanged from H2d's own baseline. `CanonicalizeStageTest.{OutputReadBackResolvesToStoredValueStraightLine,OutputReadBackResolvesAcrossControlFlow}` (the latter confirming the `phi` case) and a new `spirv-canonicalize-stage-output-readback.ll` lit test cover the fix; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1817/1818 (1 pre-existing, unrelated `Unsupported`), up from 1814/1815 before this row. See "Roadmap H2e: measured impact" in VulkanCTSReport.md for the full breakdown) | H2d | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/Graphics/ValidateStage.cpp` | P2 |
| H2f | ~~**Occlusion query availability reporting appears incorrect under multiview** (`dEQP-VK.multiview.non_precise_queries_with_availability`'s own shape, 18 of H2d's own measured 421 failures, `occlusion availability bit N is 0`): not yet investigated at all -- root cause unknown, could be the query-pool/multiview interaction itself or something unrelated the multiview test happens to exercise first~~ (done -- root cause: per the Vulkan spec, `vkCmdBeginQuery`/`vkCmdEndQuery` for an occlusion query recorded inside a multiview render pass instance implicitly span one query index per set bit of the active subpass's view mask (ordered by ascending bit position), not the single index a non-multiview query uses. `feme::vulkan::QueryPool`/`CommandBuffer.cpp` had no representation for this: `begin`/`markAvailable` always touched exactly one index, and `runDraw`'s per-view loop summed every rendered view's own passed-sample count into one shared counter applied to every currently-active query pool broadcast-style (`accumulateActiveOcclusionSamples`) -- so a two-view query's second (and every further) implicit index was simply never written, staying permanently unavailable, and its first index held the sum across both views rather than either view's own count. `QueryPool::begin`/`markAvailable` now take a `ViewCount` (the popcount of `GraphicsState::Binding.ViewMask` at `vkCmdBeginQuery` time, 1 outside multiview) and operate on `[Query, Query+ViewCount)`; `accumulateActiveOcclusionSamples` is replaced by `accumulateOcclusionSamples(Query, Samples)`, adding to one explicit index; `CommandBuffer.cpp` tracks each active occlusion query as an explicit `ActiveOcclusionQuery{Pool, FirstQuery, ViewCount}` rather than a plain `QueryPool *` set, and `runDraw`'s per-view loop resets its own passed-sample counter per view, routing each view's own count to its own enumerated query index. `DrawTest.MultiviewOcclusionQueryWritesOneQueryIndexPerView` (a two-view `viewMask == 0b11` render pass, a single `vkCmdBeginQuery`/`vkCmdEndQuery` pair at query index 0) locks this down at the unit level, confirmed to fail against the pre-fix code with exactly this row's own symptom. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1818/1819 (1 pre-existing, unrelated `Unsupported`), up from 1817/1818 before this row. A targeted `dEQP-VK.multiview` re-run (838 cases) confirms `grep -c "availability bit"` now returns 0 (down from 18) and the pass count rises to 96/838 (up from H2e's own 78/838), with the remaining 403 failures unchanged from H2d/H2e's own already-tracked buckets (H2g's 358, H2b's 3 + H8's 14 pipeline-creation failures, H8's 28 format-not-supported failures) -- see "Roadmap H2f: measured impact" in VulkanCTSReport.md for the full breakdown) | H2d | `feme/lib/Vulkan/QueryPool.h`, `feme/lib/Vulkan/QueryPool.cpp`, `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H2g | ~~**A widespread rendering-correctness gap spanning most of `dEQP-VK.multiview`'s subgroups** (334 of H2d's own measured 421 failures, now 358 once H2e's own 24 `input_instance` cases joined this bucket instead of failing earlier at pipeline creation -- `clear_attachments`, `index`, `secondary_cmd_buffer`, `readback_{implicit,explicit}_clear`, `multisample{,_resolve}`, `masks`, `instanced`, `input_attachments`, `draw_indirect{,_indexed}`, `draw_indexed`, `stencil`, `depth`, `input_instance`, each in both `renderpass2`/`dynamic_rendering` variants): plain image-comparison failures, the first real check of these tests' rendered output at all (every one previously hit H2a's SIMDize crash before ever reaching image comparison). Spot-checking `instanced.no_queries.15` rules out H2e's own read-back gap (a plain, unconditional multi-branch `gl_Position`/`out_color` write) as the shared cause -- root cause otherwise undetermined, needs its own triage pass before assuming a single fix closes all 358~~ (done -- the triage this row called for, plus the two fixes it found: (1) **SPIR-V's clip-space Y already increases downward** (Vulkan's own window-space convention), but `feme::graphics::Executor::projectVertex`'s viewport transform assumes the opposite, Y-up convention -- correct for DXIL/HLSL's own clip space (the only other producer of a `SignatureSystemValue::Position` output), the executor's actual target convention, but silently double-flipping every SPIR-V vertex shader's `gl_Position` instead of normalizing it first. `canonicalizeSPIRVStage` (`CanonicalizeStage.cpp`) now negates a `Position`-system-value store's Y component (`negateSystemValuePositionY`) before it reaches the executor. This alone raised the group from H2f's own 96/838 to 366/838. (2) **`vkCmdClearAttachments` inside a multiview render pass instance only ever cleared array layer 0**, ignoring every other set `viewMask` bit entirely (`clearAttachmentRects` had no per-view replication at all, unlike `runDraw`'s own `sliceAttachmentLayer` loop) -- confirmed against `clear_attachments.no_queries.15`: layers 1-3 kept their pre-clear draw content where the reference expected the clear color. `clearAttachmentRects` now loops over `RenderTargetBinding::ViewMask`'s own set bits (1, outside multiview) and slices each into its own attachment layer first. This raised the group further to 420/838 (50.1%), fixing all of `clear_attachments`/`readback_explicit_clear` and 6 of `readback_implicit_clear`'s 24. The remaining 79 failures split into 34 newly-triaged, independent gaps (16 `input_attachments` -- a totally blank `Fail (Fail)`, new roadmap row H2h; 18 `readback_implicit_clear`, only its multi-subpass view-mask combinations, new roadmap row H2i) and 45 already-tracked pipeline/render-pass-creation failures unchanged from H2d's own breakdown (H2b's 3, H8's 14 + 28). `DrawTest.MultiviewClearAttachmentsClearsEveryViewsOwnLayer` locks down fix (2) at the unit level; fix (1)'s own `CanonicalizeStageTest` coverage is the existing `spirv-canonicalize-stage-interface-block-byte-offset.ll` lit test, updated to expect the negated value, plus `DrawTest.DynamicLineWidthWidensTheLine`'s own SPIR-V-sourced line position updated to match the corrected convention. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1820/1821 (1 pre-existing, unrelated `Unsupported`). See "Roadmap H2g: measured impact" in VulkanCTSReport.md for the full breakdown) | H2d | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Vulkan/ImageOps.cpp` | P1 |
| H2h | ~~**`dEQP-VK.multiview.input_attachments` renders a totally blank (`Fail (Fail)`, every pixel black/transparent) image in every one of its 16 remaining cases** (H2g's own triage): unlike H2g's other two fixes (a wrong-but-present value), nothing is drawn at all, suggesting either the subpass-input read itself always returns zero/fails silently under multiview, or the pipeline/subpass wiring for a multiview input-attachment subpass never reaches the draw in the first place. Not yet root-caused; needs its own investigation (dump the subpass-input descriptor heap contents and confirm whether the fragment shader's `subpassLoad` executes at all, or short-circuits)~~ (done -- root cause: `RenderTargetBinding` (`RenderPass.h`), the shape both a classic `VkRenderPass`+`VkFramebuffer` and a `vkCmdBeginRendering` instance normalize into before reaching `CommandBuffer.cpp`'s draw path, never captured `SubpassDescription::InputAttachments` at all, only `Colors`/`Depth`/`Stencil`. `buildSubpassInputHeap`'s only attachment-resolution mechanism was `Gfx.ColorAttachmentInputIndices`'s identity-mapping fallback, built solely for `VK_KHR_dynamic_rendering_local_read` (which has no separate classic input-attachment list, and instead maps a `subpassInput`'s `InputAttachmentIndex` directly onto one of the current subpass's own color attachments). That fallback was silently reused for classic render passes too -- wrong whenever a subpass's input attachment is *not* one of its own color attachments, exactly this test's own shape, where a later subpass reads back an *earlier* subpass's color output through a `subpassInput`. That earlier attachment was never present in the later subpass's own color-attachment list, so `subpassLoad` addressed index 0 against the later subpass's own freshly-cleared color attachment instead, reading transparent black and rendering every pixel of every one of the 16 cases blank. Fix: `RenderTargetBinding` gains an `Inputs` field (one resolved `ImageView *` per `InputAttachmentIndex`, `nullptr` for `VK_ATTACHMENT_UNUSED`), populated by `buildRenderTargetBinding` from `SubpassDescription::InputAttachments` resolved against the whole framebuffer rather than just the current subpass's own color attachments. `runDraw` resolves and per-view-slices these into a new `SubpassInputs` list threaded into a new `buildSubpassInputHeap` parameter of the same name, which is authoritative when non-empty (mapping directly onto input-attachment indices) and falls back to the old identity-mapping logic, unchanged, only when empty (every `vkCmdBeginRendering` instance, which has no classic input-attachment list of its own and so always leaves `RenderTargetBinding::Inputs` empty) -- the two paths cannot conflict. `DrawTest.MultiviewInputAttachmentReadsBackAnEarlierSubpassColorOutput` (a two-subpass classic `VkRenderPass`, `viewMask == 0b11` both subpasses: subpass 0 writes attachment 0 solid red per-view; subpass 1 declares attachment 0 -- not one of its own color attachments -- as its input attachment, reads it back with the existing `SubpassLoadFragmentSource` shader, and writes solid green to attachment 1) locks this down at the unit level; confirmed (via `git stash` of just the fix) to fail on the pre-fix code and pass with it restored. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1762/1821 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1761/1820 before this row's own new test. **Measured impact**: a real, targeted `dEQP-VK.multiview` run (838 cases) rose from H2g's own 420/838 (50.1%) to 436/838 (52.0%) passed, `Failed` falling from 79 to 63 -- exactly this row's own 16 cases, confirmed individually (`input_attachments`/`renderpass2.input_attachments`, 8/8 and 8/8 now `Pass`), with every other remaining `Failed` case unchanged and independently tracked (roadmap H2i's 18, H8's 42, H2b's 3). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md`: confirmed no change needed -- this fix corrects existing input-attachment rendering behavior, advertising no new feature or extension. See "Roadmap H2h: measured impact" in VulkanCTSReport.md for the full breakdown) | H2g | `feme/lib/Vulkan/{RenderPass.h,CommandBuffer.cpp}` | P2 |
| H2i | ~~**`dEQP-VK.multiview.readback_implicit_clear`'s multi-subpass view-mask combinations still fail** (18 of H2g's own triage, exactly the cases whose numeric suffix names more than one subpass -- e.g. `1_2_4_8`, `5_10_5_10`, `8_1_1_8` -- while every single-subpass combination, e.g. `15`, now passes after H2g's `vkCmdClearAttachments` fix): a per-subpass `VK_ATTACHMENT_LOAD_OP_CLEAR` interacting with multiple multiview subpasses (each its own view mask) inside one render pass instance is not yet correctly modeled -- root cause otherwise undetermined~~ (done -- two compounding bugs: (1) `applyClear` (the `VK_ATTACHMENT_LOAD_OP_CLEAR` handler `CommandBuffer.cpp` runs at `vkCmdBeginRenderPass`/`vkCmdBeginRendering`) never sliced by array layer at all -- unlike H2g's own `clearAttachmentRects` fix, it always cleared only the first attachment layer's own byte range regardless of `RenderTargetBinding::ViewMask`, so even a *single*-subpass case whose mask excluded view 0 (e.g. `8`, view 3 alone) already failed for this reason alone; (2) `vkCmdNextSubpass` never called `applyLoadOps` at all, so a later subpass introducing a *new* view of an attachment already referenced by an earlier subpass (exactly `1_2_4_8`'s own shape: four subpasses, each a disjoint one-bit slice of one shared color attachment's view mask) never got its own share of the load op, leaving that view's own layer with whatever memory already held instead of the clear color. Fix: `applyClear` now slices per view like `clearAttachmentRects` already does, and only clears the views in the current subpass's `ViewMask` a new `GraphicsState::LoadedAttachmentViewMask` map does not yet mark as already loaded for that attachment -- gating against re-clearing a view an earlier subpass already used (which would erase that subpass's own draws); `applyLoadOps` is now called again at every `vkCmdNextSubpass`, not just at the render-pass instance's own start. `DrawTest.MultiviewLoadOpClearAppliesEachSubpassOwnNewlyIntroducedViews` (a three-subpass render pass exercising both fixes: view 0 drawn by subpass 0, view 1's own layer only ever painted by its own load-op share at subpass 1, subpass 2 re-using view 0 without erasing subpass 0's own draw) locks this down at the unit level, confirmed to fail on the pre-fix code and pass with it restored. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1763/1822 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). A targeted `dEQP-VK.multiview.readback_implicit_clear` run (24 cases) now passes 24/24, up from 6/24; a full `dEQP-VK.multiview` run (838 cases) rises from H2h's own 436/838 (52.0%) to 454/838 (54.2%) passed, `Failed` falling from 63 to 45 -- exactly this row's own 18 cases, with every other remaining `Failed` case unchanged and independently tracked (H8's 42, H2b's 3). See "Roadmap H2i: measured impact" in VulkanCTSReport.md for the full breakdown) | H2g | `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H2b | ~~**A pipeline with zero color attachments** (`dEQP-VK.multiview.depth_without_fragment_shader`'s own shape, and presumably the same case outside multiview): `feme::graphics::executeDraws` unconditionally requires at least one (`"a draw needs at least one color attachment"`), but a depth-only pipeline with no fragment shader output is legal Vulkan. Needs the executor's attachment-count validation relaxed to allow zero color attachments when the pipeline itself declares none, not a multiview-specific fix~~ (done, exactly as described, but insufficient to turn the named case green -- see this row's own Deviation: `executeDraws`'s unconditional `Draw.Attachments.empty()` rejection is removed; the existing `Pipeline.getColorBlends().size() != Draw.Attachments.size()` count-match check already enforces the same pipeline/draw consistency in the zero-attachment case as any other, so nothing else in the function needed to change (every `Draw.Attachments` iteration already loops `size()` times, and the extent/dual-source-blend fallbacks already handle an empty color list, per roadmap E5/C4). `vkCreateGraphicsPipelines`'s own analogous `Targets->Colors.empty()` rejection (`GraphicsPipeline.cpp`, not named by this row's own text but blocking pipeline creation for the exact same reason, one step earlier) is relaxed the same way; nothing downstream of it (color blend state translation, stage interface validation, the cached attachment-format list) assumes a nonempty `Colors`. `ExecutorTest.RendersWithZeroColorAttachments` (a real depth-only draw, no color attachment bound, a fragment stage with no output at all) and `GraphicsPipelineTest.AcceptsZeroColorAttachments` (a dynamic-rendering pipeline with zero color attachments and a fragment stage writing only `gl_FragDepth`) lock down each fix at the unit level; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1765/1824 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1763/1822 before this row. **Deviation**: a targeted `dEQP-VK.multiview.depth_without_fragment_shader*` run (1 case, plus its `dynamic_rendering`/`renderpass2` siblings a full `dEQP-VK.multiview` run also exercises, 3 total, matching this row's own originally-cited count) still fails identically after both fixes above, `VK_ERROR_INITIALIZATION_FAILED` for a *different* reason: `GraphicsPipeline.cpp`'s `if (!VertexInfo || !FragmentInfo) return ... "a graphics pipeline needs both a vertex and a fragment stage"` -- the real CTS case does not merely write no fragment *output*, as this row's own title assumed, it omits the fragment shader stage from `VkGraphicsPipelineCreateInfo::pStages` entirely, which is separately legal Vulkan (a fragment shader is optional whenever its subpass/dynamic-rendering render target has no color attachments, `VUID-VkGraphicsPipelineCreateInfo-pStages-06894`/`-06895` and neighbors). Making the fragment stage itself optional is a materially larger change than the attachment-count relaxation this row's own text scoped to -- it touches pipeline creation (`compileAndValidateStages`/`translateStageInterfaces` skipping fragment compilation and interface validation entirely), the compiled-artifact/`GraphicsPipeline` shape (`FragmentStage` becoming genuinely optional rather than always-present), and the executor's whole per-quad fragment-invocation loop (`Executor.cpp`'s `FSSig`/`FSColors`/`FS.invokeFragments` path, currently unconditional), not just its attachment-count check -- so it is left to new row H2j rather than folded into this one. A full targeted `dEQP-VK.multiview` re-run (838 cases) after both of this row's own fixes remains unchanged at 454/838 (54.2%) passed, confirming the fixes are real but do not yet move this row's own named case; see "Roadmap H2b: measured impact" in VulkanCTSReport.md for the full reproduction) | none | `feme/lib/Graphics/Executor.cpp` | P2 |
| H2j | ~~**Make a graphics pipeline's fragment shader stage genuinely optional** (roadmap H2b's own Deviation): `GraphicsPipeline.cpp`'s `translateFixedFunctionState` unconditionally rejects `VkGraphicsPipelineCreateInfo::pStages` missing a `VK_SHADER_STAGE_FRAGMENT_BIT` entry (`"a graphics pipeline needs both a vertex and a fragment stage"`), but Vulkan permits omitting it entirely whenever the pipeline's render target has no color attachments (depth/stencil-only rendering, `VUID-VkGraphicsPipelineCreateInfo-pStages-06894`/neighbors) -- exactly `dEQP-VK.multiview.depth_without_fragment_shader`'s own shape (and its `dynamic_rendering`/`renderpass2` siblings), still failing after H2b's own fix for this exact reason. Needs `compileAndValidateStages`/`validateStageInterfaces` to skip fragment-stage compilation and cross-stage interface validation when no fragment stage is named (only when `Targets.Colors.empty()`, matching the VUID's own condition -- a color-attached pipeline still requires one), `GraphicsPipeline`'s `FragmentStage` to become a genuinely optional (`nullptr`) `shared_ptr<CompiledStage>` rather than always-present, and `feme::graphics::executeDraws` to skip its whole fragment-invocation loop (`FSSig`/`Varyings`/`FSColors`/`FS.invokeFragments`) when the pipeline has none, running only vertex-stage clip/rasterize/early-depth-test with no per-fragment shading at all~~ (done: `translateColorBlendState` also needed a matching fix -- see this row's own Deviation -- `translateFixedFunctionState` now only requires a vertex stage, resolving fragment robustness only when a fragment stage is present and only rejecting a missing one once `Targets.Colors` is known non-empty; `compileAndValidateStages` takes `FragmentInfo` as a nullable pointer, skipping fragment compilation and its push-constant/bound-range validation when absent; `validateStageInterfaces` takes `FragmentStage` as a nullable pointer, skipping every fragment-side interface check when null (asserting `FragmentStage || ColorAttachmentCount == 0`); `compileGraphicsPipeline` computes `FragmentModule` and the fragment half of the pipeline cache key conditionally so a fragment-less pipeline still gets a correct, distinct cache key; `GraphicsPipelineArtifact`/`GraphicsPipeline`'s `FragmentStage` is now a genuinely optional (possibly null) `shared_ptr<CompiledStage>` with a new `hasFragmentStage()` query on both the Vulkan-level and executor-facing wrappers; and `executeDraws`'s `FSSig` is now a plain default-constructed `EntrySignature` populated only when `Pipeline.hasFragmentStage()`, leaving it with empty `Elements` to naturally short-circuit every downstream varyings/color-output loop, with a fragment-less pipeline instead taking an early return right after the `Quads.empty()` check that still counts `Draw.PassedSampleCounter` per already-depth-tested passing lane for occlusion queries. **Deviation**: `translateColorBlendState` (not named by this row's own text) still unconditionally validated `Info->attachmentCount == Targets.Colors.size()` even with no fragment stage, rejecting exactly the shape real CTS tests build -- `vktMultiViewRenderTests.cpp`'s `depth_without_fragment_shader` case hardcodes `pColorBlendState.attachmentCount = 1` unconditionally, which is legal Vulkan because a pipeline with no fragment shader has no fragment output interface and `pColorBlendState` must be entirely ignored (`VUID-VkGraphicsPipelineCreateInfo-renderPass-06055`'s scope only applies when a fragment output interface state is present); fixed by threading a `HasFragmentStage` flag into `translateColorBlendState` that skips its `attachmentCount` check (and the rest of `pColorBlendState`) when false, tightly coupled to this row's own change and fixed as part of it rather than spun off. `GraphicsPipelineTest.AcceptsMissingFragmentStage`, `.RejectsMissingFragmentStageWithColorAttachments`, and `.AcceptsMissingFragmentStageWithMismatchedColorBlendState` (the last exercising the Deviation's own shape) and `ExecutorTest.RendersWithNoFragmentStage` lock down each fix at the unit level; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1769/1828 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1768/1827 before this row. A targeted `dEQP-VK.multiview.depth_without_fragment_shader*` run (3 cases, the exact case this row and H2b both cite) now passes in full, and a full `dEQP-VK.multiview` re-run (838 cases) moves from H2b's own 454/838 (54.2%) baseline to 457/838 (54.5%) -- exactly the 3 named cases, no regressions elsewhere; see "Roadmap H2j: measured impact" in VulkanCTSReport.md for the full reproduction) | H2b | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp`, `feme/include/feme/Graphics/Pipeline.h` | P2 |
| H3 | ~~**Multiple viewports and scissors.** `maxViewports` is hard-coded to 1 (`PhysicalDeviceInfo.cpp:247`) and `vkCmdSetViewportWithCount` and the pipeline path both reject anything else (`GraphicsPipeline.cpp:825`). Needs a per-primitive viewport index in the executor, `ViewportIndex` as a stage output, and `shaderOutputViewportIndex`/`shaderOutputLayer` (both `VK_FALSE` in the aggregate 1.2 struct today) raised in lockstep with H2~~ (done: `PhysicalDeviceInfo.h`'s `MaxViewportCount` (16) replaces the hard-coded `maxViewports == 1`, and `multiViewport` is now advertised `VK_TRUE` too (found missing during this row's own CTS run -- `deqp-vk` gates the whole `shader_viewport_index`/`multi_viewport` CTS surface on this one feature bit, not `maxViewports`' numeric value alone); `GraphicsPipeline.cpp`'s `translateViewportState`/`GraphicsPipelineState`/`DynamicGraphicsState` carry a full `Viewports`/`Scissors` array (`SmallVector<T, MaxViewportCount>`) rather than one element, validating any static count from 1 to 16; `CommandBuffer.cpp`'s `vkCmdSetViewport`/`vkCmdSetScissor` write a `first*`-relative sub-range of the active array, and the new `vkCmdSetViewportWithCount`/`vkCmdSetScissorWithCount` (plus their already-implemented `*EXT` aliases) replace the whole array from element 0, truncating it via a new `RecordedCommand::ResetViewportOrScissorCount` flag rather than only ever growing it; `Executor.cpp`'s `resolvePrimitiveState` reads a vertex stage's `ViewportArrayIndex` output (`gl_ViewportIndex`, SPIR-V `BuiltIn` 10) per primitive and routes each triangle/line/point through the array element (and paired scissor) it names, via a new `resolveViewportArrayIndex` (`LayeredRendering.h`, alongside `resolveRenderTargetArrayLayer`) that discards a primitive whose index is out of range rather than clamping it -- a vertex stage that writes no `ViewportArrayIndex` still resolves to element 0, preserving single-viewport behavior untouched. Along the way, fixed two bugs the previous agent's stashed partial progress had introduced: an ambiguous-overload compile error from `vkCmdSetViewport{,WithCount}{,EXT}`/`vkCmdSetScissor{,WithCount}{,EXT}` calling each other unqualified (fixed by factoring shared logic into private `setViewportImpl`/`setScissorImpl` helpers, never one entry point calling another), and a multiview regression (3 `DrawTest` cases) from `runDraw`'s per-view loop no longer pre-slicing `ResolveAttachments`/`DepthStencil` per view while still pre-slicing color `Attachments`, disagreeing with `Executor.cpp`'s `getDrawLayerCount()` and silently discarding every primitive past view 0 (fixed by restoring the per-view slicing for all attachment kinds, and changing the executor's default `RequestedLayer` from `Draw.ViewIndex` to 0 since attachments arrive already sliced to the current view). `GraphicsPipelineTest.AcceptsMultipleViewportsAndScissors`/`.RejectsTooManyViewports`, `DrawTest.DynamicViewportWithCountRoutesInstancesToDifferentViewports`/`.OutOfRangeViewportIndexDiscardsThePrimitive`, and `PhysicalDeviceInfoTest.ShaderViewportIndexLayerFeaturesAreTrue` lock down each new behavior at the unit level; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1776/1835 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). A targeted `dEQP-VK.draw.*.shader_viewport_index.*` run (196 cases) moves from 0/196 passed (100% `NotSupported`, gated on the now-fixed `multiViewport` bit alone) to 64/196 passed, 68 `Failed`, 64 `NotSupported` (the `geometry_shader`/`tessellation_*` variants, unrelated to this row, H4/H5); every `vertex_shader_N` case (`gl_ViewportIndex` written by, and only ever consumed downstream of, the vertex stage) now passes 64/64, while every `fragment_shader_N` case (`gl_ViewportIndex` read back as a fragment-shader input, not just written by the vertex stage) still fails -- broken out as new row H3a rather than fixed here, since it is a separate fragment-stage-signature gap, not a viewport-count/pipeline/executor one. A full `dEQP-VK.multiview` regression re-run (838 cases, since this row's fixes touch the same per-view attachment-slicing/per-primitive layer-resolution code multiview depends on) confirms no regression: 457/838 (54.5%) passed, unchanged from H2j's own baseline. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` updated: `multiViewport` (1.0) and `shaderOutputViewportIndex`/`shaderOutputLayer` (1.2) move to `VK_TRUE`, and `VK_EXT_shader_viewport_index_layer` moves from "Planned" to "Implemented (core, not advertised by name)". See "Roadmap H3: measured impact" in VulkanCTSReport.md for the full reproduction) | H2 | `feme/lib/Vulkan/{PhysicalDeviceInfo,GraphicsPipeline,CommandBuffer}.cpp`, `feme/lib/Graphics/Executor.cpp` | P1 |
| H3a | ~~**`gl_ViewportIndex` read back as a fragment-shader input fails pipeline creation** (roadmap H3's own Deviation, `dEQP-VK.draw.*.shader_viewport_index.fragment_shader_*`, 68 of H3's own measured 196 cases, `GL_ARB_shader_viewport_layer_array`'s other half: `out_color = color[gl_ViewportIndex]`): `vkCreateGraphicsPipelines` fails with `VK_ERROR_INITIALIZATION_FAILED`, `FragmentWrapper.cpp`'s `lowerFragmentStageOps` reporting "fragment stage wrapper requires attached feme.signature metadata" -- i.e. the fragment entry function reaches code generation with no `feme.signature` metadata attached at all, not merely a mismatched one. `CanonicalizeStage.cpp`'s builtin-to-`SignatureSystemValue` mapping (`getSystemValueForBuiltIn`) already maps SPIR-V `BuiltIn` 10 (`ViewportIndex`) to `SignatureSystemValue::ViewportArrayIndex` regardless of storage class, so the reflection-side mapping itself looks direction-agnostic; root cause not yet isolated to a single line, needs its own investigation into why the fragment stage's globals loop (`CanonicalizeStage.cpp`'s `InputGlobals`/`OutputGlobals` collection) does not see this variable at all for a fragment-shader `Input`-storage-class `ViewportIndex` builtin, unlike the small set of fragment-input builtins already wired up before this milestone (`FragCoord`/`Position`, `FrontFacing`, `SampleId`, `SampleMask`)~~ (done: `CanonicalizeStage.cpp`'s globals loop was never the culprit -- root cause turned out to be four independent, previously-undiscovered gaps entirely downstream of it, only reachable once a real fragment shader used a *bound resource* (e.g. the failing CTS shader's `Colors` UBO), which is why H3's own simpler vertex-side testing never tripped them: (1) `SPIRVResourceLowering.cpp`'s (and the DXIL-oriented `ResourceLowering.cpp`'s) `addResourceEnvParams` rebuilds the stage entry function via `Function::Create`+`copyAttributesFrom` to append resource-heap ABI parameters, but `GlobalObject::copyAttributesFrom` never copies function-attached metadata, silently dropping the `!feme.signature` `CanonicalizeStagePass` had already attached -- fixed by adding an explicit `NewF->copyMetadata(&F, 0)` in both files; (2) `FragmentWrapper.cpp`'s `loadFragmentSystemValue` had no `case SignatureSystemValue::ViewportArrayIndex`, hitting the "unsupported fragment system value" error path -- fixed by adding a new per-lane `ViewportIndex` field to `FemeFragmentInvocation` (`RuntimeABI.h`) and `FragmentInvocationField` (`StageArgsLayout.h`), and the matching read case; (3) the CPU runtime (`FeMeRuntimeCPU.c`) only defined scalar `feme.cpu.resource.load/store.raw.i32`/`.f32`, even though `ResourceCalls.cpp`'s mangling already generically supports vector element types, so the real shader's whole-`vec4` UBO load hit a late JIT "symbol not found" -- fixed by adding `feme.cpu.resource.load/store.raw.v4f32`; (4) `Executor.cpp`'s `resolvePrimitiveState` already computed `gl_ViewportIndex` via `resolveViewportArrayIndex` but discarded it after selecting the viewport/scissor array slot, never threading it into the per-lane `FemeFragmentInvocation` the fragment body reads from -- fixed by adding `ViewportIndex` to `PrimitiveState`/`ScreenTriangle` and filling `Inv.ViewportIndex[Lane]`. New unit tests: `SPIRVResourceLoweringTest.PreservesFunctionMetadataAcrossEnvParamRewrite`, `ResourceLoweringTest.PreservesFunctionMetadataAcrossEnvParamRewrite`, `FragmentWrapperTest.LowersViewportArrayIndexSystemValueInput`, `RuntimeCPUTest.RawLoad/StoreV4F32*` (5 cases), `DrawTest.FragmentShaderReadsBackViewportIndex`; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1785/1844 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`, +9 over H3's own 1776/1835 baseline -- exactly the new tests above). The real `dEQP-VK.draw.*.shader_viewport_index.fragment_shader_*` CTS surface moves from 0/68 to 68/68 passed; the full `shader_viewport_index` group (196 cases) now measures 132/196 passed (68 fragment + 64 vertex), 64 `NotSupported` (tessellation, H4/H5, unrelated), 0 `Failed`. Broader regression checks found no regressions and a net improvement: `dEQP-VK.multiview` (838 cases, unchanged 457/838 passed, confirmed identical via `git stash`) and a representative 882-case sample of `dEQP-VK.ubo` (32/882 passed pre-fix vs. 47/882 passed post-fix, 0 new failures -- the metadata/runtime fixes also unblock some previously-broken non-viewport UBO cases). See "Roadmap H3a: measured impact" in VulkanCTSReport.md for the full reproduction) | H3 | `feme/lib/Transforms/CPU/{SPIRVResourceLowering,ResourceLowering,FragmentWrapper}.cpp`, `feme/include/feme/Target/CPU/RuntimeABI.h`, `feme/lib/Transforms/CPU/StageArgsLayout.h`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H4 | ~~**Tessellation stages.** `GraphicsPipeline.cpp` accepts exactly `VK_SHADER_STAGE_VERTEX_BIT` and `VK_SHADER_STAGE_FRAGMENT_BIT` (its `mapStage`/stage-mask check, now ~lines 1157-1174, not the ~983-1118 this row originally cited); everything else is rejected. Needs G5's hull/domain wrappers, patch storage, and tessellator, then the `tessellationShader` feature bit, `maxTessellation*` limits, and the `VkPipelineTessellationStateCreateInfo`/patch-list topology path. Whole `dEQP-VK.tessellation` group (partially done: the graphics-executor half of this row is finished and tested -- see R34's own last four increments, landed under this row's name -- so a `feme::graphics::GraphicsPipeline` carrying hull/patch-constant/domain `CompiledStage`s and `graphics::PrimitiveTopology::PatchList` now tessellates real patches through `Executor::executeDraws` and rasterizes the domain stage's output, covered end to end by `unittests/Graphics/ExecutorTest.cpp`. What remains is entirely on the Vulkan-API side and is broken out below as H4a (importing a SPIR-V tessellation module at all) and H4b (accepting one in `vkCreateGraphicsPipelines` and advertising the feature bit and limits); this row stays open until both close. Measured: `dEQP-VK.tessellation.*` is unchanged at 0 `Pass`/0 `Fail`/1114 `NotSupported` -- correctly so, since deliberately *not* flipping `tessellationShader` ahead of H4a/H4b is what keeps those 1114 honest `NotSupported`s from becoming 1114 `Fail`s~~ (done -- H4a and H4b (below) both closed, and a `vkCreateGraphicsPipelines`-shaped tessellation pipeline now genuinely compiles and runs end to end through this ICD. Measured: a real `dEQP-VK.tessellation.*` run (1114 cases) moves from 0/0/1114 to 8 `Pass`/227 `Fail`/879 `NotSupported`, real progress rather than the false-positive 1114 `Fail`s this row was written to avoid. The 227 `Fail`s split into one new gap this milestone's own splitting logic introduced (24 cases, roadmap H4c), one more newly-triaged gap specific to this milestone's own two-phase compile design (24 cases, roadmap H4d), and 203 already-tracked, pre-existing SPIR-V/codegen limitations unrelated to tessellation specifically (C8's matrix/aggregate-legalization and unhandled-opcode bucket, plus H3a's fragment-signature-metadata gap, now reachable for the first time because a tessellation pipeline can reach code generation at all). See "Roadmap H4b: measured impact" in VulkanCTSReport.md for the full breakdown) | G5 | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics` | P1 |
| H4a | ~~**No SPIR-V tessellation-control or tessellation-evaluation entry point can be reflected, so H4b has nothing to compile.** `feme::graphics::CanonicalizeStagePass::run` (`feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, ~line 1200) skips every function whose `feme::getShaderStage` is not `Vertex` or `Fragment`, so a `TessellationControl`/`TessellationEvaluation` entry point reaches code generation with no `EntrySignature` attached and cannot be wrapped. Lifting that filter is necessary but nowhere near sufficient: FeMe's hull ABI is D3D-shaped and needs **two** separately compiled entry points -- a control-point phase for `HullWrapperPass` and a patch-constant phase for `PatchConstantWrapperPass`, discriminated by `feme::cpu::isPatchConstantPhase`'s `SignatureDirection::PatchOutput` test -- whereas GLSL/SPIR-V compiles a tessellation-control shader to a **single** entry point that writes both its per-vertex outputs and `gl_TessLevelOuter`/`gl_TessLevelInner`, typically with an intervening `OpControlBarrier`. Splitting that one entry into the two FeMe phases is exactly R34's own still-deferred "generalize `EntryWrapperPass`'s barrier-region splitting to the control-point batch ABI" item, and is the real work here. Also needs SPIR-V's tessellation execution modes (`Triangles`/`Quads`/`Isolines`, `SpacingEqual`/`SpacingFractionalEven`/`SpacingFractionalOdd`, `VertexOrderCw`/`VertexOrderCcw`, `PointMode`, `OutputVertices`) mapped onto `feme::graphics::TessellationState`, and `BuiltIn` `TessLevelOuter`/`TessLevelInner`/`TessCoord`/`PatchVertices`/`InvocationId` mapped onto the corresponding `SignatureSystemValue`s. Closed: `CanonicalizeStagePass::run` now reflects `TessellationControl`/`TessellationEvaluation` entries; `splitTessellationControlEntry` splits a single SPIR-V tessellation-control entry at its one required group-sync barrier into a `HullWrapperPass`-shaped control-point phase and a `PatchConstantWrapperPass`-shaped `<name>.patchconstant` phase (diagnosing, not mis-splitting, any unsupported shape -- zero/multiple barriers, no post-barrier region, multiple entry edges, or a cross-boundary SSA value); `ConvertSPIRVToLLVMPass` captures all seven tessellation execution modes into `feme.tessellation.*` passthrough attributes and `feme::graphics::getTessellationState` reads them back into `TessellationState` (evaluation-only and control-only fields treated as independently optional, since no one SPIR-V entry point declares both); and `BuiltIn` `TessLevelOuter`/`TessLevelInner`/`TessCoord`/`PatchVertices`/`InvocationId` map onto `SignatureSystemValue::TessFactorEdge`/`TessFactorInside`/`DomainLocation`/`PatchVertices`/`OutputControlPointID` (see `FeMeGraphicsDesign.md`'s `CanonicalizeStagePass` section for the full writeup, including why this does *not* close R34's `EntryWrapperPass` barrier-splitting deferral). This did **not** need `feme/lib/Transforms/CPU/HullPhase.cpp` (no such file exists; `isPatchConstantPhase` lives in `HullWrapper.h`) or changes to `EntryWrapper.cpp` -- see above. Full assembly of `TessellationState` from both compiled stages plus `VkPipelineTessellationStateCreateInfo::patchControlPoints`, and everything else needed to actually compile and run a tessellated pipeline through Vulkan, remains H4b's job; `dEQP-VK.tessellation.*` is therefore still 0 `Pass`/0 `Fail`/1114 `NotSupported`, unchanged and expected~~ | G5, R34 | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/CPU/{EntryWrapper,HullWrapper,PatchConstantWrapper,HullPhase}.cpp` | P1 |
| H4b | ~~**`vkCreateGraphicsPipelines` still rejects `VK_SHADER_STAGE_TESSELLATION_CONTROL_BIT`/`VK_SHADER_STAGE_TESSELLATION_EVALUATION_BIT`, and `tessellationShader` is still `VK_FALSE`.** `GraphicsPipeline.cpp`'s `mapStage`/stage-mask loop (~lines 1157-1174) needs to accept the two bits, require exactly one of each when either is present, reject any topology other than `VK_PRIMITIVE_TOPOLOGY_PATCH_LIST` for such a pipeline (and reject patch-list topology without them), translate `VkPipelineTessellationStateCreateInfo::patchControlPoints` into `graphics::TessellationState::InputControlPointCount` after validating it against `maxTessellationPatchSize`, compile the tessellation modules into the hull control-point, patch-constant and domain `CompiledStage`s, and call `graphics::GraphicsPipeline::setTessellationStages` -- all of which the executor already consumes. `PhysicalDeviceInfo.cpp` then advertises `tessellationShader = VK_TRUE` and real `maxTessellation*` limits; note that the implementation's own honest ceilings are `feme::graphics::MaxPatchControlPoints` (32, `Graphics/Patch.h`) for `maxTessellationPatchSize` and `feme::graphics::DefaultMaxTessFactor` (64, `Graphics/Tessellator.h`) for `maxTessellationGenerationLevel`, so neither may be advertised higher without raising those first. Blocked on H4a: flipping the feature bit before a tessellation module can even be reflected would turn the group's 1114 honest `NotSupported`s into 1114 `Fail`s~~ (done: `GraphicsPipeline.cpp`'s `mapStage`/stage-mask loop now accepts `VK_SHADER_STAGE_TESSELLATION_{CONTROL,EVALUATION}_BIT`, requiring exactly one of each when either is present and `VK_PRIMITIVE_TOPOLOGY_PATCH_LIST` iff both are (and rejecting patch-list topology without them); `translateFixedFunctionState` validates `VkPipelineTessellationStateCreateInfo::patchControlPoints` against `maxTessellationPatchSize` and stores it into `graphics::TessellationState::InputControlPointCount`; `compileAndValidateStages` compiles the tessellation-control module twice (once for its own control-point entry, once for its `<entry>.patchconstant` phase, per H4a's own split) and the tessellation-evaluation module once, merges their independently-optional reflected `TessellationState` halves, and validates the result via `feme::graphics::validatePatchControlPointCounts`; `compileGraphicsPipeline`/`buildExecutorPipeline` thread the three new `CompiledStage`s into `graphics::GraphicsPipeline::setTessellationStages`, which `Executor::executeDraws` already consumed (H4/R34); `PhysicalDeviceInfo.cpp` advertises `tessellationShader = VK_TRUE` (`maxTessellationPatchSize`/`maxTessellationGenerationLevel` were already the true `MaxPatchControlPoints`/`DefaultMaxTessFactor` ceilings this row cites, just gated behind the false feature bit). Also fixed as a prerequisite this row's own work surfaced: `isSPIRVGroupSyncBarrier` (`CanonicalizeStage.cpp`) only recognized the `llvm.spv.*.barrier.with.group.sync` HLSL-builtin intrinsics, not the mangled `_Z22__spirv_ControlBarrieriii` call MLIR's real upstream `ControlBarrierPattern` lowers `spirv.ControlBarrier` to -- meaning H4a's own `splitTessellationControlEntry` never actually fired for any real SPIR-V-imported tessellation-control shader before this fix (confirmed against `mlir/test/Conversion/SPIRVToLLVM/barrier-ops-to-llvm.mlir`'s own lowering shape), locked down by `CanonicalizeStageTest.SplitsHullEntryAtMangledSPIRVControlBarrierCall`. `GraphicsPipelineTest.AcceptsTessellationStages`/`.RejectsUnpairedTessellationStage`/`.RejectsTopologyTessellationStageMismatch`/`.RejectsInvalidPatchControlPoints` (real end-to-end SPIR-V tessellation-control/-evaluation modules, hand-verified against `feme-opt --feme-convert-spirv-to-llvm`) and `PhysicalDeviceInfoTest`'s updated feature test plus new `TessellationLimitsMatchImplementationCeilings` lock this down at the unit level; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1821/1880 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). **Measured impact**: a real `dEQP-VK.tessellation.*` run (1114 cases) moves from H4's own 0/0/1114 to 8 `Pass`/227 `Fail`/879 `NotSupported` -- a targeted `dEQP-VK.draw.*` regression sample (1957 of the 29419-case mustpass list, every 15th case with `*viewport_height*` removed, per H2f's own established methodology) confirms no regression outside tessellation itself: exactly 6 of the sample's cases change status, every one a `shader_layer`/`shader_viewport_index` `tessellation_shader_N` case moving from `NotSupported` (feature bit was false) to `Fail` (the same known gaps below, roadmap H4c/H4d), with every one of the sample's other 1951 cases unchanged. `Vulkan14FeatureInventory.md` updated: `tessellationShader` (1.0) moves to `VK_TRUE`, `AdvertisedPromotedFeatures.txt` grown to match, and the "N of M unimplemented 1.0 feature bits are graphics capabilities" finding's count and list updated (51->50, 16->15); `VulkanExtensionInventory.md` confirmed no change needed (tessellation adds no extension of its own). See "Roadmap H4b: measured impact" in VulkanCTSReport.md for the full breakdown and root-cause triage of the 227 `Fail`s) | H4a | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp`, `feme/docs/Vulkan14FeatureInventory.md` | P1 |
| H4c | ~~**`splitTessellationControlEntry` (H4a) explicitly rejects a tessellation-control shader whose patch-constant phase reads back an SSA value computed before the barrier** (24 of H4b's own measured 227 `dEQP-VK.tessellation.*` `Fail`s, diagnosed rather than mis-compiled: `"tessellation-control SPIR-V entry point's patch-constant region cannot yet capture SSA values defined before the barrier"`), yet this is the common, real GLSL-compiled shape -- computing a per-patch tessellation factor from data derived from the control-point body (e.g. control-point output positions) and referencing it after `OpControlBarrier`. Needs the split to either re-materialize/clone the relevant pre-barrier computation into the patch-constant phase (correct only when that computation reads no other invocation's per-vertex output, i.e. is not itself a cross-invocation reduction) or thread the captured value through a synthetic patch-scoped storage location both phases can address (likely the right shape in general, since it mirrors how the control-point phase's own per-vertex outputs already cross the barrier through patch-shared storage)~~ (done: implemented the storage-threading design this row itself named as "likely the right shape in general" -- `splitTessellationControlEntry` now creates one new address-space-8 (`Output`-storage-class) `GlobalVariable` per captured pre-barrier SSA value, with a synthetic, collision-free `Location` decoration (`computeNextSyntheticLocation`); stores the value into it right after its own definition in the control-point phase; and loads it back at the top of a new `patchconst.captures` block prepended to the patch-constant phase. No new linking code was needed -- `classifySPIRVElement` already treats such a global as an ordinary per-vertex `Output` in the control-point phase and as an ordinary, non-`FromInputPatch` `Input` in the patch-constant phase (the same "OutputPatch" shape a real `gl_out[]` read-back already uses), so `PatchPipeline.cpp`'s existing `Location`-based `HullToPatchConstant` link picks up the new pair automatically. Sound unconditionally, not just for the common case: SPIR-V only defines cross-invocation reads after a barrier, so anything captured from before the one barrier this pass splits at can only be the current invocation's own state. Locked down by `CanonicalizeStageTest.SplitsHullEntryThreadingCapturedSSAValue`; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1822/1881 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). **Measured impact**: this row's own 24 named cases no longer hit the SSA-capture diagnostic and the split now genuinely succeeds, but the group's headline `dEQP-VK.tessellation.*` totals (8 `Pass`/227 `Fail`/879 `NotSupported`) and the `dEQP-VK.draw.*` regression sample (12/139/1806) are byte-identical to H4b's own baseline -- every one of the 24 now reaches real code generation but immediately hits a different, pre-existing C8-bucket legalization gap one step later (`MaskIntrinsics.cpp`'s masked-load/store scalar mangling has no case for a matrix/aggregate element type), so the headline cannot move until that gap closes. That gap now surfaces as a process-aborting `llvm_unreachable` rather than a graceful `Fail` for these 24 cases specifically -- confirmed pre-existing, not introduced by this row (A/B rebuild shows the pre-H4c build never reaches it, stopping at the old diagnostic first) -- broken out as follow-up H4e below rather than fixed in this row, since hardening it is CPU masked-memory-op legalization (C8's own scope), not tessellation-control splitting. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed unchanged (no feature bit or extension touched). See "Roadmap H4c: measured impact" in VulkanCTSReport.md for the full breakdown, including the per-case-resume methodology the process-abort above required) | H4b | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P1 |
| H4d | ~~**A JIT-time `"Symbols not found: [ spirv_var_N, spirv_var_N ]"` error rejects some tessellation-control shaders that do *not* hit H4c's SSA-capture diagnostic** (24 of H4b's own measured 227 `dEQP-VK.tessellation.*` `Fail`s, e.g. all of `dEQP-VK.tessellation.winding.*`): the control-point and patch-constant phases each get their own `CompiledStage::create`/`LLJIT` (H4b's own design, matching every other stage), so this is not a cross-phase symbol collision; more likely `splitTessellationControlEntry`'s clone of the module for one phase leaves a global variable referenced-but-undefined after some later pass (a `GlobalDCE`-shaped pass reachable only from the phase's own new entry point, not the original one) strips its definition while a reference to it survives. Root cause not yet isolated -- needs its own investigation, likely starting from a minimal reproduction of `dEQP-VK.tessellation.winding.default_domain.glsl_quads_ccw`'s own tessellation-control module reduced through the same `feme-convert-spirv-to-llvm`/`feme-canonicalize-stage` pipeline H4b's own tests used to hand-verify shapes~~ (done: the roadmap's own `GlobalDCE`/two-`LLJIT` hypothesis was a red herring, confirmed by a synthetic no-barrier repro that hit a clean, different error instead of the crash. The real root cause, isolated with a real, glslang-compiled `dEQP-VK.tessellation.winding.default_domain.glsl_quads_ccw` tessellation-control module fed straight into `vkCreateShaderModule`: `resolveStageIOAccess` (`CanonicalizeStage.cpp`) treated *every* single-`ElementID` stage-IO global (i.e. every one not part of a builtin interface block/struct) as whole-value-only, unconditionally returning `std::nullopt` for any nonzero byte offset -- correct for a scalar global, but wrong for one whose own type is a multi-row array/vector, exactly `gl_TessLevelInner`/`gl_TessLevelOuter`'s own `[2 x f32]`/`[4 x f32]` `BuiltIn`+`Patch`-decorated shape, which every GLSL tessellation-control shader writes one element at a time. Only element 0 (byte offset 0) of such a global ever got rewritten into a `feme.stage.output.store`; every other element's store was left as a raw, unrewritten reference to the never-defined (`external`-only) SPIR-V-imported global -- an unresolvable symbol at JIT link time, exactly `LLJIT`'s own "Symbols not found" error, confirmed reproduced byte-for-byte (`JIT session error: Symbols not found: [ gl_TessLevelOuter, gl_TessLevelInner ]`) before this fix and gone after it. Fixed by applying the same `resolveRowComponent` byte-offset-to-(Row,Component) resolution the builtin-interface-block-member branch already used, to the single-`ElementID` branch's own global type instead of a struct member's. Locked down by `CanonicalizeStageTest.RewritesSPIRVArrayOutputStorePerElementByteOffset` (LLVM-IR level: a bare `[4 x float]` `Output` global written at 4 different byte offsets, all 4 now resolve) and `GraphicsPipelineTest.AcceptsTessellationControlMultiElementArrayOutput` (end to end: a real tessellation pipeline writing 2 elements of a `gl_TessLevelOuter`-shaped `BuiltIn`+`patch` global across a barrier split now JIT-links and creates successfully, confirmed to fail exactly as before this fix when A/B-tested with it reverted); `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1824/1883 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). **Measured impact**: a real `dEQP-VK.tessellation.winding.*` (glsl-only, 24 cases) run confirms 0 `"Symbols not found"` JIT crashes (was 24/24), but all 24 still `Fail` -- immediately hitting a new, distinct, previously-unreached gap this fix itself surfaces (not introduced by it): `splitTessellationControlEntry` only clones a `.patchconstant` phase when it finds a barrier, but winding's own trivial `layout(vertices=1)` shader (a single output control point needs no cross-invocation sync) has none, so no `.patchconstant` function is ever created and `compileAndValidateStages` -- which always expects one -- now fails cleanly instead (`"no hull entry point named 'main.patchconstant'"`, `VK_ERROR_INITIALIZATION_FAILED`, no crash). Broken out as follow-up H4f below rather than fixed in this row, since it is a distinct gap in the barrier-split design (R34/H4a's own scope), not a `resolveStageIOAccess` addressing bug. The same fix, applied at the `resolveStageIOAccess` level rather than winding-specifically, also unblocks 8 more `dEQP-VK.tessellation.shader_input_output.*` cases from a pre-existing, differently-shaped `'llvm.getelementptr' op operand #0 must be LLVM pointer type ...` `Fail` into reaching real code generation -- where they now hit H4e's own already-tracked `MaskIntrinsics.cpp` abort instead (confirmed by A/B rebuild: reverting this fix reproduces the old `getelementptr`-error `Fail` for all 8, unchanged from before). Net result on the full group: **byte-identical to H4b/H4c's own baseline**, 8 `Pass`/227 `Fail`/879 `NotSupported` (32 cases now reach the H4e crash bucket instead of their own prior distinct `Fail` reasons, but `Fail` is `Fail` either way) -- expected, not a sign the fix did nothing, since converting a crash or a wrong-shaped error into a different clean `Fail`/crash does not move any of the three buckets by itself. The `dEQP-VK.draw.*` regression sample (1957 of the 29419-case mustpass list, every 15th case with `*viewport_height*` removed) is also byte-identical to H4b/H4c's own 12 `Pass`/139 `Fail`/1806 `NotSupported` -- 0 regressions, 0 new passes, as expected for a fix scoped to stage-IO array addressing rather than anything reachable from a non-tessellation pipeline. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (no feature bit or extension touched). See "Roadmap H4d: measured impact" in VulkanCTSReport.md for the full breakdown) | H4b | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P1 |
| H4e | ~~**`MaskIntrinsics.cpp`'s `appendScalarMangling` calls `llvm_unreachable("unsupported feme.cpu.masked.* element type")` for any masked-load/store element type it does not recognize (matrix/aggregate shapes among them), aborting the whole process instead of diagnosing gracefully** -- newly reached (not introduced) by H4c's own fix: all 24 of H4c's named cases now split successfully and reach `feme::cpu::SIMDizePass`'s masked-memory-op path, which is very likely the same C8-bucket matrix/aggregate-legalization gap H4b's own triage table already names for the 88 `feme-cpu-simdize: ... divergent value ... of vector type` and 74 `llvm.getelementptr` cases (not yet independently confirmed), just reached through a third path. Needs both the root legalization fix (C8's own scope) and, independently and more urgently, hardening this specific function to `emitError`+return a null/sentinel rather than `llvm_unreachable`, so an unsupported shape becomes a graceful per-case `Fail` the way every other gap in this codebase already does, rather than a `SIGABRT` that kills an entire CTS run's remaining cases~~ (done: implemented the more urgent, independent half of this row's own description -- `appendScalarMangling` now returns `bool`, reporting an unsupported element `Type` through its own `LLVMContext::emitError` and returning `false` instead of asserting unreachable; `mangleMaskedMemOpName` now returns `std::optional<std::string>` to propagate that failure; `getOrInsertMaskedLoad`/`getOrInsertMaskedStore`/`getOrInsertMaskedAtomicRMW` and their `createMasked*` wrappers all propagate a `nullptr` in turn; and `feme::cpu::LinearizePass`'s `applyStageMasks` (the only caller of the three `createMasked*` entry points) now checks each for `nullptr`, leaving the original `load`/`store`/`atomicrmw` un-rewritten rather than dereferencing a null `CallInst *`. No new plumbing was needed to turn the resulting `emitError` into a graceful pipeline failure -- `feme::cpu::runPipeline`'s existing `ErrorDiagnosticGuard` already catches it, the same mechanism every other diagnosed-rather-than-asserted gap in this codebase relies on. The root legalization fix (actually widening a matrix/aggregate-typed masked memory access) remains C8's own scope and is not attempted here -- confirmed by a real reproduction: the 24 `dEQP-VK.tessellation.winding.*hlsl*` cases this gap was first found through, and the 8 `dEQP-VK.tessellation.shader_input_output.*` cases H4d's own fix separately routed into it, all still `Fail` (32/32), just cleanly -- a single, uninterrupted `deqp-vk` run now reaches `DONE!` and prints a normal totals summary instead of `SIGABRT`ing partway through, making H4c's own per-case-resume methodology unnecessary for every case that used to hit this abort. Locked down by three new `MaskIntrinsicsTest` cases (one per `createMasked*` entry point, each asserting a `nullptr` result and a captured `DS_Error` diagnostic for a struct-typed operand standing in for an unsupported matrix/aggregate shape) and one new `LinearizeTest` case (a `store {float, float}` under a divergent `feme.stage.discard` mask that must diagnose and survive as a plain, unmasked `StoreInst` rather than crash `LinearizePass`); `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1828/1887 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). **Measured impact**: the full `dEQP-VK.tessellation.*` group (8 `Pass`/227 `Fail`/879 `NotSupported`) and the `dEQP-VK.draw.*` regression sample (12/139/1806) are both byte-identical to H4b/H4c/H4d's own baselines -- expected, since converting a crash into a clean `Fail` for the same 32 cases cannot move any of the three headline buckets by itself, and no non-tessellation pipeline in this codebase's own test corpus ever creates a masked memory access over a matrix/aggregate-typed value in the first place. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed unchanged (no feature bit or extension touched). See "Roadmap H4e: measured impact" in VulkanCTSReport.md for the full breakdown) | C8, H4c | `feme/lib/Transforms/CPU/MaskIntrinsics.cpp` | P1 |
| H4f | ~~**`splitTessellationControlEntry` (H4a/H4c) only clones a `<entry>.patchconstant` phase when it finds a barrier -- a tessellation-control shader with none (legally the case whenever `OutputVertices == 1`, since a single control-point invocation needs no cross-invocation synchronization, `dEQP-VK.tessellation.winding.*`'s own shape) leaves `PatchConstantPhase == nullptr`, but `compileAndValidateStages` (H4b) unconditionally expects a `.patchconstant` sibling to exist and compiles it as a second stage regardless** (24 of H4d's own re-measured `dEQP-VK.tessellation.*` `Fail`s, all of `dEQP-VK.tessellation.winding.*`'s glsl variants -- confirmed via a real `deqp-vk` run, root-caused and isolated as part of H4d's own investigation but deliberately not fixed there, since it is a distinct gap in the barrier-split design itself, not the `resolveStageIOAccess` array-addressing bug H4d fixed). The whole entry, in this no-barrier shape, is semantically already "the patch-constant phase" for `OutputVertices == 1` (there is only one control point, so nothing meaningfully distinguishes "per control point" from "per patch" here) -- needs either (a) treating a no-barrier entry as *solely* a patch-constant phase whenever every one of its stage-IO writes is patch-frequency (`Patch`-decorated or a tess-factor `BuiltIn`), with an empty/trivial control-point phase synthesized instead, or (b) the more general fix of cloning the *whole* function as `<entry>.patchconstant` unconditionally when there is no barrier (sound only when `OutputVertices == 1`, since with more than one output control point and no barrier there is by definition no legal way for one invocation to see another's data, so nothing but the current invocation's own patch-frequency writes could safely be duplicated this way either -- needs care not to naively assume general soundness)~~ (done, via option (a) of this row's own two proposed fixes: added `isPatchConstantOnlyEntry(Function &F)` (`CanonicalizeStage.cpp`), which scans every address-space-8 store an entry makes and returns true only when at least one exists and every one is patch-frequency (`Patch`-decorated or a tess-factor `BuiltIn`, matching this row's own condition; conservatively `false` for any global carrying `feme.spirv.MemberDecorations`, since a real GLSL tess-factor write is always a plain global, never an interface-block member), and `splitBarrierlessTessellationControlEntry`, which `CloneFunctionInto`s the whole entry as `<name>.patchconstant` when that holds and replaces the original with a trivial `ret void` stub, leaving `splitTessellationControlEntry`'s old barrier-found behavior untouched otherwise. Option (b) (an unconditional whole-function clone) was rejected after tracing `classifySPIRVElement`'s `HullPatchConstant`-phase branch: it classifies any non-`Patch`-decorated address-space-8 write as `Direction::Input` (a read-back of a captured cross-barrier value, never a store), so cloning a function with an ordinary per-vertex output store into the patch-constant phase would misclassify that store's direction and trip `ValidateStagePass`'s `OutputStore` direction check -- a real correctness bug, not merely a theoretical one; `CanonicalizeStageTest.NoBarrierPatchConstantOnlyEntryIsSplitWhole` covers the new split (trivial control-point stub plus a patch-constant clone with the right `PatchOutput`/`TessFactorEdge` signature element), and the pre-existing `HullStageWithNoBarrierIsNotSplit` (an ordinary per-vertex write, no barrier) continues to correctly not split, confirming the `isPatchConstantOnlyEntry` gate is not over-broad. **Deviation**: fixing this row's own gap immediately exposed a second, distinct blocker -- "compiled stage carries no signature reflection" -- spun off and fixed as roadmap H4g below, since it is not part of this row's own described scope (the split logic itself), and a third, still-open blocker spun off as roadmap H4h. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1829/1888 (59 pre-existing, unrelated `Unsupported`), up from 1828/1887 before this row. See "Roadmap H4f: measured impact" in VulkanCTSReport.md for the full `dEQP-VK.tessellation.*` before/after breakdown) | H4d | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Vulkan/GraphicsPipeline.cpp` | P1 |
| H4g | ~~**A compiled stage with zero SPIR-V stage-IO globals never gets `!feme.signature` metadata attached at all** (surfaced by H4f's own new trivial control-point stub, and independently by `dEQP-VK.tessellation.winding.*`'s own genuinely-empty vertex shader, `void main (void) {}`): `canonicalizeSPIRVStage`'s guard (`if (!InputGlobals.empty() || !OutputGlobals.empty())`) skips attaching any signature when a function has no stage-IO globals to begin with, so `feme::cpu::CompiledStage::create` never serializes a `Signature` byte vector for it (its `if (std::optional<EntrySignature> Sig = ...)` branch is simply never taken), and `GraphicsPipeline.cpp`'s `getStageSignature` then hard-errors ("compiled stage carries no signature reflection") on the empty `Bytes`, reached unconditionally via `validateStageInterfaces`'s `getStageSignature(VertexStage)` call -- a second, distinct blocker from H4f's own split-logic gap, hit immediately once H4f's fix let a tessellation pipeline reach this far~~ (done -- fixed at the narrowest safe layer, `CompiledStage::create`'s own serialization step (`feme/lib/Target/CPU/CompiledStage.cpp`), which now always serializes `feme::dxil::getEntrySignature(**Entry).value_or(EntrySignature{})` rather than only when a signature exists, treating an entirely-absent signature identically to an explicit empty one at the artifact-serialization boundary only -- matching `CanonicalizeStagePass::run`'s own pre-existing documented philosophy ("an absent signature is treated as an empty one") without touching `canonicalizeSPIRVStage`'s `Changed`/rewriting behavior anywhere. A more general fix attempted first -- attaching an explicit empty `!feme.signature` in `canonicalizeSPIRVStage` itself whenever no stage-IO globals are found and no signature already exists -- was rejected and reverted: it cannot distinguish a genuinely-empty SPIR-V entry from an unresolved DXIL-origin entry using only that pass's local view, and would have broken the pre-existing `CanonicalizeStageTest.UnresolvableLoadInputIsLeftAlone` test (a DXIL-origin fragment entry with no `!feme.signature` metadata, asserting `run(*M)` returns `false`), which continues to pass unmodified under the layer actually chosen. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1829/1888 (59 pre-existing, unrelated `Unsupported`; same headline as H4f's own row, since this fix changes serialization content, not pass counts), confirmed via `FeMeTargetCPUTests`/`FeMeTransformsGraphicsTests`/`FeMeVulkanTests` all passing including the DXIL-path test above. A real `dEQP-VK.tessellation.*` re-run confirms the "compiled stage carries no signature reflection" error is gone entirely (0 occurrences, down from all 24 `dEQP-VK.tessellation.winding.*` glsl cases) -- see "Roadmap H4g: measured impact" in VulkanCTSReport.md) | H4f | `feme/lib/Target/CPU/CompiledStage.cpp` | P1 |
| H4h | ~~**Fixing H4f and H4g still does not turn any of `dEQP-VK.tessellation.winding.*`'s glsl cases green -- all 24 now fail at a third, later blocker: `validateStageInterfaces` (`GraphicsPipeline.cpp`) unconditionally requires the *vertex* stage to write a 4-component `SV_Position`/`gl_Position` output** (`findSystemValue(*VSSig, SignatureDirection::Output, SignatureSystemValue::Position)`), which is correct for the ordinary vertex -> fragment pipeline (the rasterizer needs a clip-space position from *somewhere*, and without a domain stage the vertex stage is the only producer) but is not a real requirement once a tessellation evaluation stage exists: the CTS's own `winding` test's vertex shader is deliberately and legally empty (`void main (void) {}`; confirmed by reading `vktTessellationWindingTests.cpp` directly), because its tessellation evaluation shader computes `gl_Position` purely from `gl_TessCoord` (`gl_Position = vec4(gl_TessCoord.xy*2.0 - 1.0, 0.0, 1.0);`) and never reads back any vertex-stage output via `gl_in[]`. `validateStageInterfaces` needs to skip (or relax) its `SV_Position` requirement on the vertex stage specifically when a tessellation evaluation stage is present in the pipeline, since in that shape the *domain* stage's own output is what gets rasterized (per `PatchPipeline.cpp`'s `runPatchPipeline`/`Executor.cpp`), not the vertex stage's -- root-caused and isolated as part of H4f/H4g's own re-measurement but deliberately not fixed here, since it is a distinct validation-layer gap, not a stage-splitting or signature-serialization one. Confirmed via a real `deqp-vk` run with `FEME_VULKAN_LOG_CREATION_ERRORS=1`: all 24 `dEQP-VK.tessellation.winding.*` glsl cases now fail with exactly `"vertex stage does not write a 4-component SV_Position output"` (0 occurrences of either of H4f/H4g's own former blockers)~~ (done -- `validateStageInterfaces` (`GraphicsPipeline.cpp`) now takes the compiled `DomainStage` (already threaded through `compileAndValidateStages` for the tessellation-state merge H4b added) as an extra, optional parameter, and checks the `SV_Position` output requirement against *its* signature instead of the vertex stage's whenever it is non-`nullptr`, leaving the vertex stage's own signature (and every other check: fragment varying linkage, per-attachment `SV_TargetN` outputs, vertex-input-attribute coverage) completely untouched otherwise -- a genuine vertex -> fragment pipeline with no domain stage still requires the vertex stage to write `SV_Position`, exactly as before. Covered by two new `GraphicsPipelineTest` cases: `AcceptsTessellationPipelineWithEmptyVertexShader` (a real four-stage tessellation pipeline whose vertex module is `void main (void) {}`, no stage-IO globals at all, now accepted end to end) and `RejectsEmptyVertexShaderWithoutTessellation` (the same empty vertex module in an ordinary two-stage pipeline is still correctly rejected, confirming the relaxation is scoped to the tessellation case only). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1831/1890 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1829/1888 before this row (the two new tests). A real `deqp-vk` run against `dEQP-VK.tessellation.winding.*glsl*` (24 cases, `FEME_VULKAN_LOG_CREATION_ERRORS=1`) confirms the `"vertex stage does not write a 4-component SV_Position output"` error is gone entirely (0 occurrences) and every one of the 24 cases now reaches `vkCreateGraphicsPipelines` success and real rendering -- all 24 still `Fail`, but now at a pixel-comparison mismatch inside the renderer itself (a distinct, later bug tracked as roadmap H4i below), not a pipeline-creation error. **Full group** (`dEQP-VK.tessellation.*`, 1114 cases) and the **regression sample** (`dEQP-VK.draw.*`, 1957-case sample) are both byte-identical to H4g's own recorded totals (8/227/879 and 12/139/1806 respectively) -- 0 regressions, expected, since this fix only relaxes a check that previously always fired for these 24 cases and never fired for any other case in either group. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (no feature bit or extension touched, same reasoning as H4f/H4g). See "Roadmap H4h: measured impact" in VulkanCTSReport.md for the full breakdown) | H4f, H4g | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P1 |
| H4i | ~~**With H4h's own `SV_Position` relaxation in place, all 24 `dEQP-VK.tessellation.winding.*` glsl cases now reach real rendering and fail at a fourth, later blocker: a systematic front-face/winding-orientation mismatch in the rasterized image**, not a pipeline-creation error. A real `deqp-vk` run (`dEQP-VK.tessellation.winding.*glsl*`, 24 cases) shows a consistent pattern across every `_ccw`/`_cw` pair: where the CTS expects a full-viewport quad/triangle of one color, the renderer instead produces the *complementary* culled/uncultured result (e.g. `glsl_quads_ccw` gets "Note: got 4081 white and 15 red pixels" / "Failure: expected only white pixels (full-viewport quad)" while its `_cw` sibling gets "got 0 white and 4096 red pixels" for the same expectation, and the `glsl_triangles_*` cases fail with "triangle orientation is incorrect" at roughly a 50/50 pixel split) -- the shape of a front-face or vertex-winding-order inversion specific to tessellated primitives (the domain stage's own emitted vertex order per `gl_TessCoord`/`VertexOrderCcw`/`VertexOrderCw`, or how the executor's rasterizer classifies a tessellated triangle's front face, likely in `PatchPipeline.cpp`/`Executor.cpp`), rather than a per-fragment color or depth bug (the two colors present in every failing image are always exactly the two the test itself defines for "correctly wound" vs "incorrectly wound"). Root cause not yet isolated -- needs its own investigation, likely starting from `vktTessellationWindingTests.cpp`'s own pass/fail image classification alongside `PatchPipeline.cpp`'s tessellator-output vertex ordering for each of `VertexOrderCcw`/`VertexOrderCw` and `Triangles`/`Quads`, and how that interacts with `VkPipelineRasterizationStateCreateInfo::frontFace`/`cullMode` translation the ordinary (non-tessellated) path already implements~~ (done, though the "systematic ... mismatch" framing was itself a red herring caught before landing the wrong fix: the pre-existing `Tessellator.cpp` `appendTriangle` Cw/Ccw operand-swap convention -- despite `TessellatorTest.cpp`'s own comment admitting it looks "not the standard convention" -- was tried and *reverted* after a real `deqp-vk` before/after comparison (`git stash`-ing just that change and rebuilding `feme_vulkan` for a fast A/B) showed it made `lower_left_domain`'s 8 cases newly pass while breaking `default_domain`/`upper_left_domain`'s previously-nearly-correct 16, an exact swap of which subgroup was broken rather than a net improvement -- proof the tessellator's own convention was already correct for the (implicitly assumed) upper-left domain origin. The real root cause: FeMe never parsed `VkTessellationDomainOrigin` at all (zero references to `DomainOrigin` anywhere in `feme/lib`/`feme/include`), silently treating every `VK_TESSELLATION_DOMAIN_ORIGIN_LOWER_LEFT` pipeline as upper-left -- and per the Vulkan spec (confirmed against `vktTessellationWindingTests.cpp`'s own `verifyResultImage`/`expectVisiblePrimitive` formula), the lower-left domain origin mirrors the tessellator's `(u,v)` parameter frame, which reverses every generated triangle's winding as a side effect of that mirror, independently of the tessellator's own (correct) Cw/Ccw convention. Fixed in `feme/lib/Vulkan/GraphicsPipeline.cpp`: `hasLowerLeftTessellationDomainOrigin` walks `VkPipelineTessellationStateCreateInfo::pNext` for a chained `VkPipelineTessellationDomainOriginStateCreateInfo`, returning true only when its `domainOrigin` is explicitly `VK_TESSELLATION_DOMAIN_ORIGIN_LOWER_LEFT` (the spec default, and an explicit `..._UPPER_LEFT`, both correctly leave it `false`); `flipTessellationWindingForDomainOrigin` swaps `TriangleCw`/`TriangleCcw` (leaving `Point`/`Line` untouched, since only a triangle has a Cw/Ccw-classified winding at all) and is applied to the merged `TessellationState::OutputPrimitive` at the very end of `compileGraphicsPipeline`, once, only when both a tessellation-control stage is present and the lower-left origin was requested. `GraphicsPipelineTest.{FlipsTessellationWindingForLowerLeftDomainOrigin,KeepsTessellationWindingForExplicitUpperLeftDomainOrigin}` cover both the flip and the "explicit upper-left behaves identically to omitted" case at the unit level, alongside the pre-existing `AcceptsTessellationStages`'s own unflipped-by-default assertion. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1833/1892 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1831/1890 before this row (the two new tests). A real `deqp-vk` before/after comparison against `dEQP-VK.tessellation.winding.*glsl*` (24 cases) confirms the fix: before, `lower_left_domain`'s 8 cases showed a genuine, complete front-face inversion (one pipeline of every `_ccw`/`_cw` pair rendering exactly the opposite of both pipelines in `default_domain`/`upper_left_domain`); after, all three domain-origin subgroups (`default_domain`/`lower_left_domain`/`upper_left_domain`) show the *identical* pattern -- confirming `lower_left_domain`'s own systematic inversion is gone. All 24 cases still `Fail` in CTS, but now uniformly for a distinct, much smaller defect (exactly 15/4096 stray pixels for quads, a 1-pixel row-fill boundary discrepancy for triangles, present only on the pipeline of each pair that is supposed to render visibly, never on the one that is supposed to be fully culled -- confirmed a rasterization-precision/tie-break issue, not a winding bug, since it is identical in every domain-origin subgroup and does not correlate with front-face or winding at all) -- spun off as new roadmap row H4j below, since it is a distinct rasterizer-correctness gap, not part of this row's own front-face/domain-origin scope. **Full group** (`dEQP-VK.tessellation.*`, 1114 cases) and the **regression sample** (`dEQP-VK.draw.*`, 1957-case sample) are both byte-identical to H4h's own recorded totals (8/227/879 and 12/139/1806 respectively) -- 0 regressions, expected, since `VkTessellationDomainOrigin` was previously silently ignored everywhere and this fix only changes behavior when a pipeline actually chains the lower-left struct. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: `VkPipelineTessellationDomainOriginStateCreateInfo` is a core Vulkan 1.2 (originally `VK_KHR_maintenance2`) pipeline-creation struct, not a feature bit or an extension name that needs advertising -- it was always legal for an application to chain, FeMe just silently ignored its contents until now, the same "always-accepted struct now actually honored" shape as H4b/H4h's own tessellation-state parsing needed no inventory change either. See "Roadmap H4i: measured impact" in VulkanCTSReport.md for the full before/after breakdown) | H4h | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P1 |
| H4j | ~~**All 24 `dEQP-VK.tessellation.winding.*glsl*` cases still fail even after H4i's own domain-origin fix, now uniformly at a small, distinct rasterization-precision defect**: exactly 15/4096 stray red pixels along what looks like a diagonal/seam pattern for `glsl_quads_*` (coordinates cluster near a corner and along a `y == x` diagonal, e.g. `(14,14),(15,15),(16,16),(17,17)` and `(45,45),(47,47),(55,55),(57,57)`, plus a small run near one corner like `(62,0),(63,0),(62,1),(61,2),(60,3),(59,4)`), and a 1-pixel-row-fill boundary discrepancy for `glsl_triangles_*` (`verifyResultImage`'s top/bottom-row white-pixel-count check is off by exactly one: 64/64 filled where 63 is expected, or 1/64 filled where 0 is expected). Only ever appears on the pipeline of each `_ccw`/`_cw` test-case pair that is supposed to render visibly (never the one supposed to be fully culled), identically across `default_domain`/`lower_left_domain`/`upper_left_domain` -- i.e. it does not correlate with front-face, winding, or domain origin at all, confirming it is a rasterizer tie-break/rounding or tessellator crack-free-bridging-seam issue, not a winding bug. Root cause not yet isolated -- candidates to investigate first: the tessellator's own "crack-free" boundary-ring-to-inset-core bridging (`Tessellator.cpp`'s `appendTriangleLattice`/`tessellateQuad`, whether the seam between the two regions is truly watertight at every sample point) and the rasterizer's own top-left fill-rule edge-ownership tie-break for two triangles sharing an exact edge (`Executor.cpp`'s `edgeFn`/coverage test)~~ (done -- both root causes were isolated entirely in `Executor.cpp`'s coverage test, not `Tessellator.cpp`'s bridging (the tessellator's own boundary-ring-to-inset-core seam was verified watertight by direct instrumentation: the two candidate triangles at a crack pixel share bit-identical vertex positions, confirming no duplicate/divergent domain-point evaluation). **Root cause 1 (quads, 15 stray pixels)**: the per-sample coverage test's `edgeFn` (`float`) is not bit-exactly antisymmetric under vertex-order reversal (its two directions subtract different operand pairs, `P - A` vs `P - B`), so two adjacent triangles sharing an exact diagonal edge (the tessellator's own quad-core-cell split) can each independently round a sample landing almost exactly on that shared edge to a spuriously negative value and both reject it -- a classic floating-point rasterization crack, confirmed via `FEME_DEBUG_XY`-gated instrumentation at CTS's own failing pixel (16,16) and reproduced in isolation with a Python float32 emulation. **Root cause 2 (triangles, 1-pixel row-fill off-by-one)**: an independent, pre-existing polarity bug in `isTopLeftEdge`: with tessellation factor 5 landing every domain edge on a clean multiple of `64/5 = 12.8`, a lone tessellated triangle's own outer hypotenuse can land a sample exactly on `E == 0.0`, and the old (backwards) tie-break formula (`(Dy == 0 && Dx > 0) || Dy < 0`) included it when Vulkan's top-left rule requires exclusion, independent of winding or domain origin (confirmed identical in both `_ccw` and `_cw` "visible" pipelines). **Fixes**: (1) a new `edgeFnD` (the same edge function, evaluated in `double`) replaces `edgeFn` for the coverage test's three inside/outside comparisons only (the barycentric interpolation weights stay `float`, since those are values, not coverage decisions) -- shrinking the rounding from `float`'s ~2^-23 relative precision to `double`'s ~2^-52, several orders of magnitude below any crack this rasterizer's own coordinate range produces; (2) `isTopLeftEdge`'s polarity is flipped to `(Dy == 0 && Dx < 0) || Dy > 0`, re-derived from and matching Vulkan's (D3D/OpenGL-identical) top-left convention for a positively-wound triangle (`Area > 0`, guaranteed by the existing `SArea < 0.0f` triangle-assembly reorder). `ExecutorTest.cpp` gains two new cases: `DoublePrecisionEdgeTestClosesAFloatRoundingCrackBetweenAdjacentTriangles` (two triangles sharing a non-axis-aligned, non-"nice"-fraction float32 edge reached through the executor's own NDC-to-screen `projectVertex` transform, confirmed to reproduce the exact crack pre-fix and close it post-fix) and `TopLeftTieBreakExcludesALoneTrianglesOwnBoundaryEdge` (a single triangle whose hypotenuse is a 4x4 viewport's own anti-diagonal, confirming all four boundary pixel centers landing exactly on it are excluded, leaving exactly the 6 strictly-interior pixels filled); both are confirmed to fail against the pre-fix code and pass against the fix. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1835/1894 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from 1833/1892 before this row (the two new tests). A real `deqp-vk` run against `dEQP-VK.tessellation.winding.*glsl*` (24 cases) confirms the fix: **all 24/24 now pass** (up from 0/24 before this row). **Full group** (`dEQP-VK.tessellation.*`, 1114 cases) rises to 32/1114 pass (up from H4i's own 8/1114, exactly the 24 newly-fixed winding cases), 203/1114 fail (down from 227, the same 24), 879/1114 not-supported (unchanged) -- confirming zero regressions, an exact pass/fail shift matching this row's own scope. The **regression sample** (`dEQP-VK.draw.*`, 1957-case sample) is byte-identical to H4i's own recorded totals (12/139/1806) -- 0 regressions, expected, since neither fix changes any coverage-test outcome that was not already a genuine floating-point tie/crack. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a pure rasterizer-precision bug fix, touching no feature bit or extension. See "Roadmap H4j: measured impact" in VulkanCTSReport.md for the full before/after breakdown) | H4i | `feme/lib/Graphics/Executor.cpp` | P1 |
| H5 | ~~**Geometry stage.** Same rejection as H4, same milestone (G5), but an independent feature bit (`geometryShader`), an independent limit block (`maxGeometry*`), stream output, and `multiviewGeometryShader` now that H2 has landed. Whole `dEQP-VK.geometry` group (partially done, and broken down below the same way H4 was: H5a closes the execution-mode half of "reflect a geometry entry point at all" (see H5a's own row for what remains of that -- the per-vertex input addressing gap it found is H5b, a genuinely new piece of machinery neither H4's tessellation precedent nor G5's own wrapper work needed, since a hull control-point phase's own per-control-point inputs are restricted to "this invocation's own", never an arbitrary dynamically-indexed one the way a geometry entry's `gl_in[i]` is). `dEQP-VK.geometry.*` is unaffected so far, still 0 `Pass`/0 `Fail`/200 `NotSupported` -- correctly so, since `CanonicalizeStagePass::run` deliberately does not yet accept `ShaderStage::Geometry` (H5a's own report entry explains why not doing so was the right call, not an oversight). This row stays open until H5a-H5e all close)~~ (closed: H5a-H5e-e all closed as of this row's own investigation; re-running the full `dEQP-VK.geometry.*` mustpass sweep to confirm the milestone's own closing condition uncovered one further, previously-undetected real bug of its own, root-caused and fixed as new row H5h (a full-process sweep had never actually completed cleanly before, silently truncating the run at the first crash rather than reporting it -- see H5h's own row). With H5h closed too, this row's own stated closing condition (H5a-H5e all closed) is met and no further genuine crash remains in the group) | G5, H4 (shared wrapper work) | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics` | P1 |
| H5a | ~~**No SPIR-V geometry entry point's declared shape (input/output primitive class, instance count, maximum emitted vertex count) is captured at all**, so nothing downstream -- not even a future `CanonicalizeStagePass` extension -- has anywhere to read it from. `ConvertSPIRVToLLVMPass::collectEntryPoints` maps SPIR-V's tessellation execution modes onto `feme::graphics::TessellationState` (H4a) but has no equivalent for a geometry entry's `InputPoints`/`InputLines`/`InputLinesAdjacency`/`Triangles`/`InputTrianglesAdjacency`, `OutputPoints`/`OutputLineStrip`/`OutputTriangleStrip`, `Invocations` or `OutputVertices` execution modes -- two of which (`Triangles`, `OutputVertices`) share their SPIR-V enumerant value with a tessellation-evaluation entry's domain and a hull entry's output control point count respectively, needing the declaring entry's own `Stage` to disambiguate~~ (done: new `feme::graphics::GeometryState`/`GeometryInputPrimitive`/`GeometryOutputPrimitive` (Geometry.h/.cpp, mirroring Tessellation.h/.cpp) plus `ConvertSPIRVToLLVMPass` capturing all six geometry execution modes into `feme.geometry.*` passthrough attributes, disambiguating the two shared enumerant values by `EntryPointInfo::Stage`. Investigating this row's own full scope (mirroring H4a+H4b) before writing code found that lifting `CanonicalizeStagePass::run`'s stage filter to accept `ShaderStage::Geometry` is not yet safe -- see "Roadmap H5: what H5a found, and why it stops here" in VulkanCTSReport.md -- so this row deliberately stops at execution-mode capture, spinning the real blocker off as new row H5b. `dEQP-VK.geometry.*` is unchanged (0/0/200), expected since nothing yet calls any of this. `GeometryTest.cpp` (new file, 7 cases) and `spirv-to-llvm-geometry-execution-modes.mlir` (new lit test, 4 `RUN`/`CHECK` blocks including the `Triangles`/`OutputVertices` disambiguation case) cover it; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1843/1902 (59 pre-existing, unrelated `Unsupported`), up from 1835/1894 before this row. See "Roadmap H5a: measured impact" in VulkanCTSReport.md for the full writeup~~ | G5, H4a | `feme/lib/Conversion/SPIRVToLLVM/ConvertSPIRVToLLVMPass.cpp`, `feme/include/feme/Graphics/Geometry.h`, `feme/lib/Graphics/Geometry.cpp` | P1 |
| H5b | ~~**Thread a non-constant, dynamically-indexed SPIR-V array index into `feme.stage.input.load`'s `Vertex` operand**, the gap H5a's own investigation found: a geometry entry point's per-vertex inputs (`gl_in[]`-shaped) are read via `gl_in[i]` for a loop-carried `i`, but `loadStageIOValue`'s existing recursion always passes a constant `Zero` for `Vertex` -- there is no code path today that recognizes a SPIR-V array-typed `Input`-storage-class global being indexed by a real SSA value (as opposed to the constant-offset-only `getStageIOBaseAndOffset`/`resolveStageIOAccess` byte-offset resolution H2d built for a builtin interface block's own *member* access) and threads that index through as `Vertex` instead of `Row`/`Component`. Needs: recognizing an array-of-per-vertex-block SPIR-V `Input` global (distinct from a matrix's `Row` dimension, which is a compile-time-fixed shape, not a genuine "the pipeline supplies N of these, addressed by this stage's own bounded loop variable" one); extracting the outer (vertex) index as a `Value*` rather than requiring it constant; and validating (`ValidateStagePass`, itself still not extended to Geometry -- see H5e) that a non-constant `Vertex` operand is only ever legal for a stage whose ABI actually supports it (`FemeGeometryArgs`'s primitive-major `Inputs` layout), diagnosed otherwise. `CanonicalizeStagePass::run` must not accept `ShaderStage::Geometry` until this lands (H5a's own report explains why). Whole `dEQP-VK.geometry` group remains blocked on this~~ (done: `CanonicalizeStage.cpp` gains `getDynamicVertexIndexedAccess`/`getStageIOGlobal`, recognizing exactly this GEP shape and threading the extracted, non-constant vertex `Value*` through `StageIOAccess::Vertex` in place of the constant `Zero` every other stage-IO access seeds it with, with `addElements` taught to peel the one outer per-vertex array dimension before a builtin interface block's own per-member metadata is read so a block member's `RowCount` isn't conflated with the vertex-array's own extent; `ValidateStage.cpp` gains `validateVertex`, diagnosing a non-constant `Vertex` operand everywhere except the one stage (`Geometry`) whose ABI actually supports addressing one at runtime. Two things this row deliberately leaves for a follow-on: a *constant*-indexed `gl_in[k]` access still folds that index into `Row` instead of `Vertex` via the pre-existing byte-offset path (spun off as H5f, since the roadmap's own title here scopes only the non-constant case), and `SPIRVToLLVMPatterns.cpp`'s `StageIOGlobalVariablePattern` still only attaches `feme.spirv.MemberDecorations` metadata for a bare block global, not an array-of-block one, so a real `gl_in[]` builtin block doesn't yet reach `CanonicalizeStage.cpp` with the metadata this row's `addElements` fix expects (spun off as H5g). `dEQP-VK.geometry.*` stays unaffected (0/0/200, expected -- H5c hasn't lifted the stage filter yet); `ninja check-feme` 1847/1906 (59 pre-existing, unrelated `Unsupported`), up from H5a's 1843/1902 by exactly the 4 new tests this row adds. See "Roadmap H5b: measured impact" in VulkanCTSReport.md for the full writeup) | H5a | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P1 |
| H5f | ~~**A *constant*-indexed `gl_in[k]`-shaped access (or any other per-vertex-arrayed `Input` global) still resolves through the pre-existing `getStageIOBaseAndOffset`/`resolveRowComponent` byte-offset path exactly as before H5b, folding that constant array index into the accessed member's own `Row` operand instead of `Vertex`**, and the corresponding `SignatureElement.RowCount` for such a global still reports the per-vertex array's own extent (e.g. 3 for a triangle's `gl_in[3]`) rather than a real matrix's row count -- the two are indistinguishable in the signature today, which H5b's own investigation found and deliberately left alone (out of that row's own scope, which asked only for the non-constant case). Needs a way for a `SignatureElement` (or a sibling flag alongside it) to record "this dimension is a per-vertex array, not a matrix row" so a real consumer (H5c's own geometry-entry canonicalization, or a future one) can tell the two apart regardless of whether the shader's own index into it happens to be constant, and route a constant index through `Vertex` (not `Row`) for consistency with the non-constant case H5b already handles~~ (done: new shared predicate `isPerVertexArrayInputGlobal` -- structural only, matching `getDynamicVertexIndexedAccess`'s own precedent of leaving stage-appropriateness to `ValidateStagePass`'s `validateVertex` rather than trying to disambiguate a genuine geometry per-vertex array from some other stage's real per-invocation matrix attribute at this level -- replaces that function's own inlined check and is now also consulted by `resolveStageIOAccess`'s ordinary constant-offset fallback, splitting a resolved byte offset into a constant vertex index plus a residual one-vertex-relative offset exactly the way the dynamic path already does, whenever the resolved global is this shape and the access is not a whole-global aggregate one. New `SignatureElement::RowCountIsVertexArray` (`feme/include/feme/Core/Signature.h`, `SignatureAbiVersion` bumped 3→4, every pre-existing serialized `feme.signature` test blob mechanically re-serialized to the new byte layout) records exactly the distinction the roadmap asked for, set true only for a whole (non-block) per-vertex-arrayed `Input` global's own element, always false for a builtin interface block's own per-member elements (whose `RowCount` never included the per-vertex dimension to begin with). `dEQP-VK.geometry.*` stays unaffected (0/0/200, expected -- H5c hasn't lifted the stage filter yet); the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5b's own baseline (12 `Pass`/139 `Fail`/1806 `NotSupported`, 0 regressions); `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1849/1908 (59 pre-existing, unrelated `Unsupported`), up from H5b's 1847/1906 by exactly the 2 new tests this row adds. See "Roadmap H5f: measured impact" in VulkanCTSReport.md for the full writeup) | H5b | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H5g | ~~**`SPIRVToLLVMPatterns.cpp`'s `StageIOGlobalVariablePattern::matchAndRewrite` only attaches `feme.spirv.MemberDecorations` metadata when a stage-IO global's pointee type is directly an `mlir::spirv::StructType`, never an `mlir::spirv::ArrayType<StructType>`** -- the exact shape a geometry entry's `gl_in[]` builtin interface block actually takes (an array of the per-vertex block, not the bare block) -- so a real SPIR-V geometry shader's `gl_in` global reaches `CanonicalizeStage.cpp` with no member-decoration metadata at all today, even after H5b's own `addElements` fix (which peels the outer array dimension correctly, but only once metadata is present to peel in front of). Needs `StageIOGlobalVariablePattern` to also recognize the pointee-is-`ArrayType`-of-`StructType` shape and attach the inner struct's own per-member decorations the same way it already does for a bare block, so H5b's mechanism has real input to exercise ahead of H5c~~ (done: `matchAndRewrite` now peels one outer `mlir::spirv::ArrayType` off the pointee, if present, before checking for a `mlir::spirv::StructType`, attaching `feme.spirv.MemberDecorations` off the inner struct either way -- matching `CanonicalizeStage.cpp`'s own `addElements` "peel one array dimension, then look for member decorations" precedent on the producing side instead of the consuming one. New `SPIRVToLLVMTest.PerVertexArrayInterfaceBlockPreservesMemberDecorations` (gtest) and a new `CHECK` block in `spirv-to-llvm-stage-io.mlir` (lit) cover the `gl_in[]`-shaped case directly. `dEQP-VK.geometry.*` stays unaffected (0/0/200, expected -- H5c hasn't lifted the stage filter yet); the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5f's own baseline (12 `Pass`/139 `Fail`/1806 `NotSupported`, 0 regressions); `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1850/1909 (59 pre-existing, unrelated `Unsupported`), up from H5f's 1849/1908 by exactly the 1 new test this row adds. See "Roadmap H5g: measured impact" in VulkanCTSReport.md for the full writeup) | H5a | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| H5c | ~~**Lift `CanonicalizeStagePass::run`'s stage filter to accept `ShaderStage::Geometry`** once H5b makes doing so safe, routing a geometry entry through `canonicalizeSPIRVStage(*F, ShaderStage::Geometry, SPIRVCanonicalPhase::Ordinary)` (no barrier-splitting needed, unlike Hull -- GLSL/SPIR-V compiles a geometry shader to a single entry point already, per GeometryWrapper.cpp's own file comment). Add unit coverage mirroring `CanonicalizeStageTest`'s Hull/Domain cases, for `gl_PrimitiveIDIn`/`gl_InvocationID`/`gl_Layer`/`gl_ViewportIndex`/`gl_PrimitiveID` at minimum (all of which already map to an existing `SignatureSystemValue` per `getSystemValueForBuiltIn` -- no new system-value work needed there)~~ (done: `CanonicalizeStagePass::run`'s stage filter now also accepts `ShaderStage::Geometry`, routed through the same `else` branch Domain already uses -- no barrier-splitting needed, confirming H5a/H5b's own reasoning. `getSystemValueForBuiltIn` needed no changes: `gl_PrimitiveIDIn`/`gl_PrimitiveID` (`BuiltIn PrimitiveId`, disambiguated by storage class), `gl_InvocationID`, `gl_Layer`, and `gl_ViewportIndex` already mapped onto their respective `SignatureSystemValue`s. New `CanonicalizeStageTest.GeometryStageMapsSystemValues` mirrors the Hull/Domain cases. `dEQP-VK.geometry.*` stays 0/0/200 (expected -- `vkCreateGraphicsPipelines` still rejects the geometry stage bit, H5e's own job); the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5g's own baseline (12 `Pass`/139 `Fail`/1806 `NotSupported`, 0 regressions). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1851/1910 (59 pre-existing, unrelated `Unsupported`), up from H5g's 1850/1909 by exactly the 1 new test this row adds. See "Roadmap H5c: measured impact" in VulkanCTSReport.md for the full writeup) | H5b | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P1 |
| H5d | ~~**Chain the geometry stage into `Executor::executeDraws`**, the "as does the whole ... executor" half FeMeGraphicsDesign.md's "Tessellation and geometry stage model" section already flags open: assemble each draw's primitives (`splitListPrimitiveAdjacency`/`splitStripPrimitiveAdjacency`, Pipeline.h, already implemented), gather their vertex attributes into a geometry batch (`feme::graphics::buildGeometryInputs`/`buildGeometryInvocations`, GeometryInputs.h, already implemented), run the compiled geometry stage (`CompiledStage::invokeGeometry`, already implemented), replay its flat emitted-vertex/strip-boundary records back into one `GeometryStreamBuilder` per primitive and merge them in lane order (`feme::cpu::collectGeometryStreams`/`feme::graphics::mergeGeometryStreamsInLaneOrder`, both already implemented), and rasterize the merged stream's own strips in place of the vertex/domain stage's output -- the same "last pre-rasterization stage" substitution H4's own tessellation chaining already established the pattern for (`RasterSig`). `GraphicsPipeline` needs a `setGeometryStage`/`hasGeometryStages` pair mirroring `setTessellationStages`/`hasTessellationStages`. Needs real unit coverage in `ExecutorTest.cpp` (a hand-compiled trivial geometry stage rendering, mirroring the tessellation domain-stage rasterization tests) before H5e's real SPIR-V-sourced pipelines can be trusted to hit the same code path correctly~~ (done: `GraphicsPipeline` gains `setGeometryStage`/`hasGeometryStages`/`getGeometryStage`/`getGeometryState`, mirroring `setTessellationStages`/`hasTessellationStages` exactly. `Executor.cpp`'s topology validation now accepts the four `*WithAdjacency` topologies only when a geometry stage is bound (later corrected by roadmap H7l: the real spec only gates these topologies on the `geometryShader` *device feature*, not on the specific pipeline binding a geometry stage, so a vertex/fragment-only pipeline using an adjacency topology is legal too -- see H7l's own entry below); a new per-draw block assembles each primitive (including adjacency reordering, matching `splitListPrimitiveAdjacency`/`splitStripPrimitiveAdjacency`'s own documented vertex order, and restart-segmented windows for the two strip-adjacency topologies), builds the `GSInput`/`GSScratch` `StageStorage` via `StageLink.h`'s `linkStageElements`/`copyLinkedElements` (a deliberate deviation from `feme::graphics::buildGeometryInputs`, which assumes a *vertex*-shaped, one-attribute-per-vertex input layout rather than geometry's own primitive-major, `VerticesPerPrimitive`-wide one -- `buildGeometryInputs` remains right for the real SPIR-V-sourced path H5e will use once a geometry `Function`'s own IR-level input signature is available; the hand-built pipeline path this row's tests exercise has no such `Function` to derive it from), builds `FemeGeometryInvocation` records via `buildGeometryInvocations`, invokes the compiled stage via `CompiledStage::invokeGeometry`, replays the emitted records through `collectGeometryStreams`/`mergeGeometryStreamsInLaneOrder`, and rebinds `RasterSig`/`RasterOut`/`RasterClass` to the merged stream's own output block and primitive topology in place of the vertex/domain stage's, exactly mirroring H4's own `RasterSig` substitution pattern. Deliberately out of scope (spun off as H5d-a): `GeometryState` has no `Invocations`/`gl_InvocationID` field yet, so this row's chaining always runs a geometry stage with exactly one invocation per input primitive (`GL_INVOCATIONS`-less GLSL, i.e. `layout(invocations = 1)` implicitly) -- correct for the common case and for every hand-compiled test this row adds, but not yet able to honor a real shader's own `layout(invocations = N)` declaration once H5e starts importing real SPIR-V geometry entries. New `ExecutorTest.cpp` coverage: `GeometryStagePassesThroughATriangleCoveringTheViewport` (hand-compiled passthrough geometry stage, checked against the same full-viewport solid-color fill the ordinary vertex/fragment pipeline test already checks) and `RejectsAdjacencyTopologyWithoutAGeometryStage`. `dEQP-VK.geometry.*` stays 0/0/200 (expected -- `vkCreateGraphicsPipelines` still rejects the geometry stage bit, H5e's own job); the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5c's own baseline (12 `Pass`/139 `Fail`/1806 `NotSupported`, 0 regressions). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1853/1912 (59 pre-existing, unrelated `Unsupported`), up from H5c's 1851/1910 by exactly the 2 new tests this row adds. See "Roadmap H5d: measured impact" in VulkanCTSReport.md for the full writeup) | H5c, G5 (all consumed pieces exist; only the chaining itself is new) | `feme/lib/Graphics/Executor.cpp`, `feme/include/feme/Graphics/Pipeline.h`, `feme/lib/Graphics/Pipeline.cpp` | P1 |
| H5d-a | ~~**`GeometryState` has no `Invocations` field, and `Executor.cpp`'s new geometry-chaining block (H5d) always invokes a bound geometry stage exactly once per input primitive** -- correct for GLSL's default `layout(invocations = 1)`, but not for a real shader that declares `layout(invocations = N)` for `N > 1`, which SPIR-V/GLSL geometry shaders can and do use to invoke the same entry point `N` times per primitive, each with a distinct `gl_InvocationID` (`SystemValue::InvocationID`, already representable in a `Signature` per `getSystemValueForBuiltIn`, but never populated into a real invocation record by H5d's own `FemeGeometryInvocation`-building loop, which has no per-invocation-index dimension at all today). Needs: a `GeometryState::Invocations` field (mirroring `TessellationState`'s own shape), `Executor.cpp`'s invocation-building loop widened to build `Invocations * PrimitiveCount` records instead of `PrimitiveCount`, each stamped with its own `gl_InvocationID`, and `collectGeometryStreams`/`mergeGeometryStreamsInLaneOrder`'s own lane-ordering contract re-checked against multiple invocations per primitive (today one invocation is one lane; N invocations per primitive means N lanes per primitive, which the merge's existing "lane order" concept may already tolerate, needs confirming with a real test rather than assumed)~~ (done: `GeometryState::Invocations` already existed (added in H5a's own commit), so the real remaining gap was entirely in the ABI/wrapper/executor plumbing to use it. `FemeGeometryInvocation` (RuntimeABI.h) gains an `InvocationID` field (`StageArgsLayout.h`'s `GeometryInvocationField`/`getGeometryInvocationType` updated to match); `GeometryWrapper.cpp` gains `lowerGeometryInvocationID` (sharing a new `lowerGeometryInvocationField` helper with the existing `lowerGeometryPrimitiveID`), wired into the `InputLoad` switch for `SignatureSystemValue::InvocationID` (which aliases hull's `OutputControlPointID` by enum value but is disambiguated by stage). `buildGeometryInvocations` (GeometryInputs.h/.cpp) gains an optional `InvocationIDs` array parameter, populating each record's new field. `Executor.cpp`'s H5d geometry-chaining block now computes `Invocations = max(GState.Invocations, 1)` and builds `RowCount = PrimitiveCount * Invocations` rows (each primitive's input vertex attributes repeated once per invocation, primitive-major/invocation-minor, i.e. row `= P * Invocations + Inv`), each stamped with both its `SV_PrimitiveID` and its own `gl_InvocationID`; `GeomInputLinks`'s varying-link filter now also excludes `InvocationID` (sourced from the invocation record, not a linked varying), mirroring `PrimitiveID`'s existing exclusion. `collectGeometryStreams`/`mergeGeometryStreamsInLaneOrder` needed zero logic changes -- both already treat `Args.PrimitiveCount` generically as "row count" and merge lanes in array order, confirmed by a real `ExecutorTest.cpp` case (`GeometryStageInvocationsRunOncePerDeclaredInvocationCount`, `Invocations = 2` on a single triangle, invocation-0/invocation-1 emit differently colored full-viewport strips, `BlendMode::Replace` requires invocation 1's strip to be the final write for the test to pass). That same test exposed a **pre-existing latent bug** in the merged `GeometryStreamBuilder`'s own capacity, sized from a single row's own `GState.MaxOutputVertices` bound rather than the combined `RowCount * GState.MaxOutputVertices` total across every row -- silently truncating any lane after the first row's own budget was exhausted (present since H5d, never before exercised since every prior geometry test used exactly one row). Fixed alongside this row's own widening. New coverage: `GeometryWrapperTest.LowersInvocationIDInputLoad`, `CompiledStageTest.InvokeGeometryReadsInvocationIDDistinctFromPrimitiveID`, `GeometryInputsTest.BuildsDistinctInvocationIDsForRepeatedPrimitives` (plus an extended `BuildsOneInvocationPerPrimitiveIdInOrder`), and `ExecutorTest.GeometryStageInvocationsRunOncePerDeclaredInvocationCount`. `dEQP-VK.geometry.*` stays byte-identical 0/0/200 (still gated on `geometryShader`, H5e's own job) and the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5d's own baseline (12 `Pass`/139 `Fail`/1806 `NotSupported`, 0 regressions, failing-case-name set unchanged). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1857/1916 (59 pre-existing, unrelated `Unsupported`), up from H5d's own 1853/1912 by exactly the 4 new tests this row adds. See "Roadmap H5d-a: measured impact" in VulkanCTSReport.md for the full writeup) | H5d | `feme/include/feme/Graphics/Geometry.h`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H5e | ~~**`vkCreateGraphicsPipelines` accepts `VK_SHADER_STAGE_GEOMETRY_BIT`, and `geometryShader`/`maxGeometry*`/`multiviewGeometryShader` are advertised.** `GraphicsPipeline.cpp`'s `translateFixedFunctionState` still rejects any stage bit besides vertex/fragment/tessellation (the `default:` case's own comment already names this row); needs a `GeometryInfo` output parameter mirroring `TessControlInfo`/`TessEvalInfo`, `PhysicalDeviceInfo.cpp`'s `geometryShader` feature bit and `maxGeometryShaderInvocations`/`maxGeometryInputComponents`/`maxGeometryOutputComponents`/`maxGeometryOutputVertices`/`maxGeometryTotalOutputComponents` limits (all currently either `VK_FALSE`/0 or absent), and -- now that H2 has landed multiview -- `multiviewGeometryShader` in the same `VkPhysicalDeviceMultiviewFeatures`/aggregate-1.2-struct H2's own row added `multiviewTessellationShader` to (still `VK_FALSE` pending H4's own remaining rows). A real `dEQP-VK.geometry.*` run (200 cases) and the standard `dEQP-VK.draw.*` regression sample close this row's own measurement, matching H4b's own precedent of reporting the real, possibly-partial pass/fail/not-supported breakdown rather than assuming "whole group"~~ (done: `vkCreateGraphicsPipelines` accepts `VK_SHADER_STAGE_GEOMETRY_BIT`, compiles it into a `feme::ShaderStage::Geometry` `CompiledStage`, reflects its `GeometryState` into `graphics::GraphicsPipeline::setGeometryStage`, enforces the four `*_WITH_ADJACENCY` topologies' own requirement of a bound geometry stage (`mapTopology` now implements them too), and validates `Invocations`/`MaxOutputVertices` against `maxGeometryShaderInvocations`/`maxGeometryOutputVertices`; `PhysicalDeviceInfo.cpp`'s `geometryShader` and `EntryPoints.cpp`'s `multiviewGeometryShader` both honestly flip to `VK_TRUE`. This is the Vulkan-acceptance half of the row only: `dEQP-VK.geometry.*` still measures 0/200 passing (167 fail, 33 not-supported) because `ConvertSPIRVToLLVMPass`/`SPIRVToLLVMPatterns` have no lowering for SPIR-V's `spirv.EmitVertex`/`spirv.EndPrimitive` at all yet, even though the LLVM-IR-level `feme.stage.stream.emit`/`.cut` intrinsics and their whole lowering pipeline (`GeometryWrapperPass`) have been ready since G5 -- see roadmap H5e-a below, and "Roadmap H5e: measured impact" in `VulkanCTSReport.md`) | H5d, H2 | `feme/lib/Vulkan/{GraphicsPipeline,PhysicalDeviceInfo,EntryPoints}.cpp` | P1 |
| H5e-a | ~~**`ConvertSPIRVToLLVMPass`/`SPIRVToLLVMPatterns` have no conversion pattern for SPIR-V's `spirv.EmitVertex`/`spirv.EndPrimitive` ops at all** (confirmed absent by grepping the whole `lib/`/`include/` tree for `EmitVertexOp`/`EndPrimitiveOp`), the root cause behind 122 of H5e's own measured 167 `dEQP-VK.geometry.*` failures (`error: failed to legalize operation 'spirv.EmitVertex'`/`'spirv.EndPrimitive'` -- virtually every real GLSL geometry shader calls both, since without them a geometry stage can emit no output vertices at all). Needs a new pattern turning each op into a call to the same `feme.stage.stream.emit`/`feme.stage.stream.cut` intrinsics `GeometryWrapperPass::lowerGeometryStreamEmit`/`lowerGeometryStreamCut` (built under G5, already fully implemented and tested) already know how to lower, mirroring how every other stage-IO SPIR-V op already routes through a `feme.stage.*` intrinsic rather than a bespoke LLVM IR shape. A real `dEQP-VK.geometry.*` re-run after this row lands should re-triage the remaining, still-unexplained ~21-case "silent `VK_ERROR_INITIALIZATION_FAILED`, no diagnostic emitted" bucket H5e's own report flagged (degenerate zero-emit shaders and the `triangle_strip_adjacency`/`basic_primitive` input-primitive-class shapes) once the dominant EmitVertex/EndPrimitive noise is gone, splitting out further lettered rows for whatever remains~~ (done: `SPIRVToLLVMPatterns.cpp` gains `EmitVertexConversionPattern`/`EndPrimitiveConversionPattern`, converting `spirv.EmitVertex`/`spirv.EndPrimitive` into a call to `feme.stage.stream.emit(0)`/`feme.stage.stream.cut(0)` respectively, mirroring `SubpassLoadPattern`'s existing precedent of calling a `feme.stage.*` function directly rather than adding a bespoke LLVM IR shape; the stream operand is always the constant `0`, matching both ops' own SPIR-V spec text restricting them to "only one stream present". New lit test `spirv-to-llvm-geometry-stream.mlir`. A real `dEQP-VK.geometry.*` re-run (200 cases) confirms the dominant `EmitVertex`/`EndPrimitive` bucket (122 of H5e's 167 failures) is completely gone -- zero matches for either op in the new log -- turning one previously-failing case into an outright pass (`0 -> 1/200`) and redistributing the other 121 into new/pre-existing buckets this row's own re-triage breaks down in full. `NotSupported` stays byte-identical (33/200); the `dEQP-VK.draw.*` 1957-case sample is byte-identical to H5e's own baseline (12 `Pass`/155 `Fail`/1790 `NotSupported`, 0 regressions, expected since no `dEQP-VK.draw.*` case in that sample exercises a geometry stage). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1863/1922 (59 pre-existing, unrelated `Unsupported`), up from H5e's own 1862/1921 by exactly the 1 new lit test this row adds. Four new lettered rows (H5e-b, H5e-c, H5e-d, H5e-e) spun off below for the still-unexplained buckets this re-triage found; see "Roadmap H5e-a: measured impact" in VulkanCTSReport.md for the full breakdown) | H5e | `feme/lib/Conversion/SPIRVToLLVM/{ConvertSPIRVToLLVMPass,SPIRVToLLVMPatterns}.cpp` | P1 |
| H5e-b | ~~**21 `dEQP-VK.geometry.*` cases fail `vkCreateGraphicsPipelines` with `VK_ERROR_INITIALIZATION_FAILED` and no diagnostic printed at all** (`builtin_variable.in_block.primitive_id_in`/`primitive_id_in_restarted`, `input.basic_primitive.{line_strip,line_strip_adjacency,triangle_fan}`, `input.triangle_strip_adjacency.vertex_count_*`, `emit.{line_strip,points,triangle_strip}_emit_0_end_0`) -- exactly H5e's own flagged bucket, unchanged in composition and count now that H5e-a's `EmitVertex`/`EndPrimitive` noise is gone. Root cause not yet isolated at all: needs bisecting which of `GraphicsPipeline.cpp`'s geometry-stage acceptance checks, `GeometryWrapperPass`, or a still-missing input-primitive-class/degenerate-zero-emit-shader code path silently rejects pipeline creation with no error text reaching the log~~ (done: two independent, unrelated false-positive validation bugs in `GraphicsPipeline.cpp` accounted for all 21 cases, both stale relative to work later milestones had already landed. (1) The creation-time `primitiveRestartEnable` gate still only allowed `VK_PRIMITIVE_TOPOLOGY_TRIANGLE_STRIP`, even though H5d's own `Executor.cpp` had long since implemented restart for every strip/fan topology (`LineStrip`/`TriangleFan`/the two `*StripWithAdjacency` topologies too) -- the two never agreed after H5d landed. Fixed by adding `feme::graphics::topologySupportsPrimitiveRestart` as the single shared source of truth both `GraphicsPipeline.cpp`'s gate and `Executor.cpp`'s own `RestartEnabled` condition now consult, accounting for 18 of the 21 cases. (2) `validateStageInterfaces`'s "the last pre-rasterization stage must write a 4-component SV_Position" check (and the fragment-input/vertex-output location-linkage loop right next to it) rejected a geometry entry point that emits no vertices at all (`dEQP-VK.geometry.emit.*_emit_0_end_0`'s degenerate `void main(void) {}` bodies): SPIR-V only lists an entry point's *used* interface variables, so such a shader's own reflected signature has zero elements at all, not merely a missing `Position`. Nothing is ever rasterized from a stage that emits nothing, regardless of whether it wrote a position or a varying, so both checks (and `Executor.cpp`'s own runtime counterpart of the first one) now treat an entirely-empty geometry signature as a legal no-op instead, accounting for the remaining 3 cases -- all three now pass outright. `FEME_VULKAN_LOG_CREATION_ERRORS=1` reruns of the exact 21-case list confirmed each fix in isolation; the full `dEQP-VK.geometry.*` group moves from 1/200 to 4/200 passing (166 -> 163 failing, 33/200 not-supported unchanged), with the 18 restart cases now correctly landing in H5e-a's own already-tracked, unrelated 60-case `getelementptr`/output-array-storage bucket instead of a silent, unattributed failure. `dEQP-VK.draw.*`'s regression sample is byte-identical to H5e-a's own baseline, 0 regressions. `GraphicsPipelineTest.cpp` gains `AcceptsPrimitiveRestartOnStripAndFanTopologies`/`AcceptsGeometryStageThatNeverEmits`, `PipelineTest.cpp` gains `SupportsPrimitiveRestartIdentifiesEveryStripAndFanKind`, and `ExecutorTest.cpp` gains `ExecutesDrawsAsNoOpWhenGeometryStageNeverEmits`) | H5e-a | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Transforms/CPU/GeometryWrapper.cpp` | P1 |
 |
| H5e-c | ~~**18 `dEQP-VK.geometry.layered.{1d_array,2d_array}.*` cases (`multiple_layers_per_invocation`, `render_to_one`, `render_to_default_layer`, `render_to_all`) fail at `vkQueueSubmit` with `VK_ERROR_INITIALIZATION_FAILED`**, newly exposed by H5e-a (previously masked by the `EmitVertex`/`EndPrimitive` legalization failure). Distinct from H5e-b's pipeline-creation-time failure and from the pre-existing `layered.3d` image-creation gap (`vk.createImage`, unrelated) and `layered.*.fragment_layer` fragment-input gap (`feme-cpu-wrap-fragment`, unrelated) -- this bucket is specific to a layered geometry-stage draw's own execution, using `gl_Layer` output from the geometry stage rather than just varying output-array storage. Root cause not yet isolated~~ (done, with a broader scope than this row's own "1d_array,2d_array" framing claimed: root cause is `RenderPass.cpp`'s `resolveAttachmentView`, which rejected any render-target view dimension other than 2D/2D-array outright -- Vulkan also permits `VK_IMAGE_VIEW_TYPE_1D`/`_1D_ARRAY`/`_CUBE`/`_CUBE_ARRAY` as a color attachment, and none of the four had ever been accepted, so `dEQP-VK.geometry.layered.cube.*`/`cube_array.*` hit the identical failure this row never listed, alongside the named `1d_array`/`2d_array` groups -- 18 cases total either way, just a different 18 than the row's own text enumerated (6 `1d_array` + 6 `cube` + 6 `cube_array`, not 6 `1d_array` + 12 `2d_array`; the `2d_array` cases this row expected never failed at `vkQueueSubmit` at all, since `Texture2DArray` was already accepted -- they instead already landed in H5e-e's own "Rendered images are incorrect" bucket, which is where all 18 of *this* row's cases now land too). Nothing about the gap was geometry-stage-specific: any draw into a 1D/cube(-array) render target hit it, a geometry stage's `gl_Layer` output was incidental. Fixed by accepting all four additional dimensions in `resolveAttachmentView`, addressed identically to the already-accepted 2D/2D-array case (`Image` never itself has a "1D"/"cube" dimension of its own -- only the *view's* `ImageDimension` records the addressing convention, which the existing layer-major addressing already honors with no further change). Along the way, `vkQueueSubmit`'s own error path (`Sync.cpp`) was found to discard its `llvm::Error` with a bare `consumeError` rather than routing it through the existing `feme::vulkan::logCreationFailure` opt-in diagnostic `vkCreateGraphicsPipelines` already uses -- fixed first, which is what let `FEME_VULKAN_LOG_CREATION_ERRORS=1` actually surface `resolveAttachmentView`'s own message ("only a 2D or 2D-array image view may be a render target") and confirm the root cause directly rather than by inference. A real `dEQP-VK.geometry.*` re-run (200 cases) confirms all 18 targeted cases now run to completion (0 `vkQueueSubmit` failures remain), reclassified into H5e-e's own already-tracked bucket (6 -> 24 cases); group totals are unchanged (4 Pass / 163 Fail / 33 NotSupported), 0 regressions. `dEQP-VK.draw.*`'s 1957-case sample is byte-identical to every prior row's own baseline (12/155/1790), 0 regressions. `RenderPassTest.cpp` gains `ResolveAttachmentViewAcceptsOneDArrayView`, `ResolveAttachmentViewAcceptsCubeArrayView`, and `ResolveAttachmentViewRejects3DView`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1870/1929 (59 pre-existing, unrelated `Unsupported`), up from H5e-b's own 1867/1926 by exactly the 3 new unit tests this row adds; see "Roadmap H5e-c: measured impact" in VulkanCTSReport.md for the full breakdown) | H5e-a | `feme/lib/Vulkan/RenderPass.cpp`, `feme/lib/Vulkan/Sync.cpp` | P1 |
| H5e-d | ~~**6 `dEQP-VK.geometry.emit.*_emit_0_end_1` cases fail with `feme-cpu-wrap-geometry: geometry stage wrapper requires attached feme.signature metadata`**, newly exposed by H5e-a. A geometry entry point compiled from one of these specific `emit`-count shapes (a shader that ends its primitive without emitting on that particular stream/count combination) reaches `GeometryWrapperPass` without the `feme.signature` metadata it requires to construct the wrapper -- a reflection/metadata-attachment gap somewhere upstream of `GeometryWrapperPass` itself (likely `CanonicalizeStagePass` or SPIR-V import), not `GeometryWrapperPass`'s own lowering (which is otherwise proven correct by G5's existing test suite and this row's own `EmitVertex`/`EndPrimitive` fix). Root cause not yet isolated~~ (done -- root-caused to `canonicalizeSPIRVStage` (`feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`) itself, the same layer roadmap H4g's own tessellation investigation flagged as a recurring shape: its signature-building branch was (and, for Vertex/Fragment, still is) scoped to `if (!InputGlobals.empty() || !OutputGlobals.empty())`, so a geometry entry compiled from an `emit_0_end_{1,2}` shape -- whose `emitCountA == 0` loop body (the only code that reads `gl_in[]`/writes `gl_Position`/etc.) never emits a single instruction, leaving only a bare `EndPrimitive()` call, already lowered to `feme.stage.stream.cut` by H5e-a's own `SPIRVToLLVMPatterns` fix -- discovers zero stage-IO globals and never reaches `dxil::setEntrySignature`, leaving no `!feme.signature` metadata for `GeometryWrapperPass` to find. Fixed with a new `else if (Stage == ShaderStage::Geometry)` branch attaching an explicit empty `EntrySignature` whenever the existing branch does not fire, scoped to `Geometry` only (unlike Vertex/Fragment, a geometry entry is only ever routed through `canonicalizeSPIRVStage`, never `canonicalizeDXILStage`, per `CanonicalizeStagePass::run`'s own dispatch, so there is no DXIL-origin-entry ambiguity to preserve here the way H4g's own broader attempt ran into and reverted) -- `lowerGeometryStageOps` only ever looks an `ElementID` up in `Sig` for an actual input-load/output-store/stream-emit call, none of which this shape has, so an empty signature is exactly correct, not just tolerated. Covered by a new `CanonicalizeStageTest.GeometryStreamCutOnlyEntryStillGetsASignature` unit test (a geometry entry with only a bare `feme.stage.stream.cut` call and no stage-IO globals at all, confirming it now gets an attached, empty `!feme.signature`). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1871/1930 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H5e-c's own 1870/1929 by exactly the 1 new unit test this row adds. A real `deqp-vk` run confirms the fix end to end: all 3 `dEQP-VK.geometry.emit.*_emit_0_end_1` cases now pass (up from 0/3), and a full `dEQP-VK.geometry.*` re-run (200 cases) finds exactly 6 new passes -- the 3 `_emit_0_end_1` cases this row's own text names plus the 3 `_emit_0_end_2` siblings sharing the identical `emitCountA == 0` shape (`emit_0_end_0`'s own 3 cases were already fixed by H5e-b) -- rising to 10/200 pass, 157/200 fail (down from 163), 33/200 not-supported (unchanged), 0 regressions. The `dEQP-VK.draw.*` 1957-case regression sample is byte-identical to H5e-c's own recorded totals (12/155/1790) -- 0 regressions, expected, since no `dEQP-VK.draw.*` case in that sample exercises a geometry stage at all. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a pure reflection/metadata-attachment fix, touching no feature bit or extension. See "Roadmap H5e-d: measured impact" in VulkanCTSReport.md for the full breakdown) | H5e-a | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/CPU/GeometryWrapper.cpp` | P2 |
| H5e-e | ~~**6 `dEQP-VK.geometry.layered.2d_array.*.multiple_layers_per_invocation` variants render an incorrect image** (`Fail (Rendered images are incorrect)`), newly exposed by H5e-a. Unlike H5e-b/H5e-c's outright creation/submission failures, these pipelines now run to completion without error and produce a wrong result -- a genuine rendering-correctness bug in the geometry-to-layered-render-target path (most likely `gl_Layer` routing, per-invocation output-vertex addressing, or stream-merge ordering under `Invocations > 1`, none yet isolated). Root cause not yet isolated~~ (done, with a broader scope than this row's own "6 `2d_array`" framing claimed: the actual bucket this row owns is 24 cases -- `render_to_one`, `render_to_default_layer`, `multiple_layers_per_invocation` across all 4 non-`3d` view types (`1d_array`/`2d_array`/`cube`/`cube_array`) x 2 sizes -- and it was two separate, unrelated `CommandBuffer.cpp` bugs, neither one actually `gl_Layer` routing/per-invocation addressing/stream-merge ordering as this row's own text suspected. (1) `applyLoadOps`'s `ViewMask` fallback for a non-multiview attachment (`Binding.ViewMask == 0`) collapsed to a single bit (`1u`) regardless of the attachment's real layer count, so `LOAD_OP_CLEAR` on a layered (non-multiview) attachment only ever cleared layer 0, leaving every other layer's backing memory uninitialized garbage -- fixed with a new `fullLayerMask(Layers)` helper. (2) `runDraw`'s per-view loop unconditionally called `sliceAttachmentLayer(A, ViewIndex)` on every attachment for every draw, including the ordinary non-multiview case (`ViewIndex` always 0), permanently slicing every attachment down to exactly 1 layer before `Executor.cpp`'s own already-correct per-primitive `gl_Layer`/`resolveRenderTargetArrayLayer` routing ever got a chance to see more than that one layer -- any primitive routed to a non-zero layer was silently discarded as out of range. Fixed by only performing that per-view slicing when `Gfx.Binding.ViewMask != 0` (true multiview); a plain layered (non-multiview) draw now sees its attachments' full, unsliced layer range, exactly as `Executor.cpp` already expected. New `DrawTest.GeometryStageLayerOutputRoutesToANonMultiviewLayer` (Vulkan unit test), covering both fixes at once: a real `EmitVertex`/`EndPrimitive`-driven geometry shader routing `gl_Layer = 1` into a two-layer, non-multiview render target, confirming layer 0 reads back `LOAD_OP_CLEAR`'s own black (not garbage) and layer 1 reads back the fragment stage's red (not discarded). Confirmed to fail without either fix individually. A real `dEQP-VK.geometry.layered.*` re-run (100 cases) goes from 0/100 Pass (H5e-c's own baseline) to 24/100 Pass -- exactly this bucket's own 24 target cases, 0 regressions, `3d`'s pre-existing unrelated `vkCreateImage` gap untouched. A real `dEQP-VK.geometry.*` re-run (200 cases) goes from 10/200 (H5e-d's own baseline) to 34/200 Pass, 0 regressions. `dEQP-VK.draw.*`'s 1957-case sample goes from 12/1957 to 14/1957 Pass (2 new: `shader_layer.vertex_shader_5`'s `renderpass`/`partial_secondary_cmd_buff` variants, which route a vertex-shader-written `gl_Layer` the same way, confirming fix (2) is not geometry-stage-specific), 0 regressions. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1872/1931 (59 pre-existing, unrelated `Unsupported`), up from H5e-d's own 1871/1930 by exactly the 1 new unit test this row adds; see "Roadmap H5e-e: measured impact" in VulkanCTSReport.md for the full breakdown) | H5e-a | `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H5h | ~~**`vkQueueSubmit` heap corruption / segfault in a real `dEQP-VK.geometry.basic.*` case** (`StageStorage::writeRaw` writing past the end of a zero-size `Data` buffer, via `StageLink.cpp`'s `copyLinkedElements`), found only by actually re-running the full `dEQP-VK.geometry.*` mustpass sweep to confirm H5's own closing condition -- no single-case CTS sample from any of H5a-H5e-e's own narrower re-runs happened to exercise the crashing shape. Root cause: `StageStorage.cpp`'s `buildStageStorage` skips allocating storage for any `SystemValue`-tagged *input* element (assuming the compiled wrapper always sources it from a fixed per-invocation record field, e.g. `SV_VertexID`/`gl_FragCoord`), but a geometry entry's own `gl_in[]`-shaped inputs (`gl_Position`/`gl_PointSize`/etc., except `PrimitiveID`/`InvocationID`) are always read through real, dynamically-indexed storage by `GeometryWrapper.cpp`'s `lowerGeometryInputLoad` -- the first and only stage where a per-element-array-indexed system-value input matters, since Hull/Domain's own `PatchInput`/`PatchOutput` direction never triggers the buggy skip-check's `Direction == SignatureDirection::Input` guard, and Fragment's own `SystemValue::Position` (`gl_FragCoord`) really is invocation-record-sourced, unlike Geometry's~~ (done: new `bool AllInputSystemValuesAreStorageBacked` parameter on `buildStageStorage` (`StageStorage.h`/`.cpp`), gating a new `IsGeometryInputVertexArrayMember` carve-out alongside the existing `IsInterpolatedFragmentInput` one (roadmap H7x); the one geometry-input call site in `Executor.cpp`'s `executeDraws` now passes `true`, keeping every other call site's existing default-`false` behavior unchanged. Also added permanent bounds-check `assert`s to `StageStorage::readRaw`/`writeRaw` (zero-cost outside an assertions build) that would have caught this immediately instead of needing manual `fprintf` instrumentation to diagnose. New `StageLinkTest.cpp` cases cover both the default-skips and flag-allocates behavior for a `Position` system-value input, that `PrimitiveID`/`InvocationID` are still skipped even with the flag set, and an end-to-end `copyLinkedElements` regression test reproducing the exact crashing shape (a vertex-stage `Position` output linked into a geometry-shaped `Position` input). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2299/2326 (27 pre-existing, unrelated `Unsupported`, 0 `Failed`). A real per-case-isolated `dEQP-VK.geometry.*` sweep (200 cases, run one subprocess per case so a genuine crash cannot truncate the remainder, since a single-process sweep silently drops every case after the first crash) goes from 72 `Pass`/93 `Fail`/11 `NotSupported`/24 `CRASH` before this fix to 85 `Pass`/104 `Fail`/11 `NotSupported`/0 `CRASH` after -- 0 regressions (every previously-passing case still passes), 13 of the 24 former crashes now pass, the other 11 become ordinary, unrelated, pre-existing-shape `Fail`s (real rendering-correctness bugs out of this row's own scope). See "Roadmap H5h: measured impact" in VulkanCTSReport.md for the full reproduction and numbers~~ | H5a-H5e-e | `feme/lib/Graphics/StageStorage.cpp`, `feme/include/feme/Graphics/StageStorage.h`, `feme/lib/Graphics/Executor.cpp` | P1 |
| H6 | **Mesh and task (amplification) shading**, `VK_EXT_mesh_shader`: G6's stage model plus the Vulkan-side pipeline/draw entry points (`vkCmdDrawMeshTasks*`), bounded payload/output limits reported truthfully, and the mesh path through the same prepared-draw code. Whole `dEQP-VK.mesh_shader` group (partially done, broken down below the same way H4/H5 were: H6a closes the execution-mode half of "reflect a mesh entry point at all"; H6b-H6i track the remaining canonicalization, CPU-lowering, executor-chaining, Vulkan-API-acceptance and CTS-triage work an investigation mirroring H5's own found necessary before landing; H6g's own CTS triage split further into H6g-a (closed, folded into H8) and H6g-b (closed: its own literal "re-run and confirm" ask is done, and it found and fixed a real, distinct bug of its own -- a mesh pipeline's spec-*ignored* `pVertexInputState`/`pInputAssemblyState` was wrongly rejected outright -- but the group's own re-run this uncovered found the 235/33-case bucket does *not* fully clear yet, still blocked on three further, newly-isolated gaps of its own, tracked as H6g-b-a (closed: root-caused and fixed an upstream MLIR SPIR-V-dialect deserialization/serialization gap, `PerPrimitiveEXT` decoration unhandled, which had been the dominant single cause -- fixing it lets 202 previously-blocked cases progress further, uncovering a new dominant blocker in their place, tracked as H6g-b-a-i (closed: root-caused and fixed a Vulkan array-stride correctness bug in the upstream MLIR SPIR-V dialect's own layout utilities and SPIRVToLLVM conversion, unrelated to `PerPrimitiveEXT` specifically -- fixing it lets the 80 previously-blocked `spirv.AccessChain` cases progress further, uncovering a new dominant blocker in their place, tracked as H6g-b-a-i-a (closed: root-caused and fixed a missing `ConvertSPIRVToLLVMPass` conversion pattern for `spirv.All`/`spirv.Any` -- fixing it lets the 81 previously-blocked `spirv.All` cases progress further, uncovering a new dominant blocker in their place, tracked as H6g-b-a-i-a-i (closed: root-caused and fixed a `feme`-local `SPIRVResourceLoweringPass` scope gap for direct struct-typed `StorageBuffer` handles that `ConvertSPIRVToLLVMPass` had already lowered into `llvm.spv.resource.getpointer` + ordinary LLVM `getelementptr` field/array accesses -- the pass only accepted flat direct load/store users before, so whole functions containing glslang's `readonly buffer { ... }` field selections were left untouched and later rejected by `UnsupportedOps`; fixing that lets 81 of the 82 formerly-blocked cases progress further, leaving only a pre-existing sampled-image/sampler combined-handle rejection and surfacing a new dominant blocker in their place, tracked as H6g-b-a-i-a-i-a (closed: root-caused and fixed a `feme.cpu.masked.store.*` divergent-vector-value consumer gap in `checkVectorDecompositionSupported`/`widenMaskedStore` -- fixing it lets 68 of the 148 formerly-blocked cases progress further, incidentally also closes H6g-b-b (the same code path, mischaracterized there as an "aggregate" masked-store value), and surfaces a new dominant blocker in their place, tracked as H6g-b-a-i-a-i-b, a divergent vector value used as an `fcmp`/`icmp` comparison operand))), and H6g-b-c (closed: wired `ShaderStage::Mesh` into `ValidateStagePass::run`, so this unresolved arrayed-builtin-block access -- left unrewritten by `H6c-a-a-iii`'s own fix, previously reaching the JIT as a genuinely undefined symbol -- is now diagnosed cleanly at compile time instead; its own re-run of `dEQP-VK.mesh_shader.ext.builtin.*` surfaces one further, narrower blocker of its own, tracked as H6l) -- H6g-b-a-i-a-i-c's own re-run of that same bucket found its 80 previously-JIT-symbol-blocked cases split evenly between the already-tracked H6g-b-c and a new `MeshOutputWrapperPass` catch-all rejection, tracked (one level under H6 rather than nested any deeper under H6g-b, per the standing instruction against nesting milestone IDs more than one lowercase letter deep going forward) as H6g-b-d (closed: root-caused and fixed a `lowerMeshStageOps` bug of its own -- its catch-all rejected *any* leftover call that was not itself one of the two shapes it lowers, including completely unrelated, perfectly ordinary calls a mesh entry legitimately still makes (e.g. its own `feme.cpu.resource.load.raw.*` buffer read feeding an output store's value), not only a genuinely-unlowered `feme.stage.*` op -- fixing it lets the 40 previously-blocked cases progress further, uncovering a new `vkQueueSubmit` vertex-output/fragment-input interface-mismatch bug in their place, tracked as H6j (closed: root-caused and fixed a `CanonicalizeStage.cpp` signature-reflection gap of its own -- a mesh entry's own plain per-vertex output global folded its outer per-vertex array dimension into `RowCount` instead of peeling it off, disagreeing with the fragment stage's own unarrayed input at the same location -- fixing it lets those 40 cases progress further, uncovering a new `SIGSEGV`/`SIGABRT` heap-corruption crash inside `executeDraws` itself in 32 of them in their place, not yet root-caused, tracked as H6k)) -- milestone remains open, depending on H6g-b-a-i-a-i-b, H6l and H6k) | G6 | `feme/lib/Vulkan/{GraphicsPipeline,CommandBuffer,PhysicalDeviceInfo}.cpp`, `feme/lib/Graphics` | P2 |
| H6a | ~~**No SPIR-V mesh or task entry point's declared shape (output topology, maximum emitted vertex/primitive count) is captured at all**, so nothing downstream -- not even a first canonicalization pass -- has anything to read (mirrors H5a's own opening move for geometry). `ConvertSPIRVToLLVMPass` had no handling for `MeshEXT`/`TaskEXT` execution modes at all, even though `getStageForExecutionModel` already mapped the two execution models onto `ShaderStage::Mesh`/`ShaderStage::Amplification`~~ (done: new `feme::graphics::MeshState`/`Mesh.h` (mirroring `GeometryState`/`Geometry.h`); `ConvertSPIRVToLLVMPass::collectEntryPoints` now captures a mesh entry point's output topology (`OutputPoints`/`OutputLinesEXT`/`OutputTrianglesEXT`), maximum emitted vertex count (`OutputVertices`) and maximum emitted primitive count (`OutputPrimitivesEXT`) into `feme.mesh.*` passthrough attributes, disambiguating the two SPIR-V enumerant values mesh shares with geometry/tessellation (`OutputPoints`, also a geometry entry's own point-output mode; `OutputVertices`, also a hull entry's output control point count and a geometry entry's own maximum emitted vertex count) by the declaring entry point's own `Stage`, exactly as H5a already disambiguates `Triangles`/`OutputVertices` between tessellation and geometry. A task entry point's workgroup size is captured the same way a compute entry point's already is (the existing `hlsl.numthreads` attribute); it declares no output shape of its own, so it gets no `feme.mesh.*` attributes. `CanonicalizeStagePass::run` does not yet accept `ShaderStage::Mesh`/`ShaderStage::Amplification` -- that, and the payload/mesh-output canonicalization it needs, is H6b. `unittests/Graphics/MeshTest.cpp` (6 cases, mirroring `GeometryTest.cpp`) and a new `spirv-to-llvm-mesh-execution-modes.mlir` lit test (4 `RUN`/`CHECK` blocks, including the `OutputPoints`/`OutputVertices` disambiguation case and the task-entry no-attributes case) cover today's scope; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1879/1938 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H5e-e's own 1872/1931 baseline by exactly the 7 new tests this row adds. `dEQP-VK.mesh_shader.*` (28044 cases, both `VK_EXT_mesh_shader` and legacy `VK_NV_mesh_shader` sub-groups) stays 0/0/28044 -- correctly so, the same shape H5a's own report entry recorded for geometry -- and the `dEQP-VK.draw.*` 1957-case regression sample stays byte-identical to H5e-e's own recorded totals (14/153/1790), confirming 0 regressions. See "Roadmap H6a: measured impact" in VulkanCTSReport.md for the full reproduction~~ | G6 | `feme/include/feme/Graphics/Mesh.h`, `feme/lib/Graphics/Mesh.cpp`, `feme/lib/Conversion/SPIRVToLLVM/ConvertSPIRVToLLVMPass.cpp` | P2 |
| H6b | ~~**Canonicalize a mesh entry's bounded per-vertex/per-primitive output-array writes** (SPIR-V's `PerVertexEXT`/`PerPrimitiveEXT`-decorated `Output` storage-class arrays, address space 8) into the existing `feme.stage.output.store` `Vertex` operand, mirroring how H5b closed geometry's own per-vertex dynamic-index gap for `Input` reads -- investigated first (mirroring H5a) rather than assumed a mechanical repeat, and a real blocker was found: unlike `Input`, `Output` storage already has a legitimate, tested, production *constant*-per-row `getelementptr` access pattern for ordinary matrix outputs (`RewritesSPIRVArrayOutputStorePerElementByteOffset`), so H5f's "fold a constant vertex index into `Vertex`" extension cannot be safely mirrored onto `Output` (doing so regressed that very test); only a genuinely *non-constant* `Output`-array index is safe to route through `Vertex` unconditionally, so `isPerVertexArrayInputGlobal`/`RowCountIsVertexArray`/the constant-index-fold path in `resolveStageIOAccess` all stay `Input`-only (address space 7), and a new, narrower `isDynamicIndexedArrayGlobal` helper (address space 7 *or* 8) is used only by `getDynamicVertexIndexedAccess`'s non-constant-index recognition. Also found and fixed along the way: the store-rewrite path in `canonicalizeSPIRVStage` never threaded `StageIOAccess::Vertex` through to `storeStageIOBlockValue` at all (it always passed a constant `Zero`), unlike the load path's own `Access->Vertex ? Access->Vertex : Zero` -- store-side dynamic vertex/primitive indexing was unreachable dead code until this row's own generalization exercised it for the first time. `ValidateStage.cpp`'s `validateVertex` now also accepts `ShaderStage::Mesh` for a non-constant `Vertex` operand (currently unreachable, `ValidateStagePass::run` does not validate Mesh yet, mirroring how Geometry's own H5b addition was unreachable until later). **Left open, tracked as H6h/H6i**: a task entry's bounded payload write (`TaskPayloadWorkgroupEXT` storage class) is *not* addressed here -- investigation found `TaskPayloadWorkgroupEXT` (SPIR-V enum 5402) has no address-space mapping at all in LLVM's own SPIR-V backend (`storageClassToAddressSpace` hits its `report_fatal_error` default case), a SPIRVToLLVM-conversion-layer gap outside this file's scope, needing a new address-space convention and global-variable pattern mirroring `WorkgroupGlobalVariablePattern`/`PushConstantGlobalVariablePattern` before a payload variable can even be imported as an LLVM global (H6h); and `CanonicalizeStagePass::run`'s stage filter itself is *not* yet lifted to accept `ShaderStage::Mesh`/`ShaderStage::Amplification` (H6i), mirroring how H5b built the per-vertex machinery before H5c actually flipped geometry's own filter. See "Roadmap H6: what H6b found, and why it stops here" in VulkanCTSReport.md~~ | H6a | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H6c | ~~**Reuse the compute workgroup/groupshared/barrier/wave lowering for task and mesh entries**, adding a bounded mesh-output builder (mirroring `feme::graphics::GeometryStreamBuilder`, but structure-of-arrays per-vertex/per-primitive rather than stream-ordered) and a bounded task payload builder, plus `FemeMeshArgs`/`FemeTaskArgs`/`CompiledStage::invokeMesh`/`invokeTask` (mirroring `FemeGeometryArgs`/`CompiledStage::invokeGeometry`, H5d)~~ (done, scoped down after investigation found the roadmap's own listed dependency, H6i, is not actually required for this row's literal content: new `feme::graphics::MeshOutputBuilder`/`MeshOutput.h` -- a bounded structure-of-arrays per-vertex/per-primitive output store, unlike `GeometryStreamBuilder`'s stream-ordered one, since mesh output is random-access-addressable by `SetMeshOutputsEXT`'s declared counts rather than `emit`/`cut`-ordered -- and `feme::graphics::TaskPayloadBuilder`/`TaskPayload.h`, a bounded byte-buffer payload store (7 and 5 new unit tests respectively, `MeshOutputTest.cpp`/`TaskPayloadTest.cpp`). `FemeMeshArgs`/`FemeTaskArgs` (`RuntimeABI.h`) deliberately share `FemeDispatchArgs`'s exact leading field layout (`Resources`, `GroupID`, `GroupCount`, `GroupShared`), which lets `feme::cpu::EntryWrapperPass` -- the existing compute group-loop/groupshared-allocation/barrier-region-splitting wrapper -- run completely unmodified on a mesh- or task-tagged module (confirmed it only ever reads that shared prefix via struct-GEP); `Pipeline.cpp`'s stage-wrapper-selection switch now routes `ShaderStage::Mesh`/`Amplification` through that same, unmodified `EntryWrapperPass`, which is this row's literal "reuse the compute lowering" ask. `MeshResources`/`TaskResources` and `PreparedMeshBatch`/`PreparedTaskBatch` (mirroring `GeometryResources`/`PreparedGeometryBatch`) and `CompiledStage::invokeMesh`/`invokeTask` (mirroring `invokeGeometry`) round out the ABI plumbing. New `CompiledStageTest.cpp` cases hand-build mesh- and task-tagged IR exercising `GroupID`, groupshared storage and a barrier round-trip end to end through the reused wrapper, plus stage-mismatch rejection tests. **What this row does not include**, tracked as H6c-a below: wiring `MeshOutputBuilder`/`TaskPayloadBuilder` to actual `feme.stage.*` mesh-output-store/task-payload-store/`SetMeshOutputsEXT`/`EmitMeshTasksEXT` operations reaching this pipeline -- that traffic does not exist yet (blocked on H6d for the ops themselves and H6h/H6i for task payload's SPIR-V import specifically), so today's mesh/task entries can only exercise ordinary resource reads plus the reused group/groupshared/barrier cooperation, not a real output/payload round-trip. `ninja check-feme` (assertions-enabled, ccache build): 1897/1956 passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6b's own 1881/1940 baseline by exactly the 16 new tests this row adds (`MeshOutputTest.cpp`'s 7, `TaskPayloadTest.cpp`'s 5, `CompiledStageTest.cpp`'s 4 new mesh/task cases). `dEQP-VK.mesh_shader.*` stays 0/0/28044 and the `dEQP-VK.draw.*` 1957-case regression sample stays byte-identical to H6b's own recorded totals -- neither is exercised by this row, which only changes the CPU-target wrapper/ABI layer, not anything Vulkan-facing. See "Roadmap H6c: measured impact" in VulkanCTSReport.md | H6i | `feme/lib/Transforms/CPU`, `feme/lib/Target/CPU`, `feme/lib/Graphics` | P2 |
| H6c-a | ~~**Wire `MeshOutputBuilder`/`TaskPayloadBuilder` into real `feme.stage.*` mesh-output-store/task-payload-store operations** reaching the reused `EntryWrapperPass` path, once those ops exist (H6d) and, for task payload specifically, once it can be imported from SPIR-V `TaskPayloadWorkgroupEXT` at all (H6h) and canonicalized (H6i) (investigated, not landed: direct inspection of `feme/include/feme/Core/StageOps.h`'s `StageOpKind` enum, `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, and `CanonicalizeStagePass::run`'s stage filter (`feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`) confirms all three named prerequisites -- H6d, H6h, H6i -- are still completely unimplemented: no mesh-output-store/task-payload-store `feme.stage.*` op exists, `TaskPayloadWorkgroupEXT` still has no address-space convention or import pattern, and the canonicalization stage filter still does not accept `ShaderStage::Mesh`/`Amplification`. Unlike H6b's own investigation, which found a real, independently-landable fix inside its own stated scope, this row has zero such content: there is nothing yet producing a canonicalized mesh-output-store/task-payload-store operation for `MeshOutputBuilder`/`TaskPayloadBuilder` to be wired to. Split below into two independently-trackable rows, since mesh output and task payload do not actually share the same blocker set -- mesh output's own canonicalization already exists (H6b's `feme.stage.output.store` `Vertex` operand), so wiring it only needs H6d (meshlet assembly to consume it) and H6i (lifting the stage filter so a mesh entry is canonicalized at all, not H6h, which is task-payload-import-specific); task payload additionally needs H6h before H6i can canonicalize a payload write into anything. See "Roadmap H6c-a: why this row could not land" in VulkanCTSReport.md)~~ (closed by its own split: this row's own text carried no independently-landable content, only a confirmed diagnosis and a split into H6c-a-a/H6c-a-b -- and all three named prerequisites (H6d, H6h, H6i) plus both split rows have since landed, so re-checking this row's own literal ask directly rather than assuming so: `MeshOutputBuilder` is wired into a mesh entry's canonicalized `feme.stage.output.store` `Vertex`-operand writes via `MeshOutputWrapperPass` (H6c-a-a), and `TaskPayloadBuilder` is wired into a task entry's canonicalized `feme.stage.task.payload.store` via `TaskPayloadWrapperPass` (H6c-a-b), both reaching the reused `EntryWrapperPass` path exactly as asked. No further source change is needed from this row itself -- everything it names was already implemented under its own split children. This does **not** mean `dEQP-VK.mesh_shader.*` runs correctly end to end yet: a real mesh workgroup's assembled meshlet still hits two further, separately-tracked gaps (H6g-b-c's unresolved arrayed-builtin-block access, H6g-b-d's `feme-cpu-wrap-mesh-output` catch-all rejection) outside this row's own "wire the builders into the ops" scope. See "Roadmap H6c-a: closed by its own split" in VulkanCTSReport.md | H6d, H6h, H6i | `feme/lib/Transforms/CPU`, `feme/lib/Graphics` | P2 |
| H6c-a-a | ~~**Wire `MeshOutputBuilder` into a mesh entry's canonicalized `feme.stage.output.store` `Vertex`-operand writes (H6b)** reaching `EntryWrapperPass`/`CompiledStage::invokeMesh`'s reused path, once `CanonicalizeStagePass::run`'s stage filter accepts `ShaderStage::Mesh` (H6i) and a mesh workgroup's assembled meshlet (H6d) gives the wiring somewhere real to write into -- does not need H6h, since mesh output never touches `TaskPayloadWorkgroupEXT`~~ (done: new `feme::cpu::MeshOutputWrapperPass` (`MeshOutputWrapper.h`/`.cpp`) runs immediately before `EntryWrapperPass` in `Pipeline.cpp`'s now-split `ShaderStage::Mesh` case (split out of the combined `Amplification`/`Mesh` case, since amplification's own task-payload wiring stays H6c-a-b's job); it appends six new named wave-body parameters mirroring `FemeMeshArgs`'s output-array fields (`mesh_vertex_output_layout`, `mesh_vertex_outputs`, `mesh_primitive_output_layout`, `mesh_primitive_outputs`, `mesh_max_output_vertices`, `mesh_max_output_primitives`) and lowers every masked output store by branching on the referenced element's `SignatureFrequency` (`PerVertex` addresses `VertexOutputs`, `PerPrimitive` addresses `PrimitiveOutputs`), defensively clamping the dynamic `Vertex` operand into `[0, Max)` via `llvm::Intrinsic::umin` since `SetMeshOutputsEXT` remains unwired (no bounds-checked heap lookup exists for a raw output-array write the way one does for a resource-heap index, mirroring `SPIRVResourceLowering.cpp`'s own `computeClampedIndex` precedent). New `MeshArgsField`/`getMeshArgsType` (`StageArgsLayout.h`) mirror `FemeMeshArgs`'s exact C layout; `EntryWrapperPass` itself (`EntryWrapper.cpp`) gained an `IsMesh`-gated `ArgsTy`/`WrapperEnv` extension (all three wrapper-building call sites -- `buildWrapper`, `buildWrapperForLoop`, `buildWrapperForBranch` -- updated) so it reads the new fields out of `getMeshArgsType`'s longer struct instead of `getDispatchArgsType`'s shorter one, reusing their shared `Resources`/`GroupID`/`GroupCount`/`GroupShared` prefix exactly as H6c's own trick already relied on. New `MeshOutputWrapperTest.cpp` (4 cases: per-vertex lowering, per-primitive lowering into the other array, params-always-appended, and chaining into `EntryWrapperPass`) plus one new `CompiledStageTest.cpp` case, `InvokeMeshWritesPerVertexOutputStore`, proving a canonicalized-shaped mesh entry compiled through the real `CompiledStage::create` pipeline writes real per-group data into `FemeMeshArgs::VertexOutputs` through `CompiledStage::invokeMesh` end to end. `ninja check-feme` (assertions-enabled, ccache build): 1939/1998 passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6i's own 1934/1993 baseline by exactly these 5 new tests. `dEQP-VK.mesh_shader.*` and the `dEQP-VK.draw.*` 1957-case regression sample both stay byte-identical to H6i/H6g's own recorded totals -- **expected and confirmed**, since this row's own closing re-run discovered two further gaps (split into new rows H6c-a-a-i, H6c-a-a-ii below) that still leave every real mesh workgroup assembling an empty meshlet regardless of this row's own output-store wiring. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H6c-a-a: measured impact" in VulkanCTSReport.md) | H6d, H6i | `feme/lib/Transforms/CPU`, `feme/lib/Graphics` | P2 |
| H6c-a-b | ~~**Wire `TaskPayloadBuilder` into a task entry's canonicalized payload-store operation**, once `TaskPayloadWorkgroupEXT` has an address-space convention and import pattern (H6h), `CanonicalizeStagePass::run` accepts `ShaderStage::Amplification` and canonicalizes a payload write into a new `feme.stage.*` op (H6i), and a task entry's checked `EmitMeshTasksEXT` dispatch queue (H6d) gives the payload somewhere real to be read from by the mesh workgroups it dispatches~~ (done: new `feme::cpu::TaskPayloadWrapperPass` (`TaskPayloadWrapper.h`/`.cpp`), `MeshOutputWrapperPass`'s deliberately simpler task-stage counterpart, runs immediately before `EntryWrapperPass` in `Pipeline.cpp`'s `ShaderStage::Amplification` case; it appends two new named wave-body parameters (`task_payload`, `task_max_payload_bytes`) and lowers every masked payload store into a store at `task_payload + offset` -- since a task payload store's `offset` operand is always the same compile-time constant for every lane at one call site (unlike a mesh output store's dynamic `Vertex` operand), every lane targets one shared address, needing only the same load-select-store pattern `MeshOutputWrapper.cpp` already uses and no `FemeStageLayout`/element-ID lookup at all; a defensive `Offset + ByteSize <= MaxPayloadBytes` bounds check is ANDed into each lane's mask, mirroring `MeshOutputWrapper.cpp`'s own clamping precedent. `StageMaskCalls`/`Linearize`/`SIMDize` gained the matching masked-call plumbing (`createMaskedTaskPayloadStore`, a `TaskPayloadStore` case in `applyStageMasks`/`hasStageMaskOps`, `widenMaskedTaskPayloadStore`); new `TaskArgsField`/`getTaskArgsType` (`StageArgsLayout.h`) mirror `FemeTaskArgs`'s exact C layout; `EntryWrapperPass` gained an `IsTask`-gated `ArgsTy`/`WrapperEnv` extension mirroring H6c-a-a's own `IsMesh` one exactly (all three wrapper-building call sites updated, both flags threaded through consistently, never both true at once). Host side: `TaskPayloadBuilder` gained a `getMutableBytes()` accessor (the only way its storage picks up a real compiled write); `GraphicsPipeline::setMeshStage` gained a `MaxTaskPayloadBytes` parameter populated from a new `feme::vulkan::MaxTaskPayloadBytes` constant (`GraphicsPipeline.h`, replacing `EntryPoints.cpp`'s previously-hardcoded `maxTaskPayloadSize` literal so the two can never disagree); `Executor::executeDraws`'s task-dispatch loop now constructs one `TaskPayloadBuilder` per task workgroup, backs `TaskResources::Payload` with its mutable bytes before `invokeTask`, and threads the same bytes into every mesh workgroup its `EmitMeshTasksEXT` request dispatches, replacing the two hardcoded `/*Payload=*/{}` call sites H6c-a-a's own row left in place. New `TaskPayloadWrapperTest.cpp` (4 cases: single-store lowering, params-always-appended, two stores at distinct offsets, chaining into `EntryWrapperPass`) plus one new `CompiledStageTest.cpp` case, `InvokeTaskWritesPayloadStore`, proving a canonicalized-shaped task entry compiled through the real `CompiledStage::create` pipeline writes a real value into `FemeTaskArgs::Payload` through `CompiledStage::invokeTask` end to end. `ninja check-feme` (assertions-enabled, ccache build): 1944/2003 passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6c-a-a's own 1939/1998 baseline by exactly these 5 new tests. `dEQP-VK.mesh_shader.*` and the `dEQP-VK.draw.*` 1957-case regression sample both stay byte-identical to H6c-a-a's own recorded totals -- **expected and confirmed**, since `H6c-a-a-i`/`H6c-a-a-ii` remain open and no mesh-side `TaskPayloadWorkgroupEXT` *load* is canonicalized yet either (out of this row's own "somewhere real to be read from" scope). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H6c-a-b: measured impact" in VulkanCTSReport.md) | H6d, H6h, H6i | `feme/lib/Transforms/CPU`, `feme/lib/Graphics` | P2 |
| H6d | ~~**Implement checked amplification dispatch queues and meshlet assembly**: a task entry's `EmitMeshTasksEXT` becomes a bounded, validated dispatch of mesh workgroups (mirroring the compute dispatch queue's own bounds checks), and a mesh workgroup's emitted per-vertex/per-primitive outputs become one meshlet the executor can consume, with topology validation (an out-of-range vertex index in a primitive's index list is diagnosed, not read out of bounds)~~ (done: new `feme::graphics::AmplificationDispatchQueue`/`AmplificationDispatch.h` -- `AmplificationDispatchQueue::create` validates a requested `EmitMeshTasksEXT(x, y, z)` group count against a per-dimension bound (`AmplificationDispatchLimits::MaxGroupCount`, mirroring `maxComputeWorkGroupCount`'s own per-dimension check in `feme/lib/Vulkan/CommandBuffer.cpp`'s `validateGroupCount`) and a total-product bound (`MaxTotalGroupCount`, mesh shading's own `maxMeshWorkGroupTotalCount`, which compute's own dispatch has no equivalent of), computing the product in 64 bits so an over-large per-dimension request cannot wrap back into range; a validated queue then enumerates each dispatched mesh workgroup's group ID (`getGroupID`, X-fastest/Z-slowest, mirroring `feme::cpu::runDispatch`'s own triple loop) rather than eagerly invoking anything, since `Executor::executeDraws` (H6e) needs to interleave that enumeration with per-workgroup mesh state. New `feme::graphics::Meshlet`/`Meshlet.h` and `assembleMeshlet` (`MeshOutputBuilder` -> `Meshlet`) trim a mesh workgroup's completed `MeshOutputBuilder` down to its actual (`SetMeshOutputsEXT`-declared) vertex/primitive counts and *re-validate* every primitive's vertex index list against that actual vertex count at assembly time, independently of `MeshOutputBuilder::setPrimitiveIndices`'s own write-time check (H6c) -- necessary because a real compiled workgroup's output, once wired directly into the builder's storage (H6c-a-a), will not necessarily have gone through that checked setter, so an out-of-range vertex index must still be diagnosed (an `llvm::Error` naming the offending primitive and index) rather than read out of bounds when the executor later walks it. 9 new unit tests (`AmplificationDispatchTest.cpp`'s 6, `MeshletTest.cpp`'s 3) cover today's scope. `ninja check-feme` (assertions-enabled, ccache build) passes in full; `dEQP-VK.mesh_shader.*` stays 0/0/28044 (correctly so: nothing here touches SPIR-V import, canonicalization, or any Vulkan-facing path) and the `dEQP-VK.draw.*` 1957-case regression sample stays byte-identical to H6c's own recorded totals. See "Roadmap H6d: measured impact" in VulkanCTSReport.md | H6c | `feme/lib/Graphics` | P2 |
| H6e | ~~**Chain the mesh path into `Executor::executeDraws`**, feeding assembled meshlets into the same clipping/rasterization path vertex/geometry primitives already use (mirroring H5d's own geometry-chaining work), with a task entry point's dispatch driving which mesh workgroups run when one is bound~~ (done: extracted the shared clip/viewport/cull/tile-bin/fragment-shade tail of `executeDraws`'s per-`DrawCommand` loop into a new `RasterizePrimitives` lambda (`(RasterOutRef, AbsTriIndices, AbsLineIndices, RasterClass) -> Error`), mirroring H5d's own geometry-chaining precedent, so the mesh path can feed into it without duplicating that tail; new `GraphicsPipeline::setMeshStage`/`hasMeshStages`/`getMeshStage`/`hasTaskStage`/`getTaskStage`/`getMeshState` (`Pipeline.h`/`.cpp`, mirroring `setGeometryStage`) require a non-null mesh stage but allow a null task stage (direct mesh dispatch, no amplification); new `MeshDrawCommand`/`PreparedDraw::MeshDraws` (`PreparedDraw.h`) carry either a direct mesh-dispatch shape or a task-dispatch shape's `GroupCount`. `Executor::executeDraws` itself: gates the vertex-topology validation switch and the unconditional vertex-stage signature fetch behind `!Pipeline.hasMeshStages()`; validates `Draw.Draws`/`Draw.MeshDraws` are mutually exclusive; derives `RasterClass` from `Mesh.OutputTopology` when mesh stages are bound; and adds a dispatch-and-rasterize branch that, per `MeshDrawCommand`, either enumerates every mesh workgroup directly (no task stage bound) or first runs each task workgroup (X-fastest order, mirroring `feme::cpu::runDispatch`) to read back `FemeTaskArgs::MeshGroupCount` and build a per-task-workgroup `AmplificationDispatchQueue` from it (task stage bound) -- either way, every enumerated mesh workgroup is invoked via `CompiledStage::invokeMesh`, its raw output flattened into a `MeshOutputBuilder` and trimmed to a `Meshlet` via `assembleMeshlet` (H6d), and all resulting meshlets (in dispatch order) merged into one `StageStorage` plus absolute triangle/line index lists fed to `RasterizePrimitives`. Two deliberate, documented simplifications carried by this row alone (not real ABI-matching work, since nothing yet writes real mesh/task output -- see below): a self-consistent (not Vulkan-spec-mandated) `flattenMeshRow`/`unflattenMeshRow` scheme converting between raw `StageStorage` and `MeshOutputBuilder`/`Meshlet` rows in `MeshSig`'s declared Output order; and a placeholder `AmplificationDispatchLimits` (`MaxGroupCount={65535,65535,65535}`, `MaxTotalGroupCount=4194304`) standing in for H6f's own real `VkPhysicalDeviceMeshShaderPropertiesEXT`-sourced bounds. New `PipelineTest.cpp` cases (`SetMeshStageRecordsTheMeshAndTaskStagesAndState`, `SetMeshStageAllowsAnOmittedTaskStage`) and new `ExecutorTest.cpp` end-to-end cases (`RunsEveryMeshWorkgroupDirectlyWhenNoTaskStageIsBoundAndRastersNothing`, `TaskStageDispatchDrivesWhichMeshWorkgroupsRunAndNoneRunUntilItRequestsAny`) hand-build compiled mesh/task IR that writes a doubled `GroupID.x` into a bound UAV buffer via groupshared+barrier cooperation (mirroring `CompiledStageTest.cpp`'s own precedent) to prove dispatch/`GroupID` correctness end-to-end through `executeDraws`, while asserting the color attachment stays untouched -- since no compiled mesh/task entry point can yet write `FemeMeshArgs::ActualVertexCount`/`VertexOutputs`/`FemeTaskArgs::MeshGroupCount` from real IR (blocked on H6h's `TaskPayloadWorkgroupEXT` lowering and H6i's `CanonicalizeStagePass` stage-filter lift for `Mesh`), every meshlet this row's chain assembles is legitimately empty at runtime, the same "correctly wired but produces nothing yet" shape as H5d's own `ExecutesDrawsAsNoOpWhenGeometryStageNeverEmits`. `ninja check-feme` (assertions-enabled, ccache build): 1910/1969 passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6d's own 1906/1965 baseline by exactly the 4 new tests this row adds. `dEQP-VK.mesh_shader.*` stays 0/0/28044 and the `dEQP-VK.draw.*` 1957-case regression sample stays byte-identical to H6d's own recorded totals -- neither is exercised by this row, which changes no Vulkan-facing file. See "Roadmap H6e: measured impact" in VulkanCTSReport.md | H6d | `feme/lib/Graphics/Executor.cpp` | P2 |
| H6f | ~~**`vkCreateGraphicsPipelines` accepts a mesh pipeline** (task stage optional, mesh stage required, no vertex-input/input-assembly state), **`vkCmdDrawMeshTasksEXT`/`vkCmdDrawMeshTasksIndirectEXT`/`vkCmdDrawMeshTasksIndirectCountEXT` route through the same prepared-draw code `vkCmdDraw*` already uses**, and `PhysicalDeviceInfo.cpp` advertises `VK_EXT_mesh_shader`, `taskShader`/`meshShader`, and every `VkPhysicalDeviceMeshShaderPropertiesEXT` limit at this implementation's own honest, bounded ceilings (mirroring H4b/H5e's own "advertise only what the implementation actually enforces" discipline)~~ (done: mesh pipeline acceptance was already landed by a prior pass; this row added the three `vkCmdDrawMeshTasks*` entry points, routed through the pre-existing `runPreparedDraw`/`runMeshDraw` (`CommandBuffer.cpp`, refactored out of `runDraw`), and `VK_EXT_mesh_shader` advertisement (`PhysicalDeviceInfo.cpp`'s extension list, `EntryPoints.cpp`'s features/properties chains). Also fixed a real, independently-necessary `Executor.cpp` bug found via testing: the pre-rasterization "last stage writes SV_Position" check had a geometry-stage empty-signature relaxation (H5e-b) but no mesh-stage equivalent, so *no* mesh pipeline draw -- real content or not -- could ever have succeeded without it (every mesh entry compilable today is necessarily output-empty, real per-vertex/per-primitive writes being blocked on H6h/H6i). A CTS run then found a second real bug: the first properties revision set `maxMeshWorkGroupSize`/`maxTaskWorkGroupSize`/`maxMeshOutputComponents`/`maxMeshOutputMemorySize`/`maxMeshPayloadAndOutputMemorySize`/`maxMeshOutputLayers` below `VK_EXT_mesh_shader`'s own specification-mandated minimums (`vktMeshShaderPropertyTestsEXT.cpp`'s `CHECK_LIMITS_MIN`), causing 80 failures plus 1 properties-limits-meta-test failure; fixed by adding genuine pipeline-creation-time enforcement of a mesh/task entry's declared group size (`validateMeshOrTaskGroupSize` in `GraphicsPipeline.cpp`, reusing `GroupSize.h`'s scanner extended to accept the `MeshEXT`/`TaskEXT` execution models, against new `feme::vulkan::MaxMeshWorkGroupSize`/`MaxTaskWorkGroupSize`/`*Invocations` constants set to the specification's floor) and raising the output-related properties to their own spec floors, since nothing in this implementation's actual mesh output storage (`MeshOutputBuilder`, a dynamically-sized `std::vector` per row) or layered-framebuffer support (already general, H2) caps them any lower -- advertising the floor is the honest choice, not an inflated one. 17 new/updated unit tests across `DrawTest.cpp` (7), `GraphicsPipelineTest.cpp` (2 new work-group-size-limit tests, 1 test's extension-count assertion updated), and `GroupSizeTest.cpp` (2 new `MeshEXT`/`TaskEXT`-acceptance tests). `ninja check-feme` (assertions-enabled, ccache build): 1930/1989 passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6e's own 1910/1969 baseline. `dEQP-VK.mesh_shader.*` moves from 0/0/28044 to 1/337/27706 (up from 0 pass; down from 338 to 337 fail, the property-limit bug's own signature resolved); the `dEQP-VK.draw.*` 1957-case regression sample stays byte-identical (153 failures, same named set) to H6e's own recorded totals -- 0 regressions. See "Roadmap H6f: measured impact" in VulkanCTSReport.md | H6e | `feme/lib/Vulkan/{GraphicsPipeline,CommandBuffer,PhysicalDeviceInfo,EntryPoints,GroupSize}.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H6g | ~~**Triage and close the `dEQP-VK.mesh_shader.*` buckets H6f's own measured run found** (mirroring H5e-a through H5e-e's own post-landing triage rows): 235 `vkCreateGraphicsPipelines` -> `VK_ERROR_INITIALIZATION_FAILED`, 68 `vkCreateRenderPass`/1 `vkGetPhysicalDeviceImageFormatProperties` -> `VK_ERROR_FORMAT_NOT_SUPPORTED`, and 33 `vkPipelineConstructionUtil.cpp` -> `VK_ERROR_INITIALIZATION_FAILED`~~ (done as triage: this row's own text originally assumed the 235+33 content-compilation bucket would be re-runnable "once H6h/H6i land", but a real re-run performed after both landed (measured here, byte-identical to H6h's own baseline: `dEQP-VK.mesh_shader.*` stays 1/337/27706, the identical 235/68/33/1 four-way split by API call and error code, `dEQP-VK.draw.*`'s 1957-case regression sample stays byte-identical too, 0 regressions) shows that text under-scoped the real blocker set -- H6h/H6i alone were never sufficient, since H6c-a's own investigation (which predates this row) had already split the remaining wiring into H6c-a-a/H6c-a-b, neither landed yet, and this row's own dependency list should have named them directly instead of only their own prerequisites. Closing this row now means correctly re-scoping, not prematurely declaring the buckets resolved: split below into H6g-a (the format-related 68+1, independently triaged and closed against existing roadmap row H8, not a new one) and H6g-b (the content-compilation 235+33, re-run confirmed still blocked, now correctly tracked against H6c-a-a/H6c-a-b directly rather than their own already-landed H6h/H6i prerequisites). See "Roadmap H6g: measured impact" in VulkanCTSReport.md | H6f, H6h, H6i | `feme/docs` (triage only, no source change) | P2 |
| H6g-a | ~~**Decide the fate of the 68 `vkCreateRenderPass`/1 `vkGetPhysicalDeviceImageFormatProperties` -> `VK_ERROR_FORMAT_NOT_SUPPORTED` bucket**: a new roadmap row, or a documented permanent gap~~ (done: every one of the 69 failing cases names a render-pass attachment format or an image-format query outside the mandatory format/usage table roadmap H8 ("Format table completeness for the graphics profile") already exists to close -- nothing mesh-shading-specific about *which* formats these particular cases exercise, just H8's own already-tracked, still-incomplete `Format.cpp` table being reached by a new CTS group. No new roadmap row needed; this bucket folds into H8's own existing scope and closes there, not here. No source change from this row itself, since the fix belongs to H8 | H6f | `feme/docs` (triage only, tracked by H8) | P2 |
| H6g-b | ~~**Re-run `dEQP-VK.mesh_shader.*` and confirm the 235 `vkCreateGraphicsPipelines`/33 `vkPipelineConstructionUtil.cpp` -> `VK_ERROR_INITIALIZATION_FAILED` content-compilation failures clear**, once real mesh/task shader content can actually compile end-to-end -- still blocked: H6c-a-a-i and H6c-a-a-ii have both now landed (`SetMeshOutputsEXT` canonicalization and `flattenMeshRow`'s `PerPrimitive` routing, respectively), but H6c-a-a-ii's own closing re-run confirms `H6c-a-a-iii` (`resolveOffsetWithinElement`'s arrayed-builtin-block crash, still hitting a subset of this row's own cases instead of a clean `VK_ERROR_INITIALIZATION_FAILED`) remains the sole open blocker. This row now depends only on that remaining row~~ (done, though the group's own re-run this row performed did *not* find the bucket clear -- it found a real, previously-unreported bug of its own, root-caused and fixed, plus three further blockers left open as new rows. With `H6c-a-a-iii` landed, a real full `dEQP-VK.mesh_shader.*` re-run (28044 cases, resume-loop methodology) reached these same 235+33 cases and found the *majority* of the 235 `vkCreateGraphicsPipelines` failures (`vkRefUtil.cpp:37`) traced to a bug this row itself introduced no fix for yet at the time: `translateGraphicsPipeline`'s own mesh-stage carve-out (`GraphicsPipeline.cpp`) rejected any non-null `pVertexInputState`/`pInputAssemblyState` on a mesh pipeline outright with `VK_ERROR_INITIALIZATION_FAILED` -- but the Vulkan spec (`VkGraphicsPipelineCreateInfo::pVertexInputState`/`pInputAssemblyState`'s own doc comments, `VK_EXT_mesh_shader`/`VK_NV_mesh_shader` carve-out) only requires both to be *ignored* when the pipeline includes a mesh shader stage, not null -- and `dEQP-VK.mesh_shader.*` itself builds every one of its pipelines through the shared, non-mesh-aware `vkObjUtil.cpp` `makeGraphicsPipeline` helper, which unconditionally passes a non-null default `pVertexInputState`/`pInputAssemblyState` regardless of stage set. Fixed by simply not reading either pointer for a mesh pipeline, per the spec's own "ignored" wording (`GraphicsPipelineTest.cpp`'s `RejectsMeshPipelineWith{VertexInput,InputAssembly}State` replaced with `AcceptsMeshPipelineWith{VertexInput,InputAssembly}State`, now expecting `VK_SUCCESS`). `ninja check-feme` (assertions-enabled, ccache build): **2009/2009** discovered, **1950** passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`) -- unchanged from `H6c-a-a-iii`'s own baseline, since this row replaces 2 existing tests rather than adding new ones. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases) after the fix: `{'Fail': 334, 'NotSupported': 27706, 'Pass': 1}`, **0 crashes -> 3 crashes** (a *new* crash bucket this row's own fix newly exposed, see H6g-b-b below) -- of the 235-case `vkRefUtil.cpp:37` bucket this row named, 3 cases moved into that new crash bucket and the rest resolved into two further, distinct gaps of their own (H6g-b-a, dominant, and residual scattered gaps out of this row's scope), and of the 33-case `vkPipelineConstructionUtil.cpp:176` bucket this row named, all 33 remain, now root-caused to a third, distinct gap (H6g-b-c). `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row's own recorded totals (14 passed / 153 failed / 1790 not supported), **0 regressions**. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: `VK_EXT_mesh_shader` was already `Advertised` (H6f); this row is a pure pipeline-validation robustness fix, no feature bit or extension changes. Milestone H6 does **not** close: this row's own re-run replaced one known blocker with three newly-isolated ones, tracked as H6g-b-a/H6g-b-b/H6g-b-c below. See "Roadmap H6g-b: measured impact" in VulkanCTSReport.md) | H6c-a-a-iii | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P2 |
| H6g-b-a | ~~**`ConvertSPIRVToLLVMPass`/the MLIR SPIR-V dialect deserializer rejects `PerPrimitiveEXT` with `error: unhandled Decoration : 'PerPrimitiveEXT'`, failing SPIR-V module deserialization outright** before `feme` ever sees the module -- the single dominant cause found within H6g-b's own 235-case `vkCreateGraphicsPipelines` bucket (202 of 232 cases still failing there hit exactly this, per a diagnostic-logged re-run of that bucket alone), and a prerequisite for any mesh entry with a real `PerPrimitiveEXT`-decorated per-primitive output block (the same shape `H6c-a-a-iii`'s own fix already had to reason about downstream, but never gets the chance to reach, since deserialization fails first). Root cause not yet isolated (upstream MLIR SPIR-V dialect decoration table, or a `feme`-local import shim over it -- not yet determined which)~~ (done: root-caused to the upstream MLIR SPIR-V dialect itself, not any `feme`-local shim -- `SPIRVBase.td`'s `Decoration` enum already lists `SPIRV_D_PerPrimitiveEXT`, but `Deserializer.cpp`'s `processDecoration` switch (which the enum's own header candidly documents as hand-written, "TODO: This function should also be auto-generated") never listed it among the plain unit-attribute decorations it groups together with `Flat`/`NoPerspective`/`Patch`/etc., so it fell through to the `default:` case's `"unhandled Decoration"` error instead. Fixed by adding `spirv::Decoration::PerPrimitiveEXT` to that case group (a spec-conformant fix: like `Patch`, `PerPrimitiveEXT` is a zero-operand decoration, so it belongs in exactly that bucket) plus the serializer-side mirror image (`Serializer.cpp`'s matching unit-attribute case list, which had the same gap for the round trip back to SPIR-V) and its attribute-name special case (`getDecorationName`'s `snake_case`->`CamelCase` guess, `convertToCamelFromSnakeCase`, cannot recover the `EXT` suffix's capitalization any more than it already couldn't recover `INTEL`'s, so `per_primitive_ext` needed the same explicit override `cache_control_load_intel`/`cache_control_store_intel` already use). New round-trip test case in `mlir/test/Target/SPIRV/mesh-ops.mlir` (a `PerPrimitiveEXT`-decorated `GlobalVariable`, exercised by the file's existing `--test-spirv-roundtrip`/`spirv-val` `RUN` lines). `ninja check-mlir-target-spirv`: 58/58 passing (up from 57/58 -- the new test -- all others unaffected); `ninja check-mlir-dialect-spirv`: 52/52 unaffected. `ninja check-feme` (assertions-enabled, ccache build): unchanged at 1950/2009 passing (59 pre-existing `Unsupported`, 0 `Failed`), since this is a pure upstream-MLIR fix with no `feme`-local code touched. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases, resume-loop methodology) confirms the fix directly: a diagnostic-logged re-run of the exact 232-case `vkRefUtil.cpp:37` bucket this row's own text named now shows **0** occurrences of `"unhandled Decoration"` (down from 202), confirming this row's own dominant cause is fully cleared. Of those 202 formerly-`PerPrimitiveEXT`-blocked cases: 3 now progress far enough to fail later, at a brand-new `vkCmdUtil.cpp:338` location (out of scope for this row, a real forward-progress signal); 11 now reach `feme::cpu::SIMDizePass` far enough to hit H6g-b-b's own already-tracked `widenMaskedStore` assertion (that row's own crash count grows from 3 to 14 cases as a direct, expected consequence -- see its own updated text below); the remaining 188, plus the 30 cases this row's own text already called "out of this row's own scope", now fail for other, further downstream reasons -- a diagnostic-logged re-run of the resulting 218-case `vkRefUtil.cpp:37` bucket found the *new* single dominant cause among them is `ConvertSPIRVToLLVMPass`'s legalization of `spirv.AccessChain` failing outright ("failed to legalize operation 'spirv.AccessChain' that was explicitly marked illegal", 80 of 218 cases), tracked as new row H6g-b-a-i below; the rest spread across roughly ten further distinct, smaller gaps (`feme-cpu-simdize` divergent-vector-value rejections, `feme-cpu-wrap-mesh-output`/`feme-cpu-wrap-fragment` metadata-missing errors, `feme-cpu-linearize` unsupported-control-flow-shape errors, a handful of `spirv.MemoryBarrier`/`spirv.AtomicExchange`/`spirv.EXT.EmitMeshTasks`/`spirv.Any`/`spirv.All` legalization failures, and 5 more `Symbols not found` cases already tracked by H6g-b-c), out of this row's own scope. All 33 `vkPipelineConstructionUtil.cpp:176` cases this row's own text named remain, unaffected either way (still tracked by H6g-b-c). `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical (14 passed / 153 failed / 1790 not supported), **0 regressions**. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a pure MLIR SPIR-V dialect deserialization/serialization fix, touching no `feme`-advertised feature bit or extension. Milestone H6 does **not** close: this row's own fix directly unblocks the dominant single cause it named, but the re-run it triggered surfaces H6g-b-a-i as the new dominant blocker in its place, alongside the already-tracked H6g-b-b/H6g-b-c. See "Roadmap H6g-b-a: measured impact" in VulkanCTSReport.md) | H6g-b | `mlir/lib/Target/SPIRV/Deserialization/Deserializer.cpp`, `mlir/lib/Target/SPIRV/Serialization/Serializer.cpp` | P2 |
| H6g-b-a-i | ~~**`ConvertSPIRVToLLVMPass` fails to legalize `spirv.AccessChain` outright ("failed to legalize operation 'spirv.AccessChain' that was explicitly marked illegal")**, now the single dominant cause found within H6g-b-a's own newly-shrunk 218-case `vkCreateGraphicsPipelines`/`vkRefUtil.cpp:37` bucket (80 of 218 cases still failing there hit exactly this, per a diagnostic-logged re-run of that bucket alone), and newly reachable only now that H6g-b-a's own fix lets `PerPrimitiveEXT`-decorated per-primitive-block SPIR-V actually deserialize far enough to reach legalization at all. Root cause not yet isolated (which `spirv.AccessChain` shape the existing conversion patterns don't cover -- a per-primitive-block member access through a `PerPrimitiveEXT`-decorated pointer specifically, given this row's own prerequisite, or something broader -- and whether the fix belongs in a new/extended conversion pattern or a legalization-target adjustment)~~ (done: root-caused to the upstream MLIR SPIR-V dialect, not any `PerPrimitiveEXT`-specific gap or `feme`-local pattern -- the actual base pointer type on every one of the 80 failing `spirv.AccessChain` ops was a generic `StorageBuffer`/`Block`-decorated struct holding ordinary vertex-attribute data (`vec2`/`vec3`/`vec4` float/int arrays of 4 elements each), not mesh-specific content at all; it only surfaces here because H6g-b-a's own fix is the first thing to let this SPIR-V shape reach legalization in this test group. `convertArrayType` (`mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp`) validated a declared `ArrayStride` against the element type's compact `SPIRVType::getSizeInBytes()` (12 bytes for `vector<3xf32>`), but the Vulkan spec requires an array's stride to be a multiple of its element's *base alignment* (16 bytes for a 3- or 4-component vector of 4-byte scalars, not its raw/compact size) -- so every conformant SPIR-V producer (glslang included) emits `ArrayStride=16` for `array<N x vec3<f32>>`, and this compact-size check spuriously rejected all of them as having an "unnatural" stride. A second, independently-triggerable copy of the same flaw was found in `VulkanLayoutUtils::decorateType(spirv::ArrayType, ...)` (`mlir/lib/Dialect/SPIRV/Utils/LayoutUtils.cpp`, shared by `SPIRVCompositeTypeLayoutPass` and `convertStructTypeWithOffset`'s own struct-identity check), which likewise computed the canonical stride and total array size from the raw compact element size instead of the alignment-rounded one. Fixed by adding `VulkanLayoutUtils::getNaturalArrayStride(Type)` (rounds the element's size up to its own alignment) and wiring it into both `convertArrayType`'s validity check and `decorateType(spirv::ArrayType, ...)`'s stride/size computation. New positive/negative lit tests: `mlir/test/Conversion/SPIRVToLLVM/spirv-types-to-llvm.mlir` (`array_with_natural_vector3_stride`, a `!spirv.array<4 x vector<3xf32>, stride=16>` now converting), `spirv-types-to-llvm-invalid.mlir` (`array_with_unnatural_vector3_stride`, confirming stride=12 for the same element -- the wrong, unrounded value -- is still correctly rejected), `mlir/test/Dialect/SPIRV/Transforms/layout-decoration.mlir` (a struct with an undecorated `array<4xvector<3xf32>>` member now decorated with `stride=16` and the following member correctly offset at 64, not 48), and `feme/test/Conversion/SPIRVToLLVM/spirv-to-llvm-storage-buffer.mlir` (`read_vec3_array_element`, a `StorageBuffer` block with a fixed-size `vec3` array member converting to the expected `llvm.spv.resource.getpointer`/GEP/load sequence, mirroring the file's existing `rtarray` cases). `ninja check-mlir-target-spirv check-mlir-dialect-spirv check-mlir-conversion-spirvtollvm`: 23/23, 52/52, 58/58 passing (all previously-passing tests unaffected, all new tests pass); the existing `MLIRSPIRVToLLVMTests` gtest unit suite: 3/3 unaffected. `ninja check-feme` (assertions-enabled, ccache build): unchanged at 1950/2009 passing (59 pre-existing `Unsupported`, 0 `Failed`) -- this row replaces no existing test, only adds a new `RUN`-split case to an existing file. A real, diagnostic-logged re-run of the exact 218-case `vkRefUtil.cpp:37` bucket this row's own text named, through feme's actual Vulkan ICD, confirms the fix directly: **0** `spirv.AccessChain` legalization failures (down from 80), with the 80 formerly-blocked cases now progressing further and the group's own re-run finding a *new* dominant cause among them, `spirv.All` failing to legalize (81 of 218 cases, up from a pre-existing, already out-of-scope 1), tracked as new row H6g-b-a-i-a below; the remaining, already-tracked-out-of-scope 11 `spirv.MemoryBarrier`/4 `spirv.AtomicExchange`/1 `spirv.EXT.EmitMeshTasks`/1 `spirv.Any` cases are unaffected either way. `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical (14 passed / 153 failed / 1790 not supported), **0 regressions**. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a pure MLIR SPIR-V layout/type-conversion correctness fix, touching no `feme`-advertised feature bit or extension. Milestone H6 does **not** close: this row's own fix directly clears the dominant cause it named, but the re-run it triggered surfaces H6g-b-a-i-a as the new dominant blocker in its place, alongside the already-tracked H6g-b-b/H6g-b-c. See "Roadmap H6g-b-a-i: measured impact" in VulkanCTSReport.md) | H6g-b-a | `mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp`, `mlir/lib/Dialect/SPIRV/Utils/LayoutUtils.cpp` | P2 |
| H6g-b-a-i-a | ~~**`ConvertSPIRVToLLVMPass` fails to legalize `spirv.All` outright ("failed to legalize operation 'spirv.All' that was explicitly marked illegal", e.g. `%124 = "spirv.All"(%123) : (vector<4xi1>) -> i1`)**, now the single dominant cause found within H6g-b-a-i's own 218-case `vkCreateGraphicsPipelines`/`vkRefUtil.cpp:37` bucket (81 of 218 cases hit exactly this, per a diagnostic-logged re-run of that bucket alone, up from a pre-existing, already out-of-scope 1 -- the 80 cases H6g-b-a-i's own fix newly unblocked progress into this same failure), and newly reachable only now that H6g-b-a-i's own fix lets the `spirv.AccessChain`-bearing SPIR-V this bucket's cases share actually legalize far enough to reach the `spirv.All`/`spirv.Any` (vector-to-scalar boolean reduction) ops that same content also contains. Root cause not yet isolated (whether MLIR's SPIRVToLLVM conversion is simply missing a pattern for `spirv.All`/`spirv.Any` outright, or one exists but doesn't cover this operand shape)~~ (done: root-caused to the first, simpler alternative -- `mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp`'s pattern-registration list (`populateSPIRVToLLVMConversionPatterns`) had a `Logical ops` section covering `spirv.LogicalAnd`/`spirv.LogicalOr`/`spirv.LogicalEqual`/`spirv.LogicalNotEqual`/`spirv.LogicalNot`, but never had an entry for `spirv.All` or `spirv.Any` at all; the pass's target correctly marks both illegal for this same conversion, but with no pattern registered to rewrite them, every occurrence hits the pass's generic "explicitly marked illegal" failure path regardless of operand shape -- confirming this row's own second alternative, not an operand-shape gap in an existing pattern. Fixed by registering two new `DirectConversionPattern` instantiations mapping `spirv.All`/`spirv.Any` (a vector-of-`i1` reduced to a scalar `i1`, per the ops' own `SPIRV_AllOp`/`SPIRV_AnyOp` `SPIRVLogicalOps.td` definitions) onto the existing LLVM dialect vector-reduction intrinsics `llvm.intr.vector.reduce.and`/`llvm.intr.vector.reduce.or` (`LLVM::vector_reduce_and`/`LLVM::vector_reduce_or`, already used the same way by `ConvertVectorToLLVM`'s own `vector.reduction`/`AND`/`OR` lowering) -- the same one-line-per-op shape every other `DirectConversionPattern` registration in this file already uses, needing no new pattern class. New lit test cases added to the existing `mlir/test/Conversion/SPIRVToLLVM/logical-ops-to-llvm.mlir` (`all_vector`/`any_vector`, each a `vector<4xi1>` `spirv.All`/`spirv.Any` converting to the expected `llvm.intr.vector.reduce.and`/`.or`), and `mlir/docs/SPIRVToLLVMDialectConversion.md`'s "Logical ops" section updated with the new op-to-intrinsic mapping. `ninja check-mlir-conversion-spirvtollvm`: 23/23 passing (0 regressions; +2 new `RUN`-split cases added to an existing file, so the discovered-test total is unchanged); `ninja check-mlir-target-spirv`: 58/58, `ninja check-mlir-dialect-spirv`: 52/52 (both unaffected, this row touches neither); the existing `MLIRSPIRVToLLVMTests` gtest unit suite: 3/3 unaffected. `ninja check-feme` (assertions-enabled, ccache build): unchanged at 1950/2009 passing (59 pre-existing `Unsupported`, 0 `Failed`) -- a pure upstream-MLIR fix, no `feme`-local code touched. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases, resume-loop methodology) confirms the fix directly: a diagnostic-logged re-run of the exact 218-case `vkRefUtil.cpp:37` bucket this row's own text named now shows **0** `spirv.All`/`spirv.Any` legalization failures (down from 81), with the 81 formerly-blocked cases now progressing further; the bucket's `vkCreateGraphicsPipelines` failure causes now partition its full 218 cases into 9 buckets (82/68/17/17/12/7/7/5/3), with the new single dominant cause among them a `feme`-local `feme::cpu::UnsupportedOps` rejection ("unsupported raised operation: ... is a register-bound resource handle the FeMe CPU target cannot normalize into a heap access or the root-constant block ...", 82 of 218 cases, up from a pre-existing, already out-of-scope 1 -- consistent with the 81 formerly-`spirv.All`-blocked cases all landing here), tracked as new row H6g-b-a-i-a-i below; the remaining 136 cases spread across the same roughly-ten already-tracked-out-of-scope smaller gaps H6g-b-a-i's own text named (68 `feme-cpu-simdize` divergent-vector-value rejections reached via widening, 17 `feme-cpu-wrap-mesh-output`/`feme-cpu-wrap-fragment` metadata-missing errors, 12 `feme-cpu-linearize` unsupported-control-flow-shape errors, 17 already-tracked `spirv.MemoryBarrier`/`spirv.AtomicExchange`/`spirv.EXT.EmitMeshTasks` legalization failures, 7 unimplemented specialization constants, 7 unimplemented rasterizer-discard/depth-clamp/depth-bias/non-fill polygon modes, 5 `Symbols not found` cases already tracked by H6g-b-c, and 3 a pipeline's declared color-blend-state count exceeding its render target's color-attachment count), unaffected either way. `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical (14 passed / 153 failed / 1790 not supported), **0 regressions**. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a pure upstream MLIR SPIRVToLLVM conversion-pattern-coverage fix, touching no `feme`-advertised feature bit or extension. Milestone H6 does **not** close: this row's own fix directly clears the dominant cause it named, but the re-run it triggered surfaces H6g-b-a-i-a-i as the new dominant blocker in its place, alongside the already-tracked H6g-b-b/H6g-b-c. See "Roadmap H6g-b-a-i-a: measured impact" in VulkanCTSReport.md) | H6g-b-a-i | `mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp` | P2 |
| H6g-b-a-i-a-i | ~~**`feme::cpu::UnsupportedOps` rejects a register-bound resource handle that survives to the FeMe CPU target's normalization pass ("unsupported raised operation: ... is a register-bound resource handle the FeMe CPU target cannot normalize into a heap access or the root-constant block ...")**, now the single dominant cause found within H6g-b-a-i-a's own 218-case `vkCreateGraphicsPipelines`/`vkRefUtil.cpp:37` bucket (82 of 218 cases hit exactly this, per a diagnostic-logged re-run of that bucket alone, up from a pre-existing, already out-of-scope 1 -- the 81 cases H6g-b-a-i-a's own fix newly unblocked progress into this same failure), and newly reachable only now that H6g-b-a-i-a's own fix lets the `spirv.All`/`spirv.Any`-bearing SPIR-V this bucket's cases share actually legalize far enough to reach `feme-cpu`'s own resource-handle normalization pass. Root cause not yet isolated (which resource-declaration shape in this content the pass's existing "finite, unambiguous traditional binding, bindless access, or root-constant" recognition doesn't cover, and whether the fix belongs in `UnsupportedOps`/`RootConstantLowering` itself or an earlier canonicalization step feeding it)~~ (done: root-caused to `feme::cpu::SPIRVResourceLoweringPass`, not `UnsupportedOps` or `RootConstantLowering`. The dominant failing handles are the fragment-stage `spirv.VulkanBuffer` values produced for glslang's direct `layout(set=0,binding=*) std430 readonly buffer { ... }` blocks: `ConvertSPIRVToLLVMPass` had already lowered each SPIR-V `AccessChain` into `llvm.spv.resource.getpointer` selecting a constant struct field followed by ordinary LLVM `getelementptr` array indexing, but `SPIRVResourceLowering.cpp`'s pre-existing `collectHandles`/`hasOnlySupportedUses` logic only accepted a flat direct load/store user of the `getpointer` result. Any further GEP into the selected field therefore made the whole function fail the pass's all-or-nothing handle classification, leaving every handle in that function to survive into `UnsupportedOps`. Fixed locally and surgically in `SPIRVResourceLowering.cpp`: struct-typed `spirv.VulkanBuffer` handles in SPIR-V `StorageBuffer` storage class are now classified separately from uniform-buffer/root-constant shapes, ordinary constant/affine GEP chains rooted at their `llvm.spv.resource.getpointer` result are validated and lowered recursively by folding the GEP byte offset into the raw resource byte offset, and the final load/store is rewritten to the existing `feme.cpu.resource.load.raw.*`/`store.raw.*` helpers. The still-intentionally-rejected negative shape (a dynamic top-level field selector on a direct storage block) stays rejected and is locked down explicitly. New unit coverage in `feme/unittests/Transforms/CPU/SPIRVResourceLoweringTest.cpp`: `LowersDirectStorageBlockFieldAndNestedArrayAccessToResourceLoad`, `LowersStructuredStorageBufferFieldAccessToFieldOffset`, and `LeavesDirectStorageBlockDynamicFieldSelectorUnchanged`; the existing lit regression `feme/test/Transforms/CPU/spirv-resource-lowering-unsupported.ll` updated so its `field_access` case now checks for the lowered raw-resource load while the still-unsupported image/sampler and unbounded-descriptor-array cases remain untouched. `ninja check-feme` (assertions-enabled, ccache build): 1953/2012 passing, 59 pre-existing `Unsupported`, 0 `Failed` (up from 1950/2009 by exactly the 3 new unit tests; the adjusted lit file adds no discovered tests). A real ICD re-run of the exact 218-case `vkRefUtil.cpp:37` bucket confirms the named cause is cleared almost completely: the previous row's own 82 register-bound-handle rejections drop to **1** (the lone remainder is a pre-existing sampled-image/sampler combined-handle shape, already out of this row's storage-buffer scope), and the 81 newly-unblocked storage-buffer cases now progress further. Re-running the bucket with a combined stdout/stderr log and classifying each case by the first emitted FeMe/MLIR diagnostic when present yields 148 `feme-cpu-simdize` divergent-vector-value rejections, 15 `feme-cpu-wrap-mesh-output` metadata failures, 13 `feme-cpu-linearize` unsupported-control-flow-shape failures, 11 `spirv.MemoryBarrier` legalization failures, 7 specialization-constant failures, 5 `Symbols not found` failures already tracked by H6g-b-c, 4 `spirv.AtomicExchange` legalization failures, 2 `feme-cpu-wrap-fragment` metadata failures, 1 `spirv.EXT.EmitMeshTasks` legalization failure, the lone remaining sampled-image/sampler `UnsupportedOps` rejection, and 11 still-failing cases that emitted no FeMe/MLIR diagnostic before `deqp-vk`'s own Linux-only device-fault-test abort path triggered. `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample is byte-identical to H6g-b-a-i-a's own baseline (14 passed / 153 failed / 1790 not supported), with **0 regressions** and an identical per-case status map. `FeMeCPUDesign.md` needs no update: this is a completeness fix within the design's existing resource-normalization scope, not a change to the documented resource model. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` also need no update: no advertised feature bit or extension changes. Milestone H6 does **not** close: this row's own fix clears the dominant storage-buffer-handle blocker it named, but the same re-run surfaces a new dominant blocker in its place, tracked as H6g-b-a-i-a-i-a below, alongside the already-tracked H6g-b-b/H6g-b-c. See "Roadmap H6g-b-a-i-a-i: measured impact" in VulkanCTSReport.md) | H6g-b-a-i-a | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/unittests/Transforms/CPU/SPIRVResourceLoweringTest.cpp` | P2 |
| H6g-b-a-i-a-i-a | ~~**`feme-cpu-simdize` rejects a newly-unblocked divergent vector value in the same 218-case `vkCreateGraphicsPipelines`/`vkRefUtil.cpp:37` bucket ("function 'main' has a divergent vector value ... used outside a supported insertelement-chain/resource-store/extractelement/select/shufflevector/phi/elementwise pattern; component decomposition is not yet supported for this use")**, now the new dominant first-emitted FeMe/MLIR diagnostic in that bucket once H6g-b-a-i-a-i's own direct-storage-buffer-handle fix lets those shaders progress further (148 of 218 cases in a combined stdout/stderr diagnostic rerun land here first; H6g-b-a-i-a-i reduced its own named `UnsupportedOps` bucket from 82 cases to a lone, out-of-scope sampled-image/sampler remainder). Root cause not yet isolated: needs a real failing shader/IR reduction to identify which divergent-vector use shape in the newly-unblocked mesh/fragment content is still outside `SIMDizePass`'s supported decomposition patterns, and whether the right fix belongs in `SIMDize.cpp` itself or in an earlier canonicalization/legalization pass feeding it~~ (done: reduced a real failing case (`dEQP-VK.mesh_shader.ext.api.draw.*`) down to its exact IR shape by temporarily instrumenting `checkVectorDecompositionSupported`'s own rejection with a one-off diagnostic dump of the offending instruction and its users, then re-running that single case through the real ICD -- the dominant shape is a mesh entry point's own `gl_PrimitiveTriangleIndicesEXT[...] = uvec3(...)` write: unlike an ordinary per-vertex/per-primitive output element store, `PrimitiveIndices` has no canonicalized `feme.stage.*` op of its own to become a `feme.cpu.resource.*`/masked-output-store call instead (see `MeshOutputWrapper.h`'s own "Left open by this row" note), so it survives as an ordinary `store` of a divergent `<3 x i32>` value that `feme::cpu::LinearizePass` masks into a `feme.cpu.masked.store.v3i32` call exactly like a scalar one. The fix belongs in `SIMDize.cpp` itself, not an earlier pass: `checkVectorDecompositionSupported` didn't recognize a matched `feme.cpu.masked.store.*` call's stored-value operand as a supported consumer of a decomposed divergent vector (only a matched `feme.cpu.resource.*` store's was), and `FunctionWidener::widenMaskedStore` unconditionally called the scalar-only `getWidened` on its stored value, which (for a vector-typed, already-decomposed operand recorded only in `WidenedVectorComponents`, not `Widened`) fell through to `getWidened`'s generic broadcast fallback and tried to build an illegal `<W x <3 x i32>>` vector-of-vectors. Fixed both: `checkVectorDecompositionSupported` now accepts a matched `feme.cpu.masked.store.*` call's stored-value operand the same way it already accepts a matched resource-store call's, and `widenMaskedStore` now recognizes a vector-typed stored value, reassembles each lane's own `<3 x i32>` from the decomposed per-component wide values (mirroring `widenResourceCall`'s equivalent per-lane reassembly), and writes it individually per lane with a load-select-store idiom (mirroring `MeshOutputWrapper.cpp`'s `lowerMeshOutputStore`) instead of `llvm.masked.scatter`, which has no vector-of-vector-element form to lower a per-lane vector value to. New lit regression `feme/test/Transforms/CPU/simdize-masked-memop-vector-divergent.ll` and unit test `SIMDizeTest.DecomposesInsertElementChainIntoMaskedVectorStore`. `ninja check-feme` (assertions-enabled, ccache build): 1955/2014 passing (59 pre-existing `Unsupported`, 0 `Failed`, up from 1953/2012 by exactly the 2 new tests). A real re-run of the exact 218-case `vkRefUtil.cpp:37` bucket (`bucket_refutil37_h6g_b_a_i_a.txt`, unchanged) with a combined stdout/stderr diagnostic log confirms the fix directly: this row's own 148 `feme-cpu-simdize` divergent-vector rejections drop to **80** (68 cases -- exactly the `gl_PrimitiveTriangleIndicesEXT` shape this row named -- progress further, landing in the already-tracked `feme-cpu-wrap-mesh-output` bucket, which grows from 15 to 83 cases as a direct, expected consequence), with every other bucket (`feme-cpu-wrap-fragment` 2, `feme-cpu-linearize` 13, `spirv.MemoryBarrier` 11, `spirv.AtomicExchange` 4, `spirv.EXT.EmitMeshTasks` 1, specialization constants 7, rasterizer/depth state 7, `Symbols not found` 5 (H6g-b-c), the lone sampled-image/sampler `UnsupportedOps` remainder, and a 3-case color-blend-state mismatch) unaffected. The remaining 80 `feme-cpu-simdize` cases surface a new dominant blocker in their place -- a divergent vector value used as a vector comparison (`fcmp`/`icmp`) operand, an entirely different consumer shape from this row's own masked-store fix -- tracked as new row H6g-b-a-i-a-i-b below. A full `dEQP-VK.mesh_shader.*` re-run (28044 cases, resume-loop methodology) additionally confirms this row's fix, as an unplanned but verified side effect, also closes H6g-b-b: that row's crash (`FunctionWidener::widenMaskedStore` -> `getWidened` -> `ConstantVector::getSplat`'s `isValidElementType` assertion) is the exact same code path this row's fix touches, and its "aggregate" description was an imprecise characterization of the same vector-of-vector-element rejection all along, not a distinct genuine-aggregate (struct/array) case -- the re-run's 14 previously-blacklisted crashing cases (`barrier_in_task`, `group_memory_barrier_in_task_*`, `memory_barrier_shared_in_task_*`, `emit_in_control_flow*`, `payload_not_accessed`) all now complete cleanly with **0** crashes (down from 14, requiring 0 resume-loop iterations instead of the prior 15), each landing in the same `vkRefUtil.cpp:37` bucket (232 cases now, up from 218 by exactly these 14) with an ordinary, non-crashing diagnostic instead (confirmed individually for `payload_not_accessed`: now `feme-cpu-wrap-mesh-output`, no crash) -- see H6g-b-b's own updated row. The 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row (14 passed / 153 failed / 1790 not supported), **0 regressions**. `FeMeCPUDesign.md`/`Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this is a completeness fix within `SIMDizePass`'s existing "vectors become components" decomposition scope (documented in `feme/docs/FeMeCPUDesign.md`'s "Phase 4: Widening"), not a new feature or extension surface. Milestone H6 does **not** close: this row's own fix and H6g-b-b's incidental closure both land, but the same re-run surfaces H6g-b-a-i-a-i-b as a new dominant blocker, alongside the already-tracked H6g-b-c. See "Roadmap H6g-b-a-i-a-i-a: measured impact" in VulkanCTSReport.md) | H6g-b-a-i-a-i | `feme/lib/Transforms/CPU/SIMDize.cpp`, `feme/test/Transforms/CPU/simdize-masked-memop-vector-divergent.ll`, `feme/unittests/Transforms/CPU/SIMDizeTest.cpp` | P2 |
| H6g-b-a-i-a-i-b | ~~**A divergent vector value used as a vector comparison (`fcmp`/`icmp`) operand is rejected by `feme-cpu-simdize` in the same `vkCreateGraphicsPipelines`/`vkRefUtil.cpp:37` bucket ("function 'main' has a divergent vector value ... used outside a supported insertelement-chain/resource-store/extractelement/select/shufflevector/phi/elementwise pattern; component decomposition is not yet supported for this use")**, now the new dominant first-emitted FeMe/MLIR diagnostic in that bucket once H6g-b-a-i-a-i-a's own masked-store fix lets those shaders progress further (80 of 218 cases in a combined stdout/stderr diagnostic rerun land here, e.g. `dEQP-VK.mesh_shader.ext.in_out.32_bits_only.permutation_0.mesh_only`'s `%8 = insertelement <4 x float> %6, float %7, i64 3` used by `%16 = fcmp ole <4 x float> %8, %15`, confirmed via the same one-off diagnostic-dump-and-single-case-rerun technique H6g-b-a-i-a-i-a used). Root cause not yet isolated: a vector comparison producing a `<N x i1>` result is not among `checkVectorDecompositionSupported`'s accepted consumer shapes at all today (unlike a matched resource-store's or masked-store's stored-value operand), and the design's own existing "a `select` with a per-lane `<N x i1>` condition remains diagnosed" deviation (`checkVectorDecompositionSupported`'s file comment) suggests decomposing a per-lane vector *condition* end-to-end -- not just a per-lane vector *value* -- may need broader work than a single consumer-acceptance addition; needs a real failing shader/IR reduction to confirm the exact GLSL/SPIR-V source shape (a component-wise `lessThanEqual`/`greaterThan`-style comparison feeding a `select`, most likely) and whether the right fix is a narrow `fcmp`/`icmp` consumer-and-producer addition mirroring this row's own sibling, or the broader per-lane-condition decomposition the file comment already anticipates~~ (done: added `fcmp`/`icmp` (`CmpInst`) as a supported vector producer (its `<N x i1>` result decomposes into `N` per-component `<W x i1>` comparisons exactly like ordinary elementwise arithmetic) and consumer, and generalized the design's own documented `select`-condition deviation away entirely: a `select`'s condition may now be a per-lane `<N x i1>` vector (decomposed into `N` widened `<W x i1>` components, one used per per-component `select`, in `widenVectorSelect`), not just a shared scalar `i1` (`dc7fe7b15f2f`, lit tests `simdize-vector-fcmp-select.ll`/updated `simdize-vector-select.ll`/`simdize-vector-unsupported.ll`, unit test `SIMDizeTest.DecomposesVectorComparisonIntoPerLaneSelectCondition` in `bf6a561b0531`). Running the *real* VK-GL-CTS `deqp-vk` (not a simulated count) against this row's own named `dEQP-VK.mesh_shader.ext.in_out.32_bits_only.permutation_0.mesh_only` case with `FEME_VULKAN_LOG_CREATION_ERRORS=1` confirmed the fix was necessary but not sufficient: the same one-off diagnostic-dump-and-single-case-rerun technique found the real rejected shape was not a `select` at all -- glslang lowers GLSL's `all(lessThanEqual(a, b))` to `llvm.vector.reduce.and.v4i1(fcmp ole <4 x float> %a, %b)`, an `llvm.vector.reduce.*` intrinsic call folding a divergent vector's components together, a shape not yet accepted as a consumer. Added `isSupportedVectorReduceIntrinsic` (accepting the integer/comparison reduces: `and`/`or`/`xor`/`add`/`mul`/`smax`/`smin`/`umax`/`umin`; the floating-point reduces are excluded as out of scope/unneeded for now) and `widenVectorReduce`, folding a vector operand's `N` components together two at a time with the matching op, landing its (non-vector-typed) result in the ordinary `Widened` map. Re-running the same real case again still failed, with a third, new blocker: a GLSL `min()`/`max()`/`clamp()` builtin lowers to `llvm.minnum`/`llvm.maxnum`/`llvm.smin`/`llvm.smax` intrinsic calls over an already-decomposed divergent vector operand, another shape not yet accepted as either a producer or a consumer. Generalized `checkVectorDecompositionSupported`, `widenVectorElementwise` (now also handling `CallInst`, using `I.args()` instead of `I.operands()` since a call's own operand list also includes its callee), and `widenInstruction`'s dispatch to accept any vector-typed, homogeneous "trivially vectorizable" intrinsic call (`isElementwiseVectorizableIntrinsic`) as both a producer and a consumer, decomposing it exactly like ordinary elementwise arithmetic: one scalar-element intrinsic call per component (`eaf8216ce1f4`, lit tests `simdize-vector-reduce.ll`/`simdize-vector-intrinsic.ll`, updated `simdize-vector-unsupported.ll`; unit tests `SIMDizeTest.DecomposesVectorComparisonIntoReduceAnd`/`DecomposesHomogeneousVectorizableIntrinsicCall` in `08c1e2b9d4e5`/`3cb7e072fda2`). With all three fixes, re-running the same single named case now gets past `feme-cpu-simdize` entirely -- it fails only at `vkCreateGraphicsPipelines` time on an unrelated, separate JIT-symbol-registration gap (missing `feme.cpu.resource.load.raw.v2f32`/`v3f32`/`v2i32`/`v3i32` overloads in `feme/runtime/CPU/FeMeRuntimeCPU.c`, which only implements the scalar and `v4f32`/`v4i32` raw-load/store overloads today), a different subsystem entirely (JIT runtime symbol registration, not `SIMDizePass` decomposition), out of scope for this row and tracked as new row H6g-b-a-i-a-i-c below. Re-running the full real `dEQP-VK.mesh_shader.ext.in_out.*` bucket (560 cases, the row's own cited 80/218-case bucket, via `--deqp-caselist-file`) confirms this at scale: still 0 passed / **80** failed / 480 not-supported (unchanged totals), but every one of the 80 failures now hits that same new JIT-symbol blocker instead of the `feme-cpu-simdize` diagnostic this row targets (spot-checked across several cases in the bucket, not just the one named case), confirming this row's own `SIMDizePass`-level fix is complete and this bucket's dominant blocker has fully moved to H6g-b-a-i-a-i-c. `ninja check-feme` (assertions-enabled, ccache build): 1961/2020 passing (59 pre-existing `Unsupported`, 0 `Failed`, up from 1955/2014 by exactly the 6 new tests across all three fixes). `FeMeCPUDesign.md` updated (Phase 4's decomposition deviation bullet now documents nine producer shapes, was six; `13051214ea77`) to reflect all three additions and retire the now-resolved per-lane-condition-`select` deviation note. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a `SIMDizePass`-completeness fix within its existing "vectors become components" decomposition scope, not a new feature or extension surface. Milestone H6 does **not** close: this row's fix lands, but the same re-run surfaces H6g-b-a-i-a-i-c as the bucket's new dominant blocker, alongside the already-tracked H6g-b-c. See "Roadmap H6g-b-a-i-a-i-b: measured impact" in VulkanCTSReport.md) | H6g-b-a-i-a-i-a | `feme/lib/Transforms/CPU/SIMDize.cpp`, `feme/test/Transforms/CPU/simdize-vector-fcmp-select.ll`, `feme/test/Transforms/CPU/simdize-vector-reduce.ll`, `feme/test/Transforms/CPU/simdize-vector-intrinsic.ll`, `feme/test/Transforms/CPU/simdize-vector-unsupported.ll`, `feme/test/Transforms/CPU/simdize-vector-select.ll`, `feme/unittests/Transforms/CPU/SIMDizeTest.cpp` | P2 |
| H6g-b-a-i-a-i-c | ~~**`vkCreateGraphicsPipelines` fails at JIT-link time with "Symbols not found: [ feme.cpu.resource.load.raw.v2f32, feme.cpu.resource.load.raw.v3f32, feme.cpu.resource.load.raw.v3i32, feme.cpu.resource.load.raw.v2i32, feme.cpu.resource.load.raw.v4i32 ]"**, now the new dominant blocker in the same 80-case `dEQP-VK.mesh_shader.ext.in_out.*` bucket H6g-b-a-i-a-i-b's own `fcmp`/`icmp`/reduce/vectorizable-intrinsic fixes let those cases progress past `feme-cpu-simdize` entirely (confirmed by re-running the full 560-case bucket and spot-checking several individual cases with `FEME_VULKAN_LOG_CREATION_ERRORS=1` against the real `deqp-vk`/`feme` Vulkan ICD once H6g-b-a-i-a-i-b's own fix landed). Root cause isolated: `feme/runtime/CPU/FeMeRuntimeCPU.c` only defines the scalar (`feme.cpu.resource.load.raw.i32`/`.f32`) and full-`<4 x T>`-width (`feme.cpu.resource.load.raw.v4f32`/`.v4i32`) raw-buffer-load overloads today (see its own `asm("feme.cpu.resource.load.raw.v4f32")`-labeled function and neighbors) -- the 2- and 3-component overloads a `vec2`/`vec3`/`ivec2`/`ivec3` mesh-shader input/output actually needs are simply missing, so the JIT's `orc::LLJIT` can't materialize a call to them at pipeline-creation time. The store-side (`feme.cpu.resource.store.raw.*`) and typed-buffer (`feme.cpu.resource.{load,store}.typed.v4{f32,i32}`) paths were not checked for the same v2/v3 gap and may need the same fix. Not yet fixed: needs new `feme.cpu.resource.load.raw.v2f32`/`.v3f32`/`.v2i32`/`.v3i32` (and likely the store-side/typed-buffer counterparts, pending the same gap check) runtime functions mirroring the existing `v4f32`/`v4i32` ones' bindless-descriptor-lookup-then-masked-load shape, plus confirming `feme::cpu::ResourceCalls`/`ResourceLowering.cpp` already emit calls to these names for narrower vector widths (or need their own fix to do so)~~ (done: added the 5 missing raw-buffer-load overloads (`v2f32`/`v3f32`/`v2i32`/`v3i32`/`v4i32`) to `feme/runtime/CPU/FeMeRuntimeCPU.c`, plus their symmetric store-side counterparts proactively (this row's own "may need the same fix" note) -- `feme::cpu::ResourceCalls`/`ResourceLowering.cpp` already emitted calls to these mangled names for every vector width with no changes needed, since `mangleResourceCallName` is fully generic over element type/width. Found and fixed a real, related bug while adding the v3-wide stores: Clang's C ABI coerces a by-value `<3 x float>`/`<3 x i32>` parameter to a `<4 x i32>` register pair and pads `sizeof()` to 16, so a naive `*(Unaligned *)Ptr = Value` or even `__builtin_memcpy(Ptr, &Value, sizeof(Value))` store body emitted a widened, out-of-bounds 16-byte `store <4 x T>` instead of the intended 12-byte one; fixed with `__builtin_memcpy(Ptr, &Value, 12)` using an explicit literal byte count. Verified the production JIT-link path (declare-then-`Linker::linkInModule`) is unaffected by the coercion mismatch itself (confirmed via a temporary probe test mirroring the real caller/linker order, since RAUW-based linking never re-validates a `CallInst`'s `FunctionType` against its callee). Added 5 new `RawLoadStoreRoundTrip*` unit tests to `RuntimeCPUTest.cpp` (generalizing its `addStoreWrapper` helper to detect and adapt to an ABI-coerced target parameter type) and 10 new `CHECK-DAG` lines to `runtime-cpu-bitcode.test`; `ninja check-feme` rises from 1961/2020 to 1966/2025 (5 new passing tests), 0 regressions. A real `deqp-vk`/`feme` Vulkan ICD re-run of this row's own named case and the full 560-case `dEQP-VK.mesh_shader.ext.in_out.*` bucket confirms the exact reported JIT symbol-not-found error is gone from all 80 previously-blocked cases; see `VulkanCTSReport.md`'s own "Roadmap H6g-b-a-i-a-i-c" section for the two new blockers this exposes (tracked as H6g-b-c, already-known, and the new H6g-b-d)) | H6g-b-a-i-a-i-b | `feme/runtime/CPU/FeMeRuntimeCPU.c` | P2 |
| H6g-b-b | ~~**`feme::cpu::SIMDizePass`'s `FunctionWidener::widenMaskedStore` -> `getWidened` asserts (`ConstantVector::getSplat`/`llvm::FixedVectorType::get`'s own `isValidElementType` check) when a masked store's stored value has an aggregate (non-scalar, non-pointer) element type**, newly reachable now that H6g-b's own fix lets real mesh/task content compile far enough to reach `SIMDizePass` at all -- originally 3 `dEQP-VK.mesh_shader.ext.misc.*` cases (`emit_in_control_flow`, `emit_in_control_flow_bad_emit_last`, `payload_not_accessed`), now **14** cases (H6g-b-a's own fix lets 11 further cases -- `barrier_in_task`, `group_memory_barrier_in_task_{array,float,struct,uint64,vector}`, `memory_barrier_shared_in_task_{array,float,struct,uint64,vector}` -- progress far enough through deserialization and legalization to reach this same crash too, confirmed via `gdb` backtrace on `barrier_in_task`: identical stack to the original 3) abort the whole `deqp-vk` process with this assertion (confirmed via `gdb` backtrace: `SIMDizePass::run` -> `FunctionWidener::widenInstruction` -> `widenMaskedStore` -> `getWidened` -> `ConstantVector::getSplat`), rather than failing cleanly. `getWidened`'s constant-splat and non-constant (`CreateVectorSplat`) paths were both written assuming a per-lane scalar/pointer value, mirroring every other masked-memory-op operand this file widens; a task-payload-store's payload value (or some other operand reaching this path with an aggregate LLVM type) breaks that assumption, since LLVM vectors cannot hold an aggregate element type at all. Root cause not yet isolated (which call site feeds `widenMaskedStore` an aggregate-typed `StoredValue`, and whether the correct fix is to reject the shape gracefully, decompose the aggregate lane-by-lane the way `getVectorComponents` already does for real vectors, or something else entirely)~~ (done, incidentally, by H6g-b-a-i-a-i-a: this row's own crash and that row's `feme-cpu-simdize` rejection turned out to be the *same* code path, not two distinct bugs -- `ConstantVector::getSplat`/`FixedVectorType::get`'s `isValidElementType` check rejects a vector *element* type just as it would a genuine struct/array element type, so "aggregate (non-scalar, non-pointer) element type" was an imprecise description of the exact same vector-of-vector-element shape H6g-b-a-i-a-i-a root-caused and fixed (a decomposed, divergent `<3 x i32>` masked-store value falling through `getWidened`'s generic scalar-only broadcast fallback and attempting to build an illegal `<W x <3 x i32>>`); no genuinely aggregate (struct/array) `StoredValue` was ever isolated or reproduced. Confirmed via re-run: a full `dEQP-VK.mesh_shader.*` re-run (28044 cases) after H6g-b-a-i-a-i-a's fix completes with **0** crashes needing blacklisting (down from this row's own 14, and down from the 15 resume-loop iterations the prior row needed), and each of the 14 previously-crashing cases now individually confirmed reaching an ordinary, non-crashing diagnostic instead (e.g. `payload_not_accessed` now cleanly hits `feme-cpu-wrap-mesh-output`, no crash, no `gdb` needed) | H6g-b | `feme/lib/Transforms/CPU/SIMDize.cpp` | P2 |
| H6g-b-c | ~~**A mesh entry's unresolved arrayed-builtin-block access -- left unrewritten by `H6c-a-a-iii`'s own `resolveOffsetWithinElement` fix (`std::nullopt`, "leave for `ValidateStagePass` to diagnose") -- is never actually diagnosed, because `ValidateStagePass::run` still does not validate `ShaderStage::Mesh` at all (every prior row that touched mesh validation, from `H6a` on, left this unreachable/not-yet-wired)**, so the raw, un-canonicalized global-variable access survives all the way to `feme::cpu`'s JIT, which then fails with a genuinely undefined symbol (confirmed directly: `dEQP-VK.mesh_shader.ext.builtin.cull_primitives`, one of this row's own 33 `vkPipelineConstructionUtil.cpp:176` cases, fails with `JIT session error: Symbols not found: [ spirv_var_16 ]`) instead of a clean, diagnosable compile-time rejection -- the exact same 9-case-turned-33-case set `H6c-a-a-ii`/`H6c-a-a-iii`'s own reports already named (`cull_primitives`, `draw_index_in_{mesh,task}`, `local_invocation_{id,index}_in_task`, `position`, `primitive_id_glsl`, `work_group_id_in_{mesh,task}`). This is the concrete, now-reachable instance of the gap `H6c-a-a-iii`'s own report already flagged as a future risk ("not yet reachable... mirroring `TaskPayloadStore`'s own 'not yet reachable' precedent")~~ (done: `ValidateStagePass::run` now validates `ShaderStage::Mesh` alongside `Vertex`/`Fragment`, which in turn required extending `isStageOpLegalForStage`'s `InputLoad`/`OutputStore` case to also accept `Mesh` (a mesh entry's own per-vertex/per-primitive output write reuses `OutputStore`, roadmap H6b -- without this, every legitimate mesh output store would have been newly, wrongly rejected as "not legal" the moment `Mesh` was wired in). Added a new check, `validateStageIOGlobalAccess`, walking every load/store's pointer operand back through any `getelementptr` chain (`GEPOperator`) to find its underlying `GlobalVariable`: one `isSPIRVStageIOGlobal` still recognizes (address space 7/8, carrying `!spirv.Decorations`/`!feme.spirv.MemberDecorations`) means `CanonicalizeStagePass` left that access un-rewritten, so it is now diagnosed directly instead of silently reaching the JIT. `isSPIRVStageIOGlobal` itself was factored out of `CanonicalizeStage.cpp`'s anonymous namespace into a new shared header, `feme/include/feme/Transforms/Graphics/StageIOGlobal.h`, so both passes recognize the identical shape (no behavior change to `CanonicalizeStagePass`). New unit tests in `ValidateStageTest.cpp`: `MeshOutputStoreIsLegal`/`DiscardInMeshStageIsIllegal` (confirming the newly-wired stage's own legality rules), `UnresolvedStageIOGlobalAccessInMeshIsDiagnosed` (reusing `CanonicalizeStageTest.ConstantIndexIntoArrayedBuiltinInterfaceBlockIsLeftUnrewritten`'s own arrayed-`PerPrimitiveEXT`-block IR directly against `ValidateStagePass`, confirming the exact shape this row targets is caught), and `OrdinaryAllocaAccessInMeshIsNotDiagnosed` (a negative control, confirming an unrelated local `alloca` access is not flagged). `ninja check-feme` (assertions-enabled, ccache build): 1972/2031 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6k's own 1968/2027 baseline by exactly the 4 new tests this row adds. A real `deqp-vk` re-run of this row's own named case confirms the fix directly: `dEQP-VK.mesh_shader.ext.builtin.cull_primitives` no longer reaches the JIT at all -- `feme-graphics-validate-stage: ... has an unresolved stage-IO global-variable access to 'spirv_var_16', a shape CanonicalizeStagePass does not yet canonicalize into a 'feme.stage.*' call` is now the first diagnostic emitted, well before pipeline creation fails (still `Fail`, `VK_ERROR_INITIALIZATION_FAILED` at `vkPipelineConstructionUtil.cpp:176` -- diagnosed, not fixed, exactly this row's own scope). A full re-run of the real `dEQP-VK.mesh_shader.ext.builtin.*` group (37 cases) confirms this at scale: of the 9-case-turned-33-case set's mesh-portion cases, 12 now hit this row's own new clean diagnostic instead of a JIT symbol error (`cull_primitives`, `draw_index_in_{mesh,task}`, `local_invocation_{id,index}_in_task`, `num_work_groups_mesh`, `num_work_groups_task_and_mesh`, `position`, `primitive_id_{glsl,spirv}`, `work_group_id_in_{mesh,task}`); the other 10 failing cases in the group hit an unrelated, pre-existing `feme-cpu-simdize` divergent-vector-value diagnostic further down the pipeline, confirmed unaffected by this row (0 `feme-graphics-validate-stage` diagnostics for any of them); group totals stay 0 Pass / 22 Fail / 15 NotSupported, unchanged from before this row (expected -- diagnosing a failure earlier and more cleanly does not itself turn it into a pass). This same re-run surfaces a genuinely new, distinct bug, entirely out of this row's own scope: `cull_primitives` alone (no other case in the group) additionally reports three `feme.stage.output.store` row-out-of-range errors (element 4 rows 1-2, element 5 rows 1-3) that only became visible now that `ValidateStagePass` validates the mesh stage at all for the first time -- not investigated further here, filed as new row H6l below. The `dEQP-VK.draw.*` 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row's own recorded totals (14 passed / 153 failed / 1790 not supported), **0 regressions**. `FeMeGraphicsDesign.md`/`Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a pure diagnostic-completeness fix within `ValidateStagePass`'s existing scope, touching no feature bit or extension. Milestone H6 does **not** close: this row's own fix lands, but the same re-run surfaces H6l as a new, narrower blocker, alongside the already-tracked H6g-b-a-i-a-i-b and H6k) | H6c-a-a-iii, H6g-b | `feme/include/feme/Transforms/Graphics/StageIOGlobal.h`, `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/Graphics/ValidateStage.cpp`, `feme/unittests/Transforms/Graphics/ValidateStageTest.cpp` | P2 |
| H6g-b-d | ~~**`feme::cpu::MeshOutputWrapperPass::lowerMeshStageOps` diagnoses "feme-cpu-wrap-mesh-output: unexpected stage op left for the mesh output wrapper"** on 40 of the 80 cases in the same `dEQP-VK.mesh_shader.ext.in_out.*` bucket H6g-b-a-i-a-i-c's own real-ICD re-run found -- discovered while confirming that row's fix: after adding the missing `feme.cpu.resource.load.raw.v2f32`/`v3f32`/`v2i32`/`v3i32`/`v4i32` runtime overloads and re-running the full 560-case bucket, the 80 previously-JIT-symbol-blocked cases split evenly, 40 now hitting the already-tracked H6g-b-c `spirv_var_NN` gap and 40 hitting this new blocker instead. Root cause not yet isolated: `lowerMeshStageOps` (`MeshOutputWrapper.cpp`) only accepts `isMaskedOutputStoreCall`/`isMaskedSetMeshOutputsCall` as the two lowerable shapes once a mesh entry point uses any `feme.stage.*`/masked-output op at all (`isStageOpCall(*CI) \|\| isMaskedOutputStoreCall(*CI) \|\| isMaskedSetMeshOutputsCall(*CI)` gates entry into the function at all); its closing catch-all `F.getContext().emitError(CI, ...)` fires on every surviving call that is neither of those two -- an unmasked (not yet lowered by `Linearize.cpp`'s `applyStageMasks`) `feme.stage.output.store`, or some other `StageOpKind` a mesh entry should never legally contain (`InputLoad`, an interpolation op, etc., per the function's own comment "a mesh entry point has no ordinary stage-IO input to read"), reaching this pass unexpectedly. Needs a real failing shader/IR reduction (the same one-off diagnostic-dump-and-single-case-rerun technique H6g-b-a-i-a-i-a/-b used) to find which `feme.stage.*` op survives unlowered and why, before deciding whether the fix belongs in `MeshOutputWrapperPass` itself (accept a new shape) or upstream in whichever pass was supposed to have already lowered/masked it~~ (done: root-caused with the same one-off diagnostic-dump-and-single-case-rerun technique H6g-b-a-i-a-i-a/-b used -- a temporary `errs()` print in `lowerMeshStageOps`'s own catch-all, rebuilt `feme_vulkan`, re-run against the real ICD on this row's own named case -- and the answer was neither of the two hypotheses this row's own text entertained (an unmasked `feme.stage.output.store`, or an illegal `StageOpKind` like `InputLoad`): the offending call was `%3409 = call <4 x float> @feme.cpu.resource.load.raw.v4f32(...)`, an entirely ordinary, already-correctly-lowered storage-buffer read feeding the mesh output store's own value (`pvd.name[i]`/`ppd.name[i]` in the real GLSL, per `vktMeshShaderInOutTestsEXT.cpp`) -- not a stage op at all. The bug was in `lowerMeshStageOps`'s own per-instruction loop, not upstream: its `UsesStageOps` gate at the top of the function correctly only requires *some* call in `F` to need this pass's attention before proceeding, but the loop below it then treated *every remaining `CallInst` in the whole function* as needing to be one of the two lowered shapes, erroring out on any call that was neither -- including completely unrelated calls (resource loads/stores, or any other intrinsic) that were never this pass's concern in the first place and had already been correctly left alone by every earlier phase. Fixed with a narrow, surgical change: the catch-all now only fires when the surviving call actually `isStageOpCall`, matching the function's own comment ("diagnoses ... if F uses a feme.stage.* op this pass does not support") for the first time; anything else falls through to `continue`, unmodified. New `MeshOutputWrapperTest.LeavesUnrelatedResourceLoadCallAlone` (a mesh entry mixing a `feme.cpu.resource.load.raw.f32` call with an ordinary per-vertex output store) reproduces the bug directly at the unit level -- confirmed to fail (`error: feme-cpu-wrap-mesh-output: unexpected stage op left for the mesh output wrapper`) without this fix and pass with it. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1967/2026 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6g-b-a-i-a-i-c's own 1966/2025 by exactly the 1 new test this row adds. A real `deqp-vk` run against the same 560-case `dEQP-VK.mesh_shader.ext.in_out.*` bucket (same caselist-derivation technique as every prior row in this chain) confirms the fix end to end: the named case's `feme-cpu-wrap-mesh-output` diagnostic is gone, and grepping the full re-run's combined log confirms it is gone from all 40 previously-blocked cases, not just the one named case. Bucket totals are unchanged (0 passed / 80 failed / 480 not-supported), as expected -- this row was never going to make new cases pass, only move the point of failure -- and the 80 failures still split the same way H6g-b-a-i-a-i-c's own re-run found: 40 hitting the already-tracked H6g-b-c `spirv_var_NN` gap (verified unchanged, not a duplicate), and the other 40 now hitting a new, previously-unseen `vkQueueSubmit` diagnostic ("vertex output and fragment input at location 0 disagree on component/row count or type"), genuinely new and out of this row's own scope -- filed as a new sibling row one level under H6 (not nested further under H6g-b, per the standing instruction), H6j. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed to need no change: a pure CPU-lowering-pass fix within `VK_EXT_mesh_shader`'s already-advertised scope, touching no feature bit or extension. See "Roadmap H6g-b-d: measured impact" in VulkanCTSReport.md for the full reproduction) | H6g-b-a-i-a-i-c | `feme/lib/Transforms/CPU/MeshOutputWrapper.cpp` | P2 |
| H6c-a-a-i | ~~**Canonicalize `SetMeshOutputsEXT` into a new `feme.stage.*` op and wire it into `MeshOutputWrapperPass`/`EntryWrapperPass`** so `FemeMeshArgs::ActualVertexCount`/`ActualPrimitiveCount` reflect a mesh workgroup's real declared output counts instead of always reading 0 (discovered by H6c-a-a's own closing re-run: without this, `feme::graphics::MeshOutputBuilder::assembleMeshlet` always trims to an empty meshlet, regardless of how much real per-vertex/per-primitive data `MeshOutputWrapperPass` itself correctly writes)~~ (done: new `StageOpKind::SetMeshOutputs` (`StageOps.h`/`.cpp`, `feme.stage.set_mesh_outputs(vertex_count, primitive_count)`, void, not overloaded) is workgroup-uniform per the SPIR-V/GLSL spec (every invocation must call it with identical arguments), but is still threaded through the full masked/widened pipeline for defensive consistency with every other stage op, rather than special-cased as always-uniform. SPIR-V's `spirv.EXT.SetMeshOutputs` (the real generated MLIR class is `mlir::spirv::EXTSetMeshOutputsOp` -- note this mnemonic is `spirv.EXT.SetMeshOutputs`, *not* `spirv.SetMeshOutputsEXT` as the op's own `.td` doc-comment example misleadingly shows) converts directly into a call to the new op at the MLIR conversion level (new `SetMeshOutputsEXTConversionPattern`, `SPIRVToLLVMPatterns.cpp`, registered in `populateSPIRVToLLVMTargetPatterns`), mirroring `EmitVertex`/`EndPrimitive`'s own "no raw-IR shape to canonicalize, so convert straight to a `feme.stage.*` call" treatment -- this op has no underlying "store to a global" shape for `CanonicalizeStage.cpp`'s raw-IR pattern matching to recognize, unlike `OutputStore`/`TaskPayloadStore`. `WaveUniformity.cpp` classifies it as void/side-effecting (falls through, like `OutputStore`/`TaskPayloadStore`); `StageMaskCalls.h`/`.cpp` gained `createMaskedSetMeshOutputs`/`isMaskedSetMeshOutputsCall` (masked-call naming mangled by mask type only, since both counts share one type); `Linearize.cpp`'s `applyStageMasks`/`hasStageMaskOps` gained a `SetMeshOutputs` case; `SIMDize.cpp`'s `FunctionWidener` gained `widenMaskedSetMeshOutputs`, which -- unlike `widenMaskedTaskPayloadStore`'s scalar offset -- widens *both* `VertexCount` and `PrimitiveCount` operands across the wave, since (unlike a task payload store's compile-time-constant offset) neither operand is guaranteed constant at the IR level even though the spec requires them to be workgroup-uniform at runtime; `ValidateStage.cpp` gained a `SetMeshOutputs` legality/validation case (mesh-only, not yet reachable since `ValidateStagePass::run` does not validate `ShaderStage::Mesh` today, mirroring `TaskPayloadStore`'s own "not yet reachable" precedent). `MeshOutputWrapperPass` (`MeshOutputWrapper.h`/`.cpp`) gained two more always-appended trailing params (`mesh_actual_vertex_count`, `mesh_actual_primitive_count`, extending `MeshOutputStageEnv`/`appendMeshOutputParams`/`getMeshOutputStageEnv`) and a new `lowerSetMeshOutputs` lowering: for each active lane, an old-value-select guards against a not-provably-all-one mask, then unconditionally stores both counts through the two new pointers -- correct and idempotent since the spec guarantees every active lane computes identical values. `EntryWrapperPass` (`EntryWrapper.cpp`) threads `MeshActualVertexCount`/`MeshActualPrimitiveCount` out of `FemeMeshArgs` (whose `MeshArgsFieldActualVertexCount`/`ActualPrimitiveCount` = 13/14 and struct-layout slots were already reserved by H6c-a-a) into the wave body by name, mirroring every other `IsMesh`-gated field exactly (only one dispatch site needed the edit, `buildWaveLoop`, shared by all three wrapper-building call sites). New tests: `StageOpsTest.cpp`'s `SetMeshOutputsIsVoidAndNotOverloaded`; `MeshOutputWrapperTest.cpp`'s `LowersSetMeshOutputsCall`; a new SPIRVToLLVM conversion test, `spirv-to-llvm-set-mesh-outputs.mlir`; and one new `CompiledStageTest.cpp` case, `InvokeMeshWritesSetMeshOutputs`, proving a canonicalized-shaped mesh entry compiled through the real `CompiledStage::create` pipeline writes real counts into `FemeMeshArgs::ActualVertexCount`/`ActualPrimitiveCount` through `CompiledStage::invokeMesh` end to end -- the same "prove a real value reaches host memory through the full compiled path" shape `InvokeMeshWritesPerVertexOutputStore`/`InvokeTaskWritesPayloadStore` already established. `ninja check-feme` (assertions-enabled, ccache build): **2007/2007** discovered, **1948** passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6c-a-b's own 1944/2003 baseline by exactly these 4 new tests. `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row's own recorded totals (14 passed / 153 failed / 1790 not supported), **0 regressions**. `dEQP-VK.mesh_shader.*`'s own 28044-case group stays at **1 passed** (byte-identical to H6c-a-b's baseline -- expected, since `H6c-a-a-ii` remains open and no real mesh-shader draw can assemble a non-empty, correctly-routed meshlet from this row's wiring alone), but this row's own closing re-run *also* discovered a new, previously-unreachable crash: 28 cases that used to fail cleanly with `VK_ERROR_INITIALIZATION_FAILED` (rejected earlier, at SPIR-V-to-LLVM conversion, for an unconverted `spirv.EXT.SetMeshOutputs`) now reach further into `CanonicalizeStage.cpp` and hit an unrelated, pre-existing `cast<StructType>` assertion in `resolveOffsetWithinElement` once a mesh entry's arrayed per-primitive/per-vertex builtin interface block (e.g. `PerPrimitiveEXT`-decorated blocks) is resolved -- a shape that function's `IDs.size() != 1` path assumes is a plain (non-arrayed) struct and was simply never reachable before this row made `SetMeshOutputsEXT` conversion succeed. This is a distinct, pre-existing gap in generic builtin-interface-block resolution, unrelated to this row's own `feme.stage.set_mesh_outputs` wiring (which is fully tested and correct in isolation, confirmed by `InvokeMeshWritesSetMeshOutputs`) -- tracked as new row H6c-a-a-iii below rather than fixed here, to keep this row's own scope to exactly what its own text names. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (this row changes no advertised feature bit or extension, only CPU-side lowering). See "Roadmap H6c-a-a-i: measured impact" in VulkanCTSReport.md) | H6c-a-a | `feme/lib/Transforms/Graphics`, `feme/lib/Transforms/CPU` | P2 |
| H6c-a-a-ii | ~~**Teach `feme::graphics::Executor::runMeshWorkgroup`'s `flattenMeshRow` to route a `PerPrimitive`-frequency `Output` element into `MeshResources::PrimitiveOutputs`/`PrimitiveOutputLayout`**, which it never populates today (every element is currently treated as per-vertex regardless of `SignatureElement::Frequency`), discovered by H6c-a-a's own closing re-run alongside H6c-a-a-i~~ (done: `runMeshWorkgroup` now builds a second `StageStorage` sized by `Mesh.MaxOutputPrimitives` (mirroring the existing `MaxOutputVertices`-sized one) and threads it into `MeshResources::PrimitiveOutputLayout`/`PrimitiveOutputs`, which had always been left null/empty -- the moment `MeshOutputWrapperPass`'s already-correct per-primitive store lowering (H6c-a-a) reached a real `PerPrimitive` element through this path, it read/wrote through that null layout pointer, an unconditional crash confirmed by reverting this fix and re-running the new test below (`SIGSEGV` in `MeshOutputWrapperPass`'s compiled lowering). `flattenMeshRow` itself gained a `SignatureFrequency` parameter and a matching `Storage` parameter (`VertexOut`/`PrimitiveOut`), filtering `MeshSig->Elements` to just that frequency instead of walking every `Output` element unconditionally; the vertex loop now calls it with `(*VertexOut, PerVertex, V)` and the primitive loop with `(*PrimitiveOut, PerPrimitive, P)`, routing the result into `Builder.setVertex`/`setPrimitive` respectively (`MeshOutputBuilder::setPrimitive`, already implemented since H6c, previously never called). **A second, closely-related bug found and fixed along the way**: `unflattenMeshRow` (the merge-side counterpart, downstream of `assembleMeshlet`) still walked every `Output` element unconditionally, so for any signature mixing `PerVertex` and `PerPrimitive` elements, its `Row[Idx++]` indexing would silently desynchronize from `flattenMeshRow`'s now-narrower per-vertex-only rows (an out-of-bounds/misaligned read, not merely stale data) -- fixed with the identical `Elt.Frequency != SignatureFrequency::PerVertex` filter, keeping the two functions' own element-order convention symmetric the way they always needed to be. New `ExecutorTest.cpp` case, `RoutesAPerPrimitiveOutputElementIntoPrimitiveOutputsAlongsideAPerVertexOne`: a real compiled mesh entry (canonicalized `feme.stage.set_mesh_outputs`/`feme.stage.output.store` calls, reaching the full `CompiledStage::create` pipeline through `MeshOutputWrapperPass`/`EntryWrapperPass`) declaring one `PerVertex` position element and one `PerPrimitive` scalar element from the same workgroup, run through the real `executeDraws` end to end with `PrimitiveTopology`/`MeshOutputTopology::Points` (needing no separate `PrimitiveIndices` write, which remains a distinct, still-open gap per `MeshOutputWrapper.h`'s own comment) -- asserts the point still rasterizes to the correct pixel, proving the frequency split does not misalign the per-vertex path, and (implicitly, by not crashing) that the primitive store's own read/write is now safe. Reverting just this row's `Executor.cpp` change and re-running that same test reproduces the pre-fix `SIGSEGV` directly, confirming both the bug and the fix. `ninja check-feme` (assertions-enabled, ccache build): **2008/2008** discovered, **1949** passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6c-a-a-i's own 2007/1948 baseline by exactly the 1 new test this row adds. `dEQP-VK.mesh_shader.*`'s own 28044-case group: `Passed` stays byte-identical at 1/28044 (expected -- H6c-a-a-iii's own `resolveOffsetWithinElement` crash remains open and independently blocks every case whose builtin interface uses an arrayed block, and no case in today's suite exercises a user-defined `PerPrimitive` varying without also hitting that crash path), `Failed` rises from H6c-a-a-i's own baseline of 309 to 328 and the crashing/`Unresolved` bucket falls from 28 to 9 (all `ext.builtin.*` cases: `cull_primitives`, `draw_index_in_{mesh,task}`, `local_invocation_{id,index}_in_task`, `position`, `primitive_id_glsl`, `work_group_id_in_{mesh,task}`) -- a real, reproducible re-run using the identical resume-loop methodology, not a regression this row introduces (H6c-a-a-iii's own crash is purely a compile-time `CanonicalizeStage.cpp` bug this row's runtime-only `Executor.cpp` change cannot affect; the count difference is not yet root-caused and is noted rather than asserted away, see "Roadmap H6c-a-a-ii: measured impact" in VulkanCTSReport.md). `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row's own recorded totals (14 passed / 153 failed / 1790 not supported), **0 regressions**. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this row changes no advertised feature bit or extension, only CPU-side (host, not compiled-IR) executor lowering. See "Roadmap H6c-a-a-ii: measured impact" in VulkanCTSReport.md) | H6c-a-a | `feme/lib/Graphics` | P2 |
| H6c-a-a-iii | ~~**Fix `CanonicalizeStage.cpp`'s `resolveOffsetWithinElement`, which asserts (`cast<StructType>`) rather than gracefully rejecting an unmodeled shape when a multi-`ElementID` builtin interface block's own value type is not a plain (non-arrayed) `StructType`** -- reachable for the first time by H6c-a-a-i's own closing re-run, since a mesh entry's `PerPrimitiveEXT`/other arrayed builtin interface blocks (e.g. an array-of-struct per-primitive output block) take exactly this shape, and previously never got this far because an unconverted `SetMeshOutputsEXT` always failed earlier, at SPIR-V-to-LLVM conversion. 28 of `dEQP-VK.mesh_shader.*`'s own cases now abort the whole `deqp-vk` process with this assertion instead of failing cleanly with `VK_ERROR_INITIALIZATION_FAILED` the way they did before H6c-a-a-i landed (same failing-case *set*, just a worse failure *mode* -- 0 `Pass`/`Fail` regressions, but a real robustness regression a fuzzer or a CTS run without this report's own resume-loop workaround would trip over)~~ (done: the crash's own two-shape cause was exactly what the function's own comment already described -- a multi-`ElementID` builtin interface block reaches `resolveOffsetWithinElement` two ways, `resolveStageIOAccess`'s `getDynamicVertexIndexedAccess` path (which already peels one array dimension into a per-vertex/per-primitive `ElemTy` before calling in, always leaving a plain struct) and its own ordinary constant-offset fallback (which does *not* peel any dimension for `Output`-storage globals, `isPerVertexArrayInputGlobal` being deliberately `Input`-only per H6b's own precedent) -- so a **constant**-indexed access into a mesh entry's arrayed `Output` builtin block (e.g. `gl_MeshPrimitivesEXT[1].gl_PrimitiveID = ...`, exactly the shape a real, unrolled GLSL-compiled per-primitive write takes) lands in that fallback with `ElemTy` still the whole array-of-struct, not a struct at all, hitting the `cast<StructType>` assertion the constant-offset path's own comment never accounted for. Fixed with the minimal, surgical change the milestone's own text asked for: `resolveOffsetWithinElement` now returns `std::optional<StageIOAccess>` instead of `StageIOAccess`, and its `IDs.size() != 1` branch uses `dyn_cast<StructType>` instead of `cast<StructType>`, returning `std::nullopt` (leaving the access unrewritten, exactly the same "unresolved, left for `ValidateStagePass` to diagnose" treatment every other unmodeled shape in this file already gets) rather than aborting -- no call-site change needed beyond the signature update, since `resolveStageIOAccess` (the function's only caller) already returns `std::optional<StageIOAccess>` itself and simply forwards the result through both of its own call sites unchanged. New `CanonicalizeStageTest.cpp` case, `ConstantIndexIntoArrayedBuiltinInterfaceBlockIsLeftUnrewritten`: a multi-`ElementID` builtin interface block (`!feme.spirv.MemberDecorations`) whose declared type is an array of that struct (address space 8, mirroring `gl_MeshPrimitivesEXT[]`'s own shape), accessed through a **constant** array index (`getelementptr ... i32 0, i32 1, i32 0`) -- confirmed to reproduce the pre-fix assertion directly (reverting just the `dyn_cast`/`cast` change and re-running this test crashes the whole test binary, exactly like `deqp-vk` did), and to leave the store instruction unrewritten (no `feme.stage.output.store` call, the raw `StoreInst` untouched) after the fix. `ninja check-feme` (assertions-enabled, ccache build): **2009/2009** discovered, **1950** passing (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6c-a-a-ii's own 2008/1949 baseline by exactly the 1 new test this row adds. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases, full run, using the identical resume-loop methodology H6c-a-a-i/H6c-a-a-ii's own reports established) confirms the fix end to end: **0 crashes** this time (down from 9, the exact bucket H6c-a-a-ii's own closing re-run found still crashing), `Passed` stays byte-identical at 1/28044, `Failed` rises from 328 to **337** (328 + the same 9 cases, now resolving as a clean `Fail` instead of a process-aborting crash -- the identical `cull_primitives`/`draw_index_in_{mesh,task}`/`local_invocation_{id,index}_in_task`/`position`/`primitive_id_glsl`/`work_group_id_in_{mesh,task}` set H6c-a-a-ii's own report named), `NotSupported` stays byte-identical at 27706/28044 -- **0 `Pass`/`Fail` regressions**, exactly the milestone's own "same failing-case set, just a worse failure mode" framing, now resolved back to the better mode. `dEQP-VK.draw.*`'s 1957-case `draw_sample.txt` regression sample stays byte-identical to every prior row's own recorded totals (14 passed / 153 failed / 1790 not supported), **0 regressions** (expected: this fix only changes what happens when an unmodeled shape is *reached*, not any already-modeled one `dEQP-VK.draw.*` exercises). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: this row changes no advertised feature bit or extension, only a robustness fix to existing CPU-side canonicalization. See "Roadmap H6c-a-a-iii: measured impact" in VulkanCTSReport.md) | H6c-a-a-i | `feme/lib/Transforms/Graphics` | P2 |
| H6h | ~~**Give `TaskPayloadWorkgroupEXT` an address-space convention and a global-variable import pattern**: LLVM's own SPIR-V backend (`storageClassToAddressSpace` in `llvm/lib/Target/SPIRV/SPIRVUtils.h`) has no mapping at all for `TaskPayloadWorkgroupEXT` (SPIR-V enum 5402) -- unlike `Input`(7)/`Output`(8)/`Workgroup`(3)/`PushConstant`(13), which FeMe's own `StageIOGlobalVariablePattern`/`WorkgroupGlobalVariablePattern`/`PushConstantGlobalVariablePattern` (`feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`) already reuse from that fixed mapping -- so a task entry's payload variable cannot be imported as an LLVM global at all today (found during H6b's own investigation). Needs a new address space (any value FeMe's own conversion layer does not otherwise use) and a `TaskPayloadGlobalVariablePattern` mirroring the two precedents above, before `CanonicalizeStage.cpp` has anything to canonicalize a payload write into~~ (done: a new `TaskPayloadGlobalVariablePattern` converts a `TaskPayloadWorkgroupEXT`-storage-class `spirv.GlobalVariable` to an ordinary `llvm.mlir.global` in address space 14 -- a FeMe-only convention, since LLVM's own `storageClassToAddressSpace` has no case for this storage class at all -- and a matching `spirv::PointerType` conversion routes any access-chain result into it to the same address space, letting a plain `spirv.AccessChain`/`spirv.Load`/`spirv.Store` through a payload variable convert with MLIR's own generic patterns exactly as a `Workgroup` variable's own access already does. New `spirv-to-llvm-task-payload.mlir` lit test (2 `RUN`/`CHECK` blocks) covers the store-through-array-element and load-scalar shapes, mirroring `spirv-to-llvm-workgroup.mlir`'s own coverage of the pattern this mirrors. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1931/1990 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6f's own 1930/1989 baseline by exactly the 1 new lit test this row adds. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases) is byte-identical to H6f's own recorded totals (1/337/27706) -- expected and confirmed, since this row is pure conversion-layer plumbing with no caller reachable from `CanonicalizeStagePass`/the mesh pipeline until H6i lands. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: no feature bit, limit, or extension is touched. See "Roadmap H6h: measured impact" in VulkanCTSReport.md for the full reproduction)~~ | H6a | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| H6i | ~~**Lift `CanonicalizeStagePass::run`'s stage filter to accept `ShaderStage::Mesh`/`ShaderStage::Amplification`**, canonicalize a task entry's bounded payload write (`TaskPayloadWorkgroupEXT`, now importable per H6h) into a new `feme.stage.*` op, and route entries reaching `canonicalizeSPIRVStage` through it, mirroring how H5c flipped geometry's own filter only once H5b's (and, separately, H5f's) prerequisite machinery had landed~~ (done: the stage filter now accepts `ShaderStage::Mesh`/`ShaderStage::Amplification`, routing both through `canonicalizeSPIRVStage` -- unblocking H6b's own mesh `Output`-array store canonicalization through the full pass, not just the function called directly -- and a new `TaskPayloadStore` `feme.stage.*` op (`feme.stage.task.payload.store(offset, value)`) canonicalizes a task entry's bounded payload write by its resolved byte offset, reusing `getStageIOBaseAndOffset`'s existing constant-offset walk against `TaskPayloadGlobalVariablePattern`'s own address-space-14 global (H6h). `ValidateStage.cpp`/`WaveUniformity.cpp`/`SIMDize.cpp`'s exhaustive `StageOpKind` switches each gained an explicit (still-unreachable) `TaskPayloadStore` case. New `MeshStageCanonicalizesOutputArrayStore`/`AmplificationStageCanonicalizesTaskPayloadStore` (`CanonicalizeStageTest.cpp`) and `TaskPayloadStoreIsVoidAndOverloadedOnValue` (`StageOpsTest.cpp`) cover this row. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1934/1993 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6h's own 1931/1990 baseline by exactly these 3 new tests. A real `dEQP-VK.mesh_shader.*` re-run (28044 cases) is byte-identical to H6h's own recorded totals (1/337/27706) -- expected and confirmed, since `feme.stage.task.payload.store` has no caller reachable from `EntryWrapperPass` until H6c-a-b wires one in, itself still blocked on H6d. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H6i: measured impact" in VulkanCTSReport.md for the full reproduction) | H6b, H6h | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H6j | ~~**A real `dEQP-VK.mesh_shader.ext.in_out.*` case now fails at `vkQueueSubmit` with "vertex output and fragment input at location 0 disagree on component/row count or type"**, newly exposed by H6g-b-d's own `MeshOutputWrapperPass` catch-all fix: once the 40 cases that fix unblocks progress past compilation, they reach submission-time interface validation between the mesh entry's own per-vertex output and the fragment stage's input, and disagree despite both sides being generated from matching `layout(location=...)` declarations in `vktMeshShaderInOutTestsEXT.cpp`'s own generated GLSL. Root cause not yet isolated: unclear whether the mismatch is a genuine signature-matching bug specific to a mesh-to-fragment interface (as opposed to the already-working vertex-to-fragment and geometry-to-fragment cases), a `MeshOutputWrapperPass`/`EntryWrapperPass` byproduct that changes the reflected output signature's own component/row count from what the fragment stage's input signature expects, or a pre-existing interface-matching gap this is simply the first mesh-stage case to exercise at all~~ (done: root-caused in `CanonicalizeStage.cpp`'s own signature-reflection `addElements` lambda, not in `MeshOutputWrapperPass`/`EntryWrapperPass` as this row's own text speculated -- neither of those two passes touch a `SignatureElement`'s `RowCount` at all. A mesh entry's own plain (non-block) per-vertex/per-primitive `Output` global (e.g. a user-defined `PerVertexEXT` varying such as `layout(location=0) out vec4 v_color[];`) was reflected the same way roadmap H5f's *`Input`*-side treatment reflects a geometry entry's `gl_in[]`-shaped varying: its whole declared type, outer per-vertex array dimension included, becomes `RowCount` via `getStageIORowShape`, with `SignatureElement::RowCountIsVertexArray` flagging that the dimension is a per-vertex array's own extent rather than a real matrix's row count. That flag-and-fold treatment is safe for `Input` because nothing downstream ever links an `Input` element's `RowCount` against another stage's -- but a mesh entry's `Output` element *is* linked, by `Location`, against the fragment stage's own unarrayed input, by both `GraphicsPipeline.cpp`'s `validateStageInterfaces` and `feme::graphics::executeDraws`'s varying-linking loop, and neither consults `RowCountIsVertexArray` when comparing the two `RowCount`s. Left folded in, a 3-vertex mesh entry's own `vec4` per-vertex output was wrongly reflected with `RowCount == 3` (the mesh's own `OutputVertices`) instead of `1` (the fragment-visible per-vertex shape), disagreeing with the fragment input's own `RowCount == 1` at the same location -- exactly this row's own named diagnostic. Fixed with a narrow, surgical change: `CanonicalizeStage.cpp`'s `addElements` lambda now peels a mesh entry's own plain `Output` global's outer per-vertex/per-primitive array dimension off before building its `SignatureElement` (the same peeling a builtin interface block's own per-member element already gets), leaving `RowCountIsVertexArray` at its default `false` to match, rather than folding the dimension in and flagging it the way `Input`'s H5f treatment does. New `CanonicalizeStageTest.MeshStagePeelsPerVertexArrayFromOutputRowCount` reproduces the bug directly at the unit level -- confirmed to fail (`RowCount` wrongly `3`, not `1`) without this fix and pass with it; the pre-existing `MeshStageCanonicalizesOutputArrayStore`/`ThreadsDynamicVertexIndexIntoOutputStore` tests (the latter deliberately tagged a non-`Mesh` stage, confirming the fix does not touch a real per-stage matrix output's own `RowCount`) are unaffected. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1968/2027 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6g-b-d's own 1967/2026 by exactly the 1 new test this row adds. A real `deqp-vk` run against the same 560-case `dEQP-VK.mesh_shader.ext.in_out.*` bucket confirms the fix end to end: grepping the full re-run's combined log (a per-crash resume-loop was needed to get through the whole bucket -- see below) confirms zero remaining occurrences of "disagree on component/row count or type", down from 40. Of those 40 previously-blocked cases, the 40 already-tracked H6g-b-c `spirv_var_NN` cases are unchanged (still Fail, not a duplicate); of the other 40 this row's own fix unblocks, 8 now reach a genuine, clean image-comparison `Fail` (`vktMeshShaderInOutTestsEXT.cpp:1590`, out of this row's own scope), and the remaining 32 crash with `SIGSEGV`/`SIGABRT` inside `feme::graphics::executeDraws` itself (a heap-corruption free of an `llvm::Expected<feme::graphics::StageStorage>`, per a real backtrace), a new, distinct, deeper bug this row's own interface-matching fix simply could not have reached before -- filed as a new sibling row one level under H6 (not nested any deeper under H6g-b, per the standing instruction), H6k. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed to need no change: a pure signature-reflection fix within `VK_EXT_mesh_shader`'s already-advertised scope, touching no feature bit or extension. `FeMeGraphicsDesign.md`'s G6 status paragraph updated with a short note on why this direction could not reuse H5f's `Input`-side flag-and-fold treatment verbatim. See "Roadmap H6j: measured impact" in VulkanCTSReport.md for the full reproduction) | H6g-b-d | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Transforms/CPU/{MeshOutputWrapper,EntryWrapper}.cpp` | P2 |
| H6k | ~~**A real `dEQP-VK.mesh_shader.ext.in_out.*` case now crashes with `SIGSEGV`/`SIGABRT` inside `feme::graphics::executeDraws` itself** (a heap-corruption `free()` of an `llvm::Expected<feme::graphics::StageStorage>`, per a real backtrace through `runPreparedDraw`/`runMeshDraw`/`vkQueueSubmit`), newly exposed by H6j's own interface-matching fix: once the 8-and-32 split of cases that fix unblocks reach real mesh-stage execution for the first time, 32 of them crash the whole `deqp-vk` process rather than completing (cleanly or not) -- unlike H6c-a-a-iii's own previously-tracked arrayed-builtin-block crash (a clean, diagnosable assertion failure), this is silent heap corruption with no FeMe/MLIR diagnostic at all, only a bad `free()` several frames removed from wherever the actual overrun happened. Root cause not yet isolated: needs the same real-ICD-plus-`gdb`/reduced-IR technique this whole H6g-b/H6j chain has used throughout to find which `StageStorage` (`feme/lib/Graphics/StageStorage.{h,cpp}`) allocation/`writeRaw`/`readRaw` call in the mesh-to-fragment `copyLinkedElements`/varying path over- or under-sizes a buffer for a mesh entry's own per-vertex output count, and whether the fix belongs in `StageStorage` itself, `Executor.cpp`'s own mesh-specific `RasterSig`/varying-linking setup, or `MeshOutputWrapperPass`'s own per-vertex output layout~~ (done: re-running the 32 crashing cases individually against a fresh build no longer crashed at all -- H6g-b-c's already-landed `ValidateStagePass` mesh-stage wiring now catches the identical bug at compile time first, as a clean `'feme.stage.output.store' ... row 1 is out of range for element 0` diagnostic, before it can ever reach `executeDraws`. Root-caused in `CanonicalizeStage.cpp`'s `resolveStageIOAccess` constant-offset fold path: glslang unrolls a real mesh entry's small, compile-time-bounded per-vertex output loop into one *constant*-indexed store per vertex (confirmed against the real SPIR-V disassembly embedded in the case's own `.qpa` log), but the fold path only ever routed a constant per-vertex index into `Vertex` for `Input`, never `Output` (a real per-stage matrix output has a legitimate constant-per-row store shape that must not be misrouted, roadmap H6b) -- safe everywhere except `Mesh`, where every plain `Output` array write is per-vertex/per-primitive, never a real matrix (per H6j's own `RowCount`-peeling fix). A first fix attempt added a new, mesh-only `isPerVertexArrayMeshOutputGlobal` helper but deliberately excluded any builtin-interface-block global (`!feme.spirv.MemberDecorations`, e.g. `gl_MeshVerticesEXT`) to avoid regressing an existing multi-member-block test -- this exclusion was too broad, since it also excluded the exact **single-member**-block shape (`gl_MeshVerticesEXT`'s own `gl_Position`) the real failing case's mesh shader actually uses, leaving it to fall through to the old, buggy default path. Corrected: the exclusion is removed entirely, since `resolveOffsetWithinElement` (the same function the already-correct *dynamic*-index path, `getDynamicVertexIndexedAccess`, already relies on for this identical shape) already generically resolves a struct-shaped element's own per-member `ElementID`, whether one member or several -- there was nothing block-specific left to special-case once the constant vertex index itself is folded the same way for both plain arrays and blocks. `CanonicalizeStageTest.ConstantIndexIntoArrayedBuiltinInterfaceBlockIsLeftUnrewritten` (previously asserting this shape stayed deliberately unrewritten) is renamed and rewritten as `FoldsConstantIndexIntoArrayedBuiltinInterfaceBlockMemberStore`, asserting the corrected rewrite instead; a new `FoldsConstantVertexIndexIntoSingleMemberInterfaceBlockOutputStore` test covers the exact single-member-block shape the real CTS case exercises. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1974/2033 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6j's own 1968/2027 by exactly the 2 new/renamed tests' own added assertions this row lands. A real `deqp-vk` re-run of the same 32 formerly-crashing cases together confirms zero crashes and zero row/component-range diagnostics -- 27 now reach a genuine, clean image-comparison `Fail` (`vktMeshShaderInOutTestsEXT.cpp:1590`, out of this row's own scope) and 5 hit the already-tracked H6g-b-c `spirv_var_NN` JIT-symbol class for `task_mesh` variants; a full re-run of the 560-case `dEQP-VK.mesh_shader.ext.in_out.*` bucket confirms this bucket-wide (0 Passed / 80 Failed / 480 Not Supported, split evenly between the same two already-tracked/out-of-scope categories, **zero** crashes or row/component-range diagnostics anywhere). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a pure signature-reflection fix within `VK_EXT_mesh_shader`'s already-advertised scope, touching no feature bit or extension. `FeMeGraphicsDesign.md`'s G6 status paragraph updated with a short note on why the fold needed to cover a builtin interface block, not just a plain per-vertex array. A side re-run of the full `dEQP-VK.mesh_shader.ext.builtin.*` group (37 cases), both immediately before and after this row's own fix via `git stash`, confirms this row is not a regression there (22/37 `Failed` unchanged either way) but does change H6l's own diagnostic shape -- H6l updated below to reflect this. See "Roadmap H6k: measured impact" in VulkanCTSReport.md for the full reproduction) | H6j | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H6l | ~~**`dEQP-VK.mesh_shader.ext.builtin.cull_primitives` reports `feme-graphics-validate-stage: 'feme.stage.output.store' ...` row/component-out-of-range errors**, alongside the already-diagnosed `spirv_var_16` unresolved-global access H6g-b-c's own fix targeted -- newly visible only because H6g-b-c wired `ShaderStage::Mesh` into `ValidateStagePass::run` for the first time; no other case in a real re-run of the full `dEQP-VK.mesh_shader.ext.builtin.*` group (37 cases) hits it, so this is narrow, not the same shape as H6g-b-c's own arrayed-block gap. **Updated by H6k**: H6k's own fix (folding a mesh entry's constant per-vertex index into `Vertex` rather than `Row`, for both plain arrays and builtin interface blocks) was the leading candidate this row's own text named, and a real re-run of `cull_primitives` confirms it changed this row's own diagnostic shape, without closing it: the originally-reported `row N is out of range for element {4,5}` is gone, replaced by a mix of `row`/`component is out of range` errors spread across elements 1-5 (tallied via a real re-run: components 1/2/3/5 each a handful of times, component 4 sixty times, rows 3/4/5 forty-eight times combined) -- a real re-run of the same 37-case group both immediately before and after H6k's own fix (via `git stash`) confirms the group's own pass/fail split is unchanged (22/37 `Failed` either way, not a regression), so this is H6k's fix surfacing a different facet of the same underlying signature/offset-resolution gap rather than a new bug. Root cause still not isolated: needs a real IR reduction of this exact, now-changed case (the same technique H6k's own investigation used) to find which of `cull_primitives`'s own per-primitive builtin outputs (`gl_PrimitivePointIndicesEXT`/`gl_PrimitiveLineIndicesEXT`/`gl_PrimitiveTriangleIndicesEXT`/`gl_CullPrimitiveEXT`-adjacent shapes, per its own name) still resolves to the wrong `Row`/component, and whether the fix belongs in `resolveOffsetWithinElement`'s own per-primitive-block handling or in this shader's own signature reflection~~ (done: root-caused, via the same real-ICD-plus-debug-print technique this whole H6g-b/H6j/H6k chain has used throughout, to `resolveStageIOAccess`'s own constant-vertex-index fold computing `VertexSize` as `DataLayout::getTypeAllocSize(ElemTy)` -- the per-vertex/per-primitive array element's own ABI-alignment-padded allocation size -- rather than the *tightly packed* stride the SPIR-V-to-LLVM conversion actually bakes into the constant byte offsets it embeds. `gl_MeshVerticesEXT`'s own full four-member `gl_PerVertex`-shaped block (`gl_Position`/`gl_PointSize`/`gl_ClipDistance`/`gl_CullDistance`) ends its last member at byte 28 but allocates to 32 (16-byte alignment, driven by its own leading `vec4` member); `gl_PrimitiveTriangleIndicesEXT`'s own `uvec3` element is addressed 12 bytes apart but allocates to 16 (LLVM's own 4-wide SIMD vector padding). Both mismatches misrouted `VertexIdx`/`Residual`, landing the struct-block case on the wrong member entirely (this row's own diagnostics) and silently misdirecting the plain-array case's own `Vertex` operand (an unrelated, uncaught correctness gap `ValidateStage.cpp` has no range check for). Fixed with a new `getPackedMeshElementSize` helper computing the tightly packed size directly (a struct's own last member's offset plus that member's own packed size, a vector's own element count times its own packed scalar size, an array's own element count times its own packed element size, recursing), used only for this fold's own `VertexSize`, leaving `resolveRowComponent`'s separate row/component peeling untouched (no equivalent trailing-alignment gap there). New `FoldsConstantVertexIndexIntoMultiMemberInterfaceBlockOutputStore`/`FoldsConstantVertexIndexIntoPlainVectorArrayOutputStoreWithPadding` unit tests (`CanonicalizeStageTest.cpp`) cover the two shapes directly, both confirmed to fail without this fix and pass with it; the pre-existing single-member-block/plain-array/multi-`ElementID`-block tests (whose own fixtures happen to need no ABI padding) are unaffected. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1976/2035 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6k's own 1974/2033 by exactly the 2 new tests this row adds. A real `deqp-vk` re-run of `cull_primitives` confirms zero remaining `feme-graphics-validate-stage` occurrences (down from 16); the case still does not pass, now blocked by a distinct, newly surfaced gap (`StageStorage.cpp`'s own long-standing "32-bit scalars only" scope limit, reached for the first time by `gl_CullPrimitiveEXT`'s own `bool` element once this row's fix lets compilation clear validation) filed as a new sibling row, H6m. A real re-run of the full `dEQP-VK.mesh_shader.ext.builtin.*` group (37 cases) confirms this row is not a regression (22/37 `Failed`, byte-identical to H6k's own recorded split, zero `feme-graphics-validate-stage` occurrences anywhere in the bucket), and a re-run of the 1957-case `draw_sample.txt` regression sample confirms no regression there either (byte-identical to every prior recorded run). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a pure lowering-correctness fix within `VK_EXT_mesh_shader`'s already-advertised scope, touching no feature bit or extension. See "Roadmap H6l: measured impact" in VulkanCTSReport.md for the full reproduction) | H6g-b-c, H6k | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H6m | ~~**`vkQueueSubmit` fails a real `dEQP-VK.mesh_shader.ext.builtin.cull_primitives` case with `"stage element N has a 1-bit scalar; only 32-bit elements are implemented yet"`** (`StageStorage.cpp`'s own long-standing "32-bit scalars only" scope limit, `StageStorage.h`), newly reachable only because H6l's own fix lets this case clear `feme-graphics-validate-stage` for the first time. `gl_CullPrimitiveEXT` (`VK_EXT_mesh_shader`'s per-primitive cull builtin) is a SPIR-V/GLSL `bool` (`i1`), the element this case's own error names; nothing mesh-shading-specific about the gap itself -- any stage-IO element with a non-32-bit scalar type hits the same generic check (`StageStorage.cpp`, `SignatureElement::BitWidth != 32`) -- but this is the first real case in this project's own CTS coverage to reach it in the stage-IO path specifically (roadmap E29f already closed the related, but distinct, `Workgroup`-storage addressable-`i1` gap this generic limit does not cover). Needs a real IR reduction of this exact case (the same technique this whole H6g-b/H6j/H6k/H6l chain has used throughout) to scope a fix: whether `StageStorage`'s own per-element layout (`InvocationStride`/`ComponentStride`/`RowStride`, all hard-coded to a 4-byte scalar today) can widen to a 1-bit (or 1-byte, packed) scalar cleanly, or whether a `bool` stage-IO element should instead canonicalize to an ordinary 32-bit integer at the `CanonicalizeStage.cpp`/SPIR-V-to-LLVM boundary before ever reaching `StageStorage` at all (mirroring how a real GPU's own driver typically represents a shader-visible `bool` as a 32-bit value in memory)~~ (done: a real IR reduction -- a mesh entry's `gl_MeshPrimitivesEXT[i].gl_CullPrimitiveEXT = %v` store, `BuiltIn` 4485, member of a `PerPrimitiveEXT` interface block -- confirmed the second option: `getComponentType` now canonicalizes SPIR-V's `OpTypeBool`/LLVM `i1` to `{Bool, 32}` rather than `{SInt, 1}`, and `loadStageIOValue`/`storeStageIOValue`'s own terminal-scalar case zero-extends/truncates the actual `i1` value across the `feme.stage.input.load`/`output.store` boundary to match, so `StageStorage::buildStageStorage` only ever sees a plain 32-bit element -- `StageStorage.cpp`/`.h` needed no change at all, since the existing `SignatureComponentType::Bool` -> `StageLayoutScalarKind::Bool` mapping (`scalarKindFor`) already anticipated a 32-bit-wide boolean scalar, just never a producer of one. `CanonicalizesBoolPerPrimitiveOutputStoreToA32BitElement` (`CanonicalizeStageTest.cpp`) covers the whole boundary directly -- the reflected `{Bool, 32}` element, the `zext`-widened store operand, and a direct `buildStageStorage` call on the resulting signature succeeding where it previously errored -- confirmed to fail without this fix and pass with it. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1977/2036 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6l's own 1976/2035 by exactly the 1 new test this row adds. A real `deqp-vk` re-run of `cull_primitives` confirms the `"1-bit scalar"` compile-time error is gone: `vkQueueSubmit` now succeeds and the case reaches actual rendering, failing instead on a clean pixel-comparison mismatch (every pixel renders black instead of blue, i.e. `gl_CullPrimitiveEXT` compiles and stores correctly but the CPU rasterizer does not yet honor it to skip a culled primitive -- a distinct, later rendering-correctness gap, not filed as a new row since it is not blocking any other case and is squarely inside "bounded payload/output limits reported truthfully" scope already tracked by this milestone's own header). A real re-run of the full `dEQP-VK.mesh_shader.ext.builtin.*` group (37 cases) confirms this row is not a regression (22/37 `Failed`, byte-identical to H6l's own recorded split via a `git stash` before/after comparison). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a pure lowering-correctness fix within `VK_EXT_mesh_shader`'s already-advertised scope, touching no feature bit or extension. `FeMeGraphicsDesign.md`'s own "32-bit scalars only" status note is extended with a short deviation note on why a `bool` stage-IO element is canonicalized rather than genuinely widening `StageStorage`'s own layout. See "Roadmap H6m: measured impact" in VulkanCTSReport.md for the full reproduction) | H6l | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H7 | **The optional core 1.0 graphics feature bits.** `PhysicalDeviceInfo.cpp` reports exactly three `VkPhysicalDeviceFeatures` bits `VK_TRUE` (`robustBufferAccess`, `dualSrcBlend`, `textureCompressionASTC_LDR`, lines 349-373); the other ~52 are all `VK_FALSE`. Each is *optional* for a 1.4 submission, so none blocks a conformance claim — but each is a block of mandatory-list cases reported `NotSupported`, and several are cheap on a software device (`imageCubeArray`, `independentBlend`, `fillModeNonSolid`, `depthClamp`, `depthBiasClamp`, `depthBounds`, `wideLines`/`largePoints` once F5's line rasterization lands, `sampleRateShading`, `alphaToOne`, `logicOp`, `occlusionQueryPrecise`, `multiDrawIndirect`, `drawIndirectFirstInstance`, `vertexPipelineStoresAndAtomics`, `fragmentStoresAndAtomics`, `shaderClipDistance`, `shaderCullDistance`, `samplerAnisotropy`, `shaderStorageImage*`). Split into sub-rows per cluster when assigned; do **not** land as one commit (broken down below the same way H4/H5/H6 were, after a full survey of every candidate bit's own real implementation status: H7a closes the first, lowest-risk cluster -- five bits the executor/pipeline layer already genuinely implements and simply never advertised; H7b-H7j each track one remaining cluster that needs real new work first, none of it started yet -- milestone remains open, depending on H7b-H7j) | G-track per cluster | `feme/lib/Vulkan/PhysicalDeviceInfo.cpp`, `feme/lib/Graphics` | P1 |
| H7a | ~~**`independentBlend`/`logicOp`/`occlusionQueryPrecise`/`multiDrawIndirect`/`drawIndirectFirstInstance`**: a real-code survey of H7's own ~20 named candidate bits found these five already fully backed by existing, working code, just never advertised -- `GraphicsPipeline.cpp`'s `translateColorBlendState` already stores one `BlendState`/`logicOpEnable`/`logicOp` independently per color attachment (not shared from attachment 0), `Executor.cpp`'s `applyLogicOp`/`mergeColor` already implement every `VkLogicOp` value, `QueryPool.cpp`'s occlusion queries (roadmap H2f) already accumulate a real per-sample passed count rather than a boolean any-samples-passed, and `CommandBuffer.cpp`'s `readIndirectDraws`/`readIndirectMeshDraws` already loop over an arbitrary indirect `DrawCount` and already copy a nonzero `firstInstance` through unconditionally~~ (done: all five flipped to `VK_TRUE` in one commit, with `maxDrawIndirectCount` raised from the spec-mandated `multiDrawIndirect == false` floor of `1` to `UINT32_MAX` to match -- `readIndirectDraws`'s own loop has no smaller real ceiling of its own. `PhysicalDeviceInfoTest.cpp`'s existing exhaustive "only these bits are advertised" test extended to cover all five, plus a new `MaxDrawIndirectCountAllowsMultiDraw` test. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1978/2037 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6m's own 1977/2036 by exactly the 1 new test this row adds. Real `deqp-vk` re-runs of `dEQP-VK.pipeline.monolithic.*logic_op*` (976 cases), `dEQP-VK.query_pool.occlusion_query.*` (441 cases), and a combined multi-draw-indirect/`first_instance` sample (740 cases) each confirm, via a `git stash` before/after comparison, that flipping these bits legitimately moves a real block of cases from `NotSupported` to actual execution (192/102/188 cases respectively) with zero regression in any previously-passing case (`Passed` count unchanged, `0` in every one of the three groups both before and after) -- every newly-attempted case now fails cleanly on a separate, already-tracked, pre-existing gap instead (chiefly C8's own matrix/aggregate `feme-cpu-simdize` legalization limitation for the logic-op group, and a generic `vkCreateGraphicsPipelines` `VK_ERROR_INITIALIZATION_FAILED` content gap, not yet independently triaged, for the other two). The 1957-case `draw_sample.txt` regression sample also shifts from 153/1790 to 179/1764 Failed/NotSupported with `Passed` unchanged at 14, consistent with the same pattern. `FeMeVulkanDesign.md` gained a new "Optional core 1.0 feature bits" status section. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: no extension or Vulkan-1.1+ feature struct is touched, only five Vulkan-1.0-core `VkPhysicalDeviceFeatures` bits. See "Roadmap H7a: measured impact" in VulkanCTSReport.md for the full reproduction) | H7 | `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P1 |
| H7b | ~~**`imageCubeArray`**: `Image.cpp` already accepts `VK_IMAGE_VIEW_TYPE_CUBE_ARRAY` at view-creation time, but `CommandBuffer.cpp`'s descriptor materialization only built a 2D, base-layer-0 image descriptor for shader access -- a cube-array view's remaining layers/faces never reached a shader-visible descriptor~~ (done: `materializeImageDescriptor` now widens to `Texture2DArray`/`TextureCube`/`TextureCubeArray` views and a nonzero `baseArrayLayer` (a per-mip adjusted subresource layout table, `Offset` shifted by `BaseArrayLayer * SlicePitch`, the same per-layer addressing `Image::texelPointer` already uses, reusing the insight `RenderPass.cpp`'s `resolveAttachmentView` already relies on for render-target attachments), confirmed via a real end-to-end dispatch test (a `VK_IMAGE_VIEW_TYPE_2D` view with `baseArrayLayer=1` over a two-layer image now samples layer 1's own texels rather than layer 0's or an all-zero read). A real code survey while implementing it found a second, more fundamental blocker this row's own text did not name -- `SPIRVResourceLowering.cpp`'s `classifySampledImage2DHandle`/`ResourceLowering.cpp`'s `classifyImageHandle` both still hard-restricting shader-visible sampled-image handle classification to `Dim=2D`/`Texture2D` only -- broken out and closed separately as H7b-a, which this row's own `imageCubeArray` flip depended on. With both fixes in place, `PhysicalDeviceInfo.cpp` now advertises `imageCubeArray = VK_TRUE`, confirmed via `PhysicalDeviceInfoTest.cpp`'s extended exhaustive feature-bit test and a real end-to-end `CommandBufferTest.cpp` dispatch (`CubeArraySampledImageDispatchTest`) sampling a bound `TextureCubeArray` through H7b-a's own widened pipeline. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1999/2058 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). See H7b-a's own row for the deeper fix's full detail and "Roadmap H7b/H7b-a: measured impact" in VulkanCTSReport.md for the real `deqp-vk` reproduction) | H7 | `feme/lib/Vulkan/CommandBuffer.cpp`, `feme/lib/Vulkan/Image.cpp` | P2 |
| H7b-a | ~~**Shader-visible cube(array)/2D-array image sampling**: `SPIRVResourceLowering.cpp`'s `classifySampledImage2DHandle` and `ResourceLowering.cpp`'s `classifyImageHandle` both hard-restrict sampled-image handle classification to `Dim=2D`/`Texture2D`, non-arrayed only, rejecting every `Cube`/`CubeArray`/`2DArray`-typed shader binding at pipeline-creation time (gracefully, per this project's own "unsupported ops fail pipeline creation" policy, but before any descriptor lookup is reached). Needs, together: (1) widened handle classification accepting `Cube`/`CubeArray`/`2DArray` dimensions; (2) a cube-face-selection coordinate transform (the classic "major axis" algorithm converting `OpImageSample`'s 3-component direction vector, or 4-component with array layer for `CubeArray`, into a face index plus 2D UV) not implemented anywhere in the codebase yet; (3) a widened CPU runtime texel-fetch primitive (`femeRTFetchTexel2D`, `feme/runtime/CPU/FeMeRuntimeCPU.c`) accepting an array-layer/face parameter, which it lacks entirely today~~ (done: `femeRTFetchTexel2D`/`femeRTSamplePoint2D`/`femeRTSampleLinear2D` (`FeMeRuntimeCPU.c`) now take a `Layer` parameter (existing exported entry points pass a constant `0`, preserving their own ABI unchanged), and five new exported entry points cover the new shapes (`feme.cpu.image.sample.2darray.v4f32`/`.load.2darray.v4f32`/`.v4i32`/`.sample.cube.v4f32`/`.sample.cubearray.v4f32`), including a new `femeRTSelectCubeFace` "major axis" helper matching Vulkan's own cube-face/array-layer order (`+X=0,-X=1,+Y=2,-Y=3,+Z=4,-Z=5`) and always clamping a cube face's own UV regardless of the bound sampler's address mode. `ImageCalls.h`/`.cpp` gained matching `ImageCallKind`/`create*`/`matchImageCall` support for all five. `SPIRVResourceLowering.cpp` widened `classifySampledImage2DHandle` to accept `Dim ∈ {2D, Cube}` and any `Arrayed` value, computing a new `ImageShape` (`Plain2D`/`Array2D`/`Cube`/`CubeArray`) carried on `HandleClassification`/`BoundHandle`/`RangeEntry`, with `hasOnlySupportedImageUses` checking each shape's own coordinate width and rejecting `OpImageFetch` outright against `Cube`/`CubeArray` (SPIR-V never legalizes it there). `ResourceLowering.cpp`'s `classifyImageHandle`/`lowerImageAccesses` got the identical DXIL-side mirror (`Texture2DArray`/`TextureCube`/`TextureCubeArray` `dxil::ResourceKind` values, `TextureCube`/`TextureCubeArray` rejecting `Load` the same way, since neither has a `Load` method in HLSL/DXIL). New unit tests cover every phase: 7 new `ImageSamplingTest.cpp` runtime cases, 5 new `SPIRVResourceLoweringTest.cpp` cases, 7 new `ResourceLoweringTest.cpp` cases (the first dedicated image-lowering coverage that file had at all), and a new end-to-end `CommandBufferTest.cpp` dispatch (`CubeArraySampledImageDispatchTest`) sampling a real bound `TextureCubeArray` through the whole chain. No `SampleCmp` counterpart added for any new shape: `SampleCmp2D` itself is already unwired on both lowering sides for plain 2D, a pre-existing, unrelated gap, kept out of scope. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 1999/2058 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H6m's own 1977/2036 by the 22 new tests this row (plus H7b's own flip) adds. A real `deqp-vk` re-run of `dEQP-VK.image.image_size.cube_array.*` (12 cases, previously `NotSupported`) now executes with 0 regressions elsewhere; `Vulkan14FeatureInventory.md` updated to flip `imageCubeArray`. `VulkanExtensionInventory.md` confirmed no change needed: no extension is touched, only a Vulkan-1.0-core feature bit and its own supporting pipeline stages. `FeMeVulkanDesign.md` gained a new status note on the cube(array)-over-2D-array addressing convention and the cube-face-selection algorithm choice. See "Roadmap H7b/H7b-a: measured impact" in VulkanCTSReport.md for the full reproduction) | H7b | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/lib/Transforms/CPU/ResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c` | P2 |
| H7c | ~~**`fillModeNonSolid`**: `GraphicsPipeline.cpp` explicitly rejects any `VkPipelineRasterizationStateCreateInfo::polygonMode` other than `VK_POLYGON_MODE_FILL` at pipeline-creation time; `Executor.cpp` has no LINE/POINT wireframe-style rasterization path at all (distinct from the already-supported `VK_PRIMITIVE_TOPOLOGY_{LINE,POINT}_LIST` topologies, which rasterize a real line/point primitive, not a filled polygon's edges/vertices). Needs a new non-solid rasterization mode in the triangle path, likely reusing F5's own line-width/stipple machinery for the LINE case~~ (done: a new `feme::graphics::PolygonMode` enum (`Fill`/`Line`/`Point`) and `RasterState::Polygon` field let `translateRasterState` map all 3 real `VkPolygonMode` enumerators instead of rejecting anything but `FILL` (a new `mapPolygonMode()` helper, mirroring `mapCullMode()`, rejects only the never-advertised `VK_POLYGON_MODE_FILL_RECTANGLE_NV`); no pipeline-cache-key or dynamic-state change was needed, since the cache key already excludes other pure-runtime raster fields like `LineMode` and `ResolvedRaster`'s full-struct copy already carries the new field through. `Executor.cpp`'s solid-triangle assembly loop gained a `PolygonMode` branch, placed after the existing cull-mode check (a real `VkCullModeFlags` only ever applies to "a polygon", so the original triangle is culled by its own winding before any decomposition) and before the CW/CCW winding-fix swap: `PolygonMode::Line` decomposes a triangle into its own 3 edges via a new `emitLineSegment` helper (extracted, behavior-preserving, from the pre-existing topology-level Line `RasterClass` path), reusing F5's own `LineMode`/`LineWidth`/stipple fields unmodified per `VK_KHR_line_rasterization`'s own spec text extending those fields to "any line segment ... drawn ... when polygonMode is VK_POLYGON_MODE_LINE" (each of the 3 edges starts stipple continuity at arc length 0, since a triangle's own edges have no Vulkan-defined "connected strip" rule unlike a real line strip's segments); `PolygonMode::Point` emits the same 3 vertices via a new `emitPointQuad` helper (extracted the same way from the topology-level Point `RasterClass` path), reusing the existing fixed-size point quad expansion. New `GraphicsPipelineTest.cpp` (`TranslatesNonFillPolygonModes`) and `ExecutorTest.cpp` (`PolygonModeLineRastersOnlyTheTrianglesThreeEdges`/`PolygonModePointRastersOnlyTheTrianglesThreeVertices`, using a triangle whose 3 vertices land exactly on pixel centers in a 4x4 attachment so each mode's expected lit-pixel set is hand-derivable and checked precisely) cover this row; `PhysicalDeviceInfoTest.cpp`'s exhaustive feature-bit test extended to require `fillModeNonSolid`. `PhysicalDeviceInfo.cpp` now advertises `fillModeNonSolid = VK_TRUE`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2002/2061 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). See "Roadmap H7c: measured impact" in VulkanCTSReport.md for the real `deqp-vk` reproduction) | F5 | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7d | ~~**`depthClamp`/`depthBiasClamp`/`depthBounds`**: `GraphicsPipeline.cpp` explicitly rejects `depthClampEnable`, depth bias entirely (`depthBiasEnable`), and `VkPipelineDepthStencilStateCreateInfo::depthBoundsTestEnable` ("the depth bounds test is not implemented") at pipeline-creation time; none of the three has any per-fragment implementation in `Executor.cpp`'s depth/stencil test path. Three related but independently gated depth-pipeline features, grouped since fixing depth-bias admission is a likely prerequisite for `depthBiasClamp` specifically~~ (done: `RasterState` gained `DepthClampEnable`/`DepthBiasEnable`/`DepthBiasConstantFactor`/`DepthBiasClamp`/`DepthBiasSlopeFactor`, and `DepthState` gained `BoundsTestEnable`/`MinDepthBounds`/`MaxDepthBounds` (`Pipeline.h`); `GraphicsPipeline.cpp`'s `translateRasterState` splits `depthClampEnable`/`depthBiasEnable` out of the old combined `rasterizerDiscardEnable`-plus-both rejection (rasterizer discard alone stays rejected) and `translateDepthStencilState` drops the `depthBoundsTestEnable` rejection entirely, both now translating real state, including two new independently-dynamic states (`VK_DYNAMIC_STATE_DEPTH_BIAS`/`_BOUNDS`, alongside the pre-existing but previously-inert `VK_DYNAMIC_STATE_DEPTH_BOUNDS_TEST_ENABLE` now finally consumed by `buildExecutorPipeline`'s `ResolvedRaster`/`ResolvedDepth` merge). `CommandBuffer.cpp`'s `vkCmdSetDepthBias`/`vkCmdSetDepthBounds` (previously no-op stubs) now record and replay real dynamic state. `Executor.cpp` implements all three honestly: depth clamp skips `clipTriangle`'s last 2 (near/far Z) of 7 clip planes, and clamps the *interpolated* per-fragment depth (not the per-vertex depth) to the viewport's own `[minDepth, maxDepth]` range at the single barycentric-interpolation choke point shared by the triangle, line, and point paths (`ScreenTriangle` carries `DepthClampLo`/`DepthClampHi` from its owning viewport for this purpose) -- an initial per-vertex-before-interpolation clamp was found, via real `deqp-vk` reproduction, to smear a false gradient across a primitive with mixed in-range/out-of-range vertex depths instead of Vulkan's own required uniform clamped result, and was corrected before this row's own real CTS pass; depth bias computes `depthBiasConstantFactor * r + depthBiasSlopeFactor * maxSlope` (clamped via `depthBiasClamp`'s sign-dependent min/max/no-clamp semantics) once per triangle via a new `depthBiasR` helper (`2^-16`/`2^-24` for `D16_UNORM`/`D24_UNORM_S8_UINT`, an ULP-based `ldexp` formula for `D32_FLOAT`) and applies it to all 3 vertices' depths ahead of H7c's own `PolygonMode` branch, so Fill/Line/Point modes all inherit it; the depth bounds test runs at the very start of `testDepthStencil`, before the stencil test, per the Vulkan spec's fixed-function order, comparing the value already stored in the depth attachment (not the incoming fragment's own new depth) against `[MinDepthBounds, MaxDepthBounds]`, discarding outright on failure. New `GraphicsPipelineTest.cpp` tests (`TranslatesDepthClampAndBiasState`/`DynamicDepthBiasOverridesStaticState`/`TranslatesDepthBoundsState`/`DynamicDepthBoundsOverridesStaticState`) cover pipeline-creation translation; new end-to-end `DrawTest.cpp` tests (`DepthClampKeepsFragmentsBeyondTheFarPlane`/`DepthBiasShiftsOverlappingDepth`/`DepthBoundsTestRejectsOutOfRangeFragments`) cover the executor path. `PhysicalDeviceInfoTest.cpp`'s exhaustive feature-bit test extended to require all three; `PhysicalDeviceInfo.cpp` now advertises `depthClamp`/`depthBiasClamp`/`depthBounds = VK_TRUE`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2011/2070 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7c's own 2002/2061 by exactly the 9 new tests this row adds (7 for the original pipeline-translation/executor coverage, plus 2 more added while root-causing the real `deqp-vk` failures below: `FragmentWrapperTest.cpp`'s `ResolvesRequestedPositionComponentNotAlwaysX` and `DrawTest.cpp`'s `DepthClampAppliesAfterInterpolationNotBeforeIt`). Real `deqp-vk` reproduction against the CTS checkout also found and fixed a second, unrelated, pre-existing bug this row's own coverage newly exercised: `FragmentWrapper.cpp`'s `lowerFragmentInputLoad` resolved a vector system-value load (e.g. `gl_FragCoord.z`) using only its signature element's I/O-packing `FirstComponent` (always 0 for a whole-`vec4` builtin), ignoring the load intrinsic's own per-call `Component` operand the ordinary varying-load path already consumed correctly -- silently collapsing any non-`.x` system-value component read to `.x`. See "Roadmap H7d: measured impact" in VulkanCTSReport.md for the real `deqp-vk` reproduction, before/after totals, and the two follow-on gaps (H7k, H7l) this row's own CTS run surfaced and broke out separately) | H7 | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7e | ~~**`wideLines`/`largePoints`**: F5 already threads a real, variable `LineWidth` through the line rasterizer's own quad-expansion path (`Executor.cpp`), so `wideLines` is likely close -- `lineWidthRange`/`lineWidthGranularity` above stay at the degenerate `[1.0, 1.0]`/`1.0` floor and need raising to match whatever this rasterizer can honestly support. `largePoints` needs real new work: point-primitive expansion is still hardcoded to a fixed 1-pixel size, unlike the line path F5 already generalized~~ (done: added `SignatureSystemValue::PointSize`, mapped from SPIR-V `BuiltIn` 1 by `CanonicalizeStage.cpp`'s `getSystemValueForBuiltIn` (previously deliberately unmodeled); `RasterVertex` gained a `PointSize` field populated from a new optional `VSPointSize` signature-element lookup, defaulting to `1.0` (Vulkan's own documented fallback for an unwritten `gl_PointSize` output) when absent; `emitPointQuad`'s half-extent now derives from `Vtx.PointSize` clamped to `[1.0, RasterState::MaxPointSize]` (a new `RasterState` field, `64.0` by default, a bare literal cross-referenced by comment with `PhysicalDeviceInfo.cpp`'s `pointSizeRange[1]` rather than a shared constant, following this codebase's existing `LineWidth` precedent -- the Vulkan layer depends on the Graphics layer, not the reverse, so `Executor.cpp` cannot include `PhysicalDeviceInfo.h`). `PhysicalDeviceInfo.cpp` raises `lineWidthRange[1]`/`pointSizeRange[1]` from the degenerate `1.0` floor to `64.0` and flips `wideLines`/`largePoints` to `VK_TRUE`; `wideLines` needed no executor change, since F5 already generalized `LineWidth`. New unit tests: `CanonicalizeStageTest.MapsSPIRVPointSizeBuiltInToSystemValue`; `ExecutorTest.{RendersAPointAtItsWrittenPointSize,ClampsPointSizeToTheDevicesMaximum,ClampsPointSizeUpToTheMinimum}`; `PhysicalDeviceInfoTest.LineWidthAndPointSizeRangesSupportWideLinesAndLargePoints`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2016/2075 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7d's own 2011/2070 by exactly the 5 new tests this row adds. Real `deqp-vk` reproduction (126-case sample covering every group touching `wideLines`/`largePoints`) found 31 newly-passing cases and 0 regressions: all 12 failures in the sample are pre-existing -- 2 are the already-tracked H7k grid-alignment gap, 9 are a previously-known-but-untracked generic vertex-side signature-metadata pipeline-construction gap (mirroring roadmap H3a's own fragment-side fix, broken out separately below as H7m), and 1 is the already-tracked mesh-shading milestone-6 deviation. See "Roadmap H7e: measured impact" in VulkanCTSReport.md for the full reproduction) | F5 | `feme/lib/Graphics/Executor.cpp` | P2 |
| H7f | ~~**`sampleRateShading`/`alphaToOne`**: the executor always invokes the fragment shader once per covered fragment/pixel, never once per covered sample, so `VkPipelineMultisampleStateCreateInfo::sampleShadingEnable`/`minSampleShading` have no per-sample invocation loop to honor yet; `alphaToOneEnable` has no handling at all in the blend path (distinct from the already-implemented alpha-to-coverage handling). Grouped since both are `VkPipelineMultisampleStateCreateInfo` fields reached by the same pipeline-state translation code~~ (done, in three stages across three follow-on milestones: `alphaToOneEnable` translation (`GraphicsPipeline.cpp`) and executor handling (`Executor.cpp`'s `processTile` forcing every color attachment's alpha to `1.0`) were confirmed conformant against a real `dEQP-VK.pipeline.monolithic.multisample.alpha_to_one.*` re-run (4/4 feme-supported-sample-count cases passing) in this row's own first pass, flipping `alphaToOne` to `VK_TRUE` immediately; `sampleRateShading`'s own translation and executor plumbing (a per-sample shade/dispatch/merge loop, narrowing each pass's invocation copy to one sample) were likewise implemented and unit-tested in this row's own first pass, but blocked on two further, independently-discovered gaps -- H7o's two pipeline-creation-time compiler bugs (a `SIMDize.cpp` divergent-vector-load producer gap and a `RootConstantLowering.cpp`/`SPIRVPushConstantLowering.cpp` metadata-copy bug), then H7p's two further bugs (`Executor.cpp`'s per-sample loop not varying `gl_FragCoord` per sample position, and `CommandBuffer.cpp`'s `buildSubpassInputHeap` using the wrong sample count for subpass-input attachments read at a different sample count than the reading pipeline) -- with all four now fixed, a real re-run of every feme-supported-sample-count `min_sample_shading_enabled`/`min_sample_shading_disabled` case (7/7) passes, so `sampleRateShading` also now flips to `VK_TRUE`. `alphaToCoverageEnable` remains rejected and untracked-with-a-feature-bit, tracked separately as H7n, not part of this row's own scope. See "Roadmap H7f: measured impact", "Roadmap H7o: measured impact", and "Roadmap H7p: measured impact" in VulkanCTSReport.md for the full reproduction of all three stages) | H7 | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7n | ~~**`alphaToCoverageEnable` has no implementation.** `GraphicsPipeline.cpp` rejects `VkPipelineMultisampleStateCreateInfo::alphaToCoverageEnable` at pipeline-creation time; found during H7f's own investigation that the roadmap's original H7f text calling this "already-implemented" was inaccurate -- an exhaustive search confirms no alpha-to-coverage handling exists anywhere in `Executor.cpp`'s blend/coverage path. Needs a real per-sample coverage mask update (clearing a sample's coverage bit when the shaded alpha at that sample falls below its own threshold, before the existing multisample-resolve averaging step) -- distinct from, and orthogonal to, H7f's own `alphaToOneEnable`/`sampleRateShading` work, which is why it is broken out as its own row rather than folded into H7f~~ (done: `alphaToCoverageEnable` translation (`GraphicsPipeline.cpp`, no feature-bit gate exists for this field in the spec, unlike `alphaToOneEnable`) and a new per-lane `BaseCoverage` mask in `Executor.cpp`'s color-merge loop -- a per-sample threshold `T_S = (S + 0.5) / SampleCount`, clearing a sample's coverage bit whenever the shaded fragment's location-0 alpha (looked up via a dedicated `FSAlphaToCoverage` element, independent of `FSColors[0]`, to also cover the "unused color attachment" CTS shape where location 0 is not bound to a real attachment) falls below it, ANDed into the existing raw coverage mask ahead of the depth/stencil test -- reproduce every one of the CTS's own tolerance-check geometry/alpha combinations (`round(alpha * SampleCount)` covered samples). `UseEarlyDepthStencil` now also requires `!getAlphaToCoverageEnable()`, since the mask depends on the fragment stage's own output. Real `dEQP-VK.pipeline.monolithic.multisample.alpha_to_coverage.*` re-run: 12/12 feme-supported cases pass. Two more CTS groups exercise the same feature but surfaced two further, independent, pre-existing gaps unrelated to the coverage-mask logic itself, broken out below as H7q (fixed) and H7r (not yet fixed): `alpha_to_coverage_no_color_attachment.*` failed until H7q's own render-target-sample-count fix; `alpha_to_coverage_unused_attachment.*` still fails on H7r's own unsupported `VK_FORMAT_R5G6B5_UNORM_PACK16`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2026/2085 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7p's own 2024/2083 baseline by exactly the 2 new tests this row adds (`GraphicsPipelineTest`'s `TranslatesAlphaToCoverageState`, `ExecutorTest`'s `AlphaToCoverageEnableGeneratesPerSampleCoverageFromAlpha`; H7q's own regression test is counted under H7q below). `Vulkan14FeatureInventory.md` updated (a confirmatory note only -- no feature bit exists for this field). `VulkanExtensionInventory.md` confirmed no change needed. `FeMeVulkanDesign.md`'s H7 status paragraph updated. See "Roadmap H7n: measured impact" in VulkanCTSReport.md for the full reproduction | H7f | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7q | ~~**A `VkRenderPass` subpass with zero color attachments silently leaves its render target's derived sample count at the single-sample default**, newly reachable only because H7n's own real CTS re-run reached `alpha_to_coverage_no_color_attachment.*` (a `RENDER_TYPE_DEPTHSTENCIL_ONLY` render) for the first time and hit `vkCreateGraphicsPipelines`'s own "the pipeline's rasterization sample count disagrees with its render target's" rejection on every genuinely multisampled case. Root cause: `GraphicsPipeline.cpp`'s `getRenderTargets` only ever sets `Targets.SampleCount` inside the loop over `Subpass.ColorAttachments`, which never executes when that list is empty~~ (done: added a fallback in `getRenderTargets` -- when a subpass has no color attachments at all, `Targets.SampleCount` is instead derived from its depth/stencil attachment's own `AttachmentDescription::SampleCount`. New regression test, `GraphicsPipelineTest.cpp`'s `AcceptsMultisampledZeroColorRenderPass` (a real 4-sample, zero-color, depth-only `VkRenderPass`, confirmed to fail without the fix). Real `dEQP-VK.pipeline.monolithic.multisample.alpha_to_coverage_no_color_attachment.*` re-run: 3/3 feme-supported cases now pass (up from 0, all failing pipeline creation before this fix). A broader `dEQP-VK.pipeline.monolithic.multisample.*` sweep (10576 cases) shows only pre-existing, unrelated failures elsewhere (root-constant/push-constant coverage gaps, a JIT symbol-materialization gap for `frag_depth` export, an unsupported large-resource-array binding shape, and H7r's own `R5G6B5` format gap) -- none newly introduced by this fix. `ninja check-feme` passes in full, 2026/2085 (0 `Failed`), counted together with H7n's own row above (this fix and its test were made in service of confirming H7n's own real CTS re-run, so both land in the same measured-impact section). See "Roadmap H7n: measured impact" in VulkanCTSReport.md | H7n | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P2 |
| H7r | ~~**`VK_FORMAT_R5G6B5_UNORM_PACK16` (and likely other packed 16-bit formats) has no support anywhere in the image/format layer**, discovered via H7n's own real CTS re-run of `dEQP-VK.pipeline.monolithic.multisample.alpha_to_coverage_unused_attachment.*` (`AlphaToCoverageColorUnusedAttachmentInstance`'s own hard-coded color format), which fails every feme-supported-sample-count case at `vkCreateImage` time with `VK_ERROR_FORMAT_NOT_SUPPORTED`. Unrelated to alpha-to-coverage's own coverage-mask logic (confirmed: the identical mask computation already passes 12/12 real cases against ordinary formats in H7n's own main group), purely a pre-existing, generic packed-format gap in the format-mapping/image layer. Needs a real survey of which packed 16-bit formats (`R5G6B5`, `R5G5B5A1`, `B5G6R5`, etc.) are worth adding, then real support through the format-mapping, image storage, and sampling/blending paths~~ (done: surveyed the real CTS's own `vktApiImageClearingTests.cpp` `colorImageFormatsToTest` list as an authoritative "worth adding" list, selecting the 7 core-1.0 packed 16-bit formats missing from `ResourceFormat` -- `R4G4B4A4`/`B4G4R4A4`/`R5G6B5`/`B5G6R5`/`R5G5B5A1`/`B5G5R5A1`/`A1R5G5B5_UNORM_PACK16` -- and excluding `R4G4_UNORM_PACK8` as out of the "16-bit" scope. Added as real, working color-attachment formats (`mapVkFormat`/`formatElementSize`/`isSupportedColorAttachmentFormat`/`getFormatInfo`/`packClearColor`/`unpackColor`, `ImageFixture.cpp`), matching `A1B5G5R5_UNORM`'s precedent rather than `A4R4G4B4_UNORM`'s recognized-only one, since the blocking CTS case needs a real attachment. Appended at the very end of the `ResourceFormat` enum (after `ASTC_12x12_SFLOAT`), not grouped near their thematic siblings, because `FeMeRuntimeCPU.c`'s image-sampling tables switch on `ResourceFormat`'s raw ordinal via hard-coded integer case labels, not symbolic names -- inserting anywhere else would silently renumber and break them; none of the 7 gets a sampling case, so `SAMPLED_IMAGE_BIT` correctly stays unset via `formatFeatureFlags`'s existing `default:` fallthrough. Real `dEQP-VK.api.image_clearing.core.clear_color_image.*`/`clear_color_attachment.*` re-run for all 7 formats: 0 failures attributable to this change (`clear_color_image`: 0 failed across all 7, some pre-existing `NotSupported` for specific 3D extents/multi-subresource shapes shared by every format including long-established ones; `clear_color_attachment`: the only failures are `cube_layers`/`multiple_layers`/MSAA-`sample_count` variants, confirmed identical for `r8g8b8a8_unorm` and pre-existing `a1b5g5r5_unorm_pack16` too -- a pre-existing, generic multi-layer/MSAA-attachment-clear gap unrelated to this row). Re-running the original blocker, `alpha_to_coverage_unused_attachment.*`, now fails at `vkCreateGraphicsPipelines` with `"an unused color attachment slot is not implemented"` instead of `VK_ERROR_FORMAT_NOT_SUPPORTED` -- confirms the format gate is cleared, tracked as a new follow-on, H7s (not folded into this row, since it is a separate, larger, previously-untracked gap in subpass construction, not a format gap). `ninja check-feme` passes in full, 2035/2094 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7n/H7q's own 2026/2085 baseline by 9 new tests (`ImageFixtureTest`'s 7 new pack/unpack round-trip cases, `FormatTest.MapsRemainingPackedSixteenBitFormats`, `RenderPassTest.CompilesRemainingPackedSixteenBitColorAttachments`); `RenderPassTest.RejectsUnsupportedAttachmentFormat` retargeted from `R5G6B5_UNORM_PACK16` (now supported) to `A4R4G4B4_UNORM_PACK16` (still not attachment-capable). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (core format capabilities, not feature bits or extensions). `FeMeVulkanDesign.md`'s format-support section updated. See "Roadmap H7r: measured impact" in VulkanCTSReport.md for the full reproduction | H7n | `feme/lib/Vulkan/Format.cpp`, `feme/lib/Graphics/ImageFixture.cpp` | P3 |
| H7s | ~~**A `VkRenderPass` subpass's color attachment list with a `VK_ATTACHMENT_UNUSED` slot is rejected outright at pipeline-creation time.** Discovered via H7r's own re-run of `alpha_to_coverage_unused_attachment.*` (color output written to fragment location 1, location 0 explicitly unused): `GraphicsPipeline.cpp`'s `getRenderTargets` fails every case with `"an unused color attachment slot is not implemented"` once H7r's own format fix lets these cases clear the format gate for the first time -- a separate, larger, previously-untracked gap in subpass/render-target construction, unrelated to color-format support itself. Needs a real investigation into what `getRenderTargets`/the fragment-stage-output-to-attachment-slot mapping assumes about every slot being bound to a real attachment, and what has to change (likely treating an unused slot as "no attachment, but a real output location the shader may still write that is simply discarded") to support it~~ (done: `GraphicsPipeline.cpp`'s `getRenderTargets` and `CommandBuffer.cpp`'s `buildRenderTargetBinding` both now treat a `VK_ATTACHMENT_UNUSED` color slot the same way `VK_KHR_maintenance5`'s own `VK_NULL_HANDLE`-imageView dynamic-rendering slot already was (roadmap E5) -- pushing a placeholder (`ResourceFormat::Unknown`/default-constructed `RenderTargetView`, `View == nullptr`) rather than erroring, reusing the entire pre-existing, already-generic "unbound attachment" consumption chain (`resolveDrawAttachments`'s `!View.View` branch, `Executor.cpp`'s `Att.Data.empty()` discard) with zero changes needed there. `validateStageInterfaces`'s per-color-attachment fragment-output-location check was also updated (its own, separate, newly-reachable gap: it previously required a `SV_TargetN` fragment output at *every* location up to `colorAttachmentCount`, including unused ones) to skip an unused slot's location entirely, since a fragment stage is not required to declare an output for a slot with nothing to write to. `getRenderTargets`' pre-existing H7n depth/stencil-only sample-count fallback was widened from "only when `Subpass.ColorAttachments` is empty" to "whenever no *used* color slot supplied a sample count" (a subpass whose color-attachment list is non-empty but every slot in it is unused is the same underlying gap, just reached a different way). Two new regression tests: `DrawTest.cpp`'s `ClassicRenderPassSkipsUnusedColorAttachment` (a real classic-`VkRenderPass` draw, one real + one unused color slot, confirming the unused slot's write is discarded while the real slot's write lands correctly) and its dynamic-rendering sibling `DynamicRenderingSkipsNullColorAttachment` (pre-existing, unchanged, confirming no regression). Real re-run of the original motivating case, `dEQP-VK.pipeline.monolithic.multisample.alpha_to_coverage_unused_attachment.*`: the `"an unused color attachment slot is not implemented"` rejection is gone (pipeline creation now proceeds structurally past attachment/location binding), but the CTS case does not yet pass end-to-end -- it surfaces a new, distinct, independent gap (the real attachment's own fragment output is a 3-component `vec3`, not the 4 components `validateStageInterfaces`/`Executor.cpp` both still hard-require for a *used* attachment's own output), tracked as a new follow-on, H7t, not folded into this row since it is unrelated to the unused-slot mechanism itself (confirmed: this row's own unit tests, which use full 4-component outputs throughout, pass cleanly). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2036/2095 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7r's own 2035/2094 baseline by exactly the 1 new test this row adds (`DrawTest.cpp`'s `ClassicRenderPassSkipsUnusedColorAttachment`). `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (a core render-pass/subpass capability, not a feature bit or extension). `FeMeVulkanDesign.md`'s render-target-binding design section updated. See "Roadmap H7s: measured impact" in VulkanCTSReport.md for the full reproduction) | H7r | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H7t | ~~**A fragment stage's color output with fewer than 4 components (e.g. a `vec3`) at a *used* color-attachment location is rejected outright**, discovered via H7s's own re-run of `alpha_to_coverage_unused_attachment.*` (its own real attachment's fragment output, `fragColor1 = vtxColor.rgb`, is a `vec3` at location 1 -- legal per spec, with the missing components implicitly defined, alpha defaulting to `1.0`). `GraphicsPipeline.cpp`'s `validateStageInterfaces` (`Color->ComponentCount != 4`) and `Executor.cpp`'s fragment-output linkage (`FSColor->ComponentCount != 4` at line 1377, and the equivalent checks for a second color output and the alpha-to-coverage element) all hard-require exactly 4 components for a real (used) attachment's own output; this is unrelated to H7s's own unused-attachment-slot mechanism (confirmed: H7s's own unit tests, all full 4-component outputs, pass cleanly with no regression). Needs a real survey of what padding/defaulting a fragment output narrower than 4 components requires end to end (accepting `ComponentCount` in `{1, 2, 3, 4}` at `validateStageInterfaces`, then `Executor.cpp` synthesizing the missing trailing components -- `0.0` for a missing G/B, `1.0` for a missing A, mirroring how a narrower vertex *input* attribute is already zero/one-extended elsewhere in this codebase)~~ (done: `GraphicsPipeline.cpp`'s `validateStageInterfaces` and all three of `Executor.cpp`'s fragment-output-linkage checks (`FSColors`, the dual-source-blend `FSColor1`, and `FSAlphaToCoverage`) relaxed from requiring `ComponentCount == 4` to accepting `1 <= ComponentCount <= 4`. A new shared helper, `readFragmentColor` (`Executor.cpp`, anonymous namespace), reads a fragment output's up-to-4 components, filling any component at or beyond the element's own `ComponentCount` with its spec-defined identity default (`0.0` for a missing G/B, `1.0` for a missing A) rather than reading out-of-bounds `StageStorage`; the alpha-to-coverage alpha read was similarly given a ternary default. Design precedent: `ImageFixture.cpp`'s pre-existing `unpackColor` ("missing channel reads as its identity value" for a color format lacking a channel entirely). Three new unit tests across three phases: `GraphicsPipelineTest`'s `AcceptsFragmentOutputNarrowerThan4Components` (pipeline-creation-time), `DrawTest.cpp`'s `RendersFragmentOutputNarrowerThan4Components` (full SPIR-V-to-pixel, clearing to transparent black to distinguish a correct opaque default from a broken zero/garbage alpha), and `ExecutorTest.cpp`'s `FillsTriangleWithColorOutputNarrowerThan4Components` (direct-IR, Executor-linkage-focused). Real re-run of the original motivating case, `dEQP-VK.pipeline.monolithic.multisample.alpha_to_coverage_unused_attachment.*`: now passes fully, 6/24 (0 failed, 18 pre-existing `NotSupported` for other sample counts) -- the H7n-H7s chain's own long-running blocker is closed. A broader `dEQP-VK.pipeline.monolithic.multisample.*` sweep (10576 cases) confirmed no regressions: 195 pass/57 fail/10324 `NotSupported` (up from H7q's own 189/63/10324 baseline by exactly the 6 newly-passing cases above); every remaining failure confirmed pre-existing (`mixed_count`, `sampled_image.*`, `3d.*`, `a2c_with_a2one.*`, `sample_rate_a2c`, and `compatible_render_pass.dynamic`, the last confirmed via a `git stash`/rebuild/re-run to fail identically without this row's own changes). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2039/2098 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7s's own 2036/2095 baseline by the 3 new tests above. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (a core signature-linkage capability, not a feature bit or extension). `FeMeVulkanDesign.md`'s H7 status paragraph updated. See "Roadmap H7t: measured impact" in VulkanCTSReport.md for the full reproduction | H7s | `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7o | ~~**A real `min_sample_shading*`/`sample_id.*` case fails at shader-compilation time with `feme-cpu-simdize: ... has a divergent value '' of vector type ...`**, newly reachable only because H7f's own feature-bit flip (subsequently reverted, see H7f's own update) let these cases clear `checkSupport` for the first time. Root cause identified but not yet fixed: these CTS shaders combine `gl_SampleID` into a per-invocation computed buffer index (`pos = ((coord.y * width) + coord.x) * samples + int(gl_SampleID)`) used to address a storage-buffer write; `SIMDize.cpp`'s divergent-vector-value producer classification (`checkVectorDecompositionSupported`-style switch around line 690) has no case at all for a `GetElementPtrInst` with a divergent (per-lane-varying) index, nor does its resource-call handling (`matchResourceCall`) treat a `StoredValue` call's own address operand as a producer needing classification -- unlike groupshared memory, which already has real scatter-store support for a divergent address (see `SIMDize.cpp`'s own `widenGroupSharedLoad`-adjacent commentary). Confirmed generic, not sample-shading-specific: the same shader shape fails identically with sample shading *disabled* (`min_sample_shading_disabled.*` cases fail identically to `min_sample_shading_enabled.*`). Needs a real IR reduction of this exact case (the same technique the H6g-b/H6j/H6k/H6l chain used throughout) to scope a fix: most likely a new `llvm.masked.scatter`-based lowering for a resource/buffer store through a divergent computed address, mirroring the groupshared-memory scatter-store path this pass already has~~ (done, but the original guess above was wrong: a real IR reduction, capturing the actual module right before `SIMDizePass` for a real `min_sample_shading_enabled.min_0_0.samples_2.quad` re-run, showed the failing value was not a `GetElementPtrInst`-addressed divergent buffer store -- already fully supported, see `widenResourceCall` -- but the CTS's own "quad" **vertex** shader's classic `positions[gl_VertexIndex]` local constant lookup table, a per-invocation-divergent index into an ordinary (non-groupshared) `<4 x float>` array; `SIMDize.cpp`'s `checkVectorDecompositionSupported` had no producer case at all for a plain divergent-address `LoadInst` of vector type, fixed by adding one, decomposing the per-lane load's vector result into widened per-component values (`widenScalarizedFallback`) exactly like a resource-call load already did. A second, independent bug then surfaced once that one cleared: `feme-cpu-wrap-fragment: fragment stage wrapper requires attached feme.signature metadata`, for any push-constant-only fragment shader (like the CTS's own `copy_sample_frag`, reading only `subpassLoad` and a push constant, no bound descriptor) -- two of the CPU pipeline's Function-replacement helpers, `RootConstantLowering.cpp`'s `addRootConstantParams` and `SPIRVPushConstantLowering.cpp`'s own inline equivalent, were silently dropping every function-attached metadata node (including `!feme.signature`) when rebuilding a function to append trailing ABI parameters, unlike the ~4 other such helpers which already correctly copy it; fixed by adding the same metadata-copy step to both. With both fixes, 11/60 real `min_sample_shading_*` cases execute for the first time (up from 0). But a third, distinct, still-unfixed gap surfaced in that re-run: `min_sample_shading_enabled.min_1_0.samples_2.quad` genuinely *fails* ("Got less unique colors than requested through minSampleShading") -- `Executor.cpp`'s own per-sample shade/dispatch/merge loop (H7f) deliberately keeps every interpolated varying, including `gl_FragCoord`, at the pixel center on every pass, so a `gl_FragCoord`-derived shader color (like this case's own `color_frag`) recomputes the identical value on every per-sample pass instead of a genuinely distinct one. Advertising the feature bit before every mandatory, feme-supported-sample-count case that exercises it passes for real would be a conformance violation, so `sampleRateShading` stays `VK_FALSE`, and this third gap is tracked as a new follow-on, H7p. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2022/2081 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7f's own 2020/2079 baseline by exactly the 2 new tests this row adds (`SIMDizeTest`'s `DecomposesPrivateMemoryDivergentLoadIntoExtractElement` and a new lit test, `Transforms/CPU/simdize-private-vector-load-scalarize.ll`; the metadata-copy fix's own coverage extends two pre-existing lit tests, `Transforms/CPU/root-constant-lowering.ll` and `Transforms/CPU/spirv-push-constant-lowering.ll`, adding no further test count). `Vulkan14FeatureInventory.md` confirmed still accurate (`sampleRateShading` stays unadvertised, comment updated to cite H7p instead of H7o); `VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H7o: measured impact" in VulkanCTSReport.md for the full reproduction of both fixes and the remaining gap) | H7f | `feme/lib/Transforms/CPU/SIMDize.cpp`, `feme/lib/Transforms/CPU/RootConstantLowering.cpp`, `feme/lib/Transforms/CPU/SPIRVPushConstantLowering.cpp` | P2 |
| H7p | ~~**`gl_FragCoord` (and any other otherwise-pixel-center interpolant) does not vary per-sample position across `Executor.cpp`'s own per-sample shading loop (H7f).** A real `dEQP-VK.pipeline.monolithic.multisample.min_sample_shading_enabled.min_1_0.samples_2.quad` re-run (once H7o's own two pipeline-creation-time bugs were fixed, letting this case reach real execution for the first time) fails with "Got less unique colors than requested through minSampleShading": the case's own `color_frag` shader derives its output color from `fract(gl_FragCoord.xy)` alone, no `gl_SampleID`/`SV_SampleIndex` dependency, so H7f's per-sample pass loop -- which deliberately keeps every interpolated varying at the pixel center on every pass, per its own "harmless, just not maximally efficient" comment -- recomputes the identical color on every one of a pixel's per-sample passes, failing the CTS's "one distinct value per sample" check for `minSampleShading=1.0` specifically (the strictest setting). Needs `Executor.cpp`'s per-sample pass loop widened to evaluate `gl_FragCoord` (and any other per-fragment-only interpolant reachable the same way) at the actual per-sample position `SamplePositions` already tracks, not just the pixel center, on every pass -- this is what keeps `sampleRateShading`'s feature bit off pending a fix~~ (done, but a second, independently-blocking bug surfaced once the `gl_FragCoord`-per-sample-position fix above was in place and *still* didn't make the real case pass: `CommandBuffer.cpp`'s `buildSubpassInputHeap` used the *currently bound draw's own pipeline* sample count uniformly for every heap entry's `RowPitch`/`SampleStride`, including subpass **input** attachments read back via `subpassLoad`. The real CTS case's own render-pass structure -- subpass 0 draws with a genuinely multisampled pipeline, then N single-sample "copy one sample back out" subpasses, each `subpassLoad`-ing one specific sample of subpass 0's own multisampled attachment -- means a subpass-input attachment's real sample count can differ from the reading draw's own pipeline sample count, and using the latter silently mis-addressed every subpass-input read (`SampleStride` even went to a flat `0`, aliasing every sample to the same texel, producing a plausible-looking but wrong checkerboard artifact). Diagnostic methodology: rather than debug the full multi-subpass CTS scenario directly, first proved the `gl_FragCoord` fix fully correct in isolation via a scratch full-image raw-MSAA-write unit test (a `R32G32B32A32_FLOAT` attachment to avoid UNORM8 saturation masking real values, `fract` computed manually in the C++ checker) -- 0 mismatches across a whole 32x32/2-sample image -- which eliminated the raw-write path and redirected investigation to the `subpassLoad` readback path, where the real bug was found by re-reading `resolveDrawAttachments`/`buildSubpassInputHeap`/`runPreparedDraw`. Fixed by adding a new, real per-input-attachment `SubpassInputSampleCounts` vector (populated from each input view's own image via `resolveDrawAttachments`) threaded through `buildSubpassInputHeap`, used for the `SubpassInputs` heap-entry path instead of the blanket pipeline sample count (which remains correct for the `Attachments`/`DepthStencil` fallback path, since a subpass's own color/depth/stencil attachments must match its own pipeline's sample count per spec). New permanent regression test, `DrawTest.cpp`'s `SubpassLoadReadsAnEarlierSubpassMultisampledColorOutputWithADifferentPipelineSampleCount` (seeded-memory technique, modeled on neighboring `SubpassLoadReadsBackAnExplicitSampleOfTheColorAttachmentItWrote`/`MultiviewInputAttachmentReadsBackAnEarlierSubpassColorOutput` tests), confirmed to fail without the fix (reverted `CommandBuffer.cpp` locally, reran: fails with the exact same wrong-value pattern) and pass with it. With both fixes in place, the real `min_sample_shading_enabled.min_1_0.samples_2.quad` case genuinely passes ("Got proper count of unique colors"), and a broader re-run of every feme-supported-sample-count `min_sample_shading_enabled`/`min_sample_shading_disabled` case (7 cases across `min_0_25`/`min_0_5`/`min_1_0` at samples 2 and 4) all pass for real, with no regressions in the neighboring `alpha_to_one.*` cases (3/3 still pass). `sampleRateShading` now honestly flips to `VK_TRUE`. The already-tracked, unrelated `sample_id.*` pipeline-creation failures (H7o's own `SIMDize.cpp` divergent-store-address gap) remain exactly as before -- 6 fails, all in that group, 0 elsewhere. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2024/2083 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7o's own 2022/2081 baseline by exactly the 2 new tests this row adds (`ExecutorTest`'s scratch diagnostic was written, used, and removed -- not committed -- so the only new committed tests are `DrawTest.cpp`'s new subpass-sample-count regression test and `PhysicalDeviceInfoTest.cpp`'s updated `sampleRateShading == VK_TRUE` assertion, which edits an existing case rather than adding one; net new test count is 1). `Vulkan14FeatureInventory.md` updated (`sampleRateShading` now advertised). `VulkanExtensionInventory.md` confirmed no change needed (a core feature-bit row, not an extension). `FeMeVulkanDesign.md`'s H7 status paragraph updated. See "Roadmap H7p: measured impact" in VulkanCTSReport.md for the full reproduction | H7f | `feme/lib/Graphics/Executor.cpp`, `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H7g | ~~**`vertexPipelineStoresAndAtomics`/`fragmentStoresAndAtomics`**: no evidence found of storage-buffer/storage-image write or atomic-operation lowering reachable from a vertex/tessellation/geometry stage, nor a fragment stage specifically (as opposed to compute, where this already works) -- needs an investigation of whatever gates a non-compute stage's `OpStore`/`OpAtomic*` against a `StorageBuffer`/storage-image resource today, and what (if anything) needs to change per-stage to allow it~~ (done: a real, exhaustive survey of `feme::cpu::runPipeline` (`Pipeline.cpp`) confirmed every pass that touches a resource store (`SPIRVResourceLoweringPass`, `ResourceLoweringPass`, `LinearizePass`, `SIMDizePass`, `WaveLoweringPass`) already operates identically regardless of `StageCompileOptions::Stage` -- no stage-based gate on `StorageBuffer`/storage-image writes ever existed anywhere in the codebase; the only stage-conditioned logic is whether graphics stage-IO validation runs at all, and which stage-specific entry-wrapper pass runs last, neither of which restricts stores. Confirmed via the real CTS's own `vktBindingShaderAccessTests.cpp` (`checkSupportShaderAccess`) that these two feature bits gate ordinary storage-buffer/image *write* access from these stages, not specifically atomic read-modify-write (real SPIR-V atomic ops, e.g. `OpAtomicIAdd`, remain entirely unimplemented for every stage including compute -- a separate, pre-existing, out-of-scope gap; the existing compute "storage buffer" unit test uses a plain load+`IAdd`+store, not a true atomic). `VK_DESCRIPTOR_TYPE_STORAGE_IMAGE` write support also remains a separate, pre-existing, stage-independent gap (`Format.cpp`'s own comment: no `feme.cpu.image.store.*` runtime helper exists for any format yet), unaffected by and not blocking this row, since the feature bits only promise writability for storage-resource kinds the device does support (storage buffers, and storage texel buffers via the existing `Buffer`-dim `spirv.ImageWrite` lowering). Two new `DrawTest.cpp` unit tests, `VertexStageWritesStorageBuffer`/`FragmentStageWritesStorageBuffer` (each avoiding any read-modify-write race, since true atomics aren't implemented, by having every invocation write either a distinct slot or an identical constant), confirmed the CPU lowering pipeline already generically supports vertex/fragment storage-buffer writes with zero compiler/executor code changes needed -- mirroring the H7a "already-implemented feature bits" pattern. `PhysicalDeviceInfo.cpp` flips both feature bits to `VK_TRUE`. Real re-run of `dEQP-VK.binding_model.shader_access.*.storage_buffer.vertex*`/`.fragment*`/`.vertex_fragment*` (excluding the pre-existing, unrelated `bind2` gap tracked below): **1200/1200 pass, 0 fail, 0 `NotSupported`** (up from 0/0/1200 `NotSupported` before the flip). This re-run also surfaced a genuine, previously-unreachable crash (`with_push` cases SIGSEGV'ing via a null `vkGetDeviceProcAddr("vkCmdPushDescriptorSetKHR")` result, since `VK_KHR_push_descriptor` is advertised as its own extension but `vk_gen_entrypoints.py`'s `SUPPORTED_EXTENSIONS` never listed it, so only the core-promoted, non-`KHR`-suffixed name was ever registered in the dispatch table) -- fixed in the same small commit set (`vk_gen_entrypoints.py`, `ImplementedEntrypoints.txt`, two thin `_KHR`-suffixed forwarders in `CommandBuffer.cpp`/`EntryPoints.h`, plus a new `CommandBufferTest.cpp` regression test, `KHRSuffixedEntryPointsProduceTheSameResult`), confirmed via a `git stash`/rebuild/re-run that the crash reproduces identically without this row's own changes (i.e. genuinely pre-existing, not a regression from the feature-bit flip, merely newly reachable through it). A separate, unrelated `bind2` (`VK_KHR_maintenance6`'s `vkCmdBindDescriptorSets2`) crash/failure was also found during this sweep, confirmed via the same `git stash` technique to reproduce identically for `compute`-stage cases too (pre-existing, stage-independent, unaffected by and out of scope for this row), tracked as a new follow-on, H7v. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2042/2101 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7t's own 2039/2098 baseline by the 3 new tests this row and its push-descriptor fix add. `Vulkan14FeatureInventory.md` updated (`vertexPipelineStoresAndAtomics`/`fragmentStoresAndAtomics` now advertised); `VulkanExtensionInventory.md` confirmed no change needed for the feature-bit flip itself, but updated to note the push-descriptor `_KHR` dispatch-table fix. `FeMeVulkanDesign.md`'s H7 status paragraph updated. See "Roadmap H7g: measured impact" in VulkanCTSReport.md for the full reproduction | H7 | `feme/lib/Vulkan/PhysicalDeviceInfo.cpp`, `feme/lib/Vulkan/CommandBuffer.cpp`, `feme/utils/vk_gen_entrypoints.py` | P2 |
| H7v | ~~**`VK_KHR_maintenance6`'s `vkCmdBindDescriptorSets2` (`bind2`) fails for every stage, including compute.** Discovered via H7g's own real CTS re-run of `dEQP-VK.binding_model.shader_access.*.bind2.storage_buffer.*`: the `vertex`/`fragment`-stage cases SIGSEGV (a null-pointer call inside `writeDrawCmdBuffer`'s `bindDescriptorSets` helper), while the `compute`-stage cases instead fail cleanly at `vkCreateComputePipelines` with `VK_ERROR_INITIALIZATION_FAILED` -- both confirmed via a `git stash`/rebuild/re-run to reproduce identically without any of H7g's own changes, so this is pre-existing and entirely unrelated to storage-buffer stage-write support itself (H7g's own non-`bind2` re-run, 1200/1200, is unaffected). Needs a real investigation into why `vkCmdBindDescriptorSets2`'s `VkBindDescriptorSetsInfo` path (`CommandBuffer.cpp`) diverges from the already-working `vkCmdBindDescriptorSets` path enough to fail pipeline creation/execution outright, for every stage~~ (closed: this row's own "compute fails cleanly" half turned out to be inaccurate the same way H7f's original text was -- re-running the ordinary, non-`bind2` `bind.storage_buffer.compute*` cases hit the *identical* `vkCreateComputePipelines` error, proving the "clean compute failure" has nothing to do with `bind2` at all. Root cause: `classifyVulkanBufferHandle` (`SPIRVResourceLowering.cpp`) treated any struct-shaped `spirv.VulkanBuffer` handle whose storage class is `Uniform` (2) as a read-only uniform buffer -- but that storage class value is ambiguous: a real read-only uniform block and a pre-1.3 storage buffer block spelled as `Uniform` class plus a `BufferBlock` decoration (still glslang's default spelling for a `buffer` block, as this CTS shader's own SPIR-V confirms) both carry it, and only the handle's own second (`Writable`) int parameter -- ignored entirely until now -- tells them apart. Fixed to treat a `Uniform`-class struct handle with `Writable=1` as a real, writable storage buffer block; a real read-only uniform block (`Writable=0`) is unaffected, confirmed by two new `SPIRVResourceLoweringTest.cpp` cases (`LowersLegacyUniformClassStorageBlockFieldToResourceLoad`/`...StoreToResourceStore`). Real re-run of `dEQP-VK.binding_model.shader_access.*.storage_buffer.compute*`'s three non-`bind2` update methods (`bind`/`bind.with_template`/`bind.with_push`+`with_push_template`): **80/80 pass, 0 fail** (up from 0/80 before the fix) -- a pre-existing, `bind2`-independent gap, now closed, unrelated to storage-buffer *stage* support at all (it is orthogonal to H7g's own vertex/fragment work). This row's other half -- the actual `bind2` SIGSEGV -- was then re-investigated fresh with a gdb backtrace on both a `vertex` case (confirming the exact same crash shape H7g's own prior gdb session found: a null function-pointer call inside a CTS-side helper, `writeDrawCmdBuffer`) and, now that the resource-lowering fix lets a `bind2` *compute* case reach the same code path for the first time, a `compute` case too (`ComputeCommand::submitAndWait`) -- both disassemble to the identical shape: a `VkBindDescriptorSetsInfoKHR`-tagged struct is built correctly, then a call through a *null* function-pointer slot in CTS's own cached device-dispatch struct. Traced to its root: this environment's system Vulkan loader (`libvulkan.so.1`, apt package `libvulkan1` 1.3.275.0) predates `VK_KHR_maintenance6`/`vkCmdBindDescriptorSets2` (added to the Vulkan headers/loader around 1.3.284, promoted to core in 1.4) -- the loader's own compiled-in device-command dispatch table has no entry for this name at all, so `vkGetDeviceProcAddr(device, "vkCmdBindDescriptorSets2")` returns null to CTS even though this ICD's own `EntryPoints.cpp`/`ProcAddr.cpp` fully implement and correctly expose it (confirmed: `CommandBufferTest.cpp`'s existing `BindDescriptorSets2AndPushConstants2ReachTheDispatch` unit test calls the ICD function directly and passes). This is an environment/toolchain limitation outside this repository, not a feme defect -- there is no ICD-side code to change, so this row closes as "root-caused, no fix possible or needed in-repo" rather than a real fix, distinct from every other row this milestone's chain has closed. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2044/2103 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7g's own 2042/2101 baseline by the 2 new `SPIRVResourceLoweringTest` cases this row adds. `Vulkan14FeatureInventory.md` confirmed no change needed (a resource-lowering correctness fix, not a feature-bit change); `VulkanExtensionInventory.md`'s `VK_KHR_maintenance6` row updated to reflect this row's closure and root cause (the extension itself was already advertised and its own command dispatch is already correctly implemented in-repo). `FeMeVulkanDesign.md`'s H7 status paragraph updated. See "Roadmap H7v: measured impact" in VulkanCTSReport.md for the full reproduction | H7 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P2 |
| H7h | ~~**`shaderClipDistance`/`shaderCullDistance`**: no `ClipDistance`/`CullDistance` SPIR-V builtin support exists anywhere in the stage-IO pipeline (`CanonicalizeStage.cpp`) or the executor's own clipping path (`Executor.cpp`'s `clipTriangle`, which only clips against the fixed homogeneous clip planes, with no per-vertex application-supplied plane distances at all) despite `maxClipDistances`/`maxCullDistances` already being set to the honest value `8`. Needs real stage-IO plumbing for both builtins plus a clip-distance consumer in `clipTriangle` and a cull-distance consumer wherever primitive culling happens~~ (partially closed: `CanonicalizeStage.cpp`'s `getSystemValueForBuiltIn` now maps SPIR-V `BuiltIn ClipDistance`/`CullDistance` onto real `SignatureSystemValue`s -- the pre-existing generic `gl_PerVertex` per-member decomposition (`getStageIORowShape`) already folds the array dimension into `RowCount` with zero new code, so this was purely a builtin-mapping gap. `Executor.cpp` gained the real consumer: `gl_ClipDistance` becomes one additional Sutherland-Hodgman half-space clip per declared plane (`clipTriangle`'s new `ClipDistanceCount` parameter, evaluated against a new per-vertex `RasterVertex::ClipDistances` array, linearly interpolated like any other varying), and `gl_CullDistance` discards a whole primitive outright, pre-clip, when one declared plane is negative for every vertex (`isCulledByCullDistance`), confirmed by two new `ExecutorTest.cpp` cases, `ClipsATriangleAgainstAWrittenClipDistance`/`CullsATriangleWhenCullDistanceIsNegativeForEveryVertex`. This is scoped to the vertex stage, compile-time-constant array indices only, with no fragment-stage read-back -- a real `dEQP-VK.clipping.user_defined.{clip_distance,clip_cull_distance}.*` re-run (with the feature bits provisionally flipped on to measure) confirms that scope is real: 16/16 passing for the vertex-stage, static-index, non-`_fragmentshader_read` subset. But it also confirms three further, separate gaps this row's own text did not ask for: `*_dynamic_index` (0/32, `CanonicalizeStagePass` does not canonicalize a non-constant array index at all), `*_fragmentshader_read` (0/16, no fragment-side system-value-linked input path exists), and `vert_tess`/`vert_geom` (an unrelated, pre-existing LLVM `getelementptr`-into-an-array-of-struct lowering gap) -- together leaving only ~16 of this feature's ~330 real mandatory CTS cases passing, so `shaderClipDistance`/`shaderCullDistance` (`PhysicalDeviceInfo.cpp`) stay `VK_FALSE` rather than risk a conformance violation, the same standard set by roadmap H7o/`sampleRateShading`. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2047/2106 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`), up from H7v's own 2044/2103 baseline by the 3 new tests this row adds (`CanonicalizeStageTest`'s `MapsSPIRVClipCullDistanceBuiltInsToSystemValues`, `ExecutorTest`'s two new cases). `Vulkan14FeatureInventory.md` updated (`shaderClipDistance`/`shaderCullDistance` still `VK_FALSE`, with a rationale note and pointers to H7w/H7x/H7y); `VulkanExtensionInventory.md` confirmed no change needed (a core feature-bit row, not an extension). `FeMeGraphicsDesign.md` gained a new "Status (roadmap H7h)" subsection. See "Roadmap H7h: measured impact" in VulkanCTSReport.md for the full reproduction. The three newly-discovered gaps are broken out below as H7w/H7x/H7y) | H7 | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H7w | ~~**`gl_ClipDistance[i]`/`gl_CullDistance[i]` with a non-constant (dynamically computed) index `i` is rejected outright**, discovered via H7h's own real re-run of `dEQP-VK.clipping.user_defined.clip_distance_dynamic_index.*`/`clip_cull_distance_dynamic_index.*` (0/32): `feme-graphics-validate-stage` fails every case with `"function 'main' has an unresolved stage-IO global-variable access to 'spirv_varN', a shape CanonicalizeStagePass does not yet canonicalize into a 'feme.stage.*' call"` -- `CanonicalizeStagePass` only recognizes a constant `OpAccessChain`/`OpCompositeExtract` index into a `gl_PerVertex` array member today, not a runtime value (e.g. a loop-varying `int`). Needs a real investigation into what canonicalization a dynamically-indexed stage-IO array member requires (likely lowering to a bounds-checked runtime read/write against the element's own backing storage, rather than the current constant-index-only `feme.stage.*` call shape)~~ (closed: `CanonicalizeStage.cpp`'s new `getDynamicRowIndexedAccess` recognizes a GEP rooted at a stage-IO global with a constant index prefix (member/nested-array selection) and exactly one trailing non-constant row index, wired into `getStageIOGlobal`/`resolveStageIOAccess` as a third fallback; `ShadowValueMap` (roadmap H2e) gained a parallel `RowCount`-sized array-alloca scheme for a non-constant `Row` (deliberately excluded from `PromoteMemToReg`'s promotable list, since a variable-index GEP is not promotable), since its prior per-constant-`Row` scalar-alloca scheme had no way to key a dynamic one. No bounds check was added (a deliberate deviation from this row's own speculative text -- see `FeMeGraphicsDesign.md`'s new "Status (roadmap H7w)" subsection), matching `getDynamicVertexIndexedAccess`'s own established, equally-unchecked precedent. Confirmed by a new `CanonicalizeStageTest.cpp` case, `ThreadsDynamicRowIndexIntoClipDistanceOutputStore`, and by a real `glslangValidator`/`feme-translate` IR reduction of the exact CTS shape, which no longer reproduces the "unresolved stage-IO global-variable access" diagnostic post-fix. `ninja check-feme` passes in full, 2048/2107 (59 pre-existing `Unsupported`, 0 `Failed`). A real CTS re-run confirms the canonicalization gap this row targets is genuinely closed (that diagnostic no longer occurs anywhere in this case list), but every sampled case still fails to produce a final image, now for a wholly different, already-documented, pre-existing `feme::cpu::LinearizePass` limitation (roadmap R27's own intentional scoping to the loop shapes it already supports, the same limitation this report already noted for `dEQP-VK.geometry.layered.*.readback`) -- unrelated to this row's own scope, so no new roadmap row is added for it. `shaderClipDistance`/`shaderCullDistance` stay `VK_FALSE` (this row's fix alone does not clear a real passing CTS case end to end). See "Roadmap H7w: measured impact" in VulkanCTSReport.md) | H7 | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp` | P2 |
| H7x | ~~**A fragment shader cannot read back the interpolated `gl_ClipDistance`/`gl_CullDistance` value**, discovered via H7h's own real re-run of `dEQP-VK.clipping.user_defined.clip_distance.vert.*_fragmentshader_read`/`clip_cull_distance.vert.*_fragmentshader_read` (0/16): these builtins have no fragment-stage system-value-linked input path today (H7h's own vertex-stage output consumer does not, by itself, make the value available to a fragment stage the way an ordinary user varying already is). Needs a real investigation into what plumbing a fragment-stage read of these two builtins requires -- likely treating them as an ordinary interpolated stage-IO input from the fragment side, distinct from the executor's own vertex-stage clip/cull consumer this row added~~ (partially closed: two real fixes, each found via a real IR reduction of the exact CTS shape. `SPIRVToLLVMPatterns.cpp` gained `StageIOArrayAccessChainPattern`, legalizing a single-constant-index `spirv.AccessChain` into a value-modeled stage-IO array (the shape glslang's own standalone, non-`gl_PerVertex`-wrapped fragment-stage `Input` `gl_ClipDistance`/`gl_CullDistance` array produces) directly to `llvm.extractvalue`, fixing a `'llvm.getelementptr' op operand #0 must be LLVM pointer type' verifier crash -- mirrors the existing `BuiltInAccessChainPattern` vector/`extractelement` precedent; a non-constant index is deliberately left unmatched (H7w's own already-tracked gap, not a new regression), falling through to a clean diagnosed error instead of a crash. `Executor.cpp`'s fragment/vertex varying-linking loop now recognizes a `ClipDistance`/`CullDistance` fragment input (no `Location` of its own) and links it against the vertex stage's already-resolved output by `SystemValue`, reusing the existing generic `LinkedVarying` interpolation machinery; `StageStorage.cpp`/`FragmentWrapper.cpp` gained a narrow carve-out so these two builtins take the ordinary-varying path rather than the per-invocation-record path every other fragment system value uses. Confirmed by 2 new `Conversion/SPIRVToLLVM` lit test cases and a new `ExecutorTest.cpp` case, `FragmentShaderReadsBackInterpolatedClipAndCullDistance`. `ninja check-feme` passes in full, 2050/2109 (59 pre-existing `Unsupported`, 0 `Failed`). A real CTS re-run confirms both of this row's own targeted gaps are genuinely closed (the `getelementptr` verifier crash no longer occurs anywhere), but reveals a third, previously-hidden, deeper blocker one layer below: `feme-cpu-simdize` rejects a divergent aggregate-typed value (the whole `[N x f32]` array loaded before this row's own `extractvalue` narrows it to a scalar) outright, since its own producer/consumer classification has no aggregate-decomposition support yet -- generic to any divergent aggregate, not specific to these two builtins, so broken out as new row H7z rather than folded in here. `shaderClipDistance`/`shaderCullDistance` stay `VK_FALSE` (this row's own fixes, while real and tested, do not by themselves clear a real passing CTS case end to end). `FeMeGraphicsDesign.md` gained a new "Status (roadmap H7x)" subsection; `Vulkan14FeatureInventory.md` updated; `VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H7x: measured impact" in VulkanCTSReport.md) | H7 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/lib/Graphics/Executor.cpp` | P3 |
| H7y | ~~**Writing `gl_ClipDistance`/`gl_CullDistance` from a tessellation-evaluation or geometry stage (rather than the vertex stage) fails at shader-compilation time**, discovered via H7h's own real re-run of `dEQP-VK.clipping.user_defined.clip_distance.vert_tess.*`/`vert_geom.*`: fails with `"'llvm.getelementptr' op operand #0 must be LLVM pointer type ... but got '!llvm.array<N x ...>'"`, an LLVM lowering gap where a `gl_ClipDistance`/`gl_CullDistance` array member reached as an SSA value (not a pointer) inside a non-vertex pre-rasterization stage's own output-composition code is indexed with a `getelementptr` that requires a pointer operand -- unrelated to H7h's own vertex-stage consumer, which stores the same array through a real pointer-backed path (`VertexWrapper.cpp`'s `lowerVertexOutputStore`). Needs a real investigation into why the tessellation-evaluation/geometry stage's own output-composition lowering produces an array SSA value here instead of a pointer, and what has to change to fix it generically (this may not be specific to clip/cull-distance -- worth checking whether any other array-shaped tessellation/geometry stage output hits the same gap)~~ (closed: the real root cause, found via a `glslangValidator`/`feme-translate --spirv-to-llvmir` IR reduction, was a foundational bug in `StageIOAddressOfPattern` (`SPIRVToLLVMPatterns.cpp`) itself, not anything non-vertex-stage- or clip/cull-distance-specific -- it eagerly loaded *any* `Input`-storage-class stage-IO variable into an SSA value, including an array-typed one (`gl_in[]`, or H7x's own standalone `gl_ClipDistance`/`gl_CullDistance` read), which cannot represent a genuinely dynamic index at all. Fixed with three coordinated changes: an array-typed `Input` variable now stays a real pointer at `StageIOAddressOfPattern`'s own site (matching `Output`'s existing treatment); the parallel `addConversion(spirv::PointerType)` callback gained the same array-pointee special case, since MLIR's dialect-conversion framework relies on it as a separate source of truth from what patterns actually produce; and a new, dedicated `StageIOArrayAccessChainPattern` builds the resulting `getelementptr` directly with an explicit real-pointer result type (MLIR's own generic `AccessChainPattern` cannot be reused, since it re-derives an inherently ambiguous result type from the access chain's leaf SPIR-V pointer type alone), plus a new `addTargetMaterialization` callback resolving the one remaining ambiguity at the following `spirv.Load`'s operand lookup. Confirmed by 3 lit tests (constant-index, dynamic-index, and two-level `gl_in[i].gl_Position` shapes) and a real IR reduction of a plain geometry-shader `gl_in[i].gl_Position` read, no longer crashing post-fix. `ninja check-feme` passes in full, 2049/2108 (59 pre-existing `Unsupported`, 0 `Failed`). A real CTS re-run (`vert_tess`/`vert_geom`, feature bits provisionally flipped on to measure) confirms this row's own targeted crash is genuinely gone everywhere in both case lists, but reveals three further, separate, pre-existing blockers one layer below -- a geometry-stage input/vertex-stage output stage-linkage signature mismatch, a tessellation-evaluation patch-constant system-value gap, and a fragment-stage synthetic-layout gap for a non-vertex-stage varying producer -- none in this row's own scope, broken out as new milestone H13 (`H7`'s own single-letter suffix space, `H7a`-`H7z`, is now fully used, and this project's own one-letter-deep nesting convention rules out `H7aa`). `shaderClipDistance`/`shaderCullDistance` stay `VK_FALSE` (this row's own fix does not by itself clear a real passing CTS case end to end, the same standard as H7h/H7w/H7x). `FeMeGraphicsDesign.md` gained a new "Status (roadmap H7y)" subsection (and an update to H7x's own stale `extractvalue`-based description, since this row's fix supersedes it); `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed (the feature bits stay `VK_FALSE`). See "Roadmap H7y: measured impact" in VulkanCTSReport.md) | H7 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P3 |
| H7z | ~~**`feme-cpu-simdize` rejects a divergent (per-fragment-varying) SSA value of aggregate type outright**, discovered via H7x's own real re-run of `dEQP-VK.clipping.user_defined.{clip_distance,clip_cull_distance}.vert.*_fragmentshader_read` (0/16, once H7x's own two fixes let these cases clear the SPIR-V-to-LLVM/varying-linking gates for the first time): `SIMDize.cpp`'s own producer/consumer precondition-checking loop fails every case with `"has a divergent value '' of aggregate type; component decomposition is not yet supported (roadmap milestone 7 deviation)"` -- the pass's own wave-lane decomposition logic already handles a divergent scalar or vector value, but not yet a divergent aggregate (here, the whole `[N x float]` `gl_ClipDistance`/`gl_CullDistance` array `StageIOAddressOfPattern` loads as one value before H7x's own `extractvalue` narrows it to a scalar). Generic to any divergent aggregate-typed value reaching this pass, not specific to these two builtins. Needs a real investigation into whether `SIMDize.cpp` should learn to decompose a divergent aggregate into N per-lane, per-field scalars (analogous to how a vector is already decomposed), or whether an aggregate should instead be prevented from ever reaching this point as a single divergent value (e.g. teaching `StageIOAddressOfPattern` or a later canonicalization step to decompose the array into per-element scalar loads before this pass ever sees it)~~ (closed, with no code change of its own needed: roadmap H7y's own, unrelated fix -- `StageIOAddressOfPattern` now keeps an array-typed `Input` stage-IO variable as a real pointer instead of eagerly loading its whole value, so `StageIOArrayAccessChainPattern`'s `getelementptr` + an ordinary scalar `llvm.load` reads exactly one `f32` element, never the whole `[N x f32]` array as a single aggregate value -- incidentally means this row's own target shape (the divergent-aggregate `gl_ClipDistance`/`gl_CullDistance` load) no longer reaches `SIMDize.cpp` at all. Confirmed by a real CTS re-run of the exact 16 `_fragmentshader_read` cases this row's own text names (feature bits provisionally flipped on to measure, then reverted): the `"has a divergent value ... of aggregate type"` diagnostic no longer occurs anywhere in either case list. `SIMDize.cpp`'s own generic aggregate-diagnostic check is deliberately left as-is (not deleted) -- it is still real, load-bearing protection against any *other* divergent-aggregate producer (a struct load, or an array load reached some other way) that was previously only reachable, and therefore only exercised, via this now-closed clip/cull-distance path; added a new, direct `SIMDizeTest.cpp` case, `DiagnosesUnsupportedDivergentAggregate` (a synthetic divergent array load, independent of stage-IO), to give it real, dedicated test coverage of its own rather than relying on an indirect path that no longer reaches it. `ninja check-feme` passes in full, 2050/2109 (59 pre-existing `Unsupported`, 0 `Failed`, up 1 test from this row's own new case). The CTS re-run's own remaining failures for these 16 cases are the already-tracked H13c gap (`feme-cpu-wrap-fragment`'s synthetic fragment layouts only supporting a vertex-stage producer), not a new blocker -- confirmed by the `FEME_VULKAN_LOG_CREATION_ERRORS=1` diagnostic text matching H13c's own exactly. `shaderClipDistance`/`shaderCullDistance` stay `VK_FALSE` (H13c alone still blocks a real passing case end to end). `FeMeGraphicsDesign.md` gained a new "Status (roadmap H7z)" subsection; `Vulkan14FeatureInventory.md` updated; `VulkanExtensionInventory.md` confirmed no change needed. See "Roadmap H7z: measured impact" in VulkanCTSReport.md) | H7 | `feme/lib/Transforms/CPU/SIMDize.cpp` | P3 |
| H7i | ~~**`samplerAnisotropy`**: `Image.cpp` already stores `anisotropyEnable`/`maxAnisotropy` on the sampler descriptor at creation time, but no anisotropic filtering logic was found in the actual texture-sampling implementation -- sampling still appears isotropic regardless of the stored state. Needs the real anisotropic filter kernel wired into the sampling path before this can honestly flip (and `maxSamplerAnisotropy`, currently the degenerate `1.0f`, would need raising to match) (in progress: a real screen-space-derivative-based implicit-LOD computation and a genuine bounded multi-tap anisotropic filter are now implemented (`FeMeRuntimeCPU.c`'s `femeRTPlanImplicitLod`, `ImageCalls.h`/`.cpp`'s widened `Sample2D` shape, both `ResourceLowering.cpp` and `SPIRVResourceLowering.cpp`'s `getOrSynthesizeSample2DDerivatives`), covering the plain `sampler2D` (`Sample2D`) shape only. Four new unit tests confirm the real behavior end to end: `ImageSamplingTest.cpp`'s `ImplicitLodWithNoDerivativesReadsBaseLevel`/`ImplicitLodSelectsCoarserMipFromDerivatives`/`AnisotropicSampleDiffersFromIsotropicSample`, and `SPIRVResourceLoweringTest.cpp`'s `FragmentStageImplicitSampleSynthesizesRealDerivatives`. However, a real re-run of `dEQP-VK.texture.filtering_anisotropy.*` found the filter kernel is never actually reached: 0/128 pass (64 Fail, 64 `NotSupported` for unrelated compute-format gaps), every graphics case failing at `vkCreateGraphicsPipelines` with `VK_ERROR_INITIALIZATION_FAILED`. Root-caused via `FEME_VULKAN_LOG_CREATION_ERRORS=1` to `SPIRVResourceLoweringPass` not recognizing a single combined `OpTypeSampledImage`-style `handlefrombinding` call -- the shape glslang actually emits for an ordinary GLSL `uniform sampler2D` declaration, as opposed to the separately-declared-then-composed image+sampler pattern the pass's existing classification logic expects. A broader sweep confirmed this is not specific to anisotropy or this row's own changes: `dEQP-VK.texture.filtering.2d.*` (1698 cases) is 0/1698 passing overall (258 Fail, all the same graphics-pipeline-creation error; 1440 `NotSupported`), and classification fails structurally upstream of anything this row's own derivative-synthesis code touches, so that code is never reached either way -- confirmed pre-existing and unrelated to H7i. `samplerAnisotropy` therefore stays `VK_FALSE` (reverted from an earlier premature `VK_TRUE`) and `maxSamplerAnisotropy` stays at its degenerate `1.0f` floor -- flipping either would be an unverifiable conformance claim. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2054/2113 (59 pre-existing, unrelated `Unsupported`, 0 `Failed`). Tracked to completion as new roadmap follow-on H13d (the `SPIRVResourceLoweringPass` combined-sampled-image-handle gap itself). See "Roadmap H7i: measured impact" in VulkanCTSReport.md for the full reproduction)~~ (closed: every blocker this row's own investigation surfaced -- H13d (the `SPIRVResourceLoweringPass` combined-sampled-image-handle gap) and the four further sampling-correctness gaps its closure exposed (H14 all-zero resource reads, H15 missing LOD clamp, H16 mag/min filter swapped, H17 no trilinear blend, H18 an off-by-one bit shift in one packed format's decoder) -- are now closed, and `dEQP-VK.texture.filtering.2d.*` is fully clean (0 `Fail`). `PhysicalDeviceInfo.cpp` now advertises `Info.Features.samplerAnisotropy = VK_TRUE` and raises `Limits.maxSamplerAnisotropy` to `16.0f` (the value real GPU drivers typically report). New/updated unit test coverage: `PhysicalDeviceInfoTest.cpp`'s `OnlyRobustBufferAccessDualSrcBlendASTCLDRAndMultiViewportAreAdvertised` now asserts `samplerAnisotropy == VK_TRUE` alongside the other advertised features. `ninja check-feme` passes in full, 2068/2127 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.texture.filtering_anisotropy.*` confirms the filter kernel is genuinely reached and passes: 64/128 Pass (up from 0), the other 64 honestly `NotSupported` (an unrelated compute-format gap, unchanged), **0 `Fail`**. A regression re-run of the broader `dEQP-VK.texture.filtering.2d.*` sweep confirms no regression (still 258/1698 Pass, 0 `Fail`, 1440 `NotSupported`, unchanged). See "Roadmap H7i: measured impact" in `VulkanCTSReport.md` for the full closure reproduction) | H7 | `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Transforms/CPU/ImageCalls.cpp`, `feme/lib/Transforms/CPU/ResourceLowering.cpp`, `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P2 |
| ~~H7j~~ | ~~**`shaderStorageImageExtendedFormats`/`shaderStorageImageMultisample`/`shaderStorageImageReadWithoutFormat`/`shaderStorageImageWriteWithoutFormat`**: no `OpImageRead`/`OpImageWrite` lowering was found anywhere in the transform path for a storage image at all, so these four format/configuration bits have no shader-side storage-image read/write implementation to be honest about yet -- a larger prerequisite than a narrow format restriction to lift. Needs storage-image read/write lowering built first (likely its own, larger milestone), with these four bits as a follow-on once it exists (split out as its own top-level milestone, H19, rather than nested further under H7 -- H19a now closes the base Plain2D/mandatory-format-floor read/write case; H19d specifically tracks this row's own remaining four feature bits, which need format/configuration breadth beyond what H19a itself claims)~~ (closed: all four bits this row named are now `VK_TRUE` in `PhysicalDeviceInfo.cpp`, each closed by its own real CTS-verified milestone within the H19 chain this row split out -- `shaderStorageImageExtendedFormats` via H19a/H19f/H19h/H19j/H19n/H19o (the mandatory storage-image format floor plus its full extended-format list); `shaderStorageImageMultisample` via H19g/H19k/H19l/H19m (per-sample storage-image addressing, then two unrelated compiler prerequisites -- `feme-cpu-linearize`'s redundant-`Flow`-block loop shape and `feme-cpu-simdize`'s missing `Store2DMS`/`Store2DArrayMS` dispatch-table entries -- then the arrayed-2D-multisample case); `shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat` via H19f/H19i (the `VK_FORMAT_FEATURE_2_STORAGE_{READ,WRITE}_WITHOUT_FORMAT_BIT` advertisement, once confirmed the compiler-side lowering was already format-agnostic). See each row's own "measured impact" section in `VulkanCTSReport.md` and the four corresponding rows in `Vulkan14FeatureInventory.md`, all already "yes" and already cross-referencing this row. No further work needed under H7j itself)~~ | H7 | `feme/lib/Transforms`, `feme/lib/Graphics` | P3 |
| H7k | ~~**Point/line-primitive quad coverage excludes exact pixel-grid-aligned centers.** Found via H7d's own real `deqp-vk.clipping.clip_volume.depth_clamp.{line_list,line_strip,point_list}` reproduction: every one of that CTS case's own point/line vertices lands with its rasterized quad centered exactly on an integer pixel-grid intersection (e.g. a point at `(8,8)` expands to a `[7.5,8.5]x[7.5,8.5]` quad), and `Executor.cpp`'s `pushQuadTriangle`/coverage test excludes the covering pixel on *both* sides of that boundary, rendering zero pixels for a primitive that should render some. Nothing in this project's own unit-test coverage exercises a point-topology draw at all yet, so this is a pre-existing gap, not a regression from H7d. Needs the coverage test's own inclusive/exclusive edge convention audited against a real GPU's fill rule (the classic "top-left" rule, or equivalent) so an exact grid-aligned primitive is guaranteed exactly one consistent side of coverage, not both-excluded~~ (this row's own original diagnosis was found incorrect on investigation: a hand simulation plus a real unit-test probe both showed the existing top-left-rule coverage test already renders exactly one consistent pixel for a quad centered exactly on a pixel-grid intersection, matching the standard inclusive/exclusive fill-rule convention -- not the "zero pixels" this row originally claimed. The real bug, found via a full real `deqp-vk.clipping.clip_volume.depth_clamp.point_list` reproduction (decoded result images cross-referenced against `vktClippingTests.cpp`'s own `genVertices`/`countPixels`): unlike a triangle (`clipTriangle`'s `FrustumPlanes`), a `Point`/`Line`-class primitive was never tested against the near/far Z planes at all -- only the pre-existing degenerate-`W` `ClipEpsilon` guard applied -- so a CTS point/line vertex behind the near plane or beyond the far plane always rendered regardless of `depthClampEnable`. Fixed: new file-scope `farPlaneDistance`/`nearPlaneDistance` functions factored out of `clipTriangle`'s own plane lambdas, reused by two new helpers in `Executor.cpp` -- `pointPassesDepthClip` (an all-or-nothing accept/reject test, since Vulkan has no notion of a partially-clipped point) and `clipLineDepthRange` (a real 2-vertex near/far segment clip reusing `lerpVertex` the same way `clipAgainstPlane` does for a triangle edge) -- both honoring `DepthClampEnable` exactly like `clipTriangle` already does. Deliberately out of scope: X/Y side-plane clipping for points/lines (triangles get this too via `clipTriangle`'s other 4 planes) -- omitted since the existing scissor/tile-binning bounding-box test already excludes out-of-bounds points/lines for every real case exercised so far; flagged as a known, minor follow-on gap if it ever proves to matter. New `ExecutorTest.cpp` tests: `DiscardsAPointBehindTheNearPlaneWhenDepthClampIsDisabled`/`RendersAPointBehindTheNearPlaneWhenDepthClampIsEnabled`, `ClipsALineAtTheNearPlaneWhenDepthClampIsDisabled`/`RendersAFullLineAcrossTheNearPlaneWhenDepthClampIsEnabled`. `ninja check-feme` passes in full, 2209/2268 (59 pre-existing `Unsupported`, 0 `Failed`), up from H7e's own 2016/2075 baseline. A real re-run of `dEQP-VK.clipping.clip_volume.depth_clamp.{point_list,line_list,line_strip}` now shows all 3 `Pass` (up from all 3 `Fail` before this fix); the broader `dEQP-VK.clipping.*` group (308 cases) shows 21 `Pass`, 12 `Fail` (all 12 are the pre-existing, unrelated `*_with_adjacency` topology gap, 0 regressions), 275 honestly `NotSupported`. See "Roadmap H7k: measured impact" in `VulkanCTSReport.md`) | H7 | `feme/lib/Graphics/Executor.cpp` | P2 |
| H7l | ~~**`*_with_adjacency` topologies fail pipeline creation for a depth/clip-volume draw.** Found via the same H7d `deqp-vk.clipping.clip_volume.depth_clamp.{triangle,line}_*_with_adjacency` reproduction: `vkCreateGraphicsPipelines` returns `VK_ERROR_INITIALIZATION_FAILED` for these 4 cases specifically, distinct from and unrelated to the depth-clamp/bias/bounds logic itself (the equivalent non-adjacency topologies in the same test group pass). Likely a geometry-shader-adjacent gap in adjacency-topology pipeline construction rather than anything in the depth-clamp path; needs its own root-cause investigation before it can be scoped as a fix~~ (done: not a geometry-shader gap at all -- `GraphicsPipeline.cpp`'s own `topologyHasAdjacency(*Topology) && !GeometryInfo` rejection (from roadmap H5d/H5e) is spec-incorrect. The real VUIDs (`-topology-00428`/`-00738`) only require the `geometryShader` *device feature* (already `VK_TRUE` since H5e), not that this specific pipeline bind a geometry stage; with no geometry stage, the spec says rasterization simply proceeds as if the non-adjacency topology were used, ignoring the adjacency-only vertices. Confirmed via the real `vktClippingTests.cpp` CTS source, which builds this whole `ClipVolume` group's pipelines with vertex+fragment stages only. Removed the rejection in `GraphicsPipeline.cpp`; `Executor.cpp` now accepts all four adjacency topologies unconditionally, reusing H5d's own `splitListPrimitiveAdjacency`/`splitStripPrimitiveAdjacency` helpers to extract core vertices and discard adjacency-only ones when no geometry stage is bound. Three stale unit tests corrected (see `VulkanCTSReport.md`'s "Roadmap H7l" section for full detail). Real CTS: all 4 originally-failing cases now `Pass`; the broader `dEQP-VK.clipping.*` mustpass group goes from 21 passed/12 failed (H7k baseline) to 33 passed/0 failed -- 0 regressions, 0 remaining failures) | H7 | `feme/lib/Vulkan/GraphicsPipeline.cpp` | P2 |
| H7m | ~~**A vertex-entry `vkCreateGraphicsPipelines` fails with "vertex stage wrapper requires attached feme.signature metadata"**, the vertex-side twin of roadmap H3a's own fragment-side finding (already flagged, in `VulkanCTSReport.md`'s own H3a write-up, as "not yet independently tracked for the vertex side"). Found via H7e's own real `deqp-vk` reproduction, newly reachable only because H7e's own feature-bit flip lets `dEQP-VK.draw.renderpass.point_size_clamp.point_size_clamp_max` and 8 of `dEQP-VK.dynamic_state.monolithic.line_width.{dyna_static,static_dyna}.*` clear their own `wideLines`/`largePoints` gate for the first time; nothing point-size/line-width-specific about the gap itself. H3a's own fragment-side root cause (`SPIRVResourceLowering.cpp`/`ResourceLowering.cpp`'s `addResourceEnvParams` rebuilding the stage entry function via `Function::Create`+`copyAttributesFrom`, which silently drops the `!feme.signature` metadata `CanonicalizeStagePass` already attached) was fixed with an explicit `NewF->copyMetadata(&F, 0)` for both files at the time, which should already cover the vertex stage too (the fix was not stage-specific) -- so this needs its own investigation into why a vertex-entry case still hits the bare "no metadata attached" error rather than H3a's fix simply working, not an assumption that H3a's fix missed the vertex side entirely~~ (done: no code change needed -- this row's own premise conflated two distinct cases under one observed error string. A real `git apply -R`/rebuild bisection against roadmap H7o's own "preserve function metadata across root/push-constant lowering" commit (`c760ed7b21b0`) isolated exactly which stage failed for each: `dEQP-VK.draw.renderpass.point_size_clamp.point_size_clamp_max`'s vertex shader genuinely does write a push constant (`layout(push_constant) uniform pointSizeBlk`), so it really did hit the vertex-side `feme-cpu-wrap-vertex: ...` error this row names -- but all 8 `dEQP-VK.dynamic_state.monolithic.line_width.*` cases instead hit `feme-cpu-wrap-fragment: ...` for their *fragment* shader's own push constant (`layout(push_constant) uniform PC { vec4 color; }`), not their vertex shader at all (whose own shader is push-constant-free and was never broken). `SPIRVPushConstantLoweringPass::run`'s inline `Function::Create` rebuild is stage-agnostic -- it lowers a push-constant access for whichever stage's entry function has one -- so H7o's single fix (an explicit `getAllMetadata`/`setMetadata` copy, landed to fix an unrelated fragment-side `min_sample_shading_*` case) already, incidentally, closed both halves of this row at once; reverting just that commit's two files and rebuilding reproduces both original errors verbatim, confirming the causal link precisely rather than assuming it. `ninja check-feme` (assertions-enabled, ccache build) stays unchanged at 2244/2271 (27 pre-existing, unrelated `Unsupported`, 0 `Failed`) -- no new tests needed since H7o's own `RuntimeCPUTest`/lit coverage already locks down the fixed behavior generically, not per-stage. Real CTS re-run confirms all 9 originally-named cases now pass (up from 0), and the bisection re-run confirms 0/9 pass with H7o's fix reverted, matching this row's own original error text exactly for both stages. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: no feature bit or extension changed by this investigation. See "Roadmap H7m: measured impact" in VulkanCTSReport.md for the full bisection) | H7 | `feme/lib/Transforms/CPU/{SPIRVResourceLowering,ResourceLowering}.cpp` | P2 |
| H8 | **Format table completeness for the graphics profile.** Roadmap C1/E24/E25 grew `vkGetPhysicalDeviceFormatProperties`/`ImageFormatProperties` from a stub to real per-format support, but the advertised set is still far short of Vulkan's mandatory format table for a graphics-capable device (BC/ETC2 compressed sampling, the full vertex-buffer format list, the mandatory blit/filter bits, multisample capability per format). This is the graphics analogue of C1 and, like C1, it unblocks whole Amber-based groups rather than single cases. Confirmed by roadmap H6g-a's own triage to also own the 68 `vkCreateRenderPass`/1 `vkGetPhysicalDeviceImageFormatProperties` -> `VK_ERROR_FORMAT_NOT_SUPPORTED` failures `dEQP-VK.mesh_shader.*` measures -- nothing mesh-shading-specific about them, just this row's own already-tracked format-table gap reached by a new CTS group | E25 | `feme/lib/Vulkan/Format.cpp`, `feme/lib/Graphics` | P0 |
| H9 | **Query and pipeline-statistics breadth.** `VK_QUERY_TYPE_PIPELINE_STATISTICS` is still declined (roadmap C5's own remaining exception) because no truthful counter existed; once the raster pipeline owns primitive/invocation counts per stage, the counters become real. Also covers inherited-render-pass secondary command buffers if that path is ever advertised | H4/H5 (for the per-stage counters) | `feme/lib/Vulkan/QueryPool.cpp`, `feme/lib/Graphics/Executor.cpp` | P2 |
| H10 | **WSI**: `VK_EXT_headless_surface` plus the full swapchain state machine first, then exactly one CI-exercisable platform surface, per FeMeVulkanDesign.md's "Window-system integration" decision. Requires this ICD to distinguish an instance-level extension list from the device-level one for the first time (`vkEnumerateInstanceExtensionProperties` currently returns the same list). Whole `dEQP-VK.wsi` group | V8 | `feme/lib/Vulkan/{EntryPoints,PhysicalDeviceInfo}.cpp`, new `Surface.{h,cpp}`/`Swapchain.{h,cpp}` | P2 |
| H11 | **Secondary command buffers inside a render pass**, and the render-pass state inheritance they carry — recorded as part of V7 but independently testable, and a prerequisite for several `renderpasses`/`dynamic_rendering` cases rather than a feature bit of its own | none | `feme/lib/Vulkan/CommandBuffer.cpp` | P2 |
| H12 | **Decide, once, which large optional extension groups stay out of scope**, and record the decision here rather than rediscovering it per run: `transform_feedback` (133,719 cases), `shader_object` (243,853), `fragment_shading_rate`, `fragment_shader_interlock`, `fragment_shading_barycentric`, `conditional_rendering`, `descriptor_indexing`, `sparse_resources`, `protected_memory`, `video`. None is required for a 1.4 submission; `descriptor_indexing` is the one with a real conformance consequence, since J-series ray tracing needs a large part of it (see §1.9.8's J2). Everything else defaults to out of scope, per Part 4 | none | this file | P1 |
| H13 | **Three separate, pre-existing blockers discovered by roadmap H7y's own real CTS re-run** of `dEQP-VK.clipping.user_defined.{clip_distance,clip_cull_distance}.vert_tess.*`/`vert_geom.*` (feature bits provisionally flipped on to measure), one layer below H7y's own targeted `getelementptr`-crash fix -- none specific to clip/cull-distance, though that is the first real CTS coverage to reach any of them. Broken out into H13a-H13c below (plus H13e, spun off once H13b's own fix exposed a further, distinct blocker in the same file) rather than as sub-letters of H7 (`H7`'s own single-letter suffix space, `H7a`-`H7z`, is fully used, and this project's own one-letter-deep nesting convention rules out `H7aa`) | H7y | see sub-rows | P3 |
| H13a | **A geometry stage's `gl_ClipDistance`/`gl_CullDistance` *input* fails stage linkage against the vertex stage's output.** `vkQueueSubmit` fails every `dEQP-VK.clipping.user_defined.{clip_distance,clip_cull_distance}.vert_geom.*` (non-`_fragmentshader_read`) case with `"vertex/domain stage output -> geometry stage input: element N and its producer element M disagree on component/row count or type"` -- a geometry stage reads every input per-primitive-vertex (`gl_in[i].gl_ClipDistance`-shaped), while the vertex stage that produces it declares a single, non-arrayed output of the same builtin, and whatever stage-linkage signature-matching code compares producer/consumer element shapes does not yet account for that arity difference. Needs a real investigation into where this signature comparison happens and what it needs to know about a geometry-stage input's own per-vertex arrayness to match it correctly against its vertex-stage producer | H13 | `feme/lib/Vulkan/GraphicsPipeline.cpp` (stage-linkage validation) | P3 |
| H13b | ~~**A tessellation-control (hull) shader's own patch-constant-phase compile cannot consume `gl_ClipDistance`/`gl_CullDistance`, or even `gl_Position` itself, as an input.** `vkCreateGraphicsPipelines` failed every `dEQP-VK.clipping.user_defined.{clip_distance,clip_cull_distance}.vert_tess.*` case with `"feme-cpu-wrap-patch-constant: unsupported patch-constant input system value"` -- root-caused (via a standalone `glslangValidator`+live-repro IR reduction, this milestone chain's established technique) to `CanonicalizeStage.cpp`'s `classifySPIRVElement`, not `PatchConstantWrapper.cpp` alone: for a `Hull`-stage function compiled as the patch-constant phase, an `AddrSpace == 7` (Input) element's `FromInputPatch` flag -- the one thing that routes it through `lowerPatchConstantInputLoad`'s already-correct, generic per-control-point array addressing rather than falling through `lowerPatchConstantSystemValue`'s narrow two-case (`OutputControlPointID`/`PatchVertices`) special-casing -- was set only for an ordinary varying (`SystemValue == None`) or `PatchVertices`, leaving every *other* system-value-tagged builtin input (`Position`, `ClipDistance`, `CullDistance` -- each a real per-control-point array, addressed exactly like an ordinary varying, just without a `Location` of its own) unclassified and therefore unsupported~~ (closed: widened `classifySPIRVElement`'s `FromInputPatch` condition to `Sys != SignatureSystemValue::OutputControlPointID` -- the only truly non-addressable, current-invocation-implicit scalar system value this phase reads -- so every other system value (or none) takes the ordinary `InputPatch`-addressed path; `PatchConstantWrapper.cpp`'s own `InputLoad` dispatch narrowed to match, routing to `lowerPatchConstantSystemValue` only for the two system values it actually implements (`OutputControlPointID`/`PatchVertices`), `lowerPatchConstantInputLoad` for everything else. New unit test `PatchConstantWrapperTest.LowersClipDistanceAndCullDistanceInputPatchElements`. `ninja check-feme` passes in full (2245/2272, 27 pre-existing `Unsupported`, 0 `Failed`, up 1 from this row's own new coverage). A real re-run of `dEQP-VK.clipping.user_defined.clip_distance.vert_tess.1_fragmentshader_read` (feature bits provisionally flipped to measure) confirms this row's own specific symptom is gone: `vkCreateGraphicsPipelines` no longer fails with "unsupported patch-constant input system value" at all -- but the same case now fails one layer further in, with a *different* diagnostic ("masked output store references an unknown patch-output signature element"), a distinct pre-existing gap in the same file's own `AddrSpace == 8` (Output) classification for this phase, spun off as new roadmap H13e rather than blocking this row (which closes on its own literal, now-fixed scope: consuming these builtins as an *input*). See "Roadmap H13b: measured impact" in `VulkanCTSReport.md`) | H13 | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/CPU/PatchConstantWrapper.cpp` | P3 |
| H13c | **A fragment stage cannot read back a varying whose producer is a tessellation-evaluation or geometry stage (rather than the vertex stage).** `vkCreateGraphicsPipelines` fails every `*_fragmentshader_read` case under both `vert_tess` and `vert_geom` with `"feme-cpu-wrap-fragment: synthetic fragment layouts only support vertex operand 0"` -- `feme-cpu-wrap-fragment`'s own synthetic fragment-input layout construction appears to hard-assume a single, fixed producer stage (vertex) rather than whichever pre-rasterization stage actually emits the varying being read; needs a real investigation into what generalizing that assumption requires, and whether it is specific to `ClipDistance`/`CullDistance` fragment inputs (roadmap H7x) or any varying produced by a non-vertex stage. live re-testing (this milestone chain's own session, after H13b's own fix) shows the picture is mixed rather than a clean split: every `vert_geom` `_fragmentshader_read` case still hits H13a's own upstream stage-linkage error first (unreached), while a `vert_tess` `_fragmentshader_read` case is inconsistent case-by-case -- `clip_distance.vert_tess.1_fragmentshader_read` still hits H13e's own upstream masked-output-store error first, but `.2_fragmentshader_read` through `.8_fragmentshader_read` reach this row's own "vertex operand 0" check directly, suggesting the underlying operand is only sometimes constant-foldable to a literal `0` depending on `numClipPlanes`-driven code shape -- needing its own further investigation once H13a/H13e are fixed, rather than assuming a uniform block across every case | H13 | `feme/lib/Transforms/CPU` (`feme-cpu-wrap-fragment`) | P3 |
| H13e | **A tessellation-control (hull) patch-constant-phase compile cannot correctly handle an ordinary (non-patch-frequency) *output* store -- `gl_Position`, or any per-control-point varying -- ending up in the same compiled clone as genuine patch-constant outputs (tessellation factors).** Found immediately after H13b's own fix let `dEQP-VK.clipping.user_defined.clip_distance.vert_tess.1_fragmentshader_read` clear its own input-side gate for the first time: `vkCreateGraphicsPipelines` now fails with `"feme-cpu-wrap-patch-constant: masked output store references an unknown patch-output signature element"` instead. Root cause (traced via the same live IR reduction as H13b): `CanonicalizeStage.cpp`'s `classifySPIRVElement`, for a `Hull`-stage patch-constant-phase element with `AddrSpace == 8` (Output) that is *not* patch-output-decorated (i.e. an ordinary per-control-point output, not a real tessellation factor/patch constant), falls through to a bare `Info.Direction = SignatureDirection::Input;` -- a placeholder that was never meant to be reached by a real case, since this phase's own design assumes every output store it ever sees is patch-frequency. This shape only arises because `splitTessellationControlEntry`'s barrier-based split (roadmap H4a) moves *everything* after the source module's own `OpControlBarrier` into the patch-constant clone verbatim, including ordinary per-control-point writes that happen to follow it in program order -- a real, previously-undiscovered gap in that split's own boundary, not specific to clip/cull-distance or even to `Position`: any per-control-point output write positioned after the barrier hits it. Needs a real investigation into either (a) teaching `classifySPIRVElement` to correctly classify (and `PatchConstantWrapper.cpp` to correctly lower) an ordinary per-control-point output store reached from the patch-constant phase, mirroring how this row's own H13b fix already treats the *input* side, or (b) revisiting `splitTessellationControlEntry`'s own split-boundary assumption that everything post-barrier is legitimately patch-frequency-only, which a real, unremarkable CTS shader (`dEQP-VK.clipping.user_defined.clip_distance.vert_tess.*`, no dynamic indexing, no geometry stage) already falsifies | H13 | `feme/lib/Transforms/Graphics/CanonicalizeStage.cpp`, `feme/lib/Transforms/CPU/PatchConstantWrapper.cpp` | P3 |
| H13d | ~~**`SPIRVResourceLoweringPass` does not recognize a single combined `OpTypeSampledImage`-style `handlefrombinding` call**, discovered via roadmap H7i's own real re-run of `dEQP-VK.texture.filtering_anisotropy.*`/`dEQP-VK.texture.filtering.2d.*` (0/128, 0/1698 respectively -- every graphics-pipeline case in both groups): `vkCreateGraphicsPipelines` fails with `VK_ERROR_INITIALIZATION_FAILED`, root-caused via `FEME_VULKAN_LOG_CREATION_ERRORS=1` to `UnsupportedOps.cpp`'s generic "register-bound resource handle the FeMe CPU target cannot normalize" diagnostic firing on a `handlefrombinding` call whose result type is a literal struct combining `spirv.Image` and `spirv.Sampler` in one handle (the shape glslang actually emits for an ordinary, idiomatic GLSL `uniform sampler2D` declaration) -- `classifySampledImage2DHandle`/`classifySamplerHandle` (`SPIRVResourceLowering.cpp`) only recognize a *separately*-declared image handle and sampler handle later composed by `insertvalue`/read apart by `extractvalue`, not a single call already returning the combined pair. Confirmed generic, not anisotropy-specific: an ordinary, unrelated `dEQP-VK.texture.filtering.2d.combinations.*` sweep reproduces the identical failure. Needs a real investigation into recognizing this combined-handle shape directly in `collectNormalizableHandles`/`classifySampledImage2DHandle` (most likely: detect a `handlefrombinding` call whose return type is a literal struct of exactly `{spirv.Image, spirv.Sampler}`, and treat every direct use of it as if it had first been split via a synthetic image/sampler pair, without requiring a real `insertvalue`/`extractvalue` round-trip to exist in the IR) -- this blocks nearly all real Vulkan CTS graphics-pipeline conformance for GLSL-style combined-sampler shaders, not just `samplerAnisotropy`~~ (closed: added `isCombinedSampledImageStructType`/`splitCombinedSampledImageHandles` to `SPIRVResourceLowering.cpp`, splitting a combined-handle `handlefrombinding` call into two synthetic single-resource calls before `foldSampledImageStructs` runs, when every use is a simple `extractvalue`. This exposed a second, previously-latent bug: the split's image-class and sampler-class handles legitimately share one `(Set, Binding)` -- exactly what a real `VK_DESCRIPTOR_TYPE_COMBINED_IMAGE_SAMPLER` binding is -- but `RangeKey` was keyed only on `(Set, Binding)` module-wide across every resource kind, so this collided as a false conflicting re-declaration and silently suppressed lowering; widened `RangeKey` to `(Set, Binding, BoundResourceClass)` to fix, confirmed no regression to any other conflict-detection case. New unit tests `LowersCombinedSampledImageHandleToImageSample`/`LeavesCombinedSampledImageHandleWithOtherUseUnchanged`, plus a lit test, `spirv-resource-lowering-combined-sampled-image.ll`. `ninja check-feme` passes in full, 2057/2116 (59 pre-existing `Unsupported`, 0 `Failed`, up 3 tests from this row's own new coverage). A real re-run of `dEQP-VK.texture.filtering.2d.*` confirms this row's own specific scope is fixed: no case fails at `vkCreateGraphicsPipelines`/`VK_ERROR_INITIALIZATION_FAILED` any longer (0/1698 before, all-`NotSupported`-or-init-failure; now 0/1698 still, but 258 real `Fail`s from actual, running pipelines producing wrong pixel values, 1440 honest `NotSupported` for unadvertised formats) -- a newly-exposed, distinct sampling-correctness gap, tracked as new roadmap H14 rather than blocking this row, since chasing it is new scope beyond the pipeline-creation failure this row's own text names. `samplerAnisotropy` (H7i) stays `VK_FALSE`: `dEQP-VK.texture.filtering_anisotropy.*` is unaffected by this fix (still `NotSupported`, correctly, since that group's own gate is the feature bit itself, not pipeline creation). See "Roadmap H13d: measured impact" in `VulkanCTSReport.md`) | H7i | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P1 |
| H14 | ~~**A combined-sampler (`uniform sampler2D`) graphics pipeline now creates and runs (roadmap H13d) but produces wrong pixel values.** A real re-run of `dEQP-VK.texture.filtering.2d.combinations.*` (0/1350; 192 `Fail`, 1158 honest `NotSupported`) shows every case that reaches rendering fails `Image verification failed` with every pixel in the surface wrong (e.g. `linear.linear.repeat.repeat`: "got 4096 invalid pixels" against a 64x64 surface, i.e. the whole image). Not yet root-caused: could be anywhere between the new `splitCombinedSampledImageHandles` split itself (e.g. a swapped image/sampler heap index or a wrong descriptor-heap base for one of the two synthetic handles) and an unrelated, pre-existing sampling-correctness bug this is simply the first real coverage to reach (H13d's own new lit/unit tests only check the *shape* of the lowered IR, not real pixel output end to end, so this gap was invisible to them). Needs a real IR/pixel-level reduction of the simplest failing case (e.g. `linear.linear.repeat.repeat`, no mip, no compute variant) to isolate whether the two synthetic handles the split produces are wired to the correct heap slots, before assuming any deeper sampling-kernel bug~~ (closed: root-caused via a from-scratch standalone Vulkan graphics-pipeline reproduction (full-screen triangle + combined sampler, isolating the bug from `splitCombinedSampledImageHandles`/SPIR-V lowering entirely, since a real compute-dispatch reproduction of the identical descriptor shape already sampled correctly) to `Executor.cpp`: building `FragmentResources`/`VertexResources` (`FRes`/`VRes`) for a real draw's fragment/vertex stage copied `Draw.Resources.ImageHeap`/`.SamplerHeap` (always empty at that point -- those fields are only *materialized* later, inside `PreparedFragmentBatch::create`/`PreparedVertexBatch::create`) but never copied `Draw.Resources.BoundImages`/`.BoundSamplers` (the actual bound-descriptor list `materializeImageHeap`/`materializeSamplerHeap` need to materialize from), unlike the mesh/task/geometry stages' own resource structs a few hundred lines below, which do copy both -- so every fragment- or vertex-stage image/sampler read saw an empty binding list and silently resolved to the all-zero (`Kind::None`) descriptor, regardless of set index, combined-vs-separate descriptor shape, or implicit-vs-explicit LOD (all ruled out one at a time via the standalone repro before finding this). Fixed by adding the two missing `FRes.BoundImages`/`FRes.BoundSamplers` (and `VRes.` counterparts) assignments. New regression test `DrawTest.cpp`'s `FragmentStageSamplesBoundImage` (confirmed to fail with the exact all-zero symptom before the fix, via a temporary `git stash` of the fix alone). `ninja check-feme` passes in full, 2058/2117 (59 pre-existing `Unsupported`, 0 `Failed`, up 1 test from this row's own new coverage). A real re-run of `dEQP-VK.texture.filtering.2d.*` confirms real progress but not full closure of graphics-pipeline sampling correctness: 50/1698 now genuinely pass (0/1698 before), 208 `Fail` (down from 258, but now a *different*, smaller-scale failure shape -- a real, non-black rendered image with a real but incorrect gradient, not an all-zero one), 1440 honest `NotSupported`. The remaining 208 fails are a distinct, still-unscoped correctness gap (likely nearest/linear-filtering texel-coordinate quantization, not a resource-plumbing bug), tracked as new roadmap H15 rather than blocking this row further. `samplerAnisotropy` (H7i) still stays `VK_FALSE`: `dEQP-VK.texture.filtering_anisotropy.*` is unaffected (still 0/128, all honest `NotSupported`, gated on the feature bit itself, not pipeline creation or sampling correctness). See "Roadmap H14: measured impact" in `VulkanCTSReport.md`) | H13d | `feme/lib/Graphics/Executor.cpp` | P1 |
| H15 | ~~**A real, running combined-sampler graphics pipeline (roadmap H14) still produces a real but incorrect rendered gradient**, discovered via H14's own real re-run of `dEQP-VK.texture.filtering.2d.*` (50/1698 pass, 208 `Fail` -- down from 258 before H14's own fix, but no longer the catastrophic all-zero shape H14 itself scoped). Not yet root-caused: could be a nearest/linear-filtering texel-coordinate quantization bug, a wrap-mode (`repeat`/`mirrored_repeat`/`clamp`) edge-case bug, or something else entirely in the real sampling kernel (`femeCpuImageSample2DV4F32`, `feme/runtime/CPU/FeMeRuntimeCPU.c`) -- H14's own standalone reproduction confirmed nearest-filtering sampling of a single, isolated texel is exact, so this is likely specific to a multi-texel filtering/wrap combination the simplest repro case didn't exercise. Needs a real IR/pixel-level reduction of the simplest still-failing case (e.g. one `nearest.nearest.repeat.repeat` case, comparing rendered vs. reference texel-by-texel) to isolate which stage of the sampling kernel first diverges~~ (closed: root-caused a pixel-level reduction of `dEQP-VK.texture.filtering.2d.combinations.nearest.nearest.repeat.repeat` (rendered image a real, correctly-shaped gradient but blocky in coarse 3-pixel bands, each band's value close to the *average* of the reference's own finer-grained values across that band -- the signature of a too-coarse mip level being nearest-sampled and magnified) to `femeRTSelectMipLevel` (`FeMeRuntimeCPU.c`) never applying the sampler's own `LodBias`/`MinLod`/`MaxLod` (`VkSamplerCreateInfo`'s `mipLodBias`/`minLod`/`maxLod`) to the level-of-detail value before selecting a mip level -- the Vulkan spec's own `lod = clamp(lod + mipLodBias, minLod, maxLod)` step was simply missing. This is exactly the technique CTS's own `mapSampler` (`vkImageUtil.cpp`) uses to realize a non-mipmapped `VkFilter` (`tcu::Sampler::NEAREST`/`LINEAR`/`CUBIC`): it sets `maxLod = 0.25` specifically to force every sample down to the base level regardless of how minified the real footprint is, and our own implicit-LOD sample was ignoring that clamp entirely, silently reading whatever coarser real mip level the derivatives implied instead. Fixed by widening `femeRTSelectMipLevel`'s own signature to take the full sampler descriptor and apply the bias-then-clamp step uniformly to both the implicit- and explicit-LOD paths, at all six call sites (2D, cube, 2D-array, cube-array, and the depth-comparison sample). Four new unit tests in `ImageSamplingTest.cpp` (`MaxLodClampsImplicitSampleToBaseLevel`, `MinLodClampsExplicitSampleAboveBaseLevel`, `LodBiasShiftsSelectedLevel`) confirm the fix directly; the existing `makeSampler` test helper's own default `MaxLod` (previously `0.0`, an unnoticed but now newly-significant "clamp to base level" default that would have masked every other mip-level test in the file) was widened to `1000.0` (`VK_LOD_CLAMP_NONE`) to keep those pre-existing tests' own intent (an unclamped mip range) accurate. `ninja check-feme` passes in full, 2061/2120 (59 pre-existing `Unsupported`, 0 `Failed`, up 3 tests from this row's own new coverage). A real re-run of `dEQP-VK.texture.filtering.2d.*` confirms real, substantial progress: 102/1698 now pass (up from 50), 156 `Fail` (down from 208), 1440 honest `NotSupported`; the specific `nearest.nearest.repeat.repeat` case this row's own reduction started from now passes outright. The remaining 156 fails are a narrower, distinct issue -- a real re-run isolated them to `magFilter=LINEAR` magnification cases specifically (off by roughly +/-1 of 255 per channel, a small rounding-scale discrepancy, not a gross wrong-level or wrong-texel error) -- tracked as new roadmap H16 rather than blocking this row further. `samplerAnisotropy` (H7i) still stays `VK_FALSE`: `dEQP-VK.texture.filtering_anisotropy.*` is unaffected by this fix (still 0/128, all honest `NotSupported`, gated on the feature bit itself). See "Roadmap H15: measured impact" in `VulkanCTSReport.md`) | H14 | `feme/runtime/CPU/FeMeRuntimeCPU.c` | P1 |
| H16 | ~~**A `magFilter=LINEAR` magnification sample still produces slightly-wrong (roughly +/-1 of 255 per channel) pixel values**, discovered via H15's own real re-run of `dEQP-VK.texture.filtering.2d.*`: 156 remaining `Fail`s, all real re-runs isolated to a `magFilter=LINEAR` case (`combinations.nearest.linear.*`, `combinations.linear.linear.*`, plus the `_mipmap_*` groups' own magnification cases) -- unlike H15's own gross wrong-mip-level symptom, this is a small, consistent rounding-scale discrepancy (e.g. `(50, 84, 171, 50)` rendered vs. `(49, 85, 170, 49)` reference), suggesting a bilinear-weight or format-conversion rounding-mode mismatch rather than a wrong texel or wrong level being read. Not yet root-caused: needs a real, careful comparison of `femeRTSampleLinear2D`'s own float-domain blend arithmetic (`FeMeRuntimeCPU.c`) and the UNORM8 encode/decode round-trip against the exact rounding convention `tcu::TexLookupVerifier`'s own tolerance expects, to determine whether the gap is in the bilinear weight computation itself, the final float-to-UNORM8 quantization, or a genuine (if small) tolerance mismatch in the verifier's own assumptions~~ (closed: H16's own roadmap description was wrong -- a fresh pixel-level reduction found errors up to 75/255 (far larger than a rounding-scale gap), and regrouping the full `combinations.*` sweep's own fails by `(minFilter, magFilter)` pair (not `minFilter` alone) revealed the real, generic pattern: every failing group is exactly where `minFilter`/`magFilter` differ in base type. Root-caused to every sampling entry point in `FeMeRuntimeCPU.c` unconditionally branching on `Samp.MagFilter` alone, regardless of whether a sample was magnifying or minifying -- `Samp.MinFilter` (already correctly populated by `Image.cpp`) was never read. Per the Vulkan spec, the mag/min filter choice must be based on the sign of the computed (biased-and-clamped) LOD: `lod <= 0` uses `magFilter`, `lod > 0` uses `minFilter`. Fixed by splitting `femeRTSelectMipLevel` into `femeRTComputeClampedLod` (shared bias/clamp) plus a new `femeRTUseLinearFilter` helper consulted at all five affected call sites (`femeCpuImageSample2DV4F32`, `femeCpuImageSampleCmp2DF32`, `femeCpuImageSample2DArrayV4F32`, `femeCpuImageSampleCubeV4F32`, `femeCpuImageSampleCubeArrayV4F32`); `FemeRTImplicitLodPlan` now also carries the resolved `ClampedLod` for the implicit-LOD paths. Four new unit tests in `ImageSamplingTest.cpp` (`ExplicitLodMinifyingUsesMinFilterNotMagFilter`/`ExplicitLodMagnifyingUsesMagFilterNotMinFilter`/`ImplicitLodMinifyingUsesMinFilterNotMagFilter`). `ninja check-feme` passes in full, 2064/2123 (59 pre-existing `Unsupported`, 0 `Failed`, up 4 tests). A real re-run of `dEQP-VK.texture.filtering.2d.combinations.*` confirms real progress: 144/1350 now pass (up from 72), 48 `Fail` (down from 120); the full `dEQP-VK.texture.filtering.2d.*` sweep: 184/1698 pass (up from 102), 74 `Fail` (down from 156). `dEQP-VK.texture.filtering_anisotropy.*` remains unaffected (still 0/128, all honest `NotSupported`). The 48 (of 74) remaining fails cluster cleanly into a `_mipmap_linear` minFilter (trilinear/`mipmapMode=LINEAR`) gap regardless of magFilter -- `femeRTPlanImplicitLod` already notes `MipFilter` is accepted but "not yet consulted" -- tracked as new roadmap H17; the remaining 4 (`b10g11r11_ufloat`, no `mipmap_linear` involved) are a separate, unrelated, pre-existing format-conversion gap, tracked as new roadmap H18. See "Roadmap H16: measured impact" in `VulkanCTSReport.md`) | H15 | `feme/runtime/CPU/FeMeRuntimeCPU.c` | P2 |
| H17 | ~~**Trilinear (`mipmapMode=VK_SAMPLER_MIPMAP_MODE_LINEAR`) filtering is not implemented**, discovered via roadmap H16's own real re-run of `dEQP-VK.texture.filtering.2d.*`: 70 of 74 remaining fails cluster cleanly into every `minFilter` with a `_mipmap_linear` suffix (`nearest_mipmap_linear`, `linear_mipmap_linear`), regardless of `magFilter` (all 12/16 or more per group). `femeRTPlanImplicitLod`'s (`FeMeRuntimeCPU.c`) own existing comment already flags this: `Samp->MipFilter` is accepted for the API shape a real trilinear blend needs, but not yet consulted -- both `Nearest` and `Linear` `mipmapMode` currently round to a single nearest level (`femeRTSelectMipLevel`'s own `+0.5f`-then-round), never blending between the two adjacent levels a real `mipmapMode=LINEAR` sampler is supposed to. Needs a real investigation into computing the two adjacent integer levels bracketing the clamped LOD float and blending `femeRTSampleLinear2D`/`femeRTSamplePoint2D`'s own per-level result by the LOD's fractional part, at every affected call site, when `Samp->MipFilter == Linear`~~ (closed: replaced the single-level `femeRTSelectMipLevel` with `femeRTSelectMipLevels`, which computes the two adjacent integer levels (`Level0`/`Level1`) bracketing the clamped LOD plus the fractional blend weight (`Frac`) between them, and a shared `femeRTSampleFiltered2D` helper wired into all five affected sample entry points (the four vector-returning 2D/array/cube/cube-array paths directly, `femeCpuImageSampleCmp2DF32`'s depth-comparison path via a parallel `femeRTSampleCmp2DAtLevel` helper, since it fetches raw texels rather than calling `femeRTSampleLinear2D`/`femeRTSamplePoint2D`). For `mipmapMode=NEAREST`, a single level is still picked, but via a new `femeRTNearestMipLevel` helper (nearest of `Level0`/`Level1` by `Frac`) rather than always the floor, preserving the pre-H17 round-to-nearest behavior -- a first version of this fix regressed that case (always picking the floor), caught by a new regression unit test before it shipped. Three new unit tests in `ImageSamplingTest.cpp` (`ExplicitLodTrilinearBlendsBetweenTwoMipLevels`/`ExplicitLodNearestMipFilterStillRoundsToOneLevel`/`ImplicitLodTrilinearBlendsBetweenTwoMipLevels`). `ninja check-feme` passes in full, 2067/2126 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.texture.filtering.2d.*` confirms the fix: 1698 cases, 252 Pass, 1440 `NotSupported` (unrelated compute-format gaps), only 6 `Fail` remaining (down from 74) -- all 6 are `formats.b10g11r11_ufloat.*` across every filter mode, the pre-existing, unrelated format gap tracked as roadmap H18 (whose fail count is corrected from 4 to 6 -- H16's own count only sampled the non-`_mipmap_linear`-suffixed subset of this format's fails). `dEQP-VK.texture.filtering_anisotropy.*` remains unaffected (still gated on the `samplerAnisotropy` feature bit via H13d). See "Roadmap H17: measured impact" in `VulkanCTSReport.md`) | H16 | `feme/runtime/CPU/FeMeRuntimeCPU.c` | P2 |
| H18 | ~~**`b10g11r11_ufloat` texture filtering fails independently of any mip/filter combination**, discovered via roadmap H16's own real re-run of `dEQP-VK.texture.filtering.2d.*`: the remaining fails not explained by H17's own trilinear gap are all `formats.b10g11r11_ufloat.*` -- no format-neutral pattern involved, so this is a distinct, format-specific gap, not trilinear-related. H17's own closure re-run corrects the count from 4 to the real 6 (all six `formats.b10g11r11_ufloat.{linear,linear_mipmap_linear,linear_mipmap_nearest,nearest,nearest_mipmap_linear,nearest_mipmap_nearest}` cases fail, not just the 4 non-`_mipmap_linear`-suffixed ones H16's own narrower sample happened to catch). Not yet root-caused: needs a real pixel-level reduction of one of these cases (e.g. `formats.b10g11r11_ufloat.nearest`, the simplest -- no mip, no blend) to determine whether the bug is in this packed-float format's own encode/decode round-trip, or something else specific to this one format~~ (closed: root-caused via a hand pixel-level reduction of `formats.b10g11r11_ufloat.nearest` to `femeRTUnpackR11G11B10Float` (`FeMeRuntimeCPU.c`), which packs each channel's exponent+mantissa field into `femeRTHalfToFloat`'s binary16 layout by left-shifting the whole field by one bit too many (`11 - MantBits` instead of the correct `10 - MantBits`) -- pushing each channel's exponent as high as binary16's own sign bit for larger exponents and corrupting almost every non-zero value (e.g. this format's own `1.0` decoded as `32768.0`). The only pre-existing unit test for this format used an all-zero-bits texel, which cannot distinguish a correct decode from this bug (`0` shifted by anything is still `0`), so it never caught the regression. Fixed by shifting the R/G (11-bit, 6-bit mantissa) fields by 4 and the B (10-bit, 5-bit mantissa) field by 5, matching `femeRTHalfToFloat`'s actual field layout. New unit test `LoadFetchesR11G11B10FloatNonZeroValues` (`ImageSamplingTest.cpp`) covers distinct non-zero values per channel (R=1.0, G=2.0, B=1.5). `ninja check-feme` passes in full, 2068/2127 (59 pre-existing `Unsupported`, 0 `Failed`, up 1 test). A real re-run of `dEQP-VK.texture.filtering.2d.*` confirms full closure: 1698 cases, 258 Pass (up from 252), 1440 honest `NotSupported` (unchanged), **0 `Fail`** (down from 6) -- every case in this group now passes or is honestly `NotSupported`. See "Roadmap H18: measured impact" in `VulkanCTSReport.md`) | H16 | `feme/runtime/CPU/FeMeRuntimeCPU.c` | P3 |
| ~~H19~~ | ~~**No `OpImageRead`/`OpImageWrite` (storage-image read/write) lowering exists anywhere in the transform path** -- the real prerequisite H7j's own row already names, split out as its own milestone rather than nested under H7 (see the "avoid nesting milestones more than one lowercase letter deep" rule): `SPIRVResourceLowering.cpp` explicitly rejected any storage-image handle (`Sampled == 2`) before this milestone, and no `feme.cpu.image.store.*` runtime entry point existed at all. H19a closes the plain, non-arrayed, non-multisample, mandatory-format-floor case; H19b-H19d break down the remaining shapes/format breadth H7j's own four feature bits still need~~ (closed: every shape (`H19a`-`H19e`: plain/arrayed/1D/3D/cube/cube-array 2D-array/1D-array), format-breadth (`H19f`/`H19h`/`H19j`/`H19n`/`H19o`), without-format (`H19f`/`H19i`), and multisample (`H19g`/`H19k`/`H19l`/`H19m`) sub-chain this milestone was split into has now reached a real-CTS-verified terminal closure, and all four feature bits H7j's own row named are `VK_TRUE`. No further open work remains directly under H19; see H7j's own closure note above for the full bit-by-bit breakdown)~~ | H7j | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c` | P2 |
| H19a | ~~**Plain2D storage-image read/write for exactly the Vulkan spec's mandatory storage-image format floor** (`R32_{SFLOAT,UINT,SINT}`, `R32G32B32A32_{SFLOAT,UINT,SINT}`), non-arrayed, non-multisample only. Needs a real `feme.cpu.image.store.2d.*` runtime entry point (none existed), `SPIRVResourceLowering.cpp` classifying a storage-image handle at all, and `Format.cpp` advertising `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` for the floor~~ (closed: added `femeCpuImageStore2DV4F32`/`V4I32` runtime helpers (`FeMeRuntimeCPU.c`, plus the pre-existing but incomplete `R32_UINT`/`R32_SINT` unpack-side gap E26 never closed for storage images specifically); `Store2D`/`Store2DI32` call vocabulary with a writing `MemoryEffects::argMemOnly(ModRefInfo::Mod)` annotation (`ImageCalls.h`/`.cpp`); `HandleKind::StorageImage2D` classification and lowering, including a combined load-and-store binding (`SPIRVResourceLowering.cpp`); `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` for the 6-format floor (`Format.cpp`). Real CTS then surfaced two further, genuinely separate compiler-phase gaps this shape's own real shader reached for the first time, both real prerequisites of their own rather than storage-image-specific: (1) `feme-cpu-simdize`'s `widenImageCall` had only ever widened read-only `feme.cpu.image.*` calls -- a store's own vector-typed `Texel` operand needed the same `WidenedVectorComponents` decomposition `widenResourceCall`'s vector `StoredValue` already gets, plus `Env.SideEffectMask` (not `Env.EntryMask`) as its governing lane mask, plus skipping result-vector construction for a void-returning call; (2) a real `dEQP-VK.image.load_store.with_format.2d.r32_sfloat` reduction found this shape's own `gl_GlobalInvocationID.xy` coordinate computation spills through a vector-typed `alloca` accessed one element at a time via `getelementptr` (the common SPIR-V `Function`-storage-class local-variable lowering) -- `PreparePass`'s existing `PromotePass` (`mem2reg`) alone refuses to promote any `alloca` with a `getelementptr` use at all, so `SROAPass` now runs first, splitting it into per-element scalar allocas `mem2reg` can then promote away entirely. Five new unit tests in `SPIRVResourceLoweringTest.cpp`, six in `ImageSamplingTest.cpp`, one in `SIMDizeTest.cpp`, one in `PrepareTest.cpp`, one in `FormatTest.cpp` (rewritten from a now-stale "never advertises" assertion). `ninja check-feme` passes in full, 2080/2139 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.2d.*` (78 cases, the non-arrayed subset) confirms the fix: the 12 mandatory-format cases (6 formats × plain/`_linear` tiling) now **all Pass** (down from 12 `Fail`), the remaining 66 honestly `NotSupported` (formats outside the floor). A broader real re-run of the full `dEQP-VK.image.load_store*` group (4400 cases, every shape) shows 66 Pass, 84 Fail, 4250 `NotSupported` -- the 84 fails are exactly the mandatory-format floor's own cases in every shape this row deliberately left out of scope (`1d`/`1d_array`/`2d_array`/`3d`/`cube`/`cube_array`'s multi-layer/-face cases; a `single_layer` sub-case of `2d_array`/`cube`/`cube_array` passes, since it binds a genuinely non-arrayed `image2D` view this row's own `Plain2D`-only classification already covers), tracked as new roadmap H19b/H19c/H19d below rather than blocking this row further. See "Roadmap H19a: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Transforms/CPU/SIMDize.cpp`, `feme/lib/Transforms/CPU/Prepare.cpp` | P2 |
| H19b | ~~**Arrayed (`2D_ARRAY`) storage-image read/write** for the same mandatory format floor H19a already covers -- `classifyStorageImage2DHandle`/`hasOnlySupportedStorageImageUses` (`SPIRVResourceLowering.cpp`) currently require `Dim == 2D`, non-arrayed only; a real re-run confirms every `dEQP-VK.image.load_store.with_format.2d_array.*` multi-layer case fails at pipeline creation the same way H19a's own cases did before it landed. Needs a real `Array2D` runtime entry point/call shape carrying a layer index, mirroring how `Sample2DArray`/`Load2DArray` already extend the non-arrayed read shapes~~ (closed: widened `classifyStorageImage2DHandle` to accept `Arrayed == 1` -- mapping to the pre-existing `ImageShape::Array2D` (already shared with `SampledImage2D`'s own arrayed sample/fetch, roadmap H7b-a) instead of unconditionally rejecting; widened `hasOnlySupportedStorageImageUses` to take a `Shape` parameter and require a 3-component `(x, y, layer)` coordinate for `Array2D`; `MS` (multisample) is still rejected -- that stays H19d's scope. Added `Store2DArray`/`Store2DArrayI32` call vocabulary (`ImageCalls.h`/`.cpp`) and `femeRTStoreTexel2DArray`/`femeRTStoreTexel2DArrayI32` runtime helpers plus `femeCpuImageStore2DArrayV4F32`/`femeCpuImageStore2DArrayV4I32` entry points (`FeMeRuntimeCPU.c`), mirroring `femeRTFetchTexel2D`'s own existing `Layer * Layout->SlicePitch` addressing and `femeCpuImageLoad2DArrayV4F32`'s own `Layer < 0` guard convention. Four new unit tests in `SPIRVResourceLoweringTest.cpp` (`LowersArrayedStorageImageWriteToImageStoreArray`, `LowersIntegerArrayedStorageImageWriteToImageStoreArrayV4I32`, `LowersArrayedStorageImageLoadStoreToBothCalls`, `LeavesAMultisampledStorageImageHandleAlone`), three in `ImageSamplingTest.cpp` (`StoreArrayWritesTexelIntoTheAddressedLayerOnly`, `StoreArrayWritesTexelIntoR32G32B32A32Uint`, `StoreArrayOutOfBoundsLayerIsANoOp`). `ninja check-feme` passes in full, 2086/2145 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.2d_array.*` (156 cases) confirms the fix: all 24 mandatory-format cases (6 formats x plain/`_linear` tiling x multi-layer/`_single_layer`) now **all Pass** (up from the multi-layer half `Fail`ing before this landed), the remaining 132 honestly `NotSupported` (formats outside H19a's own floor, unrelated to this row). A probe of two `1d_array.*` cases confirms they still `Fail` the same way (`Dim == 1D` is untouched by this row, correctly tracked as H19c's own scope, not this row's -- the H19c row description above already said `1d.*`/`3d.*`; this row's own final scope is `2d_array` only). See "Roadmap H19b: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c` | P3 |
| H19c | ~~**1D and 3D storage-image read/write** for the same mandatory format floor -- a real re-run confirms every `dEQP-VK.image.load_store.with_format.1d.*`/`3d.*` case fails the same way. Needs its own coordinate-shape runtime entry points (a single scalar `X` for 1D; `X`/`Y`/`Z` for 3D), distinct from H19b's own array-layer shape~~ (closed: widened `isCoordN`/`hasOnlySupportedStorageImageUses`/`lowerImageAccesses` (`SPIRVResourceLowering.cpp`) to accept a bare scalar coordinate for `Plain1D` (SPIR-V spells a 1D image's fetch/store coordinate as a scalar `i32`, not a 1-element vector) and a 3-component `(x, y, z)` coordinate for `Plain3D`; added `Load1D`/`Store1D`/`Load3D`/`Store3D` (plus `I32` variants) call vocabulary (`ImageCalls.h`/`.cpp`); added thin 1D wrapper helpers delegating to the existing 2D helpers with `Y=0` (a 1D image's `Height==1` already makes the 2D addressing math correct) and fresh 3D helpers bounds-checked against `Img->Depth` rather than `Img->ArrayLayers` (`FeMeRuntimeCPU.c`). Twenty new unit tests (8 in `SPIRVResourceLoweringTest.cpp`, 12 in `ImageSamplingTest.cpp`). `ninja check-feme` passes in full, 2106/2165 (59 pre-existing `Unsupported`, 0 `Failed`). A first real CTS re-run surfaced a genuine further gap this shape's own real shader reached for the first time: all 24 mandatory-format `1d.*`/`3d.*` cases moved from `NotSupported`/pipeline-creation-failure to a real `Fail` ("Image comparison failed") -- root-caused to `materializeImageDescriptor` (`CommandBuffer.cpp`), the Vulkan-level step that turns a real bound `VkImageView` into the CPU runtime's descriptor struct, whose dimension switch had only ever accepted the `Texture2D` family, silently leaving a real `Texture1D`/`Texture3D` binding as the all-zero descriptor (the compiler/runtime-level lowering above was correct throughout; this was a distinct, Vulkan-API-layer gap). Fixed by adding `Texture1D`/`Texture3D` to the accepted-dimension switch (both fall through the same per-layer addressing `Texture2D` already uses) and computing `Dst.Depth` from the image's own per-mip depth for `Texture3D` instead of the hard-coded `1` every other dimension keeps. A second real re-run then confirms the fix: all 24 mandatory-format cases now **all Pass** (up from all 24 `Fail`), the remaining 132 honestly `NotSupported` (formats outside the floor). A probe of `1d_array.r32_uint` confirms it still fails at pipeline creation, untouched by this row's non-arrayed-only scope (tracked as new roadmap H19e below). See "Roadmap H19c: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Vulkan/CommandBuffer.cpp` | P3 |
| H19d | ~~**Cube and cube-array storage-image read/write** for the same mandatory format floor -- a real re-run confirms every `dEQP-VK.image.load_store.with_format.cube.*`/`cube_array.*` case fails the same way H19b/H19c's own shapes do~~ (closed: widened `classifyStorageImage2DHandle` (`SPIRVResourceLowering.cpp`) to accept `Dim == DimCube` (arrayed or not), mapping directly to the pre-existing `ImageShape::Array2D` rather than a distinct `Cube`/`CubeArray` shape -- confirmed via a real CTS shader dump (`dEQP-VK.image.load_store.with_format.cube.r32_uint`) that a storage cube image's own `imageLoad`/`imageStore` addresses its texel by an ordinary `(x, y, face)` triple, structurally identical to `Array2D`'s own `(x, y, layer)` triple, unlike a *sampled* cube's direction-vector addressing (`createSampleCube`). A non-arrayed cube still needs the 3-component coordinate (six faces to select between even without `Arrayed`), unlike `Dim::2D` where `Arrayed` itself is what adds the third component -- so `DimCube` maps to `Array2D` unconditionally, independent of the `Arrayed` flag. This needed no new call vocabulary, no new runtime helper, and no `CommandBuffer.cpp` change: the existing `Store2DArray`/`Load2DArray` shape (H19b) and `materializeImageDescriptor`'s existing `TextureCube`/`TextureCubeArray` handling (roadmap H7b) already covered everything a cube storage image needs. Five new unit tests in `SPIRVResourceLoweringTest.cpp` (`LowersCubeStorageImageWriteToImageStoreArray`, `LowersIntegerCubeStorageImageWriteToImageStoreArrayV4I32`, `LowersCubeStorageImageLoadStoreToBothCalls`, `LowersCubeArrayStorageImageWriteToImageStoreArray`, `LeavesAMultisampledCubeStorageImageHandleAlone` -- replacing the now-stale `LeavesACubeStorageImageHandleAlone` negative test). `ninja check-feme` passes in full, 2110/2169 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.cube.*`/`cube_array.*` (312 cases) confirms the fix: 48 Pass (up from 0), 0 Fail, 264 honestly `NotSupported` (formats outside the 6-format floor). A probe of `load_store_multisample.2d.r32_uint.samples_2` confirms it's still correctly honestly `NotSupported` on `shaderStorageImageMultisample`, untouched by this row's non-multisample-only scope. The format/configuration breadth H19d's own original text also bundled (`shaderStorageImageExtendedFormats`'s non-mandatory formats, `shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat`) is split out as new roadmap H19f below, and `shaderStorageImageMultisample` as new roadmap H19g, rather than being this row's own scope (this row's final scope is cube/cube-array shape support only). See "Roadmap H19d: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P3 |
| H19e | ~~**Arrayed (`1D_ARRAY`) storage-image read/write** -- the one dimension left out of both H19b's own array scope (`2D_ARRAY` only) and H19c's own non-arrayed scope (`1D`/`3D` only). A real re-run confirms `dEQP-VK.image.load_store.with_format.1d_array.*` still fails at `vkCreateComputePipelines` with `VK_ERROR_INITIALIZATION_FAILED`, the same pre-H19b/H19c pipeline-creation failure mode. Needs `classifyStorageImage2DHandle`/`hasOnlySupportedStorageImageUses` (`SPIRVResourceLowering.cpp`) to accept `Dim1D`+`Arrayed`, a coordinate shape carrying both an `X` and a layer index (distinct from H19b's `(x, y, layer)` and H19c's bare scalar `X`), and `materializeImageDescriptor` (`CommandBuffer.cpp`) to accept `Texture1DArray` in its dimension switch (the same descriptor-materialization gap H19c's own closure just fixed for `Texture1D`/`Texture3D`)~~ (closed: widened `classifyStorageImage2DHandle` (`SPIRVResourceLowering.cpp`) to accept an arrayed `Dim1D` handle -- previously rejected alongside the correctly-rejected arrayed `Dim3D` case by a single combined check -- mapping it to a new `ImageShape::Array1D` with a 2-component `(x, layer)` coordinate, distinct from `Array2D`'s 3-component `(x, y, layer)` since a 1D image has no spatial `Y` to begin with. Updated `lowerImageAccesses`' coordinate-extraction (`Y` stays null for `Array1D` same as `Plain1D`; the existing `C2` "layer/Z" slot becomes `Coord[1]`, not `Coord[2]`, for this shape alone) and both the load- and store-dispatch switches. Added `Load1DArray`/`Load1DArrayI32`/`Store1DArray`/`Store1DArrayI32` call vocabulary (`ImageCalls.h`/`.cpp`) and `femeRTFetchTexel1DArray`/`femeRTStoreTexel1DArray` (plus `I32`) runtime helpers -- thin wrappers reusing `femeRTFetchTexel2D`'s/`femeRTStoreTexel2DArray`'s own existing array-layer parameter with `Y == 0`, needing no new addressing math (`FeMeRuntimeCPU.c`). Added `Texture1DArray` to `materializeImageDescriptor`'s (`CommandBuffer.cpp`) accepted-dimension switch, needing no `Dst.Width`/`Height`/`Depth` changes beyond what `Texture2DArray` already computes. Seven new unit tests in `SPIRVResourceLoweringTest.cpp` (three new positive tests plus a renamed `LeavesAnArrayed3DStorageImageHandleAlone` covering the one rejection that remains, replacing the now-stale `LeavesAnArrayed1DStorageImageHandleAlone`), six in `ImageSamplingTest.cpp`. `ninja check-feme` passes in full, 2119/2178 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.1d_array.*` (156 cases) confirms the fix: all 24 mandatory-format cases now **all Pass** (up from all pipeline-creation failures), the remaining 132 honestly `NotSupported` (formats outside the floor). See "Roadmap H19e: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Vulkan/CommandBuffer.cpp` | P3 |
| H19f | **The format/configuration breadth `shaderStorageImageExtendedFormats`/`shaderStorageImageReadWithoutFormat`/`shaderStorageImageWriteWithoutFormat` actually need**, split out of H19d's own original bundled scope once H19d itself narrowed to cube/cube-array shape support only. `shaderStorageImageExtendedFormats` needs every `VkFormat` beyond the current 6-format mandatory floor (`Format.cpp`'s `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` advertisement) to actually round-trip through `FeMeRuntimeCPU.c`'s fetch/store helpers, not just the floor H19a hard-coded. `shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat` need a shader to declare a storage image with no `layout(format)` qualifier at all (SPIR-V's `Format == Unknown`) and still read/write correctly against whatever format the bound view actually has at runtime, rather than assuming the compiled shader's own declared format always matches -- a real re-run of `dEQP-VK.image.format_reinterpret.*`/an unqualified-format subset of `load_store.*` needed to scope the actual gap size once attempted (partially closed: confirmed storage-image *reads* already worked for any format `femeRTImageFormatElementSize` recognizes -- `femeRTFetchTexel2D` et al. reuse the same `femeRTUnpackImageTexel(I32)` sampled-image reads use -- so the real gap was write-side only. Widened `femeRTPackImageTexel`/`femeRTPackImageTexelI32` (`FeMeRuntimeCPU.c`) and `Format.cpp`'s `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` switch to also cover `R16G16B16A16_{SFLOAT,UINT,SINT}` (a new `femeRTFloatToHalf` helper, the inverse of the existing `femeRTHalfToFloat`, for the float case; 16-bit truncation for the integer cases) -- three new unit tests in `ImageSamplingTest.cpp`, one widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2122/2181 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r16g16b16a16_{sfloat,uint,sint}*` (140 cases) confirms the fix: 66 Pass (up from 0), 0 Fail, 74 honestly `NotSupported` (the `_unorm`/`_snorm` variants of the same 4-channel-16-bit format, still outside this row's own pack support). `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`: the real Vulkan mandatory extended-format list is far larger than the one format family this change closes (single- and two-channel `R8`/`R16`/`R32` formats, `10:10:10:2`/`11:11:10` packed formats, etc.), most without any `ResourceFormat`/pack-table entry yet. `shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat` also stay `VK_FALSE`: `dEQP-VK.image.load_store.without_format.*`'s own `checkSupport` (`vktImageLoadStoreTests.cpp`) gates on the *format's own* `VK_FORMAT_FEATURE_2_STORAGE_{READ,WRITE}_WITHOUT_FORMAT_BIT` tiling features, which `Format.cpp` does not compute at all today (a distinct, larger gap than this row's own pack/unpack breadth work, and dependent on the full extended-format list existing first since `without_format.*`'s own 828 real CTS cases exercise exactly that list). Split into two new sibling rows below: H19h (the rest of the mandatory extended-format list) and H19i (the without-format format-feature-bit work))~~ | H19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/lib/Vulkan/Format.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c` | P3 |
| H19g | **`shaderStorageImageMultisample`**, split out of H19d's own original bundled scope. `dEQP-VK.image.load_store_multisample.*` (252 cases) is still all honestly `NotSupported` on this bit today (confirmed via a probe of `load_store_multisample.2d.r32_uint.samples_2`, unaffected by H19d's own cube/cube-array closure). Needs `classifyStorageImage2DHandle` to accept `MS == 1`, a per-sample coordinate component the runtime's fetch/store helpers do not take today (no `feme.cpu.image.*` entry point accepts a sample index), and `PhysicalDeviceInfo.cpp` to flip the feature bit plus raise whatever `VkPhysicalDeviceLimits` sample-count field gates it once real (partially closed, scoped to a plain (non-arrayed) `Dim::2D` handle only -- the real CTS test shaders only ever declare `uniform image2DMS`/storage images, never a sampled `sampler2DMS`, so this row's whole scope is `OpImageRead`/`OpImageWrite`'s own explicit-`Sample` operand against a storage image, not any sampled-image multisample path. Since `llvm.spv.resource.getpointer` is a real, fixed 2-operand upstream LLVM intrinsic shared with the DXIL-to-SPIR-V direction (cannot grow a 3rd "Sample" operand), the fix folds `Sample` into the `Coordinate` vector's own trailing lane instead -- structurally identical to how `Array2D`'s own array-layer is already the coordinate's 3rd component -- via a new `appendVectorLane` helper in `SPIRVToLLVMPatterns.cpp`'s `ImageLoadPattern`/`ImageWritePattern` (gated to `ImageReadOp` only, and only for a plain multisampled 2D image, via a new `isPlainMultisampled2DImage` check). `SPIRVResourceLowering.cpp` gained `ImageShape::Plain2DMS` (3-component `(x, y, sample)` coordinate, reusing `Array2D`'s existing 3rd-component extraction machinery unchanged); `ImageCalls.h`/`.cpp` gained `Store2DMS`/`Store2DMSI32` call vocabulary and widened `Load2DI32`'s own signature to carry a `Sample` operand (mirroring `Load2D`'s existing one from roadmap F8c). `FeMeRuntimeCPU.c` gained new `femeRTStoreTexel2DMS`/`I32` helpers and widened `femeRTFetchTexel2DI32`/`femeCpuImageLoad2DV4I32` to accept `Sample` -- the storage layout itself (`FemeImageSubresourceLayout::SampleStride`) was already fully multisample-ready from earlier F8c/F8b work, so only the helper *signatures* needed widening. Four new positive/negative `SPIRVResourceLoweringTest.cpp` cases (plus a renamed `LeavesAnArrayedMultisampledStorageImageHandleAlone`, replacing the now-stale non-arrayed rejection test), six new `ImageSamplingTest.cpp` runtime cases, and a new lit test `spirv-to-llvm-image-access-multisample.mlir` (plus one new `expected-error` case in `spirv-to-llvm-image-access-invalid.mlir` for the still-rejected arrayed-multisample case). `ninja check-feme` passes in full, 2134/2193 (59 pre-existing `Unsupported`, 0 `Failed`). A real CTS re-run (with the feature bit temporarily flipped on, purely to probe the actual shader path -- reverted before landing, since the group is not honestly closeable yet) surfaced a distinct, larger, unrelated prerequisite: every `dEQP-VK.image.load_store_multisample.2d.*` case's own verification shader source contains a `for (int sampleNdx = 0; ...) { imageStore/imageLoad(...) }` loop, which `feme-cpu-linearize` rejects outright with "loop ... has an internal branch in 'Flow'; unsupported" -- a pre-existing control-flow-linearization gap, unrelated to storage-image multisample addressing itself, that blocks 100% of this row's own real CTS coverage regardless of how complete the addressing-side fix above is. `shaderStorageImageMultisample` therefore stays `VK_FALSE`: 0 real passes were observed even with the bit forced on, so flipping it now would be dishonest. Split the loop-linearization prerequisite into new roadmap row H19k below; H19g itself is otherwise addressing-complete for the plain 2D case and only needs H19k's own closure (plus a future arrayed-2D-multisample follow-on row) to actually pass real cases) | H19 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/include/feme/Transforms/CPU/ImageCalls.h`, `feme/lib/Transforms/CPU/ImageCalls.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c` | P3 |
| H19h | **The rest of the mandatory `shaderStorageImageExtendedFormats` list**, split out of H19f once it narrowed to `R16G16B16A16_{SFLOAT,UINT,SINT}` only. Needs new `ResourceFormat` entries and pack/unpack support (mirroring the sampled-image side, `femeRTUnpackImageTexel`/`I32`) for the rest of the real Vulkan mandatory extended-format list: single- and two-channel `R8`/`R16`/`R32` formats (`R8_{UNORM,SNORM,UINT,SINT}`, `R8G8_*`, `R16_{UNORM,SNORM,UINT,SINT,SFLOAT}`, `R16G16_*`, `R32G32_*`), plus the packed 32-bit formats `A2B10G10R10_{UNORM,UINT}_PACK32`/`B10G11R11_UFLOAT_PACK32` (distinct component order from the existing `R10G10B10A2`/`R11G11B10` decode this project already has for the *sampled*-image side -- needs confirming the two actually agree before reusing that decode for storage). Only once this list is materially complete should `shaderStorageImageExtendedFormats` itself flip to `VK_TRUE` (partially closed: found `R16G16B16A16_UNORM`/`_SNORM` already existed as `ResourceFormat` enum values -- `mapVkFormat` already mapped both real `VkFormat`s to them -- but were entirely unimplemented at the runtime level, not even readable (`femeRTImageFormatElementSize` had no case for either, so both decoded all-zero and neither was advertised for sampling at all). Added `femeRTImageFormatElementSize` cases (8 bytes each), new `femeRTUnpackR16G16B16A16Unorm`/`Snorm` and their pack-side inverses `femeRTPackR16G16B16A16Unorm`/`Snorm` (`FeMeRuntimeCPU.c`, the 16-bit-per-component analogues of the existing `femeRTUnpackR8G8B8A8Unorm`/`Snorm` helpers), wired both into `femeRTUnpackImageTexel`/`femeRTPackImageTexel`'s dispatch switches, and added both formats to `Format.cpp`'s sampled-image and storage-image feature switches. Four new unit tests in `ImageSamplingTest.cpp`, two widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2126/2185 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r16g16b16a16_{unorm,snorm}*` (56 cases) confirms the fix: 44 Pass (up from 0), 0 Fail, 12 `NotSupported` (the `buffer.*` texel-buffer variants, gated on a distinct `VK_FORMAT_FEATURE_STORAGE_TEXEL_BUFFER_BIT` capability this row's own image-only scope does not cover -- correct, honest behavior, matching this project's pre-existing narrower `isTexelBufferFormatSupported` scope). `shaderStorageImageExtendedFormats` itself still stays `VK_FALSE`: the real mandatory list's single/two-channel `R8`/`R16`/`R32` formats and the packed 32-bit formats under their storage-specific component ordering still have no `ResourceFormat` entry at all. Split the remainder into new roadmap row H19j below) | H19 | `feme/lib/Vulkan/Format.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/include/feme/Vulkan/Format.h` | P3 |
| H19j | **The single/two-channel `R8`/`R16`/`R32` formats and the packed 32-bit formats' storage-specific component ordering**, split out of H19h once it narrowed to `R16G16B16A16_{UNORM,SNORM}` only. Needs entirely new `ResourceFormat` enum entries (`feme/include/feme/Target/CPU/RuntimeABI.h`) for `R8_{UNORM,SNORM,UINT,SINT}`, `R8G8_{UNORM,SNORM,UINT,SINT}`, `R16_{UNORM,SNORM,UINT,SINT,SFLOAT}`, `R16G16_{UNORM,SNORM,UINT,SINT,SFLOAT}`, and `R32G32_{UINT,SINT}`'s own storage-mandatory partial-component siblings (the plain `R32G32_FLOAT` already exists but is sampled-only today, not storage-image-packable) -- plus new `mapVkFormat` entries, `femeRTImageFormatElementSize`/unpack/pack cases (mirroring the 4-component helpers' own partial-component convention: unread components pad `0`/alpha pads `1`), and `Format.cpp` sampled/storage feature-bit wiring for each. The packed 32-bit formats `A2B10G10R10_{UNORM,UINT}_PACK32`/`B10G11R11_UFLOAT_PACK32` need their own real component-order verification first: this project's existing `R10G10B10A2`/`R11G11B10` sampled-image decode needs confirming it agrees with what a real storage-image `imageStore` through the same `VkFormat` actually expects before reusing it, since a sampled-image format's decode and a storage-image format's decode are not guaranteed identical bit layouts by the Vulkan spec alone (partially closed: added the single-channel `R8_{UNORM,SNORM,UINT,SINT}` slice only (4 new `ResourceFormat` entries appended at the tail of the enum per this project's own append-only convention, plus `mapVkFormat`/`formatElementSize`/sampled- and storage-feature-bit wiring in `Format.cpp`, and 8 new always-inline pack/unpack helpers in `FeMeRuntimeCPU.c` wired into all five existing dispatch tables). Both `feme/lib/Graphics/ImageFixture.cpp`'s `formatFixtureName`/`parseFixtureFormat` and `feme/tools/feme-run/feme-run.cpp`'s `imageFormatElementSize` needed new cases too, both being deliberately exhaustive `switch (ResourceFormat)` tables with no `default:` (by contrast, `feme-run.cpp`'s own `parseResourceFormat` is a curated allowlist and did not need updating). New unit tests: 8 in `ImageSamplingTest.cpp` (read float/int and write float/int paths), 1 new plus 2 widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2157/2216 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r8_*` (112 cases) confirms the fix: 88 Pass (up from 0), 0 Fail, 24 honestly `NotSupported` (shape variants outside this project's existing storage-image shape coverage, unrelated to format). The `load-store.txt` mustpass regression caselist (3446 cases) shows 348 Pass (up from the pre-existing 260 baseline by exactly the +88 new R8 passes), 0 Fail, 3098 `NotSupported` -- 0 regressions. `R8G8_*`, `R16_*`, `R16G16_*`, `R32G32_{UINT,SINT}`, and the packed 32-bit formats all remain entirely unimplemented; `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`. Split the remainder into new roadmap row H19n below. See "Roadmap H19j: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Vulkan/Format.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/include/feme/Target/CPU/RuntimeABI.h` | P3 |
| H19n | **The rest of the mandatory `shaderStorageImageExtendedFormats` list**, split out of H19j once it narrowed to the single-channel `R8_{UNORM,SNORM,UINT,SINT}` slice only. Needs entirely new `ResourceFormat` enum entries for `R8G8_{UNORM,SNORM,UINT,SINT}`, `R16_{UNORM,SNORM,UINT,SINT,SFLOAT}`, `R16G16_{UNORM,SNORM,UINT,SINT,SFLOAT}`, and `R32G32_{UINT,SINT}`'s own storage-mandatory partial-component siblings, plus the packed 32-bit formats `A2B10G10R10_{UNORM,UINT}_PACK32`/`B10G11R11_UFLOAT_PACK32` (still needing their own component-order verification against the existing sampled-image decode, per H19j's own unresolved note). Only once this list is materially complete should `shaderStorageImageExtendedFormats` itself flip to `VK_TRUE`, and only then can H19i's own `without_format.*` work proceed, since that row's 828 real CTS cases exercise this same full list (partially closed: added the two-channel `R8G8_{UNORM,SNORM,UINT,SINT}` slice only (4 new `ResourceFormat` entries appended at the enum's own tail, `mapVkFormat`/`formatElementSize`/sampled- and storage-feature-bit wiring in `Format.cpp`, and 8 new always-inline pack/unpack helpers in `FeMeRuntimeCPU.c` -- each a straightforward two-lane widening of the existing single-channel `R8` helpers H19j added, packed/unpacked little-endian via `__builtin_memcpy` into/out of a `uint16_t` the same way the existing 4-component 16-bit-per-lane helpers already do -- wired into all five existing dispatch tables). `ImageFixture.cpp`'s `formatFixtureName`/`parseFixtureFormat` and `feme-run.cpp`'s `imageFormatElementSize` needed new cases too, same as H19j's own R8 slice. New unit tests: 8 in `ImageSamplingTest.cpp` (read float/int and write float/int paths), 1 new (`MapsTwoChannelR8G8Formats`) plus 2 widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2166/2225 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r8g8_*` (112 cases) confirms the fix: 88 Pass (up from 0), 0 Fail, 24 honestly `NotSupported` (the same shape variants outside existing coverage as H19j's own R8 re-run, unrelated to format). The `load-store.txt` mustpass regression caselist (3446 cases) shows 436 Pass (up from the H19j-era 348 baseline by exactly the +88 new R8G8 passes), 0 Fail, 3010 `NotSupported` -- 0 regressions. `R16_*`, `R16G16_*`, `R32G32_{UINT,SINT}`, and the packed 32-bit formats all remain entirely unimplemented; `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`. Further partially closed: added the single-channel `R16_{FLOAT,UNORM,SNORM,UINT,SINT}` slice next (5 new `ResourceFormat` entries appended at the enum's own tail, `mapVkFormat`/`formatElementSize`/sampled- and storage-feature-bit wiring in `Format.cpp`, and new always-inline pack/unpack helpers in `FeMeRuntimeCPU.c` -- `R16_UNORM`/`_SNORM` get dedicated `femeRTUnpackR16Unorm`/`Snorm`/`femeRTPackR16Unorm`/`Snorm` helpers and `R16_UINT`/`_SINT` get `femeRTUnpackR16Uint`/`Sint`/`femeRTPackR16Uint`/`Sint` (the `Sint` pack sharing `Uint`'s truncation, mirroring the `R8G8` precedent), while `R16_FLOAT` has no standalone helper at all and is instead wired directly into the `femeRTUnpackImageTexel`/`femeRTPackImageTexel` switch cases using `femeRTHalfToFloat`/`femeRTFloatToHalf` -- mirroring the existing `R16G16B16A16_FLOAT` case, since those two conversion helpers are themselves defined later in the file than the small pack/unpack helpers, so a standalone `R16_FLOAT` helper placed alongside `R16_UNORM`/`_SNORM` would be a use-before-declaration error -- wired into all five existing dispatch tables). `ImageFixture.cpp`'s `formatFixtureName`/`parseFixtureFormat` and `feme-run.cpp`'s `imageFormatElementSize` needed new cases too, same as the `R8G8` slice; two pre-existing `ImageTest.cpp` tests that used `VK_FORMAT_R16_UNORM` as their own "known unmapped format" fixture needed to switch to `VK_FORMAT_R16G16_UNORM` instead, since `R16_UNORM` is now mapped. New unit tests: 10 in `ImageSamplingTest.cpp` (read float/int and write float/int paths, including the `FLOAT` variant), 1 new (`MapsSingleChannelR16Formats`) plus 2 widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2177/2236 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r16_*` (140 cases) confirms the fix: 110 Pass (up from 0), 0 Fail, 30 honestly `NotSupported` (the same shape variants outside existing coverage as the prior slices' own re-runs, unrelated to format). The `load-store.txt` mustpass regression caselist (3446 cases) shows 546 Pass (up from the `R8G8`-era 436 baseline by exactly the +110 new R16 passes), 0 Fail, 2900 `NotSupported` -- 0 regressions. `R16G16_*`, `R32G32_{UINT,SINT}`, and the packed 32-bit formats all remain entirely unimplemented; `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`. Further partially closed: added the two-channel `R16G16_{FLOAT,UNORM,SNORM,UINT,SINT}` slice next (5 new `ResourceFormat` entries appended at the enum's own tail, `mapVkFormat`/`formatElementSize`/sampled- and storage-feature-bit wiring in `Format.cpp`, and new always-inline pack/unpack helpers in `FeMeRuntimeCPU.c` -- `R16G16_UNORM`/`_SNORM` get dedicated `femeRTUnpackR16G16Unorm`/`Snorm`/`femeRTPackR16G16Unorm`/`Snorm` helpers and `R16G16_UINT`/`_SINT` get `femeRTUnpackR16G16Uint`/`Sint`/`femeRTPackR16G16Uint`/`Sint` (the `Sint` pack sharing `Uint`'s truncation, mirroring every prior INT-pair format), each using `const uint16_t Raw[2]`/`uint16_t Out[2]` array-based signatures following the `R16G16B16A16_*` multi-16-bit-lane precedent rather than `R8G8`'s single-packed-scalar precedent, while `R16G16_FLOAT` again has no standalone helper and is instead wired directly into the `femeRTUnpackImageTexel`/`femeRTPackImageTexel` switch cases using `femeRTHalfToFloat`/`femeRTFloatToHalf` twice per texel, mirroring `R16_FLOAT`'s own prior asymmetry for the same use-before-declaration reason -- wired into all five existing dispatch tables). `ImageFixture.cpp`'s `formatFixtureName`/`parseFixtureFormat` and `feme-run.cpp`'s `imageFormatElementSize` needed new cases too, same as the prior slices; two pre-existing `ImageTest.cpp` tests that used `VK_FORMAT_R16G16_UNORM` as their own "known unmapped format" fixture (set there by the `R16` slice) needed to switch to `VK_FORMAT_R16G16B16_UNORM` instead, since `R16G16_UNORM` is now mapped -- the third session in a row this exact pattern has recurred as each slice maps its predecessor's placeholder fixture format. New unit tests: 10 in `ImageSamplingTest.cpp` (read float/int and write float/int paths, including the `FLOAT` variant), 1 new (`MapsTwoChannelR16G16Formats`) plus 2 widened in `FormatTest.cpp`. `ninja check-feme` passes in full, 2188/2247 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of `dEQP-VK.image.load_store.with_format.*.r16g16_*` (140 cases) confirms the fix: 110 Pass (up from 0), 0 Fail, 30 honestly `NotSupported` (the same shape variants outside existing coverage as the prior slices' own re-runs, unrelated to format). The `load-store.txt` mustpass regression caselist (3446 cases) shows 656 Pass (up from the `R16`-era 546 baseline by exactly the +110 new R16G16 passes), 0 Fail, 2790 `NotSupported` -- 0 regressions. `R32G32_{UINT,SINT}` and the packed 32-bit formats remain entirely unimplemented; `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`. Further closed: added `R32G32_{UINT,SINT}`'s own storage-mandatory partial-component siblings (identity format, no scalar conversion, reusing the existing `ResourceFormat`/`mapVkFormat` entries from earlier sampled-only work -- only the storage-image pack/unpack side needed adding), then the packed 32-bit formats `A2B10G10R10_{UNORM,UINT}_PACK32`/`B10G11R11_UFLOAT_PACK32` (new `femeRTPackR10G10B10A2Unorm`/`Uint`/`femeRTPackR11G11B10Float` helpers, each the mathematical inverse of this project's own existing sampled-image unpack helper for the same format -- a real CTS re-run of `dEQP-VK.image.load_store.with_format.*.{a2b10g10r10,b10g11r11}*` (84 cases) confirms the storage-image bit layout does in fact match the sampled-image decode: 66 Pass, 0 Fail, resolving H19j's own original component-order-verification concern), then a further gap discovered via the real Vulkan spec's own full mandatory-format table (not originally called out in this row's own text): `R8G8B8A8_{SNORM,SINT}` also need storage-image support (distinct from `R8G8B8A8_{UNORM,UINT}` staying unclaimed), closed by reusing `femeRTPackR8G8B8A8Snorm`/`Sint`, already defined for this project's own texel-buffer conversion path (a real CTS re-run of `dEQP-VK.image.load_store.with_format.*.r8g8b8a8_s*`, 56 cases, confirms: 56 Pass, 0 Fail). New unit tests: 12 in `ImageSamplingTest.cpp` (R32G32_{UINT,SINT} read/write, R10G10B10A2_{UNORM,UINT}/R11G11B10_FLOAT write, R8G8B8A8_{SNORM,SINT} storage write) plus 3 widened blocks in `FormatTest.cpp`. `ninja check-feme` passes in full, 2197/2256 (59 pre-existing `Unsupported`, 0 `Failed`). The `load-store.txt` mustpass regression caselist (3446 cases) shows 822 Pass (up from the `R16G16`-era 656 baseline by exactly +44 R32G32, +66 packed-32-bit, and +56 R8G8B8A8-signed passes), 0 Fail, 2624 `NotSupported` -- 0 regressions. `shaderStorageImageExtendedFormats` itself stays `VK_FALSE`: the real Vulkan spec's own mandatory list also requires `A2B10G10R10_{SNORM,SINT}_PACK32`, which this project does not implement yet (no `ResourceFormat` entry, no pack/unpack helpers) -- the sole remaining gap, split into a new roadmap row H19o below. See "Roadmap H19n: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Vulkan/Format.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/include/feme/Target/CPU/RuntimeABI.h` | P3 |
| ~~H19o~~ | ~~**The final `A2B10G10R10_{SNORM,SINT}_PACK32` gap in the mandatory `shaderStorageImageExtendedFormats` list**, split out of H19n once every other format in the real Vulkan spec's own mandatory-format table was closed. Added 2 new `ResourceFormat` enum entries (appended at the tail: `SNORM`=103, `SINT`=104), `mapVkFormat`/`formatElementSize` wiring, and new `femeRTUnpackR10G10B10A2Snorm`/`femeRTPackR10G10B10A2Snorm` helpers in `FeMeRuntimeCPU.c` (the signed-normalized sibling of the existing `R10G10B10A2_UNORM` helpers H19n added). Corrected a mistaken premise in this row's own original task text along the way: `_SINT` does **not** get to reuse `_UINT`'s unpack helper unmodified -- only the *pack* side is legitimately shared (two's-complement truncation to N bits is signedness-independent), but *unpacking* a sub-32-bit signed field requires real sign-extension (`femeRTUnpackR10G10B10A2Sint`, a new real helper) or negative field values silently come back positive; this exactly mirrors the pre-existing `femeRTUnpackR8G8B8A8Uint`/`Sint` asymmetry already in the file, and two new unit tests (`LoadI32FetchesR10G10B10A2Sint`, `StoreWritesTexelIntoR10G10B10A2Sint`) were deliberately built with top-bit-set field values to catch this class of bug. Also added the sampled-image feature-bit and `ImageFixture.cpp`/`feme-run.cpp` fixture-name cases. `shaderStorageImageExtendedFormats` flipped to `VK_TRUE` (`PhysicalDeviceInfo.cpp`), completing the H19a-H19o chain. New unit tests: `MapsSignedPacked32BitFormats` plus 2 widened tests in `FormatTest.cpp`; 4 new tests in `ImageSamplingTest.cpp`. `ninja check-feme` passes in full, 2202/2261 (59 pre-existing `Unsupported`, 0 `Failed`). Real CTS: no `with_format` cases exist for these two formats at all -- SPIR-V has no `Rgb10a2Snorm`/`Rgb10a2Sint` image-format literal (confirmed via VK-GL-CTS's own `hasSpirvFormat`/`spirvFormats` table), so the mustpass caselist only exercises them under `without_any_format`, which gates on `shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat` (H19i, still unimplemented) -- ran all 40 `without_any_format.*.a2b10g10r10_s*` cases: 40/40 honestly `NotSupported`, 0 `Fail`, as expected. Full `load-store.txt` mustpass regression (3446 cases): 822 Pass, 0 Fail, 2624 `NotSupported` -- unchanged from the H19n baseline (expected, since no with_format cases exist for these formats) and 0 regressions. `shaderStorageImageExtendedFormats` is now `VK_TRUE` and H19i's own `without_format.*` work can proceed. See "Roadmap H19o: measured impact" in `VulkanCTSReport.md`~~ | H19 | `feme/lib/Vulkan/Format.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/include/feme/Target/CPU/RuntimeABI.h` | P3 |
| H19k | ~~**`feme-cpu-linearize`'s own inability to linearize a loop containing an internal branch in `Flow`**, discovered as a hard, unrelated prerequisite blocking H19g's own real CTS closure: every `dEQP-VK.image.load_store_multisample.2d.*` verification shader contains a `for (int sampleNdx = 0; sampleNdx < N; ++sampleNdx) { imageStore/imageLoad(...) }` loop that `feme-cpu-linearize` rejects at pipeline-creation time with "loop ... has an internal branch in 'Flow'; unsupported", regardless of how complete the storage-image addressing side is -- confirmed via a real CTS re-run with `shaderStorageImageMultisample` temporarily forced `VK_TRUE`: 0/84 real passes, 27/84 hit this exact error, the remaining 57/84 `NotSupported` on formats outside today's mandatory floor. Needs a real investigation into `feme-cpu-linearize`'s own control-flow-linearization algorithm (`feme/lib/Transforms/CPU/` -- exact file not yet identified) to determine why this particular loop shape's own internal branch is unsupported (a simple bounded counting loop with a compile-time-constant trip count, structurally unlike the more complex divergent-control-flow cases this milestone's own `Flow`-based linearization already handles elsewhere) and what a fix looks like -- likely its own, larger milestone given the "roadmap milestone 6 deviation" note already attached to the existing error message, not a narrow follow-on~~ (closed: root-caused via a real IR reduction of the exact CTS shader (`glslangValidator` -> `feme-translate`/`feme-opt`, stage by stage) -- `StructurizeCFGPass` (run inside `feme-cpu-prepare`) unconditionally splits a loop's own uniform trip-count check into two separate `CondBr` blocks whenever that check is not already fused with the latch (the ordinary `for (init; cond; ++i) { body }` shape): the check itself, plus a "Flow" block that re-derives the *identical* decision via a `phi` of two compile-time-constant `i1`s selecting between the check's own two successors. First attempted a broader fix (adding `SimplifyCFGPass` to `feme-cpu-prepare`'s pipeline, between `StructurizeCFGPass` and `BreakCriticalEdgesPass`), but abandoned it after discovering it also unconditionally invokes `tailMergeBlocksWithSimilarFunctionTerminators` (`llvm/lib/Transforms/Scalar/SimplifyCFGPass.cpp`, not gated by any `SimplifyCFGOptions` toggle), which corrupted a genuinely different, legitimate two-exit-destination loop shape (`Transforms/CPU/CFG/loop-early-return.ll`, an early `return` inside a loop body alongside its normal exit), violating `feme::cpu::verifyStructured`'s "cycle has exactly one exit block" invariant. Replaced it with a narrow, dedicated fold directly inside `feme::cpu::LoopLinearizer` (`Linearize.cpp`) instead: a new `foldRedundantFlowBlock` recognizes only a block whose `CondBr` condition is a `PHINode` physically in that block with exactly 2 incoming values that are both literal `ConstantInt`s mapping to the block's two different successors -- a syntactic, conservative precondition that can never misfire on real divergent control flow (whose condition is always a genuine runtime value, never a phi of pure literal constants), confirmed by `loop-early-return.ll`'s own analogous guard (fed by a *nested*, non-constant phi) correctly failing to match and staying untouched. Rewires each predecessor directly to the correct successor, forwards any of the block's own value-phis through `BreakCriticalEdges`-inserted relay/trampoline chains to the real downstream merge point, and erases the block; a new `foldRedundantFlowBlocksInCycle` driver applies it to every non-Header/Latch block of a cycle before the rest of `linearizeCycle` runs. Also extended the `OtherCondBrBlocks`/`CheckBlock` exit-matching fallback to tolerate a persisting `BreakCriticalEdges` relay between the check and `ExitBlock` (via `straightChain`), since the fold can leave one standing. Three new/updated lit tests (`loop-uniform-check-separate-structurized.ll`, `loop-value-diamond-check-separate-structurized.ll`, both exercising the redundant-Flow-fold directly; `loop-break-structurized.ll` reverted to its original content since `Prepare.cpp` itself needed no change in the end). `ninja check-feme` passes in full (2136/2195, 59 pre-existing `Unsupported`, 0 `Failed`), all 249 `FeMeTransformsCPUTests` gtest cases pass, and the full `Transforms/CPU/` lit suite (139 tests, including the `loop-early-return.ll` regression guard) passes with zero failures. A real CTS re-run of `dEQP-VK.image.load_store_multisample.2d.*` (84 cases, `shaderStorageImageMultisample` again temporarily forced `VK_TRUE` purely to probe the shader path, reverted before landing) confirms the fix: **0 of the 27 previously-failing cases still hit the "internal branch in 'Flow'" error** -- all 27 now hit a distinct, later, unrelated error instead (`feme-cpu-simdize: ... divergent vector value ... component decomposition is not yet supported for this use`), split into new roadmap row H19l below. The `load-store.txt` mustpass regression caselist (3446 cases) is byte-identical to its pre-H19k baseline after reverting the probe (260 Pass, 0 Fail, 3186 `NotSupported`) -- 0 regressions. `shaderStorageImageMultisample` stays `VK_FALSE`: H19l's own newly-discovered `feme-cpu-simdize` gap still blocks every real case in this group. See "Roadmap H19k: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/Linearize.cpp` | P3 |
| H19l | ~~**`feme-cpu-simdize`'s own inability to decompose a divergent vector value used outside its supported use-pattern set**, discovered as a new, distinct prerequisite immediately downstream of H19k's own closure: with H19k's Flow-fold fix in place and `shaderStorageImageMultisample` temporarily forced `VK_TRUE` to probe the real shader path, all 27 of `dEQP-VK.image.load_store_multisample.2d.*`'s previously-`feme-cpu-linearize`-blocked cases now instead fail pipeline creation with `feme-cpu-simdize: function 'main' has a divergent vector value '' used outside a supported insertelement-chain/resource-store/extractelement/select/shufflevector/phi/elementwise/comparison/reduce/vectorizable-intrinsic pattern; component decomposition is not yet supported for this use (roadmap milestone 7 deviation)` -- a pre-existing `feme-cpu-simdize` scope limit (per its own "roadmap milestone 7 deviation" note), not anything H19k's own linearizer fold introduced. Needs a real IR reduction of this exact case (the same technique the H6g-b/H6j/H6k/H6l/H19k chain has used throughout) to identify the specific divergent-vector use shape this loop's own per-sample body produces that falls outside `feme-cpu-simdize`'s current supported-pattern set, and what a fix looks like -- likely its own, larger milestone given the existing "roadmap milestone 7 deviation" note, not a narrow follow-on~~ (closed: a real IR reduction (hand-written minimal GLSL mirroring the actual CTS shader's per-sample `imageStore`/`imageLoad` loop, compiled via `glslangValidator` then run through the exact `feme-cpu-fold-spirv-builtins,...,feme-cpu-linearize,feme-cpu-simdize` pass pipeline `Pipeline.cpp` uses) plus temporary debug instrumentation at `SIMDize.cpp`'s own diagnostic site found the "component decomposition" text was a **red herring**: the actual failing pair was a `<4 x i32>` insertelement-chain result used as the `Texel` argument of a `feme.cpu.image.store.2dms.v4i32` call -- a normally-fully-supported producer/consumer shape. Root cause: `feme::cpu::matchImageCall`'s own `static constexpr ImageCallKind AllKinds[]` lookup table (`ImageCalls.cpp`) never listed `ImageCallKind::Store2DMS`/`Store2DMSI32` at all, even though the enum, `getImageCallName`, the `create*` builders, and the operand-extraction `switch` immediately below already had fully-correct entries for both -- an isolated oversight from H19g's own original implementation, never caught because H19g's own feature bit stayed `VK_FALSE` the whole time (first blocked by H19k), so this dead-switch-case was never actually exercised by any real code path until now. This also meant `widenImageCall`'s own gating check (`matchImageCall` returning non-`nullopt`) silently skipped widening these calls entirely -- a second, unrelated latent bug beyond just the diagnostic. Fix: added the two missing entries to `AllKinds`; `widenImageCall` itself needed no changes, confirmed fully generic. Two new `ImageCallsTest.cpp` cases (`MatchesStore2DMSCall`, `MatchesStore2DMSI32Call`, a new dedicated test file for `matchImageCall`/`create*` in isolation) plus a new `DecomposesInsertElementChainIntoImageStore2DMS` in `SIMDizeTest.cpp`; a `git stash` round-trip confirmed all three correctly fail (reproducing the exact real diagnostic) without the fix. `ninja check-feme` passes in full, 2139/2198 (59 pre-existing `Unsupported`, 0 `Failed`). A real CTS re-run of `dEQP-VK.image.load_store_multisample.2d.*` (84 cases, feature bit again temporarily forced `VK_TRUE` purely to probe, reverted before landing) confirms the fix: **27/84 Pass (up from 0), 0/84 Fail** -- a full, clean closure of every case reachable through the plain-2D path, the remaining 57 honestly `NotSupported` on sample counts beyond the rasterizer's existing 8-sample floor (R33/C4b, unrelated) or formats outside the existing storage-image mandatory floor (H19a/H19f/H19h, unrelated). The `load-store.txt` mustpass regression caselist (3446 cases) is byte-identical to its pre-H19l baseline (260 Pass, 0 Fail, 3186 `NotSupported`) -- 0 regressions. `shaderStorageImageMultisample` still stays `VK_FALSE`: probing the full `dEQP-VK.image.load_store_multisample.*` group (252 cases, both `2d.*` and `2d_array.*`) with the bit forced on surfaced 27 real `Fail`s in `2d_array.*` (`spirv.ImageWrite` failing MLIR SPIR-V-to-LLVM legalization for an `Arrayed`+`MultiSampled` image dim), confirming H19g's own documented scope narrowing ("plain (non-arrayed) `Dim::2D` handle only") is still the accurate boundary -- flipping the bit now would newly attempt and fail this group. Split the arrayed-2D-multisample gap into new roadmap row H19m below. See "Roadmap H19l: measured impact" in `VulkanCTSReport.md`) | H19 | `feme/lib/Transforms/CPU/ImageCalls.cpp` | P3 |
| H19m | ~~**Arrayed 2D multisample storage-image read/write** -- split out of H19g/H19l once both were confirmed otherwise-complete for the plain (non-arrayed) case. A real probe of `dEQP-VK.image.load_store_multisample.2d_array.*` (27 cases, `shaderStorageImageMultisample` temporarily forced `VK_TRUE`) confirms every case fails pipeline creation with `error: failed to legalize operation 'spirv.ImageWrite' ... : (!spirv.image<..., Dim2D, ..., Arrayed, MultiSampled, ...>, ...)`, since `SPIRVToLLVMPatterns.cpp`'s own `isPlainMultisampled2DImage` check (added by H19g) explicitly rejects any `Arrayed` multisampled image. Needs a 4th coordinate component (`x`, `y`, `layer`, `sample`) threaded through `appendVectorLane`/`ImageShape`/`ImageCalls`/the runtime fetch-store helpers -- likely a new `ImageShape::Array2DMS` mirroring `Plain2DMS`'s existing sample-lane handling plus `Array2D`'s existing layer-lane handling combined -- and is the last row blocking an honest `shaderStorageImageMultisample = VK_TRUE` flip | H (closed: added a new `ImageShape::Array2DMS` (4-component `(x, y, layer, sample)` coordinate) and loosened `classifyStorageImage2DHandle`'s gate to accept `Arrayed && MS`. On the read side, `Load2DArray`'s float entry point already accepted a real `Sample` operand (only ever passed `0` before this row); `Load2DArrayI32` was widened in place to add the same `Sample` operand `Load2DArray` already had (mirroring H19g's own `Load2DI32` widening) -- the runtime's own `femeRTFetchTexel2D`/`femeRTFetchTexel2DI32` already independently accepted both a `Layer` and a `Sample` parameter, so no runtime addressing changes were needed on the read side at all. On the write side, two new dedicated call kinds (`Store2DArrayMS`/`Store2DArrayMSI32`) and two new runtime helpers (`femeRTStoreTexel2DArrayMS`/`I32`, exported as `feme.cpu.image.store.2darrayms.v4f32`/`.v4i32`) were added, since neither `Store2DArray` nor `Store2DMS` had a spare operand slot for the other axis -- mirroring H19g's own precedent of adding new call kinds rather than widening in place when no existing kind's ABI could safely absorb the new operand. `SPIRVToLLVMPatterns.cpp`'s own `isPlainMultisampled2DImage` gate was loosened (and renamed `isMultisampled2DImage`) to accept an arrayed image too -- `appendVectorLane` itself needed no changes, since SPIR-V's own `Coordinate` operand already carries the array layer as an ordinary coordinate component. New unit tests: three `SPIRVResourceLoweringTest.cpp` cases (replacing the old negative `LeavesAnArrayedMultisampledStorageImageHandleAlone` test, which asserted rejection of exactly this now-supported case), three `ImageCallsTest.cpp` `matchImageCall` cases for the two new call kinds plus a widened `Load2DArrayI32` case, and eight new/updated `ImageSamplingTest.cpp` runtime tests (`StoreArrayMS*` write-side coverage plus a `Load2DArrayI32ReadsRequestedLayerAndSample` read-side regression test). The existing negative lit test in `spirv-to-llvm-image-access-invalid.mlir` was removed and replaced with a new positive `read_write_arrayed_ms` case appended to `spirv-to-llvm-image-access-multisample.mlir`. `ninja check-feme` passes in full, 2148/2207 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of the *full* `dEQP-VK.image.load_store_multisample.*` group (252 cases, both `2d.*` and `2d_array.*`) confirms the fix: **81/252 Pass, 0/252 Fail**, 171 honestly `NotSupported` outside today's mandatory storage-image format floor (the same gap H19g's own closure already documented, now also covering `2d_array.*`) -- a fully clean group with no real, in-scope failure left. `shaderStorageImageMultisample` therefore flips permanently to `VK_TRUE` (`PhysicalDeviceInfo.cpp`), the last row in the H19g/H19k/H19l/H19m chain. The `load-store.txt` mustpass regression caselist (3446 cases) is byte-identical to its pre-H19m baseline (260 Pass, 0 Fail, 3186 `NotSupported`) -- 0 regressions. See "Roadmap H19m: measured impact" in `VulkanCTSReport.md`)~~ | H19 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/lib/Transforms/CPU/ImageCalls.cpp` | P3 |
| ~~H19i~~ | ~~**`shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat`**, split out of H19f. `dEQP-VK.image.load_store.without_format.*`/`without_any_format.*`'s own `checkSupport` gates on the per-format `VK_FORMAT_FEATURE_2_STORAGE_{READ,WRITE}_WITHOUT_FORMAT_BIT` tiling/buffer feature bits (`vktImageLoadStoreTests.cpp`), which `Format.cpp` did not compute at all -- a `VkFormatProperties3`/`vkGetPhysicalDeviceFormatProperties2`-only pair of bits (like roadmap F11's `VK_FORMAT_FEATURE_2_HOST_IMAGE_TRANSFER_BIT`, neither has a 32-bit `VkFormatFeatureFlags` equivalent) distinct from the plain `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` this project already advertises. Confirmed the row's own premise: `SPIRVResourceLowering.cpp`'s storage-image classification (`classifyStorageImage2DHandle`/`hasOnlySupportedStorageImageUses`) never inspects a handle's compile-time SPIR-V `Format` operand, so a `Format == Unknown` handle already lowers identically to a declared-format one -- no compiler-side gap existed at all, only the advertisement. Added the two `VkFormatFeatureFlags2` computations directly in `EntryPoints.cpp`'s `vkGetPhysicalDeviceFormatProperties2` (mirroring F11's own `HostImageTransfer` pattern): both bits are set on a format's tiling features whenever `formatFeatureFlags` already reports `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT` for it, and separately on its buffer features whenever `isTexelBufferFormatSupported` recognizes it -- two independent format sets, since a texel-buffer-capable format (the identity `R32G32B32A32_*`/`R8G8B8A8_*` formats) is not always storage-image-capable and vice versa. Flipped `Info.Features.shaderStorageImageReadWithoutFormat`/`WriteWithoutFormat` to `VK_TRUE` (`PhysicalDeviceInfo.cpp`) now that the full mandatory `shaderStorageImageExtendedFormats` list (H19a-H19o) gives this bit real reach. New unit tests: 3 in `EntryPointsTest.cpp` covering a storage-image-only format (tiling bits set, buffer bits not), a texel-buffer-only format (buffer bits set, tiling bits not), and a neither-capable format (no bits anywhere); widened the pre-existing `FormatProperties2FillsChainedFormatProperties3` test (its `R8G8B8A8_UNORM` fixture format gained the new buffer-feature bits as a side effect of being texel-buffer-capable). `ninja check-feme` passes in full, 2205/2264 (59 pre-existing `Unsupported`, 0 `Failed`). A real re-run of the full `load-store.txt` mustpass regression caselist (3446 cases) confirms the fix: **1918 Pass** (up from the H19o-era 822 baseline), 0 Fail, 1528 `NotSupported` -- broken down by subgroup: `with_format.*` unchanged at 822/1092 (this row's own work cannot move it, since every `with_format.*` case already declares a format), `without_format.*` (828 cases, exactly matching this row's own original CTS-count estimate) now 534 Pass/0 Fail, and `without_any_format.*` (1526 cases) now 562 Pass/0 Fail -- **+1096 new passes, 0 regressions.** A broader smoke check (this feature bit gates `checkSupport` project-wide, not just `load-store.txt`) found: the relevant `dEQP-VK.robustness.image_robustness.*.storage_image.no_fmt_qual.*`/`storage_texel_buffer.no_fmt_qual.*` slice (a 3000-case sample) stays fully `NotSupported` -- gated on the separate, unimplemented `robustImageAccess` feature, so no new surface opened there; a `dEQP-VK.spirv_assembly.instruction.*image*` sweep surfaced a real, pre-existing crash (`UNREACHABLE executed at llvm/lib/IR/Value.cpp:99`, a "pass a sampled image handle to a function" pattern, `imagesample.*.pass_image_and_sampler_to_function*`) -- confirmed unrelated to this row's own scope (sampled-image function-parameter passing, not storage-image format advertisement; no `shaderStorageImage*WithoutFormat` requirement anywhere near it) and pre-existing rather than a new regression, so left untouched and not tracked as a new roadmap row here (out of scope for H19). See "Roadmap H19i: measured impact" in `VulkanCTSReport.md`~~ | H19 | `feme/lib/Vulkan/EntryPoints.cpp`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P3 |
| H19p | **A `..._with_group_sync` barrier reached through a `Function` whose calling convention the CPU back end rejects aborts the whole process with `LLVM ERROR: unsupported calling convention`.** Found while spot-checking a real `dEQP-VK.compute.pipeline.basic.*` run: `branch_past_barrier` does not fail, it `report_fatal_error`s, killing `deqp-vk` itself and silently truncating every remaining case in the group (the same whole-group-truncation failure mode roadmap C2 already cost this report once). Pre-existing and unrelated to the barrier work itself -- confirmed by reproducing it against a build with the H-series entry-wrapper diagnostic change reverted. Needs the rejecting call site found and turned into an ordinary diagnostic (the `Context::emitError` + return-nullptr convention `feme::cpu::EntryWrapperPass`/`LinearizePass` already use, caught by `runPipeline`'s `ErrorDiagnosticGuard`) so one unsupported shader fails one case rather than the whole run. Should land before the next full CTS regeneration, since the `compute` group's own totals are unreliable until it does |

Sequencing: H1 first (it is what makes every other row measurable), then H8
and H2 in parallel (the two largest unlocks, in disjoint files) — H2 is now
closed, see its own row — then H4/H5
behind G5, H7's clusters continuously alongside, and H3/H6/H9/H10/H11
after. H12 is a decision, not an implementation, and should be settled
before anyone starts a row it would have excluded.

#### 1.9.8 Ray tracing (J-series)

Ray tracing is not part of the Vulkan 1.4 core floor: `VK_KHR_ray_query`,
`VK_KHR_ray_tracing_pipeline` and `VK_KHR_acceleration_structure` are
optional extensions, and a 1.4 conformance submission is valid without any
of them. They are in scope anyway, per
[FeMeVulkanDesign.md](FeMeVulkanDesign.md)'s "Conformance Target": the CTS's
ray-tracing groups are the only conformance-grade tests that exist for the
traversal and continuation machinery
[FeMeGraphicsDesign.md](FeMeGraphicsDesign.md)'s G7/G8 design, and both
report zero failures today only because nothing is advertised.

Two properties make this series different from every other one in §1.9,
and both change how it should be distributed:

- **It is a dependency chain, not a set of independent rows.** Nothing can
  be advertised until J1-J4 all land, because the extension set's own
  `requires` graph forbids advertising a member without its prerequisites
  (see FeMeVulkanDesign.md's "Mesh shading and ray tracing exposure").
- **Most of the work is in `feme::graphics`/a new `feme::raytracing`, not
  in `lib/Vulkan`.** The Vulkan rows below are the API mapping; G7/G8 are
  the engine. Direct3D's DXR (FeMeWARPDesign.md's W6) reuses the same
  engine, which is why it is worth building once, carefully, rather than
  twice.

| # | Task | Depends on | Files | Priority |
|---|---|---|---|---|
| J1 | **Buffer device address** (`bufferDeviceAddress`, core since 1.2, currently `VK_FALSE` in the aggregate `VkPhysicalDeviceVulkan12Features` case). Every acceleration-structure build input and shader binding table is addressed by `VkDeviceAddress`, so this is the true first row: `vkGetBufferDeviceAddress`, a device-address space over this ICD's own allocations, and the bounds model that turns an attacker-supplied address back into a checked allocation + offset. Also unblocks `VK_KHR_buffer_device_address`'s own CTS coverage independently of ray tracing | none | `feme/lib/Vulkan/{Memory,Buffer}.{h,cpp}`, `EntryPoints.cpp` | P0 |
| J2 | **Descriptor indexing** (the core 1.2 `descriptorIndexing` cluster: `runtimeDescriptorArray`, `descriptorBindingPartiallyBound`, `descriptorBindingVariableDescriptorCount`, the `shader*ArrayNonUniformIndexing` bits — all `VK_FALSE` today). Not ray tracing itself, but the `dEQP-VK.ray_tracing_pipeline` corpus leans on it heavily, and the shader-side non-uniform indexing is a `feme::cpu` question (a divergent descriptor index through the existing heap model) rather than an API one | §1.2, R26 | `feme/lib/Vulkan/Descriptor.{h,cpp}`, `feme/lib/Target/CPU` | P1 |
| J3 | **`VK_KHR_deferred_host_operations`.** A prerequisite of `VK_KHR_acceleration_structure` with no traversal content of its own: `VkDeferredOperationKHR` plus the join/status protocol. This ICD already owns a worker pool (roadmap R21), so a truthful implementation is either a real deferral onto it or an honest "everything completes immediately"; both are legal, and the second is a good first row for a new contributor | none | new `feme/lib/Vulkan/DeferredOperation.{h,cpp}` | P1 |
| J4 | **`VK_KHR_acceleration_structure`.** The API mapping of G7's canonical acceleration structures: `vkCreateAccelerationStructureKHR`, host and device builds, `VkAccelerationStructureBuildRangeInfo`/geometry validation (attacker-controlled counts and strides — FeMeVulkanDesign.md's "Mesh shading and ray tracing exposure" owns this obligation), copies/compaction, and `vkWriteAccelerationStructuresPropertiesKHR`. Ships with the build-input fuzzer that document's rule requires | J1, J3, G7 | new `feme/lib/Vulkan/AccelerationStructure.{h,cpp}`, `feme/lib/RayTracing` | P1 |
| J5 | **`VK_KHR_ray_query`.** SPIR-V's `OpRayQuery*` family needs `spirv`->`llvm` conversion patterns and a CPU-side implementation over G7's traversal, with no new pipeline type at all — which is what makes it the *first advertisable* ray-tracing capability, and the one that validates traversal independently of the continuation transform. Whole `dEQP-VK.ray_query` group | J4, G7 | `feme/lib/Conversion/SPIRVToLLVM`, `feme/lib/RayTracing` | P1 |
| J6 | **`VK_KHR_ray_tracing_pipeline`.** All six stage kinds, hit-group linkage, `vkCreateRayTracingPipelinesKHR`, shader binding tables with checked strides and alignments (plus their own fuzzer), `vkCmdTraceRaysKHR`/`Indirect`, and recursion depth enforced at pipeline creation. Depends on G8's continuation transform, the largest single piece of new compiler work left in this document. Whole `dEQP-VK.ray_tracing_pipeline` group | J5, G8 | new `feme/lib/Vulkan/RayTracingPipeline.{h,cpp}`, `feme/lib/RayTracing` | P2 |
| J7 | **`VK_KHR_pipeline_library`** for library-linked ray-tracing pipelines, and `VK_KHR_ray_tracing_maintenance1` (`vkCmdTraceRaysIndirect2`, the ray-query-side feature bits) — separable follow-ons that several CTS cases require by name once J6 advertises anything | J6 | `feme/lib/Vulkan/RayTracingPipeline.cpp` | P2 |
| J8 | **`VK_KHR_ray_tracing_position_fetch`** and the remaining small ray-tracing extensions the CTS enables by name; assign only after J6's own measured run shows which ones its cases actually require, the same "measure, then assign" discipline E19 used for 1.3's small extensions | J6 | `feme/lib/RayTracing` | P2 |
| J9 | **A ray-tracing conformance measurement**, matching §1.9.6's G3 but for a group that is currently 100% `NotSupported`: `dEQP-VK.ray_query` and `dEQP-VK.ray_tracing_pipeline` must each be run and recorded in VulkanCTSReport.md as a real baseline (case counts, not zeros) *before* J5/J6 land, so their own before/after has something to compare against | none | `feme/utils`, VulkanCTSReport.md | P1 |

#### 1.9.9 What "full Vulkan 1.4 conformance" is done against

A single, checkable definition, so that "are we conformant yet" never
becomes a matter of opinion. All five must hold simultaneously:

1. **Zero `Fail`** in the mandatory `deqp-vk` list for
   `VK_API_VERSION_1_4`, plus zero `Fail` in `dEQP-VK.ray_query` and
   `dEQP-VK.ray_tracing_pipeline` for the advertised ray-tracing set
   (§1.9.8's own criterion; not required for the submission itself).
2. **Zero crashed or truncated groups.** The current run has seven
   (headline table, VulkanCTSReport.md); a crashed group's totals are not
   a result at all.
3. **Every mandatory feature, limit, format and queue capability
   advertised and truthful** — §1.9.4 (E), §1.9.5 (F) and §1.9.7 (H)
   closed, cross-checked against
   [Vulkan14FeatureInventory.md](Vulkan14FeatureInventory.md) regenerated
   from the registry rather than from this tree's notes.
4. **A stock, unpatched CTS** (roadmap C9), on a build with assertions
   enabled.
5. **Reproduced by the continuous job** (C10/D4/G1-G2), not by a
   hand-run sweep, so that the claim survives the next commit.

Milestone V9 in [FeMeVulkanDesign.md](FeMeVulkanDesign.md) owns turning
this into an actual submission (driver ID, conformance version, submission
package).

#### 1.9.10 The 1.1/1.2 floor this ICD skipped (K-series)

§1.9.4 and §1.9.5 close 1.3 and 1.4. They assume the versions *below*
them are closed, because roadmap C6 closed the 1.2 floor — but C6 closed
it as a list of *feature bits and limits*, derived by hand, and never
checked which promoted *extensions* those bits belonged to. Regenerating
[Vulkan14FeatureInventory.md](Vulkan14FeatureInventory.md) over 1.0-1.4
rather than 1.3/1.4 is what surfaced the difference:

- **Zero of Vulkan 1.1's twelve feature bits are advertised**
  (`multiview`, `variablePointers`/`variablePointersStorageBuffer`, the
  four 16-bit storage bits, `samplerYcbcrConversion`,
  `shaderDrawParameters`, `protectedMemory`, and the two multiview stage
  bits). A 1.4 claim inherits all of them.
- **Seven of 1.2's 47 are**, so 40 remain — the whole descriptor-indexing
  cluster, 8-bit storage, `shaderFloat16`/`shaderInt8`, `scalarBlockLayout`,
  `bufferDeviceAddress`, the Vulkan memory model, `drawIndirectCount`,
  `samplerFilterMinmax`, `samplerMirrorClampToEdge`,
  `shaderOutputViewportIndex`/`shaderOutputLayer`, the int64 atomics.
- **Eleven core-promoted extensions are only partially implemented**, which
  neither inventory can express and which reads as a bare "no" in both:
  `VK_KHR_dedicated_allocation`, `VK_KHR_device_group`,
  `VK_KHR_get_memory_requirements2`,
  `VK_KHR_get_physical_device_properties2`, `VK_KHR_maintenance2`,
  `VK_KHR_multiview`, `VK_KHR_shader_draw_parameters`,
  `VK_EXT_sampler_filter_minmax`, `VK_KHR_sampler_mirror_clamp_to_edge`,
  `VK_KHR_separate_depth_stencil_layouts`, `VK_KHR_spirv_1_4`.

The distinction that orders this series: a promoted extension's *commands*
are mandatory even when its *feature* may be reported false. This ICD's
own rule already says so — FeMeVulkanDesign.md's "Initial Non-Goals": "An
advertised core version may report a feature as unsupported; it may not
omit a command." K1 is that rule applied; the rest are features.

| # | Task | Depends on | Files | Priority |
|---|---|---|---|---|
| K1 | **The missing mandatory commands, regardless of feature support.** `vkGetPhysicalDeviceImageFormatProperties2`, `vkGetPhysicalDeviceSparseImageFormatProperties2`, `vkGetImageSparseMemoryRequirements2`, `vkEnumeratePhysicalDeviceGroups`, `vkGetDeviceGroupPeerMemoryFeatures`, `vkGetPhysicalDeviceExternal{Buffer,Fence,Semaphore}Properties`, and `vkCreateSamplerYcbcrConversion`/`vkDestroySamplerYcbcrConversion` are all core 1.1 commands this ICD does not implement. Each may report a degenerate/empty result truthfully; none may be absent. Highest priority in this section because an absent command is a null dispatch-table entry, i.e. a crash, not a rejection — the exact failure mode roadmap D0 hit with `VK_KHR_copy_commands2` | none | `feme/lib/Vulkan/{EntryPoints,Image,Memory,Sync}.cpp`, `ImplementedEntrypoints.txt` | P0 |
| K2 | **`VK_KHR_get_physical_device_properties2`/`VK_KHR_get_memory_requirements2` completion**, the two partials K1's command list mostly covers; close the row explicitly once every query in each extension exists, so the inventory can move it out of "planned" | K1 | same as K1 | P0 |
| K3 | ~~**`multiview`**, the 1.1 bit with the largest dependency: it is §1.9.7's H2 (layered rendering) seen from the version-floor side, and closes roadmap C6's one deliberate exception. Tracked in both places on purpose; land it once, cite both~~ (closed by roadmap H2: see that row's own detail. `multiview` is now advertised `VK_TRUE`; `Vulkan14FeatureInventory.md`'s 1.1 feature row moves from 0 of 12 to 1 of 12, and its extension row from 6 of 23 to 7 of 23) | H2 | `feme/lib/Vulkan/{RenderPass,PhysicalDeviceInfo}.cpp` | P0 |
| K4 | **The storage/arithmetic width clusters**: 16-bit storage (four bits), 8-bit storage (three), `shaderFloat16`/`shaderInt8`, and the int64 atomics. All four are `spirv`→`llvm` conversion and CPU-target questions rather than API ones, and all four are what a shader corpus reaches for long before an API test does. Assign as one lane, not one row | §1.2 | `feme/lib/Conversion/SPIRVToLLVM`, `feme/lib/Target/CPU` | P1 |
| K5 | **Descriptor indexing** (the 1.2 cluster: `runtimeDescriptorArray`, `descriptorBindingPartiallyBound`, `descriptorBindingVariableDescriptorCount`, the update-after-bind bits, the non-uniform indexing bits). Identical scope to §1.9.8's J2, which needs it for the ray-tracing corpus; whichever series reaches it first closes both | R26 | `feme/lib/Vulkan/Descriptor.{h,cpp}`, `feme/lib/Target/CPU` | P1 |
| K6 | **`variablePointers`, `shaderDrawParameters`, `scalarBlockLayout`, `samplerFilterMinmax`, `samplerMirrorClampToEdge`, `drawIndirectCount`, `shaderOutputViewportIndex`/`shaderOutputLayer`, `VK_KHR_depth_stencil_resolve`, `VK_KHR_image_format_list`, `VK_EXT_separate_stencil_usage`, `VK_KHR_shader_float_controls`.** Each is small and independent; several are nearly free (`shaderDrawParameters`' builtins are already mapped by `CanonicalizeStage.cpp`; `samplerMirrorClampToEdge`'s address mode is already implemented in `Image.cpp` and merely reported false). Split per bit when assigned | none | mixed | P1 |
| K7 | **`samplerYcbcrConversion` and `VK_EXT_ycbcr_2plane_444_formats`.** Roadmap E19 declined the format extension because no YCbCr sampling exists at all; that reasoning is correct for the *format* and wrong for the *floor* — the conversion object's commands are core 1.1 (K1), and the feature bit may stay false only if nothing depends on it. Decide explicitly here whether multi-planar sampling is implemented or the bit stays a truthful false forever | K1 | `feme/lib/Vulkan/{Format,Image}.cpp` | P2 |
| K8 | **`bufferDeviceAddress`**, identical to §1.9.8's J1 — listed here too because it is a core 1.2 bit, not only a ray-tracing prerequisite, and its own CTS coverage exists independently of any ray-tracing group | none | `feme/lib/Vulkan/{Memory,Buffer}.{h,cpp}` | P1 |
| K9 | **The Vulkan memory model** (`vulkanMemoryModel`, `...DeviceScope`, `...AvailabilityVisibilityChains`) and `protectedMemory`. Both are deliberately last: the memory model needs a real answer about what this CPU target's memory ordering guarantees are (not a bit flip), and protected memory is listed as a non-goal in FeMeVulkanDesign.md — this row is where that non-goal is either confirmed on the record or overturned | none | `feme/lib/Vulkan/EntryPoints.cpp`, FeMeVulkanDesign.md | P2 |

Sequencing: K1 first and alone (it is the only row in this section whose
absence is a crash rather than a `NotSupported`), then K2/K3 in parallel,
then K4/K5/K6 as independent lanes, K7-K9 last. Nothing in §1.9.5 (F) or
§1.9.7 (H) is blocked by this section — but a conformance *submission*
(§1.9.9) is blocked by all of it, which is why it is P0-heavy despite
being about versions two releases old.

### 1.10 Direct3D software adapter

Owned by [FeMeWARPDesign.md](FeMeWARPDesign.md). Nothing exists.

| Gap | Owner section | Priority |
|---|---|---|
| The integration boundary is undecided: whether a software/render-only adapter can be installed and enumerated through DXGI at all, versus an application-local compatibility runtime. Choosing wrong invalidates most object-layer work, so W0 is explicitly a throwaway-capable prototype and gates every Windows-facing line of code | "Replacement and Deployment Model", W0 | P0 |
| No Windows SDK/WDK/DDI version selection, signing, INF, CI or debugging story — and no Windows CI in this tree at all | W0 | P0 |
| Same in-process LLVM coexistence requirement as §1.9 (the adapter loads into processes that may already host DXC), plus an exported symbol set checked against the selected DDI contract | "Project and Binary Boundaries" | P0 |
| Serialized root signatures of both versions, descriptor heaps and tables | W2 | P1 |
| Device/queue/allocator/list/fence/heap/buffer/pipeline objects, device-removal propagation, indirect dispatch, copies, barriers, queries | W1–W2 | P1 |
| Textures, sampling, copy footprints, views, format matrix | W3 | P1 (blocked on G2) |
| Raster, output merge, tessellation/geometry, pipeline libraries, persistent cache, HLK | W4–W5 | P2 (blocked on G3–G5) |
| DXGI presentation, shared resources/fences, D3D11-on-12 evaluation | W6 | P2 |
| DXR (state objects, ray-tracing pipelines, shader tables, inline ray tracing at `D3D12_RAYTRACING_TIER_1_1`) and mesh/amplification shading, both now inside the declared scope rather than "evaluate later" | W6 | P2 (blocked on G6–G8, and on §1.9.8 proving the shared engine against the Vulkan CTS first) |
| No conformance measurement of any kind is possible in this tree: the HLK is Windows-only and there is no Windows CI, so the shared graphics/ray-tracing core's conformance evidence comes from the Vulkan side until W0 solves that | W7 | P1 |

Its portable half (command execution, resource layout, software graphics) is
required to stay unit-testable on non-Windows hosts, which is what makes W1–W5
partially reviewable in this tree at all; only `lib/Direct3D/Windows` is
genuinely Windows-only.

#### 1.10.1 The equivalent conformance scope

[FeMeWARPDesign.md](FeMeWARPDesign.md)'s "Conformance Target" now states the
Direct3D counterpart of §1.9.9's definition: feature level 12_2 in full
(DXR 1.1, mesh shaders, sampler feedback, variable-rate shading, binding
tier 3), measured by the HLK Direct3D 12 suites and this project's own
portable execution tests, with the same "advertise only what passes" rule.
Two scheduling facts follow, and both are the reason this stays a §1.10
note rather than its own E/F/H/J-style series:

- **The work is mostly already scheduled, under other names.** Everything
  the Direct3D target needs from the shared core is a G-row (G2–G8) or,
  transitively, a §1.9.7/§1.9.8 row that drives one. Adding a parallel
  Direct3D row per capability would double-track the same engine work; what
  is genuinely Direct3D-specific stays in W0–W7.
- **It is deliberately behind Vulkan, not beside it.** The Vulkan CTS runs
  on this project's hosts today and the HLK does not, so the shared core
  reaches conformance quality through §1.9 first. "Eventually equivalent"
  is a scope statement, not a schedule one — and W7 is the milestone that
  makes it checkable rather than aspirational.

The one thing this section *does* add is an obligation on §1.9.7/§1.9.8
rows: a fix landed in `feme::graphics`/`feme::raytracing` for a Vulkan CTS
failure must not be written as if Vulkan were its only consumer, because
W4–W7 will re-run the same code under Direct3D semantics. Where the two
APIs genuinely differ (raster rules, coordinate origins, precision
requirements), the difference belongs in the API runtime, per
FeMeGraphicsDesign.md's "Architectural Boundary" — recording which of the
two it is, in the fix itself, is what keeps the Direct3D side from
rediscovering it as a bug.

### 1.11 HLSL conformance via offload-test-suite

Owned by no single design document yet (a real gap-inventory row, not a
design-document deviation): `llvm/offload-test-suite`'s `beanz/feme` branch
wires up a `check-hlsl-feme-vk` lit target running that suite's real HLSL
(`dxc`-compiled) test corpus against this project's own Vulkan ICD, a
substantially different (and, for many shapes, entirely new) source of
SPIR-V than every §1.9 row's own `deqp-vk`-derived GLSL coverage. A first
full run (612 discovered tests) found 268 real failures; this section
tracks the buckets a per-failure-signature triage of that run's own full
verbose log found, one row per coherent root cause/cluster, following the
same "max one lowercase letter of nesting" convention every H/J/K row
above uses.

| Gap | Owner section | Priority |
|---|---|---|
| L1 | ~~**`FunctionWidener::widenInstruction`'s vector-typed, homogeneous "trivially vectorizable" intrinsic call special case (`llvm.fabs.v3f32` and siblings) decomposed and erased a *uniform* such call unconditionally**, ahead of the general `isDivergentAtDef` gate every other producer/consumer shape in `SIMDize.cpp` respects -- its own `extractelement` users, correctly gated on uniformity and so left unchanged, kept referencing the since-erased (RAUW'd-to-poison) call, producing a `poison` read. Found by reducing a real `Feature/HLSLLib/abs.32.test` failure (`{abs(v.xyz), abs(v.w)}`, a composite-construct-after-vector-fabs shape) down to its exact IR shape via `feme-translate`/`feme-opt`'s own SPIR-V-to-LLVM-to-CPU pipeline stages, run standalone outside any Vulkan runtime~~ (done: gated the special case on `UI.isDivergentAtDef(CI)`, so a uniform vectorizable-intrinsic call now falls through to the ordinary "uniform: leave it exactly as it is" handling every other instruction already gets, exactly matching its own already-uniformity-gated `extractelement` consumers. New lit regression `simdize-vector-intrinsic-uniform.ll` and unit test `SIMDizeTest.LeavesUniformVectorizableIntrinsicCallUnchanged`, both confirmed to fail without the fix (`extractelement <3 x float> poison`) and pass with it. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2275 discovered (27 pre-existing, unrelated `Unsupported`, 0 `Failed`), up by exactly the 1 new lit test and 1 new unit test this row adds. A real `check-hlsl-feme-vk` re-run confirms the fix at scale: 60 -> **86** `Passed` (+26), 268 -> **242** `Failed` (-26), 0 regressions (`Unsupported`/`Expectedly Failed` unchanged) -- this single fix is the highest-leverage one found in the first triage pass, since the composite-construct-after-vector-builtin-call shape it covers is common across the `Feature/HLSLLib/*.32.test` family. `FeMeCPUDesign.md`'s own "Phase 4: Widening" section already documented every producer as gated by divergence in prose; no update needed, since this is a correctness fix restoring already-documented intent, not a new deviation. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a `SIMDizePass`-internal correctness fix, no feature or extension surface touched) | none | `feme/lib/Transforms/CPU/SIMDize.cpp` | P1 |
| L2 | ~~**A `gpu-exec: error: Failed to create compute pipeline. (VkResult = -3)` (actually `VK_ERROR_INITIALIZATION_FAILED`'s own numeric value, not `VK_ERROR_OUT_OF_HOST_MEMORY` as this row originally guessed -- `VK_ERROR_OUT_OF_HOST_MEMORY` is `-1`; `Pipeline.cpp`'s own `vkCreateComputePipelines` sets exactly `VK_ERROR_INITIALIZATION_FAILED` on any `compileComputePipeline` failure, masking a real, more specific internal failure this ICD reports generically) bucket**~~ (done: triaged with the real-ICD-plus-diagnostic technique this row itself called for. First wired `vkCreateComputePipelines`'s own `compileComputePipeline` failure path through `Diagnostics.h`'s existing opt-in `logCreationFailure` helper (it bare-`consumeError`'d before, unlike `vkCreateGraphicsPipelines`, which already used it) so `FEME_VULKAN_LOG_CREATION_ERRORS=1` recovers the real underlying `llvm::Error` instead of only the bare `VkResult` -- a small, independent fix landed on its own. Running every one of this bucket's own cases directly through `offloader` (not through `llvm-lit`, whose own `lit.cfg.py` strips `FEME_VULKAN_LOG_CREATION_ERRORS` via `with_system_environment`'s allowlist) with that flag set found: (1) an 18-case sub-bucket rejected purely by `PhysicalDeviceInfo.cpp`'s own artificially-conservative `maxComputeWorkGroupInvocations`/`maxComputeWorkGroupSize` (pinned at Vulkan's own bare mandated minimum, 128, not any real limit this CPU target's dispatch loop enforces anywhere) -- fixed by raising both to 1024, a value real Vulkan implementations commonly report (measured: the 18-case "resolved group size exceeds maxComputeWorkGroupSize/Invocations" sub-bucket is now completely empty against a real `check-hlsl-feme-vk` re-run; most of those 18 cases now clear pipeline creation and go on to hit one of L7's own already-tracked legalization gaps instead, so net `Passed` only moves 109 -> 110); (2) roughly 108 of the remaining ~165 cases are real, already-tracked SPIR-V/MLIR legalization gaps -- matrix `spirv.CompositeConstruct`/`spirv.AccessChain`/`spirv.Transpose`, `spirv.GL.*` builtins, `spirv.GroupNonUniform*` wave-op variants, `spirv.Image`/`spirv.ImageDrefGather`/`spirv.ImageFetch`/`spirv.ImageSampleExplicitLod`, several `spirv.Atomic*` variants, `spirv.mlir.referenceof` (specialization constants), an `unhandled Decoration : 'NonUniform'`, an `unknown extension: SPV_KHR_compute_shader_derivatives`, and a couple of raw `unhandled opcode`/deserialization gaps -- all already exactly L7's own list, confirming L7 (not a new row) is where this remainder belongs; (3) a new, distinct, ~36-case sub-bucket -- `llvm.spv.resource.handlefrombinding` calls for a **single-channel-format texel buffer** (`RWBuffer<int>`/`RWBuffer<float>`/etc., e.g. every `Basic/Matrix/*.test` case, which all use `RWBuffer<int>` for their I/O) that `SPIRVResourceLoweringPass` never normalizes at all, split out to new roadmap row L9 below; (4) a handful of small, genuinely distinct residual cases (`Feature/PushConstant/types.test`'s own root-constant-span-not-covered diagnostic, a couple of `feme-cpu-simdize` groupshared-access gaps, an `'llvm.getelementptr'`/`'llvm.call'` dialect-type error) needing their own individual scoping, split out to new roadmap row L10 below. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change from this triage pass itself (the workgroup-size fix is a limits correction, not a feature/extension bit; the newly-split L9/L10 rows are not yet fixed)) | L1, L4 | `feme/lib/Vulkan/Pipeline.cpp`, `feme/lib/Vulkan/PhysicalDeviceInfo.cpp` | P1 |
| L9 | ~~**`SPIRVResourceLoweringPass` never normalizes a single-channel-format texel-buffer (`Dim::Buffer` image) store**, discovered via L2's own triage: `classifyTexelBufferHandle`/`hasOnlySupportedUses`/`isSupportedTexelElementType` (`SPIRVResourceLowering.cpp`) already correctly treat every `OpImageRead` as returning a full `<4 x T>` regardless of the underlying format's real channel count (per SPIR-V's own spec, matching the header comment's already-recorded V4 scope note) -- but a **write** to a single-channel format (e.g. `R32i`/`R32f`, `RWBuffer<int>`/`RWBuffer<float>`'s own SPIR-V shape) is not similarly widened by `dxc`/MLIR's own SPIR-V-to-LLVM conversion~~ (done: fixed at all three layers a single-channel texel buffer actually touches, not just the one the milestone named. (1) `isSupportedTexelElementType` (`SPIRVResourceLowering.cpp`) now accepts a bare scalar `float`/`i32` element type, so the scalar `OpImageWrite` Texel operand this row's own IR reduction confirmed (`store i32 %4, ptr %18`, via a real `dxc -spirv` `RWBuffer<int> In/Out; Out[0]=In[0];` reduction through `feme-translate --import-spirv`/`feme-opt --feme-convert-spirv-to-llvm`) now lowers cleanly instead of falling through to `UnsupportedOps.cpp`'s generic diagnostic -- confirmed directly by re-running that exact reduced IR through `feme-opt --passes=feme-cpu-lower-spirv-resources` with and without the fix. (2) A **separate, not-originally-named** gap: `feme::vulkan::isTexelBufferFormatSupported` (`Format.cpp`) -- the whitelist gating `vkCreateBufferView` itself, found only by cross-referencing the stale `FeMeVulkanDesign.md` "V4 status note" -- still rejected `R32_FLOAT`/`R32_UINT`/`R32_SINT` outright, so even with fix (1) a real app could never create the `VkBufferView` in the first place; now accepts all three. (3) A **second, not-originally-named** gap, found only by writing a real end-to-end `vkCmdDispatch` unit test (`ScalarIntTexelBufferDispatchTest`, CommandBufferTest.cpp) and seeing it read back `0` instead of the stored value: `femeCpuResourceLoadTypedV4F32`/`StoreTypedV4F32`/`LoadTypedV4I32`/`StoreTypedV4I32` (`FeMeRuntimeCPU.c`) -- the functions the **read** side of *any* texel buffer access always goes through, since SPIR-V's `OpImageRead`/`OpImageFetch` always return a full `<4 x T>` regardless of channel count, per spec -- had no format-detection branch for the single-channel 32-bit identity formats at all; now zero/one-pad the unread G/B/A lanes on load and write only the addressed R lane on store, matching the same convention the pre-existing image-sampling path's `femeRTUnpackImageTexel(I32)`/`femeRTPackImageTexel(I32)` tables already used. A dedicated scalar (non-`<4 x T>`) load/store entry-point pair (`femeCpuResourceLoadTypedF32`/`StoreTypedF32`/`LoadTypedI32`/`StoreTypedI32`, reusing those same tables) also now exists for any future caller needing that ABI directly. 18 new unit tests across `SPIRVResourceLoweringTest.cpp`/`RuntimeCPUTest.cpp`/`FormatTest.cpp`/`EntryPointsTest.cpp`/`BufferTest.cpp`/`CommandBufferTest.cpp` (including the real dispatch-level `ReadsAndWritesThroughScalarBufferViews` test) plus a new lit regression (`spirv-resource-lowering-texel-buffer-scalar.ll`); `ninja check-feme` passes in full (2292 discovered, 27 pre-existing unrelated `Unsupported`, 0 `Failed`). **Caveat:** despite the milestone's own claim, this fix does **not** move `Basic/Matrix/*.test`'s own pass count -- a real `check-hlsl-feme-vk` re-run confirms all 24 cases in that family still fail, for reasons entirely unrelated to this row (a `feme-cpu-linearize` loop-internal-branch error, and matrix `spirv.CompositeConstruct` legalization gaps), both already tracked under L7; the milestone's "accounts for the entire `Basic/Matrix/*.test` family" framing was therefore optimistic, and no CTS-visible improvement from this fix specifically was found beyond the direct IR reduction and the new real-dispatch unit test. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change (no feature/extension bit touched, only an existing format's texel-buffer usability)) | L2 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp`, `feme/runtime/CPU/FeMeRuntimeCPU.c`, `feme/lib/Vulkan/Format.cpp` | P1 |
| L10 | ~~**A handful of small, distinct residual cases from L2's own triage, each needing its own individual scoping rather than sharing a root cause**: `Feature/PushConstant/types.test`'s own "shader's root-constant span is not fully covered by a VkPushConstantRange" failure (the test's own `PushConstants:` YAML block only ever supplies one `float` value at offset 0, while the shader's own `S` struct spans several more members past it -- likely an `offload-test-suite`-side test-authoring gap in how its own harness sizes the generated `VkPushConstantRange`, not necessarily a `feme` bug, so needs checking against the harness's own range-sizing logic before assuming which side to fix); a couple of `feme-cpu-simdize` "groupshared global ... feeds an unrecognized broadcast"/"feeds a nested getelementptr" diagnostics (`WaveOps/GroupMemoryBarrierWithGroupSync.test`, `WaveOps/GroupSharedMatrixElementExprComponentDataRace.test`, `WaveOps/GroupSharedMatrixRowComponentDataRace.test`); an `'llvm.getelementptr'`/`'llvm.call'` "must be LLVM dialect-compatible type, but got 'si32'" MLIR-dialect-conversion error (`Feature/ResourceArrays/overflow-unbounded-array.test`, `WaveOps/WaveActiveSum.convergence.test`)~~ (done, in three separate commits, one per sub-issue, plus two milestone-description corrections split out below: (1) `Feature/PushConstant/types.test` -- `SPIRVPushConstantLowering.cpp`'s own declared-struct-size-based span was too conservative; fixed to report the actual max-accessed byte instead, so the harness's own `VkPushConstantRange` sizing (confirmed correct once the shader's own real access span is reported accurately -- not an `offload-test-suite`-side gap after all) now covers it. (2) The groupshared broadcast/nested-GEP family -- root-caused to two distinct `llvm::convertUsersOfConstantsToInstructions` (`ReplaceConstant.cpp`) quirks: it memoizes per `(Constant, BasicBlock)`, so one broadcast constant reused across multiple blocks in a function expands into one independent chain *per block*; and even within one block, out-of-program-order worklist processing can discard its own cache and mint a structurally-identical but distinct clone. Fixed in `GroupShared.cpp`: `matchPointerBroadcasts` (renamed, plural) now returns every independent chain; a new `coalesceIdenticalGroupSharedGEPs` merges duplicate clones; `retargetGroupSharedProducer`'s trailing leaf-use loop now skips `GetElementPtrInst` users (left to a separate, later loop) instead of asserting every leftover use is a Load/Store/AtomicRMW/Call; and the `HasBroadcast` branch's shared `Flat` GEP now builds at the function's entry block (guaranteed to dominate every use) rather than an arbitrary use site. Confirmed via real `llvm-lit` runs: `GroupMemoryBarrierWithGroupSync.test` and `GroupSharedMatrixElementExprComponentDataRace.test` both now clear this bug entirely (each hits only the pre-existing, already-tracked H19p "unsupported calling convention" abort instead) -- but `GroupSharedMatrixRowComponentDataRace.test` does **not** hit this bug at all, despite the milestone grouping it with the other two; it hits an unrelated, already-documented `SIMDize.cpp` scope gap (a divergent groupshared address whose result is a full row, i.e. vector-typed -- `widenGroupSharedLoad` does not yet support that), split out to new row L11 below. (3) The `si32` family -- root-caused to a genuine upstream MLIR bug (`GroupReducePattern`, `mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp`, builds its `llvm.call`/`llvm.func` from the raw, possibly-signed SPIR-V type instead of running it through the type converter first); fixed with a new feme-side `IntegerGroupNonUniformReducePattern`, registered at `FeMeBenefit` to override upstream for the nine integer `spirv.GroupNonUniform*` arithmetic reductions an HLSL `Wave*` intrinsic can reach, reusing upstream's own name-mangling convention but normalizing the type through `getTypeConverter()->convertType(...)`. Confirmed via a standalone `feme-translate`/`feme-opt` reduction and a real `llvm-lit` run: `WaveOps/WaveActiveSum.convergence.test` no longer hits the `si32` diagnostic (though it still does not pass end-to-end, blocked by the same pre-existing H19p abort as (2) above, since the fixed call site's own `spir_func` calling convention is exactly what H19p already tracks as CPU-target-unsupported). `Feature/ResourceArrays/overflow-unbounded-array.test`, however, does **not** hit an `si32` error at all, despite the milestone's framing -- it hits a structurally different `'llvm.getelementptr'` error indexing into a `!llvm.target<"spirv.SignedImage",...>` handle type directly, an unbounded/runtime-sized resource-array-of-handles indexing gap unrelated to integer signedness; split out to new row L12 below. Every sub-issue got its own new lit/unit-test coverage (`spirv-push-constant-lowering.ll` case, `simdize-groupshared-multi-broadcast.ll`, `spirv-to-llvm-group-nonuniform-integer.mlir`) plus updated existing coverage where the fix changed already-`CHECK`ed IR shape (`simdize-groupshared-masked-store-uniform.ll`). `ninja check-feme` (assertions-enabled, ccache build) passes in full after each commit, ending at 2268/2295 (27 pre-existing, unrelated `Unsupported`, 0 `Failed`), up by exactly the 3 new test cases this row's three commits add. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: all three fixes are internal correctness fixes (push-constant span accounting, CPU groupshared-widening robustness, SPIR-V-to-LLVM type-conversion correctness), no feature or extension surface touched. No real `deqp-vk` case was found to exercise the groupshared-broadcast or `GroupNonUniform*`-integer-reduce code paths differently from the existing `check-hlsl-feme-vk` coverage above (this milestone's own tests are the most direct real reproduction available for all three), so no `VulkanCTSReport.md` change from this row) | L2 | `feme/lib/Transforms/CPU/SPIRVPushConstantLowering.cpp`, `feme/lib/Transforms/CPU/GroupShared.cpp`, `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L11 | ~~**`SIMDize.cpp`'s `widenGroupSharedLoad` does not support a vector-typed result for a divergent groupshared address** -- found as an L10 milestone-description correction: `WaveOps/GroupSharedMatrixRowComponentDataRace.test` was grouped under L10's own "unrecognized broadcast"/"nested getelementptr" family, but its real failure is a distinct, already-documented (in a `SIMDize.cpp` comment, ~line 745: "A groupshared address keeps its own dedicated gather-based widening (`widenGroupSharedLoad`), which does not (yet) support a vector-typed result") scope gap: a divergent groupshared address whose load result is a full row (e.g. `float4`, not a scalar) has nowhere to go once `SIMDize.cpp`'s own general divergent-value handling reports "function 'main' has a divergent value '' of vector type" instead. Needs its own scoping pass to determine whether `widenGroupSharedLoad`'s existing scalar gather-based approach extends cleanly to a vector element type (likely a per-lane gather of the whole row, then a `shufflevector`/`insertelement` assembly, mirroring how the scalar case already gathers one lane at a time) or needs a different strategy entirely~~ (done: `FunctionWidener::widenGroupSharedLoad` (`SIMDize.cpp`) now special-cases a `FixedVectorType` result -- builds one `getelementptr` per vector component off the already-widened `<W x ptr>` row address (LLVM's fixed-vector-source-element-type GEP indexing plus its vector-base-pointer scalar-index broadcast, the same rule `widenGroupSharedGEP` already exploits for the outer array index), then one `llvm.masked.gather` per component with its own `commonAlignment`-derived alignment (only the first component shares the whole row's own alignment), recording results in `WidenedVectorComponents` like every other decomposed-vector producer. This produces a second-level `getelementptr` off a first-level one, a shape `GroupShared.cpp`'s `rewriteGroupSharedGlobals` validation/`retargetGroupSharedProducer` retargeting had never anticipated (previously capped at one level of GEP-nesting); extended both, gated specifically on the first-level GEP's own type being a vector-of-pointers (so the pre-existing, narrower, uniform-nested-GEP scope limit `simdize-groupshared-nested-gep-unsupported.ll` covers remains unchanged and still correctly rejected). New `simdize-groupshared-vector-row-load.ll` lit test covers the fix end to end; all pre-existing `simdize-groupshared-*.ll`/`simdize-vector-*.ll` tests and `ninja check-feme` (assertions-enabled, ccache build) pass in full with no regressions. Confirmed via the same real-IR-reduction technique this whole H6g-b/H6j/H6k/H6l/L9/L10 chain has used throughout, reduced directly from `WaveOps/GroupSharedMatrixRowComponentDataRace.test`'s own real failure; a full offload-test-suite/`check-hlsl-feme-vk` rebuild-and-rerun of that real test was not done this session due to time/scope (no cached build tree existed to reuse), but the lit reduction reproduces the exact divergent-vector-groupshared-load code path this milestone names, so this is still a confirmed fix, not merely a plausible one. No real `deqp-vk` case independently exercises this fix's code path either (see `VulkanCTSReport.md`'s new "Roadmap L11" section); `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` unchanged (an internal compiler-pass correctness fix, no feature/extension surface touched)) | L10 | `feme/lib/Transforms/CPU/SIMDize.cpp` | P2 |
| L12 | ~~**Indexing an array of resource handles (`RWBuffer<int> Buf[N]`/`Buf[]`) fails pipeline creation** with `'llvm.getelementptr' op result #0 must be LLVM pointer type or LLVM dialect-compatible vector of LLVM pointer type, but got '!llvm.target<"spirv.SignedImage", i32, 5, 2, 0, 0, 2, 24>'`~~ (done, entirely via its own L12a/L12b/L12c sub-rows below: L12a closed the SPIR-V-to-LLVM conversion-layer gap for both a bounded and unbounded array of handles; L12b surveyed and flipped the safe subset of the `VK_EXT_descriptor_indexing` feature-bit cluster; L12c closed the remaining Vulkan-runtime-layer gap (an unbounded array's `RangeSize` resolution against the pipeline layout, plus the full `VARIABLE_DESCRIPTOR_COUNT` descriptor-set object model). The real `Feature/ResourceArrays/overflow-unbounded-array.test` this milestone names now passes end to end) | none | see L12a/L12b/L12c | P2 |
| L12a | ~~**The SPIR-V-to-LLVM conversion-layer half of L12**: no existing pattern in `SPIRVToLLVMPatterns.cpp` recognized an array of image/sampled-image/sampler resource handles at all (bounded or unbounded), so their `spirv.AccessChain` fell through to the generic upstream access-chain pattern, which cannot build a `llvm.spv.resource.handlefrombinding` call and so produced the ill-typed `llvm.getelementptr` this milestone names~~ (done: added `getArrayedResourceCount` -- recovers a resource array's descriptor count from its pointee type, with a new reserved sentinel `Count == 0` (distinct from a real array-of-blocks' count, which is never `0`) for an unbounded `spirv.rtarray`; `prepareResourceVariables` now also tries this helper so an image/sampled-image/sampler array gets a real `ResourceInfo` entry; `ResourceAddressOfPattern` now branches on `Count != 1` (was `Count > 1`) so the new `Count == 0` sentinel also takes the "erase, let the access chain build the handle" path; new `ResourceArrayAccessChainPattern` converts the access chain directly into a `handlefrombinding` call using the chain's leading index as the intrinsic's own `Index` operand, since a resource array's element is the whole opaque handle with no further content-indexing needed (unlike an array of memory-backed blocks, which delegates to the shared `rewriteBlockAccess` helper). `ArrayedBlockAccessChainPattern`'s own `Count <= 1` guard is left unchanged (not widened to `!= 1`): it unconditionally casts the pointee to `spirv::ArrayType`, which would assert-crash on an unbounded array's `Count == 0` pointee. Verified against a real `dxc`-compiled bounded (`RWBuffer<int> Buf[3]`) and unbounded (`RWBuffer<int> Buf[]`) reduction, both of which now convert cleanly through `feme-opt --feme-convert-spirv-to-llvm` instead of hitting the verifier error, confirmed by inspecting the resulting IR's own `handlefrombinding` `Count`/`Index` operands directly. New lit coverage `spirv-to-llvm-resource-arrays.mlir` (bounded and unbounded cases); `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2270 passed, 0 failed, up by exactly the 1 new lit test this row adds. No unit test added beyond the lit test, matching this same conversion layer's own existing convention (`ArrayedBlockAccessChainPattern`'s own sibling fix has no unit-test counterpart either -- lit coverage is this phase's established test form). `ResourceInfo::Count`'s own doc comment (`SPIRVToLLVM.h`) updated to document the new `0` sentinel and its `!= 1` comparison requirement. No real `deqp-vk` case independently exercises an array-of-image-resources access chain (this milestone's own tests, both the real `overflow-unbounded-array.test` reduction and the from-scratch bounded reduction, are the most direct real reproduction available), so no `VulkanCTSReport.md` change from this row; `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` unchanged (an internal compiler-pass correctness fix, no feature/extension bit touched -- the Vulkan-side `VK_EXT_descriptor_indexing` feature-bit survey remains L12b's own scope, not touched here). Confirmed via the real reduction technique this whole H6g-b/H6j/H6k/H6l/L9/L10/L11 chain has used throughout, and the real `overflow-unbounded-array.test`'s own end-to-end pass/fail status was not re-measured this session (no cached `offload-test-suite` build tree existed to reuse) -- this fix advances that case past the conversion-layer error this milestone names, but since its own real Vulkan-side plumbing for an *unbounded* array still doesn't exist (see L12b/L12c), that specific test is not expected to fully pass yet) | L12 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`, `feme/include/feme/Conversion/SPIRVToLLVM/SPIRVToLLVM.h` | P2 |
| L12b | ~~**Survey and implement the `VK_EXT_descriptor_indexing` (core-1.2-promoted) Vulkan feature-bit cluster**, entirely `VK_FALSE` today (`descriptorIndexing`, `shaderSampledImageArrayNonUniformIndexing`, `descriptorBindingVariableDescriptorCount`, `runtimeDescriptorArray`, and ~16 sibling bits, `feme/lib/Vulkan/EntryPoints.cpp` ~line 1252-1273)~~ (done, with a milestone-description correction: this row's own original framing -- "needed before an *unbounded* resource array... can be exposed" -- turns out not to gate on this row at all, since the two bits that actually gate unbounded-array exposure (`runtimeDescriptorArray`, `descriptorBindingVariableDescriptorCount`) are deliberately kept `VK_FALSE` here, exactly as this row's own text already anticipated, and remain solely L12c's own scope to flip once its descriptor-set-layout/allocation-time plumbing lands; this row's real, completable scope was the survey-and-flip-the-safe-subset work itself. Full per-bit survey against the real Vulkan spec (`VkPhysicalDeviceDescriptorIndexingFeatures` man page) found: `shaderUniformTexelBufferArrayDynamicIndexing`/`shaderStorageTexelBufferArrayDynamicIndexing` -- "dynamic" (ordinary, non-constant but group-uniform SSA) indexing already works generically, since L12a's `ResourceArrayAccessChainPattern` threads any index through and `SPIRVResourceLowering.cpp`'s `BoundHandle` deliberately re-reads (not caches) it -- flipped `VK_TRUE`, confirmed with a new real dispatch (`CommandBufferTest.cpp`'s `TexelBufferArrayDynamicIndexDispatchTest.PushConstantIndexSelectsTheRightArrayElement`); `descriptorBinding{UniformBuffer,SampledImage,StorageBuffer,UniformTexelBuffer,StorageTexelBuffer}UpdateAfterBind`, `descriptorBindingUpdateUnusedWhilePending`, `descriptorBindingPartiallyBound` -- `vkQueueSubmit` executes every submission fully synchronously and resolves descriptor contents only at execution time, so there is never a real in-flight window a concurrent update could race with, and an unwritten array element already zero-fills rather than crashing -- flipped `VK_TRUE`, confirmed with two more new real dispatches (`DescriptorUpdatedAfterBindingIsVisibleAtSubmission`, `UnwrittenArrayElementReadsAsZeroInsteadOfCrashing`). Kept `VK_FALSE`: `descriptorIndexing` and all 7 `shader*ArrayNonUniformIndexing` bits (blocked on roadmap L7's unimplemented `NonUniform` SPIR-V decoration, which the spec's own descriptorIndexing "Feature Requirements" need); `shaderInputAttachmentArrayDynamicIndexing` (same underlying mechanism as the texel-buffer bits, but not yet confirmed with a real multi-attachment-array dispatch, left for a follow-up rather than advertised speculatively); `descriptorBindingStorageImageUpdateAfterBind` (storage images are not usable at all yet, `Format.cpp` never sets `VK_FORMAT_FEATURE_STORAGE_IMAGE_BIT`); `descriptorBindingVariableDescriptorCount`/`runtimeDescriptorArray` (L12c's own scope, as above). A real `deqp-vk` sweep (`dEQP-VK.api.info.*`) caught a real regression this session's first attempt introduced -- flipping only the aggregate `VkPhysicalDeviceVulkan12Features`/`Properties` struct fields left the pre-promotion `VkPhysicalDeviceDescriptorIndexingFeaturesEXT`/`PropertiesEXT` struct cases unhandled, breaking `dEQP-VK.api.info.vulkan1p2.feature_extensions_consistency`/`...property_extensions_consistency` -- fixed by adding the two missing `_EXT`-suffixed struct cases; the sweep afterward shows 5382/569 pass/fail, one *better* than the pre-existing 5381/570 baseline. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2273 passed. `Vulkan14FeatureInventory.md`/`VulkanCTSReport.md` updated; `VulkanExtensionInventory.md`'s own `VK_EXT_descriptor_indexing` row reviewed and left unchanged, still accurate) | L12a | `feme/lib/Vulkan/EntryPoints.cpp`, `feme/unittests/Vulkan/CommandBufferTest.cpp` | P2 |
| L12c | ~~**Descriptor-set-layout/pipeline-layout support for `VARIABLE_DESCRIPTOR_COUNT`**: `VkDescriptorSetLayoutBinding`'s own `VK_DESCRIPTOR_BINDING_VARIABLE_DESCRIPTOR_COUNT_BIT` (declaring a binding's real element count is deferred to descriptor-set-allocation time, via `VkDescriptorSetVariableDescriptorCountAllocateInfo`) and the runtime bounds-checking an unbounded array's own indexing needs at that point are not implemented anywhere in `feme`'s Vulkan layer today; needed for `overflow-unbounded-array.test` (and any other unbounded-array case) to actually pass end-to-end, on top of L12a's conversion-layer fix and L12b's feature-bit advertisement~~ (done, in three commits: (1) the actual blocker for the real test named by this row turned out to be narrower than the row's own framing -- a real reduction confirmed `overflow-unbounded-array.test`'s own `offload-test-suite` harness (`Device.cpp`) never uses `VARIABLE_DESCRIPTOR_COUNT_BIT` at all, just a plain fixed `descriptorCount`; the real blocker is that `getArrayedResourceCount`'s own reserved `RangeSize == 0` sentinel for an unbounded `spirv.rtarray` (L12a) is never resolved to a real count anywhere before `SPIRVResourceLoweringPass::collectHandles` rejects it outright. Fixed with a new `feme::vulkan::patchUnboundedResourceRanges(Module&, const PipelineLayout&)` (`Pipeline.h`/`.cpp`), run right after `importShaderModule` and before compilation in both `compileComputePipeline` and (threading a new `const PipelineLayout&` parameter through `compileGraphicsStage` and all 9 call sites) `compileGraphicsStage`/`compileAndValidateStages`: rewrites any `handlefrombinding` call's constant-`0` `RangeSize` operand to the matching (set, binding)'s declared `Count` from the pipeline layout. No new runtime bounds-check code was needed for the zero-fill-shorter-than-declared case `VARIABLE_DESCRIPTOR_COUNT` itself promises -- `ResourceHeap.cpp`'s existing `materializeHeap` (originally built for `descriptorBindingPartiallyBound`) already computes `NumToCopy = min(RangeSize, real descriptor count)` against a pre-zeroed heap, so it transparently satisfies that semantic for free once the `RangeSize` is patched to a real, layout-known value. (2) Implemented the milestone's own full-scope object model anyway (`Descriptor.h`/`.cpp`): `DescriptorSetLayoutBinding::VariableCount`; `vkCreateDescriptorSetLayout` parses `VkDescriptorSetLayoutBindingFlagsCreateInfo`, rejecting the flag on any binding but the layout's own highest-numbered one (the only placement the spec permits); `DescriptorSet`/`DescriptorPool::allocate` gained an `std::optional<uint32_t> VariableDescriptorCount` parameter; `vkAllocateDescriptorSets` parses `VkDescriptorSetVariableDescriptorCountAllocateInfo`, validating each requested count against the layout's declared max (rejecting over-max requests with proper cleanup); `vkGetDescriptorSetLayoutSupport` fills a chained `VkDescriptorSetVariableDescriptorCountLayoutSupport`. (3) Flipped `descriptorBindingVariableDescriptorCount`/`runtimeDescriptorArray` to `VK_TRUE` in both the aggregate `VkPhysicalDeviceVulkan12Features` and the `_EXT` pre-promotion twin (`EntryPoints.cpp`), per L12b's own established regression-prevention convention. 7 new unit tests (`PipelineTest.cpp`'s `PatchUnboundedResourceRangesTest` x2, `DescriptorTest.cpp`'s 5 `VariableDescriptorCount*`/`GetLayoutSupport*` cases); `ninja check-feme` (assertions-enabled, ccache build) passes in full (2280/2307, 27 pre-existing unrelated unsupported, 0 failed); `FeMeVulkanTests` 502/502 pass. Real `overflow-unbounded-array.test` now **passes** end to end (was failing before this row); the rest of `Feature/ResourceArrays/*` and the full `feme-vk` lit suite show no regressions (484 pass, 111 unsupported, 42 XFAIL, the same 13 pre-existing unrelated failures as before). A real `deqp-vk` sweep found the two newly-`VK_TRUE` bits do not currently move any CTS case's pass/fail status: every `dEQP-VK.descriptor_indexing.*` case (115, full mustpass caselist) gates on the single aggregate `descriptorIndexing` feature bit (`vktTestCase.cpp`'s `isDeviceFunctionalitySupported`, since apiVersion is already >= 1.2 so `VK_EXT_descriptor_indexing`'s own core-promotion path checks `vk12Features.descriptorIndexing` directly, not the individual sub-bits or the extension-name list), which correctly remains `VK_FALSE` per L12b's own still-accurate reasoning (blocked on roadmap L7's unimplemented `NonUniform` SPIR-V decoration) -- so `misc_variable_count` and every sibling case report `NotSupported` regardless, expected and not a bug. A targeted `dEQP-VK.api.info.*` (10,481 cases: 5382 pass / 569 fail, identical to L12b's own baseline) and a `dEQP-VK.binding_model.*` compute-filtered subset (6026 cases: 1143 pass / 673 fail, consistent with the pre-existing, already-documented `dEQP-VK.binding_model.*` baseline of 10,318 total failures) sweep found zero regressions and no failure in any descriptor/variable-count-related case. `Vulkan14FeatureInventory.md`/`VulkanCTSReport.md` updated to record the two flipped bits and this sweep; `VulkanExtensionInventory.md`'s own `VK_EXT_descriptor_indexing` row reviewed and left unchanged (still correctly not listed in `getSupportedDeviceExtensions()`, since the extension's own aggregate feature remains `VK_FALSE`); `FeMeVulkanDesign.md`'s Descriptor Model section updated) | L12b | `feme/lib/Vulkan/Pipeline.h`, `feme/lib/Vulkan/Pipeline.cpp`, `feme/lib/Vulkan/GraphicsPipeline.cpp`, `feme/lib/Vulkan/Descriptor.h`, `feme/lib/Vulkan/Descriptor.cpp`, `feme/lib/Vulkan/EntryPoints.cpp` | P2 |
| L13 | ~~**A nested identified-struct-inside-(runtime-)array conversion gap** -- roadmap L5's own fix stopped `mlir::VulkanLayoutUtils::decorateType` from crashing on this shape (a struct/cbuffer member whose own element is itself a user-defined struct, reached whenever FeMe's own dedicated block-conversion pattern in `SPIRVToLLVMPatterns.cpp` declines the shape first), but the shape itself is still not converted at all -- it now fails gracefully with `failed to legalize operation 'spirv.AccessChain' that was explicitly marked illegal`, the same real `Feature/StructuredBuffer/packed.test`, `Feature/CBuffer/{structs,array-of-structs,dynamic-struct,vectors}.test`, and `Feature/ConstantBufferT/vectors.test` cases L5 named still `FAIL` (no longer crash). Needs its own scoping pass: likely extending FeMe's own `convertOffsetStructTypeIgnoringDecorations`/`convertBlockType` (`SPIRVToLLVMPatterns.cpp`) to recognize a nested identified-struct member directly (reusing its own already-decorated layout rather than routing through upstream's generic, identified-struct-refusing `decorateType` path at all), since that dedicated path already handles the sibling non-nested-identified-struct shapes these same tests would otherwise hit~~ (done: reduced each of the 6 real cases from scratch (`dxc`/`feme-translate --import-spirv`/`feme-opt --feme-convert-spirv-to-llvm`) and found the actual gap is narrower than the milestone's own framing suggested -- not the *nested* identified struct itself (which already converts fine recursively), but `convertOffsetStructTypeIgnoringDecorations`'s natural-ABI-layout check failing to reproduce a `-fvk-use-scalar-layout`/`-fvk-use-dx-layout` struct's own tightly-packed member offset for a **vector-typed member** (e.g. `int3`/`int2`): LLVM's own vector ABI size/alignment rounds the innermost dimension up to the next power of two (a 3-lane vector sized/aligned as 4 lanes), which a real HLSL-emitted offset never leaves room for. Fixed with a pure-fallback "tight-vector retry" in that same function: if the ordinary attempt fails and at least one member is vector-typed, retry once with every vector member represented as a same-bit-width, tightly-packed `!llvm.array<N x ScalarTy>` instead (whose own natural alignment/size has no such rounding), redoing the offset validation; only reachable once the ordinary attempt already failed, so no currently-succeeding struct representation changes. This substitution is transparent to pointer-based access (GEP-then-independent-Load/Store, unaffected since this codebase's pointers are all opaque), but exposed a **second, pre-existing, entirely separate gap** needed for at least `packed.test`: MLIR has no conversion pattern at all for a **struct-typed** `spirv.CompositeConstruct` (only a 1-D-vector-typed one existed, per that pattern's own header comment) -- HLSL's "assemble a whole struct value, then store it in one shot" shape hits this directly. Extended `CompositeConstructPattern` with a struct case (one `llvm.insertvalue` per member, in SPIR-V's own required 1:1 constituent-to-member order), reassembling a real-vector constituent into a substituted-array member's type lane-by-lane (`llvm.extractelement` + `llvm.insertvalue` per lane, since `llvm.bitcast` itself cannot produce an aggregate result) whenever the two disagree. New/updated lit coverage: `spirv-to-llvm-composite-construct.mlir` gained two new split-file cases (a naturally-laid-out struct construct, and a tight-vector-substituted one); `spirv-to-llvm-nested-identified-struct-invalid.mlir` (L5's own negative regression, which exercised exactly this now-fixed shape) renamed to `spirv-to-llvm-nested-identified-struct.mlir` and flipped from an `expected-error` negative test to a `CHECK`-verified positive one. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2281/2308 (27 pre-existing unrelated `Unsupported`, 0 `Failed`). All **6** real cases this milestone named now pass end-to-end against a real `offload-test-suite`/`feme-vk` rebuild-and-rerun (confirmed individually and as part of a full-suite run) -- including `structs.test`/`array-of-structs.test`/`dynamic-struct.test`, whose own fixed-size cbuffer arrays this session had initially suspected would need a separate, larger array-stride-padding fix (a scoped-out concern from this session's own early investigation that turned out not to block these particular real tests after all, since their own arrays' declared strides happened to already match `VulkanLayoutUtils::getNaturalArrayStride`'s computation once the vector-offset gap above was fixed); no follow-on sub-milestone needed. Full suite totals otherwise unchanged (the 13 other pre-existing `feme-vk` failures, e.g. `HLSLLib`/`InlineRT`/`Bugs`/`Tools`, are unrelated to this row). `VulkanCTSReport.md` updated with a real `deqp-vk` regression sweep (`dEQP-VK.ubo.*`/`dEQP-VK.ssbo.layout.*`, the same struct/vector-layout-sensitive families L5 swept); `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change (internal SPIR-V-to-LLVM conversion correctness fix, no feature/extension bit touched)) | L5 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L3 | **A `gpu-exec: error: Failed to create render pass. (VkResult = -11)` (`VK_ERROR_FORMAT_NOT_SUPPORTED`'s numeric value) bucket** -- re-counted alongside L2: 35 distinct failing cases (34 originally estimated, effectively unchanged by L1/L4, as expected since neither fix touches render-pass/format code at all). Likely a real, currently-unadvertised format gap in this suite's own render-target format choices (distinct from any §1.9 `deqp-vk` coverage, which may simply never probe the same formats this HLSL suite's own YAML pipeline descriptions request); needs a per-case format survey to confirm before deciding between "extend format support" and "the format genuinely is out of scope, document why" | none | `feme/lib/Vulkan/{Format,RenderPass}.cpp` (unconfirmed) | P1 |
| L4 | ~~**10 failures hit `JIT session error: Symbols not found: [ buffer ]`**, an ORC JIT symbol-resolution failure distinct from every other bucket -- flagged as a likely simple, high-confidence bug (a missing or misregistered exported runtime symbol named literally `buffer`) but not yet examined at all~~ (done: `buffer` is not a runtime symbol name at all -- it is literally the user's own HLSL push-constant variable name (`[[vk::push_constant]] S buffer;`, every `Feature/PushConstant/*.test` case's own convention), and the real bug is in `SPIRVPushConstantLowering.cpp`'s `matchSPIRVPushConstantAccess`: it only recognized a genuine, per-function `GetElementPtrInst` as a supported push-constant member access, but a constant-index GEP off the push-constant global's own (constant) address is almost always folded straight to a `ConstantExpr` instead of surviving as a real instruction (both the global's address and every index are already compile-time constants, and every ordinary constant-folding IR builder, including `feme-translate`'s own `--llvmdialect-to-llvmir` step, collapses it eagerly) -- confirmed by reducing a real `Feature/PushConstant/bool.test` failure down to its exact IR shape via `feme-translate`/`feme-opt`. Only a struct's zero-offset member (needing no GEP at all, so never hitting this shape) was ever rewritten to read the appended `root_constants` blob; every other member's own load was left referencing the push-constant global itself, an external declaration with no definition, producing this row's own run-time JIT symbol-resolution failure rather than a compile-time diagnostic. Fixed by recognizing a `ConstantExpr` GEP identically to an instruction-typed one, with its own per-load function filter (a `ConstantExpr`'s uses can span more than one function, since constants are uniqued module-wide, unlike an instruction-typed GEP already guaranteed single-function). New case added to the existing `spirv-push-constant-lowering.ll` lit test covering the constant-expression shape directly, confirmed to fail without the fix and pass with it. `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2276 discovered (27 pre-existing, unrelated `Unsupported`, 0 `Failed`), up by exactly the 1 new lit-test case this row adds. A real `check-hlsl-feme-vk` re-run confirms the fix: **86 -> 95** `Passed` (+9, matching this row's own cited 10-failure bucket almost exactly -- 9 of the 10 fully pass, the 10th, `Feature/PushConstant/array_of_matrices.test`, was already marked `XFAIL` upstream in `offload-test-suite` for an unrelated reason and now reports `Unexpectedly Passed` instead, an upstream test-annotation staleness outside this repository's own scope to fix), **242 -> 233** `Failed` (-9), 0 regressions elsewhere. `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` confirmed no change needed: a `SPIRVPushConstantLoweringPass`-internal correctness fix, no feature or extension surface touched) | none | `feme/lib/Transforms/CPU/SPIRVPushConstantLowering.cpp` | P1 |
| L5 | ~~**6 failures are real internal crashes** ("PLEASE submit a bug report...", i.e. an uncaught `llvm_unreachable`/assertion/signal, not a graceful diagnostic) -- each needs its own reduction and root-cause pass; a crash silently truncates or corrupts a suite run the same way roadmap C2 and H19p already document for `deqp-vk`, so these are higher priority than an equivalent graceful-failure bucket of the same size~~ (done: all 6 (`Feature/StructuredBuffer/packed.test`, `Feature/CBuffer/{structs,array-of-structs,dynamic-struct,vectors}.test`, `Feature/ConstantBufferT/vectors.test`) shared one root cause, confirmed by reducing a minimal repro (`RWStructuredBuffer<S2> Out`, `S2 { int3 Legs; int TailState; }`) down through `feme-translate --import-spirv`/`feme-opt --feme-convert-spirv-to-llvm` and catching the live `SIGSEGV` with `gdb -p <pid> -batch -ex bt`: `mlir::VulkanLayoutUtils::decorateType` (`mlir/lib/Dialect/SPIRV/Utils/LayoutUtils.cpp`) deliberately returns `nullptr` when asked to re-decorate an *identified* (named) struct nested inside another type (identified structs are uniqued by name, so a second, differently-decorated copy isn't possible), but every one of its own recursive callers (the struct-member loop, and the `VectorType`/`ArrayType`/`MatrixType`/`RuntimeArrayType` overloads) fed that `nullptr` straight into a `*Type::get(...)` constructor without checking, dereferencing a null `mlir::Type`. Since `dxc` emits `OpName`/`OpMemberName` for every user HLSL struct, every such struct is "identified" once imported into MLIR, making any array/runtime-array-of-struct shape that falls through FeMe's own dedicated block-conversion pattern (`SPIRVToLLVMPatterns.cpp`) into upstream MLIR's generic fallback reach this bug. Fixed with `if (!memberType) return nullptr;` guards after each recursive `decorateType(...)` call, converting the crash into the upstream-already-handled graceful "failed to legalize operation ... explicitly marked illegal" diagnostic every other unsupported shape already produces (`convertStructTypeWithOffset` in `SPIRVToLLVM.cpp` already treats a `nullptr` from `decorateType` as an ordinary conversion failure). New lit regression `feme/test/Conversion/SPIRVToLLVM/spirv-to-llvm-nested-identified-struct-invalid.mlir` reproduces the exact runtime-array-of-identified-struct shape and confirms the clean diagnostic. `ninja check-feme` (assertions-enabled, ccache build) passes in full (2281/2308, 27 pre-existing unrelated `Unsupported`, 0 `Failed`, up by exactly this row's 1 new lit test). A full `feme-vk` offload-test-suite re-run shows byte-for-byte identical totals to the pre-fix baseline (133/266/26/224/1) with zero `"PLEASE submit a bug report"` matches (down from 6) -- the crash is eliminated, but the underlying 6 cases still fail (now gracefully) since the nested-identified-struct-in-array conversion gap itself remains unimplemented, split out to new row L13 below. `VulkanCTSReport.md` updated with a real `deqp-vk` regression sweep (`dEQP-VK.ubo.*`/`dEQP-VK.ssbo.layout.*`); `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change (internal correctness fix, no feature/extension bit touched)) | none | `mlir/lib/Dialect/SPIRV/Utils/LayoutUtils.cpp` | P1 |
| L6 | ~~**The remaining "Test failed: TestN" numeric-mismatch bucket (27+7+2+1 = 37 cases before L1, an unmeasured remainder after)** -- a real `check-hlsl-feme-vk` re-run is needed to determine exactly how many of these 37 L1's fix actually resolved (L1's own measured +26 `Passed`/-26 `Failed` swing strongly suggests most of them, since the composite-construct-after-vector-builtin-call shape is common across this family, but the two numbers are not proven identical without a fresh per-case diff); whatever remains needs its own individual reduction, since "wrong number" can hide an unrelated root cause per case~~ (done: a fresh `feme-vk` re-run against a correctly-selected `feme_vulkan` ICD -- see `feme/.instructions.md`'s new "Running `feme-vk` / offload-test-suite against the real driver" section, added this same session after finding this container's system-wide `VK_ICD_FILENAMES` silently defaults to Mesa's `lavapipe`, not `feme`, unless overridden -- found this milestone's own bucket has narrowed to exactly 8 real remaining cases, all sharing one root cause: `Feature/HLSLLib/{degrees,log,log10,log2,radians,rsqrt,sinh,sqrt}.32.test` each feed a subnormal (denormal) `float32` input through one HLSL library intrinsic, and each test's own golden/expected output assumes the subnormal was flushed to a same-signed zero before the intrinsic's own math ran (e.g. `log(-denormal)` expected `-inf`, as if `log(-0)`, not the real, IEEE-754-correct `NaN` a genuine negative input produces) -- matching a real GPU's own special-function hardware unit, which (unlike ordinary arithmetic) commonly flushes a subnormal operand unconditionally, distinct from `VK_KHR_shader_float_controls`'s own `DenormFlushToZero` execution mode (roadmap F15b), which only flushes ordinary arithmetic, and only when a shader explicitly requests it (`dxc` never emits `DenormFlushToZero` for a plain HLSL `log`/`sqrt`/etc. call, confirmed by inspecting its own `spirv-dis` output, yet a real GPU's own reference output still behaves as if flushed). Fixed in `SPIRVToLLVMPatterns.cpp`: three new patterns (`TranscendentalFlushInputPattern` for `Log`/`Log2`/`Sqrt`/`Sinh`, `FlushedInverseSqrtPattern` for `rsqrt`, `FlushedScalePattern` for `Radians`/`Degrees`) reuse the existing `flushSubnormalToZero` helper unconditionally rather than gating it on the `DenormFlushToZero` execution mode, registered at `FeMeBenefit` to override upstream MLIR's own unconditional `DirectConversionPattern`/`InverseSqrtPattern`/`ScalePattern` for exactly these six ops (`log10` lowers to `Log2` plus an ordinary, unaffected `FMul` scale, so needs no pattern of its own); siblings whose own math is continuous/non-singular near zero (`Exp`, `Cos`, `Asin`, etc.) already produce the same answer with or without a flush for these tests' own inputs, so are deliberately left on MLIR's own default pattern. New `spirv-to-llvm-transcendental-flush-to-zero.mlir` lit test covers all six ops (scalar and vector-typed). `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2282/2309 (27 pre-existing unrelated `Unsupported`, 0 `Failed`). Confirmed via a direct A/B `feme-vk` re-run against the corrected ICD, isolating this fix from every other change: 137 `Passed`/220 `Failed` before, 145 `Passed`/212 `Failed` after (+8/-8, exactly the 8 named cases, 0 regressions, `Unsupported`/`Expectedly Failed`/`Unexpectedly Passed` unchanged at 266/26/1). The wider full-suite total (145/212, vs. the `Passed`/`Failed` figures L1-L13's own rows report) reflects this same corrected-ICD measurement, not a regression -- see M1 below, opened this session after this discovery, to re-audit which of this section's own earlier "confirmed via a real `feme-vk` rerun" claims used the correct ICD throughout (L5's own byte-for-byte-reproduced 133/224 baseline, recorded before this row's fix, is consistent with this row's own 137/220 pre-fix count modulo the L6-L13 fixes landed in between, so is likely trustworthy; this row's own investigation, before the ICD mistake was caught and corrected mid-session, is not). `VulkanCTSReport.md` updated with a real `deqp-vk` regression sweep; `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change (internal CPU-target-math correctness fix mirroring real hardware's own special-function-unit behavior, no feature/extension bit touched)) | L1 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L14 | ~~**Re-audit which of this document's own "confirmed via a real `check-hlsl-feme-vk`/`feme-vk` rerun" claims (L2 onward) actually ran against a correctly-selected `feme_vulkan` ICD**, discovered as an L6 milestone-description correction: this container's system-wide `VK_ICD_FILENAMES` silently defaults to Mesa's `lavapipe` software driver rather than `feme`'s own (`feme/.instructions.md`'s new "Running `feme-vk` / offload-test-suite against the real driver" section, added this same session, documents the required override and a `vulkaninfo`-based sanity check), so any session's own `llvm-lit`/`check-hlsl-feme-vk` invocation that omitted it would have silently measured `lavapipe`'s own (real, mature, and entirely unaffected by any `feme` source change) pass/fail counts instead -- a risk this row's own investigation fell into mid-session before catching and correcting it (see L6's own row above). A coarse cross-check this session found L5's own recorded 133/224 baseline plausible (consistent, modulo the L6-L13 fixes landed since, with this row's own freshly-measured, correctly-ICD-selected 137/220 pre-L6-fix count), but a real per-row audit -- rerunning each of L2/L9/L10/L11/L12(a/b/c)/L13's own named "before"/"after" cases individually against a correctly-selected `feme_vulkan` ICD and diffing against each row's own recorded numbers -- has not been done, and is the actual scope of this milestone~~ (done: rebuilt `feme_vulkan`/`offloader` and re-ran the full `feme-vk` suite with `VK_ICD_FILENAMES` correctly exported, confirming today's baseline (650 discovered, 145 Passed, 266 Unsupported, 26 XFAIL, 212 Failed, 1 Unexpectedly Passed) is byte-for-byte identical to L6's own just-recorded numbers -- ICD selection has been correct and stable across at least the L6-through-L13 span's own final measurement. Then individually re-ran every real named test case each of L2/L9/L10/L11/L12/L12a/L12b/L12c/L13 claims to have fixed or left failing, against both the correct `feme` ICD and (as a direct cross-check) the default (`lavapipe`) one: L2's own historical absolute counts are superseded by many later fixes and not independently re-verifiable in isolation, but its own qualitative claims (the 18-case workgroup-limit sub-bucket, the L9/L10 splits) are consistent with today's state; L9's own "24-case still-failing `Basic/Matrix/*.test` family" is now 17 (7 cases improved via unrelated later fixes, expected drift, not a discrepancy); L10's own two named still-failing groupshared cases (`GroupMemoryBarrierWithGroupSync.test`, `GroupSharedMatrixElementExprComponentDataRace.test`) both still fail exactly as it predicted, on the exact already-tracked H19p "unsupported calling convention" abort; L12/L12a/L12c's own `Feature/ResourceArrays/overflow-unbounded-array.test` claim holds (confirmed still passing today). Found two real discrepancies, neither an absolute-count-drift artifact: (1) **L11's own named `WaveOps/GroupSharedMatrixRowComponentDataRace.test` still fails identically today**, reproducing the exact pre-L11 error -- L11's own row already disclosed no real end-to-end rerun was done ("due to time/scope"), and this audit's own reduction (`dxc`/`feme-translate`/`feme-opt`, replaying this pipeline's real `feme-cpu-fold-spirv-builtins,feme-cpu-prepare,...,feme-cpu-linearize,feme-cpu-simdize,...` stage sequence) found why: the real test's own genuinely-divergent `if (ThreadID.x == 0)` branch means `feme-cpu-linearize` masks the groupshared row load/store into a `feme.cpu.masked.load/store.v4f32.as3` *call* first (needed for correct per-lane side effects under a real divergent branch), not the plain `LoadInst`/`StoreInst` `widenGroupSharedLoad` (and `checkVectorDecompositionSupported`'s own producer-recognition loop) dispatches on -- a shape L11's own fix never covered, tracked as new milestone L15 below. (2) **L13's own claim that all 6 named cases "now pass end-to-end" is false: only 2 of 6 (`Feature/CBuffer/vectors.test`, `Feature/ConstantBufferT/vectors.test`) actually pass against the real `feme` ICD** -- the other 4 (`Feature/StructuredBuffer/packed.test`, `Feature/CBuffer/{structs,array-of-structs,dynamic-struct}.test`) still fail with the exact same `failed to legalize operation 'spirv.AccessChain' that was explicitly marked illegal` error L13 itself was supposed to close, **and this audit directly confirmed why L13's own session missed it: re-running these same 4 cases against the default (`lavapipe`) ICD shows all 4 `PASS`** (a real, mature driver's own SPIR-V compiler has no trouble with this shape at all) -- conclusive, reproduced evidence that L13's own "confirmed... as part of a full-suite run" claim was made against `lavapipe`, exactly the failure mode this row exists to catch. Root-caused via a real `feme-opt` reduction of `structs.test`'s own SPIR-V: each failing case's own struct has a fixed-size array member of an identified struct whose declared per-element `stride` does not match that struct's own natural size (e.g. `!spirv.array<2 x !spirv.struct<X, (si32 [0])>, stride=16>`, stride 16 vs. `X`'s own natural 4-byte size) -- a gap `convertOffsetStructTypeIgnoringDecorations`'s own doc comment already anticipated verbatim ("a fixed-size array member whose own declared stride does not match its element's natural size -- tracked separately, see roadmap L13a"), but whose own referenced roadmap row **L13a was never actually added** to this document, a pure bookkeeping gap this audit restores as new milestone L13a below (not new work discovered by this row, just a citation this document itself never fulfilled). `VulkanCTSReport.md` updated with this session's own findings; no feature-bit or extension-advertisement change from this audit itself (an audit-and-bookkeeping pass, not a compiler fix), so `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` are unchanged) | L6 | `feme/docs/Roadmap.md` | P2 |
| L13a | ~~**`convertOffsetStructTypeIgnoringDecorations`'s own "tight-vector retry" fallback (`SPIRVToLLVMPatterns.cpp`) does not handle a fixed-size array member of an identified struct whose declared per-element `stride` does not match that struct's own natural (ABI) size** -- e.g. `!spirv.array<2 x !spirv.struct<X, (si32 [0])>, stride=16>`, a real `-fvk-use-dx-layout`/`-fvk-use-scalar-layout` shape whenever an array-of-structs member is immediately followed by another member needing 16-byte alignment. Already anticipated verbatim in that function's own doc comment ("tracked separately, see roadmap L13a") when L13 landed, but never actually added to this document until this L14 audit restored it (a pure citation-vs-row bookkeeping gap, not new work L14 itself discovered). Confirmed via this L14 audit's own real `feme-opt` reduction of `Feature/CBuffer/structs.test`'s own SPIR-V (and via `Feature/CBuffer/array-of-structs.test`/`dynamic-struct.test`/`Feature/StructuredBuffer/packed.test`, all four hitting the identical `failed to legalize operation 'spirv.AccessChain'` error) to be the exact, complete remaining gap in L13's own scope: with this fixed, all four of L13's own originally-named cases still failing today should pass. Needs its own scoping pass: likely a further fallback alongside the existing tight-vector-array retry -- representing the identified struct's *own* body as a tightly-packed byte-array stand-in stride-wise (mirroring `getTightVectorArrayType`'s own vector case) whenever its natural size undershoots the declared stride, reassembled the same way `CompositeConstructPattern`'s own struct case already reassembles a tight-vector substitution today~~ (done, for the exact SPIR-V-to-LLVM-conversion-layer gap this row itself scoped: added `padStructToSize` -- appends a trailing byte-array pad *member* to an already-bodied identified struct's own body so its natural size exactly matches a declared stride/gap, deliberately appending rather than wrapping in a new outer struct, since struct layout computation is strictly forward-only (an appended trailing member can never change any earlier member's own offset) -- so, unlike the tight-vector substitution L13 already added, this needed **no** consumer-side `AccessChain`/GEP change at all (confirmed by manually tracing `structs.test`'s own multi-field `Z` struct, which has a non-zero-offset field after the padded one, through to a successful legalization). Added `padUndersizedMembersIfNeeded` (a "growth retry" alongside the existing tight-vector retry, composable with it) and a new `convertArrayTypeIgnoringDecorations` override (registered in `populateSPIRVToLLVMTargetTypeConversions`, mirroring the existing `StructType` override's placement) that supersedes MLIR's own array-type conversion -- which unconditionally fails via `VulkanLayoutUtils::getNaturalArrayStride` for any identified-struct element -- and pads that element to the declared `ArrayStride` via `padStructToSize`. New lit test `spirv-to-llvm-array-of-identified-struct-stride.mlir` covers both the array-of-identified-struct-element and standalone-identified-struct-member shapes; `ninja check-feme` (assertions-enabled, ccache build) passes in full, 2283/2310 (27 pre-existing unrelated `Unsupported`, 0 `Failed`, up by exactly this row's 1 new lit test). Confirmed via direct `feme-opt --feme-convert-spirv-to-llvm` reduction that `Feature/CBuffer/structs.test`'s own real SPIR-V (and a minimal `X x1; X x2;` repro) now fully legalizes, and that the 2 already-passing L13 cases (`CBuffer/vectors.test`, `ConstantBufferT/vectors.test`) still legalize unchanged (no regression). **However, a real `check-hlsl-vk-feature-{cbuffer,structuredbuffer}` rebuild-and-rerun against the actual `feme_vulkan` ICD (not just the `feme-opt` reduction above) found `structs.test` still fails end to end** -- not at this row's own SPIR-V-to-LLVM conversion layer at all (which this row confirms now succeeds), but at a *later*, entirely separate compiler phase (LLVM-IR-to-CPU-runtime resource lowering) this row never touched, split out to new row L16 below; `array-of-structs.test`/`dynamic-struct.test` still fail even earlier, at conversion, for a distinct scalar-array-element shape this row deliberately left out of scope (see its own `convertArrayTypeIgnoringDecorations` doc comment), split out to new row L17 below; and `packed.test` still fails, now for a different, newly-exposed, pre-existing `spirv.BranchConditional`-to-`llvm.cond_br` signedness gap unrelated to this row's own scope, split out to new row L18 below. `VulkanCTSReport.md` updated with a real `deqp-vk` regression sweep (`dEQP-VK.ubo.*`/`dEQP-VK.ssbo.layout.*`, same families L5/L13/L14 already swept); `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no change (internal SPIR-V-to-LLVM conversion correctness fix, no feature/extension bit touched)) | L13 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L15 | ~~**`SIMDize.cpp`'s L11 fix (`widenGroupSharedLoad`'s vector-row gather) is only reachable through a plain, unmasked `LoadInst`/`StoreInst` -- not through the `feme.cpu.masked.load/store.*.as3` *call* form `feme-cpu-linearize` produces whenever the groupshared access is itself inside genuinely divergent control flow** (e.g. a real `if (ThreadID.x == 0)` guard, as `WaveOps/GroupSharedMatrixRowComponentDataRace.test` itself has) -- found as an L14 milestone-description correction (L11's own real named test still fails identically today; L11's own row had already disclosed no real end-to-end rerun confirmed this, only a from-scratch IR reduction with no divergent branch in it at all). `checkVectorDecompositionSupported`'s own producer-recognition loop has no case at all for a `feme.cpu.masked.load.*` call producing a vector result (only `matchResourceCall`/`matchImageCall`/a homogeneous intrinsic/an ordinary `LoadInst` are recognized), so it hits the same generic "unsupported producer" diagnostic L11 was supposed to close; even if recognized, `widenMaskedLoad` itself would still need its own vector-aware path, since it currently builds one illegal `<W x <4 x float>>` `llvm.masked.gather` unconditionally (`FixedVectorType::get(CI.getType(), WaveSize)` where `CI.getType()` is already a vector) rather than `widenGroupSharedLoad`'s existing per-component decomposition. Needs its own scoping pass: likely extending `checkVectorDecompositionSupported` to accept a `matchMaskedLoad`/`matchMaskedStore` call over a groupshared address as a supported vector producer/consumer, and giving `widenMaskedLoad`/`widenMaskedStore` a groupshared-specific vector-typed branch that reuses `widenGroupSharedLoad`'s own per-component gather logic (mirroring how `widenGroupSharedLoad` itself already reuses `widenGroupSharedGEP`'s address widening) instead of the generic single-gather path~~ (done: added a `matchMaskedLoad` producer case plus a new vector-typed branch in `widenMaskedLoad` -- `N` per-component masked gathers off the same `<W x ptr>` row address, mirroring `widenGroupSharedLoad`'s unmasked vector case, gated on the call's own governing mask/passthru -- and a groupshared-gated (`isGroupSharedPointerType`) vector-typed branch in `widenMaskedStore` -- `N` per-component `llvm.masked.scatter`s, same address pattern -- since the existing generic per-lane extractelement/load/select/store idiom there produced a leaf `feme::cpu::rewriteGroupSharedGlobals` could not retarget for a groupshared address; that pass's own existing second-level-getelementptr-feeding-a-masked-gather-or-scatter validation already generically covers a scatter too, so it needed no changes. Two new lit tests added (`simdize-groupshared-masked-vector-row-load.ll`, `simdize-groupshared-masked-vector-row-store.ll`), each confirmed to fail with the pre-fix error and pass post-fix; the existing non-groupshared `simdize-masked-memop-vector-divergent.ll` still passes unchanged. `ninja check-feme`: 2285/2312 passed, 27 unsupported, 0 failed (up by exactly these 2 new tests). Confirmed end-to-end against the real `feme_vulkan` ICD: the real `WaveOps/GroupSharedMatrixRowComponentDataRace.test` now clears both the SIMDize and GroupShared errors this milestone targeted, and fails only on the pre-existing, separately-tracked "unsupported calling convention" abort (roadmap H19p) -- confirming this milestone's own scope is fully closed. No feature-bit or extension-advertisement change (an internal CPU-target compiler-pass fix), so `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` are unchanged; see `VulkanCTSReport.md` for this session's CTS sweep) | L11 | `feme/lib/Transforms/CPU/SIMDize.cpp` | P2 |
| L16 | ~~**`SPIRVResourceLowering.cpp`'s `hasOnlySupportedUses`/`hasOnlySupportedPointerUses` never allows a `getelementptr` past a `HandleKind::Uniform` (real read-only `cbuffer`) resource's own `llvm.spv.resource.getpointer` result** (`AllowGEPs` is hard-coded `Kind == HandleKind::Storage \|\| Kind == HandleKind::StorageStruct`, never `Uniform`) -- found as an L13a milestone-description correction: `structs.test`'s own struct-typed `cbuffer` member (`X x1;`, `X` an identified struct with its own field `a1`) now converts cleanly at the SPIR-V-to-LLVM layer (L13a's own fix), but the CPU resource-lowering pass this row names still rejects the resulting `getpointer`-then-`getelementptr`-into-`x1.a1` chain outright, via `UnsupportedOps.cpp`'s generic "is a register-bound resource handle the FeMe CPU target cannot normalize" diagnostic (confirmed directly with `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader`, since `llvm-lit`'s own `lit.cfg.py` strips that env var). Distinct from, and blocking end-to-end pass independently of, L13a's own scope: **any** `cbuffer`/direct-field-storage-block member needing further field navigation beyond a single flat scalar/vector load (i.e. any struct-typed member at all, not just the newly-legalizable padded ones) has apparently never been supported by this pass -- `CBuffer/vectors.test`'s own passing case has no struct-typed member, so never exercised this path. Needs its own scoping pass: likely enabling `AllowGEPs` for `HandleKind::Uniform` too, confirming `hasResolvableGEPByteOffset`'s existing byte-offset math needs no `Uniform`-specific change, and checking whatever emits the actual runtime call for a GEP-resolved `Uniform` address (`createRawLoad`/`mangleResourceCallName` and siblings) already handles a nonzero byte offset the same way the already-supported `Storage`/`StorageStruct` GEP path does~~ (done: enabled `AllowGEPs` for `HandleKind::Uniform` too, reusing the identical GEP-chain-to-byte-offset machinery (`hasResolvableGEPByteOffset`, `computePointerOffset`, `lowerRawPointerUses`) `HandleKind::StorageStruct` already exercises -- confirmed that machinery needed no `Uniform`-specific change via a real `dxc`/`feme-translate`/`feme-opt --llvm -passes=feme-cpu-lower-spirv-resources` reduction of the exact shape (a nested `getelementptr` into a struct-typed cbuffer field's own body), and via a real `offloader` run (`FEME_VULKAN_LOG_CREATION_ERRORS=1`) confirming the "register-bound resource handle... cannot normalize" error is gone. Added a new unit test (`LowersUniformBufferNestedStructFieldToItsOwnStructLayoutOffset`) and lit test (`spirv-resource-lowering-uniform-nested-struct.ll`); `ninja check-feme`: 2287/2314 passed, 27 unsupported, 0 failed (up by exactly these 2 new tests). Rebuilt `feme_vulkan`/`offloader` and confirmed end-to-end against the real ICD: `Feature/CBuffer/structs.test` -- this row's own named case -- now **passes** outright; `array-of-structs.test`/`dynamic-struct.test` still fail, but now confirmed identical to L17's own already-tracked `failed to legalize operation 'spirv.AccessChain'` error, and `StructuredBuffer/packed.test` still fails, confirmed identical to L18's own already-tracked `'llvm.cond_br' op operand #1` error -- both distinct, out-of-scope gaps this row's own fix does not touch, exactly as L13a's own report predicted. A `dEQP-VK.ubo.*` sweep (13,240 cases) measured 1406/13240 (10.6%) passed, at or above the high end of L13a's own already-documented ~115-case flaky band (1051-1166); `dEQP-VK.ssbo.layout.*` (5,275 cases) measured 227/5275, byte-for-byte identical to the prior baseline (expected, since a storage-buffer's own `AllowGEPs` path was already supported and untouched by this fix). No feature-bit or extension-advertisement change; confirmed, not assumed, that `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no update) | L13a | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P2 |
| L17 | ~~**A fixed-size array of a *scalar* type inside a struct/cbuffer, immediately followed by another sibling member, is not handled by L13a's own `convertArrayTypeIgnoringDecorations`** -- deliberately left out of that row's own scope (see its doc comment). The real shape (`Feature/CBuffer/array-of-structs.test`/`dynamic-struct.test`'s own `uint x[2]; uint q;`) is `struct<S, (array<2 x i32, stride=16> [0], i32 [20])>`: real HLSL/Vulkan layout semantics require every array element *except the last* to occupy the full declared stride (so a dynamic index into `x[1]` still addresses correctly), but the array's own occupied footprint for a *subsequent* sibling member's own placement is only `(N-1)*Stride + NaturalSize(last element)` -- i.e. `q` is allowed to pack into the otherwise-unused trailing padding of `x`'s own last element. A single, homogeneous `LLVM::LLVMArrayType` cannot represent "N-1 stride-wide elements plus one natural-size element" at all (every element, including the last, must be the same width); representing it instead as a heterogeneous struct (`struct<(array<(N-1) x Padded>, Unpadded)>`) cannot support a *dynamic* (runtime-value) index selecting between the array portion and the final-element portion, since LLVM `getelementptr` struct-member selection requires a compile-time-constant index -- a materially harder problem than L13a's own append-only padding. Needs its own scoping pass to determine whether a dynamic index into such an array can be legalized at all without either (a) always widening the *last* element to the full stride too (over-allocating by `Stride - NaturalSize(last)` bytes, wasting space but preserving a uniform array and requiring no representation change), or (b) a real heterogeneous-with-dynamic-index scheme this codebase does not have a precedent for anywhere yet~~ (done: generalized `convertArrayTypeIgnoringDecorations` so a non-struct array element undershooting its declared stride substitutes a uniform `Stride`-sized opaque byte-array stand-in for every element (mirroring `getTightVectorArrayType`'s identical opaque-pointer-safe reasoning) instead of returning `nullptr` -- this alone handles the general case, including a genuinely *dynamic* SPIR-V index, resolving option (a)'s real scope (uniform widening's own memory-correctness limitation -- not merely "wasted space" -- is that it only works when no sibling's own declared offset conflicts with the widened footprint). Added a new, narrower helper, `convertUndersizedScalarArrayMemberIgnoringDecorations`, for the sibling-conflicting case: a literal heterogeneous LLVM struct with one member per array element (the `Stride`-sized stand-in for every element except the last, which keeps its own real, unpadded type), reproducing the sibling's true declared offset -- this relies on `LLVM::GEPOp`'s own `destructureIndices` auto-detecting a compile-time-constant SPIR-V index when navigating a literal struct type (confirmed via `LLVMDialect.cpp`), so it is scoped to constant-index accesses only (true of every real `dxc`-emitted HLSL array index in the affected tests); a genuinely dynamic index into this specific sibling-conflicting shape remains unsupported, a real residual limitation matching option (b)'s own "no precedent" framing, not merely a documentation gap. Two new lit tests added (`spirv-to-llvm-scalar-array-with-trailing-sibling.mlir`, `spirv-to-llvm-scalar-array-uniform-widening.mlir`); `ninja check-feme`: 2289/2316 passed, 27 unsupported, 0 failed (up by exactly these 2 new tests, 0 regressions). Rebuilt `feme_vulkan`/`feme-opt`/`feme-translate`/`offloader` (the real Vulkan ICD `.so`, a separate ninja target that must be rebuilt independently of `feme-opt`/`offloader` to pick up any conversion-layer change for real end-to-end testing) and confirmed against the real ICD: **`Feature/CBuffer/array-of-structs.test` and `Feature/CBuffer/dynamic-struct.test` (this row's own two named cases) now pass end-to-end**, and `Feature/CBuffer/array-dynamic-index.test` also newly passes as a bonus fix from the generalized uniform-widening path; `check-hlsl-vk-feature-cbuffer`: 27 passed, 2 expectedly-failed (pre-existing, unrelated), 0 unexpected failures. A `dEQP-VK.ubo.*array*` sweep (8,186 cases), run twice before and twice after the fix via a temporary revert/rebuild cycle, measured 639/615 (pre-fix) vs. 519/594 (post-fix) passed -- an overlapping, noisy band consistent with (slightly wider than) this project's own already-documented `dEQP-VK.ubo.*` flakiness (L13a), providing no clear signal at that scale; the deterministic real-ICD `llvm-lit` end-to-end confirmation above is the authoritative before/after signal. No feature-bit or extension-advertisement change; confirmed, not assumed, that `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no update) | L13a | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L18 | ~~**`Feature/StructuredBuffer/packed.test` fails with `'llvm.cond_br' op operand #1 must be variadic of LLVM dialect-compatible type, but got 'si32'`**, found as an L13a milestone-description correction: this is a real, distinct, newly-*exposed* (not newly-caused) gap L13a's own fix did not touch -- `packed.test` was one of L5's own original 6 crash cases and one of L13's own 4 still-graceful-failure cases, but L13a's own fix advances its legalization far enough to reach a different failure than either of those rows saw. Root cause not yet confirmed, but the shape (a raw, un-type-converted `si32` block argument reaching `llvm.cond_br`) closely mirrors L10's own already-fixed `spirv.GroupNonUniform*`-integer-reduce `si32` gap (an upstream MLIR pattern building an op directly from a raw SPIR-V-signed type rather than running it through the type converter first) -- likely the analogous upstream `spirv.BranchConditional`-to-`llvm.cond_br` conversion pattern (`mlir/lib/Conversion/SPIRVToLLVM/SPIRVToLLVM.cpp`) has the identical bug for a block argument's own type, needing a feme-side override at `FeMeBenefit` the same way L10's `IntegerGroupNonUniformReducePattern` overrode the analogous group-reduce pattern, once confirmed via its own real IR reduction~~ (done: confirmed via a real `feme-opt`/`dxc` reduction of `packed.test`'s own SPIR-V that the root cause is exactly as guessed -- `mlir::BranchConditionalConversionPattern` (upstream) builds `llvm.cond_br`'s successor operands directly from `op.getTrueBlockArguments()`/`op.getFalseBlockArguments()`, the *original* op's own raw, un-remapped accessors, rather than `adaptor.getTrueTargetOperands()`/`adaptor.getFalseTargetOperands()`, the dialect conversion's own type-converted ones. Added `BranchConditionalPattern` (`SPIRVToLLVMPatterns.cpp`), registered at `FeMeBenefit`, mirroring the upstream pattern exactly except for using the adaptor's own operands -- unlike L10's own fix (scoped to nine specific integer-reduce ops), this applies uniformly to every `spirv.BranchConditional`, since it is a strict improvement with no downside for any other case. New lit test `spirv-to-llvm-branch-conditional-signed-argument.mlir` covers the exact shape; `ninja check-feme`: 2290/2317 passed, 27 unsupported, 0 failed (up by exactly this 1 new test, 0 regressions). Rebuilt `feme_vulkan`/`offloader` and confirmed via `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader` that `packed.test`'s own pre-fix `llvm.cond_br`/`si32` error is gone post-fix (a real before/after `git checkout`/rebuild/revert cycle on just this one file, matching L17's own methodology) -- **this row's own scope is fully closed**. `packed.test` itself still does not pass end-to-end: its real, deterministic `check-hlsl-vk-feature-structuredbuffer` pass/fail count (9/650 failing, byte-for-byte identical before and after this fix, confirmed via the real `feme_vulkan` ICD with `VK_ICD_FILENAMES` explicitly exported) is unchanged, since the fix advances `packed.test`'s own legalization to a distinct, deeper, newly-exposed gap (a struct-typed storage-buffer array element's own pointer misclassified as itself a top-level `Block`), split out to new row L19 below. A `dEQP-VK.ssbo.layout.*`/`dEQP-VK.ssbo.*` sweep (both against the real ICD, before and after this fix via the same revert cycle) measured byte-for-byte identical totals (260/5275 `dEQP-VK.ssbo.layout.*`, both runs), unsurprising since this fix's own target shape (an HLSL/dxc-emitted `si32` branch-successor merge) is not one this GLSL/SPIR-V-assembly-sourced CTS family happens to exercise; no regression evident. No feature-bit or extension-advertisement change; confirmed, not assumed, that `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no update) | L13a | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L19 | ~~**A struct-typed storage-buffer array element's own `spirv.ptr<StructType, StorageBuffer>` is misclassified by `isBufferBlockStorage`/`getBufferBlockElement` (`SPIRVToLLVMPatterns.cpp`) as itself a top-level buffer-block pointer**, found as an L18 milestone-description correction: `isBufferBlockStorage` returns `true` unconditionally for *any* `StorageBuffer`-storage-class pointer to *any* struct (never checking for a `Block` decoration at all in that branch, unlike its own `Uniform`-storage-class/`BufferBlock`-decoration branch just below it), so a `RWStructuredBuffer<Doggo>`'s own per-element `Doggo` struct pointer (reached once `spirv.AccessChain` has already selected one array element, as `Feature/StructuredBuffer/packed.test`'s own `Doggo Fido = Buf[GI]; ...; Buf[GI] = Fido;` whole-struct-copy idiom does, unlike every other `StructuredBuffer` test, which only ever navigates directly to an individual scalar/vector field via a single multi-index access chain) is spuriously converted into a *second*, nested `spirv.VulkanBuffer` resource handle instead of ordinary memory (address space 11), confirmed directly via `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader` (`'llvm.store' op operand #1 must be LLVM pointer type, but got '!llvm.target<"spirv.VulkanBuffer", ...>'`) and a real `feme-opt --feme-convert-spirv-to-llvm` reduction of `packed.test`'s own SPIR-V. Needs its own scoping pass: likely requiring `isBufferBlockStorage`'s `StorageBuffer`-class branch to also check for the struct's own `Block` decoration (mirroring its `Uniform`-class/`BufferBlock` branch immediately below), confirming every currently-passing `StructuredBuffer`/`ConstantBuffer`/`cbuffer` test's own top-level block struct still carries that decoration (so this tightened check does not regress any of them), and checking whether any further pattern (e.g. a struct-typed `spirv.Store`/`spirv.Load` reaching an ordinary address-space-11 pointer) needs its own new support once this misclassification is fixed~~ (done: confirmed the guess exactly -- fixed `isBufferBlockStorage`'s `StorageBuffer`-class branch to also require the struct's own `Block` decoration, mirroring the `Uniform`-class/`BufferBlock` branch immediately below. Every synthetic `StorageBuffer` test fixture in-tree (lit tests and gtest-embedded SPIR-V alike) turned out to declare its own top-level block struct *without* the `Block` decoration -- previously invisible, since this branch never checked for it -- so all of them needed the missing decoration added; real `dxc`/glslang output always includes it, so this is a pure test-fixture correction, not a behavior change for anything real. Added a new lit test, `spirv-to-llvm-storage-buffer-struct-element.mlir`, covering the exact misclassified shape (a whole-struct load/store through a per-array-element struct pointer with no `Block` decoration) in isolation, confirmed failing without the fix and passing with it. `ninja check-feme`: 2291/2318 passed, 27 unsupported, 0 failed (up by exactly 1 new test, 0 regressions). Rebuilt `feme_vulkan`/`offloader` and confirmed via a real `feme-translate`/`feme-opt` reduction of `packed.test`'s own SPIR-V (compiled with `-fspv-target-env=vulkan1.3`, matching `offload-test-suite`'s own real invocation) that the exact `llvm.store` operand-type error this milestone names is gone post-fix -- **this row's own scope is fully closed**. `check-hlsl-vk-feature-structuredbuffer` against the real ICD: 9/650 failing both before and after (byte-for-byte identical, confirmed via a temporary `git checkout`/rebuild/revert cycle matching L17/L18's own methodology) -- 0 regressions. `packed.test` itself still does not pass end-to-end: its own post-fix error (confirmed via `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader`) has advanced from this milestone's own named error to a new, distinct gap in `feme::cpu::SPIRVResourceLoweringPass` (a whole-struct load/store off a resource pointer is rejected outright, since `isSupportedRawElementType` only ever accepts a scalar or fixed vector, never a struct) -- split out to new row L20 below. `dEQP-VK.ssbo.layout.*` (5,275 cases) and `dEQP-VK.ssbo.*` (12,225 cases), both against the real ICD via the same revert cycle: byte-for-byte identical totals before and after (260/5275 and 361/12225 respectively); no regression. No feature-bit or extension-advertisement change; confirmed, not assumed, that `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no update) | L18 | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` | P2 |
| L20 | ~~**`feme::cpu::SPIRVResourceLoweringPass`'s `isSupportedRawElementType` (`SPIRVResourceLowering.cpp`) rejects a whole-struct `load`/`store` off a resource pointer outright, accepting only a scalar or fixed vector**, found as an L19 milestone-description correction: with L19's own fix landed, `Feature/StructuredBuffer/packed.test`'s own `Doggo Fido = Buf[GI]; ...; Buf[GI] = Fido;` whole-struct-copy idiom now converts cleanly at the SPIR-V-to-LLVM layer (an ordinary address-space-11 pointer, not a spurious nested handle), but `hasOnlySupportedPointerUses`/`hasOnlySupportedUses` (both in `SPIRVResourceLowering.cpp`) still reject the resulting whole-`Doggo`-struct `llvm.load`/`llvm.store` outright via `isSupportedRawElementType`, which only ever recognizes a half/float/double/integer scalar or a fixed vector of one -- never a struct -- so the pass falls through to `UnsupportedOps.cpp`'s generic "is a register-bound resource handle the FeMe CPU target cannot normalize" diagnostic, confirmed directly via `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader`. Distinct from, and blocking end-to-end pass independently of, L16's own already-fixed scope (a struct-typed *field*, reached via `getelementptr`, converting to its own further load/store) and L19's own scope (the SPIR-V-to-LLVM type-conversion layer): this gap is purely in the CPU resource-lowering pass's own raw-load/store mangling, for the specific case of loading/storing an entire aggregate value directly, with no `getelementptr` navigation into an individual field at all. Needs its own scoping pass: likely either (a) extending `isSupportedRawElementType` to accept a struct type too, and teaching `createRawLoad`/`createRawStore`/`mangleResourceCallName`'s own `appendScalarMangling` (`ResourceCalls.cpp`) to describe an aggregate's own flattened field/element types in its mangled name (mirroring how a fixed vector's own element type and width are already encoded), or (b) decomposing a whole-struct load/store into its own per-field raw loads/stores at this pass, reassembled/exploded via `insertvalue`/`extractvalue` the same way `CompositeConstructPattern`'s own struct case already does at the SPIR-V-to-LLVM layer -- whichever avoids ambiguity with `isSupportedTexelElementType`'s own texel-buffer case, which already special-cases a texel buffer's own `<4 x T>` element type for a different reason (see `hasOnlySupportedUses`'s own comment)~~ (done: extended `isSupportedRawElementType` to recursively accept a struct or fixed-size array whose own members/elements are themselves supported, and added `lowerRawLoad`/`lowerRawStore` helpers that decompose a whole-aggregate load/store into one raw call per leaf scalar/vector field/element, reassembled with `insertvalue`/`extractvalue` -- approach (b), mirroring `CompositeConstructPattern`'s own struct-reassembly precedent, avoiding any `appendScalarMangling`/`mangleResourceCallName` change. A real IR reduction of `packed.test`'s own SPIR-V found the actual shape needs array handling too, not just structs: `Doggo`'s own `int3`/`int2` vector fields convert to fixed-size LLVM arrays (not LLVM vectors) once nested inside this tightly-packed struct; array support was not scoped to nested-in-struct only, since a bare top-level array (e.g. `Feature/StructuredBuffer/matrix.test`'s own per-row array-of-vectors storage) never itself reaches `appendScalarMangling` either, and decomposing it the same way is a real, safe improvement (confirmed via the renamed `LowersStorageBufferArrayStoreToPerElementRawStores` gtest, covering this project's own `dEQP-VK.spirv_assembly.instruction.spirv1p4.opselect.array_select` shape too). New lit test `spirv-resource-lowering-struct.ll` and gtest `LowersWholeStructLoadAndStoreWithArrayFieldsToPerLeafRawCalls` cover the exact struct-with-array-fields shape; `ninja check-feme`: 2293/2320 passed, 27 unsupported, 0 failed (up by exactly 2 new tests, 0 regressions). Rebuilt `feme_vulkan`/`offloader` and confirmed against the real ICD: `check-hlsl-vk-feature-structuredbuffer` improved from 9/650 to 7/650 failing -- `matrix.test`/`matrix_assign.test` now pass in addition to this row's own named case. **`packed.test` itself still does not pass end-to-end**: its post-fix error (confirmed via `FEME_VULKAN_LOG_CREATION_ERRORS=1 offloader`) has advanced past this row's own scope to a distinct, newly-exposed gap in `feme::cpu::SIMDizePass` (a divergent value of aggregate type has no component-decomposition support at all), split out to new row L21 below. A `dEQP-VK.ssbo.layout.*` (5,275 cases) and `dEQP-VK.ssbo.*` (12,225 cases) sweep against the real ICD measured byte-for-byte identical totals before and after (260/5275 and 361/12225 respectively); no regression evident, unsurprising since this fix's own target shape (an HLSL-emitted whole-struct-copy idiom) is not one this GLSL/SPIR-V-assembly-sourced CTS family happens to exercise. No feature-bit or extension-advertisement change; confirmed, not assumed, that `Vulkan14FeatureInventory.md`/`VulkanExtensionInventory.md` need no update) | L19 | `feme/lib/Transforms/CPU/SPIRVResourceLowering.cpp` | P2 |
| L21 | ~~**`feme::cpu::SIMDizePass`'s `checkVectorDecompositionSupported`-adjacent preflight check rejected any divergent value of aggregate (struct or array) type outright, with no component-decomposition path at all.** Fixed: a divergent `insertvalue` chain (mixing a whole sub-array/sub-struct inserted at once with genuine scalar leaves) and an `extractvalue` reading either a scalar leaf or a nested sub-aggregate back out now decompose into one `<W x T>` per flattened scalar leaf (`WidenedAggregateComponents`/`getAggregateComponents`/`checkAggregateValueSupported`/`widenInsertValue`/`widenExtractValue` in SIMDize.cpp), mirroring the existing vector-component scheme, gated on every leaf being a genuine scalar (`isAllScalarAggregateLeaves`). `Feature/StructuredBuffer/packed.test` now passes end-to-end (confirmed via `check-hlsl-vk-feature-structuredbuffer`, all 12 discovered cases pass, and a direct `offloader` run); an aggregate-typed `PHINode` and a non-`insertvalue`/`extractvalue` aggregate producer (e.g. an ordinary aggregate-typed `load`) remain diagnosed, and a vector-typed leaf is still out of scope -- no real case has needed either yet.~~ (see "Roadmap L21" in `VulkanCTSReport.md`) | L20 | `feme/lib/Transforms/CPU/SIMDize.cpp` | P2 |
| L7 | **A long tail of genuinely unimplemented SPIR-V/MLIR legalization patterns this suite's own HLSL-derived shapes reach that no `deqp-vk` case in this project's own recorded runs has ever reached**: matrix `spirv.CompositeConstruct`/`spirv.AccessChain`/`spirv.Transpose`, combined-image-sampler `spirv.Image`, several `spirv.GL.*` builtins (`SmoothStep`/`Length`/`Distance`/`Atan2`/`Step`/`Normalize`/`UnpackHalf2x16`/`ImageDrefGather`), several `spirv.GroupNonUniform*` wave-op variants (`IMul`/`IAdd`/`AllEqual`/`Shuffle`/`Elect`/`BitwiseAnd`/`BitwiseOr` across multiple int widths -- the `BitwiseAnd`/`BitwiseOr` pair already has a narrower, tracked `feme-cpu-simdize`-side gap at H6g-b-a-i-a-i-b's own citation, but the *legalization* gap for the other variants is new and untracked anywhere), an `unhandled Decoration : 'NonUniform'`, an `unknown extension: SPV_KHR_compute_shader_derivatives`, and a couple of raw `unhandled opcode`/`unhandled deserializations ... from extension set GLSL.std.450` errors. None of this is mesh/ray-tracing/version-floor work already covered by an H/J/K row above (confirmed by grepping this document for every one of these names before adding this row); each needs its own scoping pass once assigned, split per coherent cluster rather than landed as one row, following H7/H19's own precedent | none | `feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp` (unconfirmed) | P2 |
| L8 | **A real `deqp-vk` case, `dEQP-VK.pipeline.monolithic.push_constant.graphics_pipeline.count_1_shader_vert_frag_command2`, silently crashes this ICD** (the `deqp-vk` process itself exits with no further log output right after printing the test case's own name -- no assertion, no `llvm_unreachable`, no stderr at all -- distinct from L5's "PLEASE submit a bug report" bucket, which at least prints a diagnosable crash message first), found incidentally while spot-checking L4's own push-constant fix against a real `deqp-vk` push-constant sweep rather than only `check-hlsl-feme-vk`. Confirmed **not** a regression from either this session's own SIMDize (L1) or push-constant (L4) fix: reproduces identically (same silent crash, same point) with both fixes reverted via a scratch `git checkout <pre-session-commit> -- SIMDize.cpp SPIRVPushConstantLowering.cpp` rebuild, so this is a pre-existing gap, newly discovered rather than newly caused. Needs its own reduction (likely via the same real-ICD-plus-diagnostic technique the H-track rows above use, since a silent exit with no log output suggests a raw signal such as `SIGSEGV`/`SIGABRT` escaping `deqp-vk`'s own crash handler entirely, which will need running under a debugger or a signal-catching wrapper rather than this project's usual `feme-translate`/`feme-opt` IR-reduction pipeline to even get a first backtrace) before it can be scoped as a fix | none | unconfirmed | P1 |

Sequencing: L1 already landed; L2-L5 are the highest-value next steps (each
plausibly a single or a handful of root causes affecting many cases, the
same "measure before assigning" discipline G3/J9 already establish), L6 is
now done, M1 is new bookkeeping to re-confirm this section's own historical
measurements used the right driver, and L7 is the largest but
lowest-priority-per-case bucket, deliberately last, since a full per-cluster
scoping survey (mirroring H7's own "H7a closes the first, lowest-risk
cluster" pattern) is needed before it can be split into real, individually
assignable rows.

## Part 2: End-to-end testing roadmap

### 2.1 What exists today

| Layer | Tests |
|---|---|
| CLI translation | `test/Tools/feme/feme-dxil-to-{dxil,spirv,amdgpu}.ll`, `feme-spirv-{null-pipeline,compute-shader,to-amdgpu}.mlir` |
| CLI CPU target | `test/Tools/feme/feme-cpu-{loop,wave-size,accept-bound-resource,reject-unbounded-register-bound}.ll` |
| Execution, hand-written IR | `test/Tools/feme-run/{thread-id-store,multi-group-dispatch,dxil-container-input,reference-mode}.ll` |
| Execution, real HLSL | `test/Tools/feme-run/HLSL/{loop,divergent-control-flow,wave-ops,barrier-groupshared,combined}.hlsl` |
| Differential | `test/Tools/feme-run/differential-harness.test` (5 seeds) |
| Execution, C++ | `unittests/Target/CPU/{JITEngineTest,AOTDispatchTest}.cpp` |

That is real end-to-end coverage — HLSL source through Clang, DXIL import,
raising, the whole CPU pipeline, JIT dispatch, and a `FileCheck`ed numeric
result. The gaps below are about *breadth of axis*, not about the pipeline
being untested.

### 2.2 The axes not yet covered

1. **Wave size** (done by R1). Three of the five executing HLSL end-to-end
   tests whose own computation does not depend on wave width (`loop.hlsl`,
   `divergent-control-flow.hlsl`, `barrier-groupshared.hlsl`) now run at `W`
   in {4, 8, 16, 32} via the `%feme-wave-size-sweep` lit substitution
   (§2.4.1); `wave-ops.hlsl`/`combined.hlsl` genuinely compute a
   wave-size-dependent answer (they use a wave intrinsic directly) and stay
   at `W = 4` until a per-wave-size expected value is worth adding.
2. **The differential harness's own scope** (done for `--divergent`/
   `--loops` by R1; `--unstructured` remains `--reference`-only). Its header
   comment used to say its `--divergent=false --loops=false
   --unstructured=false` restriction existed because milestone 4's widener
   was acyclic/uniform-only, "at which point this harness's scope should
   grow with them". Milestones 6 and 7 landed; the harness did not grow
   until R1, which turns `--divergent`/`--loops` on (the configuration that
   exercises the linearizer and the widened-loop path against ground
   truth) and found the new P0 gap in §1.6's table when `--unstructured`
   was also tried against the normal pipeline (a divergent branch inside a
   loop that should have been diagnosed instead reaching the JIT
   unwidened). `--unstructured` stays `--reference`-only (still real
   coverage: it also caught, and this milestone fixed, a genuine
   `feme-cfg-gen` termination bug in its own irreducible-edge construct)
   until that gap closes.
3. **Front-end equivalence** (done by R10). Both of §1.2's P0 items now
   close: `test/Tools/feme-run/HLSL/front-end-equivalence.hlsl` runs a
   `RWStructuredBuffer<float>` compute shader through both the DXIL and
   SPIR-V front ends and `FileCheck`s the same expected numbers from each.
   The DXIL half is real HLSL compiled by Clang; the SPIR-V half is
   hand-written `spirv` dialect MLIR (see that test's own comment for why
   -- Clang's HLSL front end only reaches SPIR-V through LLVM's in-tree
   SPIRV backend, which this roadmap step's own build did not configure),
   so it is not literally *one* `.hlsl` source compiled twice, but the same
   shader's logic independently authored for each front end and checked
   for agreement, which is the property this item exists to give SPIR-V
   input the same confidence the DXIL side already has.
4. **JIT vs AOT** (done by R8). `AOTDispatchTest.cpp` establishes the pattern
   in `gtest` for one shader; `feme-run --object` now loads a shader
   compiled with `feme --target=<host>` -- a real object file -- with
   `orc::LLJIT::addObjectFile` and dispatches it directly, so `lit` covers
   the same AOT path (see test/Tools/feme-run/feme-run-object-aot.ll).
5. **Optimization level** (done by R14). Every end-to-end HLSL execution
   test JITs its shader through `feme::cpu::JITEngine`'s `OptimizerPipeline`
   pass, which has always run at a hardcoded `CodeGenOptLevel::Default`
   (`-O2`-equivalent, see `JITEngine.cpp`'s `toOptimizationLevel`) with no
   way for a test to pick a different level, so nothing ever checked that a
   shader's answer stays the same across levels. `feme-run` gains a `-O`
   flag (matching `llc`'s own spelling, `cl::Prefix` and all) that feeds
   `JITOptions::OptLevel` -- a field that already existed for this purpose
   but was never wired to the CLI; `test/Tools/feme-run/HLSL/opt-level-
   differential.hlsl` runs the same shader (a small unrolled per-lane
   multiply-add loop, with real reassociation/vectorization opportunities
   for the optimizer to find) at `-O0` through `-O3` and checks all four
   produce identical output. This is a different knob from `feme`'s own
   `-O0`/.../`-Od` (`feme::FrontendOptions`, which select the level the
   *front-end* pipeline runs its optimizer at, before any CPU-specific
   lowering) -- the two are independent, and this item is specifically
   about the JIT path's own post-CPU-lowering optimization pass.
6. **Round trips** (DXIL→DXIL→run done by R14; SPIR-V→SPIR-V→run remains
   blocked, see below). `feme-dxil-to-dxil.ll` checks a DXIL→DXIL retarget
   produces a container, but no test re-imports the produced artifact and
   executes it. `test/Tools/feme-run/HLSL/dxil-roundtrip-execute.hlsl`
   closes this: real HLSL compiled to a DXContainer, retargeted back to
   `dxil` through the full `feme` CLI (a second pass through
   `feme::DXILImporter`/the raising passes/`feme::DXILExporter`), and the
   *result* of that round trip executed and `FileCheck`ed through
   `feme-run` -- a much stronger statement than one that only parses the
   output. The equivalent SPIR-V→SPIR-V→run test turns out not to be
   possible yet: `feme --target=spirv` on a SPIR-V-derived module retargets
   through `feme::SPIRVExporter`, which (unlike the hand-authored `spirv`
   dialect fixtures every existing SPIR-V test in this tree assembles with
   `feme-translate --serialize-spirv`) emits its binary through LLVM's own
   in-tree SPIR-V code generator (`feme::TargetMachineBackend`) -- and
   `feme::SPIRVImporter` (a thin wrapper around MLIR's `spirv::deserialize`,
   see Design.md's SPIR-V section) cannot parse that generator's own
   output back in (`error: unhandled opcode 83` -- `OpAccessChain` --
   deserializing a module that `feme-run` executes just fine *before* that
   round trip). This is a genuine incompatibility between two independently
   evolving upstream SPIR-V producers/consumers (LLVM's SPIR-V backend and
   MLIR's SPIR-V deserializer), not something this roadmap step's own scope
   (testing, not new import breadth) can or should fix; see the "Known gap"
   this adds to Design.md's SPIR-V section for the concrete repro.
7. **Resource shapes** (typed buffers done by R8). Every executing test
   used to use an unstructured raw buffer, because that was all
   `feme-run`'s heap YAML could describe (§1.6); it now also accepts
   `kind`/`format`/`stride`, and `typed-buffer.hlsl` exercises a real
   `RWBuffer<float4>`. Structured-buffer strides and constant buffers
   remain untested by execution (the heap YAML can describe them, but no
   test yet does).
8. **DXBC.** CLI-level (`feme`/`feme-translate`) end-to-end coverage now
   exists, per §1.4's R7 entry (`test/Tools/feme/feme-dxbc-to-dxil.dxasm`).
   Execution (a DXBC-derived module reaching `feme-run`/the CPU target) is
   still uncovered -- `feme-run` only accepts `.ll`/`.bc`/DXIL, per §1.2.

### 2.3 The interesting cases to add

Concretely, in rough value order, all under `test/Tools/feme-run/HLSL/`
unless noted:

- **`reduction.hlsl`** (done by R5) — a barrier inside a loop, folding
  per-lane contributions with `WaveActiveSum` and publishing the result
  through groupshared. Directly exercises §1.6's "barrier inside a loop is
  diagnosed" narrowing (now closed for this uniform-loop shape); an honest
  groupshared *tree* reduction (indexing `groupshared` by
  `SV_GroupThreadID`) was blocked on §1.6's separate "Divergent
  groupshared access is diagnosed" row, which R5 found is one case broader
  than recorded (a *masked* store at a uniform address, not just a
  divergent index) -- both are now closed by R23.
- **`prefix-sum.hlsl`** (done by R4) — `WavePrefixSum`/`WavePrefixCountBits`
  over a divergent mask; exercises §1.3's flag-selected `WavePrefixOp`
  family.
- **`histogram.hlsl`** — divergent atomics into a shared buffer. This is the
  scalarization fallback's only realistic workload, and the one that catches
  §1.6's unmasked-lane P0 (done: a single groupshared counter a divergent
  condition gates `InterlockedAdd` into, reading the atomic's own return
  value rather than a separate reload -- a genuine multi-bucket histogram,
  indexing a groupshared array by a divergent bucket, was blocked on
  §1.6's separate "Divergent groupshared access is diagnosed" row (closed
  by R23): Clang itself folds an `if`/`else` each doing the same op on a
  different constant address into a single `select`-of-pointer
  `atomicrmw`, the address-divergent shape that narrowing covered).
- **`ballot.hlsl`** (done by R3) — `WaveActiveBallot` + `countbits`; was
  gated on §1.3's aggregate-returning mechanism.
- **`nested-divergence.hlsl`** — divergent loop containing a divergent
  diamond containing an early return. The linearizer's hardest supported
  shape, currently only reached through generated CFGs.
- **`multi-group-barrier.hlsl`** (done by R5) — several groups, each
  publishing through two barrier-separated groupshared slots, asserting
  groups do not observe each other's memory.
- **`matrix-multiply.hlsl`** — tiled, groupshared-staged, loop-heavy; the
  closest thing to a real workload, and a natural first performance
  regression fixture for milestone 12/13.
- **`typed-buffer.hlsl`** (done by R8) — `RWBuffer<float4>` with a real
  format (`r32g32b32a32_float`), using §2.4's heap YAML work.
- **A DXBC pair** under `test/Tools/feme/`: a `dxbc-as`-assembled `.dxasm`
  fixture retargeted with `feme --target=dxil`, and the same fixture executed
  through `feme-run` once DXBC import reaches `Driver` (§1.4).

Each of these is written the way the existing HLSL tests are — `split-file`,
`clang -target dxil--shadermodel6.5-compute`, a `heap.yaml`, a `CHECK` line
of numbers — so the incremental cost per shader is small once the
prerequisites in §2.4 exist.

### 2.4 Test infrastructure prerequisites

These are the changes that make §2.3 cheap rather than repetitive, and each
is small enough to land on its own:

1. **A wave-size sweep substitution** (done by R1: `%feme-wave-size-sweep`,
   feme/utils/feme-wave-size-sweep.py). A lit substitution that runs one
   input at several wave sizes and `FileCheck`s each run's output, so a
   shader opts into the sweep by one substitution instead of four `RUN:`
   lines.
2. **`feme-run` SPIR-V input** (done by R10). `feme-run` now links
   `FeMeImportSPIRV`/`FeMeTranslateSPIRV` alongside `FeMeImportDXIL`, and
   `loadModule` sniffs a SPIR-V binary's own magic number the same way
   `feme::Driver::detectFormat` does, rather than re-deriving that logic
   (the DXIL/SPIR-V import + translation split is small enough that
   sharing `Driver`'s exact detection code was not worth the coupling; see
   `feme-run`'s own file comment). Unlocks §2.2's front-end-equivalence
   axis, once the CPU pipeline itself could also execute a SPIR-V-sourced
   module's resource access and builtin-variable idiom (see
   `feme::cpu::SPIRVResourceLoweringPass`/`SPIRVBuiltinFoldingPass` and
   the Deviation note roadmap step R10 adds to
   feme/docs/FeMeCPUDesign.md's Status section).
3. **Heap YAML `kind`/`format`/`stride`** (done by R8). FeMeCPUDesign.md
   already specified the richer schema; milestone 11 shipped the raw-buffer
   subset. `feme-run`'s `resource-heap`/`bindings` entries now accept all
   three, unlocking §2.3's typed-buffer cases and stopping tests from
   hand-encoding float bit patterns as integers.
4. **A `%feme-run-differential` harness helper** (done by R1:
   feme/utils/feme-run-differential.py). Takes a seed list, a `feme-cfg-gen`
   flag set, and a wave-size list, generating each seed once and diffing its
   `--reference` output against every requested wave size -- replacing the
   differential harness's previous five copy-pasted five-line blocks with
   one substitution per shape/wave-size combination, so §2.2's item 2 can
   grow the shape space without growing the file. It also refuses to trust
   a `feme-run` invocation that printed a diagnostic even if its exit code
   was zero (see §1.6's new divergent-branch-in-a-loop gap, found by this
   helper), so a shape is only accepted once the pipeline ran it with no
   caveats.
5. **An AOT lit recipe** (done by R8). `feme-run --object` loads a shader
   compiled with `feme --target=<host-triple>` -- a real object file -- with
   `orc::LLJIT::addObjectFile` and dispatches it directly through
   `feme::cpu::runDispatch`, so AOT is covered by `lit`, not only by
   `gtest` (test/Tools/feme-run/feme-run-object-aot.ll).
6. **`check-feme-fuzz` (done by R6).** Per §1.7: bounded (`-runs=N`,
   seed-corpus-only) runs of every fuzz target, wired as a dependency of
   nothing by default (`ninja check-feme-fuzz` runs it explicitly) but with
   every fuzz target added to `FEME_TEST_DEPENDS` so `ninja check-feme`
   still builds all of them.

### 2.5 Corpus discipline

Two rules the existing tests already follow, worth stating so the additions
keep following them:

- **No binary fixtures.** Every input is assembled at test time — `.hlsl`
  through `clang`, `.ll` through `llvm-as`/`llc`, `.yaml` through
  `yaml2obj`, `.dxasm` through `dxbc-as` (Design.md, "Avoiding binary test
  fixtures").
- **Every restructurization bug found anywhere reduces into a named shape
  file** under `test/Transforms/CPU/CFG/` (FeMeCPUDesign.md, "CFG
  restructurization test suite", layer 1).

### 2.6 Testing the graphics and API runtime track

§2.1–§2.5 are about the compute track's breadth. The new components need
their own layers, and three of them are infrastructure that must exist before
the first shader-facing milestone rather than alongside it.

#### 2.6.1 Infrastructure prerequisites

1. **An image resource class in `feme-run`'s heap YAML.** Today's schema
   accepts `resource-heap`/`bindings` entries with `kind`/`format`/`stride`
   (§2.4.3). An image entry additionally needs dimensionality, extent, mip and
   array ranges, format, and layout. Required by every G2 test and by the
   textual image fixtures G3 onward compare against.
2. **`feme-render`.** A new tool that renders a textual scene description to a
   textual image fixture through the graphics executor, the way `feme-run`
   dispatches a compute shader. It is specified in Design.md's "Testing
   Tools" and has a [CommandGuide page](CommandGuide/feme-render.md);
   only the implementation is left. Needed from G3; the alternative — growing
   `feme-run` a draw mode — mixes two very different argument models.
3. **Textual scene and image fixtures.** §2.5's "no binary fixtures" rule
   applies unchanged: scenes, textures and expected images are text, generated
   or compared at test time. This is what makes edge-rule failures reviewable
   in a diff. Both formats are specified in Design.md's "Textual scene and
   image fixtures": one image format serves as texture input, expected output
   and actual-output dump, compared exactly by default, and a scene is YAML
   extending `feme-run`'s heap schema with attachments, pipeline state and
   vertex streams spelled in FeMe's own enumerations rather than any API's.

Two more become prerequisites once the runtimes start:

4. **Lit clients under `test/Vulkan/`** running against the build-tree
   manifest via `VK_DRIVER_FILES`, plus a second-ICD coexistence
   configuration and an exported-dynamic-symbol check against the version
   script (§1.9).
5. **A differential reference for each API.** Vulkan compares against Mesa's
   lavapipe, Direct3D against Microsoft's WARP, in both cases only for the
   subset each milestone actually advertises. Both are optional, detected
   dependencies: a missing reference must skip, never fail.

#### 2.6.2 Layers

The graphics design's six layers map onto this tree as:

| Layer | Where | First needed |
|---|---|---|
| Core import convergence (same canonical signature/operations from DXIL and SPIR-V) | `test/Transforms/Graphics/` | G0 |
| CPU pass tests at wave sizes 4, 8 and one native width, checking mask, quad, barrier, output-bound and spill invariants | `test/Transforms/CPU/Graphics/` | G1 |
| Runtime helper tests exhausting image coordinates, formats, filtering, robustness and helper-lane side effects | `unittests/Graphics/` | G2 |
| Fixed-function unit tests with no shader frontend (clipping, edge rules, interpolation, depth/stencil, blend, tessellator, meshlet validation, BVH build/traversal, layout math) | `unittests/Graphics/`, `unittests/RayTracing/` | G3 |
| End-to-end executor tests rendering textual scenes | `test/Tools/feme-render/` | G3 |
| Frontend tests creating real Vulkan/Direct3D pipelines | `test/Vulkan/`, `test/Direct3D/` | V1, W1 |

#### 2.6.3 Metamorphic properties

These are the checks that catch the failures image comparison alone does not,
and each should land with the milestone that makes it meaningful rather than
as a late sweep:

- identical wave-size-independent output across wave sizes (extends §2.2.1's
  existing sweep to stages);
- identical deterministic output across worker counts and tile traversal
  orders — the graphics counterpart of §2.2's differential harness, and the
  first thing a thread-pooled dispatch (§1.6) needs;
- identical linked varyings after irrelevant signature elements are added;
- identical sampling through storage-compatible API format aliases;
- no resource or attachment change from helper-only quads;
- the same canonical behavior from equivalent DXIL and SPIR-V inputs — the
  stage-shaped version of §2.2.3's front-end equivalence test;
- identical primitive streams from equivalent conventional and mesh pipelines;
- identical hits between BVH traversal and brute-force intersection;
- identical scalar and packetized ray continuation results.

#### 2.6.4 Fuzzing and sanitizers

§1.7's rule — "a fuzzing harness lands alongside each importer" — generalizes
to "alongside each attacker-controlled parser". The new ones are descriptor
updates, pipeline-cache blobs, command-stream decoding, serialized root
signatures, artifact/`StageArtifactInfo` blobs, and acceleration-structure
builds. Each belongs in `check-feme-fuzz` (§2.4.6) with its own seed corpus
the day its parser lands.

Sanitizer configurations stop being §1.7's P1 nice-to-have: ASan/UBSan for
import and raster code, TSan for prepared-draw and tile scheduling (and for
queue/pipeline concurrency in both runtimes), forced allocation/JIT failure
injection, and stress configurations that cap tessellation, mesh queues,
continuation memory and ray recursion at small values so every
resource-exhaustion path is exercised.

## Part 3: Suggested sequencing

Each step is independently landable and independently testable; the
dependency column is the only ordering constraint.

§3.1's table is the compute and retargeting track (R1–R15, the original
sequencing). §3.2 adds the graphics core and CPU stage work (R16–R37), and
§3.3 places the two API runtimes' own milestones against it. R16 onward may
proceed in parallel with R15, which has no dependents.

### 3.1 Compute and retargeting track

| # | Step | Covers | Depends on |
|---|---|---|---|
| R1 | ~~Grow the differential harness to divergent/loop shapes; add the wave-size sweep~~ (done: `--unstructured` stays `--reference`-only, see §1.6's new gap) | §2.2.1, §2.2.2, §2.4.1, §2.4.4 | — |
| R2 | ~~Mask the scalarization fallback's per-lane execution; add `histogram.hlsl`; make `feme-cpu-simdize` reject every shape `feme-cpu-linearize` left an unwidened divergent branch in, including one inside a loop (§1.6's new gap, found by R1)~~ (done: the divergent-branch gap turned out to be `feme::cpu::runPipeline` not propagating a pass diagnostic, not `feme-cpu-simdize`'s own check -- see §1.6's table) | §1.6 P0, §2.3 | R1 (harness catches regressions) |
| R3 | ~~Multi-return-value raising mechanism (`IMul`/`UMul`/`UAddc`/`SplitDouble`/`WaveActiveBallot`) + `ballot.hlsl`~~ (done: `feme::dxil::OpRaisingPass::raiseAggregateCall` raises all five; `feme::cpu::WaveCallKind::Ballot`/`lowerBallot` lower `WaveActiveBallot` on the CPU target) | §1.3 P0 | — |
| R4 | ~~Flag-selected opcode families (`WaveActiveOp`/`WaveActiveBit`/`WavePrefixOp`/`QuadOp`/`Barrier`) + `prefix-sum.hlsl`~~ (done: `feme::dxil::OpRaisingPass` raises all four remaining families; `feme::cpu::WaveLoweringPass` lowers every one of them except `QuadOp`, which stays raised-only pending the quad/derivative lane mapping FeMeCPUDesign.md's "Non-Goals" defers) | §1.3 P0 | — |
| R5 | ~~Barriers inside a uniform loop; values live across barriers; `reduction.hlsl`, `multi-group-barrier.hlsl`~~ (done: `feme::cpu::matchLoopShape`/`buildWrapperForLoop` split a barrier inside a header-tested uniform loop by cloning its header/latch into the wrapper as an ordinary scalar loop; `feme::cpu::spillValuesLiveAcrossBarriers` spills any value live across a barrier into a per-wave context array; a barrier inside a *branch* remains diagnosed, and a divergent groupshared access -- including a masked store at a uniform address, found writing `reduction.hlsl` -- was a separate narrowing, closed by R23, see §1.6) | §1.6, §2.3 | R4 (`Barrier` raising) |
| R6 | ~~DXBC importer fuzzer; `check-feme-fuzz`~~ (done: `feme-dxbc-import-fuzzer` fuzzes `feme::dxsa::deserialize` directly and found/fixed a real `SrcOperandAttr` builder assertion on a malformed immediate operand; `check-feme-fuzz` runs all five fuzz targets, seed-corpus-only, and found/fixed a bit-rotted `dxbc-as-fuzzer` call site broken by an unrelated `wrapInContainer` signature change) | §1.4 P0, §1.7 P0 | — |
| R7 | ~~DXBC through `Driver`/`feme`/`feme-translate` — Design.md milestone 8 end to end~~ (done: `feme::DXBCImporter` + `feme::dxsa::DXSAToLLVMIRTranslator` wired into `feme::Driver`/`feme`/`feme-translate --import-dxbc`; found/fixed a latent `!dx.resources` UAV-metadata bug the new end-to-end path exposed, see §1.4) | §1.4 P0, §2.2.8 | — |
| R8 | ~~Heap YAML `kind`/`format`/`stride`; `typed-buffer.hlsl`; AOT lit recipe~~ (done: `feme-run`'s heap YAML `resource-heap`/`bindings` entries accept `kind`/`format`/`stride`, closing the "raw buffers only" narrowing §2.2's "Resource shapes" row and milestone 11's own deviation note flagged; `typed-buffer.hlsl` gives `femeCpuResourceLoadTypedV4F32`/`StoreTypedV4F32` real execution coverage; `feme-run --object` loads a real `feme --target=<host>`-compiled object file with `orc::LLJIT::addObjectFile` and dispatches it through the `feme::cpu::runDispatch` loop factored out of `JITEngine::dispatch` for this reuse, closing §2.2's "JIT vs AOT" row for `lit` -- it has no `ResourceInfo` to place a `bindings` entry's reserved prefix, so only `resource-heap` is supported in that mode) | §2.4.3, §2.4.5, §2.2.4 | — |
| R9 | ~~`spirv`→`llvm` dialect breadth (storage buffers, sampling, push constants)~~ (done: `StorageBuffer` blocks convert to `target("spirv.VulkanBuffer", ...)` handles with `llvm.spv.resource.getpointer`/GEP access chains; `PushConstant` variables convert to an ordinary global in address space 13, which LLVM's own `SPIRVPushConstantAccess` pass rewrites the rest of the way; basic `spirv.ImageSampleImplicitLod` sampling and `OpImageFetch` (reusing `spirv.ImageRead`'s lowering) convert -- sampling variants needing extra operands, `Uniform`-storage-class `cbuffer`/`ConstantBuffer<T>` blocks (a differently-shaped problem, see §1.2's updated note) and graphics stage inputs/outputs remain open) | §1.2 P0 | — |
| R10 | ~~`feme-run` SPIR-V input; one HLSL source executed through both front ends~~ (done: `feme-run` links `FeMeImportSPIRV`/`FeMeTranslateSPIRV`, sniffing SPIR-V's own magic number the same way `feme::Driver::detectFormat` does; `feme::cpu::SPIRVResourceLoweringPass` normalizes a bound `spirv.VulkanBuffer` storage-buffer handle directly into the same canonical `feme.cpu.resource.*` calls the DXIL `BoundResourceNormalizationPass`/`ResourceLoweringPass` pair produces (SPIR-V has no bindless heap to normalize into, so one pass suffices where DXIL needs two), and `feme::cpu::SPIRVBuiltinFoldingPass` folds the `insertelement`-chain-then-`extractelement` idiom SPIR-V's builtin-variable materialization always produces back into the single scalar lane read, matching DXIL's already-scalar `llvm.dx.thread.id` -- `test/Tools/feme-run/HLSL/front-end-equivalence.hlsl` runs a `RWStructuredBuffer<float>` shader through both front ends and checks the same expected numbers from each, though the SPIR-V half is hand-written `spirv` dialect MLIR rather than compiled by Clang from the same `.hlsl` file, since this build configures no LLVM SPIRV backend for Clang's own HLSL-to-SPIR-V path to use; image/sampler resources and per-field structured-buffer access remain unexecutable on the CPU target) | §1.2 P0, §2.2.3 | R9 |
| R11 | ~~Thread-safety test; route library diagnostics through `Context`; `FormatRegistry`; `Exporter` interface~~ (done: `unittests/Driver/ThreadSafetyTest.cpp` drives one shared, stateless `DXILImporter` plus the DXIL raising passes from N threads, each with its own `Context`, and checks every `Context`'s underlying `LLVMContext`/`MLIRContext` stays distinct while all are simultaneously alive; `feme::Diagnostic`/`Context::setDiagnosticHandler`/`diagnose` replace `feme::Driver::run`'s direct `errs()` write for its `--wave-size`-ignored warning, with `feme`/`feme-run` each installing their own stderr handler; `feme::FormatRegistry` maps format names to `Importer`/`Exporter` instances, populated lazily by `feme::Driver`'s constructor rather than `Context`'s own -- see the Deviation note this adds to the "`feme::Context`" section of feme/docs/Design.md -- and now backs `feme::detectFormat`; `feme::Exporter`/`ExportOptions` plus `feme::DXILExporter`/`feme::SPIRVExporter` close the "`Exporter` was never written" gap, each a thin wrapper resolving the same target triple `resolveTargetTriple` already computes and delegating to the existing `TargetMachineBackend`, registered into the `FormatRegistry` and used by `Driver` for a `--target` of `dxil`/`spirv` specifically) | §1.1 | R7 (a third format makes the registry pay) |
| R12 | ~~Root constants; `WaveReadLaneAt` with a varying lane; vector/aggregate decomposition~~ (done: `feme::cpu::RootConstantLoweringPass` lowers the one recognized register-bound constant buffer -- `(b0, space0)` by default, matching "Root constants" -- into bounds-checked loads from the CPU ABI's root-constant block, closing a real gap in `feme::dxil::OpRaisingPass` along the way (`raiseCBufferLoadLegacy` raises `dx.op.cbufferLoadLegacy` into `llvm.dx.resource.load.cbufferrow.4`, the standard 32-bit-per-component row shape, which nothing raised before); a shader that also performs bindless resource access has that root-constant access finished by `feme::cpu::ResourceLoweringPass` instead, reusing its own already-added `root_constants`/`root_constant_size` parameters rather than colliding by name with a second pair (see RootConstantLowering.h's file comment for the two-pass split, and `feme::cpu::checkSupportedRaisedOps`'s updated tolerance for either). `feme::cpu::WaveLowering.cpp`'s `lowerReadLane` now builds a genuine per-lane gather (an unrolled lane loop) instead of assuming a uniform index and extracting lane 0; `feme::cpu::WaveTTIImpl` keeps DXIL's `WaveReadLaneAt` classified uniform (HLSL's own language rule), but SPIR-V's broader shuffle-style read is left to the generic operand-divergence rule, since it has no such guarantee. `feme::cpu::SIMDizePass`'s vector/aggregate decomposition ("Vectors become components, not nested vectors") now also accepts a vector-typed resource *load* as a producer and a constant-index `extractelement` as a consumer of either producer shape, in addition to the existing constant-index-`insertelement`-chain/resource-store pair; a non-constant-index `extractelement`, `shufflevector`, `phi`/`select` of vector type, and every aggregate remain diagnosed) | §1.6 P1 | — |
| R13 | ~~SPIR-V → DXIL direction; `BinaryWriter`; NVPTX/AArch64~~ (done: `feme::dxil::SPIRVRaisingPass` raises the SPIR-V-derived `llvm.spv.*`/`target("spirv.")` conventions a `Translator` produces back into the `llvm.dx.*`/`target("dx.")` ones `feme::dxil::OpRaisingPass`'s own output already uses -- the mirror image of `feme::spirv::RaisedLoweringPass` -- covering the four thread/group index queries with a direct mapping and a bound `StorageBuffer` resource (`target("spirv.VulkanBuffer", ...)`, HLSL's `(RW)StructuredBuffer<T>`) accessed through a flat `getpointer` plus an ordinary load/store, raised into DXIL's `target("dx.RawBuffer", ...)` handle and `llvm.dx.resource.load.rawbuffer`/`store.rawbuffer`; a typed-buffer image resource stays unraised, since MLIR's `SPIRVToLLVM` conversion still has no patterns for image *access* ops (only types), so none reaches LLVM IR to raise. `feme::Driver` runs it whenever a SPIR-V-derived module retargets to DXIL, closing Design.md milestone 6 end to end (`test/Tools/feme/feme-spirv-to-dxil.mlir`); doing so exposed a real bug shared by `feme::Driver::resolveTargetTriple` and `feme::DXILExporter` -- both fell back to a stage-less `dxil-unknown-shadermodel6.5-library` triple for any non-DXIL-originated module, which LLVM's DirectX codegen rejects outright for a stage-specific op like `llvm.dx.thread.id` -- now fixed by recovering the real pipeline stage from the entry point's `hlsl.shader` attribute first, the same attribute a SPIR-V `Translator` already sets. `feme::dxsa::serialize` (`BinaryWriter.cpp`) is implemented too, closing the DXBC-export prerequisite: it reuses `feme::dxbc::lookupOpcode`/`getOpcodeInfo`/`encodeProgram` (the exact table/encoder `dxbc-as`'s own text assembler already builds) rather than re-deriving SM4/SM5's bit layouts, and covers every operation built from DXSAOpBase.td's five generic shapes (no-operand/unary/binary/ternary/multiply-add) plus `DXSA_MovConditionalOp`'s `movc`/`dmovc` family -- the arithmetic/logic/comparison/conversion core of the ISA; declarations, control flow, and resource/texture ops are not yet covered and diagnose cleanly instead of mis-encoding, matching this dialect's own "extend incrementally" precedent. NVPTX gets its own `feme::nvptx::RaisedLoweringPass`/`ResourceLoweringPass`, mapping the same raised conventions onto NVVM/PTX-kernel primitives instead of AMDGPU's -- narrower than AMDGPU's own coverage in one respect the design didn't anticipate: NVPTX has no native object-file (ELF) codegen, only PTX assembly text, and `feme::Backend`'s `BackendOptions::FileType` has no knob yet to request that instead of the hard-coded `ObjectFile` default, so (unlike AMDGPU) there is no end-to-end `feme --target=nvptx*` object-file test yet, only the two passes' own `feme-opt` coverage (`test/Transforms/NVPTX`). AArch64 needed no new code at all: `feme::Driver`'s triple resolution and `feme::cpu::runPipeline` were already triple-generic, so `test/Tools/feme/feme-dxil-to-aarch64.ll` simply exercises that against a genuine non-host CPU ISA instead of leaving it an untested claim) | §1.2, §1.4, §1.5 P1 | R9 |
| R14 | ~~`-O2` end-to-end differential; execute-after-round-trip tests~~ (done: `feme-run` gains a `-O` flag wired to `JITOptions::OptLevel`, and `test/Tools/feme-run/HLSL/opt-level-differential.hlsl` checks `-O0`-`-O3` all compute the same answer; `test/Tools/feme-run/HLSL/dxil-roundtrip-execute.hlsl` closes the DXIL half of §2.2.6 -- the SPIR-V half remains blocked on a genuine `feme::SPIRVImporter`/LLVM-SPIR-V-backend incompatibility this step found and documented, see §2.2.6's own updated entry and the "Known gap" it adds to Design.md) | §2.2.5, §2.2.6 | R8 |
| R15 | CPU milestones 12/13 (resource and general performance), C API | §1.5, §1.6 P2, Design.md milestone 10 | R1–R14 |

R1 comes first deliberately, and for the same reason FeMeCPUDesign.md put its
own restructurization suite before its linearizer: it is the step that makes
every subsequent step's failures visible as a diff instead of as a wrong
number in a `CHECK` line nobody wrote yet.

### 3.2 Graphics core and CPU stage track

Every step here lands in `feme` proper — core reflection, the CPU target, and
the two new executor libraries — and is testable through `feme-opt`,
`feme-run`, `feme-render` and `gtest` without any API runtime existing. The
"Milestone" column is the FeMeGraphicsDesign.md milestone the step belongs to;
a milestone is complete when its own completion test passes, which is normally
the last step carrying its name.

| # | Step | Milestone | Covers | Depends on |
|---|---|---|---|---|
| R16 | ~~`feme::ShaderStage` plus the `feme.shader.stage` entry-point attribute, derived at import from the source stage and diagnosed against the module triple's environment; `feme::cpu::PreparePass` selects by enumeration instead of `isComputeEntryPoint`'s string comparison, with `hlsl.shader` still accepted~~ (done: `feme/include/feme/Core/ShaderStage.h` defines the enumeration, its canonical spellings, its mapping to and from the shader-stage triple environments a raised module already carries, and the attribute accessors -- `getShaderStage` preferring `feme.shader.stage` but still falling back to `hlsl.shader`, which import keeps writing because LLVM's own DirectX/SPIRV backends read the stage from it. `feme::dxil::MetadataRaisingPass` records the attribute and diagnoses an entry point whose `ShaderKind` property disagrees with the module's shader model profile; the `spirv` -> `llvm` dialect conversion records it and, since it *derives* the triple from the first entry point's execution model rather than checking against an authored one, diagnoses a module mixing two stages instead. `feme::cpu::PreparePass` takes a `feme::ShaderStage`, exposed as `feme-opt -feme-cpu-stage=<stage>`, so an entry point of another stage is a non-candidate rather than an ambiguity. See the "Status" note added to FeMeGraphicsDesign.md's "Stage identity" for the three decisions the design left open) | G0 | §1.8.2 | — |
| R17 | ~~The signature reflection data model (element ID, direction, location, semantic, system value, component type, shape, interpolation, frequency, stream), its verifier, and its serialization round trip in `gtest`~~ (done: `feme/include/feme/Core/Signature.h` defines `feme::SignatureElement`/`feme::EntrySignature` and the enumerations for each field; `feme::verifySignature` checks unique element IDs, an in-range component shape, a supported bit width, a semantic index only alongside a semantic name, and patch direction/frequency agreement; `feme::serializeSignature`/`feme::parseSignature` round-trip it through a versioned byte layout, following `feme::cpu::ArtifactInfo`'s convention. `unittests/Core/SignatureTest.cpp` covers the verifier's acceptance and each rejection rule plus the serialization round trip, including truncated/trailing-byte/unknown-enumerator failures. Import wiring (DXIL rows into the model, SPIR-V variables into the model) is left to R18/R19; see the "Status" note added to FeMeGraphicsDesign.md's "Signature reflection") | G0 | §1.8.2 | R16 |
| R18 | ~~Preserve DXIL input/output/patch-constant/root-signature rows from `!dx.entryPoints` into that model before `feme::dxil::MetadataRaisingPass` erases the source metadata~~ (done: `feme::dxil::convertEntrySignature` (`feme/include/feme/Transforms/DXIL/SignatureImport.h`) converts an entry's `Signatures` tuple into a `feme::EntrySignature`, renumbering `ElementID` by combined position since DXIL numbers its input/output/patch-constant lists independently; `feme::dxil::MetadataRaisingPass` calls it and attaches the result as `!feme.signature` function metadata, and separately preserves an entry's `EntryRootSigTag` (12) root-signature bytes verbatim as `!feme.dxil.rootsignature` function metadata (`setRootSignature`/`getRootSignature`) -- parsing the root signature's contents is left to W2. `unittests/Transforms/DXIL/SignatureImportTest.cpp` covers the element conversion (system value, component type, interpolation-mode mapping, patch direction by stage) and both metadata round trips; `test/Transforms/DXIL/dxil-raise-metadata-signature.ll`/`dxil-raise-metadata-patch-constant.ll` cover the pass end to end. SPIR-V's `Input`/`Output` variables (R19) are unaffected; see the "Status" note added to FeMeGraphicsDesign.md's "Signature reflection") | G0 | §1.8.2 | R17 |
| R19 | ~~Convert SPIR-V non-builtin `Input`/`Output` variables and their `Location`/`Component`/`Index`/interpolation/per-primitive/per-patch decorations instead of failing to legalize them~~ (done: `feme::spirv::populateSPIRVToLLVMTargetTypeConversions` converts a non-builtin `Input` variable's pointer to its pointee type (collapsing its `spirv.mlir.addressof`+`spirv.Load` into one `llvm.load`, like a builtin's `llvm.spv.*` intrinsic result already does) and a non-builtin `Output` variable's to an ordinary pointer in address space 8; `StageIOGlobalVariablePattern`/`StageIOAddressOfPattern` (`feme/lib/Conversion/SPIRVToLLVM/SPIRVToLLVMPatterns.cpp`) convert the declaration itself to an `llvm.mlir.global` in address space 7/8, recording `Location`/`Component`/`Index`/`NoPerspective`/`Flat`/`Patch`/`Centroid`/`Sample`/`PerPrimitiveEXT` as a `feme.spirv.decorations` attribute that `feme::spirv::attachStageIODecorations` (`feme/lib/Conversion/SPIRVToLLVM/StageIODecorations.cpp`, called from `feme::SPIRVToLLVMTranslator::translate`) turns into real `!spirv.Decorations` metadata once a genuine `llvm::Module` exists -- the same shape LLVM's SPIRV backend reads `OpDecorate`s back from, verified through `llc`+`spirv-val`. `test/Conversion/SPIRVToLLVM/spirv-to-llvm-stage-io.mlir`/`test/Translate/SPIRV/spirv-to-llvmir-stage-io.mlir` and `unittests/Conversion/SPIRVToLLVM/SPIRVToLLVMTest.cpp` cover it; feeding these variables into the `feme::EntrySignature` model (R17) itself is left to R20, which is what actually consumes it -- see the "Signature reflection" Status note this adds to FeMeGraphicsDesign.md) | G0 | §1.8.2, §1.2 | R17 |
| R20 | ~~The `feme.stage.*` operation family for vertex/fragment (input load, output store, discard, demote, is_helper, derivatives, quad read, pull-model interpolation) plus `FeMeTransformsGraphics`' canonicalization and validation pass, rewriting DXIL `loadInput`/`storeOutput` and SPIR-V interface accesses into it. **Completes G0**~~ (done: `feme/include/feme/Core/StageOps.h` declares the `feme.stage.*` family as named calls, mangled per overload like DXIL's own `dx.op.*` convention; a new `FeMeTransformsGraphics` library (`feme/lib/Transforms/Graphics`) adds `feme::graphics::CanonicalizeStagePass` (`feme-graphics-canonicalize-stage`), which raises DXIL's `loadInput`(4)/`storeOutput`(5)/`IsHelperLane`(221)/the pull-model interpolation family (`EvalCentroid`/`EvalSampleIndex`/`EvalSnapped`) directly -- resolving each call's DXIL per-list signature ID through the entry's `!feme.signature` -- and renames the already-raised `llvm.dx.discard`/derivative/quad-read intrinsics into their `feme.stage.*` peers; on the SPIR-V side it rewrites a non-builtin `Input`/`Output` stage-IO global's load/store into `feme.stage.input.load`/`output.store`, building and attaching the `feme::EntrySignature` R19 deferred to this milestone, and renames the analogous `llvm.spv.*` intrinsics the same way. `feme::graphics::ValidateStagePass` (`feme-graphics-validate-stage`) diagnoses a non-constant/unknown/wrong-direction element or out-of-range row/component, and any stage op illegal for its entry's declared stage. Both are scoped to the vertex and fragment stages only, per the design's "only operations required by implemented stages are legal" -- the patch/stream-emission/mesh-output/ray families and SPIR-V `demote`/`is_helper` (no upstream `llvm.spv.*` intrinsic exists to raise from yet) are left to later milestones; see the "Status" note added to FeMeGraphicsDesign.md's "Canonical stage operations". `unittests/Transforms/Graphics/{CanonicalizeStage,ValidateStage}Test.cpp` and `test/Transforms/Graphics/*.ll` cover both passes) | G0 | §1.8.2, §1.4 | R18, R19 |
| R21 | ~~Factor `CompiledStage`/`PreparedDispatch`/`invokeGroup` out of `JITEngine`, with the wave loop and entry mask owned by `invokeGroup`; `JITEngine` becomes a convenience wrapper and `JITOptions::NumThreads` becomes real. Land it under the final `CompiledStage` name so V1/W1 never build against `CompiledKernel`~~ (done: `feme::cpu::CompiledStage` (`feme/include/feme/Target/CPU/CompiledStage.h`) now owns everything `JITEngine::create` used to -- module cloning, wave-size resolution, the CPU pipeline/reference lowering, linking `libFeMeRuntimeCPU`, JIT compilation, and entry-point resolution -- and exposes `invokeGroup(const PreparedDispatch &, GroupID, GroupShared) const`, the per-workgroup entry point both FeMeVulkanDesign.md's `CompiledKernel` sketch and FeMeGraphicsDesign.md's `CompiledStage` describe (see FeMeGraphicsDesign.md's "there is one type"); `feme::cpu::PreparedDispatch`/a free `invokeGroup` (`feme/include/feme/Target/CPU/ResourceHeap.h`) materialize a dispatch's physical heap once and build one group's `FemeDispatchArgs`, shared by both `CompiledStage::invokeGroup` and the AOT `runDispatch` path. `JITEngine` is now exactly the thin wrapper the design describes: it holds a `CompiledStage` and (when `JITOptions::NumThreads != 1`) an `llvm::DefaultThreadPool` sized by `llvm::hardware_concurrency(NumThreads)`, created once and owned for the engine's lifetime; `dispatch` schedules every group across it through a per-call `llvm::ThreadPoolTaskGroup` and joins, or runs sequentially on the calling thread with no pool at all when `NumThreads == 1`. Deviation: `invokeGroup` does *not* own a separate host-side wave loop as this row and FeMeVulkanDesign.md's sketch describe -- `feme::cpu::EntryWrapperPass` already builds that loop, with its own entry-mask computation, *inside* the compiled `feme_cpu_entry_<name>` wrapper, precisely so it can also split a barrier into separate wave loops and spill values live across it (milestone 9, predating this row); moving it to the host would mean either duplicating that machinery or re-deriving barrier correctness against a changed entry-point ABI, neither of which this row's actual gap (a dispatch's unit of work smaller than the whole thing) required solving. `invokeGroup` therefore calls the compiled entry point exactly once per group; see the Status notes this adds to FeMeVulkanDesign.md's "CPU Runtime API Changes" and FeMeGraphicsDesign.md's "Compiled stage API" for the full accounting, including `StageCompileOptions`/`getStage()`/the stage-specific `invoke*` methods being left to R27. `unittests/Target/CPU/CompiledStageTest.cpp` covers `invokeGroup` directly, including concurrently from several threads for independent `GroupID`s; a new `JITEngineTest` case covers `dispatch`'s real worker-pool path end to end; existing `JITEngineTest`/`AOTDispatchTest`/`ResourceHeapTest` coverage is unchanged and still passes) | G1 (shared with V1, W1) | §1.6, §1.8.1, §1.8.3 | — |
| R22 | ~~Populate `ArtifactInfo`'s `WaveSize`/`GroupSize`/`GroupSharedSize`/`GroupSharedAlign`, then generalize it into stage-tagged `StageArtifactInfo` with signatures and side-effect summaries; bump the artifact ABI version and round-trip JIT and AOT reflection through the same structure~~ (done: `feme::cpu::CompiledStage::getArtifactInfo` and `feme::Driver`'s CPU retargeting path both populate `WaveSize`/`GroupSize`/`GroupSharedSize`/`GroupSharedAlign` from the same already-resolved wave size/thread-group size and the new `feme::cpu::getGroupSharedRequirements` (feme/include/feme/Transforms/CPU/GroupSharedInfo.h); `ArtifactInfo` is then renamed `StageArtifactInfo` and gains a `feme::ShaderStage Stage` tag (default `Compute`, since every `CompiledStage` still is one), a serialized `feme::EntrySignature` `Signature` tail (empty until R27/R28 make compilation itself stage-aware), and side-effect-summary `Flags` bits (`FEME_CPU_ARTIFACT_USES_DISCARD`/`_DEMOTE`/`_HELPER`, computed by the new `feme::cpu::computeSideEffectFlags` scanning for `feme.stage.*` calls); `ArtifactAbiVersion` is bumped to 3. `unittests/Target/CPU/{ResourceInfo,CompiledStage}Test.cpp` cover the populated fields, the new struct fields' serialization round trip, and rejecting an unknown stage/truncated signature; `test/Tools/feme/feme-cpu-artifact-reflection.ll` covers the AOT path end to end through a real object file. Deviation: tessellation/mesh/ray layouts remain unaddressed, as they describe stages that do not exist yet (left to R34/R35/R37)) | G1 (shared with V1, W1) | §1.8.1, §1.8.3 | R17, R21 |
| R23 | ~~Divergent groupshared access in `feme::cpu` — a divergent index, an access through a `getelementptr`, and a masked store at a uniform address (§1.6's three recorded shapes)~~ (done: `feme::cpu::FunctionWidener::widenGroupSharedGEP` widens a divergent groupshared index into a real vector-of-pointers `getelementptr` (LLVM allows a scalar base with vector index operands) instead of the generic scalarization fallback's per-lane clone-and-reassemble via `insertelement`, which `feme::cpu::rewriteGroupSharedGlobals` could not see through; `widenGroupSharedLoad`/`widenGroupSharedStore` turn a raw (unmasked) divergent-address `load`/`store` into a real `llvm.masked.gather`/`.scatter`, masked only by the wave's own entry mask; `widenGroupSharedAtomicRMW` (replacing the unconditional `widenElementwise` call every `atomicrmw` used to get) and a matching fix to the existing `widenMaskedAtomicRMW` both reuse a *uniform* groupshared pointer -- most commonly a `getelementptr` at a compile-time-constant array index -- directly per lane instead of `getWidened`'s usual broadcast-then-extract, which (unlike a direct, unindexed global reference, a `Constant` `ConstantFolder` folds straight back to itself) would otherwise leave a real `insertelement`/`shufflevector` broadcast in place of the uniform address. `feme::cpu::rewriteGroupSharedGlobals` (GroupShared.cpp) is generalized to retarget every one of these shapes: a `getelementptr` may now be vector-typed; a gather/scatter call (`llvm.masked.gather`/`.scatter`, the only surviving shape once every `feme.cpu.masked.*` call has been widened away) is a recognized leaf alongside `load`/`store`/`atomicrmw`; and a new `matchPointerBroadcast` recognizes the same-value `<W x ptr>` broadcast a masked store at a *uniform* address (even a direct global reference, or a uniform `getelementptr`) still needs -- retargeting it by rebuilding a fresh splat of the flat, address-space-0 pointer and erasing the old, now-dead broadcast chain. A *nested* `getelementptr` (a groupshared array of arrays/structs, one level deeper than a single index) remains diagnosed, unchanged from before. `test/Transforms/CPU/simdize-groupshared-{divergent-index,atomic-array,masked-store-uniform,masked-store-gep,nested-gep-unsupported}.ll` cover the new shapes and the one still-diagnosed narrower case; existing `simdize-groupshared-{uniform,atomic-scalar}.ll`/`entry-wrapper-groupshared-{host,stack}.ll` coverage is unchanged and still passes) | prerequisite | §1.6, §1.8.1 | — |
| R24 | ~~Barrier inside a surviving *branch*, and a `phi` live across a group-sync barrier~~ (done: `feme::cpu::matchBranchShape` recognizes a uniform two-way branch -- guaranteed uniform, since a divergent branch a barrier could survive inside is already gone by `feme::cpu::LinearizePass` -- whose arms are each a linear chain reconverging at a merge block with no phi of its own; `feme::cpu::buildWrapperForBranch` clones the branch's own (pure, uniform-parameter-only) condition directly into the wrapper as an ordinary scalar `br`, run once for the whole group, and barrier-splits each arm exactly like a straight-line wave body (`feme::cpu::splitArmAtBarriers`), with the wrapper's own real control flow choosing which arm's wave loops run. `feme::cpu::spillValuesLiveAcrossBarriers` now also spills a `phi` live across a barrier exactly like any other value, placing its spill store after its own block's last phi rather than immediately after itself. Two narrower shapes remain diagnosed rather than mis-compiled: a branch merge block with a phi (a value one arm computes differently from the other, which would mean spilling across the wrapper's own scalar branch choice, not just across a barrier within one region) and a value live across a barrier *within* one arm (each arm is barrier-split independently, and `feme::cpu::EntryWrapperPass` allocates only one `barrier_spill` buffer per wrapper, which two independently split arms cannot safely share). `test/Transforms/CPU/entry-wrapper-barrier-{in-branch,live-phi-spill,in-branch-merge-phi-unsupported,in-branch-arm-spill-unsupported}.ll` cover the newly-supported shapes and the two still-diagnosed narrower cases; `EntryWrapperTest.{SplitsBarrierInsideUniformBranch,BranchMergePhiIsDiagnosed,SpillsPhiLiveAcrossGroupSyncBarrier}` cover the same at the IR-shape level) | prerequisite | §1.6, §1.8.1 | R23 |
| R25 | ~~Root-constant breadth: any register-bound constant buffer rather than only `(b0, space0)`, array and non-constant-row-index shapes, and the full advertised push-constant range~~ (done: `feme::cpu::RootConstantLoweringPass`'s `matchRootConstantHandle` now accepts any single, finite `(space, register)` `dx.CBuffer` binding rather than only `(b0, space0)` -- reported back via new `RootConstantAccess::Space`/`Register` fields, threaded into `!feme.cpu.resources`'/`StageArtifactInfo`'s own new `RootConstantSpace`/`RootConstantRegister` fields (`ArtifactAbiVersion` bumped to 4) so a host knows which binding a given `RootConstantSize` belongs to; two or more distinct bindings in the same function remain ambiguous (there is still only one root-constant block) and are left for `checkSupportedRaisedOps` to reject exactly as a single non-default binding was before this row. An array binding (`RangeSize > 1`) is accepted, keyed by the `handlefrombinding` call's own `Index` operand (constant or dynamic); a `cbufferrow.4` load's row index may likewise be constant or dynamic now (`RootConstantRowLoad::Row` generalized from a resolved `uint64_t` to a `Value*`, constant-folding back down to the exact code a compile-time row produced before whenever it happens to be one). `lowerRootConstantAccess` reports the binding's *full advertised* byte span (`ElementSize * RangeSize`, the declared `dx.CBuffer` handle type's own byte length times the array length) rather than only the rows a function's own loads happen to touch statically, which R12's approach could no longer even compute once a row or array index is dynamic. Deviation: fixing this exposed a latent ordering bug `matchRootConstantAccess`'s new `Value*` row/index made reachable for the first time -- `feme::cpu::lowerFunctionRootConstants` matched against the *original* function, then rebuilt it (`addRootConstantParams` moves the body to a new `Function`, RAUW'ing every argument), leaving any `Value*` captured from a stale `Argument` dangling; fixed by re-matching against the rebuilt function, mirroring what `feme::cpu::ResourceLoweringPass`'s own combined path already did. `--cpu-root-constants=bN,spaceM` remains unimplemented and is no longer needed for the reason it was proposed, since any single binding is now recognized automatically; an override only matters once more than one binding must be disambiguated, which is unrelated future work. `test/Transforms/CPU/root-constant-lowering.ll` covers a non-default binding, an array binding with a dynamic index, a dynamic row index, two-distinct-bindings rejection, and the original constant-row default-binding shape (re-verified byte-for-byte identical codegen); `UnsupportedOpsTest`/`ResourceLoweringTest`/`ResourceInfoTest` cover the same at the pass-interaction and metadata/ABI level) | prerequisite | §1.8.1 | — |
| R26 | ~~A SPIR-V descriptor-set binding-to-heap normalization matching DXIL's `BoundResourceNormalizationPass`, with arrayed bindings in contiguous heap ranges and dynamic buffer offsets~~ (done: `feme::cpu::SPIRVResourceLoweringPass`'s `collectHandles` now reads `llvm.spv.resource.handlefrombinding`'s own range-size and array-index operands instead of assuming an implicit range size of 1 -- the range size must still be a compile-time constant, exactly like the (set, binding) identity itself, but the array index may be dynamic, matching `feme::cpu::BoundResourceNormalizationPass`'s own DXIL treatment. `assignHeapBases` assigns each non-conflicting identity a contiguous *run* of `RangeSize` heap slots (not a single slot), and `lowerAccesses` range-checks and clamps the array index into that run with `computeClampedIndex`/`computeOverflowClampedIndex` -- copied from `BoundResourceNormalizationPass` rather than shared, matching this file's existing precedent of duplicating `addResourceEnvParams` for its own differently-shaped signature. An unbounded range (`RangeSize == 0`, SPIR-V's own spelling of an unbounded descriptor array) is left un-normalized, and two handles at the same (set, binding) identity disagreeing about the range size (not just the element stride) are now a conflicting declaration too, both matching the DXIL pass's own rejections. Deviation: the array index is deliberately *not* cached alongside the handle -- it is re-read from the handle's own operand at lowering time instead, avoiding the exact stale-`Argument`-pointer bug R25 fixed in `RootConstantLowering.cpp` for the same reason (`addResourceEnvParams` rebuilds the handle's function, RAUWing every argument and erasing the original, so a `Value*` captured beforehand from one of its arguments would dangle). A Vulkan *dynamic* storage/uniform buffer offset needs no shader-side model at all, and this row adds none: per "Memory and Buffers" in feme/docs/FeMeVulkanDesign.md, a dynamic offset is folded into `FemeDescriptor::Data` when a host materializes a dispatch's physical heap, exactly like every other buffer's binding offset -- the existing `BoundResourceRange`/`materializeResourceHeap` model (feme/include/feme/Target/CPU/ResourceInfo.h, ResourceHeap.h) already carries this without any Vulkan-specific reflection record, answering FeMeVulkanDesign.md's open questions 3 (a separate SPIR-V-specific pass, not a raised `SPV_EXT_descriptor_heap`) and 7 (yes, the existing metadata carries it). `test/Transforms/CPU/spirv-resource-lowering-array.ll` covers an arrayed, dynamically-indexed binding; `spirv-resource-lowering-unsupported.ll` gains an unbounded-array case and `spirv-resource-lowering-conflicting.ll` a range-size-only conflict; `unittests/Transforms/CPU/SPIRVResourceLoweringTest.cpp` covers the same at the pass level, mirroring `BoundResourceNormalizationTest.cpp`'s structure) | prerequisite (V2) | §1.2, §1.9 | — |
| R27 | ~~`StageCompileOptions` and a stage-aware `runPipeline` (compute-only overload retained), stage-aware `PreparePass` and pre-mutation graphics validation, and the split of the single active mask into live and side-effect masks through `LinearizePass`/`SIMDizePass` and the reference path~~ (done: `feme::cpu::StageCompileOptions` (`feme/include/feme/Target/CPU/Pipeline.h`) carries `Stage`/`EntryPoint`/`WaveSize`, and a new `runPipeline(llvm::Module &, const StageCompileOptions &)` overload selects the entry point by `feme::ShaderStage` instead of assuming compute, tagging its `PipelineResult` with the compiled `Stage`; the original `runPipeline(llvm::Module &, llvm::StringRef, unsigned)` is kept, unchanged in behavior, as the compute-only compatibility overload the design explicitly asks to retain, and every existing caller (`JITEngine`/`CompiledStage`, `Driver`) still goes through it. For a non-`Compute` stage, `runPipeline` also runs `feme::graphics::ValidateStagePass` against the incoming module before `feme::cpu::PreparePass` (already stage-selecting since R16) or any other pass mutates it -- the "pre-mutation graphics validation" gate -- diagnosing a `feme.stage.*` violation while the module is still in its as-authored shape. `feme::cpu::LinearizePass`'s `DiamondFlattener`/`LoopLinearizer` now thread a `MaskPair{Live, SideEffect}` instead of a single scalar mask: `feme.stage.discard(cond)` narrows both by `!cond`, `feme.stage.demote(cond)` narrows only `SideEffect`, and `feme.stage.is_helper()` lowers to `Live && !SideEffect`; a plain `load`/resource load uses `Live`, a `store`/`atomicrmw`/resource store uses `SideEffect` -- the two masks are identical for any function with neither call (every existing compute shader), so this is a strict extension verified by updating every existing Linearize/SIMDize lit test's CHECK lines to the mechanically-renamed IR shape with `check-feme` still 100% green. `feme::cpu::SIMDizePass` needed no code changes: it already widens whichever `i1` value governs a masked call generically. `--reference` mode gets its own counterpart in `feme::cpu::ReferenceLoweringPass`: one invocation at a time has no mask to narrow, so `discard` becomes a real conditional early return (splitting the block right after the call) and `demote`/`is_helper` read/write a per-invocation `helper` `alloca` instead. Deviations, recorded in FeMeGraphicsDesign.md's "CPU Lowering Pipeline"/"Preparation and validation"/"Shared middle-end phases" Status notes: the mask split and `--reference`'s early return only cover the divergent-diamond/divergent-loop-exit shapes `LinearizePass` already supported before this row (a stage-mask call inside an otherwise-uniform loop is diagnosed, not mis-widened); `--reference` mode does not yet suppress a `demote`d invocation's later side effects; `CompiledStage::create` is not yet stage-aware (still `JITOptions`-only, left to R28 alongside the vertex/fragment wrappers that would actually dispatch a non-compute artifact); and validation's remaining bullets (wave-size range, resource/image/sampler kinds, patch/mesh/ray layout limits, structured shared reflection) have no implementation to gate on yet. `unittests/Target/CPU/PipelineTest.cpp`, new `LinearizeTest`/`ReferenceLoweringTest` cases, and two new `test/Transforms/CPU/Linearize/*.ll`/one new `test/Transforms/CPU/reference-lowering-stage-mask-ops.ll` cover this row; `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after) | G1 | §1.8.3 | R20, R21 |
| R28 | ~~Vertex and fragment wrappers over in-memory synthetic stage layouts, `FemeStageLayout`/`FemeVertexArgs`/`FemeFragmentArgs`, and derivative/quad lowering at wave sizes 4 and 8~~ (done: `feme/include/feme/Target/CPU/RuntimeABI.h` now defines the stage-batch ABI -- `FemeShaderResources`, dense-by-`ElementID` `FemeStageLayout`, `FemeVertexInvocation`, `FemeFragmentInvocation`, `FemeFragmentResult`, `FemeVertexArgs`, `FemeFragmentArgs`, and `StageArgsAbiVersion`; `feme::cpu::PreparedVertexBatch`/`PreparedFragmentBatch` (`feme/include/feme/Target/CPU/ResourceHeap.h`) materialize those argument blocks with the same ownership/lifetime convention `PreparedDispatch` already used for compute. The CPU pipeline's final wrapper is now stage-selected: `runPipeline` keeps `EntryWrapperPass` for `Compute`, but `VertexWrapperPass` and `FragmentWrapperPass` lower `feme.stage.input.load`/`output.store` against those layouts and emit the same `feme_cpu_entry_<name>` ABI symbol for vertex/fragment stages. `feme::cpu::WaveLoweringPass` now lowers `feme.stage.derivative.{x,y}.{fine,coarse}` and `feme.stage.quad.read` at wave sizes 4 and 8 with the fixed `(0,0),(1,0),(0,1),(1,1)` quad mapping, diagnosing any other wave size that reaches one of those ops instead of mis-lowering it; pull-model interpolation (`InterpolateAt*`) remains diagnosed as unsupported, explicitly scoped out of this row. `feme::cpu::CompiledStage` is now stage-aware: a new `create(Context &, Module, const StageCompileOptions &)` overload, `getStage()`, `invokeVertices`, and `invokeFragments` JIT-compile and execute real vertex/fragment batches end to end, and `StageArtifactInfo` now reports their `Stage` plus serialized `EntrySignature` when one is attached. Decision point finding: the shared middle end did **not** need a boundary revision. The localized extensions actually required were confined to the existing shared phases themselves -- `WaveUniformity` now classifies stage IO/derivative/quad ops as per-lane, `LinearizePass` rewrites `feme.stage.output.store` to a masked internal helper and records fragment return masks, and `SIMDizePass`/`WaveBodyEnv` grow the second entry side-effect mask plus widening for the stage-op/internal-helper calls that R27 had left compute-only. `PreparePass`, `ResourceLoweringPass`, and the resource/root-constant lowering path needed no stage-specific changes. Coverage: new lit tests `wave-lowering-derivative-{quad,unsupported}.ll`, `vertex-wrapper-stage-io.ll`, and `fragment-wrapper-stage-io.ll`; new wrapper/unit tests and `CompiledStageTest` end-to-end vertex/fragment invocation cases all pass with `check-feme`) | G1 | §1.8.3 | R27, R22 |
| R29 | ~~The image and sampler descriptors, `FemeShaderResources` folded into `FemeDispatchArgs`, and `SamplerHeap` retyped. This is the deliberate ABI break: artifacts built before it stop loading~~ (done: `feme/include/feme/Target/CPU/RuntimeABI.h` now defines `FemeImageDescriptor` (base allocation/size, `ImageDimension`, extent, mip/array/plane/sample counts, format, a dense-by-mip-level `FemeImageSubresourceLayout` row/slice/sample-pitch table, and sampled/storage/depth flags) and `FemeSamplerDescriptor` (min/mag/mip filter, per-axis addressing modes, LOD bias/clamp, comparison function, border color, anisotropy, reduction mode); `FemeShaderResources` gained `ImageHeap`/`ImageHeapCount` and its `SamplerHeap` is now `const FemeSamplerDescriptor *`; `FemeDispatchArgs` no longer declares its own `ResourceHeap`/`SamplerHeap`/`RootConstants` fields, instead embedding a `FemeShaderResources Resources` member so compute, vertex, and fragment stages share exactly one resource-binding contract. `feme::cpu::EntryWrapperPass`/`ReferenceEntryWrapperPass` read the embedded block through a new `loadResourcesField` helper (`lib/Transforms/CPU/DispatchArgsLayout.h`), and `feme::cpu::PreparedDispatch`/`PreparedVertexBatch`/`PreparedFragmentBatch` (`feme/include/feme/Target/CPU/ResourceHeap.h`) grew matching image-heap/retyped-sampler-heap fields on their `*Resources` input structs. Canonical `feme.image.*`/`feme.sampler.*` operations, format conversion, and sampling/addressing math are scoped out to R30) | G2 | §1.8.4 | R22 |
| R30 | ~~`feme.image.*`/`feme.sampler.*` canonicalization from DXIL (including §1.3's texture/sampler handle-kind gap) and SPIR-V (including §1.2's sampling variants), the `runtime/CPU` sampling helpers (1D/2D addressing, mip layout, point/linear filtering, explicit and implicit LOD, addressing modes, comparison sampling), the initial format table with sRGB, and active-lane SIMD lowering. **Completes G2**, unblocking V5 and W3~~ (status: 2D sampling/loading landed end to end -- DXIL `Sample`/`SampleLevel`/`TextureLoad`/`GetDimensions.x`/`.xy` raising, SPIR-V `ImageSampleExplicitLod` conversion, `runtime/CPU`'s addressing/filtering/mip-selection/comparison-sampling/format-table helpers, and `feme::cpu::ResourceLoweringPass`'s consumption of both into `feme.cpu.image.*`, wired through `EntryWrapperPass`/`ReferenceEntryWrapperPass`/`VertexWrapperPass`/`FragmentWrapperPass` -- and, in a follow-up pass, **G2 is now complete for the 2D sampled-image path end to end**: `feme::cpu::SPIRVResourceLoweringPass` normalizes a bound 2D `spirv.Image`/`spirv.Sampler` pair into the image and sampler heaps and lowers `llvm.spv.resource.sample`/`samplelevel`/`OpImageFetch` into the same `feme.cpu.image.*` calls, `feme::cpu::BoundResourceRange` gained a `BoundResourceClass` (plus `ReservedImageHeapSize`/`ReservedSamplerHeapSize`, artifact ABI version 5) so a host can place a bound image/sampler descriptor at all, `feme::cpu::materializeImageHeap`/`materializeSamplerHeap` do that placement, and `feme::cpu::SIMDize`'s new `widenImageCall` scalarizes a *divergent* sample per lane. That unblocked V5 -- see its own status note. Still open, each documented in FeMeGraphicsDesign.md's "Canonical image operations"/"Texture layout and formats" status notes: 1D/3D/cube sampling, arrayed/multisampled/storage images, bias/gradient sampling, comparison sampling on DXIL (blocked upstream -- no numbered DXIL wire opcode exists in this LLVM tree to raise from), gather, and nonzero texel offsets) | G2 | §1.8.4, §1.2, §1.3 | R29 |
| R31 | ~~`FeMeGraphics` skeleton: normalized pipeline and prepared-draw descriptions, the `feme-render` tool (already specified in Design.md's "Testing Tools" and `docs/CommandGuide/feme-render.md`, along with its scene and image fixture formats -- only the implementation is left), and the heap YAML image resource class~~ (done: new `FeMeGraphics` library (`feme/include/feme/Graphics`, `feme/lib/Graphics`) defines `feme::graphics::GraphicsPipeline`/`PreparedDraw` as plain description types -- the former owns the compiled vertex/fragment `feme::cpu::CompiledStage`s plus primitive topology, raster/depth/blend state and attachment formats, the latter holds one draw's attachments, viewport/scissor, vertex buffers, resource heap and draw commands -- matching "Normalized pipeline" in FeMeGraphicsDesign.md, but implementing no clip/raster/interpolation logic (that is R32). The same library implements the textual image fixture (`feme::graphics::ImageFixture`) and scene (`feme::graphics::Scene`) formats "Textual scene and image fixtures" in Design.md specifies, shared by the new `feme-render` tool and `unittests/Graphics/` as that section requires; fixture format coverage matches what `runtime/CPU`'s image helpers already implement (`R8G8B8A8_*` and the `R32*_FLOAT/UINT/SINT` family) and grows mechanically on demand. `feme-render` (`feme/tools/feme-render`) implements the CLI docs/CommandGuide/feme-render.md already specified: it parses a scene, builds and clears every attachment, compiles `pipeline.vertex`/`pipeline.fragment` into a real `GraphicsPipeline` when a scene has one, and dumps attachments (`--dump`, `--expect`, `--tolerance`); a non-empty scene `draws` list is diagnosed as not implemented rather than silently misrendering, since R32 is what actually executes one. `feme-run`'s heap YAML gains an `images` list (`ImageEntry` in feme-run.cpp), building `feme::cpu::FemeImageDescriptor`s into the ABI's separate image heap alongside `resource-heap`/`bindings`, covering a single mip level and (for a non-array dimension) a single array layer; multisample dimensions are rejected, matching G4's later multisample milestone. Shader modules `feme-render` loads are plain already-raised LLVM IR only for now -- DXIL/SPIR-V import follows `feme-run`'s own precedent once a test needs it. `test/Tools/feme-run/heap-image*.ll`, `unittests/Graphics/{ImageFixture,Pipeline,PreparedDraw,Scene}Test.cpp`, and `test/Tools/feme-render/*.test` cover this row; `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after) | G3 | §2.6.1 | R28 |
| R32 | ~~Vertex/index fetch, triangle assembly, clipping, viewport transform, culling, tile binning, top-left coverage, interpolation, and both stages run through the executor: one color attachment, one viewport/scissor, no MSAA. **Completes G3**~~ (done: `feme::graphics::executeDraws` (`feme/include/feme/Graphics/Executor.h`, `feme/lib/Graphics/Executor.cpp`) implements the "Draw flow" FeMeGraphicsDesign.md specifies end to end for one `TriangleList`/`TriangleStrip` draw against one color attachment: vertex/index fetch decodes bound vertex-buffer attributes (the 32-bit float/int family and `R8G8B8A8_*`) matched to a vertex-stage input by `Location`; vertex-output/fragment-input varyings are linked by `Location` (Vulkan-style, since no `StageInterfaceMap` exists yet); triangles are clipped against all 6 homogeneous half-spaces (plus a `w > 0` guard) via Sutherland-Hodgman and fan-triangulated; viewport transform, front-face culling and top-left-rule coverage share one self-consistent directed-edge-function convention; primitives are binned into fixed-size tiles, each batching its own covered 2x2 quads (with helper lanes) into one `invokeFragments` call, with output merge performed in submission order (painter's algorithm, since depth testing is R33's); interpolation is perspective-correct/no-perspective/flat per `SignatureInterpolationMode`. `PreparedDraw` (R31) grew `VertexBufferBinding::Attributes` and `IndexBufferBinding`/`DrawCommand::Indexed` for index buffers; the scene YAML (`feme::graphics::Scene`) grew a matching `index-buffer` key and per-draw `indexed`/`first-index`/`vertex-offset` fields. `feme-render` now executes a scene's `draws` instead of diagnosing them as unimplemented, encoding `vertex-buffers`/`index-buffer` scene data into the executor's byte layouts and defaulting `viewport`/`scissor` to the sole color attachment's extent when the scene omits them. Deferred, each a documented scope note in Executor.cpp's own file comment: no post-transform vertex cache (every vertex re-runs, matching "the first implementation may perform all vertex work before tile work"); matrices/16-/64-bit stage elements; a non-`Float` varying is carried from the first vertex of the *rasterized* (possibly clipped) triangle rather than tracking the original mesh's provoking vertex through clipping; and `--workers`/`--tile-order`/`--reference` remain accepted-but-inert in `feme-render` (the executor is already a deterministic single-threaded scalar implementation, so every value of each produces identical output, satisfying but not yet exercising the metamorphic checks "Determinism and Reference Execution" describes -- true parallel tiling and a differential scalar-reference path are scheduling optimizations, not part of this milestone's correctness scope). `unittests/Graphics/ExecutorTest.cpp` covers full/partial coverage, indexed draws, back-face culling, unsupported-topology rejection, perspective-correct interpolation, and the top-left tie-break's gap/overlap-free adjacent-triangle property; `test/Tools/feme-render/draw-{triangle,vertex-buffer,indexed}.test` cover the CLI path. `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after) | G3 | §1.8.5 | R31, R30 |
| R33 | ~~Depth/stencil attachments with legal early/late scheduling, blending, write masks, logic ops, multiple render targets, multisample coverage and resolves, the format expansion the first advertised profile needs, and deterministic parallel tiled schedules~~ (done: `feme::graphics::executeDraws` implements every bullet against `feme::graphics::GraphicsPipeline`'s new `StencilState`/`BlendState`/`LogicOp`/`getColorBlends()` state and `PreparedDraw`'s new `DepthStencilAttachment`/`ResolveAttachments`. Depth (`D16_UNORM`/`D32_FLOAT`) and stencil (`S8_UINT`) were originally two separate attachments rather than one packed surface; roadmap C1 ("Mandatory formats") added real pack/unpack support for the combined `D24_UNORM_S8_UINT` format (`feme::graphics::packDepthClear`/`packStencilClear` and their `unpack*` inverses are independent read-modify-writes of the shared word's two halves), so a subpass may now bind either or both halves of that format to the same image; `D32_FLOAT_S8X24_UINT` remains declared in `cpu::ResourceFormat` but not yet wired, a mechanical, on-demand addition matching this codebase's established "declared, not yet wired" convention. Early-vs-late depth/stencil scheduling is chosen from the fragment stage's own already-existing reflection (`SignatureSystemValue::Depth`/`StencilRef` outputs, `FEME_CPU_ARTIFACT_USES_DISCARD`/`_DEMOTE`) -- no new reflection pass was needed, since the compute track's discard/demote lowering and the graphics signature model already carried everything "Early and late tests" in FeMeGraphicsDesign.md needs. Blending implements the full Vulkan/Direct3D-shared `BlendFactor`/`BlendOp` equation plus a per-channel write mask honored regardless of blend/logic-op state; logic ops are implemented for `R8G8B8A8_UNORM/_UINT/_SINT` only, matching both APIs' own restriction on which formats support one. Multiple render targets link each fragment `SV_TargetN` output to `Draw.Attachments[N]` with its own `BlendState`. Multisampling supports 1/2/4 samples at fixed, deterministic per-pixel sample offsets (FeMe's own convention, not copied from either API's standard pattern, per "Determinism and Reference Execution"'s fixed-sample-location requirement); coverage is tested per sample (`FemeFragmentInvocation::Coverage`'s long-documented "coverage mask" meaning, previously always 0/1) and depth/stencil tested/written per sample against its own stored value, but shading and the depth/stencil candidate value stay per-pixel -- a documented precision scope decision, not a gap in the coverage/resolve correctness a completion test observes. Depth/stencil resolve and 8+ sample counts are mechanical follow-ups. Deterministic parallel tile scheduling dispatches `processTile` across `WorkerCount` worker threads pulling from a shared atomic cursor; since tiles own disjoint attachment regions, output is bit-identical regardless of worker count or tile order, wiring up `feme-render`'s previously-inert `--workers` flag. `feme::graphics::Scene`/`feme-render` grow `depth-attachment`/`stencil-attachment` scene keys (documented in Design.md); MRT/blend/stencil/MSAA scene YAML wiring beyond the depth attachment is not yet added to `feme-render` itself -- a mechanical follow-up, since the executor library is what this row's completion test exercises directly. `unittests/Graphics/ExecutorTest.cpp` covers depth test/write/early-late scheduling, stencil test/ops, blending, write masks, logic ops, MRT, multisample coverage/resolve, sample-count rejection, and the worker-count determinism property; `unittests/Graphics/ImageFixtureTest.cpp` covers the new depth/stencil fixture formats and `unpackColor`; `test/Tools/feme-render/draw-depth.test` covers the depth attachment end to end. `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after) | G4 | §1.8.5, §2.6.3 | R32 |
| R34 | Geometry/hull/domain signatures and wrappers, patch storage, control-stage barriers, tessellator state and domain-coordinate generation, bounded geometry streams, stream output, adjacency, layered rendering (status: the host-side, standalone-tested core lands -- the signature/stage-op model (`SignatureSystemValue::TessFactorEdge`/`TessFactorInside`/`DomainLocation`/`OutputControlPointID`, `StageOpKind::StreamEmit`/`StreamCut`, patch input/output reusing the existing `InputLoad`/`OutputStore` ops), the fixed-function tessellator (`feme::graphics::tessellate`, new Tessellator.h, generating domain coordinates/connectivity for isoline/triangle/quad domains across every partitioning/output-primitive combination, including crack-free non-uniform per-edge tessellation for the triangle/quad domains -- each edge's own factor places that edge's boundary vertices, so two adjacent patches agreeing on a shared edge's factor produce identical vertices along it regardless of their other factors, bridged to a uniformly-subdivided interior core via a concentric-ring triangulation, `bridgeRingsByEdge`), bounded patch storage (`feme::graphics::PatchRecord`, new Patch.h -- control-stage barriers need no new code, since `feme::cpu`'s groupshared/barrier lowering is already stage-agnostic), the four adjacency `PrimitiveTopology` variants plus list- and strip-topology adjacency splitting (Pipeline.h's `splitListPrimitiveAdjacency`/`splitStripPrimitiveAdjacency`), a bounded per-invocation multi-stream geometry builder (`feme::graphics::GeometryStreamBuilder`, new GeometryStream.h) retaining strip boundaries/emission order for stream output and rasterization to share, plus `mergeGeometryStreamsInLaneOrder` (added after R34's initial landing to close its own "documented follow-up"): SIMD-lane stream-range reservation via a checked prefix sum, merging one per-lane builder into a combined one in deterministic lane order, rejecting a lane's (and every later lane's, for that stream) whole reservation rather than overflowing the combined builder's declared capacity, and forcing a strip boundary at every lane edge even when a lane's own trailing strip was left open; layered-rendering array-layer selection that discards rather than clamps an out-of-range index (`feme::graphics::resolveRenderTargetArrayLayer`, new LayeredRendering.h, plus `AttachmentView::ArrayLayers`); and, added after R34's initial landing to begin closing its largest deferred item, `feme::cpu::HullWrapperPass` (new HullWrapper.h/.cpp) plus `FemePatchArgs`/`PreparedPatchBatch`/`CompiledStage::invokePatch`: the control-point phase of a real hull entry point, compiled through the CPU lowering pipeline into an invokable batch, for the common per-control-point-independent shape (each control point reads only its own input control point's attributes, addressed by `StageLayoutSystemValue::OutputControlPointID`) -- see HullWrapper.cpp's file comment for why this phase alone needs none of `EntryWrapperPass`'s barrier-region-splitting machinery (the patch-constant function is a separate compiled entry receiving the *completed* `OutputPatch`, so the phase boundary itself is the synchronization point) and what two shapes remain diagnosed rather than silently mishandled (a control point indexing a sibling control point's input, and a group-sync barrier within the phase); and, added in a further follow-up session to close that same deferred list's first item, `feme::cpu::PatchConstantWrapperPass` (new PatchConstantWrapper.h/.cpp) plus `FemePatchConstantArgs`/`PatchConstantResources`/`PreparedPatchConstantBatch`/`CompiledStage::invokePatchConstant`: the hull shader's second phase, a single non-batched invocation per patch that may read any (not only its own) output control point of the completed `OutputPatch` and writes tessellation factors/patch constants to unbatched per-patch storage, still routed through the general SIMDize/WaveLowering machinery but invoked with only lane 0 active rather than a wave loop over some batch count. `feme::cpu::isPatchConstantPhase` (new HullPhase.h/.cpp, private to `lib/Transforms/CPU`) discriminates a hull-stage function's two phases -- Direct3D/Vulkan give the patch-constant function no stage of its own -- by checking for a `SignatureDirection::PatchOutput` element, which only the patch-constant phase ever writes; `HullWrapperPass` now skips a candidate this identifies as the patch-constant phase. And, added in a further follow-up session to close that deferred list's remaining, "smaller, more scoped" item, an `InputPatch` parameter on the patch-constant function (the original, pre-control-stage input control points, distinct from the completed `OutputPatch`): `FemePatchConstantArgs` grows a second, independent structure-of-arrays input block (`InputPatch`/`InputPatchLayout`/`InputPatchControlPointCount`), and `SignatureElement::FromInputPatch` on a `SignatureDirection::Input` element tells `PatchConstantWrapperPass`'s `lowerPatchConstantInputLoad` which of the two blocks a given `feme.stage.input.load` addresses. And, added in a further follow-up session to close that deferred list's own largest remaining item, `feme::cpu::DomainWrapperPass` (new DomainWrapper.h/.cpp) plus `FemeDomainInvocation`/`FemeDomainArgs`/`DomainResources`/`PreparedDomainBatch`/`CompiledStage::invokeDomain`: the domain (evaluation) stage, batched one independent invocation per tessellator-generated domain point exactly the way `VertexWrapperPass` batches vertices, with each `feme.stage.input.load` routed by the signature element it names to one of this stage's three input sources -- the completed patch's control points (`SignatureDirection::Input`, readable at any control-point index, since evaluating a patch means blending its control points), the per-patch tessellation factors/patch constants (`SignatureDirection::PatchInput`, addressed by row/component alone, the mirror image of the patch-constant phase's own unbatched output store), and `SV_DomainLocation` (`SignatureSystemValue::DomainLocation`/`StageLayoutSystemValue::DomainLocation`, read from the per-invocation `FemeDomainInvocation` record the way a vertex batch reads `SV_VertexID` from its own) -- writing ordinary per-vertex outputs, since a domain shader's result is a vertex; a dynamically indexed domain-location component (the record is a fixed-size ABI struct) and a group-sync barrier (domain invocations are independent) are diagnosed rather than silently mishandled. And, added in a further follow-up session to close that deferred list's last remaining "wrapper" item, `feme::cpu::GeometryWrapperPass` (new GeometryWrapper.h/.cpp) plus `FemeGeometryInvocation`/`FemeGeometryArgs`/`GeometryResources`/`PreparedGeometryBatch`/`CompiledStage::invokeGeometry`: one invocation per assembled input primitive, batched over `FemeGeometryArgs::PrimitiveCount` exactly the way `VertexWrapperPass` batches vertices, reading a structure-of-arrays input block addressed `primitive * VerticesPerPrimitive + vertexInPrimitive` (any vertex in the primitive, not just the invocation's own, unlike the hull control-point phase's own restriction -- an adjacency triangle's "opposite" vertices, for instance). `feme.stage.stream.emit`/`.cut` (`StageOpKind::StreamEmit`/`StreamCut`) turn ordinary per-invocation output-store scratch storage into the stage's real, bounded, variable-count result: `emit` snapshots that scratch storage into one record of three flat, host-owned arrays rather than calling back into a live `GeometryStreamBuilder` object from JIT-compiled code (no precedent in this codebase for that), and `feme::graphics::collectGeometryStreams` (new GeometryStreamCollection.h/.cpp, living in `feme::graphics` since `FeMeTargetCPU` does not depend on `FeMeGraphics`) replays those flat records back into one real `GeometryStreamBuilder` per primitive and merges them via `mergeGeometryStreamsInLaneOrder`, finally closing that function's own "driving it from a real widened invocation" deferral. This also closed a latent gap: `feme.stage.stream.emit`/`.cut` needed the same per-lane side-effect-mask threading `feme.stage.output.store` already had (`LinearizePass` now creates masked variants of them, and `FunctionWidener` widens those variants in SIMDize.cpp), since without it SIMDize left a uniform-operand `feme.stage.stream.emit`/`.cut` call completely unwidened, firing once per *wave* rather than once per active *lane*. Two shapes remain diagnosed rather than silently mishandled: more than one output stream (this milestone's `FemeGeometryArgs` only carries storage for stream 0), and a group-sync barrier (geometry invocations are independent, like the domain stage's). (Crack-free non-uniform per-edge tessellation, previously deferred here, was added after R34's initial landing -- see `bridgeRingsByEdge` in Tessellator.cpp.) And, added in a further follow-up session to begin closing the "wiring the compiled hull/domain/geometry stages into `executeDraws`/`feme-render`" item (the last of the two items the prior session's open-issue list carried), three pieces of host-side marshaling glue, each unit-tested standalone ahead of a real chained draw exercising them together: `feme::graphics::PatchRecord` (Patch.h) grows a second, independent structure-of-arrays block for a patch's original input control points (`writeInputControlPoint`/`readInputControlPoint`, alongside the existing output-control-point storage), which `PatchConstantWrapperPass`'s `InputPatch` parameter needs a per-patch home for; `feme::graphics::buildDomainInvocations` (new DomainInvocations.h/.cpp) converts a `feme::graphics::tessellate` result's `DomainPoint`s into a `feme::cpu::FemeDomainInvocation` array for `FemeDomainArgs::Invocations`; and `feme::graphics::buildGeometryInputs`/`buildGeometryInvocations` (new GeometryInputs.h/.cpp) gather an assembled primitive batch's vertex-stage output attributes into `FemeGeometryArgs::Inputs`'s primitive-major layout (given the same per-primitive vertex-index lists `splitListPrimitiveAdjacency`/`splitStripPrimitiveAdjacency` already produce) and build one `FemeGeometryInvocation` per primitive's `SV_PrimitiveID`. Still deferred, documented in HullWrapper.cpp's and GeometryWrapper.cpp's own comments and in this row's own history above: generalizing `EntryWrapperPass`'s barrier-region splitting to the control-point batch ABI for a hull shader that needs it, and actually chaining the four compiled stage invocations (hull control-point phase, patch-constant phase, domain, geometry) together per patch/primitive and wiring the result into `executeDraws`/`feme-render` -- this session's glue narrows that gap but does not close it: `feme::graphics::Executor` does not yet call `invokePatch`/`invokePatchConstant`/`invokeDomain`/`invokeGeometry` at all. `unittests/Graphics/{Tessellator,Patch,GeometryStream,GeometryStreamCollection,LayeredRendering,DomainInvocations,GeometryInputs}Test.cpp`, `unittests/Transforms/CPU/{HullWrapper,PatchConstantWrapper,DomainWrapper,GeometryWrapper}Test.cpp` (including `LowersInputPatchAndOutputPatchReadsSeparately` and `LowersAllThreeInputSourcesAndBuildsWrapper`), `unittests/Target/CPU/CompiledStageTest.cpp`'s `InvokePatch{,Constant}RunsStageAwarePath`, `InvokePatchConstantReadsInputPatchSeparatelyFromOutputPatch`, `InvokeDomainRunsStageAwarePath` and `InvokeGeometryRunsStageAwarePath` cases, and `PipelineTest.cpp`'s/`SignatureTest.cpp`'s/`StageOpsTest.cpp`'s new cases cover today's scope; `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after -- G5 is not yet complete, since no image-comparison completion test exists) And, added in a further follow-up session to begin closing the remaining "actually chaining the four compiled stage invocations ... together per patch/primitive" item for its tessellation stages, `feme::graphics::runPatchPipeline` (new PatchPipeline.h/.cpp): given one patch's input control points and the three compiled stages a real hull shader's two phases and a domain shader compile into, it runs `CompiledStage::invokePatch` (the control-point phase), `invokePatchConstant` (reading that completed `OutputPatch`), extracts `SignatureSystemValue::TessFactorEdge`/`TessFactorInside` from the patch-constant phase's own attached signature to feed `feme::graphics::tessellate`, and `invokeDomain` (one invocation per generated domain point, via `buildDomainInvocations`), returning the domain stage's per-vertex outputs alongside the tessellator's connectivity -- closing the hull/patch-constant/domain three of that item's four stages standalone, ahead of a real chained draw. Since the hull control-point phase, patch-constant phase, and domain stage are three independently compiled entry points each with its own `EntrySignature`/`ElementID` numbering, and a real cross-stage attribute linker (the generalization of the `Location`-based linking `feme::graphics::Executor` already does for the vertex/fragment stages) does not exist yet for these three, this function instead requires its caller to build one shared `FemeStageLayout` for "the completed patch's control points" (the hull phase's own output layout, the patch-constant phase's own input layout, and the domain stage's own input layout, all at once) and another for "the patch constants" (the patch-constant phase's own output layout and the domain stage's own patch-constant input layout) -- a documented, narrower stand-in for that missing linker. Still deferred: chaining the geometry stage on top of this function's result, generalizing `EntryWrapperPass`'s barrier-region splitting to the control-point batch ABI, a real cross-stage attribute linker, and wiring any of this into `executeDraws`/`feme-render` (`feme::graphics::Executor` still does not call `invokePatch`/`invokePatchConstant`/`invokeDomain`/`invokeGeometry` at all). `unittests/Graphics/PatchPipelineTest.cpp` covers today's scope; `ninja check-feme` (assertions-enabled, ccache build) passes in full before and after. And, added in four further follow-up sessions' worth of increments landed under roadmap H4 (which cannot advance its own Vulkan-API surface until this row's machinery is reachable from a draw), two of those deferred items -- "a real cross-stage attribute linker" and "wiring any of this into `executeDraws`/`feme-render`" -- are now done. (i) `feme::graphics::StageStorage` moved out of Executor.cpp into its own `Graphics/StageStorage.h`/`.cpp` unchanged, so the patch pipeline and the executor build per-stage attribute blocks from one signature-driven implementation rather than two. (ii) `feme::graphics::linkStageElements`/`copyLinkedElements` (new `Graphics/StageLink.h`/`.cpp`, `unittests/Graphics/StageLinkTest.cpp`) are that missing linker: given a producing `EntrySignature` and a consuming one they pair elements by `Location`/`Component` (or by `SignatureSystemValue` for system values) and copy scalars between the two stages' own independently numbered, tightly packed `StageStorage` blocks -- exactly the `Location`-based matching `Executor` already did for the vertex/fragment pair, but as a reusable pass over any two signatures. `feme::graphics::linkPatchPipeline` (PatchPipeline.h/.cpp) builds a `PatchPipelineLinkage` from the four signatures involved (vertex out to hull in, hull control-point out to patch-constant `InputPatch` in and to domain in, patch-constant out to domain patch-constant in), and `runPatchPipeline` now takes that linkage instead of requiring its caller to hand-build one shared `FemeStageLayout` per group of stages; the stand-in documented above is gone, and `PatchPipelineTest.cpp` was rewritten around the new API with deliberately disjoint per-stage `ElementID` numbering so that the linker, not coincidence, is what makes the chain work. (iii) A real bug in `feme::graphics::tessellate` fell out of writing the first end-to-end test: `bridgeRingsByEdge` indexed the two rings it stitches as `OuterEdge[I % Mo]`/`InnerEdge[J % Mi]`, so when one ring ran out first the strip wrapped back to *that edge's own first vertex* instead of advancing to the corner it shares with the next edge. Every edge's last bridging triangle therefore folded back across the strip, leaving a crack of exactly equal area (which is why the obvious total-area test does not catch it, and did not -- an area-based watertightness test was written first, passed against the buggy code, and was replaced); at inside factor 1 the fold degenerated into a bowtie and roughly a third of the triangle domain went uncovered. `TessellatorTest.cpp` gained a combinatorial `findNonManifoldEdge` helper (no directed edge may be used twice, and every interior directed edge must be paired by its reverse) plus four crack-free tests over the triangle and quad domains, all four of which fail if the old indexing is restored. (iv) `feme::graphics::Executor::executeDraws` finally calls the chain: `graphics::PrimitiveTopology::PatchList` is new, `graphics::GraphicsPipeline::setTessellationStages` attaches the hull control-point phase, the patch-constant phase, the domain stage and their `TessellationState`, and a draw on such a pipeline runs the vertex stage over its fetched control points exactly as before, then feeds each `VertexCount / InputControlPointCount` group of them through `runPatchPipeline` and concatenates every patch's domain output into one flat block via the new `StageStorage::appendStageInvocations` (a per-scalar copy, not a `memcpy`, because a stage block's component stride is a function of its own invocation count). Everything downstream of the vertex stage -- `SV_Position`, `SV_RenderTargetArrayIndex`, `SV_ViewportArrayIndex` and every fragment-stage varying -- now reads a `RasterSig`/`RasterOut` pair that is the domain stage's signature and output block on a tessellating pipeline and the vertex stage's otherwise, the per-instance index lists became absolute `AbsTriIndices`/`AbsLineIndices` (two instances of one patch compute their tessellation factors from their own control points and so need not emit the same vertex count, leaving no per-instance stride to bias by), and a new `RasterPrimitiveClass` chooses the point/line/triangle rasterization path from the tessellator's `TessOutputPrimitive` rather than from the topology, which on a patch-list pipeline says nothing about what reaches the rasterizer. `unittests/Graphics/ExecutorTest.cpp` covers it end to end with a five-stage vertex/hull/patch-constant/domain/fragment draw (`TessellatedPatchListCoversTheWholeViewport` requires tessellation factors 1 and 4 to fill the viewport identically, `TessellationFactorZeroCullsTheWholePatch` requires a culled patch to rasterize nothing, plus two negative cases). Still deferred, and now tracked as roadmap H4a/H4b rather than here: chaining the geometry stage on top of `runPatchPipeline`'s result, generalizing `EntryWrapperPass`'s barrier-region splitting to the control-point batch ABI, and the whole Vulkan-API surface -- `CanonicalizeStage.cpp` still refuses to reflect any entry point that is not `Vertex` or `Fragment`, so a SPIR-V `TessellationControl`/`TessellationEvaluation` module cannot be imported at all, and `GraphicsPipeline.cpp` still rejects the two stage bits. `ninja check-feme` passes in full at every one of the four increments -- G5 is not yet complete, since no image-comparison completion test exists) | G5 | §1.8.5 | R33, R24 |
| R35 | Amplification and mesh stages: import and canonicalization, bounded payload and mesh-output builders reusing the compute workgroup/barrier/wave lowering, checked dispatch queues, meshlet assembly and validation, feeding the shared clip/raster path | G6 | §1.8.5 | R33, R24 |
| R36 | `FeMeRayTracing`: canonical acceleration structures and deterministic builders, triangle/instance traversal with bounds validation, inline ray-query import from DXIL and SPIR-V, ray/payload/attribute/callable/record/continuation reflection, scalar traversal from compute and fragment shaders | G7 | §1.8.5 | R30 |
| R37 | Ray-tracing pipelines: all six stage kinds plus hit-group linkage, the continuation transform, entry/resume wrappers, bounded frame allocation, shader-record translation, recursion enforcement, and packetization only once scalar continuation execution is the differential reference | G8 | §1.8.5 | R36, R28 |

Three ordering notes that are not visible in the dependency column:

- **R21 and R22 come first for the runtimes, not for graphics.** They are the
  only steps Vulkan V1 and Direct3D W1 need from this table, so landing them
  early lets both runtime tracks start while R16–R20 are still in progress.
- **R29's ABI break is cheap exactly once.** It must land before any artifact
  format is depended upon outside this tree, which in practice means before
  V4's persistent pipeline cache and W5's validated cache.
- **R23/R24/R25 have no graphics dependency of their own** and close §1.6
  narrowings that are already wrong answers waiting to happen. They can land
  at any time and unblock the most milestones per unit of work.

### 3.3 API runtime tracks

These milestones live in FeMeVulkanDesign.md and FeMeWARPDesign.md; they are
reproduced here only with their dependencies on §3.2, since neither document
can see this tree's schedule. Both tracks may run concurrently with each other
and with §3.2 once their prerequisites land.

| # | Milestone | Depends on |
|---|---|---|
| V0 | ~~Loader-visible skeleton: optional Vulkan-Headers dependency, `vk.xml` entrypoint generator, hidden-visibility ICD with a version script and development manifest, instance/physical device/device/compute queue, truthful properties and limits, loader smoke and two-ICD coexistence tests~~ (done: `feme/utils/vk_gen_entrypoints.py` generates the entrypoint table directly from vk.xml; `lib/Vulkan/Objects.h`/`PhysicalDeviceInfo.cpp`/`EntryPoints.cpp` implement the object model, truthful Vulkan 1.0/1.1 core properties/limits/memory/queue-family data and the host-derived pinned subgroup size; `VulkanICD.cpp`/`libfeme_vulkan.map` export exactly the four loader-facing symbols; `feme-vulkan-loader-smoke` plus `test/Vulkan/{loader-smoke,two-icd-coexistence}.test` cover the real Khronos loader and Mesa lavapipe coexistence; see FeMeVulkanDesign.md's "V0" Status note for its two scope deviations -- advertising `apiVersion` 1.1 rather than 1.2, and deriving the host vector width without standing up a full `TargetMachine`) | — (new build-system work, §1.9) |
| V0.5 | ~~SPIR-V import that survives real shaders: a glslang/DXC/Clang corpus, the decision between fixing MLIR's structurized deserializer and translating the SPIR-V CFG to unstructured LLVM IR for `PreparePass` to restructure, a prototype of the chosen approach, and the importer fuzzer extended to it~~ (done: decided in favor of the unstructured path, implemented as `SPIRVImporter`'s unconditional default rather than a failure-triggered fallback -- `ImportOptions::SPIRVEnableControlFlowStructurization` now defaults to `false`, see FeMeVulkanDesign.md's "SPIR-V import prerequisites" Status note for the downstream `spirv.mlir.loop` -> `llvm` dialect conversion crash the prototype found, which is *why* it had to be the unconditional default rather than a retry. Validated against a DXC-compiled corpus, `feme/test/Tools/feme-run/SPIRV/{diamond,loop-merge-phi}.hlsl` (gated on a new `system-dxc` lit feature), plus the pre-existing `llc`/SPIRV-backend fixtures; `feme-spirv-import-fuzzer`'s seed corpus gained a matching unstructured, multi-block seed. glslang was unavailable in this pass's environment, so the corpus has no GLSL-sourced entry, and the `OpCopyObject` failure mode was not reproduced (left open, not confirmed fixed) -- both remain for whoever next extends the corpus) | — (may change V1's design; schedule before V1) |
| V1 | ~~Empty compute dispatch: memory, buffers, shader modules, pipeline layouts, command pools/buffers, group-size resolution, submit/fences/idle, direct, base and indirect dispatch~~ (done: `feme/lib/Vulkan/{Memory,Buffer,Pipeline,CommandBuffer,Sync,GroupSize}.{h,cpp}` implement the full bullet list; `SyncTest.SubmitDispatchAndWaitOnFence` (feme/unittests/Vulkan/SyncTest.cpp) is the milestone's own end-to-end scenario, submitting a real compiled empty compute dispatch to a queue and observing its fence signal. See FeMeVulkanDesign.md's "V1" Status note for its deviations: group-size resolution is a Vulkan-local raw-SPIR-V scan rather than a `ConvertSPIRVToLLVMPass` change (MLIR's deserializer drops a `BuiltIn` decoration on a spec-constant composite entirely), `vkQueueSubmit` runs synchronously rather than through a dedicated queue thread, and dispatch execution calls `CompiledStage::invokeGroup` directly rather than through `JITEngine::dispatch`, for direct control over `vkCmdDispatchBase`'s `GroupID` offset and `vkCmdDispatchIndirect`'s runtime-read group count) | V0, V0.5, R21, R22 |
| V2 | ~~Storage buffers and descriptors, descriptor pools/sets/updates and dynamic offsets, buffer copies and barriers, lavapipe differential~~ (done: `feme/lib/Vulkan/{Descriptor,Pipeline,CommandBuffer}.{h,cpp}` implement descriptor set layouts/pools/sets/updates (writes and copies), `VkPipelineLayout`'s ordered descriptor-set-layout list, `vkCmdBindDescriptorSets` with dynamic offsets, buffer copy/fill/update, and `vkCmdPipelineBarrier`; dispatch execution's `buildBoundResources` materializes a `FemeDescriptor` per bound (set, binding) -- exactly `feme::cpu::BoundResourceRange`'s `(Space, BaseRegister)`, so no translation table is needed -- folding a dynamic offset into the descriptor's `Data` pointer with no shader-side change. `StorageBufferDispatchTest` (feme/unittests/Vulkan/CommandBufferTest.cpp) is the milestone's own end-to-end scenario: bind a descriptor set over two storage buffers, dispatch a shader that reads one and writes the other, and observe the result. `feme-vulkan-storage-buffer-diff` plus `test/Vulkan/storage-buffer-lavapipe-diff.test` close the lavapipe-differential bullet, running the same compiled SPIR-V against FeMe's ICD and Mesa lavapipe's and diffing their output. `maxComputeSharedMemorySize` was also raised from the core-required minimum to 32768 now that R23's prerequisite is satisfied. See FeMeVulkanDesign.md's "V2" Status note for its deviations: per-descriptor-type pool-size accounting is not modeled (only `maxSets` is enforced), and update-after-bind/descriptor update templates remain unimplemented) | V1, R26, R23 |
| V3 | ~~Push constants onto FeMe root constants, uniform buffers, binary and timeline semaphores, secondary command buffers, events, query pools~~ (done: `feme::cpu::SPIRVPushConstantLoweringPass` (plus `feme::cpu::SPIRVResourceLoweringPass`'s combined-case handling) lowers a SPIR-V push-constant global access into the CPU ABI's root-constant block, the prerequisite R25 did not cover since Vulkan push constants have no DXIL register binding at all; doing so also fixed a previously-latent MLIR SPIRVToLLVM struct-conversion bug affecting any `Block`-decorated struct with explicit member offsets (every real push-constant/uniform block), documented in SPIRVToLLVMPatterns.cpp's `convertOffsetStructTypeIgnoringDecorations`. `feme::vulkan::PipelineLayout` records `VkPushConstantRange`s and `vkCreateComputePipelines` validates full byte-for-byte coverage against `maxPushConstantsSize`; `vkCmdPushConstants` writes into new command-buffer push-constant state snapshotted into `RootConstants` per dispatch (`PushConstantDispatchTest`, feme/unittests/Vulkan/CommandBufferTest.cpp, is the milestone's own end-to-end push-constant scenario). `feme::vulkan::Semaphore` (binary and timeline, `vkCreateSemaphore`/`vkWaitSemaphores`/`vkSignalSemaphore`/`vkGetSemaphoreCounterValue`) required moving the advertised core API version and `vk_gen_entrypoints.py`'s `CORE_FEATURES` to 1.2 for the host timeline functions' core (non-`KHR`) names. `feme::vulkan::Event`/`QueryPool` (the latter accepting only `VK_QUERY_TYPE_TIMESTAMP`, since occlusion/pipeline-statistics measure rasterization/shading work this compute-only device does not perform yet) and secondary command buffers (`executeCommandBuffer`'s per-command loop factored into `executeCommandsInto`, recursed into for `vkCmdExecuteCommands`) round out the command set. `entry-wrapper-barrier-multi-wave.ll`/`multi-wave-barrier-groupshared.ll` are the first test coverage of a group spanning more than one wave, both for the barrier-splitting structure and end to end. Uniform buffers are done end to end: the Vulkan object model (`VK_DESCRIPTOR_TYPE_UNIFORM_BUFFER`/`_DYNAMIC` sharing storage buffers' pool/set/dynamic-offset accounting, producing a read-only `FemeDescriptor`) and the SPIR-V `Uniform` storage-class shader-side lowering both landed -- `feme::spirv::convertUniformBlockType`/`UniformBufferAccessChainPattern` (SPIRVToLLVMPatterns.cpp) convert a uniform block to the same `spirv.VulkanBuffer` handle representation a storage buffer uses, over the block's own field struct directly rather than a runtime array, and `feme::cpu::SPIRVResourceLoweringPass` was generalized (`BufferKind::Storage`/`Uniform`) to resolve a field access to a compile-time struct-layout byte offset instead of `index * stride`; `UniformBufferDispatchTest` (feme/unittests/Vulkan/CommandBufferTest.cpp) is the milestone's own end-to-end uniform-buffer scenario. See FeMeVulkanDesign.md's "Descriptor Model" table and "V3" Status note) | V2, R25 |
| V4 | ~~Typed buffers, `VkFormat` mapping, texel buffers, broader subgroup/atomic/robustness coverage, persistent pipeline cache with a blob fuzzer, first CTS runs over the advertised subset~~ (done, scoped: `feme::vulkan::Format.{h,cpp}` maps every `ResourceFormat`; `VkBufferView` plus `VK_DESCRIPTOR_TYPE_{UNIFORM,STORAGE}_TEXEL_BUFFER` resolve to a `Kind::Typed` `FemeDescriptor`, with `feme::cpu::SPIRVResourceLoweringPass` normalizing the `Dim::Buffer` image handle into `createTypedLoad`/`createTypedStore` -- scoped to the formats (`R32G32B32A32_{SFLOAT,UINT,SINT}`, `R8G8B8A8_{UNORM,SNORM,UINT,SINT}`) the CPU runtime's typed-load/store helpers implement a conversion for (`femeCpuResourceLoadTypedV4F32`/`StoreTypedV4F32` and, added in a later V4 pass alongside `isSupportedTexelElementType`'s `<4 x i32>` acceptance, `femeCpuResourceLoadTypedV4I32`/`StoreTypedV4I32`; the `R8G8B8A8_{SNORM,UINT,SINT}` packed-byte conversions were added in a still-later V4 follow-up pass), enforced at `vkCreateBufferView` by `feme::vulkan::isTexelBufferFormatSupported` (previously unenforced: any `mapVkFormat`-recognized format was silently accepted rather than rejected); `robustBufferAccess` is now advertised (bounds checking was already unconditional); `feme::cpu::SIMDizePass` lowers `SubgroupSize`/`SubgroupLocalInvocationId`, previously raised with no CPU-target lowering at all; `feme::vulkan::PipelineCache` implements `vkCreate/Destroy/GetData/MergePipelineCaches` (previously entirely unimplemented) with a header/UUID/digest-validated blob and `feme-vulkan-pipeline-cache-fuzzer`; `feme/utils/filter_vulkan_cts_cases.py` plus a `system-vulkan-cts`-gated lit test give the CTS bullet its infrastructure. See FeMeVulkanDesign.md's "V4" Status note for what remains: no `spirv.Atomic*` op has any dialect-conversion pattern at all -- a gap this milestone's investigation surfaced, not a previously-tracked one -- so atomic buffer access is unraised entirely; a texel buffer's narrower-than-`<4 x T>` channel-count formats and the remaining 16-bit packed formats (`R16G16B16A16_*`, `R11G11B10_FLOAT`, `R10G10B10A2_*`) still have no runtime conversion; the pipeline-cache blob carries no relocatable object code (an in-process hit is a real skip, a cross-process one is not); and no actual CTS run happened -- `deqp-vk` was not available in this environment) | V3, R22 |
| V5 | ~~Images and sampling: image memory requirements, views, layout tracking, copies, storage and sampled images, samplers~~ (done, scoped: `feme::vulkan::Image`/`ImageView`/`Sampler` (lib/Vulkan/Image.{h,cpp}) implement `vkCreateImage`/`vkGetImageMemoryRequirements{,2}`/`vkBindImageMemory{,2}`/`vkCreateImageView`/`vkCreateSampler`, a packed mip-major subresource layout computed once at creation time into `feme::cpu::FemeImageSubresourceLayout` (R29's own ABI, landed ahead of this milestone), and per-subresource `VkImageLayout` tracking; `vkCmdCopyBufferToImage`/`vkCmdCopyImageToBuffer`/`vkCmdCopyImage` and a `vkCmdPipelineBarrier` image memory barrier's layout transition (lib/Vulkan/CommandBuffer.{h,cpp}) round out the copy/layout bullets; `VK_DESCRIPTOR_TYPE_SAMPLED_IMAGE`/`_STORAGE_IMAGE`/`_SAMPLER`/`_COMBINED_IMAGE_SAMPLER` (lib/Vulkan/Descriptor.{h,cpp}) let a descriptor set hold an image/sampler binding alongside its existing buffer ones. `unittests/Vulkan/ImageTest.cpp` and new `DescriptorTest`/`ProcAddrTest` coverage exercise all of it, plus a follow-up pass (`feme/test/Vulkan/image-loader-smoke.test`, `tools/feme-vulkan-image-loader-smoke`) that exercises the same create/copy path through the real Khronos loader rather than only `libfeme_vulkan` directly, and widened this milestone's own narrower deviations: multisample images are now accepted at the object-model level (up to 4 samples for a sampled/storage 2D image; `vkCmdCopyImage` copies every sample verbatim, but no shader/render-target path reads one yet), `vkCmdCopyImage` now requires only a matching texel size rather than an identical `VkFormat` (matching real Vulkan's own "compatible formats" rule -- no value conversion, on either side, either before or after this change), and `vkCreateSampler` explicitly rejects a chained `VkSamplerCustomBorderColorCreateInfoEXT`/`VkSamplerBorderColorComponentMappingCreateInfoEXT`, not only the `VkBorderColor` `..._CUSTOM_EXT` enumerators it already rejected. This milestone's one remaining deviation -- "a real dispatch still cannot *consume* an image or sampler" -- is now closed by R30's follow-up: `buildBoundResources` materializes `FemeImageDescriptor`/`FemeSamplerDescriptor` arrays from a set's image/sampler bindings, `compileComputePipeline` validates each bound range against a descriptor type of its own class instead of rejecting `UsesSamplerHeap` outright, and `SampledImageDispatchTest` plus `feme-vulkan-sampled-image-smoke`/`sampled-image-loader-smoke.test` exercise a real dispatch that samples a bound image, the latter through the real Khronos loader. See FeMeVulkanDesign.md's "V5" Status note for the narrower shader-side scope that remains, all of it inherited from R30) | V4, R30 |
| V6 | ~~Graphics queue and basic rendering: graphics stage compilation, `VkRenderPass` and dynamic rendering, graphics pipeline state, draws, and `VK_QUEUE_GRAPHICS_BIT`~~ (done, scoped: `feme::vulkan::GraphicsPipeline` (lib/Vulkan/GraphicsPipeline.{h,cpp}) compiles a SPIR-V vertex/fragment pair through the compute path's own import/translate flow plus `feme::graphics::CanonicalizeStagePass`, validates the cross-stage interface against the core reflection, and translates every `VkGraphicsPipelineCreateInfo` state block into `feme::graphics`' normalized pipeline description with dynamic state resolved per draw; `feme::vulkan::RenderTargetBinding` (lib/Vulkan/RenderPass.{h,cpp}) is the one internal shape `VkRenderPass`/`VkFramebuffer` and `vkCmdBeginRenderingKHR` both normalize into; the command set grows by render pass instances, vertex/index binding, the implemented `vkCmdSet*` subset, direct and indirect draws with once-read, bounds-checked arguments, and `vkCmdClear{ColorImage,DepthStencilImage,Attachments}`/`vkCmdBlitImage`/`vkCmdResolveImage` (lib/Vulkan/ImageOps.{h,cpp}); `VK_QUEUE_GRAPHICS_BIT` joins the existing universal queue family and the framebuffer sample-count limits become real contracts. Two compiler-side prerequisites landed with it: a SPIR-V graphics builtin now keeps its `BuiltIn` decoration through the stage-IO conversion and maps onto `feme::SignatureSystemValue`, and a vector-typed interface access is decomposed per component for `SIMDizePass`. `unittests/Vulkan/{GraphicsPipeline,Draw,RenderPass,ImageOps}Test.cpp` (the latter now including depth, stencil, blend, MRT, and multisample-resolve draws) plus `feme-vulkan-graphics-smoke`/`test/Vulkan/graphics-loader-smoke.test` (the same scene through the real Khronos loader) cover it, and a follow-up pass closed the lavapipe half of this milestone's own CTS/differential bullet: `test/Vulkan/graphics-lavapipe-diff.test` runs seven scenarios -- a `VkRenderPass`, dynamic rendering, depth, stencil, blending, MRT, and a multisample resolve -- against both FeMe and Mesa lavapipe and diffs the results byte-for-byte. See FeMeVulkanDesign.md's "V6" Status note for the deviations and what a follow-up pass closed -- every unsupported state combination fails at creation rather than at draw time, dynamic rendering is exposed as `VK_KHR_dynamic_rendering` since the advertised version is 1.2, no `deqp-vk` CTS run happened since it was unavailable in this environment, and the lavapipe differential covers seven scenarios rather than "every format and state combination the driver reports"; per-instance vertex input rate, primitive restart on indexed triangle strips, and a graphics pipeline-cache entry (`translateFixedFunctionState` now runs every fixed-function translation before either stage compiles, so its result plus both stages' SPIR-V and the pipeline layout can be hashed into a key checked *before* paying for compilation) were all closed in follow-up passes, and `vkCmdBlitImage` now converts between differing formats and mirrors a region along either axis) | V5, R32, R33 |
| V7 | Tessellation, geometry, and graphics completeness: the optional stages, queries over real draws, layered rendering, multi-viewport, the advertised format matrix, and closing V6's own tracked deviation — `subgroupSupportedStages`/`subgroupSupportedOperations` growing to cover vertex and fragment once their `feme::cpu::WaveLoweringPass` coverage is CTS-measured, per FeMeVulkanDesign.md's "Graphics queue family" deviation note | V6, R34 |
| V8 | Mesh shading, ray tracing, and presentation: `VK_EXT_mesh_shader`, the ray-tracing extension set with validated build inputs and shader binding tables, and WSI starting with `VK_EXT_headless_surface` | V7, R35–R37 |

| # | Milestone | Depends on |
|---|---|---|
| W0 | Integration feasibility prototype: SDK/WDK/DDI/OS version selection, proof that a software adapter can be installed and enumerated through DXGI or selection of the application-local compatibility-runtime boundary instead, and the signing/INF/CI/debugging story. Throwaway-capable, and gates every other W step | — |
| W1 | Headless compute device: objects, DXIL import through the existing path, a resource-free dispatch, multi-wave barrier correctness, device-removal propagation | W0, R21, R22 |
| W2 | Root signatures of both versions, descriptor heaps and tables, root constants and CBVs, raw/structured/typed buffers with UAV writes and atomics, dispatch/copy/barrier/query, WARP differential | W1, R25, R23 |
| W3 | Textures and sampling: image/sampler descriptor ABI, committed and placed layouts, copy footprints, views, the initial format matrix, UAV texture access | W2, R30 |
| W4 | Basic graphics: input assembly through pixel shading with derivatives, interpolation modes, helper lanes, one render target, depth, minimal blend | W3, R32 |
| W5 | Graphics completeness: depth/stencil, blending, MSAA, formats, MRT, queries, predication, stream output, indirect draw, tessellation and geometry, pipeline libraries and a validated cache, Windows conformance and HLK | W4, R33, R34 |
| W6 | Interoperability: DXGI presentation or cross-adapter prototype, shared resources and fences, D3D11-on-12 evaluation, mesh-shader and ray-tracing evaluation | W5, R35–R37 |

The Vulkan conformance steps C1–C10 (§1.9.1) are not a milestone row in
this table. They cut across V4–V7 rather than following them, they are
ordered by measured CTS cost rather than by feature dependency, and C1/C4a
in particular should be done *before* the next V milestone, since they are
what makes the next CTS run's output readable.

The same is true of every later conformance series, and increasingly so:
§1.9.4's E-rows and §1.9.5's F-rows cut across V4–V6, §1.9.7's H-rows cut
across V6–V8 (H2/H3 are V7 work, H6/H10 are V8 work, H1/H7/H8 belong to no
milestone at all), and §1.9.8's J-rows are V8's ray-tracing content
re-expressed as an ordered dependency chain. Read the milestone tables for
*what a coherent device looks like at each step*, and §1.9's series for
*what to do next*; where they disagree about ordering, the series wins,
because it is the one derived from measurement. V9 (conformance
submission) and W7 (its Direct3D counterpart) are the two milestones that
only exist because of the conformance target, and neither has a §3.2
dependency of its own beyond "everything else".

The capability rule from FeMeGraphicsDesign.md applies to both tables and is
the one scheduling constraint that cannot be traded away: neither runtime may
advertise a graphics-capable queue, `VK_QUEUE_GRAPHICS_BIT`, or a
raster-implying feature level until the corresponding G milestone's completion
test passes for *every* format and state combination it reports.

### 3.4 Documentation debt

Three documentation items were prerequisites for the steps above rather than
follow-ups, and each was small. All three are now written; each entry records
what was decided, since the decisions are what the later steps build on:

- **FeMeVulkanDesign.md has no V6–V8** (done). The graphics design listed
  what they unblock but explicitly did not own their Vulkan-side content
  (graphics queue family, `VkRenderPass`/dynamic rendering, graphics pipeline
  state, WSI). FeMeVulkanDesign.md's "Graphics, Presentation, and
  Window-System Integration" section now specifies all four — graphics adds
  `VK_QUEUE_GRAPHICS_BIT` to the *existing* universal queue family rather than
  inventing a second one, `VkRenderPass` and dynamic rendering are both
  implemented and normalized into one internal render-target binding, pipeline
  state translates into `FeMeGraphics`' normalized pipeline description with
  dynamic state resolved per draw, and WSI starts with
  `VK_EXT_headless_surface` before exactly one CI-exercisable platform
  surface — and milestones V6 (G3/G4), V7 (G5) and V8 (G6–G8 plus WSI) are
  written against it. R32's work now has a Vulkan consumer to aim at.
- **Design.md's tool list and `docs/CommandGuide/` need `feme-render`**
  (R31), which is also where the textual scene and image fixture formats
  should be specified (done). Design.md's "Testing Tools" now carries the
  tool, `docs/CommandGuide/feme-render.md` documents it, and Design.md's
  "Textual scene and image fixtures" specifies both formats — placed under
  "Avoiding binary test fixtures" rather than in the command guide, since the
  graphics unit tests and both API runtime suites consume the same formats.
- **DXIL texture/sampler handle kinds needed a decision recorded in
  Design.md's DXIL section** before R30 implements them — §1.3 had flagged
  this as blocking since before the graphics design existed (done).
  Design.md's "Decision: texture and sampler handle kinds" records it, and
  corrects the premise: the bits §1.3 said `ResourceProperties` does not
  carry are carried, so the decision is to decode them into LLVM's existing
  `dx.Texture`/`dx.MSTexture`/`dx.FeedbackTexture`/`dx.Sampler` types, with
  the legacy `!dx.resources` path recovering the texel width from access
  sites and normalized (UNORM/SNORM/packed) element kinds deferred to G2's
  format table.

## Part 4: Explicitly not scheduled

- MLIR `gpu`-dialect retargeting (Design.md Non-Goals — no client).
- DXIL → DXBC and SPIR-V → DXBC (Translation Matrix: "not a priority").
- HLSL/GLSL source front ends (Design.md Non-Goals).
- Standalone out-of-tree builds against an installed LLVM+MLIR (Design.md
  Goals: out of scope for now, no redesign needed to add later).
- Work graphs (FeMeGraphicsDesign.md, G8's closing note): a persistent,
  dynamically composed node graph with its own backing-memory model is not
  equivalent to amplification fanout or bounded ray continuation queues, and
  needs its own design.
- A native Direct3D 11 DDI (FeMeWARPDesign.md W6): evaluate D3D11-on-12
  coverage first.
- Vulkan video, sparse residency, protected memory, device groups, external
  memory/synchronization, YCbCr sampling, transform feedback,
  `VK_EXT_shader_object`, and cooperative matrix/vector (FeMeVulkanDesign.md
  Initial Non-Goals). Each is optional for a Vulkan 1.4 submission, which is
  the test this list now applies: an optional capability stays here, and
  anything a *mandatory* CTS case is traced to leaves this list for §1.9.7
  instead. Ray tracing, mesh shading, tessellation, geometry and WSI were
  all on this list under the previous, compute-first scope and are not any
  more — see §1.9.7 and §1.9.8.
- Transparent substitution for Microsoft's WARP binary or
  `D3D_DRIVER_TYPE_WARP`/`EnumWarpAdapter` interception (FeMeWARPDesign.md
  Status): those are Windows-owned selection paths, and a separately
  installed, explicitly selected adapter is the first deployment target.
